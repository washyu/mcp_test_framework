# AUTOGENERATED by mcp-test-framework gen-sdet-classes -- DO NOT HAND-EDIT.
# Source: homelab_mcp (serverInfo.name="homelab-mcp", version="1.7.0")
# Regenerated: 2026-05-13T03:14:18+00:00
# Extend by subclassing in tests/sdet/, not by editing this file.
from __future__ import annotations

import typing

from pydantic import BaseModel, ConfigDict, Field

from mcp_test_framework.sdet.response import ToolResponse


class ScanInfrastructureDriftParams(BaseModel):
    """Params for the `scan_infrastructure_drift` tool.

    Generated from inputSchema. Pydantic validates required / type /
    default rules at construction time before the wire call.
    """
    model_config = ConfigDict(extra="forbid")

    node: str | None = Field(default=None, description="Optional sitemap hostname filter. Exact-match only — no wildcards, no case folding. When omitted, all sitemap rows are scanned. When set to a hostname that does not match any sitemap row, the scan returns status='success' with all four buckets empty and a guidance field — never an error.")
    # codegen: degraded -- enum not in v1.3 coverage
    vm_type: typing.Any = Field(default='all', description='Reserved for Phase 39 per-VM detection; currently filters at host level only (no-op until per-VM enumeration ships). Accepts qemu, lxc, or all — all three values produce identical scan results in this release.')


class ScanInfrastructureDriftResponse(ToolResponse):
    """Typed response for the `scan_infrastructure_drift` tool.

    Inherits .raw / .data / .text / .is_error from ToolResponse (CODEGEN-04).
    outputSchema was undeclared at codegen time; .data falls
    back to JSON-parse / text-dict per CODEGEN-04 chain.
    """
    pass
