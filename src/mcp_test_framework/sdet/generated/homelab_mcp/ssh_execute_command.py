# AUTOGENERATED by mcp-test-framework gen-sdet-classes -- DO NOT HAND-EDIT.
# Source: homelab_mcp (serverInfo.name="homelab-mcp", version="1.7.0")
# Regenerated: 2026-05-13T03:14:18+00:00
# Extend by subclassing in tests/sdet/, not by editing this file.
from __future__ import annotations

import typing

from pydantic import BaseModel, ConfigDict, Field

from mcp_test_framework.sdet.response import ToolResponse


class SshExecuteCommandParams(BaseModel):
    """Params for the `ssh_execute_command` tool.

    Generated from inputSchema. Pydantic validates required / type /
    default rules at construction time before the wire call.
    """
    model_config = ConfigDict(extra="forbid")

    command: str = Field(..., description='Command to execute on the remote system')
    hostname: str = Field(..., description='Hostname or IP address')
    password: str | None = Field(default=None, description='SSH password. Omit if credentials were stored with `credentials add` — they are auto-injected.')
    port: int = Field(default=22, description='SSH port (default: 22)')
    sudo: bool = Field(default=False, description='Execute command with sudo privileges')
    username: str | None = Field(default=None, description='SSH username. Omit if credentials were stored with `credentials add` — they are auto-injected.')


class SshExecuteCommandResponse(ToolResponse):
    """Typed response for the `ssh_execute_command` tool.

    Inherits .raw / .data / .text / .is_error from ToolResponse (CODEGEN-04).
    outputSchema was undeclared at codegen time; .data falls
    back to JSON-parse / text-dict per CODEGEN-04 chain.
    """
    pass
