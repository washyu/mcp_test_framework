# mcp_test_framework

Pytest framework for testing MCP (Model Context Protocol) servers end-to-end.

See `docs/mcp_test_framework_mvp_spec.md` for the authoritative design document
and `.planning/PROJECT.md` for the current scope and constraints.
