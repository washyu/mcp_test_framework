---
phase: 23-test-suite-debt-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every operator-facing `mcp-test-framework` invocation in README.md pairs with `--config config.yaml` (D-05, Phase 12 D-08 rule)."
    - "Zero banned-token regex matches remain in README.md under the pattern set `BASE_BANNED + [r'src/[\\w/.]+\\.py:\\d+']` (D-06)."
    - "Banned-token hits are scrubbed via semantic rewrite preserving prose meaning and citing surviving doc anchors — not regex-strip (D-05, Phase 12 D-10 rule)."
    - "test_doc_invocations_consistently_pair_with_config and test_readme_exists_and_no_banned_tokens both pass."
  artifacts:
    - path: "README.md"
      provides: "Operator-facing README with paired invocations and no banned tokens"
      contains: "mcp-test-framework run --config config.yaml --explain"
  key_links:
    - from: "README.md"
      to: "tests/framework/unit/test_doc_scrub.py"
      via: "Doc-scrub close-gate tests"
      pattern: "test_doc_invocations_consistently_pair_with_config|test_readme_exists_and_no_banned_tokens"
---

<objective>
Cluster C — fix two README defects flagged by `tests/framework/unit/test_doc_scrub.py`:

1. **D-05 (Phase 12 D-08 pairing rule):** Every operator-facing `mcp-test-framework` invocation in README must pair with `--config config.yaml`. README line ~104 has `$ mcp-test-framework run --explain` (bare). Fix shape: `$ mcp-test-framework run --config config.yaml --explain`. Any other unpaired invocations enumerated by running the test must also be fixed.

2. **D-06 (Phase 12 D-10 semantic-rewrite rule):** README has one or more hits against the banned-token regex set `BASE_BANNED + [r"src/[\w/.]+\.py:\d+"]`. Fix shape: semantic rewrite that preserves prose meaning and cites surviving doc anchors (`docs/ERROR-STYLE.md`, `docs/EXTENDING.md`, `docs/MIGRATION-v1-to-v2.md`, `examples/homelab-mcp.yaml`). DO NOT regex-strip.

The planner CANNOT pre-enumerate exact hits (depends on current README content). The executor must run the close-gate tests to get hit lists, then rewrite each.

Purpose: Clear the two README doc-drift reds so the close-gate plan (Plan 04) sees green.

Output: One patched `README.md`; two doc-scrub tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-test-suite-debt-cleanup/23-CONTEXT.md
@.planning/phases/23-test-suite-debt-cleanup/23-PATTERNS.md
@tests/framework/unit/test_doc_scrub.py
@README.md

<interfaces>
<!-- The banned-token regex set (single source of truth, from tests/framework/unit/test_doc_scrub.py:31-39, 49). -->

```python
BASE_BANNED = [
    r"\bPhase \d",
    r"\bPlan \d-\d",
    r"\bTOOLCFG-\d",
    r"\bISOL-\d",
    r"\bOUTPUT-\d",
    r"\bD-\d{2}",
    r"\b\d{6}-[a-z0-9]{3}",
]
# README rule:
patterns = BASE_BANNED + [r"src/[\w/.]+\.py:\d+"]
```

<!-- D-05 pairing exemptions (from tests/framework/unit/test_doc_scrub.py:158-221). -->
Invocations with `--config`, `--help`, or `--command` are exempt.
Output-line heuristic prefixes excluded as pytest output: `platform `, `rootdir:`, `configfile:`, `plugins:`, `asyncio:`, `collected `, `tests\\`, `tests/`, `=====`.

<!-- The specific D-05 fix target (README line ~104, from PATTERNS.md). -->
BEFORE: `$ mcp-test-framework run --explain`
AFTER:  `$ mcp-test-framework run --config config.yaml --explain`

<!-- D-10 semantic-rewrite example (from PATTERNS.md §S5). -->
BEFORE: `Per Phase 12 D-08, every invocation pairs with --config`
AFTER:  `Every operator-facing invocation in this README pairs with --config so the framework launches against a known config; see ERROR-STYLE.md for the launch-error surface.`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate D-05 unpaired-invocation hits via the close-gate test</name>
  <files>(no files modified — diagnostic only)</files>
  <read_first>
    - tests/framework/unit/test_doc_scrub.py:158-221 (the rule + exemption list)
    - README.md (skim once before running)
  </read_first>
  <action>
Run the doc-invocation pairing test and capture every offending line number:

```
uv run pytest tests/framework/unit/test_doc_scrub.py::test_doc_invocations_consistently_pair_with_config -v
```

The assertion message names every offending line in README.md (the test scans fenced code blocks). Record the line numbers and exact text of each unpaired invocation. Expected from CONTEXT.md/PATTERNS.md: README line ~104 (`$ mcp-test-framework run --explain`). There may be additional hits — confirm with the actual run.

DO NOT modify README.md in this task.
  </action>
  <verify>
    <automated>uv run pytest tests/framework/unit/test_doc_scrub.py::test_doc_invocations_consistently_pair_with_config -v 2>&1 | grep -i "readme\|line" | head -20</automated>
  </verify>
  <done>
- Inventory of every offending `README.md` line number and exact text captured in task SUMMARY.
- No source edits.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enumerate D-06 banned-token hits via the close-gate test</name>
  <files>(no files modified — diagnostic only)</files>
  <read_first>
    - tests/framework/unit/test_doc_scrub.py:31-49 (BASE_BANNED + README rule)
    - README.md
  </read_first>
  <action>
Run the banned-token test:

```
uv run pytest tests/framework/unit/test_doc_scrub.py::test_readme_exists_and_no_banned_tokens -v
```

The assertion lists every matching regex. For each matching regex, grep `README.md` to find the exact text and line numbers:

```
Grep "\bPhase \d" README.md -n
Grep "\bPlan \d-\d" README.md -n
Grep "\bTOOLCFG-\d" README.md -n
Grep "\bISOL-\d" README.md -n
Grep "\bOUTPUT-\d" README.md -n
Grep "\bD-\d{2}" README.md -n
Grep "\b\d{6}-[a-z0-9]{3}" README.md -n
Grep "src/[\w/.]+\.py:\d+" README.md -n
```

Record line number + surrounding sentence for each hit. Per CONTEXT.md, draft a semantic-rewrite proposal for each — preserve operator-meaningful intent and cite surviving doc anchors. Banned-token DO NOT regex-strip.

DO NOT modify README.md in this task.
  </action>
  <verify>
    <automated>uv run pytest tests/framework/unit/test_doc_scrub.py::test_readme_exists_and_no_banned_tokens -v 2>&1 | tail -30</automated>
  </verify>
  <done>
- Inventory captures every banned-token hit: regex matched, line number, surrounding sentence, proposed rewrite.
- No source edits.
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply D-05 --config pairing fixes</name>
  <files>README.md</files>
  <read_first>
    - Task 1 inventory
    - README.md (the affected line ranges)
    - .planning/phases/23-test-suite-debt-cleanup/23-PATTERNS.md (§"README.md (Cluster C: D-05 + D-06 rewrites)")
  </read_first>
  <action>
For each offending line from Task 1 inventory, change the bare invocation to include `--config config.yaml`. Confirmed target from CONTEXT/PATTERNS:

`README.md` line ~104:
BEFORE: `$ mcp-test-framework run --explain`
AFTER:  `$ mcp-test-framework run --config config.yaml --explain`

For any additional hits from Task 1, apply the same pattern: insert `--config config.yaml` immediately after the subcommand (`run`, `list-tools`, `config-init`) and before any other flags.

Preserve surrounding prose (per PATTERNS.md `README.md:98-114` context block); do not touch headings, downstream sample output (`========================================`, `MCP server:`, `Discovered:`, `Running:`, `Skipping:` lines), or unrelated invocations.

DO NOT:
- Add a `--help` or `--command` workaround to dodge the rule.
- Modify `tests/framework/unit/test_doc_scrub.py` — the test defines the contract.
- Edit anywhere outside `README.md`.
  </action>
  <verify>
    <automated>uv run pytest tests/framework/unit/test_doc_scrub.py::test_doc_invocations_consistently_pair_with_config -v</automated>
  </verify>
  <done>
- `grep -n "mcp-test-framework run --explain" README.md` returns no bare hits (only ones preceded by `--config config.yaml`).
- `grep -n "mcp-test-framework run --config config.yaml" README.md` returns at least one match including the line at ~104.
- `uv run pytest tests/framework/unit/test_doc_scrub.py::test_doc_invocations_consistently_pair_with_config -v` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 4: Apply D-06 banned-token semantic rewrites</name>
  <files>README.md</files>
  <read_first>
    - Task 2 inventory
    - README.md (affected line ranges)
    - .planning/phases/23-test-suite-debt-cleanup/23-PATTERNS.md (§Pattern S5)
  </read_first>
  <action>
For each banned-token hit from Task 2 inventory, apply a semantic rewrite per Phase 12 D-10 precedent (NOT regex-strip):

Procedure per hit:
1. Identify the offending planning-anchor (e.g., `Phase 12`, `D-08`, `Plan 21-02`, `src/foo.py:42`, `260512-dcs`).
2. Rewrite the sentence to preserve the operator-meaningful intent.
3. Cite a surviving doc anchor instead of the planning ID. Allowed anchors: `docs/ERROR-STYLE.md`, `docs/EXTENDING.md`, `docs/MIGRATION-v1-to-v2.md`, `docs/SDET-AUTHORING.md`, `examples/homelab-mcp.yaml`. Do NOT cite `.planning/` paths.

Example transformations (illustrative):
- `Per Phase 12 D-08, invocations pair with --config` → `Every operator-facing invocation in this README pairs with --config so the framework launches against a known config; see docs/ERROR-STYLE.md for the launch-error surface.`
- `See src/mcp_test_framework/cli.py:42 for the implementation` → `The CLI surface is documented in docs/EXTENDING.md.`
- `Fixed in Plan 21-02` → drop the planning anchor; rewrite the sentence to describe the operator-observable behavior instead.

After every rewrite, re-run:
```
uv run pytest tests/framework/unit/test_doc_scrub.py::test_readme_exists_and_no_banned_tokens -v
```

Iterate until clean. The test must pass with zero remaining hits.

DO NOT:
- Add a comment like `<!-- noqa: banned -->` or any allowlist — Phase 22 D-04 (cited in PATTERNS.md) forbids that posture.
- Strip the offending word without rewriting the sentence — D-10 says preserve prose meaning.
- Touch any file other than `README.md`.
  </action>
  <verify>
    <automated>uv run pytest tests/framework/unit/test_doc_scrub.py::test_readme_exists_and_no_banned_tokens tests/framework/unit/test_doc_scrub.py::test_doc_invocations_consistently_pair_with_config -v</automated>
  </verify>
  <done>
- Every banned-token hit from Task 2 has a semantic rewrite applied.
- `uv run pytest tests/framework/unit/test_doc_scrub.py -v` exits 0 (both close-gate tests green; other tests in the file are unaffected).
- `git diff README.md` shows only sentence-level rewrites in the offending sentences; no allowlist comments added.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → operator | Operator reads README to learn the CLI. Doc drift (bare invocations, planning-system anchors) misleads operators into wrong invocations or invokes terminology they have no map for. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03-01 | Information Disclosure | Planning-system anchors leak in operator-facing README | mitigate | D-06 banned-token regex enforces; this plan scrubs all current hits via semantic rewrite. Surviving doc anchors (docs/*) are operator-meaningful and allowed. |
| T-23-03-02 | Tampering | Operator follows bare invocation and silently runs against no config | mitigate | D-05 pairing rule enforces. Reinforces the `project_config_discovery_and_safety` memory — bare `mcp-test-framework run` is exactly the destructive-default scenario that motivated the rule. |
| T-23-03-03 | Repudiation | Semantic rewrite changes prose meaning (loses important information) | accept | Reviewer (or follow-up doc-pass) can spot prose drift. The close-gate is automatic (regex pass), prose-quality is human-readable. |
</threat_model>

<verification>
- `uv run pytest tests/framework/unit/test_doc_scrub.py -v` exits 0.
- `grep "mcp-test-framework run --explain" README.md` returns zero bare hits.
- README sentences with surviving doc-anchor citations are present.
- No allowlist or `noqa` comments added.
</verification>

<success_criteria>
- Both close-gate tests green: `test_doc_invocations_consistently_pair_with_config`, `test_readme_exists_and_no_banned_tokens`.
- Every Task 1 and Task 2 inventory entry has a corresponding fix.
- Only `README.md` is modified.
</success_criteria>

<output>
After completion, create `.planning/phases/23-test-suite-debt-cleanup/23-03-SUMMARY.md` recording:
- Task 1 unpaired-invocation inventory + fixes applied.
- Task 2 banned-token inventory: regex matched, line, before/after sentence.
- Final test results for both close-gate tests.
</output>
