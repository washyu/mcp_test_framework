---
phase: 23-test-suite-debt-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/framework/unit/test_migration_doc.py
  - tests/framework/unit/test_cli_errors.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "tests/framework/unit/test_migration_doc.py:14 uses parents[3] (was parents[2]) — repo-root resolves correctly post Phase 15 folder split (D-04)."
    - "tests/framework/unit/test_cli_errors.py near line 407-408 uses parents[3] (was parents[2]) — D-04."
    - "No shared helper is extracted; heterogeneous parents[N] vs walk-to-pyproject styles remain in the suite (D-04 explicit refusal of consolidation)."
    - "All Cluster B reds (5 tests) go green after this plan."
  artifacts:
    - path: "tests/framework/unit/test_migration_doc.py"
      provides: "Migration doc regression test with correctly-resolving _repo_root()"
      contains: "parents[3]"
    - path: "tests/framework/unit/test_cli_errors.py"
      provides: "CLI errors AST-scan test with correctly-resolving repo_root"
      contains: "parents[3]"
  key_links:
    - from: "tests/framework/unit/test_migration_doc.py"
      to: "docs/MIGRATION-v1-to-v2.md"
      via: "Path resolution via parents[3]"
      pattern: "MIGRATION-v1-to-v2"
    - from: "tests/framework/unit/test_cli_errors.py"
      to: "src/mcp_test_framework/cli.py"
      via: "Path resolution via parents[3]"
      pattern: "src.*mcp_test_framework.*cli"
---

<objective>
Cluster B — mechanical bump of `parents[2]` → `parents[3]` at exactly two sites: `tests/framework/unit/test_migration_doc.py:14` and `tests/framework/unit/test_cli_errors.py:~407`. After Phase 15's `tests/framework/unit/` folder split, these tests resolved repo-root under `tests/` instead of the actual repo root. D-04 explicitly forbids extracting a shared helper or switching to the walk-to-`pyproject.toml` style used in `test_doc_scrub.py:16-21`; heterogeneous resolution styles in the suite are acceptable for this debt-cleanup phase.

Purpose: Two distinct one-line edits. Lowest-risk plan in the phase.

Output: Two patched test files; 5 Cluster B reds go green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-test-suite-debt-cleanup/23-CONTEXT.md
@.planning/phases/23-test-suite-debt-cleanup/23-PATTERNS.md
@tests/framework/unit/test_migration_doc.py
@tests/framework/unit/test_cli_errors.py

<interfaces>
<!-- Current (broken) and target code at each site, from PATTERNS.md. -->

`tests/framework/unit/test_migration_doc.py:14-18` (current, broken):
```python
def _repo_root() -> Path:
    return Path(__file__).resolve().parents[2]


MIGRATION_DOC = _repo_root() / "docs" / "MIGRATION-v1-to-v2.md"
```

`tests/framework/unit/test_migration_doc.py:14` (target):
```python
def _repo_root() -> Path:
    return Path(__file__).resolve().parents[3]
```

`tests/framework/unit/test_cli_errors.py:401-409` (current, broken):
```python
def test_cli_errors_static_call_sites_no_banned_tokens() -> None:
    """AST scan: every _emit_operator_error call's literal args are operator-tone."""
    import ast
    # Walk up from this test file to the repo root (same pattern as
    # test_doc_scrub._repo_root) so the test passes regardless of the
    # cwd pytest is invoked from.
    repo_root = Path(__file__).resolve().parents[2]
    src = (repo_root / "src" / "mcp_test_framework" / "cli.py").read_text(
        encoding="utf-8"
    )
```

`tests/framework/unit/test_cli_errors.py:407` (target): change `parents[2]` → `parents[3]`.

The misleading comment "Walk up from this test file ... same pattern as test_doc_scrub._repo_root" is technically incorrect (this isn't the walk-up style; it's depth-counting). Per D-04, leave the comment alone — heterogeneous styles + minor comment drift are acceptable during debt cleanup.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump test_migration_doc.py parents[2] → parents[3] at line 14</name>
  <files>tests/framework/unit/test_migration_doc.py</files>
  <read_first>
    - .planning/phases/23-test-suite-debt-cleanup/23-CONTEXT.md (D-04)
    - .planning/phases/23-test-suite-debt-cleanup/23-PATTERNS.md (§"tests/framework/unit/test_migration_doc.py (Cluster B: mechanical bump)")
    - tests/framework/unit/test_migration_doc.py (full file before editing)
  </read_first>
  <action>
Make a single one-character change at `tests/framework/unit/test_migration_doc.py:14` (or wherever the `_repo_root()` function lives — confirm via Read before editing):

BEFORE:
```python
def _repo_root() -> Path:
    return Path(__file__).resolve().parents[2]
```

AFTER:
```python
def _repo_root() -> Path:
    return Path(__file__).resolve().parents[3]
```

No other changes to the file. Do NOT:
- Extract a shared helper.
- Switch to the walk-to-`pyproject.toml` style.
- Modify imports.
- Modify the docstring or surrounding comments.
- Touch any other line.

Rationale (per PATTERNS.md): the file lives at `tests/framework/unit/test_migration_doc.py` (depth 3 from repo root). `parents[2]` resolves to `tests/`; `parents[3]` resolves to the repo root, which is correct.
  </action>
  <verify>
    <automated>uv run pytest tests/framework/unit/test_migration_doc.py --tb=short -q</automated>
  </verify>
  <done>
- `grep -n "parents\[3\]" tests/framework/unit/test_migration_doc.py` returns one match at the `_repo_root()` body.
- `grep -n "parents\[2\]" tests/framework/unit/test_migration_doc.py` returns zero matches.
- `uv run pytest tests/framework/unit/test_migration_doc.py --tb=short -q` exits 0.
- `git diff tests/framework/unit/test_migration_doc.py` shows exactly one line changed (the `parents[2]` → `parents[3]` swap).
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump test_cli_errors.py parents[2] → parents[3] at ~line 407</name>
  <files>tests/framework/unit/test_cli_errors.py</files>
  <read_first>
    - .planning/phases/23-test-suite-debt-cleanup/23-CONTEXT.md (D-04)
    - .planning/phases/23-test-suite-debt-cleanup/23-PATTERNS.md (§"tests/framework/unit/test_cli_errors.py (Cluster B: mechanical bump)")
    - tests/framework/unit/test_cli_errors.py:395-420 (read this range before editing)
  </read_first>
  <action>
Make a single one-character change inside `test_cli_errors_static_call_sites_no_banned_tokens` near line 407:

BEFORE:
```python
repo_root = Path(__file__).resolve().parents[2]
```

AFTER:
```python
repo_root = Path(__file__).resolve().parents[3]
```

Locate the line via Grep (`Grep "parents\[2\]" tests/framework/unit/test_cli_errors.py -n`) — exact line number may have shifted slightly from 407.

Do NOT:
- Modify the (slightly misleading) "Walk up from this test file..." comment — D-04 leaves heterogeneous styles in place.
- Touch any other line, import, or test in the file.
- Extract a shared helper.

If `Grep` returns multiple `parents[2]` hits in this file, change ONLY the one inside `test_cli_errors_static_call_sites_no_banned_tokens` (the one used to build `repo_root` before reading `cli.py`). PATTERNS.md confirms there is one site; verify before editing.
  </action>
  <verify>
    <automated>uv run pytest tests/framework/unit/test_cli_errors.py --tb=short -q</automated>
  </verify>
  <done>
- `grep -n "parents\[3\]" tests/framework/unit/test_cli_errors.py` returns one match in the body of `test_cli_errors_static_call_sites_no_banned_tokens`.
- `grep -n "parents\[2\]" tests/framework/unit/test_cli_errors.py` returns zero matches (or only matches that pre-existed for other tests AND were not in the Cluster B red set — verify via Task 1 of Plan 01's inventory).
- `uv run pytest tests/framework/unit/test_cli_errors.py --tb=short -q` exits 0.
- `git diff tests/framework/unit/test_cli_errors.py` shows exactly one line changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → filesystem (repo root) | Both edited tests resolve repo-root via `Path(__file__).resolve().parents[N]` and then read repo files (`docs/MIGRATION-v1-to-v2.md`, `src/mcp_test_framework/cli.py`). Bumping the depth changes which directory is treated as root. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-02-01 | Tampering | parents[3] resolution lands on wrong directory if file is moved again | accept | If the folder split is reversed or extended, both tests fail loudly (the doc/cli source they read won't exist). No silent failure mode. Walk-to-pyproject style (alternative) would be more robust but is explicitly out of scope per D-04. |
| T-23-02-02 | Repudiation | Misleading comment in test_cli_errors.py ("Walk up...") stays | accept | Pre-existing drift; D-04 keeps it. Future refactor (deferred per CONTEXT.md) can consolidate. |
</threat_model>

<verification>
- Both sites use `parents[3]`.
- No shared helper extracted.
- `tests/framework/unit/test_migration_doc.py` and `tests/framework/unit/test_cli_errors.py` are green.
- All other Cluster B reds (test_cli_errors.py has multiple tests; verify the AST-scan one was the failing site) are green.
</verification>

<success_criteria>
- Exactly two lines changed across two files.
- `uv run pytest tests/framework/unit/test_migration_doc.py tests/framework/unit/test_cli_errors.py --tb=no -q` exits 0.
- Cluster B reds from Plan 01's Task 1 inventory are all green.
</success_criteria>

<output>
After completion, create `.planning/phases/23-test-suite-debt-cleanup/23-02-SUMMARY.md` recording:
- The exact line numbers patched.
- Pre/post test results for `test_migration_doc.py` and `test_cli_errors.py`.
</output>
