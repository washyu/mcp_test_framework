---
phase: 11
plan: 03-requirements-traceability-cleanup
wave: 1
depends_on: []
files_modified:
  - ".planning/REQUIREMENTS.md"
requirements: []
autonomous: true
must_haves:
  truths:
    - "REQUIREMENTS.md ## Traceability table shows 'Complete' for every v1.1 REQ-ID — no rows say 'Pending'"
    - "REQUIREMENTS.md body checkboxes for DOC-04, DOC-05, DOC-06, DOC-07 are [x] (not [ ])"
    - "Coverage summary line at the top of the Traceability section reads '25/25 (100%)' — already correct, just verify it stays put"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "v1.1 requirements with traceability table reflecting actual completion state — closes audit W-4"
      contains: "25/25"
  key_links:
    - from: "REQUIREMENTS.md ISOL-03 row"
      to: "06-VERIFICATION.md SC-1 (passed)"
      via: "status column should now match"
    - from: "REQUIREMENTS.md ISOL-06 row"
      to: "06-VERIFICATION.md SC-2 (passed Windows arm; POSIX deferred-by-design per CD-04)"
      via: "status column should now match"
    - from: "REQUIREMENTS.md DOC-04..07 rows"
      to: "10-v1-1-documentation/VERIFICATION.md (all four passed)"
      via: "status column + body checkbox should now match"
sc_addressed:
  - "ROADMAP Phase 11 SC-3 (W-4): Traceability table 'Complete' for every v1.1 REQ-ID; DOC-04..07 body checkboxes [x]; coverage summary 25/25"
---

<objective>
Fix the REQUIREMENTS.md traceability drift identified by the v1.1 milestone audit (W-4): six rows in the `## Traceability` table currently say "Pending" despite the corresponding requirements being marked `[x]` in the body (ISOL-03, ISOL-06) or shipped via Phase 10 but not yet flipped (DOC-04..07). Plus, four body checkboxes (DOC-04..07) are still `[ ]` and need to flip to `[x]`.

Purpose: After this plan, REQUIREMENTS.md internally agrees with the milestone audit (`.planning/v1.1-MILESTONE-AUDIT.md`), 06-VERIFICATION.md, and 10-v1-1-documentation/VERIFICATION.md — every v1.1 REQ-ID is shown as Complete in both body and table.
Output: One file modified. Six table-cell edits + four checkbox edits = ten surgical changes. Zero source-code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/06-per-session-host-state-isolation/06-VERIFICATION.md
@.planning/phases/10-v1-1-documentation/VERIFICATION.md

<exact_edits>
PART 1 — Body checkbox flips (DOC-04 through DOC-07).

Find the `### DOC — Documentation updates for v1.1` section (REQUIREMENTS.md:45-50). The four bullets currently read:

   `- [ ] **DOC-04**: README documents how to write a per-tool config block ...`
   `- [ ] **DOC-05**: README documents the isolation guarantee ...`
   `- [ ] **DOC-06**: README documents the JUnit XML output flag ...`
   `- [ ] **DOC-07**: \`docs/EXTENDING.md\` updated to describe how to add a new MCP tool target ...`

Flip each leading `- [ ]` to `- [x]`. Do NOT change any of the descriptive text after the bold REQ-ID. Result:

   `- [x] **DOC-04**: README documents how to write a per-tool config block ...`
   `- [x] **DOC-05**: README documents the isolation guarantee ...`
   `- [x] **DOC-06**: README documents the JUnit XML output flag ...`
   `- [x] **DOC-07**: \`docs/EXTENDING.md\` updated to describe how to add a new MCP tool target ...`

PART 2 — Traceability table cell flips (six rows).

The `## Traceability` table currently has six rows where the `Status` column reads `Pending` despite the corresponding requirements being satisfied:

   `| ISOL-03 | Phase 06 | Pending |`   →   `| ISOL-03 | Phase 06 | Complete |`
   `| ISOL-06 | Phase 06 | Pending |`   →   `| ISOL-06 | Phase 06 | Complete |`
   `| DOC-04  | Phase 10 | Pending |`   →   `| DOC-04 | Phase 10 | Complete |`
   `| DOC-05  | Phase 10 | Pending |`   →   `| DOC-05 | Phase 10 | Complete |`
   `| DOC-06  | Phase 10 | Pending |`   →   `| DOC-06 | Phase 10 | Complete |`
   `| DOC-07  | Phase 10 | Pending |`   →   `| DOC-07 | Phase 10 | Complete |`

Match the existing column-padding convention in the file (single space around `|`); do not introduce new whitespace patterns. The cells are short — `Complete` and `Pending` are the same character count modulo trailing-space; preserve the line-length pattern of neighboring rows.

PART 3 — Coverage summary verification (no edit needed if already 25/25).

The line at REQUIREMENTS.md:85 currently reads:

   `Every v1.1 requirement maps 1:1 to exactly one phase. Coverage: 25/25 (100%).`

Verify it still says `25/25 (100%)` after PARTs 1 and 2. If for any reason it currently differs, correct it to `25/25 (100%)`. (The audit's W-4 ack is "coverage summary at top reflects 25/25 complete" — this line IS that summary.)

Total: 4 checkbox flips + 6 table-cell flips = 10 surgical changes. NO other lines should be modified. Do NOT renumber rows. Do NOT touch the "Phase coverage summary" table at the bottom of the file (it counts 7+4+7+3+4 = 25 reqs by category, which is independently correct).
</exact_edits>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip DOC-04..07 body checkboxes from [ ] to [x]</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 45-52: the `### DOC — Documentation updates for v1.1` section)
    - .planning/phases/10-v1-1-documentation/VERIFICATION.md (frontmatter `status: passed` + `score: 18/18 must-haves verified` + Requirements Coverage table showing DOC-04..07 SATISFIED — confirms the flip is justified)
    - .planning/v1.1-MILESTONE-AUDIT.md (W-4 section, specifically the bullet "Requirements checkboxes for DOC-04..07 are still '[ ]' in the body of REQUIREMENTS.md — should be '[x]' since Phase 10 PASSED.")
  </read_first>
  <action>
    In the `### DOC — Documentation updates for v1.1` section of REQUIREMENTS.md, flip exactly four lines: the bullets for DOC-04, DOC-05, DOC-06, and DOC-07. Each currently starts with `- [ ] **DOC-NN**:` — change the `- [ ]` to `- [x]` while preserving everything after (the bold tag, the colon, the descriptive text).

    Do NOT touch any other section. Do NOT modify the descriptive text. Do NOT add new bullets. Surgical 4-line replacement only.
  </action>
  <verify>
    <automated>grep -c "^- \[x\] \*\*DOC-0[4567]\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^- \[x\] \*\*DOC-04\*\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[x\] \*\*DOC-05\*\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[x\] \*\*DOC-06\*\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[x\] \*\*DOC-07\*\*" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "^- \[ \] \*\*DOC-0[4567]\*\*" .planning/REQUIREMENTS.md` returns 0 (none of the four still has unchecked box)
    - The descriptive text after each REQ-ID is unchanged (verify by grep for a literal substring from each: "README documents how to write a per-tool config", "README documents the isolation guarantee", "README documents the JUnit XML output flag", "docs/EXTENDING.md`")
  </acceptance_criteria>
  <done>
    All four DOC-NN bullets in REQUIREMENTS.md show `[x]` and their descriptive text is byte-identical to before the edit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip the six "Pending" rows in the Traceability table to "Complete"</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (lines 87-114: the `## Traceability` table — already loaded if Task 1 ran in same session, but reload the table region after Task 1's edit to ensure clean state)
    - .planning/v1.1-MILESTONE-AUDIT.md (W-4 section + the per-req status column in the Requirements Coverage table at lines 60-71 — audit canonically says ISOL-03 / ISOL-06 / DOC-04..07 are "satisfied" / "satisfied (POSIX-arm explicitly deferred)" / "satisfied")
    - .planning/phases/06-per-session-host-state-isolation/06-VERIFICATION.md (Requirements Coverage table at line 130-138 — confirms ISOL-03 PASS and ISOL-06 PASS-Windows-DEFERRED-POSIX)
  </read_first>
  <action>
    In the `## Traceability` table of REQUIREMENTS.md, change the third column (`Status`) from `Pending` to `Complete` for exactly these six rows:
      - ISOL-03 row
      - ISOL-06 row
      - DOC-04 row
      - DOC-05 row
      - DOC-06 row
      - DOC-07 row

    Do NOT touch any other table row (the existing 19 rows that already say `Complete` should stay `Complete`). Do NOT touch the table header or separator. Do NOT modify the first or second column (REQ-ID and Phase). Do NOT add a footnote or status-qualifier column — the audit accepts ISOL-06's POSIX-arm deferral as "satisfied" with the deferral noted elsewhere (REQUIREMENTS.md:36 already records the CD-04 deferral inline; the table just needs the status flipped).

    Preserve the table's existing column-padding (single space around `|`).
  </action>
  <verify>
    <automated>grep -cE "^\| (ISOL-03|ISOL-06|DOC-0[4567]) \| Phase (06|10) \| Complete \|" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\| ISOL-03 \| Phase 06 \| Complete \|" .planning/REQUIREMENTS.md` returns 1
    - `grep -cE "^\| ISOL-06 \| Phase 06 \| Complete \|" .planning/REQUIREMENTS.md` returns 1
    - `grep -cE "^\| DOC-04 \| Phase 10 \| Complete \|" .planning/REQUIREMENTS.md` returns 1
    - `grep -cE "^\| DOC-05 \| Phase 10 \| Complete \|" .planning/REQUIREMENTS.md` returns 1
    - `grep -cE "^\| DOC-06 \| Phase 10 \| Complete \|" .planning/REQUIREMENTS.md` returns 1
    - `grep -cE "^\| DOC-07 \| Phase 10 \| Complete \|" .planning/REQUIREMENTS.md` returns 1
    - `grep -c "| Pending |" .planning/REQUIREMENTS.md` returns 0 (the six "Pending" rows are the only Pending values in the file; after this task, zero remain)
    - The 19 rows already showing `Complete` are unchanged: `grep -c "| Complete |" .planning/REQUIREMENTS.md` returns 25 (= 19 prior + 6 newly flipped)
    - The coverage line `Every v1.1 requirement maps 1:1 to exactly one phase. Coverage: 25/25 (100%).` remains in the file (`grep -c "Coverage: 25/25 (100%)" .planning/REQUIREMENTS.md` returns 1)
    - The header line of the table (`| Requirement | Phase | Status |`) and the separator line (`|-------------|-------|--------|`) are untouched
  </acceptance_criteria>
  <done>
    All six previously-"Pending" rows now say "Complete"; total `| Complete |` count is 25 (= total v1.1 REQ-IDs); zero `| Pending |` cells remain; the 25/25 coverage summary still reads correctly.
  </done>
</task>

</tasks>

<threat_model>
No new attack surface — paper-only cleanup. REQUIREMENTS.md is a documentation artifact; this plan flips status cells/checkboxes to match the already-shipped implementation state per the audit.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Drift / status mis-claim | REQUIREMENTS.md traceability surface | accept | Each flip is justified by a passed VERIFICATION.md (06-VERIFICATION.md for ISOL-03/06; 10-v1-1-documentation/VERIFICATION.md for DOC-04..07). The audit document is the canonical cross-reference. |
</threat_model>

<verification>
- 4 body checkboxes flipped, 6 table cells flipped — 10 surgical edits total
- No other lines in REQUIREMENTS.md modified
- The "Phase coverage summary" sub-table at the bottom of the file is untouched (it already reports 25 reqs by category, which is independently correct)
- Final state: zero `| Pending |` cells; twenty-five `| Complete |` cells; four `[x] **DOC-0N**` body bullets
</verification>

<success_criteria>
- [ ] DOC-04..07 body checkboxes are all [x]
- [ ] ISOL-03, ISOL-06, DOC-04..07 traceability rows all say Complete
- [ ] Zero "| Pending |" cells remain in the file
- [ ] Total "| Complete |" cells equals 25
- [ ] Coverage line still reads "25/25 (100%)"
- [ ] All other rows / sections / tables untouched
</success_criteria>

<output>
After completion, create `.planning/phases/11-v1-1-cleanup-verification-hygiene/11-03-SUMMARY.md` per the standard summary template, citing audit W-4 closure and listing the ten surgical edits applied.
</output>
