---
phase: 11
plan: 02-roadmap-drift-fixes
wave: 1
depends_on: []
files_modified:
  - ".planning/ROADMAP.md"
requirements: []
autonomous: true
must_haves:
  truths:
    - "ROADMAP.md Phase 06 SC-1 says 'sha256' (not 'mtimes') matching the D-09 strengthening that actually shipped"
    - "ROADMAP.md Phase 07 has [x] checkbox in the bullet list under v1.1"
    - "ROADMAP.md Progress table row for Phase 07 says 'Complete 2026-05-07' (not 'Planned' / 'Pending' / '0/1')"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Authoritative roadmap with v1.1 milestone state correctly reflected — closes audit W-5 + the Phase 07 drift"
      contains: "sha256"
  key_links:
    - from: "ROADMAP.md Phase 06 SC-1"
      to: "Phase 06 D-09 + 06-VERIFICATION.md SC-1 evidence (which already says sha256)"
      via: "wording alignment"
    - from: "ROADMAP.md Phase 07 checkbox + Progress row"
      to: "07-VERIFICATION.md (which already exists and is passed)"
      via: "completion state alignment"
sc_addressed:
  - "ROADMAP Phase 11 SC-4 (W-5): Phase 06 SC-1 wording 'mtimes' → 'sha256'"
  - "ROADMAP Phase 11 SC-5: Phase 07 checkbox [x] + Progress row 'Complete 2026-05-07'"
---

<objective>
Fix the two ROADMAP.md drift items surfaced by the v1.1 milestone audit: the Phase 06 SC-1 wording that still says "mtimes" (the implementation strengthened to sha256 per D-09), and the Phase 07 row that still shows the planned-but-not-marked-complete state despite Phase 07 having shipped on 2026-05-07.

Purpose: Close audit W-5 (mtimes → sha256) and the planner-flagged Phase 07 drift in a single small file edit. After this plan, ROADMAP.md internally agrees with 06-VERIFICATION.md, 07-VERIFICATION.md, and the milestone audit.
Output: One file modified. Two minimal edits. Zero source-code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/06-per-session-host-state-isolation/06-VERIFICATION.md

<exact_edits>
Two surgical replacements only. Do NOT rewrite or restructure the ROADMAP. Make exactly these two changes.

EDIT 1 — Phase 06 SC-1 (W-5):
Find the line currently in ROADMAP.md (within the "### Phase 06: Per-session host-state isolation" → "**Success Criteria** (what must be TRUE):" → "1. After a full ..." bullet):

   `1. After a full \`uv run mcp-test-framework run\` invocation, the mtimes of \`~/.homelab_mcp/credential_registry.json\`, \`~/.homelab_mcp/known_hosts\`, and \`~/.homelab_mcp/migration_state.json\` are unchanged from before the run (verified by a dedicated test).`

Replace the substring `the mtimes of` with `the sha256 hashes of`. After the edit the bullet reads:

   `1. After a full \`uv run mcp-test-framework run\` invocation, the sha256 hashes of \`~/.homelab_mcp/credential_registry.json\`, \`~/.homelab_mcp/known_hosts\`, and \`~/.homelab_mcp/migration_state.json\` are unchanged from before the run (verified by a dedicated test).`

This wording change is justified by Phase 06 D-09 which strengthened ISOL-03 from mtime checks to sha256 byte-equality (catches same-second identical-content overwrites). 06-VERIFICATION.md SC-1 already says sha256; the ROADMAP is the only artifact with the stale "mtimes" wording.

EDIT 2 — Phase 07 checkbox (planner-flagged drift):
Find the v1.1 bullet list line that currently reads:

   `- [ ] **Phase 07: Multi-tool discovery & parameterized testing** — Generalize from one-tool-per-run to N-tools-per-run via \`pytest.mark.parametrize\` over discovered tools (no codegen)`

Replace the leading `- [ ]` with `- [x]` and append ` (completed 2026-05-07)` to the END of that line so it reads:

   `- [x] **Phase 07: Multi-tool discovery & parameterized testing** — Generalize from one-tool-per-run to N-tools-per-run via \`pytest.mark.parametrize\` over discovered tools (no codegen) (completed 2026-05-07)`

Match the exact format of the surrounding bullets (Phase 06 / 08 / 09 / 10 already use the `(completed YYYY-MM-DD)` suffix on the same line — see ROADMAP.md lines 25, 28, 30, 31).

EDIT 3 — Phase 07 Progress table row (planner-flagged drift):
Find the row in the Progress table at the bottom of the file currently reading:

   `| 07. Multi-tool discovery & parameterized testing | v1.1 | 0/1 | Planned | — |`

Replace with:

   `| 07. Multi-tool discovery & parameterized testing | v1.1 | 1/1 | Complete | 2026-05-07 |`

The status string `Complete` must match exactly the case used by other v1.1 Complete rows in the same table (verify by inspection of Phase 06 / 08 / 09 / 10 rows — note Phase 08 / 09 / 10 use `Complete` with trailing whitespace-padding; ignore the padding, just write `Complete` as a clean cell value).

Three edits total, ONE file. The edits are independent — none depends on the others — but all three should land in a single commit.
</exact_edits>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the three ROADMAP.md edits (W-5 sha256 + Phase 07 checkbox + Phase 07 progress row)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (the entire file is short — load once, identify the three target lines)
    - .planning/v1.1-MILESTONE-AUDIT.md (specifically the W-5 section in tech_debt + the cross-phase wording in line 121: "ROADMAP wording 'mtimes' → 'sha256' for ISOL-03")
    - .planning/phases/06-per-session-host-state-isolation/06-VERIFICATION.md (SC-1 section confirming sha256 is the shipped form — line 45 onwards)
  </read_first>
  <action>
    Make EXACTLY the three edits described in `<exact_edits>` above:

    1. In the Phase 06 Success Criteria block, change `the mtimes of` to `the sha256 hashes of` in the SC-1 bullet line.
    2. In the v1.1 milestone bullet list near top of file, flip the Phase 07 bullet from `- [ ] **Phase 07:` to `- [x] **Phase 07:` and append ` (completed 2026-05-07)` to the end of that bullet line. Match the placement convention already used by the surrounding Phase 06/08/09/10 bullets.
    3. In the Progress table at the bottom of the file, change the Phase 07 row's `0/1 | Planned | —` cells to `1/1 | Complete | 2026-05-07`.

    Do NOT touch any other line. Do NOT reorder sections. Do NOT change Phase 07 plan list (`Plans: ... [x] 07-01-multi-tool-discovery-PLAN.md`) — the plan checkbox is already `[x]` and is correct as-is. Do NOT add new content. Surgical replacements only.

    After editing, the file should still parse as valid markdown and the YAML-style "**Plans:** N plan" lines should remain unchanged.
  </action>
  <verify>
    <automated>grep -q "the sha256 hashes of \`~/.homelab_mcp/credential_registry.json\`" .planning/ROADMAP.md && ! grep -q "the mtimes of \`~/.homelab_mcp/credential_registry.json\`" .planning/ROADMAP.md && grep -q "^- \[x\] \*\*Phase 07: Multi-tool discovery" .planning/ROADMAP.md && ! grep -q "^- \[ \] \*\*Phase 07: Multi-tool discovery" .planning/ROADMAP.md && grep -q "07. Multi-tool discovery & parameterized testing | v1.1 | 1/1 | Complete | 2026-05-07" .planning/ROADMAP.md && ! grep -q "07. Multi-tool discovery & parameterized testing | v1.1 | 0/1 | Planned" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "the sha256 hashes of" .planning/ROADMAP.md` returns ≥1 in the Phase 06 SC-1 region (line range containing "Phase 06: Per-session host-state isolation")
    - `grep -c "the mtimes of" .planning/ROADMAP.md` returns 0 (the old wording is fully replaced)
    - `grep -c "^- \[x\] \*\*Phase 07: Multi-tool" .planning/ROADMAP.md` returns 1
    - `grep -c "^- \[ \] \*\*Phase 07: Multi-tool" .planning/ROADMAP.md` returns 0
    - `grep -c "Phase 07: Multi-tool discovery .* (completed 2026-05-07)" .planning/ROADMAP.md` returns 1 (the bullet has the date suffix)
    - `grep -c "07\. Multi-tool discovery & parameterized testing | v1.1 | 1/1 | Complete | 2026-05-07" .planning/ROADMAP.md` returns 1
    - `grep -c "07\. Multi-tool discovery & parameterized testing | v1.1 | 0/1 | Planned" .planning/ROADMAP.md` returns 0
    - The Phase 11 entry in the Progress table is unchanged (still `Planned` until Phase 11 itself completes — do not pre-mark)
    - `wc -l .planning/ROADMAP.md` is within ±2 lines of pre-edit count (no major restructuring; this is a 3-line surgical edit)
  </acceptance_criteria>
  <done>
    ROADMAP.md says "sha256 hashes" in Phase 06 SC-1, has [x] + completion date on the Phase 07 bullet, and shows "1/1 | Complete | 2026-05-07" in the Phase 07 Progress row. No other lines changed.
  </done>
</task>

</tasks>

<threat_model>
No new attack surface — paper-only cleanup. ROADMAP.md is a documentation artifact; this plan corrects three drift lines to match the already-shipped implementation state.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02-01 | Tampering / drift propagation | ROADMAP.md authoritative state | accept | The audit (.planning/v1.1-MILESTONE-AUDIT.md) is the canonical source for "what actually shipped"; this edit makes ROADMAP agree with the audit + 06/07-VERIFICATION.md. No new content invented. |
</threat_model>

<verification>
- Three surgical edits applied exactly as specified
- No other lines in ROADMAP.md modified
- Phase 07 plan-list checkbox (`[x] 07-01-multi-tool-discovery-PLAN.md`) untouched (already correct)
- Phase 11 row in the Progress table untouched (still Planned — do not pre-mark)
- File still parses as valid markdown
</verification>

<success_criteria>
- [ ] sha256 wording lands in Phase 06 SC-1
- [ ] "mtimes" no longer appears in ROADMAP.md
- [ ] Phase 07 bullet is `[x]` with `(completed 2026-05-07)` suffix
- [ ] Phase 07 Progress row shows `1/1 | Complete | 2026-05-07`
- [ ] No other rows or sections changed
</success_criteria>

<output>
After completion, create `.planning/phases/11-v1-1-cleanup-verification-hygiene/11-02-SUMMARY.md` per the standard summary template, citing the audit items closed (W-5 + Phase 07 drift) and the diff applied.
</output>
