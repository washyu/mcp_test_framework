---
phase: 11
plan: 01-create-09-verification
wave: 1
depends_on: []
files_modified:
  - ".planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md"
requirements: []
autonomous: true
must_haves:
  truths:
    - "Phase 09 has a VERIFICATION.md artifact that cites the existing 09-0N-SUMMARY trio + tests/test_reporter.py as evidence"
    - "Frontmatter shape matches the 06/07/08/10 VERIFICATION.md convention (phase, verified, status, score, requirements_completed, etc.)"
    - "All three OUTPUT-01..03 success criteria are mapped to evidence with file:line citations"
    - "The 3 deferred live subprocess tests (per 09-03-SUMMARY) are explicitly recorded as deferred-with-evidence-path, not as gaps"
  artifacts:
    - path: ".planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md"
      provides: "Formal Phase 09 verification report — closes audit W-1"
      contains: "phase: 09-junit-xml-output-per-tool-reporting"
  key_links:
    - from: "09-VERIFICATION.md"
      to: "09-01-SUMMARY.md, 09-02-SUMMARY.md, 09-03-SUMMARY.md"
      via: "explicit citations in body + Required Artifacts table"
    - from: "09-VERIFICATION.md"
      to: "tests/test_reporter.py"
      via: "Behavioural Spot-Checks table referencing the 29 unit tests"
sc_addressed:
  - "ROADMAP Phase 11 SC-1 (W-1): author 09-VERIFICATION.md citing 09-0N-SUMMARY trio + tests/test_reporter.py + the 3 deferred live tests"
---

<objective>
Author `.planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md` from existing summary evidence so Phase 09 has a formal verification artifact matching the 06/07/08/10 convention.

Purpose: Close audit W-1 (Phase 09 is the only milestone phase missing a VERIFICATION.md). Goal-achievement is already evidenced by the 09-0N-SUMMARY trio + tests/test_reporter.py 29 passing unit tests; this plan formalizes that evidence into the standard VERIFICATION.md template.
Output: One new file. Zero source-code edits. Zero dependencies on other plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/06-per-session-host-state-isolation/06-VERIFICATION.md
@.planning/phases/10-v1-1-documentation/VERIFICATION.md
@.planning/phases/09-junit-xml-output-per-tool-reporting/09-01-SUMMARY.md
@.planning/phases/09-junit-xml-output-per-tool-reporting/09-02-SUMMARY.md
@.planning/phases/09-junit-xml-output-per-tool-reporting/09-03-SUMMARY.md

<frontmatter_template>
The new VERIFICATION.md MUST start with frontmatter using these EXACT keys
(distilled from 06-VERIFICATION.md and 10-v1-1-documentation/VERIFICATION.md
which are the in-scope reference shapes for this milestone):

```yaml
---
phase: 09-junit-xml-output-per-tool-reporting
verified: 2026-05-08T00:00:00Z
status: passed
score: 3/3 ROADMAP success criteria verified
criteria_met: 3
criteria_total: 3
requirements_completed: 3
requirements_total: 3
re_verification: false
findings:
  pass: 3
  partial: 0
  fail: 0
  human_verify: 0
deferred:
  - truth: "Live subprocess tests (3) for end-to-end JUnit XML well-formedness, [<tool>] SUFFIX contract, and per-tool summary always-on"
    addressed_in: "tests/test_reporter.py @pytest.mark.live_homelab — runnable in any env where `mcp-test-framework run -- -m live_homelab` completes within --timeout=600 (per 09-03-SUMMARY.md:101-104 environmental note)"
    evidence: "tests/test_reporter.py: test_junit_xml_emitted_and_well_formed, test_junit_xml_testcase_names_carry_tool_suffix, test_per_tool_summary_section_always_on (3 functions, all gated by @pytest.mark.live_homelab; 09-03-SUMMARY.md:101-104 documents the timeout-budget reason for sandbox deferral)"
notes:
  - "Phase 09 SUMMARYs were authored before VERIFICATION.md was written; this report retroactively formalizes evidence already captured across 09-01-SUMMARY.md / 09-02-SUMMARY.md / 09-03-SUMMARY.md and the 29 passing unit tests in tests/test_reporter.py."
---
```

The above frontmatter MUST appear verbatim (date, status: passed, the deferred
block citing the 3 live tests, and the notes paragraph) — those values are the
specific findings the report records.
</frontmatter_template>

<body_outline>
Body sections (H2 unless noted) and what each MUST contain:

1. `# Phase 09: JUnit XML Output & Per-Tool Reporting — Verification Report`
   then a metadata block (Phase Goal / Verified / Status / Score / Re-verification),
   matching the 06-VERIFICATION.md preamble shape.

2. `## Goal Achievement` — 1-2 paragraphs stating the goal is met. The
   ROADMAP Phase 09 goal verbatim is: "A CI engineer can wire the test
   suite into their pipeline using JUnit XML and trend per-tool failure
   rates; locally, a concise per-tool summary helps triage failures
   without reading full pytest output." Cite tests/test_reporter.py 29
   unit tests + the cli.py / _reporter.py implementation as the evidence.

3. `## ROADMAP Success Criteria` — three H3 subsections, one per SC:
   - `### SC-1: --junit-xml=results.xml produces a standard JUnit XML file consumable by GitHub Actions / Jenkins / generic CI dashboards`
     Status: PASS. Evidence: cite cli.py:92-117 _build_pytest_args helper (per 09-01-SUMMARY:60), the 5 helper-shape unit tests in tests/test_reporter.py, and the live test test_junit_xml_emitted_and_well_formed.
   - `### SC-2: Each test case in the JUnit XML carries a [<tool_name>] suffix`
     Status: PASS. Evidence: cite Phase 07 parametrize-id contract (incoming from 07), _reporter.py _extract_tool_name helper (09-02-SUMMARY:55-65), live test test_junit_xml_testcase_names_carry_tool_suffix asserting BOTH `[` in name AND name.endswith("]") per the SUFFIX contract.
   - `### SC-3: Terminal output includes a per-tool result section showing <tool_name>: PASS|FAIL|SKIP — <reason>`
     Status: PASS. Evidence: cite _reporter.py pytest_terminal_summary (09-02-SUMMARY:73-91), the em-dash separator literal U+2014, the 6 terminal_summary unit tests in tests/test_reporter.py, the live test test_per_tool_summary_section_always_on.

4. `## Requirements Coverage (OUTPUT-01..OUTPUT-03)` — table with 3 rows:

   | Req | Acceptance Criteria | Verdict | Evidence |
   |-----|---------------------|---------|----------|
   | OUTPUT-01 | CLI accepts --junit-xml=PATH; passthrough to pytest; produces JUnit XML | PASS | cli.py:130-140 Typer option; _build_pytest_args helper; tests/test_reporter.py::test_build_pytest_args_* (5 tests) + test_junit_xml_emitted_and_well_formed (live) |
   | OUTPUT-02 | JUnit output records per-tool granularity ([<tool>] suffix in test names) | PASS | _reporter.py::_extract_tool_name; tests/test_reporter.py::test_extract_tool_name_* (4 tests) + test_junit_xml_testcase_names_carry_tool_suffix (live) |
   | OUTPUT-03 | Per-run summary includes per-tool result section | PASS | _reporter.py::pytest_terminal_summary; em-dash separator; tests/test_reporter.py::test_terminal_summary_* (6 tests) + test_per_tool_summary_section_always_on (live) |

5. `## Required Artifacts` — table:

   | Artifact | Expected | Status | Details |
   |----------|----------|--------|---------|
   | src/mcp_test_framework/cli.py | _build_pytest_args helper + --junit-xml Typer option | VERIFIED | Per 09-01-SUMMARY: cli.py:92-117 helper; cli.py:130-140 Typer option; +43/-2 net |
   | src/mcp_test_framework/_reporter.py | Pytest plugin with pytest_runtest_logreport + pytest_terminal_summary | VERIFIED | Per 09-02-SUMMARY: 202 lines; 5 named functions; em-dash U+2014 separator |
   | tests/conftest.py | mcp_test_framework._reporter added to pytest_plugins | VERIFIED | Per 09-02-SUMMARY:80-87 |
   | tests/test_reporter.py | 29 unit tests + 3 live subprocess tests | VERIFIED | Per 09-03-SUMMARY: 551 lines, 32 test functions, 29 passing in 3.63s, 3 deferred |

6. `## Key Link Verification` — table mapping the connections referenced
   by the cross-phase audit (cite the audit's 08↔09 / 09↔10 boundaries):

   | From | To | Via | Status |
   |------|----|----|--------|
   | cli.py::run | pytest.main argv | _build_pytest_args(junit_xml, pytest_args) | WIRED |
   | _reporter.py::pytest_runtest_logreport | _PER_TOOL aggregation buffer | module-level dict | WIRED |
   | _reporter.py::pytest_terminal_summary | terminal output | terminalreporter.write_sep / write_line only (D-02c) | WIRED |
   | tests/conftest.py::pytest_plugins | _reporter plugin registration | "mcp_test_framework._reporter" string entry | WIRED |
   | Phase 08 pytest.skip(reason=...) | Phase 09 SKIP rows with em-dash | _reporter._extract_skip_reason | WIRED |

7. `## Behavioural Spot-Checks` — table with grep-verifiable commands the
   verifier ran. Mirror the 06-VERIFICATION.md style (Behaviour / Command /
   Result / Status). At minimum:
   - `wc -l src/mcp_test_framework/_reporter.py` ≥ 80 (actual: 202)
   - `grep -c "^def test_" tests/test_reporter.py` ≥ 25 (actual: 32)
   - `grep -c "@pytest.mark.live_homelab" tests/test_reporter.py` == 3
   - `grep -c "SKIP — " tests/test_reporter.py` ≥ 2 (em-dash form)
   - `grep -n "_build_pytest_args" src/mcp_test_framework/cli.py` 1 match at line ~92
   - `grep -n "_extract_tool_name" src/mcp_test_framework/_reporter.py` (multiple)
   - Unit suite passes: `MCPTF_CONFIG_FILE=./config.yaml uv run --group dev pytest tests/test_reporter.py -m "not live_homelab and not live_ollama" -v` → 29 passed

8. `## Deferred Items` — explicit list of the 3 live tests with names
   + the timeout-budget reason from 09-03-SUMMARY:101-104. Reference the
   audit's W-2 disposition ("Re-run with --timeout=600 on a developer host
   or in CI to capture artifact evidence").

9. `## Verification Gaps` — single subsection `### G-01 — Live subprocess
   evidence not yet captured (deferred-by-design)` paraphrasing the
   audit's W-2 wording. Severity: WARNING. Why it's not a blocker: the
   3 live test bodies + assertions are committed; the deferral is purely
   the env-budget for actually running them; the 29 unit tests already
   pin every contract that the live tests would re-verify at the e2e
   level.

10. `## Final Verdict` — `VERIFICATION PASSED` paragraph: 3/3 SC met,
    3/3 OUTPUT requirements complete, 1 deferred-by-design gap (live
    evidence) explicitly tracked.

11. Trailing line: `_Verified: 2026-05-08_` and
    `_Verifier: Claude (gsd-verifier — backfill from existing SUMMARY evidence per audit W-1)_`.
</body_outline>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 09-VERIFICATION.md from existing SUMMARY evidence</name>
  <files>.planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/06-per-session-host-state-isolation/06-VERIFICATION.md (frontmatter shape + body section ordering — primary template)
    - .planning/phases/10-v1-1-documentation/VERIFICATION.md (alternate frontmatter shape — confirm criteria_met / criteria_total naming)
    - .planning/phases/09-junit-xml-output-per-tool-reporting/09-01-SUMMARY.md (cli.py changes, _build_pytest_args helper, OUTPUT-01)
    - .planning/phases/09-junit-xml-output-per-tool-reporting/09-02-SUMMARY.md (_reporter.py plugin, em-dash, OUTPUT-03)
    - .planning/phases/09-junit-xml-output-per-tool-reporting/09-03-SUMMARY.md (test_reporter.py 29+3 tests, deferred-live notes, OUTPUT-01/02/03 verification)
    - .planning/v1.1-MILESTONE-AUDIT.md (W-1 wording in tech_debt section + W-2 deferred-live-evidence note — line 18 specifically lists what should be cited)
    - .planning/ROADMAP.md (Phase 09 section — verbatim goal + 3 SC strings to copy into the new file)
  </read_first>
  <action>
    Create the file at `.planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md` with the EXACT frontmatter block from `<frontmatter_template>` above (do not improvise key names — use the keys present in 06-VERIFICATION.md: phase, verified, status, score, criteria_met, criteria_total, requirements_completed, requirements_total, re_verification, findings, deferred, notes). Body must follow the 11-section outline in `<body_outline>` above. Cite line numbers from the source files when claiming evidence (e.g., "cli.py:92-117" not "in cli.py"). Use the goal verbatim from ROADMAP.md Phase 09 line 86. Use the three SC strings verbatim from ROADMAP.md Phase 09 lines 90-92. The frontmatter `deferred` block MUST list all three live test function names (test_junit_xml_emitted_and_well_formed, test_junit_xml_testcase_names_carry_tool_suffix, test_per_tool_summary_section_always_on). The "notes" key MUST cite this is a backfill per audit W-1.
  </action>
  <verify>
    <automated>test -f .planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md && grep -q "phase: 09-junit-xml-output-per-tool-reporting" .planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md && grep -q "status: passed" .planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md && grep -q "09-01-SUMMARY" .planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md && grep -q "09-02-SUMMARY" .planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md && grep -q "09-03-SUMMARY" .planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md && grep -q "tests/test_reporter.py" .planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md && grep -q "test_junit_xml_emitted_and_well_formed" .planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md && grep -q "test_junit_xml_testcase_names_carry_tool_suffix" .planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md && grep -q "test_per_tool_summary_section_always_on" .planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md && grep -q "OUTPUT-01" .planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md && grep -q "OUTPUT-02" .planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md && grep -q "OUTPUT-03" .planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at .planning/phases/09-junit-xml-output-per-tool-reporting/09-VERIFICATION.md
    - Frontmatter contains literal `phase: 09-junit-xml-output-per-tool-reporting`
    - Frontmatter contains literal `status: passed`
    - Frontmatter contains a `deferred:` block with all three live test function names: `test_junit_xml_emitted_and_well_formed`, `test_junit_xml_testcase_names_carry_tool_suffix`, `test_per_tool_summary_section_always_on`
    - Frontmatter has the same top-level keys as 06-VERIFICATION.md (phase, verified, status, score, criteria_met, criteria_total, requirements_completed, requirements_total, re_verification, findings, deferred, notes) — verify with `grep -E "^(phase|verified|status|score|criteria_met|criteria_total|requirements_completed|requirements_total|re_verification|findings|deferred|notes):" 09-VERIFICATION.md` returns ≥10 matches
    - Body cites all three SUMMARY files: `09-01-SUMMARY`, `09-02-SUMMARY`, `09-03-SUMMARY` each appear ≥1 time
    - Body cites `tests/test_reporter.py` ≥1 time
    - Body contains `OUTPUT-01`, `OUTPUT-02`, `OUTPUT-03` (each ≥1 time)
    - Body contains the literal SC headings `### SC-1`, `### SC-2`, `### SC-3` (mirrors 06-VERIFICATION.md structure)
    - Body contains a `## Final Verdict` section with `VERIFICATION PASSED`
    - File is at least 80 lines (`wc -l 09-VERIFICATION.md` ≥ 80)
  </acceptance_criteria>
  <done>
    09-VERIFICATION.md exists, has matching-shape frontmatter, cites the three SUMMARYs and the regression test file, maps all three OUTPUT-NN requirements to evidence, and records the 3 deferred live tests by name.
  </done>
</task>

</tasks>

<threat_model>
No new attack surface — paper-only cleanup. The new VERIFICATION.md is a documentation artifact under .planning/ describing already-shipped code; it adds no runtime, network, or auth surface.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Information disclosure | 09-VERIFICATION.md content | accept | The file cites internal file paths and test names (already public in the repo). No secrets, credentials, or homelab-specific identifiers. Same disclosure profile as 06-/07-/08-/10-VERIFICATION.md which already shipped. |
</threat_model>

<verification>
- File created at the canonical phase-directory path
- Frontmatter parses as YAML (no tabs, key: value indentation matches 06-VERIFICATION.md)
- Every claim in the body cites either a file:line, a SUMMARY.md, or a test function name — no unsourced "verified" assertions
- The audit's W-1 ask is satisfied: SUMMARY trio + test_reporter.py + 3 deferred live tests all referenced
</verification>

<success_criteria>
- [ ] 09-VERIFICATION.md exists at the canonical path
- [ ] Frontmatter shape matches 06-VERIFICATION.md (same top-level keys)
- [ ] Body has SC-1, SC-2, SC-3 sections with PASS verdicts
- [ ] OUTPUT-01..03 traceability table present with PASS verdicts
- [ ] Deferred section names all 3 live test functions
- [ ] No new requirements added; ROADMAP.md and REQUIREMENTS.md untouched
</success_criteria>

<output>
After completion, create `.planning/phases/11-v1-1-cleanup-verification-hygiene/11-01-SUMMARY.md` per the standard summary template, citing this PLAN, the new 09-VERIFICATION.md path, and the audit W-1 it closes.
</output>
