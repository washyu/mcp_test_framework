---
phase: 11
plan: 04-extending-env-passthrough-section
wave: 1
depends_on: []
files_modified:
  - "docs/EXTENDING.md"
requirements: []
autonomous: true
must_haves:
  truths:
    - "docs/EXTENDING.md contains a top-level section titled '## Environment passthrough allowlist' that explains the _PASSTHROUGH_ALLOWLIST module-level constant and why widening it is dangerous"
    - "The new section absorbs the verbatim warning text from src/mcp_test_framework/_isolation.py:33-36 ('DO NOT widen `_PASSTHROUGH_ALLOWLIST` without updating EXTENDING.md...') so contributors don't widen the allowlist accidentally"
    - "The same section records the WR-04 disposition (Branch B): a one-paragraph rationale explaining why POSIX `USER` is intentionally NOT in the allowlist and why `USERNAME` alone is sufficient for v1.1's tool surface"
    - "A back-reference link to the section is added to docs/EXTENDING.md '## Further reading' so the section is discoverable from the existing TOC"
  artifacts:
    - path: "docs/EXTENDING.md"
      provides: "Public-doc home for the env-passthrough allowlist warning + the WR-04 rationale — closes audit W-3 + W-6"
      contains: "_PASSTHROUGH_ALLOWLIST"
  key_links:
    - from: "docs/EXTENDING.md '## Environment passthrough allowlist'"
      to: "src/mcp_test_framework/_isolation.py:33-36 forward-reference comment"
      via: "the in-source warning's reference 'DOC-07 in Phase 10' is now satisfied by a real EXTENDING.md anchor (and this plan extends DOC-07 with the SC-4-mandated allowlist warning that was the unaddressed half)"
    - from: "docs/EXTENDING.md '## Environment passthrough allowlist'"
      to: "Phase 06 ROADMAP SC-4 ('documented in code comments and EXTENDING.md')"
      via: "the EXTENDING.md half of SC-4, structurally deferred at Phase 06 closeout (see 06-VERIFICATION.md G-01) is now complete"
    - from: "docs/EXTENDING.md '## Further reading'"
      to: "docs/EXTENDING.md '## Environment passthrough allowlist'"
      via: "GitHub auto-anchor `#environment-passthrough-allowlist`"
sc_addressed:
  - "ROADMAP Phase 11 SC-2 (W-3): EXTENDING.md absorbs the _isolation.py:33-36 env-passthrough-allowlist warning"
  - "ROADMAP Phase 11 SC-6 (W-6 / WR-04): rationale recorded in docs/EXTENDING.md explaining why USERNAME alone is sufficient (Branch B disposition; no _isolation.py code change)"
---

<objective>
Add a new section `## Environment passthrough allowlist` to `docs/EXTENDING.md` that (a) absorbs the warning text currently living only in `src/mcp_test_framework/_isolation.py:33-36` so future contributors see it before reading source, and (b) records the WR-04 cross-platform-parity disposition (Branch B): a one-paragraph rationale explaining why POSIX `USER` is intentionally NOT in `_PASSTHROUGH_ALLOWLIST` and why `USERNAME` alone is sufficient for v1.1's tool surface.

Purpose: Close audit W-3 (Phase 06 SC-4 EXTENDING.md half — structurally deferred from Phase 06 to Phase 10, but Phase 10 shipped the per-tool walkthrough only and did not absorb the allowlist warning) AND audit W-6 (WR-04 disposition — adopt Branch B from the planning_context: rationale-only, no code change).

Output: One file modified. One new H2 section + one bullet added to "Further reading". Zero source-code changes. The `_isolation.py:33-36` warning stays in source unchanged (its forward-reference now resolves to a real EXTENDING.md anchor).

Branch B rationale (recorded for the executor): The audit explicitly classifies WR-04 as "informational only — `USERNAME` is diagnostic, no functional dependency for HOME redirect" (.planning/v1.1-MILESTONE-AUDIT.md line 124) and 06-VERIFICATION.md G-03 says "the HOME redirect (which is the actual isolation guarantee) does not depend on user identity" (line 233). The locked allowlist (D-07) names exactly USERNAME; widening to USER would require a CONTEXT.md amendment. The cleanest disposition is to document the deliberate POSIX-stripped behaviour in EXTENDING.md (which the existing inline comment in `_isolation.py:55-62` already justifies) and leave the code unchanged — same outcome the audit recommends.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v1.1-MILESTONE-AUDIT.md
@.planning/phases/06-per-session-host-state-isolation/06-VERIFICATION.md
@docs/EXTENDING.md
@src/mcp_test_framework/_isolation.py

<existing_extending_md_structure>
docs/EXTENDING.md (current state, for context — read it once before editing) has these H2 sections in order:

1. `# Extending mcp_test_framework` (H1 title) + introductory paragraphs
2. `## Add a new description-quality rubric`
3. `## Swap the judge backend`
4. `## Add a new MCP tool target`  ← Phase 10 deliverable (DOC-07)
5. `## Further reading`            ← bulleted link list, currently 5 entries (lines 154-158)

The new section MUST be inserted between section 4 and section 5 — i.e., AFTER `## Add a new MCP tool target` (which ends at line ~150) and BEFORE `## Further reading`.

Rationale for placement: Sections 2-4 are "extension recipes" (writer-facing). Section 5 is a TOC. Environment passthrough is a governance / safety-warning section that logically follows the recipes and precedes the TOC — same pattern other open-source projects use ("Contributing → Caveats → Further reading").
</existing_extending_md_structure>

<exact_section_content>
The new section MUST contain (verbatim H2 heading + the structural elements below). The executor may polish prose but MUST preserve every named element:

```markdown
## Environment passthrough allowlist

The framework spawns the MCP server subprocess with a deliberately narrow
environment. By default `mcp.client.stdio.stdio_client` would inherit the full
parent shell — your real `~/.homelab_mcp/` registry, OS keyring credentials,
AWS keys, `GITHUB_TOKEN`, and similar — into the subprocess under test. That
violates the "test runs do not mutate user state" guarantee documented in the
README's [Isolation guarantee](../README.md#isolation-guarantee) section.

Instead, `src/mcp_test_framework/_isolation.py` ships a module-level
`_PASSTHROUGH_ALLOWLIST` of exactly five entries:

- `PATH` — binary lookup for the MCP server command itself
- `SYSTEMROOT` — Windows DLL resolution; without it, Python interpreters in
  the spawned subprocess fail to import stdlib modules
- `LANG` — locale resolution for non-English server output
- `USERNAME` — informational; some servers log it for diagnostics
- `MCP_*` (prefix match) — pass-through for framework-set MCP env vars
  (e.g. `MCP_CONNECTION_NONBLOCKING`)

Plus five always-overridden vars (`HOME`, `USERPROFILE`, `TEMP`, `TMP`,
`TMPDIR`) that point at a per-session tempdir, and one keyring null-backend
override (`PYTHON_KEYRING_BACKEND=keyring.backends.null.Null`).

### Do not widen this allowlist without justification

This is the warning currently living at
`src/mcp_test_framework/_isolation.py:33-36`, copied verbatim so contributors
see it before reading source:

> DO NOT widen `_PASSTHROUGH_ALLOWLIST` without updating `EXTENDING.md`. Each
> new pass-through is a hole in the isolation guarantee and must be justified
> by a real subprocess need (e.g., locale resolution for a non-English
> server) — not "the test wouldn't run otherwise" without a root cause.

If you find yourself wanting to add an entry, the right workflow is:

1. **Identify the real need.** What does the spawned MCP server fail to do
   without the var? Capture a reproducer.
2. **Try the override path first.** Most "I need X env var" cases are
   actually "I need a redirected `HOME`" — see the existing `_HOME_OVERRIDES`
   tuple.
3. **If pass-through is genuinely required**, propose the change with a
   CONTEXT-style decision record explaining: (a) which subprocess behaviour
   requires it, (b) what threat surface it widens, (c) whether the var ever
   carries secrets (e.g. `AWS_PROFILE` does in some shells). Update this
   section's allowlist enumeration in the same PR.

### Why POSIX `USER` is NOT in the allowlist

A natural question reading the list above: on Windows the framework passes
`USERNAME` through, but on POSIX (Linux / macOS), `getpass.getuser()`
consults the `USER` env var and `USERNAME` is rarely set. The spawned
subprocess on POSIX therefore sees no user identity at all.

This is intentional and not a bug:

- The locked v1.1 allowlist (Phase 06 D-07) names exactly `USERNAME`. The
  HOME redirect — which IS the load-bearing isolation guarantee — does not
  depend on user identity (it's driven by `HOME` / `USERPROFILE` / `TEMP` /
  `TMP` / `TMPDIR`).
- `USERNAME` is documented in the source as "informational; some servers
  log it for diagnostics" (`_isolation.py:55-62`). It is not consulted by
  any v1.1 code path that affects test outcome.
- Widening the allowlist to include POSIX `USER` would require a CONTEXT.md
  amendment and a code change. The cross-platform-parity concern was
  explicitly recorded as informational in 06-VERIFICATION.md G-03 and
  v1.1-MILESTONE-AUDIT.md W-6.

If a future MCP server target genuinely needs `USER` for non-diagnostic
reasons (e.g. a server that derives a config path from
`getpass.getuser()`), follow the "Do not widen this allowlist without
justification" workflow above.
```

The above prose is the floor, not the ceiling — the executor may tighten
wording or improve flow, but every NAMED element below MUST appear:

- H2 heading: exactly `## Environment passthrough allowlist`
- A reference to the README isolation-guarantee anchor: `../README.md#isolation-guarantee`
- The five allowlist entries enumerated by name: PATH, SYSTEMROOT, LANG, USERNAME, MCP_*
- The five HOME-override vars enumerated: HOME, USERPROFILE, TEMP, TMP, TMPDIR
- The keyring null-backend reference: `PYTHON_KEYRING_BACKEND=keyring.backends.null.Null`
- A blockquote (or visually-distinct quoted block) of the warning text
  copied from `_isolation.py:33-36` (the four-line warning starting with
  "DO NOT widen `_PASSTHROUGH_ALLOWLIST`")
- The literal token `_PASSTHROUGH_ALLOWLIST` appearing ≥3 times across the section
- A subsection (H3) heading "Why POSIX `USER` is NOT in the allowlist"
- The token `USERNAME` appearing in that subsection
- A reference to either "v1.1-MILESTONE-AUDIT.md W-6" or "06-VERIFICATION.md G-03" (or both) so a future verifier can trace the disposition

PART 2 — Further reading bullet:

After the new section is inserted, add ONE new bullet to the existing
`## Further reading` list (currently 5 entries at lines 154-158 of
EXTENDING.md). The new bullet, inserted as the last entry in that list,
should read:

```
- [`Environment passthrough allowlist`](#environment-passthrough-allowlist) — what the spawned MCP subprocess inherits, and why widening the list is dangerous
```

The anchor `#environment-passthrough-allowlist` is the GitHub auto-slug of
the new H2 heading. Verify the slug by running through GitHub's known
heading-slugification rule: lowercase, spaces → hyphens, all other
non-alphanumeric stripped. `Environment passthrough allowlist` →
`environment-passthrough-allowlist`. Match exactly.
</exact_section_content>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add '## Environment passthrough allowlist' section to docs/EXTENDING.md</name>
  <files>docs/EXTENDING.md</files>
  <read_first>
    - docs/EXTENDING.md (entire file — it is short, ~158 lines; verify the section ordering described in `<existing_extending_md_structure>` and locate the exact insertion point between "## Add a new MCP tool target" and "## Further reading")
    - src/mcp_test_framework/_isolation.py (entire module — short, ~108 lines; specifically the module docstring at lines 1-40 and the inline comments on each allowlist entry at lines 50-62 — these are the source-of-truth for the warning text and the USERNAME-vs-USER rationale)
    - .planning/v1.1-MILESTONE-AUDIT.md (W-3 section in tech_debt at line 22-23 + W-6 at line 124)
    - .planning/phases/06-per-session-host-state-isolation/06-VERIFICATION.md (G-01 at lines 203-215 + G-03 at lines 227-235 — the verifier's canonical write-up of the EXTENDING.md gap and the WR-04 advisory)
    - README.md (only to confirm the `## Isolation guarantee` heading exists and resolves at the GitHub auto-anchor `#isolation-guarantee` — the new section back-links to it)
  </read_first>
  <action>
    Open `docs/EXTENDING.md`. Locate the closing prose of the existing `## Add a new MCP tool target` section (the paragraph that mentions "Typos in field names are caught at config load by `extra=\"forbid\"`..." — currently around line 150).

    INSERT a new H2 section immediately after that paragraph and before `## Further reading`, using the content described in `<exact_section_content>` Part 1 above. The H2 heading MUST be exactly `## Environment passthrough allowlist`. The section MUST contain:
      - All five `_PASSTHROUGH_ALLOWLIST` entries enumerated by name (PATH, SYSTEMROOT, LANG, USERNAME, MCP_*)
      - All five `_HOME_OVERRIDES` vars enumerated by name (HOME, USERPROFILE, TEMP, TMP, TMPDIR)
      - The keyring null-backend reference verbatim (`PYTHON_KEYRING_BACKEND=keyring.backends.null.Null`)
      - The warning blockquote copied from `_isolation.py:33-36` (the four-line "DO NOT widen ..." warning)
      - An H3 subsection `### Why POSIX \`USER\` is NOT in the allowlist` recording the WR-04 / Branch B rationale
      - A reference to either `v1.1-MILESTONE-AUDIT.md W-6` or `06-VERIFICATION.md G-03` for traceability

    THEN, edit the existing `## Further reading` list (currently 5 bullets) to add ONE new bullet at the END:
      `- [\`Environment passthrough allowlist\`](#environment-passthrough-allowlist) — what the spawned MCP subprocess inherits, and why widening the list is dangerous`

    Do NOT remove or reorder any existing Further-reading bullets. Do NOT modify any other section. Do NOT change `_isolation.py` (the in-source warning at lines 33-36 stays as-is — its forward-reference now resolves to a real anchor).
  </action>
  <verify>
    <automated>grep -q "^## Environment passthrough allowlist$" docs/EXTENDING.md && grep -q "^### Why POSIX \`USER\` is NOT in the allowlist$" docs/EXTENDING.md && grep -c "_PASSTHROUGH_ALLOWLIST" docs/EXTENDING.md && grep -q "DO NOT widen" docs/EXTENDING.md && grep -q "PYTHON_KEYRING_BACKEND=keyring.backends.null.Null" docs/EXTENDING.md && grep -q "USERNAME" docs/EXTENDING.md && grep -q "(#environment-passthrough-allowlist)" docs/EXTENDING.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^## Environment passthrough allowlist$" docs/EXTENDING.md` returns 1 (the H2 heading exists exactly once)
    - `grep -c "^### Why POSIX \`USER\` is NOT in the allowlist$" docs/EXTENDING.md` returns 1 (the H3 subheading exists exactly once)
    - `grep -c "_PASSTHROUGH_ALLOWLIST" docs/EXTENDING.md` returns ≥3 (allowlist token appears multiple times in the new section)
    - `grep -c "DO NOT widen" docs/EXTENDING.md` returns ≥1 (the warning blockquote is present)
    - `grep -c "PATH" docs/EXTENDING.md` returns ≥1 within the new section (verify by sectioning grep output; the token appears in the allowlist enumeration)
    - `grep -c "SYSTEMROOT" docs/EXTENDING.md` returns ≥1
    - `grep -c "USERPROFILE" docs/EXTENDING.md` returns ≥1
    - `grep -c "TMPDIR" docs/EXTENDING.md` returns ≥1
    - `grep -c "PYTHON_KEYRING_BACKEND=keyring.backends.null.Null" docs/EXTENDING.md` returns ≥1
    - `grep -c "USERNAME" docs/EXTENDING.md` returns ≥2 (appears both in allowlist enumeration and in the H3 rationale subsection)
    - `grep -cE "(W-6|G-03)" docs/EXTENDING.md` returns ≥1 (audit-trail reference for the WR-04 disposition)
    - `grep -c "(#environment-passthrough-allowlist)" docs/EXTENDING.md` returns ≥1 (the new Further-reading bullet anchor link)
    - `grep -c "^## " docs/EXTENDING.md` returns 5 (originally 4 H2 sections + 1 new = 5; verifies no other section was added or removed)
    - The existing `## Further reading` section still contains all 5 original bullets PLUS the new one (`grep -cE "^- \[" docs/EXTENDING.md` within the Further-reading region — the new bullet brings the count from 5 to 6 in that section; total `^- \[` lines in file may differ depending on other lists)
    - `src/mcp_test_framework/_isolation.py` is UNCHANGED (`git diff src/mcp_test_framework/_isolation.py` returns empty — the WR-04 disposition is Branch B, no code change)
  </acceptance_criteria>
  <done>
    docs/EXTENDING.md has a new `## Environment passthrough allowlist` H2 section between "Add a new MCP tool target" and "Further reading", containing the verbatim allowlist enumeration, the warning blockquote, and an H3 subsection on USERNAME-vs-USER. "Further reading" gains one new bullet pointing at the new section. `_isolation.py` is untouched.
  </done>
</task>

</tasks>

<threat_model>
This plan is paper-only doc edits. WR-04 disposition is Branch B (rationale only, no code change), so no allowlist literal is modified.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Information disclosure | docs/EXTENDING.md content | accept | The new section enumerates env-var names already documented in the public README and in `_isolation.py` (which ships in the package). No secrets or homelab-specific identifiers introduced. |
| T-11-04-02 | Drift between source warning and doc warning | _isolation.py:33-36 vs EXTENDING.md blockquote | accept | The audit explicitly recommends absorbing the warning into EXTENDING.md (W-3); having the same text in two places is the goal, not a bug. If the source warning is later updated, EXTENDING.md should follow — track via grep on either string. |
</threat_model>

<verification>
- New H2 section inserted at the correct location (after "## Add a new MCP tool target", before "## Further reading")
- All five allowlist entries + five HOME-override vars enumerated by name
- Warning blockquote present (substring "DO NOT widen")
- H3 subsection records the WR-04 / USER-vs-USERNAME rationale
- One new bullet added to Further reading; existing bullets preserved
- `_isolation.py` is unchanged (Branch B disposition)
- File still parses as valid markdown (no broken fenced blocks, list nesting consistent)
</verification>

<success_criteria>
- [ ] `## Environment passthrough allowlist` section exists in docs/EXTENDING.md
- [ ] All five `_PASSTHROUGH_ALLOWLIST` entries (PATH, SYSTEMROOT, LANG, USERNAME, MCP_*) are enumerated by name
- [ ] Warning blockquote ("DO NOT widen ...") is present
- [ ] H3 "Why POSIX `USER` is NOT in the allowlist" subsection records the WR-04 disposition
- [ ] Further reading list gains one bullet linking to the new section
- [ ] `src/mcp_test_framework/_isolation.py` is unchanged (Branch B = no code change)
- [ ] No other docs/EXTENDING.md sections modified
</success_criteria>

<output>
After completion, create `.planning/phases/11-v1-1-cleanup-verification-hygiene/11-04-SUMMARY.md` per the standard summary template, citing audit W-3 + W-6 closure, recording the Branch B disposition for WR-04 (rationale-only, no code change), and listing the new section's anchor.
</output>
