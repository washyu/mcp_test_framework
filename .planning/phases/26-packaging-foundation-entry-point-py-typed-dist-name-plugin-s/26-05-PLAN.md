---
phase: 26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s
plan: 05
type: execute
wave: 4
depends_on: [26-01, 26-02, 26-03, 26-04]
files_modified:
  - README.md
  - CLAUDE.md
autonomous: false
requirements: [PACK-01, PACK-02, PACK-03, PACK-04]
requirements_addressed: [PACK-01, PACK-02, PACK-03, PACK-04]
tags: [docs-sweep, testpypi, verification]

user_setup:
  - service: TestPyPI
    why: "Phase 26 SC1 verification — `pip install mcp-contracts` from TestPyPI proves the published wheel is consumable end-to-end. Production PyPI publish defers to Phase 30 per CONTEXT.md Claude's Discretion."
    env_vars:
      - name: UV_PUBLISH_TOKEN
        source: "TestPyPI -> Account settings -> API tokens -> Add API token (scope: project-wide on first publish; tighten to per-project after)"
    dashboard_config:
      - task: "Create a TestPyPI account if none exists"
        location: "https://test.pypi.org/account/register/"
      - task: "Generate an API token scoped for first-time upload of mcp-contracts"
        location: "https://test.pypi.org/manage/account/token/"

must_haves:
  truths:
    - "README.md operator-facing prose uses `mcp-contracts` script name everywhere except the explicit `mcp-test-framework` deprecation note"
    - "All operator-facing docs (README, CLAUDE.md, inline CLI docstrings) reference the new `mcp-contracts` script name; the old `mcp-test-framework` name appears ONLY inside the canonical verbatim D-07 deprecation message and nowhere else (D-08)"
    - "CLAUDE.md `## Tooling` command examples use `mcp-contracts` script name"
    - "CLAUDE.md deprecation note quotes the **verbatim D-07 hardcoded copy** in backticks (revision 1 fix — plan-checker Issue #4 — so a single grep finds every site of the deprecation string for v1.5 cleanup)"
    - "TestPyPI publish recipe documented in plan with the exact `uv` (or `twine` fallback) command sequence and credential-handling guidance"
    - "Fresh-venv install recipe documented: `pip install --index-url https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple/ mcp-contracts`"
    - "Local acceptance recipe documented: `pytest --trace-config` shows `mcp_test_framework` plugin AND `python -c \"import mcp_test_framework, mcp_test_framework.contracts, mcp_test_framework.test_code\"` succeeds in the fresh venv"
  artifacts:
    - path: "README.md"
      provides: "Operator-facing prose using the new script name + deprecation paragraph"
      contains: "mcp-contracts"
    - path: "CLAUDE.md"
      provides: "Project guidance using the new script name + verbatim D-07 deprecation copy"
      contains: "mcp-contracts"
  key_links:
    - from: "Operator reading README"
      to: "`pip install mcp-contracts` install command"
      via: "README §Install / §Usage prose"
      pattern: "pip install mcp-contracts"
    - from: "TestPyPI publish (manual, gated checkpoint)"
      to: "fresh-venv install verification"
      via: "`uv publish --publish-url https://test.pypi.org/legacy/` then `pip install --index-url https://test.pypi.org/simple/`"
      pattern: "test.pypi.org"
    - from: "v1.5 cleanup grep"
      to: "all three deprecation-copy sites (`_deprecated_script.py`, README.md, CLAUDE.md)"
      via: "literal string `mcp-test-framework command is deprecated since v1.4`"
      pattern: "mcp-test-framework command is deprecated since v1.4"
---

<objective>
Close Phase 26 end-to-end: sweep operator-facing docs (README + CLAUDE.md) for the new `mcp-contracts` script name (legacy name only in the verbatim D-07 deprecation note), and gate the phase complete behind a TestPyPI dry-run + local install acceptance test. The `.planning/REQUIREMENTS.md` PACK-03 + `.planning/ROADMAP.md` Phase 26 Goal/SC1 amendments were moved from this plan to Plan 26-01 (Wave 1) during revision 1 (plan-checker Issue #5) so source-of-truth docs are correct from Wave 1 onward.

**Purpose:** Wave 4 — depends on Waves 1+2+3 because the wheel must be built-and-verified (Wave 3 gate) AND the docs need to reflect what was actually shipped. This plan has one checkpoint: the TestPyPI publish requires a human-supplied API token (CONTEXT.md `user_setup` for TestPyPI). Everything else is automated.

**Output:** README + CLAUDE.md sweep + a successful TestPyPI publish + a recorded fresh-venv install verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s/26-CONTEXT.md
@.planning/phases/26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s/26-PATTERNS.md
@.planning/phases/26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s/26-01-PLAN.md
@.planning/phases/26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s/26-04-PLAN.md
@README.md
@CLAUDE.md
@pyproject.toml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Sweep README.md + CLAUDE.md for the new `mcp-contracts` script name + add the verbatim D-07 deprecation note</name>
  <files>
    README.md,
    CLAUDE.md
  </files>
  <read_first>
    - README.md (every occurrence of `mcp-test-framework` and `mvp-test-framework`; identify which are operator-facing prose vs (a) deprecation notes, (b) literal snippets of pyproject content, (c) historical phase references in roadmap-style sections)
    - CLAUDE.md (the `## Tooling` block in particular — current `uv run mcp-test-framework ...` examples need to flip to `mcp-contracts`; `Project Status`, `Project Reference`, and the v1.4 framing paragraphs preserve their references to the legacy name where they describe v1.3-or-earlier behavior)
    - src/mcp_test_framework/_deprecated_script.py (Plan 26-02 Task 3 creates this — confirm the literal D-07 copy `mcp-test-framework command is deprecated since v1.4 and will be removed in v1.5 — use mcp-contracts instead.` exists; this task quotes that string verbatim in README + CLAUDE.md so a single grep finds every site for v1.5 cleanup)
    - .planning/phases/26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s/26-PATTERNS.md "Docs updates" section (lines 471-487 — the replace logic + post-edit grep audit)
    - .planning/phases/26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s/26-CONTEXT.md D-07, D-08
  </read_first>
  <action>
**Audit-first, edit-second.** Begin by running this grep across the two files and noting every match with line number + surrounding context:

```bash
grep -n -E 'mcp-test-framework|mvp-test-framework' README.md CLAUDE.md
```

For each match, classify into one of:

- **A. Operator-facing prose / command example** (e.g., `pip install mcp-test-framework`, `uv run mcp-test-framework run`, `mcp-test-framework gen-test-classes`) → **REWRITE** to `mcp-contracts` / `uv run mcp-contracts run` / etc.
- **B. Deprecation note / legacy-script context** (e.g., a paragraph explicitly describing the v1.4 shim) → **KEEP** the old name as it is the subject of the note
- **C. Historical phase reference** (e.g., a sentence in CLAUDE.md that describes Phase 21's CLI behavior) → **KEEP** the old name if it is contextually anchored to a pre-Phase-26 description; **REWRITE** if it describes current/future operator behavior
- **D. Verbatim snippet of `pyproject.toml` content** (rare — only if the README has a code block showing `[project.scripts]`) → **REWRITE** to match the new pyproject content (`mcp-contracts = ...` + `mcp-test-framework = "mcp_test_framework._deprecated_script:main"`)

**Edit 1 — README.md operator-facing rewrites:**

For every Class-A match in README.md, replace `mcp-test-framework` with `mcp-contracts`. Preserve surrounding markdown structure, code-fence languages, and inline-code backticks.

**Edit 2 — README.md deprecation paragraph (NEW):**

Add a new paragraph somewhere in the README's "Install" or "CLI" section (whichever exists; if neither, place it directly after the first `mcp-contracts` command example). The paragraph contains EXACTLY this text — the backticked string MUST be the **verbatim D-07 hardcoded copy** that also appears in `src/mcp_test_framework/_deprecated_script.py:main`:

> **Legacy script alias:** the `mcp-test-framework` console-script is retained in v1.4 as a back-compat shim. Invoking it emits a `DeprecationWarning` carrying the literal copy `mcp-test-framework command is deprecated since v1.4 and will be removed in v1.5 — use mcp-contracts instead.` and then dispatches to the same Typer app. The shim drops in v1.5 alongside every other Phase 25 / Phase 26 deprecation.

The backticked deprecation string above MUST appear verbatim — character-for-character identical to the string in `_deprecated_script.py:main` (D-07 hardcoded copy). This makes the v1.5 cleanup sweep a single `grep "mcp-test-framework command is deprecated since v1.4"` across the repo (catches all three sites: the script shim source, this README paragraph, and the CLAUDE.md note added in Edit 3).

**Edit 3 — CLAUDE.md `## Tooling` block rewrites + verbatim D-07 deprecation note (revision 1 fix — plan-checker Issue #4):**

The current `## Tooling` block has command examples like `uv run mcp-test-framework list-tools` and `uv run mcp-test-framework run`. Rewrite every Class-A occurrence in that block to `uv run mcp-contracts ...`. Then, immediately after that command-block, add this single paragraph quoting the **verbatim D-07 hardcoded copy** in backticks (so the same `grep "mcp-test-framework command is deprecated since v1.4"` finds this CLAUDE.md site too):

```
The legacy `uv run mcp-test-framework` invocation continues to work in v1.4 with a DeprecationWarning carrying the literal copy `mcp-test-framework command is deprecated since v1.4 and will be removed in v1.5 — use mcp-contracts instead.`; the alias drops in v1.5 alongside every other Phase 25 / Phase 26 deprecation shim.
```

**Why verbatim (not paraphrase) — revision 1 fix:** the plan-checker (Issue #4) flagged the prior paraphrased version because D-08 requires the old script name to appear only inside the canonical deprecation message; paraphrase drift weakens the v1.5 cleanup grep. By embedding the **same literal string** that lives in `_deprecated_script.py:main`, a single `grep "mcp-test-framework command is deprecated since v1.4 and will be removed in v1.5"` enumerates every site for v1.5 removal — script shim, README paragraph, CLAUDE.md note.

**Edit 4 — Distribution-name references:**

For any line that says `pip install mcp-test-framework` (Class-A install-command example) → rewrite to `pip install mcp-contracts`. For any line that says `uv add mcp-test-framework` → rewrite to `uv add mcp-contracts`. For any line that says `mvp-test-framework` outside historical context → rewrite to `mcp-contracts`.

**Post-edit grep audit — must pass before this task is done:**

After all edits:

```bash
grep -n -E 'mcp-test-framework|mvp-test-framework' README.md CLAUDE.md
```

Every remaining match must be classifiable as **B** (deprecation note containing the verbatim D-07 copy) or **C** (historical reference unambiguously anchored to pre-Phase-26 context). If a remaining match is Class A or D, fix it.

**DO NOT:**
- Touch `docs/` files in this plan unless they are referenced by README or CLAUDE.md and contain operator-facing prose (the v1.5 cleanup phase will sweep `docs/` comprehensively; Phase 26 D-08 limits the doc surface to README + CLAUDE + inline CLI docstrings, and the inline CLI docstrings were updated in Plan 26-01 Task 2)
- Modify any `.planning/` files in this task — REQUIREMENTS.md + ROADMAP.md amendments were moved to Plan 26-01 Task 3 during revision 1 (plan-checker Issue #5)
- Rewrite historical phase references where the legacy name is part of the narrative of an already-shipped phase (e.g., "Phase 25 added the `mcp-test-framework gen-sdet-classes` shim" — that sentence stays as-is because it describes a phase that shipped using that name; Phase 26's renaming does not retroactively edit history)
- **Paraphrase the deprecation copy** — the backticked string in Edits 2 and 3 MUST be character-identical to `_deprecated_script.py:main`'s D-07 hardcoded copy (revision 1 fix — see "Why verbatim" above)
- Add the deprecation paragraph more than once in README.md or more than once in CLAUDE.md
  </action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; r=Path('README.md').read_text(encoding='utf-8'); c=Path('CLAUDE.md').read_text(encoding='utf-8'); lit='mcp-test-framework command is deprecated since v1.4 and will be removed in v1.5 — use mcp-contracts instead.'; assert 'mcp-contracts' in r and 'mcp-contracts' in c; assert lit in r, 'README must contain verbatim D-07 copy'; assert lit in c, 'CLAUDE.md must contain verbatim D-07 copy'; print('docs swept ok')"</automated>
  </verify>
  <done>
    - `grep -c 'mcp-contracts' README.md` returns at least 3 (install command + CLI example + deprecation paragraph at minimum)
    - `grep -c 'mcp-contracts' CLAUDE.md` returns at least 2 (the rewritten `## Tooling` examples)
    - `grep -n 'mcp-test-framework' README.md CLAUDE.md` matches are ALL classifiable as Class B (deprecation note with verbatim D-07 copy) or Class C (historical/Phase-25-or-earlier narrative)
    - The deprecation paragraph in README.md appears EXACTLY once and contains the verbatim D-07 string `mcp-test-framework command is deprecated since v1.4 and will be removed in v1.5 — use mcp-contracts instead.`
    - The CLAUDE.md `## Tooling` block has been rewritten to use `mcp-contracts` everywhere except in the post-block deprecation note, which contains the verbatim D-07 string `mcp-test-framework command is deprecated since v1.4 and will be removed in v1.5 — use mcp-contracts instead.`
    - A single `grep "mcp-test-framework command is deprecated since v1.4" README.md CLAUDE.md src/mcp_test_framework/_deprecated_script.py` finds all 3 sites (proves the verbatim-copy invariant for v1.5 cleanup)
  </done>
  <acceptance_criteria>
    - grep `mcp-contracts` in `README.md` returns at least 3 matches
    - grep `mcp-contracts` in `CLAUDE.md` returns at least 2 matches
    - grep `mcp-test-framework command is deprecated since v1.4 and will be removed in v1.5 — use mcp-contracts instead.` (the literal verbatim D-07 hardcoded copy) in `README.md` returns exactly 1 match
    - grep `mcp-test-framework command is deprecated since v1.4 and will be removed in v1.5 — use mcp-contracts instead.` (the literal verbatim D-07 hardcoded copy) in `CLAUDE.md` returns exactly 1 match
    - grep `mcp-test-framework command is deprecated since v1.4` across `README.md CLAUDE.md src/mcp_test_framework/_deprecated_script.py` returns exactly 3 matches (the three intended verbatim-copy sites: script shim + README paragraph + CLAUDE.md note — single source-greppable invariant for v1.5 cleanup)
    - grep `pip install mcp-test-framework` in `README.md` returns 0 matches (rewritten to `pip install mcp-contracts`)
    - grep `pip install mvp-test-framework` in `README.md` returns 0 matches (legacy dist name fully removed from install examples)
    - grep `uv run mcp-test-framework` in `CLAUDE.md` (outside the deprecation-note line) returns 0 matches — easily checked by `grep -n 'uv run mcp-test-framework' CLAUDE.md` and confirming any remaining hit is the literal deprecation-note line you added
    - `uv run python -c "from pathlib import Path; r=Path('README.md').read_text(encoding='utf-8'); c=Path('CLAUDE.md').read_text(encoding='utf-8'); lit='mcp-test-framework command is deprecated since v1.4 and will be removed in v1.5 — use mcp-contracts instead.'; assert 'mcp-contracts' in r and 'mcp-contracts' in c and lit in r and lit in c"` exits 0
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 [CHECKPOINT]: Publish wheel to TestPyPI</name>
  <what-built>
    Plans 26-01 through 26-04 produced a wheel that contains every required artifact and is locally verified by the wheel-shape gate. This checkpoint asks the user to publish that wheel to **TestPyPI** so the install pathway can be exercised end-to-end. Production PyPI publish defers to Phase 30.
  </what-built>
  <how-to-verify>
    **1. Confirm prerequisites:**
    - TestPyPI account exists (https://test.pypi.org/account/register/)
    - API token generated with scope "project-wide on first publish" (https://test.pypi.org/manage/account/token/)
    - The token is exported as `UV_PUBLISH_TOKEN` in the current shell (or stored in `~/.pypirc` / `keyring` for `twine` fallback)

    **2. Build the wheel fresh (matches what `test_wheel_shape.py` builds):**
    ```bash
    rm -rf dist/
    uv build --wheel
    ls dist/
    ```
    Expected: a single file `dist/mcp_contracts-0.1.0-py3-none-any.whl`. If `dist/` already exists from a prior build, delete it first to avoid republishing a stale wheel.

    **3. Publish to TestPyPI:**

    *Primary path (uv ≥ 0.5.0):*
    ```bash
    UV_PUBLISH_TOKEN=<token-value> uv publish --publish-url https://test.pypi.org/legacy/ dist/mcp_contracts-0.1.0-py3-none-any.whl
    ```

    *Fallback (twine, if `uv publish` is not available in your local uv version):*
    ```bash
    uv tool run --from twine twine upload --repository testpypi dist/mcp_contracts-0.1.0-py3-none-any.whl
    # When prompted: username is `__token__`, password is the API token value (including the `pypi-` prefix).
    ```

    Expected output: a confirmation URL of the form `https://test.pypi.org/project/mcp-contracts/0.1.0/`.

    **4. Verify in a fresh venv (this is the SC1 acceptance evidence):**

    ```bash
    # In a tmp dir, NOT inside the project's working tree:
    cd $(mktemp -d)
    uv venv .venv-mcp-contracts-testpypi --python 3.14
    source .venv-mcp-contracts-testpypi/bin/activate    # Windows PowerShell: .venv-mcp-contracts-testpypi\Scripts\Activate.ps1
    pip install --index-url https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple/ mcp-contracts
    ```

    Expected: `Successfully installed mcp-contracts-0.1.0 ...` (plus all transitive deps from PyPI proper via the `--extra-index-url` fallback).

    **5. Run the three acceptance checks (SC1 / SC2 / SC3):**

    ```bash
    # SC1: pip install succeeded → already proven by step 4

    # SC2: plugin auto-loads
    mkdir empty-conftest-proj && cd empty-conftest-proj
    echo "" > conftest.py
    pytest --trace-config 2>&1 | grep -E 'mcp_test_framework' || echo "PLUGIN NOT AUTO-LOADED"
    # Expected: at least one line mentioning `mcp_test_framework` from the trace-config output.

    # SC3: typed imports resolve cleanly
    python -c "import mcp_test_framework; import mcp_test_framework.contracts; import mcp_test_framework.test_code; print('all 3 imports OK')"
    # Expected: `all 3 imports OK`

    # Also verify the console scripts are wired:
    mcp-contracts version
    # Expected: `0.1.0` (or whatever __version__ resolves to)
    mcp-test-framework version 2>&1 | head -5
    # Expected: a DeprecationWarning line PLUS `0.1.0` on stdout (D-06/D-07 shim behavior).
    ```

    **6. Record the evidence in `26-05-SUMMARY.md`:**
    - The TestPyPI project URL
    - The `pip install` success line (sanitized of timestamps if you wish)
    - The `pytest --trace-config` line(s) mentioning `mcp_test_framework`
    - The `python -c "import ..."` output
    - The `mcp-contracts version` output
    - The `mcp-test-framework version` output (showing the DeprecationWarning)

    **7. Tear down the test venv:**
    ```bash
    deactivate
    rm -rf .venv-mcp-contracts-testpypi    # PowerShell: Remove-Item -Recurse -Force .venv-mcp-contracts-testpypi
    ```
  </how-to-verify>
  <resume-signal>
    Type `approved` once the TestPyPI publish + fresh-venv verification succeeds and the evidence has been pasted into `26-05-SUMMARY.md`. Type `issues: <description>` if any step fails.

    **Known issue paths:**
    - If `uv publish` is not available: use the twine fallback in step 3.
    - If `pytest --trace-config` doesn't mention `mcp_test_framework`: the `[project.entry-points.pytest11]` declaration didn't make it into the wheel; rerun `tests/framework/test_wheel_shape.py::test_wheel_declares_pytest11_entry_point` locally to confirm before resuming.
    - If `mcp-test-framework version` does NOT emit a DeprecationWarning: the `_deprecated_script.main` body or its console-script wiring is broken; revisit Plan 26-02 Task 3 acceptance criteria.
    - If TestPyPI rejects the upload with "filename already exists": bump the patch version in `pyproject.toml` (e.g., `0.1.0` → `0.1.0.dev1`) — TestPyPI is immutable per file; this is normal for republish attempts.
  </resume-signal>
  <action>
This task is a checkpoint — Claude pauses here while the user executes the TestPyPI publish. The instructions in `<how-to-verify>` are the script the user follows. Claude resumes after `approved`.
  </action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; s=Path('.planning/phases/26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s/26-05-SUMMARY.md'); assert s.exists(), 'SUMMARY.md must record TestPyPI evidence before resume'"</automated>
  </verify>
  <done>
    - TestPyPI lists `mcp-contracts 0.1.0` at `https://test.pypi.org/project/mcp-contracts/`
    - Fresh-venv `pip install --index-url https://test.pypi.org/simple/ --extra-index-url https://pypi.org/simple/ mcp-contracts` succeeds
    - `pytest --trace-config` from an empty-conftest project lists `mcp_test_framework` as an auto-discovered plugin (SC2 / PACK-01 evidence)
    - `python -c "import mcp_test_framework, mcp_test_framework.contracts, mcp_test_framework.test_code"` exits 0 in the fresh venv (SC3 / PACK-02 evidence)
    - `mcp-contracts version` and `mcp-test-framework version` both exit 0 in the fresh venv; the latter emits a DeprecationWarning per D-06/D-07
    - All evidence pasted into `26-05-SUMMARY.md`
  </done>
  <acceptance_criteria>
    - `26-05-SUMMARY.md` exists and contains the literal string `https://test.pypi.org/project/mcp-contracts/`
    - `26-05-SUMMARY.md` contains evidence of `pip install mcp-contracts` succeeding against the TestPyPI index
    - `26-05-SUMMARY.md` contains a `pytest --trace-config` output snippet mentioning `mcp_test_framework`
    - `26-05-SUMMARY.md` contains a `python -c "import mcp_test_framework, mcp_test_framework.contracts, mcp_test_framework.test_code"` success line
    - `26-05-SUMMARY.md` contains the `mcp-contracts version` output line
    - `26-05-SUMMARY.md` contains the `mcp-test-framework version` output line showing the `DeprecationWarning: mcp-test-framework command is deprecated since v1.4 ...` text
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → TestPyPI publish endpoint | API token authenticates the upload; TestPyPI is the publish target, NOT production PyPI |
| TestPyPI index → fresh-venv `pip install` | Public index; resolver pulls deps from `--extra-index-url` (production PyPI) for the transitive graph |
| `~/.pypirc` / shell env → `UV_PUBLISH_TOKEN` | API token must NEVER be committed to the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-05-01 | Information disclosure | API token committed to repo | mitigate | Token is supplied via shell env (`UV_PUBLISH_TOKEN`) per checkpoint instructions; `.gitignore` covers `.env`, `.pypirc` already; checkpoint explicitly warns the user not to paste the literal token into `26-05-SUMMARY.md` (the evidence is the *project URL*, not the credential) |
| T-26-05-02 | Tampering | TestPyPI MITM replacing wheel during install | accept | TestPyPI uses HTTPS; the `--index-url`/`--extra-index-url` combo trusts the same CA bundle as production PyPI. This risk is inherited from `pip` itself, not unique to Phase 26 |
| T-26-05-03 | Spoofing | typosquat on `mcp-contracts` name | mitigate | First-publish takes the name on TestPyPI; D-01 verified availability 2026-05-15. If a typosquat appears between 2026-05-15 and the publish, the upload fails loudly with a name-conflict error |
| T-26-05-04 | Repudiation | doc sweep silently leaving Class-A `mcp-test-framework` references | mitigate | Task 1 acceptance criteria grep-count every Class-A occurrence; post-edit audit re-runs the grep |
| T-26-05-05 | Elevation of privilege | inadvertent production-PyPI publish instead of TestPyPI | mitigate | The `uv publish --publish-url https://test.pypi.org/legacy/` URL is **explicit** — no default-target ambiguity. The twine fallback uses `--repository testpypi` which is similarly explicit. CONTEXT.md "Claude's Discretion" defers prod publish to Phase 30 |
| T-26-05-06 | DoS | TestPyPI uploads with same-version filename rejecting subsequent attempts | accept | If republish needed, bump `version` in `pyproject.toml` to `0.1.0.dev1`; this is standard TestPyPI dry-run hygiene and is called out in the checkpoint's "Known issue paths" |
| T-26-05-07 | Tampering | paraphrased deprecation copy weakening v1.5 cleanup grep | mitigate | Revision 1 plan-checker Issue #4 fix: README + CLAUDE.md notes embed the verbatim D-07 string from `_deprecated_script.py:main`; Task 1 acceptance enumerates all 3 sites via single grep |
</threat_model>

<verification>
- `grep mcp-test-framework README.md CLAUDE.md` returns only Class-B / Class-C matches; the Class-B matches contain the verbatim D-07 copy
- `grep "mcp-test-framework command is deprecated since v1.4" README.md CLAUDE.md src/mcp_test_framework/_deprecated_script.py` returns exactly 3 matches (verbatim-copy invariant)
- TestPyPI lists `mcp-contracts 0.1.0`
- Fresh-venv install + auto-load + typed-import + both console-scripts verifications captured in `26-05-SUMMARY.md`
- (Note: REQUIREMENTS.md PACK-03 + ROADMAP.md Phase 26 Goal/SC1 amendments are NO LONGER this plan's responsibility — they were moved to Plan 26-01 Task 3 during revision 1, plan-checker Issue #5)
</verification>

<success_criteria>
- All <acceptance_criteria> on both tasks pass
- README + CLAUDE swept; deprecation paragraphs added exactly once each and quote the verbatim D-07 copy
- TestPyPI publish succeeded; `mcp-contracts 0.1.0` consumable via `pip install --index-url https://test.pypi.org/simple/`
- Fresh-venv evidence captured: plugin auto-load (PACK-01 / SC2), typed imports (PACK-02 / SC3), console-script primary + shim (D-05 / D-06 / D-07)
- Phase 26 is now closeable via `/gsd-verify-phase 26` — every requirement (PACK-01..04) has been exercised against a built and TestPyPI-published wheel
</success_criteria>

<output>
After completion, finalize `.planning/phases/26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s/26-05-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Required sections:
- The doc-sweep grep audit output (before + after) — including the all-3-sites verbatim-copy grep
- The TestPyPI publish URL + fresh-venv install evidence (per the checkpoint's "record the evidence" instruction)
- Deferred-to-Phase-30 callout: production PyPI publish + README rewrite leading with library mode (CLOSE-01..04)
- The state of `--config` / SDET-specific carry-forwards (NONE in Phase 26 — packaging foundation is hermetic with respect to operator-config concerns; Phase 28 owns CFG-01/02)
- Cross-reference: the REQUIREMENTS.md PACK-03 + ROADMAP.md Phase 26 Goal/SC1 amendments lived here in the original plan but were moved to Plan 26-01 Task 3 during revision 1 (plan-checker Issue #5); reference 26-01-SUMMARY.md for those amendment diffs
</output>
</content>
</invoke>
