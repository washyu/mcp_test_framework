---
phase: 26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_test_framework/py.typed
  - src/mcp_test_framework/contracts/__init__.py
  - src/mcp_test_framework/contracts/py.typed
  - src/mcp_test_framework/test_code/py.typed
autonomous: true
requirements: [PACK-02]
requirements_addressed: [PACK-02]
tags: [py-typed, pep-561, contracts-subpackage]

must_haves:
  truths:
    - "`from mcp_test_framework.contracts import ...` resolves to an existent (empty) subpackage (D-13)"
    - "`mcp_test_framework/py.typed` exists (PEP 561 marker for the root package)"
    - "`mcp_test_framework/test_code/py.typed` exists (PEP 561 marker for the test_code subpackage)"
    - "`mcp_test_framework/contracts/py.typed` exists (PEP 561 marker for the contracts stub)"
    - "`mcp_test_framework/contracts/__init__.py` exists with a one-line docstring noting Phase 27 lands `register()` here"
    - "`register()` is NOT defined here (D-14: Phase 27 owns the body)"
  artifacts:
    - path: "src/mcp_test_framework/py.typed"
      provides: "PEP 561 marker for the root importable package"
      contains: ""
    - path: "src/mcp_test_framework/contracts/__init__.py"
      provides: "Empty subpackage stub (Phase 27 lands register() here)"
      contains: "Phase 27"
    - path: "src/mcp_test_framework/contracts/py.typed"
      provides: "PEP 561 marker for the contracts subpackage"
      contains: ""
    - path: "src/mcp_test_framework/test_code/py.typed"
      provides: "PEP 561 marker for the test_code subpackage"
      contains: ""
  key_links:
    - from: "operator's `from mcp_test_framework.contracts import ...` import"
      to: "src/mcp_test_framework/contracts/__init__.py"
      via: "Python package resolution (sys.path → site-packages → installed wheel)"
      pattern: "from mcp_test_framework.contracts"
    - from: "operator's pyright/mypy"
      to: "py.typed markers in installed wheel"
      via: "PEP 561 §Packaging Type Information"
      pattern: "py.typed"
---

<objective>
Create the three PEP 561 `py.typed` markers and the new `contracts/` subpackage stub. This unblocks PACK-02 (SC3 — operator's pyright/mypy resolves typed signatures from `mcp_test_framework`, `mcp_test_framework.contracts`, `mcp_test_framework.test_code`) and prepares the import surface Phase 27's `register()` will land on.

**Purpose:** Wave 1 plans 26-01 and 26-03 land in parallel. This plan is purely additive — four new files under `src/mcp_test_framework/`, zero modifications to existing files. The hatchling `packages = ["src/mcp_test_framework"]` line auto-includes them in the wheel (RESEARCH.md key finding — no `force-include` needed).

**Output:** Four new files; the `contracts/` subpackage exists; PEP 561 markers ship for the root package and both operator-imported subpackages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s/26-CONTEXT.md
@.planning/phases/26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s/26-PATTERNS.md
@src/mcp_test_framework/__init__.py
@src/mcp_test_framework/test_code/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the three PEP 561 py.typed marker files (empty)</name>
  <files>
    src/mcp_test_framework/py.typed,
    src/mcp_test_framework/test_code/py.typed,
    src/mcp_test_framework/contracts/py.typed
  </files>
  <read_first>
    - src/mcp_test_framework/__init__.py (confirms the package layout — the root `__init__.py` already exists)
    - src/mcp_test_framework/test_code/__init__.py (confirms the subpackage exists post-Phase-25 rename)
    - .planning/phases/26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s/26-CONTEXT.md D-13 (which subpackages need markers)
    - .planning/phases/26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s/26-PATTERNS.md "py.typed markers" section (lines 299-310)
  </read_first>
  <action>
Create three files using the Write tool. Each file is **completely empty** — PEP 561 §"Packaging Type Information" specifies that the marker file's *presence* is the signal; its contents are ignored by type checkers.

**File 1:** `src/mcp_test_framework/py.typed`
Content: empty (zero bytes)

**File 2:** `src/mcp_test_framework/test_code/py.typed`
Content: empty (zero bytes)

**File 3:** `src/mcp_test_framework/contracts/py.typed` — this file's parent directory `src/mcp_test_framework/contracts/` will be created by Task 2 of this plan. If Task 2 has not yet run when this task is executing, create the directory implicitly via the Write tool (which will create parents). Otherwise, the directory will already exist from Task 2.

**DO NOT:**
- Write any content (no docstring, no comment, no whitespace, no newline)
- Add `py.typed` to any other directory in the codebase (no other subpackages need markers in Phase 26 — `_codegen`, `_slugs`, `_tool_factory` are internal-only and not in the operator import surface)
- Modify hatchling config — `packages = ["src/mcp_test_framework"]` already auto-includes these (RESEARCH.md key finding; CONTEXT.md D-13)
  </action>
  <verify>
    <automated>uv run python -c "from pathlib import Path; assert Path('src/mcp_test_framework/py.typed').exists(); assert Path('src/mcp_test_framework/test_code/py.typed').exists(); assert Path('src/mcp_test_framework/contracts/py.typed').exists(); print('all 3 markers exist')"</automated>
  </verify>
  <done>
    - `src/mcp_test_framework/py.typed` exists; size is 0 bytes
    - `src/mcp_test_framework/test_code/py.typed` exists; size is 0 bytes
    - `src/mcp_test_framework/contracts/py.typed` exists; size is 0 bytes
    - PEP 561 marker semantics satisfied for the three operator-imported subpackages
  </done>
  <acceptance_criteria>
    - File `src/mcp_test_framework/py.typed` exists
    - File `src/mcp_test_framework/test_code/py.typed` exists
    - File `src/mcp_test_framework/contracts/py.typed` exists
    - Python expression `Path('src/mcp_test_framework/py.typed').stat().st_size` evaluates to `0`
    - Python expression `Path('src/mcp_test_framework/test_code/py.typed').stat().st_size` evaluates to `0`
    - Python expression `Path('src/mcp_test_framework/contracts/py.typed').stat().st_size` evaluates to `0`
    - `find src/mcp_test_framework -name py.typed` lists exactly 3 paths (no spurious markers in other subpackages)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create the contracts/ subpackage stub (__init__.py with one-line docstring)</name>
  <files>src/mcp_test_framework/contracts/__init__.py</files>
  <read_first>
    - src/mcp_test_framework/__init__.py (analog — 3-line file with module docstring and __version__)
    - .planning/phases/26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s/26-CONTEXT.md D-13 and D-14
    - .planning/phases/26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s/26-PATTERNS.md "contracts/__init__.py" section (lines 274-296)
  </read_first>
  <action>
Create file `src/mcp_test_framework/contracts/__init__.py` with EXACTLY the following content (the Write tool will create the parent `src/mcp_test_framework/contracts/` directory):

```python
"""mcp_test_framework.contracts -- Phase 27 lands register() here.

Phase 26 ships this as an empty subpackage stub so PEP 561-driven type
resolution finds `mcp_test_framework.contracts` from an installed wheel
without "missing stubs" complaints. Phase 27 (LIB-01..LIB-08) fills the
register() API + virtual _ContractsModule injection + contract-test
extraction inside this subpackage.

Per Phase 26 D-13/D-14: NO `register()` import or implementation in v1.4
pre-Phase-27. An operator who imports from this module in the
Phase 26-only milestone window simply finds an empty namespace. Phase 27
ships within the same milestone (v1.4) before any release, so no helpful
runtime ImportError is needed here.
"""
```

(end of file — no other code, no `__version__`, no `__all__`, no exports)

**DO NOT:**
- Define `register()`, any class, or any callable here (D-14 — Phase 27 owns the body)
- Add `from __future__ import annotations` (no type annotations in this file)
- Set `__version__` (the root `mcp_test_framework/__init__.py` is the single source of `__version__`)
- Import anything from `mcp_test_framework.fixtures`, `cli`, or `_plugin` (this stub stays inert)
  </action>
  <verify>
    <automated>uv run python -c "import mcp_test_framework.contracts as c; print(repr(c.__doc__)[:60]); assert 'Phase 27 lands register()' in c.__doc__"</automated>
  </verify>
  <done>
    - `src/mcp_test_framework/contracts/__init__.py` exists
    - File contains the literal substring `Phase 27 lands register()`
    - File defines no callables, classes, or non-docstring assignments
    - `import mcp_test_framework.contracts` succeeds
    - `hasattr(mcp_test_framework.contracts, 'register')` is `False` (D-14)
  </done>
  <acceptance_criteria>
    - File `src/mcp_test_framework/contracts/__init__.py` exists
    - grep `Phase 27 lands register()` in `src/mcp_test_framework/contracts/__init__.py` returns 1 match
    - grep `D-13` in `src/mcp_test_framework/contracts/__init__.py` returns 1 match (proves the docstring cites the decision)
    - grep `^def \|^class \|^[a-zA-Z_][a-zA-Z0-9_]* =` in `src/mcp_test_framework/contracts/__init__.py` returns 0 matches (no callables, no module-level assignments)
    - `uv run python -c "import mcp_test_framework.contracts"` exits 0
    - `uv run python -c "import mcp_test_framework.contracts; assert not hasattr(mcp_test_framework.contracts, 'register'), 'register must not exist in Phase 26 (D-14)'"` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's type-checker → installed wheel | pyright/mypy reads `py.typed` markers and `.py` source from the wheel; markers signal that inline annotations are authoritative |
| empty subpackage → operator import | `from mcp_test_framework.contracts import X` resolves before Phase 27 lands `register()`; ImportError for any name is the safe failure mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03-01 | Tampering | empty `py.typed` markers | accept | PEP 561 spec defines empty file as the signal; no content to tamper with |
| T-26-03-02 | Repudiation | missing markers in built wheel | mitigate | Plan 26-04 wheel-shape gate asserts every marker present in the `.whl`; CI fails if hatch drops them |
| T-26-03-03 | Elevation of privilege | premature `register()` exposure | mitigate | D-14: `__init__.py` defines no callables; Task 2 acceptance criterion grep-counts module-level definitions = 0; Plan 26-04 source-scan re-verifies |
</threat_model>

<verification>
- `find src/mcp_test_framework -name py.typed` lists exactly 3 paths
- `uv run python -c "import mcp_test_framework, mcp_test_framework.contracts, mcp_test_framework.test_code"` exits 0
- `uv run python -c "import mcp_test_framework.contracts; assert not hasattr(mcp_test_framework.contracts, 'register')"` exits 0 (D-14)
- `wc -c src/mcp_test_framework/py.typed src/mcp_test_framework/test_code/py.typed src/mcp_test_framework/contracts/py.typed` shows all three files at 0 bytes
</verification>

<success_criteria>
- All <acceptance_criteria> on both tasks pass
- Three empty `py.typed` markers exist at the prescribed paths
- `src/mcp_test_framework/contracts/__init__.py` exists as a docstring-only stub
- `import mcp_test_framework.contracts` succeeds
- `register` is NOT defined in the contracts subpackage (D-14)
- Goal-backward must_haves verified
</success_criteria>

<output>
After completion, create `.planning/phases/26-packaging-foundation-entry-point-py-typed-dist-name-plugin-s/26-03-SUMMARY.md` per `$HOME/.claude/get-shit-done/templates/summary.md`. Highlight:
- The four new files created (three empty markers + one docstring-only `__init__.py`)
- Confirmation that `register()` is NOT defined in `contracts/__init__.py` (Phase 27's contract)
- That hatchling auto-inclusion will be verified by Plan 26-04's wheel-shape gate
</output>
</content>
</invoke>