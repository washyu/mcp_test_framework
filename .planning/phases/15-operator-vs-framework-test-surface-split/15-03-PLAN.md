---
phase: 15-operator-vs-framework-test-surface-split
plan: 03
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - README.md
  - docs/mcp_test_framework_mvp_spec.md
autonomous: true
requirements:
  - SURFACE-02

must_haves:
  truths:
    - "README.md no longer documents `uv run pytest tests/` as the canonical dev recipe without also distinguishing between contract-only and full-suite invocations"
    - "README.md mentions the new `tests/contract/` / `tests/framework/` layout at least once in either Layout, Run, or Isolation sections so operators discover it"
    - "docs/mcp_test_framework_mvp_spec.md `tests/` references in the layout block (line ~65) and Test Cases block (line ~232) reflect the post-split paths (`tests/contract/`)"
    - "Snippet regression tests (now at tests/framework/test_readme_snippets.py) continue to pass with no new failures introduced by README edits"
  artifacts:
    - path: "README.md"
      provides: "Operator-facing recipe sweep — `tests/` -> contract-aware invocations"
      contains: "tests/contract"
    - path: "docs/mcp_test_framework_mvp_spec.md"
      provides: "Spec layout block + Test Cases section reflect post-split paths"
      contains: "tests/contract"
  key_links:
    - from: "README.md (Run the test suite section)"
      to: "post-Phase-15 invocations"
      via: "documented recipes"
      pattern: "tests/contract|--with-framework"
---

<objective>
Refresh user-facing documentation so the post-split test layout is discoverable and the documented recipes match reality. Two files in scope: `README.md` (operator-facing) and `docs/mcp_test_framework_mvp_spec.md` (design spec). Edits are surgical — only the path references and one or two clarifying sentences. No README restructuring; no rewriting of unrelated sections.

The `tests/test_readme_snippets.py` regression test does NOT need changes (Grep confirmed it pins `tests/` paths nowhere in its assertions — it only enforces YAML-fence parseability and TOOLCFG model field correspondence, neither of which is affected by Phase 15).

Purpose: An operator browsing the repo first lands in README.md; the doc must reflect the post-split layout so the operator understands the difference between `mcp-test-framework run` (operator default, contract only), `mcp-test-framework run --with-framework` (full suite, CI use), and `uv run pytest tests/` (maintainer escape hatch — still works because pytest recurses into subdirectories per CONTEXT.md §specifics).

Output: README.md and the spec document accurately describe the post-split layout. No CHANGELOG entry (per CONTEXT.md `Claude's Discretion` — no CHANGELOG.md exists in the repo).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/15-operator-vs-framework-test-surface-split/15-CONTEXT.md
@README.md
@docs/mcp_test_framework_mvp_spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md "Run the test suite" + "Isolation" sections to reflect post-split layout</name>
  <files>
    - README.md (lines ~40-55 "Run the test suite" section; line ~166 "tests/smoke/" reference; lines ~177-185 "Isolation guarantee" section)
  </files>
  <read_first>
    - README.md lines 1-200 (full surface — locate every literal `tests/` reference; Grep showed lines 49, 166, 182, 185)
    - 15-CONTEXT.md §Claude's Discretion bullet on README updates
    - 15-CONTEXT.md §specifics on `pytest tests/` backward compat for maintainers
  </read_first>
  <action>
    The pre-Phase-15 README has 4 literal `tests/` mentions (from Grep):
    - Line 49: `run` invokes pytest against the `tests/` directory and exits with pytest's exit
    - Line 166: The 5 deselected tests are the live-marker smoke tests in `tests/smoke/` gated
    - Line 182: uv run pytest tests/test_isolation.py -v
    - Line 185: The test in `tests/test_isolation.py` computes sha256 hashes...

    Make these surgical edits:

    1. Line 49 — replace the sentence so it distinguishes operator default from maintainer escape hatch:

    Before:
    ```
    `run` invokes pytest against the `tests/` directory and exits with pytest's exit
    code (0 on green). Anything after the `--` separator is forwarded verbatim to
    `pytest.main()` -- use it to pass `-k`, `-x`, `--lf`, or any other pytest flag.
    ```

    After:
    ```
    `run` invokes pytest against `tests/contract/` (the operator-relevant SUT-contract
    surface) by default and exits with pytest's exit code (0 on green). Pass
    `--with-framework` to also collect `tests/framework/` (the framework's own
    self-tests — config validation, runner internals, snippet checks, banned
    imports). Anything after the `--` separator is forwarded verbatim to pytest --
    use it to pass `-k`, `-x`, `--lf`, or any other pytest flag.
    ```

    Note: the `pytest.main()` mention is dropped because Phase 14 moved to subprocess dispatch — saying "verbatim to pytest" is accurate without anchoring on the deprecated entry point.

    2. Line 166 — `tests/smoke/` becomes `tests/framework/smoke/` (post-Plan-01 path):

    Before:
    ```
    The 5 deselected tests are the live-marker smoke tests in `tests/smoke/` gated
    ```

    After:
    ```
    The 5 deselected tests are the live-marker smoke tests in `tests/framework/smoke/` gated
    ```

    3. Lines 182 + 185 — `tests/test_isolation.py` becomes `tests/framework/test_isolation.py`:

    Line 182 before:
    ```
    uv run pytest tests/test_isolation.py -v
    ```
    Line 182 after:
    ```
    uv run pytest tests/framework/test_isolation.py -v
    ```

    Line 185 before (first occurrence of `tests/test_isolation.py` only — there's only one in the paragraph):
    ```
    The test in `tests/test_isolation.py` computes sha256 hashes...
    ```
    Line 185 after:
    ```
    The test in `tests/framework/test_isolation.py` computes sha256 hashes...
    ```

    Do NOT add a CHANGELOG-style section (CONTEXT.md `Claude's Discretion` — no CHANGELOG.md in repo).

    Do NOT touch lines outside these 4 spots — the operator-walkthrough sections, the persona reframe sections from Phase 12, and the config recipes are all out of scope for Phase 15.

    Verify after edit:
    - `grep -c "tests/contract" README.md` returns at least 1
    - `grep -c "tests/framework/smoke" README.md` returns at least 1
    - `grep -c "tests/framework/test_isolation.py" README.md` returns at least 2
    - `grep -c "with-framework" README.md` returns at least 1
    - `grep -nE "^\\s*uv run pytest tests/test_" README.md` returns no matches (no leftover top-level-test invocations)
    - `grep -nE "tests/smoke/" README.md` returns no matches (the directory no longer exists at that path)
  </action>
  <verify>
    <automated>uv run pytest tests/framework/test_readme_snippets.py -x</automated>
  </verify>
  <acceptance_criteria>
    - 4 literal edits applied at the lines named above; no other lines touched (verify with `git diff --stat README.md` showing a single file changed; `git diff README.md | grep -c '^+'` returns a count consistent with the additions — roughly 6-8 added lines and 4-6 removed lines, not a wholesale rewrite)
    - `grep -c "tests/contract" README.md` >= 1
    - `grep -c "tests/framework/smoke" README.md` >= 1
    - `grep -c "tests/framework/test_isolation.py" README.md` == 2 (lines 182 + 185 region)
    - `grep -c "with-framework" README.md` >= 1
    - `grep -c "tests/smoke/" README.md` == 0 (the bare `tests/smoke/` path is gone)
    - `grep -c "tests/test_isolation.py" README.md` == 0 (the bare top-level path is gone)
    - `uv run pytest tests/framework/test_readme_snippets.py -x` passes — the snippet regression suite is unaffected by these path-string changes (it pins YAML-fence parseability and TOOLCFG model fields, not path literals)
  </acceptance_criteria>
  <done>README.md "Run the test suite" section documents the contract-default / `--with-framework` distinction. The "Isolation guarantee" section cites the post-split `tests/framework/test_isolation.py` path. The snippet regression suite still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update docs/mcp_test_framework_mvp_spec.md layout block + Test Cases section</name>
  <files>
    - docs/mcp_test_framework_mvp_spec.md (lines ~60-68 layout tree block; line ~232 Test Cases section)
  </files>
  <read_first>
    - docs/mcp_test_framework_mvp_spec.md lines 55-75 (Module Layout block — Grep showed line 65 has `└── tests/`)
    - docs/mcp_test_framework_mvp_spec.md lines 225-245 (Test Cases section — Grep showed line 232 has `tests/test_homelab_list_registered_servers.py`, the pre-Phase-07 filename that has since been renamed to `tests/test_mcp_tool_contract.py` and now to `tests/contract/test_mcp_tool_contract.py`)
    - 15-CONTEXT.md §Claude's Discretion on spec updates
  </read_first>
  <action>
    The MVP spec has TWO references to update (per Grep results at lines 65 and 232).

    1. Update the Module Layout tree at line ~65. The current block reads:
    ```
    └── tests/
        ├── conftest.py
        └── test_homelab_list_registered_servers.py
    ```

    Replace with:
    ```
    └── tests/
        ├── conftest.py                                    # session-scoped fixtures (applies to both subtrees)
        ├── contract/
        │   └── test_mcp_tool_contract.py                  # operator-relevant SUT-contract surface
        └── framework/                                     # framework self-tests (config, runner, snippets, banned imports, smoke)
            ├── test_banned_imports.py
            ├── test_isolation.py
            ├── test_readme_snippets.py
            ├── test_runner_*.py
            ├── test_config_init_cli.py
            ├── test_tool_config.py
            ├── unit/                                      # 20 unit self-tests
            ├── smoke/                                     # 3 live-marker smoke tests (homelab-mcp + Ollama)
            ├── _fixtures/                                 # banned-imports negative fixture
            └── fixtures/                                  # JUnit XML parser fixtures
    ```

    2. Update the Test Cases section at line ~232. The current sentence reads:
    ```
    All tests live in `tests/test_homelab_list_registered_servers.py`.
    ```

    This sentence is doubly out-of-date — `test_homelab_list_registered_servers.py` was renamed to `test_mcp_tool_contract.py` in an earlier phase (Phase 07 multi-tool generalization), and now Phase 15 moves it into `tests/contract/`. Replace with:

    ```
    The MVP's SUT-contract tests live in `tests/contract/test_mcp_tool_contract.py` (parametrized across the operator's enabled tool list); framework self-tests (config validation, runner internals, snippet correctness, isolation, banned imports, smoke) live under `tests/framework/`.
    ```

    Do NOT update any other section of the spec — Phase 15 is mechanical retarget only; section-level rewrites are out of scope.

    Verify after edit:
    - `grep -c "tests/contract" docs/mcp_test_framework_mvp_spec.md` >= 2
    - `grep -c "tests/framework" docs/mcp_test_framework_mvp_spec.md` >= 2
    - `grep -c "test_homelab_list_registered_servers" docs/mcp_test_framework_mvp_spec.md` == 0 (the stale filename is gone)
  </action>
  <verify>
    <automated>! grep -q "test_homelab_list_registered_servers" docs/mcp_test_framework_mvp_spec.md && grep -q "tests/contract/test_mcp_tool_contract.py" docs/mcp_test_framework_mvp_spec.md && grep -q "tests/framework/" docs/mcp_test_framework_mvp_spec.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "tests/contract" docs/mcp_test_framework_mvp_spec.md` >= 2 (Layout tree + Test Cases sentence)
    - `grep -c "tests/framework" docs/mcp_test_framework_mvp_spec.md` >= 2 (Layout tree + Test Cases sentence)
    - `grep -c "test_homelab_list_registered_servers" docs/mcp_test_framework_mvp_spec.md` == 0 (stale Phase-04-era filename eliminated)
    - `grep -c "test_mcp_tool_contract.py" docs/mcp_test_framework_mvp_spec.md` >= 1
    - `git diff --stat docs/mcp_test_framework_mvp_spec.md` shows a single file with a localized diff (not a wholesale rewrite) — roughly 10-20 lines changed total across the two edit sites
  </acceptance_criteria>
  <done>The spec's Module Layout block and Test Cases section reflect the post-split layout and the post-Phase-07 contract-file name. No other spec sections touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-vs-docs | The operator reads README.md / spec to learn how to invoke the framework. Stale paths cause confusion but no security impact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | I (information disclosure) | README path references | accept | Docs only document public paths; no secrets. NONE severity. |
| T-15-09 | T (tampering) | snippet regression test | mitigate | After README edits, re-run `tests/framework/test_readme_snippets.py` to ensure YAML fences and TOOLCFG field references still parse. LOW severity. |

**Overall threat surface: NONE.** Pure documentation refresh.
</threat_model>

<verification>
After both tasks:
1. `grep -c "tests/contract" README.md` >= 1
2. `grep -c "tests/contract" docs/mcp_test_framework_mvp_spec.md` >= 2
3. `grep -c "test_homelab_list_registered_servers" docs/mcp_test_framework_mvp_spec.md` == 0
4. `uv run pytest tests/framework/test_readme_snippets.py -x` passes
</verification>

<success_criteria>
- README.md reflects post-split layout in the "Run the test suite" and "Isolation guarantee" sections
- The spec's Module Layout tree shows the `contract/` + `framework/` subdirectory split
- The spec's Test Cases section names the current contract file (`test_mcp_tool_contract.py`) at its post-split path
- Snippet regression suite is green after edits
</success_criteria>

<output>
After completion, create `.planning/phases/15-operator-vs-framework-test-surface-split/15-03-SUMMARY.md` describing the 4 README edits, the 2 spec edits, and the snippet-suite pass.
</output>
