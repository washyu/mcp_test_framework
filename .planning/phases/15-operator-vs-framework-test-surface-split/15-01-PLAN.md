---
phase: 15-operator-vs-framework-test-surface-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_mcp_tool_contract.py
  - tests/test_config_init_cli.py
  - tests/test_isolation.py
  - tests/test_readme_snippets.py
  - tests/test_runner_live_smoke.py
  - tests/test_runner_renderer.py
  - tests/test_runner_subprocess.py
  - tests/test_runner_verbosity.py
  - tests/test_tool_config.py
  - tests/unit/
  - tests/smoke/
  - tests/_fixtures/banned_import_should_fail.py.txt
  - tests/fixtures/junit-all-pass.xml
  - tests/fixtures/junit-all-skip.xml
  - tests/fixtures/junit-mixed.xml
  - tests/fixtures/junit-one-fail-with-reasoning.xml
autonomous: true
requirements:
  - SURFACE-01
  - SURFACE-03
  - SURFACE-04

must_haves:
  truths:
    - "tests/contract/test_mcp_tool_contract.py exists and tests/test_mcp_tool_contract.py does not"
    - "tests/framework/ subtree contains all 8 top-level framework test files + unit/ + smoke/ + _fixtures/ + fixtures/"
    - "tests/framework/test_banned_imports.py exists at the literal path SURFACE-04 names"
    - "git log --follow on every moved file shows its pre-move commit history"
    - "Top-level tests/conftest.py is untouched and continues to apply to both subtrees"
  artifacts:
    - path: "tests/contract/test_mcp_tool_contract.py"
      provides: "Operator-relevant SUT contract surface"
      contains: "test_target_tool_exists"
    - path: "tests/framework/test_config_init_cli.py"
      provides: "Framework self-test (config-init CLI)"
    - path: "tests/framework/test_isolation.py"
      provides: "Framework self-test (host-state isolation)"
    - path: "tests/framework/test_readme_snippets.py"
      provides: "Framework self-test (README snippet correctness)"
    - path: "tests/framework/test_runner_live_smoke.py"
      provides: "Framework self-test (runner end-to-end smoke)"
    - path: "tests/framework/test_runner_renderer.py"
      provides: "Framework self-test (domain UI renderer)"
    - path: "tests/framework/test_runner_subprocess.py"
      provides: "Framework self-test (subprocess dispatch)"
    - path: "tests/framework/test_runner_verbosity.py"
      provides: "Framework self-test (verbosity ladder)"
    - path: "tests/framework/test_tool_config.py"
      provides: "Framework self-test (per-tool config)"
    - path: "tests/framework/unit/"
      provides: "20 unit self-tests (excluding banned_imports which hoists out)"
    - path: "tests/framework/smoke/"
      provides: "3 framework smoke files (homelab-mcp + ollama + teardown regression)"
    - path: "tests/framework/_fixtures/banned_import_should_fail.py.txt"
      provides: "Banned-imports negative fixture"
    - path: "tests/framework/fixtures/"
      provides: "4 JUnit XML parser fixtures"
    - path: "tests/framework/test_banned_imports.py"
      provides: "SURFACE-04 literal path for the black-box-rule enforcer"
  key_links:
    - from: "tests/conftest.py"
      to: "both tests/contract/ and tests/framework/"
      via: "pytest auto-discovery of subdirectories"
      pattern: "pytest_plugins\\s*=\\s*\\[\"mcp_test_framework.fixtures\"\\]"
    - from: "tests/framework/test_banned_imports.py"
      to: "src/mcp_test_framework/"
      via: "AST scan for `import homelab_mcp` / `from homelab_mcp`"
      pattern: "banned_import|homelab_mcp"
---

<objective>
Mechanically relocate every existing test artifact under `tests/` into either `tests/contract/` (operator-relevant: the single SUT-contract test) or `tests/framework/` (framework self-tests, fixtures, snippet checks, and smoke). Every move uses `git mv` so `git log --follow` continues to show pre-move history. Hoist `test_banned_imports.py` out of `unit/` to satisfy SURFACE-04's literal path. The top-level `tests/conftest.py` stays as-is per D-04.

Purpose: Establish the folder layout that Plan 02's runner-scope flip will target. Without this move first, retargeting `_build_pytest_args` to `["tests/contract"]` would point at a non-existent directory.

Output: A reorganized `tests/` tree with two subdirectories (`contract/` and `framework/`), all moves committed in a single atomic commit (so the index never observes a half-moved state), and pre-move history preserved on every moved file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-operator-vs-framework-test-surface-split/15-CONTEXT.md
@.planning/seeds/SEED-010-operator-vs-framework-test-surface.md
@tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create target directories and move the single contract test</name>
  <files>
    - tests/contract/ (new directory, with __init__.py)
    - tests/framework/ (new directory, with __init__.py)
    - tests/contract/test_mcp_tool_contract.py (moved from tests/test_mcp_tool_contract.py)
  </files>
  <read_first>
    - tests/test_mcp_tool_contract.py (verify it is the file being moved)
    - tests/conftest.py (verify D-04 — confirm it stays put; do NOT move)
    - 15-CONTEXT.md §D-06 (one-file contract surface decision)
  </read_first>
  <action>
    1. Create `tests/contract/` and `tests/framework/` as new directories with empty `__init__.py` files (matches the existing `tests/__init__.py` empty file convention — confirmed: `tests/__init__.py` is 0 bytes).

    Commands (run from repo root, PowerShell-compatible):
    ```
    mkdir tests/contract
    mkdir tests/framework
    New-Item -ItemType File -Path tests/contract/__init__.py
    New-Item -ItemType File -Path tests/framework/__init__.py
    git add tests/contract/__init__.py tests/framework/__init__.py
    ```

    2. Move the single contract test (D-06 — `tests/test_mcp_tool_contract.py` is the sole operator-relevant SUT-contract test). Use `git mv`, NOT a regular file move, to preserve history per SURFACE-03:
    ```
    git mv tests/test_mcp_tool_contract.py tests/contract/test_mcp_tool_contract.py
    ```

    3. Do NOT edit the file contents. D-04 mandates the top-level `tests/conftest.py` stays where it is; it auto-applies to subdirectories. The moved file's imports continue to resolve because `pytest_plugins = ["mcp_test_framework.fixtures"]` in the unchanged top-level conftest registers the same fixture surface.

    4. Do NOT commit yet — Task 2 and Task 3 add more `git mv`s to the same staged set; we commit once at the end of Task 4 so the index never holds a half-moved state.
  </action>
  <verify>
    <automated>test -f tests/contract/test_mcp_tool_contract.py && test -f tests/contract/__init__.py && test -f tests/framework/__init__.py && ! test -f tests/test_mcp_tool_contract.py</automated>
  </verify>
  <acceptance_criteria>
    - `tests/contract/test_mcp_tool_contract.py` exists (file)
    - `tests/test_mcp_tool_contract.py` does NOT exist
    - `tests/contract/__init__.py` exists, 0 bytes
    - `tests/framework/__init__.py` exists, 0 bytes
    - `git diff --cached --name-status` shows `R` (rename) for `tests/test_mcp_tool_contract.py -> tests/contract/test_mcp_tool_contract.py` (NOT a `D` + `A` pair — that would mean history is lost)
    - `git status --short` shows the renames as staged changes
  </acceptance_criteria>
  <done>Target directories exist; the contract file has moved into `tests/contract/` with `git mv` (rename detected by git). No commits yet.</done>
</task>

<task type="auto">
  <name>Task 2: git mv the 8 top-level framework test files</name>
  <files>
    - tests/test_config_init_cli.py -> tests/framework/test_config_init_cli.py
    - tests/test_isolation.py -> tests/framework/test_isolation.py
    - tests/test_readme_snippets.py -> tests/framework/test_readme_snippets.py
    - tests/test_runner_live_smoke.py -> tests/framework/test_runner_live_smoke.py
    - tests/test_runner_renderer.py -> tests/framework/test_runner_renderer.py
    - tests/test_runner_subprocess.py -> tests/framework/test_runner_subprocess.py
    - tests/test_runner_verbosity.py -> tests/framework/test_runner_verbosity.py
    - tests/test_tool_config.py -> tests/framework/test_tool_config.py
  </files>
  <read_first>
    - 15-CONTEXT.md §D-07 (framework surface list — exact 8 top-level files)
    - List `tests/test_*.py` to confirm exactly these 8 files remain after Task 1's contract move (no extras)
  </read_first>
  <action>
    Run each `git mv` separately so a single failure (e.g., file already moved, unexpected addition since planning) is isolated and recoverable. Sequence per D-07:

    ```
    git mv tests/test_config_init_cli.py    tests/framework/test_config_init_cli.py
    git mv tests/test_isolation.py          tests/framework/test_isolation.py
    git mv tests/test_readme_snippets.py    tests/framework/test_readme_snippets.py
    git mv tests/test_runner_live_smoke.py  tests/framework/test_runner_live_smoke.py
    git mv tests/test_runner_renderer.py    tests/framework/test_runner_renderer.py
    git mv tests/test_runner_subprocess.py  tests/framework/test_runner_subprocess.py
    git mv tests/test_runner_verbosity.py   tests/framework/test_runner_verbosity.py
    git mv tests/test_tool_config.py        tests/framework/test_tool_config.py
    ```

    Do NOT edit any file content. The top-level `tests/conftest.py` is auto-discovered for `tests/framework/*.py` by pytest's directory traversal rules; no per-subdir conftest is needed (D-05).

    Pre-check: if any of these 8 files is missing OR if `ls tests/test_*.py` returns more than 0 files after this task (meaning some unexpected top-level test was added since planning), STOP and report — the planning surface is stale.

    Do NOT commit yet — held until Task 4.
  </action>
  <verify>
    <automated>test ! -f tests/test_config_init_cli.py && test ! -f tests/test_isolation.py && test ! -f tests/test_readme_snippets.py && test ! -f tests/test_runner_live_smoke.py && test ! -f tests/test_runner_renderer.py && test ! -f tests/test_runner_subprocess.py && test ! -f tests/test_runner_verbosity.py && test ! -f tests/test_tool_config.py && test -f tests/framework/test_config_init_cli.py && test -f tests/framework/test_isolation.py && test -f tests/framework/test_readme_snippets.py && test -f tests/framework/test_runner_live_smoke.py && test -f tests/framework/test_runner_renderer.py && test -f tests/framework/test_runner_subprocess.py && test -f tests/framework/test_runner_verbosity.py && test -f tests/framework/test_tool_config.py</automated>
  </verify>
  <acceptance_criteria>
    - All 8 source paths under `tests/test_*.py` no longer exist
    - All 8 destination paths under `tests/framework/test_*.py` exist
    - `git status --short | grep -c "^R"` (via Bash tool) returns at least 8 — the 8 framework renames; the 1 contract rename from Task 1 is also `R`. PowerShell equivalent: `(git status --short | Select-String "^R").Count`
    - No `^A` (add-without-history) entries for any of the 8 framework files in `git status`
    - `ls tests/test_*.py 2>/dev/null` (via Bash tool) returns empty — no top-level test files remain. PowerShell equivalent: `Get-ChildItem tests/test_*.py -ErrorAction SilentlyContinue` returns nothing.
  </acceptance_criteria>
  <done>All 8 framework-tier top-level test files have been moved to `tests/framework/` via `git mv` with rename detection intact. No commits yet.</done>
</task>

<task type="auto">
  <name>Task 3: git mv unit/, smoke/, _fixtures/, and fixtures/ subtrees</name>
  <files>
    - tests/unit/ -> tests/framework/unit/ (20 files + __init__.py)
    - tests/smoke/ -> tests/framework/smoke/ (3 files + __init__.py)
    - tests/_fixtures/banned_import_should_fail.py.txt -> tests/framework/_fixtures/banned_import_should_fail.py.txt
    - tests/fixtures/junit-all-pass.xml -> tests/framework/fixtures/junit-all-pass.xml
    - tests/fixtures/junit-all-skip.xml -> tests/framework/fixtures/junit-all-skip.xml
    - tests/fixtures/junit-mixed.xml -> tests/framework/fixtures/junit-mixed.xml
    - tests/fixtures/junit-one-fail-with-reasoning.xml -> tests/framework/fixtures/junit-one-fail-with-reasoning.xml
  </files>
  <read_first>
    - 15-CONTEXT.md §D-07 ("entire directory moves intact"), §D-08 (smoke goes under framework/, not contract/)
    - Confirm via `ls tests/unit/`, `ls tests/smoke/`, `ls tests/_fixtures/`, `ls tests/fixtures/` that the file counts match D-07 (20 unit files + __init__.py; 3 smoke files + __init__.py; 1 banned-fixture .txt; 4 junit XML)
  </read_first>
  <action>
    `git mv` accepts directory operands and moves all contained files atomically. Run each subtree move as a single command:

    ```
    git mv tests/unit       tests/framework/unit
    git mv tests/smoke      tests/framework/smoke
    git mv tests/_fixtures  tests/framework/_fixtures
    git mv tests/fixtures   tests/framework/fixtures
    ```

    Rationale: `tests/smoke/*` goes to `tests/framework/smoke/` per D-08 — these tests hardcode `homelab-mcp` and live Ollama; running them against an operator's MCP server would produce false failures. They are framework reachability checks, not SUT-contract checks.

    `tests/_fixtures/` (underscore prefix) and `tests/fixtures/` (no prefix) are TWO different directories — both move. The underscore-prefix one contains the banned-imports negative fixture; the no-prefix one contains JUnit XML parser fixtures.

    Pre-check: confirm no unexpected files exist in any of the four source directories (e.g., uncommitted scratch files) — `git status --short tests/` should be clean for these subtrees BEFORE the move so git's rename detection works cleanly.

    Do NOT commit yet — held until Task 4 so the entire reorganization lands as one commit.
  </action>
  <verify>
    <automated>test ! -d tests/unit && test ! -d tests/smoke && test ! -d tests/_fixtures && test ! -d tests/fixtures && test -d tests/framework/unit && test -d tests/framework/smoke && test -d tests/framework/_fixtures && test -d tests/framework/fixtures && test -f tests/framework/unit/test_banned_imports.py && test -f tests/framework/unit/__init__.py && test -f tests/framework/smoke/__init__.py && test -f tests/framework/_fixtures/banned_import_should_fail.py.txt && test -f tests/framework/fixtures/junit-all-pass.xml && test -f tests/framework/fixtures/junit-all-skip.xml && test -f tests/framework/fixtures/junit-mixed.xml && test -f tests/framework/fixtures/junit-one-fail-with-reasoning.xml</automated>
  </verify>
  <acceptance_criteria>
    - Source directories no longer exist: `tests/unit/`, `tests/smoke/`, `tests/_fixtures/`, `tests/fixtures/`
    - Destination directories exist: `tests/framework/unit/`, `tests/framework/smoke/`, `tests/framework/_fixtures/`, `tests/framework/fixtures/`
    - Unit subtree file count: `ls tests/framework/unit/*.py | wc -l` (via Bash tool) returns exactly 21 — 20 test files + `__init__.py`. PowerShell equivalent: `(Get-ChildItem tests/framework/unit/*.py).Count` returns 21.
    - Smoke subtree file count: `ls tests/framework/smoke/*.py | wc -l` (via Bash tool) returns exactly 4 — 3 test files + `__init__.py`. PowerShell equivalent: `(Get-ChildItem tests/framework/smoke/*.py).Count` returns 4.
    - `tests/framework/_fixtures/banned_import_should_fail.py.txt` exists (1 file)
    - `tests/framework/fixtures/*.xml` count is exactly 4 (junit-all-pass, junit-all-skip, junit-mixed, junit-one-fail-with-reasoning). PowerShell equivalent: `(Get-ChildItem tests/framework/fixtures/*.xml).Count` returns 4.
    - `git status --short | grep -E "^R " | wc -l` (via Bash tool) shows enough rename entries that git tracked these moves with history — at least 1 + 8 + 21 + 4 + 1 + 4 = 39 renames including Task 1 and Task 2; allow for git's rename heuristic to occasionally show as D+A for tiny / empty `__init__.py` files (fine for empty files since there is no content to follow). PowerShell equivalent: `(git status --short | Select-String "^R ").Count` >= 39.
  </acceptance_criteria>
  <done>All four subtrees have moved under `tests/framework/` with directory `git mv`s. The post-move directory listing matches D-07's spec exactly. No commits yet.</done>
</task>

<task type="auto">
  <name>Task 4: Hoist test_banned_imports.py for SURFACE-04 literal path + commit the entire reorganization</name>
  <files>
    - tests/framework/unit/test_banned_imports.py -> tests/framework/test_banned_imports.py
    - All previously staged moves from Tasks 1-3 (single commit)
  </files>
  <read_first>
    - 15-CONTEXT.md §D-10 ("Recommend hoisting" for SURFACE-04 spec literalism)
    - REQUIREMENTS.md §SURFACE-04 (exact literal path: `tests/framework/test_banned_imports.py`)
    - tests/framework/unit/test_banned_imports.py (verify it landed there from Task 3's `git mv tests/unit tests/framework/unit`)
  </read_first>
  <action>
    1. Hoist the banned-imports test up one level so it lives at the path SURFACE-04 names verbatim:

    ```
    git mv tests/framework/unit/test_banned_imports.py tests/framework/test_banned_imports.py
    ```

    Rationale (D-10): SURFACE-04 specifies the literal path `tests/framework/test_banned_imports.py`. Without the hoist, the file lives at `tests/framework/unit/test_banned_imports.py` — functionally equivalent but not the path SURFACE-04 names. Hoisting is cheap (single `git mv`) and removes any ambiguity.

    2. Verify the staged change set covers EVERY move from Tasks 1-4:
    ```
    git status --short
    ```
    Expected: a long list of `R` entries; no `??` (untracked) entries for moved files; no unstaged `M` for the conftest (which must stay put per D-04).

    3. Commit the entire reorganization as a single atomic commit:
    ```
    git commit -m "refactor(15): split tests/ into contract/ and framework/ subtrees

    Move every test artifact under tests/ into either tests/contract/
    (operator-relevant SUT-contract surface — exactly one file:
    test_mcp_tool_contract.py) or tests/framework/ (framework self-tests,
    fixtures, snippet checks, and live smoke).

    All moves use git mv; pre-move history is preserved for every file
    (verified by git log --follow in Plan 04).

    The top-level tests/conftest.py is unchanged (D-04 — the black-box
    sys.modules guard and pytest_plugins registration apply to both
    subtrees automatically via pytest's directory discovery).

    test_banned_imports.py is hoisted out of unit/ so it lives at the
    literal path SURFACE-04 names: tests/framework/test_banned_imports.py
    (D-10).

    Implements: SURFACE-01, SURFACE-03, SURFACE-04 (folder split,
    history preservation, banned-imports relocation).
    "
    ```

    4. Immediately after commit, sanity-check git's rename detection on three representative files (one per category):
    ```
    git log --follow --oneline tests/contract/test_mcp_tool_contract.py | head -5
    git log --follow --oneline tests/framework/test_banned_imports.py | head -5
    git log --follow --oneline tests/framework/test_isolation.py | head -5
    ```
    Each command must return at least 2 distinct commits (the rename commit + at least one pre-move commit). If `head -5` returns only 1 line, history was lost — STOP and investigate.
  </action>
  <verify>
    <automated>test -f tests/framework/test_banned_imports.py && test ! -f tests/framework/unit/test_banned_imports.py && git log --follow --oneline tests/framework/test_banned_imports.py | wc -l | awk '$1 >= 2 {exit 0} {exit 1}' && git log --follow --oneline tests/contract/test_mcp_tool_contract.py | wc -l | awk '$1 >= 2 {exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `tests/framework/test_banned_imports.py` exists at the SURFACE-04 literal path
    - `tests/framework/unit/test_banned_imports.py` does NOT exist (hoisted out)
    - `tests/framework/unit/` still contains the other 20 unit files — the hoist removes exactly one file from `unit/`. PowerShell equivalent: `(Get-ChildItem tests/framework/unit/*.py).Count` returns 20 (no `__init__.py` here? confirm: should be 20 test files + `__init__.py` = 21 → after hoist 19 test files + `__init__.py` = 20).
    - `git log --follow --oneline tests/contract/test_mcp_tool_contract.py` shows at least 2 commits — the rename commit + pre-move history from Phase 04. PowerShell equivalent: `(git log --follow --oneline tests/contract/test_mcp_tool_contract.py).Count` >= 2.
    - `git log --follow --oneline tests/framework/test_banned_imports.py` shows at least 2 commits — the rename + the original create commit. PowerShell equivalent: `(git log --follow --oneline tests/framework/test_banned_imports.py).Count` >= 2.
    - `git log --follow --oneline tests/framework/test_isolation.py` shows at least 2 commits — the rename + Phase 06 / isolation work. PowerShell equivalent: `(git log --follow --oneline tests/framework/test_isolation.py).Count` >= 2.
    - The phase reorganization is a single commit (verify with `git log --oneline -1` — message starts with `refactor(15):`)
    - `git status --short` is clean (no staged or unstaged changes) after the commit
    - `tests/conftest.py` is unchanged (verify with `git diff HEAD~1 HEAD -- tests/conftest.py` returns empty)
  </acceptance_criteria>
  <done>The banned-imports test sits at the SURFACE-04 literal path. The entire reorganization is committed as one atomic commit. `git log --follow` proves pre-move history is preserved on representative files from each tier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-vs-filesystem | The operator (or CI) runs `git mv` commands; no network or external input crosses any boundary in this plan. Filesystem moves are local-only and scoped to the repo's `tests/` subtree. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | T (tampering) | git history preservation | mitigate | Use `git mv` on every move (not `mv` then `git add`). Verify in Task 4 with `git log --follow` on three representative files (one per tier). LOW severity — only a maintainer workflow regression, no security impact. |
| T-15-02 | I (information disclosure) | banned_import_should_fail.py.txt | accept | The fixture contains a deliberately-failing example import line; it is `.txt` (not `.py`) so pytest does not collect it as a test. No secrets. NONE severity. |
| T-15-03 | D (denial of service) | half-moved index state | mitigate | All 4 tasks defer commit to Task 4; the index never holds a half-moved state across distinct git operations during the plan. If interrupted mid-task, `git reset` restores the pre-move state. NONE severity. |
| T-15-04 | E (elevation of privilege) | filesystem permissions | accept | No permission changes; `git mv` preserves file modes. NONE severity. |

**Overall threat surface: NONE / LOW.** Pure filesystem reorganization within a repo; no privilege boundary, no external input, no network egress, no new attack surface. All threats are workflow-correctness concerns, not security concerns.
</threat_model>

<verification>
After all 4 tasks complete and the commit lands, run these checks (no failures permitted):

1. `tests/contract/test_mcp_tool_contract.py` exists and is the only `.py` test file under `tests/contract/` (besides `__init__.py`).
2. `tests/framework/` contains: 8 top-level `test_*.py` files INCLUDING all 8 from D-07 + 1 hoisted `test_banned_imports.py` + `unit/` subdir (20 .py files + `__init__.py`) + `smoke/` subdir (3 .py files + `__init__.py`) + `_fixtures/` subdir + `fixtures/` subdir.
3. `tests/conftest.py` is byte-identical to its pre-Plan-01 state: `git diff HEAD~1 HEAD -- tests/conftest.py` returns empty output.
4. `git log --follow tests/contract/test_mcp_tool_contract.py` shows commits from Phase 04 era (the original create commit).
5. `git log --follow tests/framework/test_banned_imports.py` shows commits from Phase 01 / Phase 12 era (the original create commit + earlier touches).
6. No files were edited; only moved: `git diff HEAD~1 HEAD --name-status` shows ONLY `R` (rename) entries plus the two new `A` entries for `tests/contract/__init__.py` and `tests/framework/__init__.py`. Zero `M` (modify) entries.
</verification>

<success_criteria>
- All 37+ moves committed in a single atomic commit with message prefix `refactor(15):`
- `git log --follow` proves history preserved on representative files from all three tiers (contract, framework-top-level, framework-unit-hoist)
- `tests/contract/test_mcp_tool_contract.py` is the SOLE contract test (D-06 — one file)
- `tests/framework/test_banned_imports.py` exists at the SURFACE-04 literal path (D-10 hoist applied)
- `tests/conftest.py` is unchanged (D-04 / D-05 honored)
- Working tree is clean post-commit; downstream Plan 02 can target `tests/contract/` as a valid pytest collection root
</success_criteria>

<output>
After completion, create `.planning/phases/15-operator-vs-framework-test-surface-split/15-01-SUMMARY.md` describing the file counts moved, the single commit SHA, and the three `git log --follow` results that prove history preservation.
</output>
