---
phase: 15-operator-vs-framework-test-surface-split
plan: 04
type: execute
wave: 3
depends_on: ["15-01", "15-02", "15-03"]
files_modified:
  - .planning/phases/15-operator-vs-framework-test-surface-split/15-VERIFICATION.md
autonomous: false
requirements:
  - SURFACE-01
  - SURFACE-02
  - SURFACE-03
  - SURFACE-04

must_haves:
  truths:
    - "`mcp-test-framework run` (operator default, with a 2-tool config) collects only contract-tier cases — output contains no `tests/framework/` paths"
    - "`mcp-test-framework run --with-framework` collects both subtrees — output references both `tests/contract/` and `tests/framework/`"
    - "`uv run pytest tests/` (maintainer escape hatch) still collects both subtrees by pytest's directory recursion"
    - "`uv run pytest tests/contract/` and `uv run pytest tests/framework/` each work standalone"
    - "`git log --follow` proves pre-move history is preserved on at least one file from each tier (contract + framework-top-level + framework-unit-hoisted)"
    - "`tests/framework/test_banned_imports.py` runs and passes (or fails for the right reason — a real `homelab_mcp` import — not because of the relocation)"
  artifacts:
    - path: ".planning/phases/15-operator-vs-framework-test-surface-split/15-VERIFICATION.md"
      provides: "Recorded verification recipe outputs for all 4 SURFACE requirements"
      contains: "SURFACE-01|SURFACE-02|SURFACE-03|SURFACE-04"
  key_links:
    - from: "verification artifact"
      to: "phase ROADMAP success criteria"
      via: "1:1 mapping between recipe and SC-1..SC-4"
      pattern: "SC-\\d"
---

<objective>
Run the 4 verification recipes named in CONTEXT.md `Claude's Discretion` step 4 and record their outputs in `15-VERIFICATION.md`. Each recipe maps 1:1 to a ROADMAP success criterion (SC-1..SC-4). A human checkpoint at the end approves the recorded evidence before phase close.

The 4 recipes (per CONTEXT.md):
1. `uv run pytest tests/contract/` — contract only; should collect only the SUT-contract tests
2. `uv run pytest tests/framework/` — framework only; should collect ~107 framework self-tests
3. `uv run pytest tests/` — both subtrees (maintainer backward compat by pytest's directory recursion)
4. `mcp-test-framework run` — default operator scope; verify NO `tests/framework/` paths in output
5. `mcp-test-framework run --with-framework` — opt-in; both subtrees, CI use

Plus the SURFACE-03 history-preservation check:
6. `git log --follow tests/contract/test_mcp_tool_contract.py` and `git log --follow tests/framework/test_banned_imports.py` — each must show pre-move commits

Plus the SURFACE-04 enforcement check:
7. `uv run pytest tests/framework/test_banned_imports.py` — passes (the test runs at its SURFACE-04 literal path)

Purpose: Generate ratified evidence that the four SURFACE requirements are met. The human checkpoint guards against subtle regressions (e.g., a framework test leaks into the operator output because of a wildcard glob somewhere, or git's rename heuristic threshold was exceeded by an `__init__.py` and history is fragmented).

Output: `15-VERIFICATION.md` with one section per recipe, capturing the exact command, the exact-or-elided output, and a PASS/FAIL verdict. Human approves before the phase closes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-operator-vs-framework-test-surface-split/15-CONTEXT.md
@.planning/phases/15-operator-vs-framework-test-surface-split/15-01-PLAN.md
@.planning/phases/15-operator-vs-framework-test-surface-split/15-02-PLAN.md
@.planning/phases/15-operator-vs-framework-test-surface-split/15-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the 7 verification recipes and capture outputs in 15-VERIFICATION.md</name>
  <files>
    - .planning/phases/15-operator-vs-framework-test-surface-split/15-VERIFICATION.md (new)
  </files>
  <read_first>
    - .planning/ROADMAP.md Phase 15 success criteria (lines 106-110 — the 4 SC items)
    - .planning/phases/15-operator-vs-framework-test-surface-split/15-CONTEXT.md §Claude's Discretion step 4 (the verification recipe list)
    - Confirm prior plans landed: `git log --oneline -10` should show commits from Plan 01 (`refactor(15):`) and Plan 02 (`feat(15):` or similar)
  </read_first>
  <action>
    Pre-flight: confirm working tree is clean (no uncommitted changes from prior plans). Run `git status --short` — if non-empty, STOP and report; the prior plans should have committed cleanly.

    Pre-flight: confirm `config.yaml` or `examples/homelab-mcp.yaml` is available for the operator-runner invocations. The runner refuses to run without config per SAFE-03. Use whichever is set in the environment; if neither, set `MCPTF_CONFIG_FILE` to `examples/homelab-mcp.yaml` for the duration of these invocations.

    Capture each recipe's output (full output or grep-filtered if >100 lines). Append to `.planning/phases/15-operator-vs-framework-test-surface-split/15-VERIFICATION.md`:

    ---

    **Recipe 1 — Maintainer: `uv run pytest tests/contract/` (collects contract only)**

    Command:
    ```
    uv run pytest tests/contract/ --collect-only -q
    ```
    (using `--collect-only -q` to keep output bounded; full run is also acceptable if quick)

    Capture:
    - Exit code (should be 0 if green; 5 is OK for collect-only)
    - Collected test count (parse from the summary line)
    - Verify: NO `tests/framework/` paths appear in the output (grep for `tests/framework` in the captured output — should be 0 matches)

    Verdict: PASS if collection succeeds and contains only `tests/contract/` paths.

    ---

    **Recipe 2 — Maintainer: `uv run pytest tests/framework/` (collects framework only)**

    Command:
    ```
    uv run pytest tests/framework/ --collect-only -q 2>&1 | tail -50
    ```

    Capture:
    - Exit code
    - Collected test count (should be roughly ~107 per the phase narrative — exact number depends on parametrize fan-out)
    - Verify: NO `tests/contract/` paths appear (grep should be 0)

    Verdict: PASS if collection succeeds, contains only `tests/framework/` paths, and the count is in the ~100+ range.

    ---

    **Recipe 3 — Maintainer backward compat: `uv run pytest tests/`**

    Command:
    ```
    uv run pytest tests/ --collect-only -q 2>&1 | tail -10
    ```

    Capture:
    - Exit code
    - Collected count (should be Recipe 1 count + Recipe 2 count)
    - Verify: BOTH `tests/contract/` and `tests/framework/` paths appear in the collection output

    Verdict: PASS if the count is approximately the sum of Recipes 1+2 (small drift OK due to conftest hooks or parametrize), AND both path prefixes are present.

    ---

    **Recipe 4 — Operator default: `mcp-test-framework run` (contract only)**

    Command (use whichever config is appropriate for the host):
    ```
    uv run mcp-test-framework run --raw 2>&1 | head -100
    ```

    Note: `--raw` is used here to surface pytest's raw collection paths so we can grep them; the default domain UI render hides path strings. The `--raw` mode for Phase 15's purpose still passes through `tests/contract/` as the scope (per Plan 02 D-11).

    Capture:
    - Exit code (0 if green; 5 if no contract tools enabled; 1 if a contract test fails)
    - Verify: `tests/framework/` does NOT appear anywhere in the output (grep should return 0)
    - Verify: `tests/contract/` appears at least once

    Verdict: PASS if `tests/framework/` is absent from output and `tests/contract/` is present.

    ---

    **Recipe 5 — Operator opt-in: `mcp-test-framework run --with-framework`**

    Command:
    ```
    uv run mcp-test-framework run --with-framework --raw 2>&1 | head -120
    ```

    Capture:
    - Exit code
    - Verify: BOTH `tests/contract/` and `tests/framework/` appear in the output (each at least once)

    Verdict: PASS if both paths appear and exit code is 0 or 1 (test-failure outcomes are acceptable; we are verifying scope, not correctness).

    ---

    **Recipe 6 — SURFACE-03 history preservation**

    Commands:
    ```
    git log --follow --oneline tests/contract/test_mcp_tool_contract.py | head -10
    git log --follow --oneline tests/framework/test_banned_imports.py | head -10
    git log --follow --oneline tests/framework/test_isolation.py | head -10
    ```

    Capture full output of each. Verify:
    - Each command returns AT LEAST 2 commit lines (the rename commit from Plan 01 + at least one pre-move commit)
    - For `test_mcp_tool_contract.py`: at least one commit from Phase 04 era (April-May 2026 range; commit message likely references "fixtures", "test cases", or Phase 04 / 07)
    - For `test_banned_imports.py`: at least one commit from Phase 01 or Phase 12 era (banned-imports test was created early)
    - For `test_isolation.py`: at least one commit from Phase 06 era (isolation work)

    Verdict: PASS if all three `git log --follow` invocations show pre-move history.

    ---

    **Recipe 7 — SURFACE-04 enforcement at the literal path**

    Command:
    ```
    uv run pytest tests/framework/test_banned_imports.py -v
    ```

    Capture:
    - Exit code (0 = test passes, 1 = test fails)
    - Verify: pytest discovers the test at the SURFACE-04 literal path (no "file not found" or "no tests collected" outcome)

    Verdict: PASS if the test runs and exits 0 (meaning no banned imports exist in `src/`). Exit 1 would indicate a real banned-imports regression somewhere in `src/`, which is OUT OF SCOPE for Phase 15 verification but should be reported.

    ---

    Compile all 7 results into a single `15-VERIFICATION.md` with this structure:

    ```markdown
    # Phase 15 Verification — 2026-05-11

    **Status:** [PASS | FAIL | PARTIAL]
    **Recorded by:** [agent name]

    ## SC-1: Operator default scope is contract only
    Recipe 4 verdict: [PASS|FAIL]
    Evidence: [paste relevant output excerpt — collected count, grep results]

    ## SC-2: --with-framework opt-in extends to framework subtree
    Recipe 5 verdict: [PASS|FAIL]
    Recipe 3 (maintainer backward compat) verdict: [PASS|FAIL]
    Evidence: [paste excerpts]

    ## SC-3: git history preserved for moved files
    Recipe 6 verdicts (3 files): [PASS|FAIL each]
    Evidence: [paste git log --follow excerpts — first 5 lines per file]

    ## SC-4: black-box enforcement at SURFACE-04 literal path
    Recipe 7 verdict: [PASS|FAIL]
    Evidence: [pytest output]

    ## Supporting recipes
    Recipe 1 (tests/contract/ standalone) verdict: [PASS|FAIL]
    Recipe 2 (tests/framework/ standalone) verdict: [PASS|FAIL]
    Recipe 3 (tests/ both subtrees) verdict: [PASS|FAIL]

    ## Outstanding issues
    [If any recipe FAILed, describe the failure and the proposed fix. Otherwise: "None — all 7 recipes passed."]
    ```

    Commit:
    ```
    git add .planning/phases/15-operator-vs-framework-test-surface-split/15-VERIFICATION.md
    git commit -m "docs(15): record verification recipe outputs for SURFACE-01..04"
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/15-operator-vs-framework-test-surface-split/15-VERIFICATION.md && grep -q "SC-1" .planning/phases/15-operator-vs-framework-test-surface-split/15-VERIFICATION.md && grep -q "SC-2" .planning/phases/15-operator-vs-framework-test-surface-split/15-VERIFICATION.md && grep -q "SC-3" .planning/phases/15-operator-vs-framework-test-surface-split/15-VERIFICATION.md && grep -q "SC-4" .planning/phases/15-operator-vs-framework-test-surface-split/15-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `15-VERIFICATION.md` exists and contains the 4 SC sections + 3 supporting recipes
    - Recipe 4 evidence shows `grep -c "tests/framework" <output>` == 0 (SC-1)
    - Recipe 5 evidence shows BOTH `tests/contract` and `tests/framework` present (SC-2)
    - Recipe 6 evidence shows >=2 commit lines for each of the 3 files checked (SC-3)
    - Recipe 7 evidence shows pytest collected and ran the test from the SURFACE-04 literal path (SC-4)
    - File is committed with prefix `docs(15):`
    - Overall Status line is either PASS (all 7 recipes pass) or PARTIAL with explicit issue list
  </acceptance_criteria>
  <done>The verification artifact records all 7 recipe outcomes. Each ROADMAP success criterion (SC-1..SC-4) has at least one recipe verdict supporting it. The artifact is committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human approves the verification artifact</name>
  <what-built>
    Plans 15-01 through 15-03 split `tests/` into `tests/contract/` + `tests/framework/`, flipped the runner's default scope, added `--with-framework`, refreshed README/spec, and Plan 15-04 Task 1 recorded the verification recipe outputs in `15-VERIFICATION.md`.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/15-operator-vs-framework-test-surface-split/15-VERIFICATION.md` in full.
    2. Confirm the overall Status line says PASS (not PARTIAL or FAIL).
    3. Spot-check Recipe 4's evidence: the operator-default invocation must not show `tests/framework/` anywhere — that is the central operator-perceivable win of Phase 15.
    4. Spot-check Recipe 6: all three `git log --follow` outputs should show genuine pre-move commits (not just the rename commit). If any file's history shows only 1 line (the rename), history was lost — REJECT and ask for investigation.
    5. Spot-check Recipe 7: `tests/framework/test_banned_imports.py` ran and passed at the SURFACE-04 literal path. If it ran but FAILED (exit 1), that means a real banned-imports regression exists in `src/` — out of Phase 15 scope but worth surfacing.
    6. Confirm the working tree is clean: `git status` returns nothing.
    7. Confirm the Phase 15 plan list in ROADMAP.md (lines 111-116) accurately lists the 4 Phase 15 plans (NOT the stale Phase 13 plan list that is currently there as a copy-paste placeholder).
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 15, or describe specific issues that need a 15-05 gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator-vs-verification | The verification artifact is markdown-only; no executable content. The recipes themselves invoke the framework against the operator's configured MCP server — same trust surface as a normal run. No new boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | T (tampering) | falsified verdicts | mitigate | The human checkpoint at Task 2 forces an operator to spot-check evidence excerpts, not just trust the verdict line. Recipe 6's `git log --follow` output is reproducible from any clone — the verdict is auditable post-hoc. LOW severity. |
| T-15-11 | I (information disclosure) | verification artifact contents | accept | Outputs may include the operator's MCP server command (from Recipes 4/5) — same exposure as any other run log; project-local. NONE severity. |
| T-15-12 | D (denial of service) | live-MCP recipes hanging | mitigate | Recipes 4/5 invoke the live MCP subprocess; if the SUT hangs, the recipe hangs. Use the framework's existing `asyncio.timeout` guards (Phase 02 contract) — no new mitigation needed; mention in operator notes if a hang occurs. LOW severity. |

**Overall threat surface: NONE / LOW.** Documentation artifact + recipes that exercise the same code paths as a normal user-initiated run.
</threat_model>

<verification>
- `15-VERIFICATION.md` exists and is committed
- Human checkpoint approved
- ROADMAP.md Phase 15 plan list and progress row accurately reflect 4/4 plans complete
- Working tree is clean
</verification>

<success_criteria>
- All 4 ROADMAP success criteria (SC-1..SC-4) have explicit PASS verdicts in 15-VERIFICATION.md
- Human-verified evidence quality at the checkpoint
- Phase 15 ready for ROADMAP closeout
</success_criteria>

<output>
After completion, create `.planning/phases/15-operator-vs-framework-test-surface-split/15-04-SUMMARY.md` describing the 7 recipe outcomes, the human approval, and any deferred items (e.g., smoke-test audit if Recipe 2 surfaced obsolete tests).
</output>
