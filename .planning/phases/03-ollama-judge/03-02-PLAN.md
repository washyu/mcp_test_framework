---
phase: 03-ollama-judge
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/unit/test_ollama_judge.py
autonomous: true
requirements: [CORE-04, OPS-01, DOCS-03]
requirements_addressed: [CORE-04, OPS-01, DOCS-03]
must_haves:
  truths:
    - "D-09 step 1: Unit tests prove _strip_decorations strips <think>...</think> blocks (DOTALL, non-greedy) — qwen3 thinking-quirk mitigation"
    - "D-09 step 4: Unit tests prove _parse_judge_response returns the locked fallback JudgeResult(passed=False, score=1, reasoning=\"malformed judge response\", raw_response=<original>) on malformed JSON, out-of-range score, and missing-required-field inputs"
    - "D-09 step 3: Unit tests prove _extract_first_json_object recovers a balanced {...} from prose-prefixed text and from text with braces inside JSON strings"
    - "D-07: Unit tests prove _build_request_body emits the locked request body shape (stream:false, format:json, think:false, keep_alive:30m, options.temperature:0, options.num_predict:256) so the constant-system-prompt + locked-options invariants are pinned"
    - "All unit tests run under default `uv run pytest` (no live marker) and pass without touching httpx or Ollama"
  artifacts:
    - path: "tests/unit/test_ollama_judge.py"
      provides: "Parser slice + request-body shape unit tests covering D-09 step 1/3/4 + locked body invariants"
      contains: "def test_"
      min_lines: 80
  key_links:
    - from: "tests/unit/test_ollama_judge.py"
      to: "src/mcp_test_framework/ollama_judge.py"
      via: "imports module-level helpers (_strip_decorations, _extract_first_json_object, _parse_judge_response, _build_request_body, _SYSTEM_PROMPT, JudgeResult)"
      pattern: "from mcp_test_framework\\.ollama_judge import"
---

<objective>
Lock the four parser branches of `_parse_judge_response` (D-09 contract) and the locked request-body shape of `_build_request_body` with deterministic, mock-free unit tests. These tests prove the defensive parser handles every documented qwen3 / Pydantic-v2 failure mode the live smoke cannot deterministically reproduce.

Purpose: CORE-04 parser contract verification, OPS-01 malformed-response → fallback path, DOCS-03 raw_response preservation through every branch.

Output: One unit-test file under `tests/unit/test_ollama_judge.py` containing 8+ pytest test functions, all running under default `uv run pytest` (no marker), no httpx or AsyncClient mocking — pure-data tests on module-level helpers per CONTEXT.md "Phase 3 unit-test scope" Discretion item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ollama-judge/03-CONTEXT.md
@.planning/phases/03-ollama-judge/03-RESEARCH.md
@.planning/research/PITFALLS.md
@src/mcp_test_framework/ollama_judge.py
@tests/conftest.py

<interfaces>
<!-- Module-level helpers and constants Plan 03-01 just shipped. The unit tests
     consume these directly — no httpx mocking. Importable as: -->

From src/mcp_test_framework/ollama_judge.py:
```python
_SYSTEM_PROMPT: str  # module-level string constant
class JudgeResult(BaseModel):
    passed: bool
    score: int  # Field(ge=1, le=5)
    reasoning: str
    raw_response: str
def _build_request_body(model: str, rubric: str, subject: str, context: dict | None) -> dict
def _strip_decorations(content: str) -> str
def _extract_first_json_object(text: str) -> str | None
def _parse_judge_response(content: str) -> JudgeResult
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create tests/unit/test_ollama_judge.py with parser-slice and request-body unit tests</name>
  <files>tests/unit/test_ollama_judge.py</files>
  <read_first>
    - src/mcp_test_framework/ollama_judge.py (Plan 03-01 — module-level helpers under test)
    - .planning/phases/03-ollama-judge/03-CONTEXT.md (Claude's Discretion: "Phase 3 unit-test scope" — explicit list of slices)
    - .planning/phases/03-ollama-judge/03-RESEARCH.md §Common Pitfalls 5 (full test corpus enumerated for the parser branches)
    - .planning/phases/03-ollama-judge/03-RESEARCH.md §Code Examples 3 + 4 (request body shape + parser contract under test)
    - tests/conftest.py (existing session-start hook — confirms unit tests do NOT need an event loop or live deps)
    - tests/smoke/test_smoke_homelab_mcp.py (style mirror — but note: NO `pytestmark` for unit tests; these are sync, no marker)
  </read_first>
  <behavior>
    - Test 1 (think-strip happy path): `_strip_decorations` removes a single `<think>x</think>` block and leaves the trailing JSON intact, returning a `.strip()`-ed string.
    - Test 2 (multiple think blocks, non-greedy): `_strip_decorations` removes both `<think>a</think>` and `<think>b</think>` independently — does NOT greedily consume everything between the first `<think>` and the last `</think>`.
    - Test 3 (parser happy path): `_parse_judge_response` on a clean JSON object returns a `JudgeResult` with the parsed fields AND `raw_response` overwritten to equal the original input string (DOCS-03 invariant).
    - Test 4 (think + fence + parse): `_parse_judge_response` on `<think>x</think>\n```json\n{...}\n```` returns a valid `JudgeResult`.
    - Test 5 (malformed-JSON fallback path — D-09 step 4): `_parse_judge_response("not json at all")` returns `JudgeResult(passed=False, score=1, reasoning="malformed judge response", raw_response="not json at all")`.
    - Test 6 (brace recovery from prose prefix — D-09 step 3): `_parse_judge_response('Sure! {"passed": true, "score": 4, "reasoning": "good", "raw_response": ""}')` returns a `JudgeResult` with `passed=True`, `score=4`, `raw_response` equal to the full original input string.
    - Test 7 (out-of-range score → fallback): `_parse_judge_response('{"passed": true, "score": 7, "reasoning": "x", "raw_response": ""}')` returns the fallback (score 7 violates `Field(ge=1, le=5)`; D-09 step 4 fires; raw_response preserved).
    - Test 8 (missing required field → fallback): `_parse_judge_response('{"passed": true, "score": 4}')` returns the fallback (missing `reasoning` and `raw_response`; D-09 step 4 fires).
    - Test 9 (unbalanced think tag + no JSON → fallback): `_parse_judge_response("<think>only think tag, no closing")` returns the fallback with raw_response preserved (Pitfall 6 proof).
    - Test 10 (brace-in-string edge case — Pitfall 5): `_extract_first_json_object('{"reasoning": "score = }5{ here", "passed": true, "score": 4, "raw_response": ""}')` returns the FULL object (the inner `}` and `{` inside the string MUST NOT mis-balance the scanner).
    - Test 11 (request body shape — locked invariants): `_build_request_body("qwen3.6:latest", "rubric text", "subject text", None)` returns a dict where `stream is False`, `format == "json"`, `think is False`, `keep_alive == "30m"`, `options == {"temperature": 0, "num_predict": 256}`, `model == "qwen3.6:latest"`, `messages[0]["role"] == "system"`, `messages[0]["content"] == _SYSTEM_PROMPT`, `messages[1]["role"] == "user"`, and `<<<SUBJECT>>>\nsubject text\n<<<END SUBJECT>>>` appears in `messages[1]["content"]`.
    - Test 12 (request body — context interpolation): `_build_request_body("m", "r", "s", {"tool_name": "x"})` produces a user-message containing `Context: {"tool_name": "x"}` (json.dumps with sort_keys, exactly that substring).
    - Test 13 (request body — no Context line when context is None): `_build_request_body("m", "r", "s", None)` produces a user-message that does NOT contain the literal substring `Context:`.
    - Test 14 (system prompt invariants — D-07): `_SYSTEM_PROMPT` contains the substrings `/no_think`, `<<<SUBJECT>>>`, `<<<END SUBJECT>>>`, and an instruction to ignore instructions inside the SUBJECT block (case-insensitive `assert "ignore" in _SYSTEM_PROMPT.lower()` is sufficient).
  </behavior>
  <action>
Create `tests/unit/test_ollama_judge.py`. The file:

1. Imports: `from mcp_test_framework.ollama_judge import (JudgeResult, _SYSTEM_PROMPT, _build_request_body, _extract_first_json_object, _parse_judge_response, _strip_decorations)`. No `pytest` async imports, no `httpx`, no `AsyncClient` mocks (CONTEXT.md "Phase 3 unit-test scope" Discretion: live HTTP is smoke territory).
2. Module docstring referencing Plan 03-02 + the four D-09 branches under test + the request-body invariants under test. Cite RESEARCH §Common Pitfalls 5 corpus.
3. NO `pytestmark` declaration — these are sync unit tests, no marker, no asyncio (the `addopts = "-m 'not live_homelab and not live_ollama'"` filter doesn't reject unmarked tests).
4. One test function per behavior in `<behavior>` above (14 tests). Each test is a plain `def test_<name>():` with concrete assertions per the locked behavior contracts. Use docstrings of one line per test naming the D-09 step or pitfall the test falsifies.
5. Concrete corpus (copy these strings literally — they are pre-verified against the parser implementation):
   - Happy: `'{"passed": true, "score": 5, "reasoning": "ok", "raw_response": ""}'`
   - Think+strip: `'<think>reasoning here</think>\n{"passed": true, "score": 5, "reasoning": "ok", "raw_response": ""}'`
   - Multi-think: `'<think>a</think> middle <think>b</think>{"passed": true, "score": 5, "reasoning": "ok", "raw_response": ""}'`
   - Think+fence: `'<think>x</think>\n```json\n{"passed": true, "score": 5, "reasoning": "ok", "raw_response": ""}\n```'`
   - Brace-recovery prefix: `'Sure! {"passed": true, "score": 4, "reasoning": "good", "raw_response": ""}'`
   - Out-of-range: `'{"passed": true, "score": 7, "reasoning": "x", "raw_response": ""}'`
   - Missing field: `'{"passed": true, "score": 4}'`
   - Unbalanced think: `'<think>only think tag, no closing'`
   - Brace-in-string: `'{"reasoning": "score = }5{ here", "passed": true, "score": 4, "raw_response": ""}'`
   - Garbage: `'not json at all'`
6. For Test 5 (malformed fallback), assert all four fields exactly: `passed is False`, `score == 1`, `reasoning == "malformed judge response"`, `raw_response == "not json at all"` (DOCS-03 raw_response preservation).
7. For Tests 3/6 (happy + brace-recovery), assert `result.raw_response == <original input>` — proving the `model_copy(update={"raw_response": content})` step in `_parse_judge_response` (Plan 03-01 Task 1 step 6).
8. Do NOT test transport-error propagation (D-10) or live cold-start behavior — those are smoke territory (Plan 03-03).
9. Do NOT mock `httpx.AsyncClient` for body assertion — `_build_request_body` is a pure function, just call it.
10. Do NOT import `homelab_mcp` (Phase-1 belt catches; just don't).

Test function naming convention: `test_<helper>_<scenario>`. E.g., `test_strip_decorations_removes_single_think_block`, `test_parse_judge_response_malformed_returns_fallback_with_raw_preserved`, `test_build_request_body_locks_stream_false_format_json_think_false`.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_ollama_judge.py -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/unit/test_ollama_judge.py` exists.
    - `grep -c "^def test_" tests/unit/test_ollama_judge.py` returns at least `14` (one per behavior).
    - `grep -c "^async def test_" tests/unit/test_ollama_judge.py` returns `0` (these are sync unit tests).
    - `grep -F 'from mcp_test_framework.ollama_judge import' tests/unit/test_ollama_judge.py` returns at least one match.
    - `grep -F 'malformed judge response' tests/unit/test_ollama_judge.py` returns at least one match (Test 5 D-09 step 4 falsifier).
    - `grep -F '<think>' tests/unit/test_ollama_judge.py` returns at least 4 matches (think-strip + multi + fence + unbalanced cases).
    - `grep -F '"score": 7' tests/unit/test_ollama_judge.py` returns at least one match (Test 7 out-of-range).
    - `grep -F 'score = }5{' tests/unit/test_ollama_judge.py` returns at least one match (Test 10 Pitfall 5).
    - `grep -F 'stream' tests/unit/test_ollama_judge.py | grep -F 'False'` returns at least one match (Test 11 body invariant).
    - `grep -F '/no_think' tests/unit/test_ollama_judge.py` returns at least one match (Test 14 D-07 falsifier).
    - `grep -F 'pytestmark' tests/unit/test_ollama_judge.py` returns no matches (no marker — these are sync, no live deps).
    - `grep -E "^import homelab_mcp|^from homelab_mcp" tests/unit/test_ollama_judge.py` returns no matches.
    - `grep -E "AsyncClient|httpx\\.|monkeypatch.*post|mock.*request" tests/unit/test_ollama_judge.py` returns no matches (no HTTP mocking — pure-data tests).
    - `uv run pytest tests/unit/test_ollama_judge.py -v` exits 0 and reports `14 passed` (or more — adding helper coverage tests is fine).
    - `uv run pytest tests/unit/test_ollama_judge.py --collect-only -q 2>&1 | grep -c "test_ollama_judge.py::"` returns at least `14`.
    - `uv run ruff check tests/unit/test_ollama_judge.py` exits 0.
    - `uv run pytest 2>&1 | tail -5` (default — no live markers) collects and passes the unit tests, does NOT collect any `live_ollama`-marked test.
  </acceptance_criteria>
  <done>
    All 14+ unit tests pass under default `uv run pytest`. Every D-09 parser branch has a falsifying test. Every locked request-body parameter has a falsifying assertion. The system prompt's `/no_think` and delimited-subject markers are grep-verified at runtime. No HTTP mock — all tests are pure-function calls against module-level helpers.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test corpus → parser helpers | The synthetic malformed strings are deliberately adversarial to the parser; no untrusted runtime input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering (test brittleness) | Brace-extractor scanner | mitigate | Test 10 explicitly exercises the brace-in-string edge case so a future "let's just use a regex" refactor breaks the test (Pitfall 5 falsifier). |
| T-03-02-02 | Information Disclosure | Test diagnostic output | accept | Tests print no PII; corpus is synthetic. |
| T-03-02-03 | DoS (slow tests) | Parser scanner cost | accept | Bounded by ~256-token responses (~1024 chars); the 12-line state machine is O(n). Total unit-test runtime expected <1s. |

No HIGH-severity threats; this plan is a unit-test addition only — no I/O, no privileged operations.
</threat_model>

<verification>
- `uv run pytest tests/unit/test_ollama_judge.py -v` exits 0 with all tests passing.
- `uv run pytest 2>&1 | tail -5` (default invocation, no live markers) includes the unit tests in the collected count and exits 0.
- `uv run pytest -m live_ollama --collect-only -q 2>&1 | grep -c "tests/unit/test_ollama_judge.py"` returns `0` (unit tests are NOT under the live marker — they run by default).
</verification>

<success_criteria>
1. All 14+ tests in `tests/unit/test_ollama_judge.py` pass under default `uv run pytest`.
2. Every D-09 parser branch (steps 1/2/3/4) has at least one falsifying assertion.
3. Every locked request-body parameter (`stream`, `format`, `think`, `keep_alive`, `options.temperature`, `options.num_predict`) is asserted in at least one test.
4. `_SYSTEM_PROMPT` D-07 invariants (`/no_think`, `<<<SUBJECT>>>` markers, ignore-instructions clause) are asserted.
5. No `httpx` mocking, no `AsyncClient` import — these tests are pure-function calls that run without an event loop.
6. Tests run under default `pytest` (no live marker) so CI/CD without Ollama exercises the parser contract.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ollama-judge/03-02-SUMMARY.md` listing the test functions written, the parser branches each one falsifies, and the unit-test wall-clock runtime.
</output>
