---
phase: 03-ollama-judge
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - tests/smoke/test_smoke_ollama_judge.py
autonomous: true
requirements: [CORE-04, OPS-01, OPS-02, DOCS-03]
requirements_addressed: [CORE-04, OPS-01, OPS-02, DOCS-03]
must_haves:
  truths:
    - "tests/smoke/test_smoke_ollama_judge.py is gated behind @pytest.mark.live_ollama and skipped by default"
    - "SC#1 falsifier asserts isinstance(OllamaJudge(...), Judge), inspect.signature shape, and inspect.iscoroutinefunction(OllamaJudge.judge)"
    - "SC#2 falsifier invokes async with OllamaJudge(...) as judge, calls judge() against live Ollama, and asserts JudgeResult shape with 1<=score<=5, non-empty reasoning, non-empty raw_response"
    - "Smoke test fails fast inside the test body via httpx.ConnectError (D-10 propagation) when Ollama is unreachable — no collection-time skip"
    - "uv run pytest -m live_ollama collects exactly the smoke test; default uv run pytest does NOT collect it"
  artifacts:
    - path: "tests/smoke/test_smoke_ollama_judge.py"
      provides: "Live cold-start canary that falsifies SC#1 (Protocol seam) and SC#2 (cold-start judge call) against live Ollama"
      contains: "pytest.mark.live_ollama"
      min_lines: 60
  key_links:
    - from: "tests/smoke/test_smoke_ollama_judge.py"
      to: "src/mcp_test_framework/judge_protocol.py"
      via: "from mcp_test_framework.judge_protocol import Judge"
      pattern: "from mcp_test_framework\\.judge_protocol import Judge"
    - from: "tests/smoke/test_smoke_ollama_judge.py"
      to: "src/mcp_test_framework/ollama_judge.py"
      via: "OllamaJudge async context manager + judge() call"
      pattern: "async with OllamaJudge"
    - from: "tests/smoke/test_smoke_ollama_judge.py"
      to: "live Ollama at OLLAMA_BASE_URL"
      via: "judge() POST /api/chat"
      pattern: "await judge\\.judge\\("
---

<objective>
Ship the permanent live-Ollama smoke test that falsifies BOTH ROADMAP Phase 3 success criteria #1 (Protocol seam) and #2 (cold-start judge call returns a valid JudgeResult). Mirrors `tests/smoke/test_smoke_homelab_mcp.py` exactly — same `pytestmark` shape, same fail-fast-inside-body discipline, same two-test layout.

Purpose: D-02 / D-04 — the smoke is committed pytest, not a throwaway script; it covers SC#1 by name+signature+async layered assertions (Pitfall 4 mitigation) and SC#2 by invoking `judge()` against live Ollama with a canned synthetic subject (decoupled from `homelab-mcp` per D-04).

Output: One test file under `tests/smoke/test_smoke_ollama_judge.py` (~80 lines) gated behind `pytest.mark.live_ollama`. Default `uv run pytest` skips it; `uv run pytest -m live_ollama` runs it; running after `ollama stop qwen3.6:latest` exercises the cold-start path (the manual UAT analog from CONTEXT `<specifics>`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-ollama-judge/03-CONTEXT.md
@.planning/phases/03-ollama-judge/03-RESEARCH.md
@src/mcp_test_framework/ollama_judge.py
@src/mcp_test_framework/judge_protocol.py
@src/mcp_test_framework/config.py
@src/mcp_test_framework/models.py
@tests/smoke/test_smoke_homelab_mcp.py
@pyproject.toml

<interfaces>
<!-- Plan 03-01 just shipped these. Plan 03-03 consumes them as a pure import. -->

From src/mcp_test_framework/judge_protocol.py:
```python
@runtime_checkable
class Judge(Protocol):
    async def judge(self, rubric: str, subject: str, context: dict | None = None) -> JudgeResult: ...
```

From src/mcp_test_framework/ollama_judge.py:
```python
class JudgeResult(BaseModel):
    passed: bool
    score: int  # Field(ge=1, le=5)
    reasoning: str
    raw_response: str

class OllamaJudge:
    def __init__(self, base_url: str, model: str, timeout_seconds: int) -> None
    async def __aenter__(self) -> "OllamaJudge"
    async def __aexit__(self, exc_type, exc, tb) -> None
    async def judge(self, rubric: str, subject: str, context: dict | None = None) -> JudgeResult
```

From src/mcp_test_framework/config.py + models.py (already shipped Phase 1):
```python
class Config(BaseSettings):  # frozen
    ollama: OllamaConfig  # .base_url, .model, .timeout_seconds
    mcp_server: McpServerConfig
    target: TargetConfig
```

Style mirror — the file structurally twins tests/smoke/test_smoke_homelab_mcp.py:
```python
pytestmark = [pytest.mark.live_homelab, pytest.mark.asyncio(loop_scope="session")]
async def test_raw_stdio_lists_target_tool() -> None: ...
async def test_wrapper_call_tool_returns_non_error_with_content() -> None: ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tests/smoke/test_smoke_ollama_judge.py with SC#1 + SC#2 falsifiers</name>
  <files>tests/smoke/test_smoke_ollama_judge.py</files>
  <read_first>
    - tests/smoke/test_smoke_homelab_mcp.py (THE structural mirror — same `pytestmark` shape, same fail-fast-inside-body discipline, same two-test SC layout — copy the structure and replace the nouns)
    - src/mcp_test_framework/judge_protocol.py (Plan 03-01 Task 2 — Judge Protocol the smoke imports for SC#1)
    - src/mcp_test_framework/ollama_judge.py (Plan 03-01 Task 1 — `OllamaJudge`, `JudgeResult`)
    - src/mcp_test_framework/config.py (existing Phase 1 — `Config()` constructor)
    - .planning/phases/03-ollama-judge/03-CONTEXT.md D-02, D-03, D-04 (smoke shape + marker)
    - .planning/phases/03-ollama-judge/03-RESEARCH.md §Code Example 6 (the verbatim file body to copy)
    - .planning/phases/03-ollama-judge/03-RESEARCH.md §Common Pitfalls 4 (why `isinstance(j, Judge)` ALONE is not a load-bearing SC#1 falsifier — must layer signature + async assertions)
    - .planning/phases/03-ollama-judge/03-RESEARCH.md §Common Pitfalls 7 (cold-start observable signals — `load_duration` for diagnostic logging)
  </read_first>
  <action>
Create `tests/smoke/test_smoke_ollama_judge.py` containing the verbatim file body from RESEARCH §Code Example 6. The file:

1. **Module docstring** — citing D-02 (permanent pytest, not throwaway), D-04 (canned synthetic subject, decoupled from `homelab-mcp` so `live_ollama` and `live_homelab` failures are unambiguous), the two ROADMAP SC falsifiers, and the fail-fast-inside-body discipline (Ollama unreachable → `httpx.ConnectError` propagates per D-10).

2. **Imports:**
   ```python
   from __future__ import annotations
   import inspect
   import pytest
   from mcp_test_framework.config import Config
   from mcp_test_framework.judge_protocol import Judge
   from mcp_test_framework.ollama_judge import JudgeResult, OllamaJudge
   ```

3. **`pytestmark` declaration** (D-03 — direct `live_homelab` analog):
   ```python
   pytestmark = [
       pytest.mark.live_ollama,
       pytest.mark.asyncio(loop_scope="session"),
   ]
   ```
   The `live_ollama` marker is registered by Plan 03-01 Task 3; `loop_scope="session"` matches `asyncio_default_fixture_loop_scope = "session"` in `pyproject.toml` and the Phase-2 mirror.

4. **Two module-level constants** for the canned synthetic subject (D-04 — orthogonal to `homelab-mcp`):
   - `_CANNED_RUBRIC` — a one-paragraph rubric scoring tool description clarity (1=useless, 5=unambiguous, penalize verbosity).
   - `_CANNED_SUBJECT` — a plausible 2-3 line MCP tool description (e.g., a `get_weather` tool with one parameter `city: string`). Use the literals from RESEARCH §Code Example 6 verbatim.

5. **Test 1 (SC#1 falsifier — sync, NO `async def`):** `def test_judge_protocol_satisfied_by_ollama_judge() -> None:` — three layered assertions per Pitfall 4:
   - **Layer 1 (cheap front-line):** `j = OllamaJudge(base_url="http://unused", model="unused", timeout_seconds=1); assert isinstance(j, Judge), <diagnostic>` — proves the runtime-checkable seam exists. (Note: `OllamaJudge.__init__` does not connect; constructing it is safe without entering the async context manager.)
   - **Layer 2 (signature, locks D-05 spec-verbatim):** `sig = inspect.signature(OllamaJudge.judge); assert list(sig.parameters) == ["self", "rubric", "subject", "context"]; assert sig.parameters["context"].default is None`. Optionally also assert `sig.return_annotation is JudgeResult or sig.return_annotation == "JudgeResult"` (the `from __future__ import annotations` import in `ollama_judge.py` may stringify this — accept either).
   - **Layer 3 (async-ness):** `assert inspect.iscoroutinefunction(OllamaJudge.judge), <diagnostic>`.
   This test runs synchronously — it does NOT use `await`, does NOT enter the `OllamaJudge` async context manager, and does NOT touch the network. The pytest-asyncio session loop hosts the marker but no awaitable is invoked.

6. **Test 2 (SC#2 falsifier — `async def`):** `async def test_cold_start_returns_valid_judge_result() -> None:`:
   - `cfg = Config()` (loads precedence chain — same pattern as the homelab smoke).
   - `async with OllamaJudge(cfg.ollama.base_url, cfg.ollama.model, cfg.ollama.timeout_seconds) as judge:` (this is the cold-start canary because no warmup runs anywhere upstream — D-08).
   - `result = await judge.judge(_CANNED_RUBRIC, _CANNED_SUBJECT)` (no `context` arg — exercises the `context is None` branch of `_build_request_body`).
   - Assertions on the JudgeResult shape:
     - `assert isinstance(result, JudgeResult), f"got {type(result).__name__}: {result!r}"`
     - `assert isinstance(result.passed, bool)` (Pydantic guarantees this, but make the contract explicit)
     - `assert 1 <= result.score <= 5, f"score out of range: {result!r}"`
     - `assert result.reasoning.strip(), f"empty reasoning: {result!r}"`
     - `assert result.raw_response.strip(), f"empty raw_response: {result!r}"` (DOCS-03 raw_response surfacing — proves the parser overwrote `raw_response` with the actual model output).
   - `print(f"judge result: {result!r}")` — diagnostic for the manual cold-start UAT (parallel to the Phase-2 02.1-style human-UAT pattern; the verifier captures stdout when running `uv run pytest -m live_ollama -v -s`).
   - **Do NOT add a try/except around `judge()`** — D-10: transport errors propagate up; `httpx.ConnectError` (Ollama unreachable) and `httpx.TimeoutException` (cold-start exceeds 120s) MUST bubble out so pytest reports them with the actual exception type and traceback. The Phase 4 fixtures (FIX-02 `_preflight`) will surface these more politely; Phase 3 ships a thin canary.

7. **No `try/except`, no `pytest.skip(...)` for missing Ollama** — D-03/D-04 lock fail-fast-inside-body. If Ollama isn't running, the test fails with `httpx.ConnectError`; that's the intended diagnostic. The marker-skip mechanism (`addopts`) handles "I don't want to run this test" — the test itself never silently skips.

8. **No `import homelab_mcp` anywhere** — Phase-1 belt catches.

9. **No `JudgeResult` mocking, no httpx mocking, no fakes** — this is THE live integration smoke; the parser unit tests (Plan 03-02) cover the deterministic surface, this file covers everything that depends on a real Ollama at `cfg.ollama.base_url`.

The full file body is approximately 60-80 lines including the docstring. Copy verbatim from RESEARCH §Code Example 6, which is pre-verified against the Phase-2 mirror and the Plan-03-01 module surface.
  </action>
  <verify>
    <automated>uv run pytest tests/smoke/test_smoke_ollama_judge.py --collect-only -q 2>&1 | tail -20 && uv run pytest -m live_ollama tests/smoke/test_smoke_ollama_judge.py::test_judge_protocol_satisfied_by_ollama_judge -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/smoke/test_smoke_ollama_judge.py` exists.
    - `grep -c "pytest.mark.live_ollama" tests/smoke/test_smoke_ollama_judge.py` returns at least `1`.
    - `grep -c "pytest.mark.asyncio(loop_scope=\"session\")" tests/smoke/test_smoke_ollama_judge.py` returns at least `1`.
    - `grep -F 'from mcp_test_framework.judge_protocol import Judge' tests/smoke/test_smoke_ollama_judge.py` returns at least one match.
    - `grep -F 'from mcp_test_framework.ollama_judge import' tests/smoke/test_smoke_ollama_judge.py` returns at least one match.
    - `grep -F 'from mcp_test_framework.config import Config' tests/smoke/test_smoke_ollama_judge.py` returns at least one match.
    - `grep -c "def test_judge_protocol_satisfied_by_ollama_judge" tests/smoke/test_smoke_ollama_judge.py` returns `1` (SC#1 sync test).
    - `grep -c "async def test_cold_start_returns_valid_judge_result" tests/smoke/test_smoke_ollama_judge.py` returns `1` (SC#2 async test).
    - `grep -F 'isinstance(j, Judge)' tests/smoke/test_smoke_ollama_judge.py` returns at least one match (SC#1 Layer 1).
    - `grep -F 'inspect.signature(OllamaJudge.judge)' tests/smoke/test_smoke_ollama_judge.py` returns at least one match (SC#1 Layer 2 — Pitfall 4 mitigation).
    - `grep -F 'inspect.iscoroutinefunction(OllamaJudge.judge)' tests/smoke/test_smoke_ollama_judge.py` returns at least one match (SC#1 Layer 3).
    - `grep -F 'async with OllamaJudge(' tests/smoke/test_smoke_ollama_judge.py` returns at least one match (SC#2 lifecycle).
    - `grep -F 'await judge.judge(' tests/smoke/test_smoke_ollama_judge.py` returns at least one match (SC#2 invocation).
    - `grep -F '1 <= result.score <= 5' tests/smoke/test_smoke_ollama_judge.py` returns at least one match (score-bound assertion).
    - `grep -F 'result.raw_response' tests/smoke/test_smoke_ollama_judge.py` returns at least one match (DOCS-03 falsifier).
    - `grep -F '_CANNED_RUBRIC' tests/smoke/test_smoke_ollama_judge.py` returns at least 2 matches (definition + use).
    - `grep -F '_CANNED_SUBJECT' tests/smoke/test_smoke_ollama_judge.py` returns at least 2 matches (definition + use).
    - `grep -E "pytest.skip|@pytest.mark.skipif" tests/smoke/test_smoke_ollama_judge.py` returns no matches (D-04 fail-fast — no silent skip).
    - `grep -E "^import homelab_mcp|^from homelab_mcp" tests/smoke/test_smoke_ollama_judge.py` returns no matches.
    - `grep -E "Mock|monkeypatch|AsyncMock" tests/smoke/test_smoke_ollama_judge.py` returns no matches (no mocks in smoke).
    - Default `uv run pytest --collect-only -q 2>&1` does NOT collect `tests/smoke/test_smoke_ollama_judge.py` (filtered by `addopts = "-m 'not live_homelab and not live_ollama'"`).
    - `uv run pytest -m live_ollama --collect-only -q 2>&1 | grep -c "test_smoke_ollama_judge.py::"` returns at least `2` (both tests collected under the marker).
    - `uv run pytest -m live_ollama tests/smoke/test_smoke_ollama_judge.py::test_judge_protocol_satisfied_by_ollama_judge -v 2>&1` exits 0 (SC#1 test does not require live Ollama — it constructs OllamaJudge but never enters the context manager).
    - `uv run ruff check tests/smoke/test_smoke_ollama_judge.py` exits 0.
    - `grep -c "Unknown pytest.mark.live_ollama" /tmp/pytest_collect.txt 2>/dev/null` is `0` OR (acceptable PowerShell equivalent) `uv run pytest --collect-only -q 2>&1` produces no `Unknown pytest.mark.live_ollama` warnings.
  </acceptance_criteria>
  <done>
    Smoke test file exists, gated by `live_ollama` marker, contains both SC falsifiers. Default `uv run pytest` collection excludes it. `uv run pytest -m live_ollama` collects exactly the two tests. SC#1 (sync) test runs offline and passes against the Plan-03-01 modules. SC#2 (async) test passes when Ollama is reachable; fails fast with `httpx.ConnectError` when not (D-10 propagation). Manual cold-start UAT pattern: `ollama stop qwen3.6:latest && uv run pytest -m live_ollama -v -s` — recorded as a Phase 5 README task; not in scope here.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Smoke test → live Ollama at `cfg.ollama.base_url` | HTTP traffic crosses the homelab LAN; Ollama is treated as trusted (locked-network dependency, same posture as Plan 03-01 T-03-04). |
| Canned synthetic subject → judge prompt body | Hard-coded test fixture; not user input. No prompt-injection risk inside this test (subject is author-controlled). |
| Test stdout (`raw_response`) → CI / developer terminal | `print(f"judge result: {result!r}")` surfaces full `JudgeResult` including `raw_response` for cold-start UAT inspection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | DoS (test hangs on cold-start) | `judge.judge(...)` HTTP call | mitigate | The 120s `httpx.Timeout` in `OllamaJudge.__aenter__` (Plan 03-01 Task 1) bounds the call. `ollama stop` mid-call → `httpx.ReadTimeout` propagates per D-10, surfaces as test failure within the 120s ceiling. |
| T-03-03-02 | Tampering (smoke runs by accident in CI without Ollama) | Marker filter | mitigate | `addopts = "-m 'not live_homelab and not live_ollama'"` (Plan 03-01 Task 3) excludes both live markers from default `uv run pytest`. CI must explicitly opt-in via `pytest -m live_ollama` to invoke this smoke. Acceptance criteria above grep-verify the default-skip behavior. |
| T-03-03-03 | Information Disclosure | `print(f"judge result: {result!r}")` | accept | Diagnostic output for cold-start UAT; canned synthetic subject contains no PII. Phase 5 README will document running with `-s` for verbose stdout. |
| T-03-03-04 | Spoofing | Live Ollama at `cfg.ollama.base_url` | accept | Inherited from Plan 03-01 T-03-04 — locked-network homelab dependency, no auth, no TLS, out of MVP scope. |

No HIGH-severity threats requiring code beyond the Plan 03-01 mitigations already in place. This plan adds only test code that consumes the Plan-03-01 surface.
</threat_model>

<verification>
- `uv run pytest --collect-only -q 2>&1` (default invocation) does NOT collect `tests/smoke/test_smoke_ollama_judge.py` and produces no `Unknown pytest.mark.live_ollama` warnings.
- `uv run pytest -m live_ollama --collect-only -q 2>&1 | grep -c "test_smoke_ollama_judge.py::"` returns at least `2`.
- `uv run pytest -m live_ollama tests/smoke/test_smoke_ollama_judge.py::test_judge_protocol_satisfied_by_ollama_judge -v 2>&1` exits 0 (SC#1 runs offline).
- `uv run pytest -m live_ollama tests/smoke/test_smoke_ollama_judge.py::test_cold_start_returns_valid_judge_result -v -s 2>&1` exits 0 when Ollama is reachable (SC#2 cold-start pass — operator UAT).
- `uv run ruff check tests/smoke/test_smoke_ollama_judge.py` exits 0.
</verification>

<success_criteria>
1. `tests/smoke/test_smoke_ollama_judge.py` exists with two test functions: SC#1 sync, SC#2 async.
2. `pytestmark` declaration includes both `pytest.mark.live_ollama` and `pytest.mark.asyncio(loop_scope="session")`.
3. SC#1 test layers `isinstance` + `inspect.signature` + `inspect.iscoroutinefunction` assertions (Pitfall 4 mitigation — `runtime_checkable` alone is not a load-bearing falsifier).
4. SC#2 test enters `OllamaJudge` as `async with`, calls `await judge.judge(_CANNED_RUBRIC, _CANNED_SUBJECT)`, asserts `JudgeResult` shape with `1 <= score <= 5`, non-empty `reasoning`, non-empty `raw_response`.
5. No `pytest.skip` or `skipif` — fail-fast inside body via `httpx.ConnectError` when Ollama is unreachable (D-10 propagation).
6. No mocks, no fakes — this is THE live integration canary.
7. Default `uv run pytest` skips it; `uv run pytest -m live_ollama` runs it; `pyproject.toml` marker registration (Plan 03-01 Task 3) makes the marker known.
</success_criteria>

<output>
After completion, create `.planning/phases/03-ollama-judge/03-03-SUMMARY.md` documenting:
1. The exact `_CANNED_RUBRIC` / `_CANNED_SUBJECT` literals chosen.
2. The result of running `uv run pytest -m live_ollama -v -s` against live Ollama (capture stdout including the `judge result:` print) — Phase 3 verification step.
3. Whether the manual cold-start UAT (`ollama stop qwen3.6:latest && uv run pytest -m live_ollama -v -s`) passed — record `load_duration_ns` for the Phase 5 README ceiling guidance.
</output>
