---
phase: 03-ollama-judge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_test_framework/ollama_judge.py
  - src/mcp_test_framework/judge_protocol.py
  - pyproject.toml
autonomous: true
requirements: [CORE-04, OPS-02, DOCS-03]
requirements_addressed: [CORE-04, OPS-02, DOCS-03]
must_haves:
  truths:
    - "D-05, D-06: src/mcp_test_framework/judge_protocol.py defines a runtime-checkable Judge Protocol with one async method judge(rubric, subject, context=None) -> JudgeResult (spec-verbatim signature, @runtime_checkable, lives in its own file so a future AgenticJudge per SEED-001 imports the Protocol without pulling in the rubric implementation)"
    - "D-07, D-08: src/mcp_test_framework/ollama_judge.py defines OllamaJudge implementing Judge over httpx.AsyncClient against Ollama /api/chat with the constant system prompt (strict-evaluator + /no_think + JSON-only + <<<SUBJECT>>> delimiters) and the locked request body; OllamaJudge ships zero warmup-specific public API (clean I/O wrapper: __init__, __aenter__, __aexit__, judge — no from_config, no warmup())"
    - "OPS-02: Every Ollama HTTP call uses httpx.Timeout(120, connect=10) so no operation can hang"
    - "D-09, D-10: Response parser strips <think>...</think>, brace-extracts on validation failure, falls back to JudgeResult(passed=False, score=1, reasoning=\"malformed judge response\", raw_response=<original>) with raw_response preserved, validates 1<=score<=5 + all 3 required fields; transport-level errors (HTTP non-2xx, httpx.TimeoutException, ConnectError) propagate up rather than collapsing to a JudgeResult"
    - "D-03: live_ollama marker is registered in pyproject.toml and addopts skips both live_homelab and live_ollama by default; missing live dependency at test-time fails fast inside the test body, not at collection"
  artifacts:
    - path: "src/mcp_test_framework/judge_protocol.py"
      provides: "Judge Protocol seam for backend swap (CORE-04, SEED-001 enabler)"
      contains: "@runtime_checkable\nclass Judge(Protocol):"
    - path: "src/mcp_test_framework/ollama_judge.py"
      provides: "OllamaJudge + JudgeResult + _build_request_body + _parse_judge_response"
      contains: "class OllamaJudge"
      min_lines: 130
    - path: "pyproject.toml"
      provides: "live_ollama marker registration + addopts skip"
      contains: "live_ollama:"
  key_links:
    - from: "src/mcp_test_framework/judge_protocol.py"
      to: "src/mcp_test_framework/ollama_judge.py"
      via: "from mcp_test_framework.ollama_judge import JudgeResult"
      pattern: "from mcp_test_framework\\.ollama_judge import JudgeResult"
    - from: "src/mcp_test_framework/ollama_judge.py"
      to: "Ollama /api/chat"
      via: "httpx.AsyncClient.post('/api/chat', json=body)"
      pattern: "post\\([\"']/api/chat[\"']"
    - from: "pyproject.toml"
      to: "tests/smoke/*"
      via: "marker filter in addopts"
      pattern: "not live_homelab and not live_ollama"
---

<objective>
Ship the Phase 3 implementation surface — the Judge Protocol seam, the OllamaJudge concrete class with its defensive parser and pure-function helpers, JudgeResult, and the pyproject.toml marker registration that lets Wave-2 tests run.

Purpose: Lock CORE-04 (Judge Protocol + OllamaJudge over `/api/chat` with the locked request body), OPS-02 (`httpx.Timeout(120, connect=10)` on every HTTP call), and DOCS-03 (`raw_response` preserved through every parser branch) per CONTEXT D-04..D-10 and ROADMAP SC#1..SC#5.

Output: Two new modules under `src/mcp_test_framework/`, a 2-line surgical edit to `pyproject.toml`. Phase 4 fixture FIX-01 will consume `OllamaJudge` (`async with OllamaJudge(...) as judge:`); the live smoke and unit tests in Plans 03-02 / 03-03 verify behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-ollama-judge/03-CONTEXT.md
@.planning/phases/03-ollama-judge/03-RESEARCH.md
@.planning/research/PITFALLS.md
@docs/mcp_test_framework_mvp_spec.md
@CLAUDE.md
@src/mcp_test_framework/mcp_client.py
@src/mcp_test_framework/models.py
@pyproject.toml

<interfaces>
<!-- Existing types Phase 3 consumes. Extracted from src/mcp_test_framework/models.py. -->

From src/mcp_test_framework/models.py:
```python
class OllamaConfig(BaseModel):
    model_config = ConfigDict(frozen=True, populate_by_name=True)
    base_url: str = Field(default="http://127.0.0.1:11434", validation_alias=AliasChoices("OLLAMA_BASE_URL", "base_url"))
    model: str = Field(default="qwen3.6:latest", validation_alias=AliasChoices("OLLAMA_MODEL", "model"))
    timeout_seconds: int = Field(default=120, ge=1, validation_alias=AliasChoices("OLLAMA_TIMEOUT_SECONDS", "timeout_seconds"))
```

From src/mcp_test_framework/mcp_client.py (the AsyncExitStack lifecycle pattern OllamaJudge mirrors):
```python
class McpTestClient:
    def __init__(self, command: str, args: list[str], timeout_seconds: int) -> None: ...
    async def __aenter__(self) -> "McpTestClient":
        ...
        stack = AsyncExitStack()
        try:
            read, write = await stack.enter_async_context(stdio_client(params))
            session = await stack.enter_async_context(ClientSession(read, write))
            async with asyncio.timeout(self._timeout_seconds):
                await session.initialize()
        except BaseException:
            await stack.aclose()
            raise
        self._stack = stack
        self._session = session
        return self
    async def __aexit__(self, exc_type, exc, tb) -> None:
        stack = self._stack
        self._stack = None
        self._session = None
        if stack is not None:
            await stack.aclose()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/mcp_test_framework/ollama_judge.py with OllamaJudge, JudgeResult, _build_request_body, _parse_judge_response</name>
  <files>src/mcp_test_framework/ollama_judge.py</files>
  <read_first>
    - src/mcp_test_framework/mcp_client.py (the AsyncExitStack `__aenter__/__aexit__` pattern lines 102-146 — OllamaJudge mirrors this exactly)
    - src/mcp_test_framework/models.py (the OllamaConfig sub-model whose fields drive `OllamaJudge(base_url, model, timeout_seconds)` from Phase 4)
    - .planning/phases/03-ollama-judge/03-CONTEXT.md (locked decisions D-04 through D-10 + Claude's Discretion items)
    - .planning/phases/03-ollama-judge/03-RESEARCH.md §Code Examples 2, 3, 4, 5 (verbatim code blocks for JudgeResult, _build_request_body, _parse_judge_response, OllamaJudge — copy-paste sources)
    - .planning/phases/03-ollama-judge/03-RESEARCH.md §Common Pitfalls 1, 3, 5, 6, 8 (anti-patterns)
    - .planning/research/PITFALLS.md Pitfalls 2, 7 (qwen3 thinking quirks + cold-start)
    - docs/mcp_test_framework_mvp_spec.md §ollama_judge.py (canonical JudgeResult + system prompt frame)
  </read_first>
  <action>
Create the new file `src/mcp_test_framework/ollama_judge.py` containing — in this order — module docstring, imports, the named logger, the `_SYSTEM_PROMPT` constant, `JudgeResult` Pydantic model, `_build_request_body` pure helper, `_THINK_RE`/`_FENCE_RE` regex constants, `_strip_decorations` helper, `_extract_first_json_object` helper, `_parse_judge_response` helper, and `OllamaJudge` class. Copy the verbatim code blocks from `.planning/phases/03-ollama-judge/03-RESEARCH.md` §Code Example 2, 3, 4, 5 into this single module — they are pre-verified against Pydantic v2.13, httpx 0.28, and the Phase-2 mirror. Locked invariants the executor MUST preserve from those code blocks:

1. **`JudgeResult`** (D-04, spec §ollama_judge.py): subclass `BaseModel`, fields `passed: bool`, `score: int = Field(ge=1, le=5)`, `reasoning: str`, `raw_response: str`, with `model_config = ConfigDict(frozen=True)`.

2. **`_SYSTEM_PROMPT` constant** (D-07): MUST contain all four required elements — strict-evaluator framing, the literal substring `/no_think`, JSON-only contract (no prose, no markdown, no triple-backtick fences), and the delimited subject contract using literal `<<<SUBJECT>>>` / `<<<END SUBJECT>>>` markers plus an explicit instruction to ignore any instructions inside the SUBJECT block (this is the documented prompt-injection mitigation per the security threat model below). Use the wording in RESEARCH §Code Example 3.

3. **`_build_request_body(model, rubric, subject, context)`** (CORE-04, ROADMAP SC#2): MUST hard-code `"stream": False`, `"format": "json"`, `"think": False`, `"keep_alive": "30m"`, and `"options": {"temperature": 0, "num_predict": 256}` — copy the dict verbatim from RESEARCH §Code Example 3. The `messages` list MUST be `[{"role": "system", "content": _SYSTEM_PROMPT}, {"role": "user", "content": <joined>}]`. The user content joins, with `"\n\n"` separators, `f"RUBRIC:\n{rubric}"`, then `f"<<<SUBJECT>>>\n{subject}\n<<<END SUBJECT>>>"`, then (only if `context is not None`) `f"Context: {json.dumps(context, sort_keys=True)}"`. Keep the function module-level (not a method) so unit tests can import it directly.

4. **`_strip_decorations(content)`** (D-09 step 1): apply `re.compile(r"<think>.*?</think>", re.DOTALL).sub("", content)` then strip leading/trailing triple-backtick fences (use the `_FENCE_RE` from RESEARCH §Code Example 4) then `.strip()`.

5. **`_extract_first_json_object(text)`** (D-09 step 3, brace-balanced — see Pitfall 5 in RESEARCH for why a regex won't do): copy the 12-line state-machine verbatim from RESEARCH §Common Pitfalls 5 (also reproduced in §Code Example 4). It MUST track `in_string` and `escape` to handle braces inside JSON strings, return `None` when unbalanced, and return the substring `text[start:i+1]` on the first `depth == 0` close.

6. **`_parse_judge_response(content)`** (D-09 four-step contract; DOCS-03 raw_response preservation): step 1 strip, step 2 `JudgeResult.model_validate_json(stripped)` and on success `return result.model_copy(update={"raw_response": content})`, step 3 brace-extract and re-validate (same `model_copy` raw-response patch on success), step 4 fallback `return JudgeResult(passed=False, score=1, reasoning="malformed judge response", raw_response=content)`. Catch `(ValidationError, ValueError)` in steps 2 and 3 — see the imprecision note: per RESEARCH §Common Pitfalls 3, `model_validate_json` only ever raises `pydantic.ValidationError` (the D-09 wording mentions `JSONDecodeError`, but Pydantic v2 wraps JSON parse errors into `ValidationError`); `ValueError` is the belt-and-suspenders catch and the JSONDecodeError clause is honored as dead-code documentation. Add a brief comment at that exception clause: `# model_validate_json wraps json parse errors as ValidationError; ValueError is belt-and-suspenders (D-09 step 3 honors the JSONDecodeError wording as dead code per Pydantic v2.13).`

7. **`OllamaJudge` class** (D-08 signature; D-10 transport-error propagation; OPS-02 timeout):
   - `__init__(self, base_url: str, model: str, timeout_seconds: int) -> None` — store `base_url.rstrip("/")`, `model`, `timeout_seconds`; init `self._stack = None`, `self._client = None`.
   - `__aenter__` — Phase-2 mirror: build `AsyncExitStack`, inside try: `client = await stack.enter_async_context(httpx.AsyncClient(base_url=self._base_url, timeout=httpx.Timeout(self._timeout_seconds, connect=10.0)))`; on `BaseException` `await stack.aclose(); raise`. Then set `self._stack` / `self._client` and `return self`. **The `httpx.Timeout(self._timeout_seconds, connect=10.0)` construction is the OPS-02 falsifier — do not use httpx defaults.**
   - `__aexit__` — copy the McpTestClient pattern verbatim: capture `stack = self._stack`, null out `self._stack` and `self._client`, then `if stack is not None: await stack.aclose()`.
   - `async def judge(self, rubric: str, subject: str, context: dict | None = None) -> JudgeResult:` — assert client entered, call `_build_request_body`, debug-log request shape (model, system_len, user_len, options dict), `response = await self._client.post("/api/chat", json=body)`, `response.raise_for_status()` (D-10: 4xx/5xx propagates as `httpx.HTTPStatusError`), `data = response.json()`, `content = data["message"]["content"]`, debug-log response shape (content_len, `data.get("load_duration")`, `data.get("eval_duration")`), `return _parse_judge_response(content)`. **Do NOT** wrap the post or json access in try/except that collapses to a JudgeResult — D-10 explicitly forbids this; transport errors propagate up so Phase 4 can `pytest.fail(...)` with the actual exception type.

8. **Logger:** `_log = logging.getLogger("mcp_test_framework.ollama_judge")` — used only for `_log.debug(...)` request/response shape lines per Pitfall "Logging full Ollama responses verbosely by default."

9. **No `homelab_mcp` import anywhere** — Phase-1 belt-and-suspenders catches violations mechanically; just don't import it.

10. **No warmup logic, no `from_config` classmethod, no `warmup()` method** — D-08 / Deferred. Public surface is exactly `__init__`, `__aenter__`, `__aexit__`, `judge`. Internal helpers (`_build_request_body`, `_parse_judge_response`, etc.) live at module level and are importable from unit tests.

Imports the executor will need: `from __future__ import annotations`, `import json`, `import logging`, `import re`, `from contextlib import AsyncExitStack`, `import httpx`, `from pydantic import BaseModel, ConfigDict, Field, ValidationError`. Do NOT import from `mcp_test_framework.judge_protocol` (avoids circular import — Protocol imports `JudgeResult` from here).
  </action>
  <verify>
    <automated>uv run python -c "from mcp_test_framework.ollama_judge import OllamaJudge, JudgeResult, _build_request_body, _parse_judge_response, _extract_first_json_object, _strip_decorations, _SYSTEM_PROMPT; import inspect, re; assert inspect.iscoroutinefunction(OllamaJudge.judge); assert inspect.iscoroutinefunction(OllamaJudge.__aenter__); assert inspect.iscoroutinefunction(OllamaJudge.__aexit__); body = _build_request_body('m','r','s',None); assert body['stream'] is False and body['format']=='json' and body['think'] is False and body['keep_alive']=='30m' and body['options']=={'temperature':0,'num_predict':256}; assert '/no_think' in _SYSTEM_PROMPT and '<<<SUBJECT>>>' in _SYSTEM_PROMPT and '<<<END SUBJECT>>>' in _SYSTEM_PROMPT; r = _parse_judge_response('not json at all'); assert r.passed is False and r.score==1 and r.raw_response=='not json at all'; print('ollama_judge.py OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/mcp_test_framework/ollama_judge.py` exists.
    - `grep -c "class OllamaJudge" src/mcp_test_framework/ollama_judge.py` returns `1`.
    - `grep -c "class JudgeResult" src/mcp_test_framework/ollama_judge.py` returns `1`.
    - `grep -c "async def __aenter__" src/mcp_test_framework/ollama_judge.py` returns `1`.
    - `grep -c "async def __aexit__" src/mcp_test_framework/ollama_judge.py` returns `1`.
    - `grep -c "async def judge" src/mcp_test_framework/ollama_judge.py` returns `1`.
    - `grep -c "def _build_request_body" src/mcp_test_framework/ollama_judge.py` returns `1`.
    - `grep -c "def _parse_judge_response" src/mcp_test_framework/ollama_judge.py` returns `1`.
    - `grep -c "def _extract_first_json_object" src/mcp_test_framework/ollama_judge.py` returns `1`.
    - `grep -F 'httpx.Timeout(self._timeout_seconds, connect=10' src/mcp_test_framework/ollama_judge.py` returns at least one match (OPS-02 falsifier).
    - `grep -F '"stream": False' src/mcp_test_framework/ollama_judge.py` returns at least one match.
    - `grep -F '"format": "json"' src/mcp_test_framework/ollama_judge.py` returns at least one match.
    - `grep -F '"think": False' src/mcp_test_framework/ollama_judge.py` returns at least one match.
    - `grep -F '"keep_alive": "30m"' src/mcp_test_framework/ollama_judge.py` returns at least one match.
    - `grep -F '"temperature": 0' src/mcp_test_framework/ollama_judge.py` returns at least one match.
    - `grep -F '"num_predict": 256' src/mcp_test_framework/ollama_judge.py` returns at least one match.
    - `grep -F '/no_think' src/mcp_test_framework/ollama_judge.py` returns at least one match (D-07 belt).
    - `grep -F '<<<SUBJECT>>>' src/mcp_test_framework/ollama_judge.py` returns at least one match.
    - `grep -F 'Field(ge=1, le=5)' src/mcp_test_framework/ollama_judge.py` returns at least one match (out-of-range falls through to fallback).
    - `grep -F 'malformed judge response' src/mcp_test_framework/ollama_judge.py` returns at least one match (D-09 step 4 fallback).
    - `grep -E "^import homelab_mcp|^from homelab_mcp" src/mcp_test_framework/ollama_judge.py` returns no matches (black-box).
    - `grep -F 'response.raise_for_status' src/mcp_test_framework/ollama_judge.py` returns at least one match (D-10 transport propagation).
    - `uv run ruff check src/mcp_test_framework/ollama_judge.py` exits 0.
    - The Bash one-liner under `<verify>` exits 0 and prints `ollama_judge.py OK`.
  </acceptance_criteria>
  <done>
    File compiles, imports resolve, public surface and locked request-body shape are present and grep-verifiable, defensive parser fallback returns `passed=False` with `raw_response` preserved on garbage input. Phase 4 can construct `async with OllamaJudge(base_url, model, timeout_seconds) as judge:` against this module without further changes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create src/mcp_test_framework/judge_protocol.py defining the runtime-checkable Judge Protocol</name>
  <files>src/mcp_test_framework/judge_protocol.py</files>
  <read_first>
    - src/mcp_test_framework/ollama_judge.py (just created in Task 1 — `JudgeResult` lives there and is the Protocol's return type)
    - .planning/phases/03-ollama-judge/03-CONTEXT.md D-05 and D-06 (signature + Protocol shape)
    - .planning/phases/03-ollama-judge/03-RESEARCH.md §Code Example 1 + §Common Pitfalls 4 (verbatim Protocol text + the `runtime_checkable` limitation)
    - docs/mcp_test_framework_mvp_spec.md §ollama_judge.py (Judge signature spec)
  </read_first>
  <action>
Create `src/mcp_test_framework/judge_protocol.py` containing exactly the verbatim contents of RESEARCH §Code Example 1, namely:

```python
from __future__ import annotations

from typing import Protocol, runtime_checkable

from mcp_test_framework.ollama_judge import JudgeResult


@runtime_checkable
class Judge(Protocol):
    """Pluggable LLM-judge seam (CORE-04, SEED-001 enabler)."""

    async def judge(
        self,
        rubric: str,
        subject: str,
        context: dict | None = None,
    ) -> JudgeResult: ...
```

Plus the module docstring from RESEARCH §Code Example 1 (covering: D-05/D-06 rationale, why `Protocol` not `abc.ABC`, why `JudgeResult` is imported from `ollama_judge.py` rather than defined here, the warning that `@runtime_checkable` only checks attribute presence by name (Pitfall 4), and the directive that smoke tests MUST add `inspect.signature` + `inspect.iscoroutinefunction` assertions to make SC#1 load-bearing).

Locked invariants:
- The decorator `@runtime_checkable` from `typing` is mandatory (D-06 — `isinstance(OllamaJudge(...), Judge)` must work at runtime for the Plan 03-03 smoke assertion).
- The signature `async def judge(self, rubric: str, subject: str, context: dict | None = None) -> JudgeResult` is spec-verbatim per D-05 — do NOT introduce a typed `JudgeContext` model (deferred).
- Import path is `from mcp_test_framework.ollama_judge import JudgeResult` — not a re-export from `models.py` (per CONTEXT D-04 / Established Patterns: domain-local result types stay in their owning module).
- The Protocol body MUST use `...` (Ellipsis literal) as the method body — Protocols document the contract, not an implementation.

No additional members beyond `judge`. No `warmup()` (deferred per D-08). No abstract methods. No `__init_subclass__` shenanigans.
  </action>
  <verify>
    <automated>uv run python -c "from mcp_test_framework.judge_protocol import Judge; from mcp_test_framework.ollama_judge import OllamaJudge, JudgeResult; import inspect, typing; assert isinstance(Judge, type), 'Judge should be a class'; assert getattr(Judge, '_is_protocol', False) or typing.get_origin(Judge) is None, 'Judge should be a Protocol'; assert getattr(Judge, '_is_runtime_protocol', False) or hasattr(Judge, '__runtime_protocol__') or any(getattr(Judge, attr, None) for attr in ('_is_runtime_protocol','__runtime_checkable__')) or '_is_runtime_protocol' in dir(Judge) or 'runtime_checkable' in str(Judge.__class__), 'Judge should be runtime_checkable'; j = OllamaJudge(base_url='http://x', model='m', timeout_seconds=1); assert isinstance(j, Judge), 'OllamaJudge should satisfy Judge Protocol'; sig = inspect.signature(Judge.judge); assert list(sig.parameters) == ['self','rubric','subject','context']; assert sig.parameters['context'].default is None; print('judge_protocol.py OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `src/mcp_test_framework/judge_protocol.py` exists.
    - `grep -c "@runtime_checkable" src/mcp_test_framework/judge_protocol.py` returns `1`.
    - `grep -c "class Judge(Protocol):" src/mcp_test_framework/judge_protocol.py` returns `1`.
    - `grep -F 'from mcp_test_framework.ollama_judge import JudgeResult' src/mcp_test_framework/judge_protocol.py` returns at least one match.
    - `grep -F 'async def judge' src/mcp_test_framework/judge_protocol.py` returns at least one match.
    - `grep -F 'context: dict | None = None' src/mcp_test_framework/judge_protocol.py` returns at least one match.
    - `grep -F '-> JudgeResult' src/mcp_test_framework/judge_protocol.py` returns at least one match.
    - `grep -E "^import homelab_mcp|^from homelab_mcp" src/mcp_test_framework/judge_protocol.py` returns no matches.
    - `uv run ruff check src/mcp_test_framework/judge_protocol.py` exits 0.
    - `uv run python -c "from mcp_test_framework.judge_protocol import Judge; from mcp_test_framework.ollama_judge import OllamaJudge; assert isinstance(OllamaJudge('http://x','m',1), Judge)"` exits 0.
    - The Bash one-liner under `<verify>` exits 0 and prints `judge_protocol.py OK`.
  </acceptance_criteria>
  <done>
    Protocol file compiles, the runtime-checkable seam exists, `isinstance(OllamaJudge(...), Judge)` returns True, signature matches D-05 spec verbatim. Plan 03-03's smoke test SC#1 falsifier can import `Judge` and assert against it without any additional plumbing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Edit pyproject.toml to register live_ollama marker and extend addopts</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current `[tool.pytest.ini_options]` block — the surgical edit target is lines 37-40)
    - .planning/phases/03-ollama-judge/03-CONTEXT.md D-03 (marker convention + addopts wording)
    - .planning/phases/03-ollama-judge/03-RESEARCH.md §Code Example 7 (the verbatim 2-line diff)
    - tests/smoke/test_smoke_homelab_mcp.py (the live_homelab analog — confirms current marker pattern that live_ollama mirrors)
  </read_first>
  <action>
Surgical 2-line edit to the existing `[tool.pytest.ini_options]` block in `pyproject.toml` (currently at lines 29-40). Do NOT touch `asyncio_mode`, `asyncio_default_fixture_loop_scope`, or `testpaths`.

**Change 1 — extend the `markers` list:** Add a second marker entry `"live_ollama: requires reachable Ollama at OLLAMA_BASE_URL with the configured model"` immediately after the existing `live_homelab` line, preserving the trailing comma on the `live_homelab` line and the closing `]`. Final state of the `markers` array:

```toml
markers = [
  "live_homelab: requires homelab-mcp runnable via uvx (or on PATH)",
  "live_ollama: requires reachable Ollama at OLLAMA_BASE_URL with the configured model",
]
```

**Change 2 — extend `addopts`:** Replace `addopts = "-m 'not live_homelab'"` with `addopts = "-m 'not live_homelab and not live_ollama'"`. Default `uv run pytest` MUST skip both live markers; `uv run pytest -m live_ollama` MUST run only Ollama smoke; `uv run pytest -m ''` MUST run everything (no marker filter).

Do not introduce any other config changes — no new dependency, no new tool, no ruff change.
  </action>
  <verify>
    <automated>uv run pytest --collect-only -q 2>&1 | head -50 && uv run pytest -m '' --collect-only -q 2>&1 | grep -c "test_smoke_homelab_mcp"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'live_ollama: requires reachable Ollama at OLLAMA_BASE_URL with the configured model' pyproject.toml` returns at least one match.
    - `grep -F "addopts = \"-m 'not live_homelab and not live_ollama'\"" pyproject.toml` returns at least one match.
    - `grep -c "live_homelab:" pyproject.toml` returns `1` (existing marker preserved).
    - `grep -c "asyncio_mode = \"strict\"" pyproject.toml` returns `1` (untouched).
    - `grep -c "asyncio_default_fixture_loop_scope = \"session\"" pyproject.toml` returns `1` (untouched).
    - `uv run pytest --collect-only -q 2>&1` exits 0 and the output does NOT contain `Unknown pytest.mark.live_ollama` warnings (marker registered).
    - `uv run pytest -m live_ollama --collect-only -q 2>&1` exits 0 (parser accepts the marker filter, even if no tests match yet — the Plan 03-03 smoke will populate it).
    - Default `uv run pytest --collect-only -q 2>&1` does NOT collect `tests/smoke/test_smoke_homelab_mcp.py` (existing addopts skip continues to work — confirms the addopts edit didn't break the existing skip).
  </acceptance_criteria>
  <done>
    `pyproject.toml` registers both `live_homelab` and `live_ollama` markers; `addopts` skips both by default; default `pytest` collection excludes live smoke tests; `pytest -m live_ollama` filter is accepted. Plans 03-02 and 03-03 (Wave 2) can write tests against the new marker.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase-3 framework process → Ollama at 127.0.0.1:11434 | HTTP traffic crosses the homelab LAN; Ollama is treated as trusted (locked-network dependency). No TLS — homelab assumption. |
| Tool description / canned synthetic subject → judge user-message body | Untrusted text content is interpolated between `<<<SUBJECT>>>` / `<<<END SUBJECT>>>` fences in the user-message of the chat call (D-07). |
| Judge model output → test failure diagnostic | `raw_response` is captured verbatim and surfaced in pytest failure output (DOCS-03). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (prompt injection) | `_build_request_body` user-message body | mitigate | The system prompt (D-07) explicitly instructs the model to treat the `<<<SUBJECT>>>...<<<END SUBJECT>>>` block as untrusted text and ignore any instructions inside. Output shape constrained by `format: "json"` + `JudgeResult.model_validate_json` (out-of-range `score` or missing required fields fall through to D-09 step-4 fallback `passed=False`). No code change beyond D-07's delimiter contract. Acceptance criteria (Task 1) grep-verify the literal `<<<SUBJECT>>>` / `<<<END SUBJECT>>>` markers and `/no_think` directive presence. |
| T-03-02 | Information Disclosure | `JudgeResult.raw_response` surfaced via test logs | accept | DOCS-03 explicitly requires raw_response surfacing for debuggability; in MVP homelab context the only data flowing through is canned synthetic subjects (Phase 3) and tool descriptions from `homelab-mcp` (Phase 4). Documented; no PII expected. |
| T-03-03 | Denial of Service (transport-level hang) | `httpx.AsyncClient.post('/api/chat')` | mitigate | `httpx.Timeout(self._timeout_seconds, connect=10.0)` — locked 120s read/write/pool ceiling, 10s connect ceiling. Cannot hang indefinitely (OPS-02). Manual `ollama stop` mid-request → clean `httpx.ReadTimeout` propagates to caller (D-10), Phase 4 surfaces. Task 1 acceptance criteria grep-verify the literal `httpx.Timeout(...connect=10` construction. |
| T-03-04 | Spoofing | Ollama at 127.0.0.1 | accept | Locked-network homelab dependency. No TLS, no auth — out of MVP scope. Documented in CONTEXT canonical_refs ("Ollama at 127.0.0.1:11434"). |
| T-03-05 | Tampering (malformed judge response → uncaught exception) | `_parse_judge_response` | mitigate | D-09 four-step parser catches `(ValidationError, ValueError)` at every step; step 4 fallback returns `JudgeResult(passed=False, score=1, ...)` rather than propagating. Task 1 acceptance criteria verify the `malformed judge response` literal and the `Field(ge=1, le=5)` constraint. |
| T-03-06 | Repudiation | Phase 3 module surface | accept | No mutating I/O (read-only HTTP to Ollama); no audit trail required for MVP. |
| T-03-07 | Elevation of Privilege | Phase 3 module surface | accept | No subprocess spawn (Ollama is HTTP), no privileged file access, no exec'd input. N/A. |

All HIGH-severity threats have `mitigate` disposition and corresponding grep-verifiable acceptance criteria in Task 1 and Task 3. No HIGH-severity threat is `accept`. Per security_enforcement default config, this plan is unblocked.
</threat_model>

<verification>
After all three tasks complete:
- `uv run python -c "from mcp_test_framework.judge_protocol import Judge; from mcp_test_framework.ollama_judge import OllamaJudge, JudgeResult, _build_request_body, _parse_judge_response; assert isinstance(OllamaJudge('http://x','m',1), Judge)"` exits 0 — confirms the cross-module Protocol/concrete linkage.
- `uv run ruff check src/mcp_test_framework/judge_protocol.py src/mcp_test_framework/ollama_judge.py` exits 0 — black-box and lint clean (the TID251 banned-api guard for `homelab_mcp` is the load-bearing rule).
- `uv run pytest --collect-only -q 2>&1` exits 0 with no `Unknown pytest.mark.live_ollama` warnings.
</verification>

<success_criteria>
1. `src/mcp_test_framework/judge_protocol.py` exists, defines `@runtime_checkable class Judge(Protocol)` with the spec-verbatim async `judge(rubric, subject, context=None) -> JudgeResult` signature, imports `JudgeResult` from `ollama_judge.py`. (CORE-04 SC#1)
2. `src/mcp_test_framework/ollama_judge.py` exists, defines `OllamaJudge(base_url, model, timeout_seconds)` with `__aenter__`/`__aexit__` lifecycle owning a single `httpx.AsyncClient` constructed with `httpx.Timeout(self._timeout_seconds, connect=10.0)`. (CORE-04, OPS-02)
3. `_build_request_body` hard-codes the locked request body parameters: `stream:false`, `format:"json"`, `think:false`, `keep_alive:"30m"`, `options.{temperature:0, num_predict:256}`. (CORE-04, ROADMAP SC#2)
4. `_parse_judge_response` implements D-09 four-step contract; out-of-range / missing-field / unparseable input returns `JudgeResult(passed=False, score=1, reasoning="malformed judge response", raw_response=<full original content>)`. (CORE-04, DOCS-03)
5. `pyproject.toml` registers `live_ollama` marker; `addopts` skips both `live_homelab` and `live_ollama` by default; default `uv run pytest` collection produces no unknown-marker warning. (D-03)
6. No `import homelab_mcp` anywhere in the new modules (Phase-1 ruff TID251 + conftest sys.modules guard catches mechanically; this plan adds nothing that violates it).
</success_criteria>

<output>
After completion, create `.planning/phases/03-ollama-judge/03-01-SUMMARY.md` describing what shipped, the file paths created, and any deviations from this plan (none expected — RESEARCH §Code Examples 1-5 are pre-verified).
</output>
