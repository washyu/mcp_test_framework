---
phase: 25-public-api-rename-seed-023-sdet-test-code
plan: 05
type: execute
wave: 5
depends_on: [01, 02, 03, 04]
files_modified:
  - docs/TEST-CODE-AUTHORING.md
  - docs/SDET-AUTHORING.md
  - docs/EXTENDING.md
  - docs/ERROR-STYLE.md
  - docs/MIGRATION-v1-to-v2.md
  - README.md
  - CLAUDE.md
  - examples/homelab-mcp.yaml
  - examples/README.md
  - config.example.yaml
autonomous: true
requirements:
  - RENAME-06
must_haves:
  truths:
    - "`docs/SDET-AUTHORING.md` no longer contains the authoring walkthrough; it is a one-line redirect stub pointing at `docs/TEST-CODE-AUTHORING.md` (D-10)."
    - "`docs/TEST-CODE-AUTHORING.md` contains the full authoring content (moved via `git mv`) with terminology updated to test-code."
    - "Operator-facing docs (README, CLAUDE.md, EXTENDING.md, ERROR-STYLE.md) use `test-code` terminology consistently."
    - "Direct references to `docs/SDET-AUTHORING.md` in `CLAUDE.md` and `README.md` body are updated to `docs/TEST-CODE-AUTHORING.md` (per D-11; Phase 30 owns the lead-with-library-mode rewrite)."
    - "`config.example.yaml` uses `test_code:` as the documented top-level key; `examples/homelab-mcp.yaml` demonstrates the legacy `sdet:` alias as a compat-alias example."
    - "`docs/MIGRATION-v1-to-v2.md` legitimately retains `sdet` references as the historical record of the rename per D-18; any such lines carry `<!-- noqa: sdet-rename-shim -->`."
  artifacts:
    - path: "docs/TEST-CODE-AUTHORING.md"
      provides: "Authoring walkthrough moved via `git mv` from SDET-AUTHORING.md with terminology swept to test-code"
    - path: "docs/SDET-AUTHORING.md"
      provides: "One-line redirect stub per D-10"
      contains: "Renamed to [TEST-CODE-AUTHORING.md]"
    - path: "README.md"
      provides: "Terminology + path references updated to test-code; PASS-sample re-capture is deferred to Phase 30 CLOSE-04"
    - path: "config.example.yaml"
      provides: "`test_code:` as the documented key"
      contains: "test_code:"
  key_links:
    - from: "docs/SDET-AUTHORING.md (stub)"
      to: "docs/TEST-CODE-AUTHORING.md"
      via: "markdown link in stub body"
      pattern: "TEST-CODE-AUTHORING.md"
    - from: "README.md, CLAUDE.md"
      to: "docs/TEST-CODE-AUTHORING.md"
      via: "direct link updates per D-11"
      pattern: "TEST-CODE-AUTHORING.md"
---

<objective>
RENAME-06 (sweep portion): `git mv docs/SDET-AUTHORING.md docs/TEST-CODE-AUTHORING.md` (D-09); drop a one-line stub at the old path (D-10); sweep operator-facing terminology and planning-ID references across README, CLAUDE.md, the remaining `docs/*.md`, `examples/`, and `config.example.yaml`. Apply exclusions per D-16/D-18 (`tests/`, `.planning/`, internal `_`-prefixed names, `MIGRATION-v1-to-v2.md` historical sections, the stub file).

Purpose: This is the operator's visible documentation surface. Renaming the import path / CLI / config without updating the docs would leave operators reading "use sdet" instructions for a surface that's been renamed.

Output: `docs/TEST-CODE-AUTHORING.md` is the canonical authoring doc; README + CLAUDE.md + auxiliary docs + example configs use `test-code` terminology consistently. The sweep gate (plan 06) verifies zero residual matches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-public-api-rename-seed-023-sdet-test-code/25-CONTEXT.md
@docs/SDET-AUTHORING.md
@README.md
@CLAUDE.md
@config.example.yaml

<interfaces>
<!-- Sweep regex patterns per D-17 (locked): -->

Planning IDs:
```
\b(SDET|RENAME|PERSONA|CLEAN|CLI|CODEGEN|PACK|LIB|REPORTER|CFG|CLOSE|STATE|UI|UX|UAT|SAFE|SURFACE|RUNNER|SEED)-\d+(\.\d+)?\b
```

Terminology:
```
\bsdet\b   (case-insensitive)
\bSDET\b   (uppercase literal)
```

Per D-16 (locked): Sweep scope = `README.md`, `CLAUDE.md`, all `docs/*.md`, all docstrings inside `src/mcp_test_framework/`, Typer CLI `--help` output strings, operator-visible error messages (`errors.py`, anything raised from CLI/discovery paths). EXCLUDES `.planning/`, `tests/`, internal `_`-prefixed names, `__dunder__` names, git history.

NOTE on src/ docstring sweep: this plan touches operator-facing surfaces in `README.md`, `CLAUDE.md`, `docs/`, `examples/`, `config.example.yaml`. Sweeping inside `src/mcp_test_framework/` docstrings is split as follows:
- Docstrings inside `test_code/` package — already handled by plan 01 (rename pass updated `docs/SDET-AUTHORING.md` reference to `docs/TEST-CODE-AUTHORING.md`).
- Docstrings inside `cli.py` — plan 02 handled command/flag references; plan 03 handled config-attr references; this plan handles any LEFTOVER `sdet` / `SDET` terminology in cli.py docstrings (e.g. help text for non-renamed options that mention "SDET" as a persona name).
- Docstrings inside `_runner.py` — plan 04 handled path references; this plan handles leftover "SDET" persona references inside docstrings.
- All other `src/` modules — handled here as a final pass.

The plan 06 leak-check gate will execute the sweep regex and exit non-zero on any residual match. This plan must leave the codebase in a state where plan 06's gate passes. Use D-18 exclusion markers liberally on legitimate compat-shim lines that this plan touches.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: git mv docs/SDET-AUTHORING.md → docs/TEST-CODE-AUTHORING.md, sweep its content, drop stub at old path</name>
  <read_first>
    - docs/SDET-AUTHORING.md
  </read_first>
  <action>
    1. Run `git mv docs/SDET-AUTHORING.md docs/TEST-CODE-AUTHORING.md` (D-09).

    2. Inside `docs/TEST-CODE-AUTHORING.md`, sweep every operator-facing occurrence:
       - `from mcp_test_framework.sdet` → `from mcp_test_framework.test_code` (import examples)
       - `mcp_test_framework.sdet` → `mcp_test_framework.test_code` (dotted paths in prose)
       - `tests/sdet/` → `tests/test_code/` (filesystem paths in prose)
       - `gen-sdet-classes` → `gen-test-classes` (CLI command name)
       - `--sdet` → `--test-code` (CLI flag)
       - `cfg.sdet` → `cfg.test_code` (config attribute)
       - `sdet:` (YAML key) → `test_code:` (YAML key) in example config blocks
       - `SdetConfig` → `TestCodeConfig`
       - The persona term "SDET" (uppercase) in user-facing prose → "test-code author" (the human role) where the prose names a person; "test-code" (the noun) where the prose names the surface.
       - The lowercase token `sdet` in prose (not inside code blocks, not inside YAML examples that demonstrate the legacy alias) → `test-code` or `test_code` depending on whether the original was a hyphenated noun or an underscore-named identifier.
       - "SDET persona", "SDET surface", "SDET-authored" — pick test-code-equivalent prose; example: "SDET persona" → "test-code author persona", "SDET-authored tests" → "test-code-authored tests".

    3. Inside `docs/TEST-CODE-AUTHORING.md`, sweep every planning-ID match per the D-17 regex and either:
       - DELETE the line if the planning ID was incidental traceability prose ("Phase 17 SEED-022 lock-in disallowed `exclude_none=True`" → "v1.3's serializer lock-in disallowed `exclude_none=True`" — strip the ID, keep the technical content).
       - REWRITE the line so the operator content is preserved without the ID (Phase 22 scrub precedent: this is the same operation scaled up).
       - If a planning ID is genuinely load-bearing for operator understanding (rare), keep it and add `<!-- noqa: sdet-rename-shim -->` to allow it through plan 06's gate per D-18. Default to deletion/rewriting unless the gloss is operator-critical.

    4. Create a new file `docs/SDET-AUTHORING.md` (stub) with EXACT content (D-10):
    ```markdown
    > Renamed to [TEST-CODE-AUTHORING.md](TEST-CODE-AUTHORING.md). The old filename is removed in v1.5.

    <!-- noqa: sdet-rename-shim -->
    ```
    The HTML comment marker on the second line tags this entire file as a legitimate compat-shim per D-18 — plan 06's gate sees the marker and skips this file's content. Per D-18 the stub file is an explicit allowed exclusion.
  </action>
  <verify>
    <automated>test -f docs/TEST-CODE-AUTHORING.md &amp;&amp; test -f docs/SDET-AUTHORING.md &amp;&amp; grep -q "Renamed to \[TEST-CODE-AUTHORING.md\]" docs/SDET-AUTHORING.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/TEST-CODE-AUTHORING.md` (moved)
    - `wc -l docs/SDET-AUTHORING.md | awk '{print $1}'` returns a count <= 5 (one-line stub plus marker comment plus optional blank lines)
    - `grep -q "Renamed to \[TEST-CODE-AUTHORING.md\](TEST-CODE-AUTHORING.md)" docs/SDET-AUTHORING.md`
    - `grep -q "noqa: sdet-rename-shim" docs/SDET-AUTHORING.md`
    - `grep -c "from mcp_test_framework.sdet" docs/TEST-CODE-AUTHORING.md` returns `0`
    - `grep -c "gen-sdet-classes" docs/TEST-CODE-AUTHORING.md` returns `0`
    - `grep -c -- "--sdet" docs/TEST-CODE-AUTHORING.md` returns `0`
    - `grep -c "tests/sdet/" docs/TEST-CODE-AUTHORING.md` returns `0`
    - `grep -Ec '\b(SDET|RENAME|PERSONA|CLEAN|CLI|CODEGEN|PACK|LIB|REPORTER|CFG|CLOSE|STATE|UI|UX|UAT|SAFE|SURFACE|RUNNER|SEED)-[0-9]+(\.[0-9]+)?\b' docs/TEST-CODE-AUTHORING.md` returns `0`
    - `git log --follow docs/TEST-CODE-AUTHORING.md --oneline | head -3 | wc -l` returns a non-zero count (blame preserved)
  </acceptance_criteria>
  <done>
    `docs/TEST-CODE-AUTHORING.md` is the canonical authoring doc with all terminology updated and zero planning-ID matches. `docs/SDET-AUTHORING.md` is a one-line redirect stub tagged with the shim marker.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Sweep README.md, CLAUDE.md, and auxiliary docs/*.md for test-code terminology and planning-ID matches</name>
  <read_first>
    - README.md
    - CLAUDE.md
    - docs/EXTENDING.md
    - docs/ERROR-STYLE.md
    - docs/MIGRATION-v1-to-v2.md
  </read_first>
  <action>
    For each of README.md, CLAUDE.md, docs/EXTENDING.md, docs/ERROR-STYLE.md, sweep:

    A. **Terminology + path updates:**
    - `from mcp_test_framework.sdet` → `from mcp_test_framework.test_code`
    - `mcp_test_framework.sdet` → `mcp_test_framework.test_code`
    - `tests/sdet/` → `tests/test_code/`
    - `gen-sdet-classes` → `gen-test-classes`
    - `--sdet` → `--test-code`
    - `cfg.sdet` → `cfg.test_code`
    - `sdet:` (YAML key) → `test_code:` (YAML key)
    - `SdetConfig` → `TestCodeConfig`
    - `docs/SDET-AUTHORING.md` → `docs/TEST-CODE-AUTHORING.md` (per D-11: this is direct-reference updates in this plan; Phase 30 owns the lead-with-library-mode rewrite)
    - The persona term "SDET" → "test-code author" (when naming a person) or "test-code" (when naming the surface)
    - The lowercase token `sdet` in operator-facing prose → `test-code` (hyphenated noun) or `test_code` (underscore identifier)

    B. **Planning-ID sweep** (per D-17 regex):
    - Scan each file for `\b(SDET|RENAME|PERSONA|CLEAN|CLI|CODEGEN|PACK|LIB|REPORTER|CFG|CLOSE|STATE|UI|UX|UAT|SAFE|SURFACE|RUNNER|SEED)-\d+(\.\d+)?\b`.
    - Per the Phase 22 scrub precedent: rewrite or delete each match to remove the planning-ID while preserving operator-meaningful content. Example: "Phase 19 D-02 substitution" → "the lifecycle-action substitution" (strip the ID, keep the technical reference).
    - If a planning ID is genuinely load-bearing for an operator (extremely rare in operator-facing docs), retain with `<!-- noqa: sdet-rename-shim -->` per D-18.

    C. **Specific file handling:**

    `README.md`:
    - The README §SDET-scenarios PASS-sample re-capture is explicitly deferred to Phase 30 CLOSE-04 (per CONTEXT.md carry-forward-items section). This plan updates the PATH references and surrounding prose but does NOT re-capture the sample output. Leave any FAIL-sample output blocks untouched (Phase 21 D-14 manual-snapshot precedent).
    - Update §"SDET scenarios" heading text → "test-code scenarios".
    - Update intro paragraph that mentions the SDET persona → test-code author persona.
    - Update the embedded `tests/sdet/` path references → `tests/test_code/`.
    - Update the post-snapshot framing paragraph terminology (test-code, not SDET).
    - Do NOT touch the captured CLI output block — it is a verbatim snapshot of a real run and will be re-captured in Phase 30.

    `CLAUDE.md`:
    - The "## What This Project Is" v1.3 SDET-persona paragraph (per CONTEXT.md) needs to be rewritten to "test-code author" persona language.
    - All references to `gen-sdet-classes`, `--sdet`, `tests/sdet/`, `cfg.sdet`, `from mcp_test_framework.sdet`, `docs/SDET-AUTHORING.md` swept per the rules above.
    - Planning-ID sweep applied — strip every match.

    `docs/EXTENDING.md`:
    - Incidental references swept; planning-ID sweep applied.

    `docs/ERROR-STYLE.md`:
    - Incidental references swept; planning-ID sweep applied.

    `docs/MIGRATION-v1-to-v2.md` (SPECIAL HANDLING per D-18):
    - This doc is a historical record of v1→v2 migration that legitimately references `sdet` to explain the rename. Per D-18 it is an allowed exclusion.
    - DO NOT rewrite `sdet` references that explain the v1→v2 history.
    - DO add `<!-- noqa: sdet-rename-shim -->` markers on every line that contains a `\bsdet\b` or `\bSDET\b` match — one comment per line. (Plan 06's gate uses this token to skip the line.)
    - DO update any planning-ID references using the same regex strip — historical reasons don't justify keeping planning IDs operator-facing; planning IDs strip per the Phase 22 precedent unless genuinely load-bearing (rare).
  </action>
  <verify>
    <automated>uv run python -c "import re; pat = re.compile(r'\b(SDET|RENAME|PERSONA|CLEAN|CLI|CODEGEN|PACK|LIB|REPORTER|CFG|CLOSE|STATE|UI|UX|UAT|SAFE|SURFACE|RUNNER|SEED)-[0-9]+(\.[0-9]+)?\b'); files = ['README.md', 'CLAUDE.md', 'docs/EXTENDING.md', 'docs/ERROR-STYLE.md']; bad = [(f, ln, line.rstrip()) for f in files for ln, line in enumerate(open(f, encoding='utf-8'), 1) if pat.search(line) and 'noqa: sdet-rename-shim' not in line]; assert not bad, bad; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "from mcp_test_framework.sdet" README.md CLAUDE.md docs/EXTENDING.md docs/ERROR-STYLE.md` returns `0`
    - `grep -c "gen-sdet-classes" README.md CLAUDE.md docs/EXTENDING.md docs/ERROR-STYLE.md` returns `0`
    - `grep -c -- "--sdet\b" README.md CLAUDE.md docs/EXTENDING.md docs/ERROR-STYLE.md` returns `0`
    - `grep -c "cfg\.sdet" README.md CLAUDE.md docs/EXTENDING.md docs/ERROR-STYLE.md` returns `0`
    - `grep -c "docs/SDET-AUTHORING.md" README.md CLAUDE.md` returns `0` (direct refs swapped; stub still links to TEST-CODE)
    - `grep -c "docs/TEST-CODE-AUTHORING.md" CLAUDE.md` returns a count >= 1 (the moved doc is referenced)
    - Every line in `docs/MIGRATION-v1-to-v2.md` matching `\bsdet\b` or `\bSDET\b` (excluding code blocks demonstrating the legacy alias) carries `<!-- noqa: sdet-rename-shim -->` — verify with `grep -nE '\bsdet\b|\bSDET\b' docs/MIGRATION-v1-to-v2.md | grep -v 'noqa: sdet-rename-shim' | wc -l` returns `0`
    - Planning-ID sweep: `uv run python -c "import re; pat = re.compile(r'\b(SDET|RENAME|PERSONA|CLEAN|CLI|CODEGEN|PACK|LIB|REPORTER|CFG|CLOSE|STATE|UI|UX|UAT|SAFE|SURFACE|RUNNER|SEED)-[0-9]+(\.[0-9]+)?\b'); files = ['README.md', 'CLAUDE.md', 'docs/EXTENDING.md', 'docs/ERROR-STYLE.md', 'docs/MIGRATION-v1-to-v2.md', 'docs/TEST-CODE-AUTHORING.md']; bad = [(f, ln) for f in files for ln, line in enumerate(open(f, encoding='utf-8'), 1) if pat.search(line) and 'noqa: sdet-rename-shim' not in line]; assert not bad, bad; print('ok')"` prints `ok`
  </acceptance_criteria>
  <done>
    README + CLAUDE.md + auxiliary docs swept; terminology consistent; planning-ID matches eliminated or tagged; MIGRATION-v1-to-v2.md retains historical references but each tagged with the shim marker.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Sweep example config files and any remaining src/ docstrings/help text leftovers</name>
  <read_first>
    - examples/homelab-mcp.yaml
    - examples/README.md
    - config.example.yaml
    - src/mcp_test_framework/cli.py
    - src/mcp_test_framework/_runner.py
  </read_first>
  <action>
    1. `config.example.yaml`:
    - Rename the top-level `sdet:` key to `test_code:` so the canonical example demonstrates the new key.
    - Update any inline comment text mentioning "SDET" or "sdet" to "test-code" / "test_code".
    - Update any planning-ID references using the D-17 regex (strip per Phase 22 precedent).

    2. `examples/homelab-mcp.yaml`:
    - Per CONTEXT.md "Operator-facing surfaces to scrub": `examples/homelab-mcp.yaml` should demonstrate the legacy `sdet:` alias as a compat-alias example. Keep the top-level key as `sdet:` here EXACTLY ONCE (so operators have a documented example of the legacy alias working). Add a `# noqa: sdet-rename-shim` comment on the `sdet:` line per D-18.
    - Update inline comment terminology elsewhere ("SDET" persona → "test-code author"; lowercase `sdet` in prose → `test-code`).
    - Update any planning-ID references (strip).

    Alternative if the planner reads `examples/homelab-mcp.yaml` and finds NO `sdet:` key already present: do not invent one. The compat-alias demonstration can live solely as a YAML code block inside `docs/MIGRATION-v1-to-v2.md` (which already retains `sdet` references per D-18). Decide based on what the file actually contains at execution time.

    3. `examples/README.md`:
    - Terminology sweep (`SDET` → `test-code author`; `sdet` → `test-code`; path references).
    - Planning-ID sweep (strip).
    - If it references `examples/homelab-mcp.yaml` as demonstrating the legacy alias, retain that prose with a `<!-- noqa: sdet-rename-shim -->` marker on the relevant line.

    4. Final src/ docstring + CLI help text sweep:
    - `src/mcp_test_framework/cli.py` — sweep any leftover `SDET` / `sdet` terminology in docstrings or Typer `help=` strings that plans 02/03 did not catch. Specifically:
      - Any `help="..."` string mentioning the SDET persona → `help="..."` mentioning test-code author.
      - Any docstring/comment that uses "SDET" as persona naming → "test-code author".
      - Any planning-ID match → strip per D-17 regex.
      - LEAVE intact: lines tagged with `# noqa: sdet-rename-shim` (the shim definitions from plans 02/03).
    - `src/mcp_test_framework/_runner.py` — same sweep: any leftover "SDET" persona references in docstrings → "test-code"; planning-ID matches stripped. LEAVE intact: the dual-discovery shim lines from plan 04 tagged with the noqa marker.
    - `src/mcp_test_framework/errors.py` (and any other module raising operator-visible error messages) — sweep for "SDET" / "sdet" in error message strings; planning-ID strip.
    - Other `src/mcp_test_framework/*.py` modules — scan for `\bsdet\b` / `\bSDET\b` / planning-ID matches in docstrings, error message strings, and `help=` strings. Strip / rewrite each per the rules above; tag with shim noqa only if the line is a legitimate compat-shim from plans 01-04.

    Identification of "operator-visible" inside src/:
    - User docstrings (module-level + class/function-level that have ANY operator audience reference).
    - Any string passed to `typer.Option(..., help=...)`, `typer.Argument(..., help=...)`, or `typer.echo(...)`.
    - Any string passed to `OperatorError(summary=..., detail=..., remediation=...)`.
    - Any string in a `raise ValueError(...)` / `raise FileNotFoundError(...)` that the operator might see in a stack trace.
    - Internal `_`-prefixed function/variable names are NOT in scope per D-16; LEAVE them.
  </action>
  <verify>
    <automated>uv run python -c "import re; pat = re.compile(r'\b(SDET|RENAME|PERSONA|CLEAN|CLI|CODEGEN|PACK|LIB|REPORTER|CFG|CLOSE|STATE|UI|UX|UAT|SAFE|SURFACE|RUNNER|SEED)-[0-9]+(\.[0-9]+)?\b'); import pathlib; files = ['config.example.yaml', 'examples/homelab-mcp.yaml', 'examples/README.md'] + [str(p) for p in pathlib.Path('src/mcp_test_framework').rglob('*.py')]; bad = [(f, ln, line.rstrip()) for f in files for ln, line in enumerate(open(f, encoding='utf-8'), 1) if pat.search(line) and 'noqa: sdet-rename-shim' not in line]; assert not bad, bad[:10]; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^test_code:" config.example.yaml` returns a count >= 1 (top-level key renamed) OR `grep -c "^  test_code:" config.example.yaml` returns >= 1 if the key is nested
    - `grep -c "^sdet:" config.example.yaml` returns `0` (legacy top-level key swapped)
    - In `examples/homelab-mcp.yaml`: any retained `sdet:` literal carries `# noqa: sdet-rename-shim` — verify with `grep -nE '^[[:space:]]*sdet:' examples/homelab-mcp.yaml | grep -v 'noqa: sdet-rename-shim' | wc -l` returns `0`
    - Planning-ID sweep across config.example.yaml + examples/ + all src/ Python files returns no untagged matches: `uv run python -c "import re; pat = re.compile(r'\b(SDET|RENAME|PERSONA|CLEAN|CLI|CODEGEN|PACK|LIB|REPORTER|CFG|CLOSE|STATE|UI|UX|UAT|SAFE|SURFACE|RUNNER|SEED)-[0-9]+(\.[0-9]+)?\b'); import pathlib; files = ['config.example.yaml', 'examples/homelab-mcp.yaml', 'examples/README.md'] + [str(p) for p in pathlib.Path('src/mcp_test_framework').rglob('*.py')]; bad = [(f, ln) for f in files for ln, line in enumerate(open(f, encoding='utf-8'), 1) if pat.search(line) and 'noqa: sdet-rename-shim' not in line]; assert not bad, bad[:10]; print('ok')"` prints `ok`
    - Terminology sweep across same file set: every `\bsdet\b` / `\bSDET\b` match either occurs inside a shim-tagged line or is absent. Verify with `uv run python -c "import re; pat = re.compile(r'\bsdet\b|\bSDET\b'); import pathlib; files = ['config.example.yaml', 'examples/homelab-mcp.yaml', 'examples/README.md'] + [str(p) for p in pathlib.Path('src/mcp_test_framework').rglob('*.py')]; bad = [(f, ln, line.rstrip()) for f in files for ln, line in enumerate(open(f, encoding='utf-8'), 1) if pat.search(line) and 'noqa: sdet-rename-shim' not in line]; print(len(bad), 'residual matches'); assert not bad, bad[:10]; print('ok')"` prints `0 residual matches\nok`
  </acceptance_criteria>
  <done>
    config.example.yaml uses `test_code:` as the canonical key; examples + src/ Python modules' operator-facing strings are swept; every legitimate compat-shim line carries the noqa marker; plan 06's gate is now poised to pass.
  </done>
</task>

</tasks>

<verification>
- `docs/SDET-AUTHORING.md` is a stub; `docs/TEST-CODE-AUTHORING.md` is the canonical doc (D-09 + D-10).
- README + CLAUDE.md + auxiliary docs use test-code terminology and direct doc-path references point at `TEST-CODE-AUTHORING.md` (D-11; Phase 30 still owns the lead-with-library-mode rewrite).
- `config.example.yaml` uses `test_code:` key; `examples/homelab-mcp.yaml` either has no `sdet:` key or carries the shim noqa on the one demonstrating the legacy alias.
- `docs/MIGRATION-v1-to-v2.md` retains its historical `sdet` references but each line is tagged with `<!-- noqa: sdet-rename-shim -->`.
- All `src/` operator-facing docstrings, error messages, and Typer help strings use test-code terminology.
- Every retained legacy reference (across docs, examples, config, src) is marked with `# noqa: sdet-rename-shim` or `<!-- noqa: sdet-rename-shim -->`.
- Phase 30 README §SDET-scenarios PASS-sample re-capture is NOT performed here (CLOSE-04 owns it).
</verification>

<success_criteria>
- RENAME-06 acceptance text (operator-facing surface portion) holds: planning-ID + sdet-terminology sweeps return zero residual matches outside D-18 exclusions.
- Plan 06's leak-check gate (next wave) passes against the swept tree.
</success_criteria>

<output>
After completion, create `.planning/phases/25-public-api-rename-seed-023-sdet-test-code/25-05-SUMMARY.md`.
</output>
