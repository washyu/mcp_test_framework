---
phase: 25-public-api-rename-seed-023-sdet-test-code
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/mcp_test_framework/config.py
  - src/mcp_test_framework/cli.py
  - src/mcp_test_framework/fixtures.py
  - src/mcp_test_framework/_runner.py
  - src/mcp_test_framework/models.py
autonomous: true
requirements:
  - RENAME-05
must_haves:
  truths:
    - "Operator can set `test_code:` key in config.yaml and the value loads into `cfg.test_code`."
    - "Operator can set `sdet:` key in config.yaml and the value still loads (via AliasChoices alias) into `cfg.test_code` with one DeprecationWarning per process."
    - "Operator setting BOTH `sdet:` and `test_code:` keys in the same YAML gets a friendly ValidationError naming the ambiguity (no silent precedence rule)."
    - "Internal code reads `cfg.test_code.*` exclusively; no `cfg.sdet` attribute exists on the Config model."
  artifacts:
    - path: "src/mcp_test_framework/config.py"
      provides: "Renamed `TestCodeConfig` model + `test_code` field with AliasChoices('test_code','sdet') + mode='before' model_validator for ambiguity + DeprecationWarning."
      contains: "AliasChoices"
  key_links:
    - from: "src/mcp_test_framework/config.py:Config.test_code"
      to: "TestCodeConfig"
      via: "Field(validation_alias=AliasChoices('test_code','sdet'))"
      pattern: "AliasChoices"
    - from: "src/mcp_test_framework/config.py:@model_validator(mode='before')"
      to: "warnings.warn(DeprecationWarning) on legacy `sdet` key; ValidationError on both keys present"
      via: "Pre-validation dict inspection"
      pattern: "model_validator"
---

<objective>
RENAME-05: Rename the Pydantic model class `SdetConfig` → `TestCodeConfig`. Rename the field on the parent `Config` from `sdet` → `test_code`. Add `Field(validation_alias=AliasChoices("test_code", "sdet"))` so legacy YAML keeps loading. Add a `@model_validator(mode='before')` that (a) fires one `DeprecationWarning` per process if the raw input dict carries the legacy `sdet` key, and (b) raises a friendly `ValidationError` if BOTH `sdet` and `test_code` keys are present in the same dict.

Purpose: Config YAML is the operator's most-edited surface after CLI flags. Aliasing lets every existing `config.yaml` keep working while internal code reads only the new attribute (D-14).

Output: `cfg.test_code.generated_root` is the only Python-side attribute; YAML can use either key; ambiguous YAML fails loud.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-public-api-rename-seed-023-sdet-test-code/25-CONTEXT.md
@src/mcp_test_framework/config.py

<interfaces>
<!-- Current grep landmarks (from `grep SdetConfig|sdet:` on config.py): -->

Line 41: `    SdetConfig,`  -- inside an export list / class body
Line 61: `    sdet: SdetConfig`  -- field declaration on parent Config

Per D-12 (locked decision):
```python
test_code: TestCodeConfig = Field(
    default_factory=TestCodeConfig,
    validation_alias=AliasChoices("test_code", "sdet"),
)
```

Per D-13 (locked decision): Pair the field with a `@model_validator(mode='before')` on the parent. If the raw dict contains `'sdet'`, emit one `DeprecationWarning` per process via `warnings.warn(...)`. If BOTH `'sdet'` and `'test_code'` keys are present, raise `ValueError` (Pydantic converts to `ValidationError`).

Per D-14: Internal code reads `cfg.test_code.*` exclusively. No `cfg.sdet` attribute, no `SdetConfig = TestCodeConfig` typing alias.

Per D-15: Rename the inner model class `SdetConfig` → `TestCodeConfig`. Hard rename — class is internal-only.

External read sites of `cfg.sdet.*` (must all be flipped to `cfg.test_code.*` in this plan to keep the codebase loadable):
- `src/mcp_test_framework/cli.py` — `_BOOTSTRAP_SDET_STUB` references at lines 58, 728, 849; `Config(sdet=...)` keyword at line 728 + 849.
- `src/mcp_test_framework/fixtures.py` — any `cfg.sdet` access (read fixtures.py during execution to confirm precise lines).
- `src/mcp_test_framework/_runner.py` — any `cfg.sdet` access.
- `src/mcp_test_framework/models.py` — likely a re-export of `SdetConfig`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename SdetConfig→TestCodeConfig, add AliasChoices, add model_validator for deprecation warning + ambiguity error</name>
  <read_first>
    - src/mcp_test_framework/config.py
  </read_first>
  <action>
    1. In `src/mcp_test_framework/config.py`, rename the class definition `class SdetConfig(...)` → `class TestCodeConfig(...)`. Update every in-file reference to `SdetConfig` (other classes' annotations, `__all__` exports, type aliases) to `TestCodeConfig`. Per D-15: do NOT add a `SdetConfig = TestCodeConfig` typing alias.

    2. Locate the field declaration on the parent Config model (line 61 area: `sdet: SdetConfig`). Rewrite it as:
    ```python
    test_code: TestCodeConfig = Field(
        default_factory=TestCodeConfig,
        validation_alias=AliasChoices("test_code", "sdet"),
    )
    ```
    Add `AliasChoices` to the existing `from pydantic import ...` import line at the top of the file if not already imported.

    3. Add a class-level `@model_validator(mode='before')` on the parent Config class (place it right after the field declarations, before any existing validators). It must:

    ```python
    @model_validator(mode="before")
    @classmethod
    def _warn_or_reject_legacy_sdet_key(cls, data: Any) -> Any:
        """Handle the v1.4 sdet→test_code alias: warn on legacy key, reject ambiguous both-keys-present input."""
        if not isinstance(data, dict):
            return data
        has_legacy = "sdet" in data
        has_new = "test_code" in data
        if has_legacy and has_new:
            raise ValueError(
                "config.yaml contains both 'sdet' and 'test_code' keys — "
                "remove the legacy 'sdet' key (deprecated since v1.4, removed in v1.5) "
                "and keep only 'test_code'."
            )
        if has_legacy:
            import warnings
            warnings.warn(
                "config.yaml key 'sdet:' is deprecated since v1.4 and will be removed in v1.5 — "
                "use 'test_code:' instead.",
                DeprecationWarning,
                stacklevel=2,
            )
        return data
    ```

    Add `model_validator` to the existing `from pydantic import ...` import line if not already there. Add `from typing import Any` (or extend the existing typing import) if `Any` is not already imported.

    4. Update any `__all__ = [...]` export list at the bottom of `config.py` (if it includes `SdetConfig`) to use `TestCodeConfig` instead. Do not add `SdetConfig` to the exports.
  </action>
  <verify>
    <automated>uv run python -c "from mcp_test_framework.config import Config, TestCodeConfig; c = Config.model_validate({'version': 2, 'sdet': {}}); print('legacy_ok', c.test_code is not None); c2 = Config.model_validate({'version': 2, 'test_code': {}}); print('new_ok', c2.test_code is not None)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class TestCodeConfig" src/mcp_test_framework/config.py` returns `1`
    - `grep -c "class SdetConfig" src/mcp_test_framework/config.py` returns `0`
    - `grep -c "SdetConfig = TestCodeConfig" src/mcp_test_framework/config.py` returns `0` (no typing alias per D-15)
    - `grep -c "AliasChoices(\"test_code\", \"sdet\")" src/mcp_test_framework/config.py` returns `1`
    - `grep -c "@model_validator(mode=\"before\")" src/mcp_test_framework/config.py` returns a count >= 1
    - `grep -q "config.yaml key 'sdet:' is deprecated since v1.4" src/mcp_test_framework/config.py`
    - `grep -q "config.yaml contains both 'sdet' and 'test_code' keys" src/mcp_test_framework/config.py`
    - `uv run python -c "from mcp_test_framework.config import Config; c = Config.model_validate({'version': 2, 'sdet': {}}); print(c.test_code)"` prints a TestCodeConfig representation (legacy YAML key still loads)
    - `uv run python -c "from mcp_test_framework.config import Config; c = Config.model_validate({'version': 2, 'test_code': {}}); print(c.test_code)"` prints a TestCodeConfig representation (new YAML key loads)
    - `uv run python -c "from mcp_test_framework.config import Config; Config.model_validate({'version': 2, 'sdet': {}, 'test_code': {}})" 2>&1 | grep -q "both 'sdet' and 'test_code' keys"` (ambiguity rejected)
    - `uv run python -W always::DeprecationWarning -c "import warnings; warnings.simplefilter('always'); from mcp_test_framework.config import Config; Config.model_validate({'version': 2, 'sdet': {}})" 2>&1 | grep -q "is deprecated since v1.4"` (legacy key fires warning)
  </acceptance_criteria>
  <done>
    config.py exposes `TestCodeConfig` (renamed from `SdetConfig`), the parent Config has `test_code: TestCodeConfig` with `AliasChoices`, and the model_validator handles both deprecation warning and ambiguity rejection.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update internal cfg.sdet read sites to cfg.test_code across cli.py / fixtures.py / _runner.py / models.py</name>
  <read_first>
    - src/mcp_test_framework/cli.py
    - src/mcp_test_framework/fixtures.py
    - src/mcp_test_framework/_runner.py
    - src/mcp_test_framework/models.py
  </read_first>
  <action>
    For each file in read_first, grep for `cfg.sdet`, `Config(sdet=`, `SdetConfig`, `_BOOTSTRAP_SDET_STUB`, and `sdet=` keyword usage that references the Pydantic field (NOT the internal `sdet=` kwarg on `_runner` machinery, which is a separate name owned by plan 04). Rewrite as follows:

    1. `cfg.sdet` (attribute access) → `cfg.test_code`. Every occurrence.
    2. `Config(sdet=...)` keyword call → `Config(test_code=...)`.
    3. `SdetConfig` import or annotation → `TestCodeConfig` import or annotation. Add `TestCodeConfig` to the existing `from mcp_test_framework.config import ...` import line; remove `SdetConfig` from that line.
    4. `_BOOTSTRAP_SDET_STUB` variable name → `_BOOTSTRAP_TEST_CODE_STUB`. Rename the definition AND every usage. This is an internal-only name (leading underscore), so no compat alias is needed.
    5. In `cli.py` line 58 (per CONTEXT.md grep): the comment block mentioning `Config(sdet=_BOOTSTRAP_SDET_STUB)` becomes `Config(test_code=_BOOTSTRAP_TEST_CODE_STUB)`. Update the comment prose too.

    For each file, leave the `--sdet` flag handling, the internal `sdet=` kwarg on `_session_needs_preflight`/`_runner` calls (the function-parameter name `sdet`), and any `tests/sdet/`-related strings UNCHANGED — those are owned by plan 02 (CLI flag) and plan 04 (runner allowlist + tests dir).

    Identification rule for whether a `sdet` reference belongs to this plan or plan 02/04:
    - If the reference is on a Config object (`cfg.sdet`, `Config(sdet=...)`, `SdetConfig`) — THIS plan.
    - If the reference is on the CLI flag/option (`--sdet`, `sdet_legacy`, the flag's help text) — plan 02.
    - If the reference is to a filesystem path (`tests/sdet/`), to `_session_needs_preflight`, or to scenario-module discovery — plan 04.
    - If the reference is in a docstring/comment using the term "SDET" as a persona name — plan 05.

    Mechanical safety: after all edits, the codebase must still import cleanly with `python -c "import mcp_test_framework.cli, mcp_test_framework.fixtures, mcp_test_framework._runner, mcp_test_framework.models"`.
  </action>
  <verify>
    <automated>uv run python -c "import mcp_test_framework.cli, mcp_test_framework.fixtures, mcp_test_framework._runner, mcp_test_framework.models; from mcp_test_framework.config import Config; cfg = Config.model_validate({'version': 2}); print(cfg.test_code)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "cfg\.sdet" src/mcp_test_framework/cli.py src/mcp_test_framework/fixtures.py src/mcp_test_framework/_runner.py src/mcp_test_framework/models.py` returns `0`
    - `grep -c "Config(sdet=" src/mcp_test_framework/cli.py src/mcp_test_framework/fixtures.py src/mcp_test_framework/_runner.py src/mcp_test_framework/models.py` returns `0`
    - `grep -c "SdetConfig" src/mcp_test_framework/cli.py src/mcp_test_framework/fixtures.py src/mcp_test_framework/_runner.py src/mcp_test_framework/models.py` returns `0`
    - `grep -c "_BOOTSTRAP_SDET_STUB" src/mcp_test_framework/cli.py` returns `0`
    - `grep -c "_BOOTSTRAP_TEST_CODE_STUB" src/mcp_test_framework/cli.py` returns a count >= 1
    - `uv run python -c "import mcp_test_framework.cli, mcp_test_framework.fixtures, mcp_test_framework._runner, mcp_test_framework.models; print('ok')"` prints `ok`
    - `uv run python -c "from mcp_test_framework.config import Config; cfg = Config.model_validate({'version': 2, 'test_code': {}}); print(type(cfg.test_code).__name__)"` prints `TestCodeConfig`
    - `grep -c "tests/sdet" src/mcp_test_framework/_runner.py` returns a count >= 1 (plan 04 owns these; they MUST still exist after this task)
    - `grep -c -- "--sdet" src/mcp_test_framework/cli.py` returns a count >= 1 (plan 02 owns these; they MUST still exist after this task)
  </acceptance_criteria>
  <done>
    All internal `cfg.sdet` / `SdetConfig` / `_BOOTSTRAP_SDET_STUB` read sites in cli.py, fixtures.py, _runner.py, models.py now use the new `test_code` attribute and `TestCodeConfig` class. No leftover Pydantic-field-side references to the old name.
  </done>
</task>

</tasks>

<verification>
- Loading a v2 `config.yaml` with `sdet:` key still works and emits one DeprecationWarning per process (D-13).
- Loading a v2 `config.yaml` with `test_code:` key works without warning.
- Loading a v2 `config.yaml` with BOTH keys present fails with a `ValidationError` mentioning the ambiguity (D-13).
- Internal Python code in `src/` reads `cfg.test_code.*` exclusively (D-14). No `cfg.sdet` reference remains anywhere in `src/` outside the alias declaration.
- `SdetConfig` class no longer exists; `TestCodeConfig` is the only inner-model name (D-15).
- `tests/sdet/` filesystem references in `_runner.py` are NOT yet touched — plan 04 owns those.
- `--sdet` CLI flag references in `cli.py` are NOT yet touched (they're plan 02's territory; plan 02 may run in parallel with plan 03 because cli.py touches by plan 03 are config-attribute reads only, while plan 02 touches are flag/command surface; the two sets of cli.py edits are disjoint line ranges).
</verification>

<success_criteria>
- RENAME-05 acceptance text holds: legacy YAML loads with warning; new YAML loads silently; ambiguity rejected.
- Class rename per D-15 is complete; no typing alias retained.
</success_criteria>

<output>
After completion, create `.planning/phases/25-public-api-rename-seed-023-sdet-test-code/25-03-SUMMARY.md`.
</output>
