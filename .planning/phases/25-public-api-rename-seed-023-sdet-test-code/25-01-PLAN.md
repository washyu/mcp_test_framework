---
phase: 25-public-api-rename-seed-023-sdet-test-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_test_framework/sdet/__init__.py
  - src/mcp_test_framework/sdet/_codegen.py
  - src/mcp_test_framework/sdet/_slugs.py
  - src/mcp_test_framework/sdet/_tool_factory.py
  - src/mcp_test_framework/sdet/errors.py
  - src/mcp_test_framework/sdet/response.py
  - src/mcp_test_framework/sdet/session.py
  - src/mcp_test_framework/test_code/__init__.py
  - src/mcp_test_framework/test_code/_codegen.py
  - src/mcp_test_framework/test_code/_slugs.py
  - src/mcp_test_framework/test_code/_tool_factory.py
  - src/mcp_test_framework/test_code/errors.py
  - src/mcp_test_framework/test_code/response.py
  - src/mcp_test_framework/test_code/session.py
  - src/mcp_test_framework/fixtures.py
  - src/mcp_test_framework/models.py
  - src/mcp_test_framework/_runner.py
  - src/mcp_test_framework/_isolation.py
  - src/mcp_test_framework/mcp_client.py
  - src/mcp_test_framework/cli.py
autonomous: true
requirements:
  - RENAME-01
must_haves:
  truths:
    - "Operator can `from mcp_test_framework.test_code import mcp_session, tool, ToolCallError, ToolResponse` and the import resolves."
    - "Old `from mcp_test_framework.sdet import ...` still resolves and emits a DeprecationWarning naming v1.5 removal."
    - "No `from mcp_test_framework.sdet` references remain inside `src/` other than the sdet/ shim package itself."
    - "D-01: sdet/__init__.py shim uses stdlib `DeprecationWarning` (not a custom subclass, not `FutureWarning`) so operators can filter it idiomatically."
    - "D-02: shim fires once-per-process via `warnings.warn(..., stacklevel=2)` relying on Python's default filter; no `simplefilter('always')`."
    - "D-05: shim warning string is the literal template `<old> is deprecated since v1.4 and will be removed in v1.5 — use <new> instead.` naming the v1.5 removal milestone."
  artifacts:
    - path: "src/mcp_test_framework/test_code/__init__.py"
      provides: "Public test-code import surface (mcp_session, tool, ToolCallError, ToolResponse)"
      contains: "from mcp_test_framework.test_code.session import mcp_session"
    - path: "src/mcp_test_framework/test_code/session.py"
      provides: "mcp_session fixture moved from sdet/"
    - path: "src/mcp_test_framework/test_code/_tool_factory.py"
      provides: "tool() factory moved from sdet/"
    - path: "src/mcp_test_framework/sdet/__init__.py"
      provides: "Re-export shim with DeprecationWarning"
      contains: "DeprecationWarning"
  key_links:
    - from: "src/mcp_test_framework/sdet/__init__.py"
      to: "mcp_test_framework.test_code"
      via: "re-export from new package + warnings.warn(DeprecationWarning)"
      pattern: "from mcp_test_framework.test_code"
    - from: "src/mcp_test_framework/fixtures.py"
      to: "mcp_test_framework.test_code"
      via: "internal import (no sdet references)"
      pattern: "from mcp_test_framework\\.test_code"
---

<objective>
RENAME-01: Rename the package directory `src/mcp_test_framework/sdet/` → `test_code/` via `git mv`. Update every internal import inside `src/` to point at `mcp_test_framework.test_code`. Leave a back-compat shim at `src/mcp_test_framework/sdet/__init__.py` that re-exports the public surface from `test_code` and emits a `DeprecationWarning` per process when imported.

Purpose: This is the structural seam every other Wave 2/3 plan hangs off — `gen-test-classes` command shim, `--test-code` flag, `cfg.test_code.*` alias, `tests/test_code/` discovery, and doc updates all reference the new package path.

Output: Renamed package, updated internal imports, sdet shim package, one `DeprecationWarning` per process on `import mcp_test_framework.sdet`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-public-api-rename-seed-023-sdet-test-code/25-CONTEXT.md
@src/mcp_test_framework/sdet/__init__.py

<interfaces>
<!-- The current public surface of sdet/__init__.py — preserve verbatim in test_code/__init__.py -->

From src/mcp_test_framework/sdet/__init__.py (current):
```python
from mcp_test_framework.sdet.errors import ToolCallError
from mcp_test_framework.sdet.response import ToolResponse
from mcp_test_framework.sdet.session import mcp_session
from mcp_test_framework.sdet._tool_factory import tool

__all__ = ["ToolCallError", "ToolResponse", "mcp_session", "tool"]
```

Internal import sites that must be updated (from grep of `src/mcp_test_framework/`):
- `src/mcp_test_framework/_tool_factory.py` references — already inside sdet/ package, updates to relative imports stay valid after git mv
- `src/mcp_test_framework/models.py` — has `from mcp_test_framework.sdet...`
- `src/mcp_test_framework/fixtures.py` — has `from mcp_test_framework.sdet...`
- `src/mcp_test_framework/_runner.py` — references `mcp_test_framework.sdet` namespace
- `src/mcp_test_framework/_isolation.py` — references `mcp_test_framework.sdet` namespace
- `src/mcp_test_framework/mcp_client.py` — references `mcp_test_framework.sdet` namespace
- `src/mcp_test_framework/cli.py` — has imports + many string references (cli.py terminology updates handled in plan 02, but internal `from mcp_test_framework.sdet` imports update here)

NOTE: `cli.py` contains both internal imports (update here) AND `gen-sdet-classes` / `--sdet` operator-facing surface (plan 02). The split rule: this plan ONLY rewrites `from mcp_test_framework.sdet` import statements in cli.py. Do NOT touch the `gen-sdet-classes` command name, `--sdet` flag name, or any operator-visible docstrings/help text. Those belong to plan 02.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: git mv sdet/ package to test_code/ and update internal references inside the renamed files</name>
  <read_first>
    - src/mcp_test_framework/sdet/__init__.py
    - src/mcp_test_framework/sdet/_codegen.py
    - src/mcp_test_framework/sdet/_slugs.py
    - src/mcp_test_framework/sdet/_tool_factory.py
    - src/mcp_test_framework/sdet/errors.py
    - src/mcp_test_framework/sdet/response.py
    - src/mcp_test_framework/sdet/session.py
  </read_first>
  <action>
    1. Run `git mv src/mcp_test_framework/sdet src/mcp_test_framework/test_code` to preserve blame history per D-06 convention (applied here to source as well).
    2. Inside each file under `src/mcp_test_framework/test_code/`, rewrite every absolute import `from mcp_test_framework.sdet` → `from mcp_test_framework.test_code` and every reference to the dotted path `mcp_test_framework.sdet` (in strings, docstrings, type annotations) → `mcp_test_framework.test_code`.
    3. In docstrings inside the test_code/ files, replace any user-facing reference `docs/SDET-AUTHORING.md` → `docs/TEST-CODE-AUTHORING.md` (the rename of the doc file lands in plan 05; the reference path here points to the eventual location).
    4. Do NOT rename any private helper names (e.g. `_session_needs_preflight`, `_slugs`, `_codegen`). Do NOT touch any logic; this is a pure rename.
    5. Per D-15: in `src/mcp_test_framework/test_code/__init__.py` keep `__all__ = ["ToolCallError", "ToolResponse", "mcp_session", "tool"]` verbatim — no new exports.
  </action>
  <verify>
    <automated>uv run python -c "from mcp_test_framework.test_code import mcp_session, tool, ToolCallError, ToolResponse; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `test -d src/mcp_test_framework/test_code` (directory exists)
    - `test -f src/mcp_test_framework/test_code/__init__.py` (file exists)
    - `test -f src/mcp_test_framework/test_code/session.py` (file exists)
    - `test -f src/mcp_test_framework/test_code/_tool_factory.py` (file exists)
    - `grep -rn "from mcp_test_framework.sdet" src/mcp_test_framework/test_code/ | wc -l` returns `0`
    - `grep -rn "mcp_test_framework\.sdet" src/mcp_test_framework/test_code/ | wc -l` returns `0`
    - `uv run python -c "from mcp_test_framework.test_code import mcp_session, tool, ToolCallError, ToolResponse; print('ok')"` prints `ok`
    - `git log --follow src/mcp_test_framework/test_code/__init__.py --oneline | head -5 | wc -l` returns a non-zero count (blame preserved across the rename)
  </acceptance_criteria>
  <done>
    Package directory renamed to test_code/. All internal imports inside the renamed files point at mcp_test_framework.test_code. Public import surface unchanged. Blame history preserved.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update internal import sites OUTSIDE the renamed package to point at test_code/</name>
  <read_first>
    - src/mcp_test_framework/fixtures.py
    - src/mcp_test_framework/models.py
    - src/mcp_test_framework/_runner.py
    - src/mcp_test_framework/_isolation.py
    - src/mcp_test_framework/mcp_client.py
    - src/mcp_test_framework/cli.py
  </read_first>
  <action>
    For each file listed in read_first, rewrite every `from mcp_test_framework.sdet` import statement and every dotted-path reference `mcp_test_framework.sdet` (in code, type annotations, docstrings) to `mcp_test_framework.test_code`.

    SCOPE BOUNDARY for cli.py: ONLY touch lines that contain `from mcp_test_framework.sdet` or the dotted module path `mcp_test_framework.sdet`. Do NOT modify:
    - The `@app.command("gen-sdet-classes")` decorator line or the `gen_sdet_classes` function name.
    - The `--sdet` flag declaration on the `run` command.
    - Any docstring/help-text mention of `gen-sdet-classes`, `--sdet`, or the term "SDET" / "sdet" used as user-facing terminology (those land in plan 02 + plan 05).
    - The `_BOOTSTRAP_SDET_STUB` / `sdet=_BOOTSTRAP_SDET_STUB` references (these touch `Config.sdet`, which is renamed in plan 03; touching them now would break Wave 1's parallel-safety).

    Concrete change pattern example:
    - Before: `from mcp_test_framework.sdet._codegen import generate_classes`
    - After: `from mcp_test_framework.test_code._codegen import generate_classes`

    Per D-14, internal code reads `cfg.test_code.*` exclusively AFTER plan 03 lands. Until plan 03 ships, `cfg.sdet` references stay as-is in this plan (they will be updated atomically with the config rename in plan 03).
  </action>
  <verify>
    <automated>uv run python -c "import mcp_test_framework.fixtures, mcp_test_framework.models, mcp_test_framework._runner, mcp_test_framework._isolation, mcp_test_framework.mcp_client, mcp_test_framework.cli; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "from mcp_test_framework.sdet" src/mcp_test_framework/fixtures.py` returns no matches
    - `grep -n "from mcp_test_framework.sdet" src/mcp_test_framework/models.py` returns no matches
    - `grep -n "from mcp_test_framework.sdet" src/mcp_test_framework/_runner.py` returns no matches
    - `grep -n "from mcp_test_framework.sdet" src/mcp_test_framework/_isolation.py` returns no matches
    - `grep -n "from mcp_test_framework.sdet" src/mcp_test_framework/mcp_client.py` returns no matches
    - `grep -n "from mcp_test_framework.sdet" src/mcp_test_framework/cli.py` returns no matches
    - `grep -c "gen-sdet-classes" src/mcp_test_framework/cli.py` returns a count >= 1 (plan 02 owns these; they MUST still exist after this task)
    - `grep -c '"--sdet"' src/mcp_test_framework/cli.py` returns a count >= 1 (plan 02 owns these)
    - `uv run python -c "import mcp_test_framework.fixtures, mcp_test_framework.models, mcp_test_framework._runner, mcp_test_framework._isolation, mcp_test_framework.mcp_client, mcp_test_framework.cli; print('ok')"` prints `ok`
  </acceptance_criteria>
  <done>
    All internal sdet-namespace imports outside the renamed package now point at test_code. Operator-facing surface (CLI command name, flag, terminology) is untouched and ready for plan 02.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create back-compat sdet/ shim package re-exporting from test_code/ with DeprecationWarning</name>
  <read_first>
    - src/mcp_test_framework/test_code/__init__.py
  </read_first>
  <action>
    Create a new directory and file:

    1. `mkdir src/mcp_test_framework/sdet` (the old git mv emptied this path; recreate as a fresh shim package).

    2. Create `src/mcp_test_framework/sdet/__init__.py` with EXACT content (no leading blank line):
    ```python
    """Back-compat shim — re-exports the public surface from mcp_test_framework.test_code.

    This module is the v1.4 deprecation-window placeholder for the old `sdet`
    import surface. Importing it fires a DeprecationWarning exactly once per
    process (Python's default warning filter); the shim is removed in v1.5.
    """  # noqa: sdet-rename-shim
    from __future__ import annotations

    import warnings

    warnings.warn(
        "mcp_test_framework.sdet is deprecated since v1.4 and will be removed in v1.5 — "
        "use mcp_test_framework.test_code instead.",
        DeprecationWarning,
        stacklevel=2,
    )  # noqa: sdet-rename-shim

    from mcp_test_framework.test_code import (  # noqa: E402, sdet-rename-shim
        ToolCallError,
        ToolResponse,
        mcp_session,
        tool,
    )

    __all__ = ["ToolCallError", "ToolResponse", "mcp_session", "tool"]  # noqa: sdet-rename-shim
    ```

    Per D-01: stdlib `DeprecationWarning` (not custom subclass, not `FutureWarning`).
    Per D-02: `stacklevel=2`; rely on Python's default filter for once-per-process behavior.
    Per D-05: literal string with explicit `v1.4` / `v1.5` milestone names.
    Per D-18: every shim line carries the `# noqa: sdet-rename-shim` token so the plan 06 sweep gate excludes it.

    Do NOT create any other files inside the shim package (no `_codegen.py`, no `session.py`, etc.). The whole shim surface is the four re-exports from `__init__.py`.
  </action>
  <verify>
    <automated>uv run python -W always::DeprecationWarning -c "import warnings; warnings.simplefilter('always'); import mcp_test_framework.sdet" 2>&amp;1 | grep -q "deprecated since v1.4 and will be removed in v1.5"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/mcp_test_framework/sdet/__init__.py` (shim exists)
    - `test ! -f src/mcp_test_framework/sdet/_codegen.py` (no leftover stale module files)
    - `test ! -f src/mcp_test_framework/sdet/session.py` (no leftover stale module files)
    - `test ! -f src/mcp_test_framework/sdet/_tool_factory.py` (no leftover stale module files)
    - `test ! -f src/mcp_test_framework/sdet/errors.py` (no leftover stale module files)
    - `test ! -f src/mcp_test_framework/sdet/response.py` (no leftover stale module files)
    - `test ! -f src/mcp_test_framework/sdet/_slugs.py` (no leftover stale module files)
    - `grep -q "noqa: sdet-rename-shim" src/mcp_test_framework/sdet/__init__.py`
    - `grep -q "deprecated since v1.4 and will be removed in v1.5" src/mcp_test_framework/sdet/__init__.py`
    - `grep -q "use mcp_test_framework.test_code instead" src/mcp_test_framework/sdet/__init__.py`
    - `uv run python -W always::DeprecationWarning -c "import warnings; warnings.simplefilter('always'); import mcp_test_framework.sdet" 2>&1 | grep -q "deprecated since v1.4"`
    - `uv run python -c "from mcp_test_framework.sdet import mcp_session, tool, ToolCallError, ToolResponse; print('ok')"` prints `ok`
  </acceptance_criteria>
  <done>
    sdet/ is a single-file shim that re-exports the public surface from test_code/ and fires one DeprecationWarning per process. Every shim line is marked with the `sdet-rename-shim` noqa token so the plan 06 sweep accepts it.
  </done>
</task>

</tasks>

<verification>
- Package directory rename committed via `git mv` (blame preserved).
- All internal imports inside `src/` (both moved files and external sites) point at `mcp_test_framework.test_code`.
- Back-compat `sdet/__init__.py` shim re-exports the four-name surface.
- Importing `mcp_test_framework.sdet` fires exactly one DeprecationWarning per process via Python's default filter.
- `from mcp_test_framework.test_code import mcp_session, tool, ToolCallError, ToolResponse` resolves with no warning.
- The CLI / config / docs surfaces (`gen-sdet-classes`, `--sdet`, `cfg.sdet`, `docs/SDET-AUTHORING.md`) are NOT yet renamed — those are owned by plans 02 / 03 / 04 / 05.
</verification>

<success_criteria>
- RENAME-01 acceptance text holds: `from mcp_test_framework.test_code import ...` resolves to the renamed package.
- All operator-perceivable surfaces other than the new import path are unchanged at end-of-plan (so plan 02 / 03 / 04 / 05 can be reviewed independently).
- No new tests required by this plan (Wave 4 plan 06 adds the leak gate; package-load smoke is the verify command on each task).
</success_criteria>

<output>
After completion, create `.planning/phases/25-public-api-rename-seed-023-sdet-test-code/25-01-SUMMARY.md`.
</output>
