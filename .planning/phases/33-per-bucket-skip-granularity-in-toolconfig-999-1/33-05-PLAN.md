---
phase: 33-per-bucket-skip-granularity-in-toolconfig-999-1
plan: 05
type: execute
wave: 3
depends_on:
  - 33-01
  - 33-02
  - 33-03
  - 33-04
files_modified:
  - README.md
  - docs/LIBRARY-MODE.md
autonomous: true
requirements:
  - BUCKET-05
tags:
  - docs
  - bucket
must_haves:
  truths:
    - "D-01: Worked example uses a real homelab-mcp tool (not a synthetic placeholder) — README + docs/LIBRARY-MODE.md cite create_proxmox_vm verbatim, droppable into an existing homelab-mcp config without mental substitution"
    - "D-02: The specific tool is create_proxmox_vm — canonical required-field tool; both docs use this exact tool name, no substitutes"
    - "README contains a worked example using create_proxmox_vm with skip_buckets: ['output']"
    - "docs/LIBRARY-MODE.md contains the SAME worked example (D-03 parity)"
    - "Each worked example includes: YAML config snippet + expected --explain output slice + pre-run digest slice"
    - "Docs disambiguate test bucket (schema/judge/output) vs result bucket (per-tool aggregation in runner/reporter) per CONTEXT Claude's Discretion"
  artifacts:
    - path: "README.md"
      provides: "Worked example for per-bucket skip"
      contains: "skip_buckets"
    - path: "docs/LIBRARY-MODE.md"
      provides: "Worked example for per-bucket skip (parity with README)"
      contains: "skip_buckets"
  key_links:
    - from: "README.md"
      to: "docs/LIBRARY-MODE.md"
      via: "same worked example, byte-aligned where reasonable"
      pattern: "create_proxmox_vm"
    - from: "README.md / docs/LIBRARY-MODE.md"
      to: "src/mcp_test_framework/models.py::ToolConfig.skip_buckets"
      via: "documented field name + valid values"
      pattern: "skip_buckets"
---

<objective>
Document per-bucket skip with a worked example in README and docs/LIBRARY-MODE.md using the real homelab-mcp tool `create_proxmox_vm` (CONTEXT D-01/D-02). Each snippet shows YAML config + expected `--explain` output + pre-run digest slice (~25-40 lines per doc, CONTEXT D-03).

Purpose: BUCKET-05 / SC#4. Operator reading either doc finds an authentic config that drops into a homelab-mcp setup without mental substitution; can verify their own config produces the expected output without running it first. Same example in both docs for parity.

Disambiguate the "bucket" name collision in docs (CONTEXT Claude's Discretion): always qualify as "test bucket" (schema / judge / output) vs "result bucket" (per-tool aggregation in `_runner.py` / `_reporter.py`). Do NOT rename either concept (deferred to v1.6).

Output:
- README.md: new subsection under the per-tool config / operator escape hatch area with the worked example.
- docs/LIBRARY-MODE.md: same subsection (D-03 parity).

EXTENDING.md scrub note (per W4 planner-time verification): docs/EXTENDING.md was read end-to-end during planning. It contains no references to "skip the output category", "whole-tool skip is the only", "there is no per-bucket", and no pre-Phase-33 workarounds for required-field tools that contradict the new `skip_buckets` field. The "Add a new MCP tool target" section at lines 204-236 documents the per-tool registry generically (skip/judges/call_arguments) without claiming `skip` is the only mechanism, and the worked example shown is the destructive-tool whole-tool skip pattern -- which remains correct after this phase. EXTENDING.md is omitted from this plan's `files_modified`; no edits required. If a future grep against EXTENDING.md surfaces a stale phrase, address it in a separate v1.6 doc pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/33-per-bucket-skip-granularity-in-toolconfig-999-1/33-CONTEXT.md
@.planning/phases/33-per-bucket-skip-granularity-in-toolconfig-999-1/33-01-PLAN.md
@.planning/phases/33-per-bucket-skip-granularity-in-toolconfig-999-1/33-03-PLAN.md
@.planning/phases/33-per-bucket-skip-granularity-in-toolconfig-999-1/33-04-PLAN.md

@README.md
@docs/LIBRARY-MODE.md
@src/mcp_test_framework/models.py
@src/mcp_test_framework/_runner.py

<interfaces>
<!-- The worked example must use these EXACT strings (verbatim from
     Plan 33-04 acceptance criteria so the docs grep-anchor against the
     implementation):

YAML config snippet:
  tools:
    create_proxmox_vm:
      skip_buckets: ["output"]

--explain output slice (per Plan 33-04 format):
  Bucket-skipped tools (1):
    create_proxmox_vm:
      bucket=output: skipped via tools.create_proxmox_vm.skip_buckets

Pre-run digest slice (per Plan 33-04 format, when bucket-skips present):
  Running:      N  (..., create_proxmox_vm, ...)
  Skipping:     M  (use --explain to list)
  Bucket skips:  1  (use --explain to list)
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add worked example to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — locate the per-tool config section, the operator-escape-hatch area, or the closest analog where `tools.<name>.skip:` is currently documented; new subsection lands there)
    - src/mcp_test_framework/models.py (the actual skip_buckets field doc string for verbatim matching)
  </read_first>
  <behavior>
    - New subsection titled `### Skipping individual test buckets per tool` (or the closest stylistic match to README's existing H3 convention — check existing subsection naming first).
    - Subsection contains EXACTLY these three blocks in order:
      1. One-paragraph rationale (~2-3 sentences): names the problem (required-field tools), names the feature, names the three valid bucket names, links concept to "test bucket" wording.
      2. YAML code block with `tools.create_proxmox_vm.skip_buckets: ["output"]` snippet (~5-8 lines).
      3. Expected output block: the `--explain` slice AND the pre-run digest slice, in plain-text fenced block (~10-15 lines).
    - Total subsection length: 25-40 lines (CONTEXT D-03).
    - Disambiguation sentence: explicitly names "test bucket (schema / judge / output)" vs "result bucket" — operator should not confuse `skip_buckets` with the runner's per-tool result aggregation.
    - No reference to Phase / Plan / requirement IDs (operator-tone, per docs/ERROR-STYLE.md banned strings).
  </behavior>
  <action>
    Edit `README.md`. Locate the per-tool config section (search for `tools:` YAML blocks or `skip:` / `skip_reason:` documentation). Add a new H3 subsection immediately after the existing whole-tool `skip:` documentation. Verbatim content:

    ```markdown
    ### Skipping individual test buckets per tool

    Some tools have required input fields and cannot satisfy the output-bucket
    tests (which call the tool with no arguments by default). Use `skip_buckets`
    on the per-tool entry to opt out of named test buckets while keeping the
    others enabled. Valid test buckets are `schema`, `judge`, and `output`.
    (The English word "bucket" also appears in the runner's per-tool result
    aggregation — those are *result buckets*; the values here are *test
    buckets*.)

    Example: `create_proxmox_vm` requires VM name, node, cores, memory, etc.,
    so the empty-args output check cannot succeed. Keep the schema and judge
    signal while dropping the output bucket:

    ```yaml
    tools:
      create_proxmox_vm:
        skip_buckets: ["output"]
    ```

    Run with `--explain` to verify which (tool, bucket) cells were filtered out
    at collection time:

    ```text
    Skipping (0):

    Bucket-skipped tools (1):
      create_proxmox_vm:
        bucket=output: skipped via tools.create_proxmox_vm.skip_buckets
    ```

    The pre-run digest summarizes the same counts:

    ```text
    Running:      1  (create_proxmox_vm)
    Skipping:     0  (use --explain to list)
    Bucket skips:  1  (use --explain to list)
    ```

    The three output-bucket tests for `create_proxmox_vm` are absent from
    `pytest --collect-only` — they are not collected, not rendered as
    runtime-SKIPPED rows. Setting both `skip: true` and a non-empty
    `skip_buckets` for the same tool is rejected at config load (the two
    levers express redundant intent).
    ```

    Snap the subsection length: ~37 lines. If a different sibling H3 in README uses a different ticks-style for fenced blocks (4-space indent vs triple-backtick), match the surrounding convention.
  </action>
  <verify>
    <automated>grep -c "create_proxmox_vm" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "### Skipping individual test buckets per tool" README.md` returns 1.
    - `grep -c "create_proxmox_vm" README.md` returns >= 3 (rationale text + YAML + --explain output).
    - `grep -c "skip_buckets" README.md` returns >= 3.
    - `grep -c "bucket=output: skipped via tools.create_proxmox_vm.skip_buckets" README.md` returns 1 (the locked grep-anchor from Plan 33-04).
    - `grep -c "Bucket-skipped tools (1):" README.md` returns 1.
    - `grep -c "Bucket skips:" README.md` returns 1.
    - `grep -c "test bucket" README.md` returns >= 1 (disambiguation sentence).
    - `grep -c "result bucket" README.md` returns >= 1 (disambiguation sentence).
    - `grep -E "Phase [0-9]|Plan [0-9]-[0-9]|BUCKET-[0-9]|D-[0-9]" README.md | wc -l` does NOT increase from the v1.5 baseline (ERROR-STYLE.md banned-strings rule).
  </acceptance_criteria>
  <done>
    README has the worked example; operator can paste the YAML, run `--explain`, and see the expected output verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the SAME worked example to docs/LIBRARY-MODE.md</name>
  <files>docs/LIBRARY-MODE.md</files>
  <read_first>
    - docs/LIBRARY-MODE.md (full file — locate the per-tool config section or the operator-escape-hatch analog)
    - README.md (the section authored in Task 1 — copy the example body verbatim so the two docs are byte-aligned per CONTEXT D-03)
  </read_first>
  <behavior>
    - Same subsection title `### Skipping individual test buckets per tool` (parity).
    - YAML block, `--explain` block, pre-run digest block IDENTICAL to README's (so an operator who reads either doc sees the same example).
    - Surrounding rationale paragraph MAY differ slightly to fit LIBRARY-MODE's voice (which is more pytest-ini-focused) — but the YAML/output blocks themselves are byte-for-byte the same.
    - Lands in a sensible spot relative to LIBRARY-MODE's existing per-tool config / `tools:` block discussion.
  </behavior>
  <action>
    Read README.md to extract the just-authored worked example. Replicate it in `docs/LIBRARY-MODE.md` with these adjustments:

    - Title: same `### Skipping individual test buckets per tool`.
    - Opening paragraph: tweak voice to LIBRARY-MODE's pytest-ini-first framing. Example:
      ```
      Same `tools.<name>.skip_buckets` field as the CLI surface — works
      identically in library mode. Use it when a tool has required input
      fields and cannot satisfy the output-bucket tests. Valid test buckets
      are `schema`, `judge`, and `output`. (The English word "bucket" also
      appears in the runner's per-tool result aggregation — those are
      *result buckets*; the values here are *test buckets*.)
      ```
    - YAML block: VERBATIM copy of README's YAML block.
    - --explain block: VERBATIM copy of README's --explain block.
    - Pre-run digest block: VERBATIM copy of README's digest block.
    - Closing sentence about collect-only absence + redundancy rejection: VERBATIM copy.

    If LIBRARY-MODE.md does not yet have a per-tool config section, add the subsection under the closest existing thematic neighbor (e.g., the `mcp_config_file` ini explanation or the `tools:` config sample). Do NOT restructure unrelated LIBRARY-MODE.md content.
  </action>
  <verify>
    <automated>grep -c "create_proxmox_vm" docs/LIBRARY-MODE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "### Skipping individual test buckets per tool" docs/LIBRARY-MODE.md` returns 1.
    - `grep -c "create_proxmox_vm" docs/LIBRARY-MODE.md` returns >= 3.
    - `grep -c "skip_buckets" docs/LIBRARY-MODE.md` returns >= 3.
    - `grep -c "bucket=output: skipped via tools.create_proxmox_vm.skip_buckets" docs/LIBRARY-MODE.md` returns 1.
    - `grep -c "Bucket-skipped tools (1):" docs/LIBRARY-MODE.md` returns 1.
    - `grep -c "Bucket skips:" docs/LIBRARY-MODE.md` returns 1.
    - `grep -c "test bucket" docs/LIBRARY-MODE.md` returns >= 1.
    - `grep -c "result bucket" docs/LIBRARY-MODE.md` returns >= 1.
    - The YAML snippet in docs/LIBRARY-MODE.md matches README.md byte-for-byte for the `tools.create_proxmox_vm.skip_buckets: ["output"]` block. Manual diff sanity check: `diff <(grep -A 3 "tools:" README.md | head -5) <(grep -A 3 "tools:" docs/LIBRARY-MODE.md | head -5)` — or grep both files for the YAML lines and confirm presence in each.
  </acceptance_criteria>
  <done>
    docs/LIBRARY-MODE.md has the parity worked example; D-03 same-example invariant satisfied.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs example -> operator copy/paste -> their config.yaml | docs snapshot of `create_proxmox_vm` schema could drift if homelab-mcp upstream renames or removes the tool |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-13 | Tampering | `create_proxmox_vm` is renamed or removed in upstream homelab-mcp; docs example references a tool that no longer exists | accept | Point-in-time worked example; if upstream renames, docs become factually stale but still demonstrate the feature shape correctly. Per CONTEXT this is the right tradeoff — authentic example > synthetic placeholder. Mitigation deferred to v1.6 doc-scrub pass. |
| T-33-14 | Tampering | --explain / digest output format drifts between Plan 33-04 implementation and docs verbatim block | mitigate | The locked grep-anchor `bucket=output: skipped via tools.create_proxmox_vm.skip_buckets` appears in BOTH the implementation tests (Plan 33-04 acceptance) AND the docs (Tasks 1 & 2 acceptance). Either drifting fails its grep check; Phase 35 capstone can additionally pin the literal across docs + src. |
| T-33-15 | Information Disclosure | n/a | accept | Docs contain non-secret tool name and expected output; no sensitive data. |
</threat_model>

<verification>
- `grep -c "bucket=output: skipped via tools.create_proxmox_vm.skip_buckets" README.md docs/LIBRARY-MODE.md` returns 2 (one per file).
- `grep -c "create_proxmox_vm" README.md docs/LIBRARY-MODE.md` returns >= 6 total.
- `grep -c "skip_buckets" README.md docs/LIBRARY-MODE.md` returns >= 6 total.
- `grep -c "Bucket-skipped tools (1):" README.md docs/LIBRARY-MODE.md` returns 2.
- `uv run pytest tests/framework/ -x` remains green (no test breakage from doc edits).
</verification>

<success_criteria>
- BUCKET-05 / SC#4: Operator reading README or docs/LIBRARY-MODE.md finds a worked example showing `create_proxmox_vm` skipping only the `output` bucket while schema + judge still run.
- Same example in both docs per D-03 (parity).
- Test-bucket vs result-bucket disambiguation surfaced in both docs per CONTEXT Claude's Discretion.
- EXTENDING.md scrub performed at planning time (W4 revision); no edits required.
</success_criteria>

<output>
After completion, create `.planning/phases/33-per-bucket-skip-granularity-in-toolconfig-999-1/33-05-SUMMARY.md`.
</output>
