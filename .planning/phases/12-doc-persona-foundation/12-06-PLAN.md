---
phase: 12-doc-persona-foundation
plan: 06
type: execute
wave: 3
depends_on: [12-03]
files_modified:
  - README.md
  - docs/EXTENDING.md
  - tests/unit/test_doc_scrub.py
autonomous: true
requirements: [CLEAN-01, CLEAN-04, PERSONA-01]
tags: [docs, persona, scrub]

must_haves:
  outcomes:
    - "An operator opening README.md sees a top-of-file 'Testing an MCP server you didn't write' framing paragraph that names the two CLI commands they will use (list-tools, config-init) — black-box framed as a feature, not a test-discipline rule."
    - "An operator reading the README's per-tool-config table or the configuration sections does NOT encounter Phase / Plan / TOOLCFG- / D-NN / quick-task IDs — the prose stands on its own."
    - "An operator following the README's link list finds BOTH `config.example.yaml` (3-pattern template) AND `examples/homelab-mcp.yaml` (worked reference) — the D-14 contract is visible to a first-time reader."
    - "docs/EXTENDING.md gains an operator-facing 'Testing an MCP server you didn't write' walkthrough section; the existing rubric/judge/isolation content has its 6 spec-ID leaks scrubbed."
  truths:
    - "README.md contains zero matches for Phase \\d, Plan \\d-\\d, TOOLCFG-\\d, ISOL-\\d, OUTPUT-\\d, D-\\w+, [0-9]{6}-[a-z0-9]{3}, src/.+\\.py:\\d+ (CLEAN-01 verified by test)."
    - "docs/EXTENDING.md contains zero such matches (CLEAN-01 verified by test)."
    - "README.md contains a heading 'Testing an MCP server you didn't write' (PERSONA-01 / D-12)."
    - "docs/EXTENDING.md contains a heading 'Testing an MCP server you didn't write' (PERSONA-01 / D-12)."
    - "README.md links to BOTH config.example.yaml and examples/homelab-mcp.yaml (CLEAN-04 / D-14)."
    - "README.md does NOT mention the TARGET_TOOL_NAME env var (D-03 — the field is leaving)."
  proofs:
    - "uv run pytest tests/unit/test_doc_scrub.py -x exits 0"
    - "Banned-token grep on README.md returns 0 matches"
    - "Banned-token grep on docs/EXTENDING.md returns 0 matches"
    - "Heading 'Testing an MCP server you didn't write' appears once in README.md and once in docs/EXTENDING.md"
    - "README.md mentions both 'config.example.yaml' and 'examples/homelab-mcp.yaml'"
    - "README.md no longer mentions 'TARGET_TOOL_NAME'"
---

<objective>
Apply per-section semantic rewrites to README.md and docs/EXTENDING.md (per D-10 — never regex-strip and leave dangling sentences). Land the PERSONA-01 framing paragraph in both files (D-12). Update the README "Further reading" link list to point at BOTH `config.example.yaml` AND `examples/homelab-mcp.yaml` (D-14 / CLEAN-04).

Purpose: Plan 06 closes CLEAN-01 (the README + EXTENDING leak scrub), CLEAN-04 (placeholder examples + link to worked reference), and PERSONA-01 (the operator-facing "black-box as feature" reframe). With Plan 06 landed, Phase 12's success-criterion #2 (zero spec-IDs in README/config.example.yaml/.env.example/EXTENDING) is satisfied across all four files.

Output:
- 7 line-targeted edits to README.md (per RESEARCH.md leak table — Task 1).
- 1 new section inserted into README.md (PERSONA-01 framing paragraph + link to EXTENDING walkthrough — Task 2).
- 6 line-targeted edits to docs/EXTENDING.md (per RESEARCH.md leak table — Task 3).
- 1 new section inserted into docs/EXTENDING.md (PERSONA-01 walkthrough — Task 3).
- 1 new test file tests/unit/test_doc_scrub.py (Wave-0 banned-token guard — Task 4).

Sequencing: Plan 06 depends on Plan 03 because the new README link list points at examples/homelab-mcp.yaml (Plan 03 lands the file). Plan 06 does NOT depend on Plan 04 or Plan 05 — the README sample-run section is being lightly edited (TARGET_TOOL_NAME removal); a full sample-run rewrite for the new domain UI lands in Phase 14, not here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-doc-persona-foundation/12-CONTEXT.md
@.planning/phases/12-doc-persona-foundation/12-RESEARCH.md
@.planning/phases/12-doc-persona-foundation/12-PATTERNS.md
@README.md
@docs/EXTENDING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrub README.md (CLEAN-01 — 7 leak sites)</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file)
    - .planning/phases/12-doc-persona-foundation/12-RESEARCH.md (Concrete leaks to scrub > README.md table — 7 leaks)
    - .planning/phases/12-doc-persona-foundation/12-PATTERNS.md (README.md per-line sweep section)
  </read_first>
  <action>
Apply the seven per-line edits below. Each edit is a per-section semantic rewrite (D-10), NOT a regex strip.

EDIT 1 — line ~78: REMOVE the entire `TARGET_TOOL_NAME` row from the env-var documentation table.

The row to delete reads:

    | `TARGET_TOOL_NAME` | `list_keyring_credentials` | Tool under test. The `.env.example` default was switched in Plan 05-05 from `list_registered_servers` (which fails the disambiguation rubric upstream). |

Disposition: DELETE the entire row. v1.2 drops `target.tool_name` (D-03); env-var docs should not advertise a field that is leaving in Phase 13. Do NOT add a replacement row pointing at the `tools:` block — that block is YAML-only, never an env var.

EDIT 2 — line ~96: rewrite the `setup` reserved-field cell.

Before:

    | `setup` | `null` | Reserved for v1.5+ stateful testing (TOOLCFG-03); runtime no-op in v1.1. |

After:

    | `setup` | `null` | Reserved for stateful testing in a future release; no runtime effect today. |

EDIT 3 — line ~97: rewrite the `depends_on` reserved-field cell.

Before:

    | `depends_on` | `null` | Reserved for v1.5+ stateful testing (TOOLCFG-03); runtime no-op in v1.1. |

After:

    | `depends_on` | `null` | Reserved for stateful testing in a future release; no runtime effect today. |

EDIT 4 — line ~150: rewrite the sample-run sentence to drop TARGET_TOOL_NAME.

Before (or near-current):

    + Ollama, with the documented default `TARGET_TOOL_NAME=list_keyring_credentials`.

After:

    + Ollama, against a representative tool surface from the connected server.

If the surrounding paragraph reads awkwardly after the change, smooth it but preserve the technical content (Ollama dependency, sample-run framing). Do NOT rewrite the entire sample-run section — Phase 14 will replace it for the new domain UI.

EDIT 5 — lines ~158-159: rewrite the rubric-failure-mode paragraph.

Before:

    If you switch `TARGET_TOOL_NAME` to a tool whose declared description does not
    satisfy the description-quality rubrics (e.g., the original Phase 04 default
    `list_registered_servers` ...

After:

    If you enable a tool whose declared description does not satisfy the
    description-quality rubrics, the test fails with the judge's reasoning
    recorded in the JUnit XML and printed in the summary. Either tweak the
    tool's description upstream, or skip the tool in your config (per the
    "Per-tool configuration" section above).

Drop `TARGET_TOOL_NAME` references; replace with `enable a tool` (operator-tone, matches v1.2 opt-in semantics). Drop "the original Phase 04 default `list_registered_servers`" — operators do not have planning artefacts.

EDIT 6 — line ~214: rewrite the bug-fix-history sentence.

Before:

    fixed in Phase 04.1; it should not recur in normal operation. The CLI's

After:

    fixed in an earlier release; it should not recur in normal operation. The CLI's

EDIT 7 — README "Further reading" / link list (around lines 225-231): update to link BOTH config.example.yaml and examples/homelab-mcp.yaml (CLEAN-04 / D-14).

Find the existing line that reads similar to:

    - [`config.example.yaml`](config.example.yaml) -- complete real-server per-tool config reference

Replace that single bullet with TWO bullets:

    - [`config.example.yaml`](config.example.yaml) — starter template with placeholder names and three pattern variations.
    - [`examples/homelab-mcp.yaml`](examples/homelab-mcp.yaml) — complete worked example for the homelab-mcp server.

This implements D-14 (different files, different purposes) and CLEAN-04 (link to BOTH).

Note for the executor: use the Edit tool's exact-string replace for each leak (read README.md first to copy the exact current text). Do NOT do a global regex strip. After applying all seven edits, the line numbers will have shifted slightly — that is expected.
  </action>
  <verify>
    <automated>uv run python -c "import re; t=open('README.md',encoding='utf-8').read(); pats=[r'\bPhase \d',r'\bPlan \d-\d',r'\bTOOLCFG-\d',r'\bISOL-\d',r'\bOUTPUT-\d',r'\bD-\d{2}',r'\b\d{6}-[a-z0-9]{3}',r'TARGET_TOOL_NAME']; bad=[p for p in pats if re.search(p,t)]; assert not bad, bad; assert 'examples/homelab-mcp.yaml' in t; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The string 'TARGET_TOOL_NAME' does not appear anywhere in README.md (Edits 1, 4, 5)
    - The pattern 'TOOLCFG-[0-9]' does not appear in README.md (Edits 2, 3)
    - The pattern 'Phase 0[0-9]' does not appear in README.md (Edits 4, 5, 6)
    - The pattern 'Plan [0-9]-[0-9]' does not appear in README.md (Edit 1)
    - The literal string 'examples/homelab-mcp.yaml' appears at least once in README.md (Edit 7)
    - The literal string 'config.example.yaml' appears at least once in README.md (Edit 7 — preserved)
    - The phrase 'starter template with placeholder names' appears at least once (Edit 7 wording check)
    - The phrase 'complete worked example' appears at least once (Edit 7 wording check)
    - README.md still parses as valid markdown (basic sanity — line count ~225-235)
  </acceptance_criteria>
  <done>README.md is leak-free for CLEAN-01; the link list points at both files per CLEAN-04/D-14.</done>
</task>

<task type="auto">
  <name>Task 2: Add PERSONA-01 framing section to README.md (D-12)</name>
  <files>README.md</files>
  <read_first>
    - README.md (locate the "Core Value" / "MVP targets..." paragraph — D-12 says insert AFTER it, near top of file)
    - .planning/phases/12-doc-persona-foundation/12-RESEARCH.md (Code Example 6 — drop-in text + Pitfall 5 — anti-marketing wording)
    - .planning/phases/12-doc-persona-foundation/12-CONTEXT.md (D-12 — top-of-README short framing paragraph + link)
  </read_first>
  <action>
Insert the section below into README.md immediately after the existing "MVP targets the `homelab-mcp` server..." paragraph (or wherever the introductory framing paragraph lives — D-12 says "near Core Value" / "top-of-README"). The section is an H2 heading + 3-5 sentences + 1-line link.

Verbatim text to insert (per RESEARCH.md Code Example 6):

    ## Testing an MCP server you didn't write

    The framework treats your MCP server as a black box — you don't need to read
    its source. `mcp-test-framework list-tools` shows you the tools the server
    exposes and their parameter shapes; `mcp-test-framework config-init` scaffolds
    a config file populated with the actual tools you have. Designed for operators
    testing servers they didn't author.

    The full walkthrough lives in [`docs/EXTENDING.md`](docs/EXTENDING.md#testing-an-mcp-server-you-didnt-write).

Wording rules (Pitfall 5):
- Do NOT change the literal sentence "The framework treats your MCP server as a black box..." — this is the locked persona-framing copy from CONTEXT.md `<specifics>`.
- Do NOT add adjectives like "powerful", "seamless", "empowers" — Pitfall 5 forbids marketing tone.
- Do NOT add a list of features. Three sentences naming the two CLI commands by name + one positioning sentence is the entire content.

The link target `docs/EXTENDING.md#testing-an-mcp-server-you-didnt-write` will resolve after Task 3 of this plan adds the corresponding section heading. The kebab-case anchor matches GitHub's auto-generated heading anchor for "Testing an MCP server you didn't write".

If a "## Core Value" or "## What This Is" section already exists in the README, insert the new section IMMEDIATELY AFTER that section's content (before the next H2 heading). If no such section exists, insert it before the first existing H2 heading (use a deterministic anchor — e.g., immediately after the introductory paragraph).
  </action>
  <verify>
    <automated>uv run python -c "t=open('README.md',encoding='utf-8').read(); assert t.count(\"## Testing an MCP server you didn't write\") == 1; assert 'treats your MCP server as a black box' in t; assert 'mcp-test-framework list-tools' in t; assert 'mcp-test-framework config-init' in t; assert '#testing-an-mcp-server-you-didnt-write' in t; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The H2 heading "## Testing an MCP server you didn't write" appears exactly once in README.md
    - The locked phrase 'treats your MCP server as a black box' appears exactly once in README.md
    - 'mcp-test-framework list-tools' appears at least once
    - 'mcp-test-framework config-init' appears at least once
    - The anchor '#testing-an-mcp-server-you-didnt-write' appears at least once (link to EXTENDING walkthrough)
    - The new section is positioned ABOVE the "## Per-tool configuration" / configuration sections (not buried at the bottom)
    - None of the words 'powerful', 'seamless', 'empowers', 'leverage', 'cutting-edge' appear in the new section's text (anti-marketing — Pitfall 5)
  </acceptance_criteria>
  <done>README.md has the operator-tone persona framing as a discoverable top-level H2 section pointing at the EXTENDING walkthrough.</done>
</task>

<task type="auto">
  <name>Task 3: Scrub docs/EXTENDING.md (CLEAN-01 — 6 leaks) and add PERSONA-01 walkthrough (D-12)</name>
  <files>docs/EXTENDING.md</files>
  <read_first>
    - docs/EXTENDING.md (full file — to identify the leak sites by current content)
    - .planning/phases/12-doc-persona-foundation/12-RESEARCH.md (Concrete leaks to scrub > docs/EXTENDING.md table — 6 leaks)
    - .planning/phases/12-doc-persona-foundation/12-CONTEXT.md (D-12 — walkthrough lives in EXTENDING.md)
  </read_first>
  <action>
Apply six per-line edits, then insert the PERSONA-01 walkthrough section.

EDIT A (~line 128) — TOOLCFG-06 reference.

Before:

    empty argument map (TOOLCFG-06 safe defaults).

After:

    empty argument map (the safe defaults).

EDIT B (~lines 181-182) — file:line reference.

Before:

    This is the warning currently living at
    `src/mcp_test_framework/_isolation.py:33-36`, copied verbatim so contributors
    see it before reading source:

After:

    This is the warning living at the top of
    `src/mcp_test_framework/_isolation.py`, copied verbatim so contributors
    see it before reading source:

EDIT C (~line 185) — DOC-07 / Phase 10 reference.

Before:

    > (DOC-07 in Phase 10). Each new pass-through is a hole in the isolation

After:

    > Each new pass-through is a hole in the isolation

(drop the parenthetical entirely; the warning text stands alone).

EDIT D (~line 215) — Phase 06 D-07 reference.

Before:

    - The locked v1.1 allowlist (Phase 06 D-07) names exactly `USERNAME`. The

After:

    - The current allowlist names exactly `USERNAME`. The

EDIT E (~line 220) — file:line reference.

Before:

    - `USERNAME` is documented in `_isolation.py:55-62` as "informational; some

After:

    - `USERNAME` is documented in `_isolation.py` as "informational; some

EDIT F (~lines 223-228) — verification doc + milestone audit references.

Before (the four-line bullet about cross-platform-parity):

    - Widening `_PASSTHROUGH_ALLOWLIST` to include POSIX `USER` would require a
      CONTEXT.md amendment and a code change, not just a doc edit. The
      cross-platform-parity concern was explicitly recorded as informational in
      `06-VERIFICATION.md` G-03 and `v1.1-MILESTONE-AUDIT.md` W-6, with the
      agreed disposition being rationale-only (Branch B): no `USER` added to
      `_PASSTHROUGH_ALLOWLIST`, the parity gap stays documented here.

After (replace the entire bullet):

    - Widening `_PASSTHROUGH_ALLOWLIST` to include POSIX `USER` would require
      a code change, not just a doc edit. The cross-platform-parity concern is
      documented as informational; the agreed disposition is rationale-only —
      no `USER` is added to `_PASSTHROUGH_ALLOWLIST`, and the parity gap stays
      documented in this section.

EDIT G — add the PERSONA-01 walkthrough section (D-12).

Insert a NEW top-level section "## Testing an MCP server you didn't write" as the FIRST section after the file's intro paragraph (before the existing first H2 heading — typically "## Add a new description-quality rubric" or similar). Per RESEARCH.md Open Question 2 recommendation.

Verbatim section content (read this carefully — the inner triple-backtick code block is part of the section):

    ## Testing an MCP server you didn't write

    The framework is designed for operators testing MCP servers they did not
    author. The "black box" rule (no SUT imports, no SUT source reading) is a
    feature of the persona, not a test-discipline rule you must obey.

    ### Step 1 — discover the surface

    Run `mcp-test-framework list-tools` against your server's launch command.
    You will see one block per tool, for example:

        list_keyring_credentials(service: str)
          Read the named credential from the user's OS keyring.

    The parameter signature comes from the tool's declared `inputSchema`. Add
    `--full` to see the full description and per-parameter descriptions:
    `mcp-test-framework list-tools --full --name keyring`. The `--name PATTERN`
    flag substring-matches case-insensitively, useful at large surfaces (~70+
    tools).

    ### Step 2 — scaffold a config

    Run `mcp-test-framework config-init -o config.yaml`. The framework will
    launch the server, list its tools, and write a self-contained config file
    with every discovered tool listed as `skip: true` and a hint to remove the
    skip from the ones you want to test. No tool will run until you opt in.

    ### Step 3 — opt in tool-by-tool

    Open `config.yaml` and, for each tool you want the framework to call,
    remove the `skip: true` and `skip_reason:` lines from its entry. For tools
    that require non-empty input, add a `call_arguments:` block (see
    `config.example.yaml` for the pattern). For destructive tools you want
    the framework to know about but never call, leave `skip: true` and write
    a curated `skip_reason:` so the test summary explains why the tool sat
    out.

    ### Step 4 — run

    `mcp-test-framework run --config config.yaml`. The framework spawns the
    server, calls each enabled tool, asks the configured Ollama judge to
    evaluate the description against the rubrics you listed, and exits 0 if
    every test passed.

    You never read your server's source. You configured the framework against
    the surface the server itself declares.

Implementation notes:
- The 8-space indentation above (in this `<action>` block) is for in-action readability ONLY. When you write the section into docs/EXTENDING.md, the markdown should be at column 0 (regular markdown, not nested under a list). The example tool block can be a 4-space-indented code block (markdown) or a triple-backtick fenced block — pick whichever matches the existing style elsewhere in EXTENDING.md.
- Edits A–F can be applied in any order; Edit G goes wherever the file structure dictates ("first section after the intro paragraph"). Read the file first to find the right insertion point.

Encoding rules: preserve UTF-8 + LF line endings. Do not normalise the existing markdown spacing or convert backticks. Do not edit any section other than the six leak sites + the new walkthrough.
  </action>
  <verify>
    <automated>uv run python -c "import re; t=open('docs/EXTENDING.md',encoding='utf-8').read(); pats=[r'\bPhase \d',r'\bPlan \d-\d',r'\bTOOLCFG-\d',r'\bISOL-\d',r'\bOUTPUT-\d',r'\bDOC-\d',r'\bD-\d{2}',r'\b\d{6}-[a-z0-9]{3}',r'_isolation\.py:\d+',r'06-VERIFICATION',r'MILESTONE-AUDIT']; bad=[p for p in pats if re.search(p,t)]; assert not bad, bad; assert t.count(\"## Testing an MCP server you didn't write\") == 1; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - None of these patterns match in docs/EXTENDING.md: Phase \d, Plan \d-\d, TOOLCFG-\d, ISOL-\d, OUTPUT-\d, DOC-\d, D-\d{2}, [0-9]{6}-[a-z0-9]{3}, _isolation\.py:\d+, 06-VERIFICATION, MILESTONE-AUDIT
    - The H2 heading "## Testing an MCP server you didn't write" appears exactly once in docs/EXTENDING.md
    - The walkthrough has Step 1, Step 2, Step 3, Step 4 sub-headings (### Step 1 through ### Step 4)
    - The walkthrough mentions `mcp-test-framework list-tools`, `mcp-test-framework config-init`, and `mcp-test-framework run`
    - The walkthrough is positioned ABOVE the existing rubric / judge / isolation sections (not buried at bottom)
    - The six leak sites no longer match RESEARCH.md's leak table
    - File still parses as valid markdown; line count ~270-310 (was 243; gained ~30 from walkthrough, lost ~10 from edits)
  </acceptance_criteria>
  <done>docs/EXTENDING.md is leak-free for CLEAN-01 and contains the operator-tone walkthrough; the README's deep-link from Task 2 now resolves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Write tests/unit/test_doc_scrub.py (Wave-0 banned-token guard for all 4 files)</name>
  <files>tests/unit/test_doc_scrub.py</files>
  <read_first>
    - tests/unit/test_config.py (existing pytest pattern — pure-python unit, no markers)
    - .planning/phases/12-doc-persona-foundation/12-RESEARCH.md (Validation Architecture / Wave 0 Gaps — `tests/unit/test_doc_scrub.py`)
    - .planning/phases/12-doc-persona-foundation/12-CONTEXT.md (CLEAN-01 — the four target files)
  </read_first>
  <behavior>
    - test_readme_no_banned_tokens: README.md contains zero matches for Phase \d, Plan \d-\d, TOOLCFG-\d, ISOL-\d, OUTPUT-\d, D-\d{2}, [0-9]{6}-[a-z0-9]{3}, src/.+\.py:\d+
    - test_extending_no_banned_tokens: docs/EXTENDING.md contains zero matches for the same patterns plus DOC-\d, _isolation\.py:\d+, 06-VERIFICATION, MILESTONE-AUDIT
    - test_config_example_no_banned_tokens: config.example.yaml contains zero matches (Plan 03 already covered this; Plan 06's test is redundant-by-design as a single regression-guard surface)
    - test_dotenv_example_no_banned_tokens: .env.example contains zero matches (Plan 02 covered; same redundancy intent)
    - test_readme_persona_heading: README.md contains exactly one "## Testing an MCP server you didn't write" heading
    - test_extending_persona_heading: docs/EXTENDING.md contains exactly one "## Testing an MCP server you didn't write" heading
    - test_readme_links_both_examples: README.md mentions both "config.example.yaml" and "examples/homelab-mcp.yaml"
    - test_readme_no_target_tool_name: README.md does not mention "TARGET_TOOL_NAME"
  </behavior>
  <action>
Create `tests/unit/test_doc_scrub.py` with the exact contents below. The file source is provided as a fenced ```python code block at column 0 — write it to disk verbatim with NO leading whitespace prepended (every line in the block already starts at column 0).

```python
"""Wave-0 banned-token regression guard for Phase 12 CLEAN-01 + PERSONA-01 + CLEAN-04.

Asserts the four user-facing files (README, config.example.yaml, .env.example,
docs/EXTENDING.md) carry zero spec IDs / phase IDs / quick-task IDs / file:line
refs after Phase 12. Asserts the PERSONA-01 framing section exists in README and
EXTENDING. Asserts CLEAN-04's "link to BOTH" contract.
"""
from __future__ import annotations

import re
from pathlib import Path

import pytest


def _repo_root() -> Path:
    p = Path(__file__).resolve()
    for parent in p.parents:
        if (parent / "pyproject.toml").is_file():
            return parent
    raise RuntimeError("repo root (pyproject.toml) not found")


ROOT = _repo_root()
README = ROOT / "README.md"
EXTENDING = ROOT / "docs" / "EXTENDING.md"
CFG_EXAMPLE = ROOT / "config.example.yaml"
DOTENV = ROOT / ".env.example"


BASE_BANNED = [
    r"\bPhase \d",
    r"\bPlan \d-\d",
    r"\bTOOLCFG-\d",
    r"\bISOL-\d",
    r"\bOUTPUT-\d",
    r"\bD-\d{2}",
    r"\b\d{6}-[a-z0-9]{3}",
]


def _scan(text: str, patterns: list[str]) -> list[str]:
    return [p for p in patterns if re.search(p, text)]


def test_readme_exists_and_no_banned_tokens() -> None:
    assert README.is_file()
    text = README.read_text(encoding="utf-8")
    patterns = BASE_BANNED + [r"src/[\w/.]+\.py:\d+"]
    bad = _scan(text, patterns)
    assert not bad, f"banned tokens in README.md: {bad}"


def test_extending_exists_and_no_banned_tokens() -> None:
    assert EXTENDING.is_file()
    text = EXTENDING.read_text(encoding="utf-8")
    patterns = BASE_BANNED + [
        r"\bDOC-\d",
        r"_isolation\.py:\d+",
        r"06-VERIFICATION",
        r"MILESTONE-AUDIT",
    ]
    bad = _scan(text, patterns)
    assert not bad, f"banned tokens in docs/EXTENDING.md: {bad}"


def test_config_example_no_banned_tokens() -> None:
    text = CFG_EXAMPLE.read_text(encoding="utf-8")
    bad = _scan(text, BASE_BANNED + [r"\bCD-\d"])
    assert not bad, f"banned tokens in config.example.yaml: {bad}"


def test_dotenv_example_no_banned_tokens() -> None:
    text = DOTENV.read_text(encoding="utf-8")
    bad = _scan(text, BASE_BANNED)
    assert not bad, f"banned tokens in .env.example: {bad}"


def test_readme_has_persona_heading_once() -> None:
    text = README.read_text(encoding="utf-8")
    assert text.count("## Testing an MCP server you didn't write") == 1


def test_extending_has_persona_heading_once() -> None:
    text = EXTENDING.read_text(encoding="utf-8")
    assert text.count("## Testing an MCP server you didn't write") == 1


def test_readme_links_both_example_files() -> None:
    text = README.read_text(encoding="utf-8")
    assert "config.example.yaml" in text
    assert "examples/homelab-mcp.yaml" in text


def test_readme_no_target_tool_name() -> None:
    """D-03: TARGET_TOOL_NAME env var is leaving in Phase 13; do not advertise."""
    text = README.read_text(encoding="utf-8")
    assert "TARGET_TOOL_NAME" not in text


def test_readme_persona_section_has_locked_phrase() -> None:
    """Pitfall 5: the locked persona-framing sentence must be verbatim."""
    text = README.read_text(encoding="utf-8")
    assert "treats your MCP server as a black box" in text


def test_readme_persona_section_has_no_marketing_words() -> None:
    """Pitfall 5: anti-marketing tone in operator-facing prose."""
    text = README.read_text(encoding="utf-8")
    # Extract the persona section: from heading to next H2 (or EOF)
    m = re.search(
        r"## Testing an MCP server you didn't write\n(.*?)(?=\n## |\Z)",
        text,
        re.DOTALL,
    )
    assert m, "persona section not found"
    section = m.group(1)
    for word in ("powerful", "seamless", "empowers", "leverage", "cutting-edge"):
        assert word.lower() not in section.lower(), f"marketing word in persona section: {word!r}"
```

After writing the file, run the full test surface to verify Plans 02 / 03 / 06 don't regress each other:

```
uv run pytest tests/unit/test_doc_scrub.py tests/unit/test_dotenv_example.py tests/unit/test_config_example.py -x
```

All ten-plus tests should pass.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_doc_scrub.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_doc_scrub.py exists with at least 10 test functions
    - `uv run pytest tests/unit/test_doc_scrub.py -x` exits 0 (all pass)
    - `uv run pytest tests/unit/test_doc_scrub.py tests/unit/test_dotenv_example.py tests/unit/test_config_example.py tests/unit/test_examples_dir.py tests/unit/test_error_style.py -x` exits 0 (all Wave-0 doc-side tests pass together — proves Plans 01/02/03/06 don't regress each other)
  </acceptance_criteria>
  <done>Banned-token + persona-section regression guard covering all four user-facing files; future drift fails fast.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → operator | Untrusted: an operator reads README.md to decide whether to use the framework; outdated env-var references could mislead them into using a leaving field |
| docs/EXTENDING.md → operator | Untrusted: same surface; the walkthrough is the canonical operator onboarding path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-16 | Information Disclosure | README.md | mitigate | The leak scrub specifically removes file:line references (e.g., `_isolation.py:33-36`) and internal spec IDs that document the v1.1 architecture. Operator-tone rewriting (D-10) does not introduce new architectural detail; it removes detail. Mitigation = the test scaffold in Task 4 prevents regression. |
| T-12-17 | Tampering | docs/EXTENDING.md | mitigate | The PERSONA-01 walkthrough names an actual server (`homelab-mcp`) in the example tool surface (`list_keyring_credentials`). Mitigation: the example is illustrative; copying `mcp-test-framework list-tools --full --name keyring` against an unrelated server returns no matches harmlessly (the empty-filter message names the server's actual tool count). No injected behavioral guidance. |
| T-12-18 | Repudiation | tests/unit/test_doc_scrub.py | accept | The banned-token grep is mechanical and passes/fails deterministically; future maintainers cannot accidentally re-introduce planning-artefact IDs without breaking the test. |
</threat_model>

<verification>
1. All Wave-0 doc-side tests run together:
       uv run pytest tests/unit/test_doc_scrub.py tests/unit/test_dotenv_example.py tests/unit/test_config_example.py tests/unit/test_examples_dir.py tests/unit/test_error_style.py -x

2. Banned-token grep (manual sanity) over all four target files:
       uv run python -c "
       import re
       files = ['README.md', 'docs/EXTENDING.md', 'config.example.yaml', '.env.example']
       pats = [r'\bPhase \d', r'\bPlan \d-\d', r'\bTOOLCFG-\d', r'\bISOL-\d', r'\bOUTPUT-\d', r'\bD-\d{2}', r'\b\d{6}-[a-z0-9]{3}']
       for f in files:
           t = open(f, encoding='utf-8').read()
           for p in pats:
               m = re.search(p, t)
               assert not m, f'{f}: {p!r} matches {m.group(0)!r}'
       print('OK')
       "

3. The README PERSONA-01 link resolves: `grep '#testing-an-mcp-server-you-didnt-write' README.md` returns one match; `grep "## Testing an MCP server you didn't write" docs/EXTENDING.md` returns one match.

4. The README sample-run snippet test is unaffected:
       uv run pytest tests/test_readme_snippets.py -x

   If the snippet test contains a hard-coded `TARGET_TOOL_NAME` reference, update the snippet test to match Plan 06's edits — README is the source of truth.
</verification>

<success_criteria>
1. README.md has 7 leak sites scrubbed, the PERSONA-01 framing section landed, and the link list points at BOTH config.example.yaml and examples/homelab-mcp.yaml.
2. docs/EXTENDING.md has 6 leak sites scrubbed and the operator-tone walkthrough section landed.
3. No banned tokens in README.md, docs/EXTENDING.md, config.example.yaml, .env.example.
4. Persona heading appears exactly once in each of README.md and docs/EXTENDING.md.
5. tests/unit/test_doc_scrub.py exists with at least 10 tests; all pass.
6. All five Wave-0 doc-side tests run together cleanly (test_doc_scrub + test_dotenv_example + test_config_example + test_examples_dir + test_error_style).
</success_criteria>

<output>
After completion, create `.planning/phases/12-doc-persona-foundation/12-06-SUMMARY.md`.
</output>
