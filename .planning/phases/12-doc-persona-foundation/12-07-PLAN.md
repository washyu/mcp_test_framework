---
phase: 12-doc-persona-foundation
plan: 07
type: execute
wave: 1
depends_on: []
coordinates_with: ["12-09"]
gap_closure: true
files_modified:
  - README.md
  - docs/EXTENDING.md
  - tests/unit/test_dotenv_example.py
autonomous: true
requirements: [CLEAN-06, CLEAN-01, PERSONA-01]
tags: [docs, dotenv, persona, gap-closure]

# Cross-plan coordination notes (BLOCKER #1 resolution from checker review):
# - This plan OWNS the rewrite of README.md lines 90-93 (the prose paragraph
#   immediately below the env-var table). The replacement reframes
#   .env.example as CI-secret passthrough only.
# - Plan 12-09 also touches this paragraph but ONLY appends one additional
#   sentence to the END of the replacement (about no cwd auto-discovery).
#   It does NOT replace or modify what this plan writes.
# - Plan 12-09 declares `coordinates_with: ["12-07"]` to enforce sequencing:
#   12-07 lands first (writes the paragraph), 12-09 lands second (appends
#   the no-cwd-auto-discovery sentence on top of 12-07's CI-secret framing).

gap_closes:
  - truth: ".env.example is visible/discoverable to the operator opening the workspace"
    source: 12-UAT.md gap 1
    severity: minor

must_haves:
  outcomes:
    - "An operator following docs/EXTENDING.md's 'Testing an MCP server you didn't write' walkthrough is told that .env.example exists for CI-secret passthrough only and is NOT part of normal local configuration."
    - "An operator scanning README.md no longer sees the misleading `cp .env.example .env` imperative; .env.example is mentioned exactly once in README, with the same CI-secret framing the file uses internally."
  truths:
    - "docs/EXTENDING.md contains a brief mention of .env.example after the 4-step walkthrough, framed as CI-secret passthrough only."
    - "README.md does NOT contain the imperative `cp .env.example .env` (or any equivalent that teaches the operator the file is required for setup)."
    - "README.md mentions .env.example at most once, and that mention uses the same CI-secret framing the file's own docstring uses ('CI secret passthrough', 'never commit a populated .env')."
    - "All four banned-token regression guards in tests/unit/test_doc_scrub.py and tests/unit/test_dotenv_example.py continue to pass after the README/EXTENDING edits."
    - "The README.md prose paragraph at ~lines 90-93 is OWNED BY this plan; Plan 12-09 will append ONE additional sentence at the end of this plan's replacement (about no cwd auto-discovery). This plan must NOT pre-emptively include that sentence — 12-09 inserts it."
  artifacts:
    - path: README.md
      provides: "Quickstart that does NOT imperatively `cp .env.example .env`; single CI-secret-framed mention of the file (or no mention at all). Paragraph at ~lines 90-93 carries CI-secret framing as its primary content."
    - path: docs/EXTENDING.md
      provides: "A short 'CI secrets' subsection (or equivalent paragraph) after Step 4 that names .env.example and explains its narrow purpose."
    - path: tests/unit/test_dotenv_example.py
      provides: "New regression test asserting README does NOT contain `cp .env.example .env` and that EXTENDING.md mentions .env.example exactly once with CI-secret framing."
  key_links:
    - from: docs/EXTENDING.md
      to: .env.example
      via: "post-walkthrough subsection naming the file"
      pattern: "\\.env\\.example"
    - from: tests/unit/test_dotenv_example.py
      to: README.md
      via: "regression assertion that the misleading imperative is gone"
      pattern: "cp \\.env\\.example"
---

<objective>
Doc-only fix for UAT gap 1 (`.env.example invisible`). The diagnosis is unambiguous: this is a documentation disconnect, not a code defect. Two surfaces lie about what .env.example is for:

1. README.md teaches `cp .env.example .env` as imperative setup (lines 25, 32, 85-86, 91), contradicting the file's own v1.1 framing.
2. docs/EXTENDING.md PERSONA-01 walkthrough never mentions .env.example, .env, environment variables, or CI secrets — operators following the canonical entry point are never told the file exists.

Per the UAT diagnosis (12-UAT.md gap 1, `missing:`), this plan delivers:
- Reconcile README.md: drop the `cp .env.example .env` imperative; single mention with CI-secret framing (or no mention at all).
- Add a brief mention in docs/EXTENDING.md (post-Step 4 or "CI secrets" subsection) that .env.example exists for CI-secret passthrough only.
- New regression test in tests/unit/test_dotenv_example.py locking both contracts so future doc rewrites don't reintroduce the broken mental model.

Out of scope (explicitly per `<scope_guardrails>`):
- `.vscode/settings.json` (environmental, not in-repo).
- Any change to .env.example content itself (its v1.1 framing is correct; it's the references TO it that lie).
- The "no cwd auto-discovery" clarification sentence Plan 12-09 will append to this plan's paragraph rewrite — that's 12-09's content, not this plan's.

Purpose: Stop two operator-facing docs from teaching a mental model the file itself contradicts. The file's content is right — only the things pointing at it are wrong.

Output:
- Modified `README.md` — quickstart no longer imperatively `cp`s .env.example; if mentioned at all, framed as CI-secret-only.
- Modified `docs/EXTENDING.md` — short subsection (or paragraph) after Step 4 naming .env.example and explaining its narrow purpose.
- Extended `tests/unit/test_dotenv_example.py` — three new tests asserting README is reconciled and EXTENDING mentions .env.example with CI-secret framing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-doc-persona-foundation/12-CONTEXT.md
@.planning/phases/12-doc-persona-foundation/12-UAT.md
@.planning/phases/12-doc-persona-foundation/VERIFICATION.md
@.planning/phases/12-doc-persona-foundation/12-09-PLAN.md
@README.md
@docs/EXTENDING.md
@.env.example
@tests/unit/test_dotenv_example.py
@tests/unit/test_doc_scrub.py

<interfaces>
<!-- Existing test patterns the new tests must mirror (extracted from tests/unit/test_dotenv_example.py). -->

Repo-root resolution helper (copy this verbatim — do NOT walk relative paths):

```python
def _repo_root() -> Path:
    p = Path(__file__).resolve()
    for parent in p.parents:
        if (parent / "pyproject.toml").is_file():
            return parent
    raise RuntimeError("repo root (pyproject.toml) not found")
```

Banned tokens to scrub the new EXTENDING/README mention against (from
docs/ERROR-STYLE.md "Banned strings (manual review checklist)"):

```
\b(Phase \d|Plan \d-\d|TOOLCFG-|ISOL-|OUTPUT-|D-\d+|CD-\d|SEED-\d|SAFE-\d|CLEAN-\d|PERSONA-\d|\d{6}-[a-z0-9]{3})
```

Current README.md mentions of `.env.example` (verified in this plan's read,
2026-05-09):

- Line 25: prose "uvx (the .env.example default) or installed on PATH"
- Line 32: code block `cp .env.example .env # edit if your Ollama / MCP server differs`
- Line 85: env-var table row "MCP_SERVER_COMMAND .env.example ships uvx for zero-install"
- Line 86: env-var table row "MCP_SERVER_ARGS .env.example ships [\"homelab-mcp\"]"
- Line 91: prose "Copy .env.example to .env and edit. The same vars can be set..."

The reconciliation removes the imperative (line 32) and the encouragement to
treat .env.example as configuration overlay (line 91). Lines 25/85/86 either
get deleted or rewritten — they assert defaults that .env.example explicitly
no longer ships in v1.2 (it has no MCP_SERVER_COMMAND or MCP_SERVER_ARGS
declarations; verified in this plan's read of .env.example).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — add three regression tests in tests/unit/test_dotenv_example.py</name>
  <files>tests/unit/test_dotenv_example.py</files>
  <behavior>
    The existing module already has 7 tests (file exists, no banned tokens, CI-secret framing, points at config-init, no legacy overlay decls, MCPTF_CONFIG_FILE hint, line count). Add three new tests (do NOT modify existing tests):

    1. `test_readme_does_not_imperatively_cp_dotenv_example` — read README.md and assert the literal substring `cp .env.example .env` is NOT present (case-sensitive). Recovery hint in failure message: "README quickstart still teaches operators to copy .env.example as part of setup, which contradicts .env.example's own CI-secret-passthrough framing."

    2. `test_extending_mentions_dotenv_example_with_ci_secret_framing` — read docs/EXTENDING.md. Assert it contains exactly one occurrence of the literal string `.env.example` (count == 1). Then assert that the surrounding 200-char window (100 chars before + 100 chars after the match) contains AT LEAST ONE of: "CI secret", "CI secrets", "secret passthrough", "CI-secret". Failure message: "EXTENDING.md mentions .env.example but without the CI-secret framing the file itself uses."

    3. `test_readme_mentions_dotenv_example_at_most_once` — read README.md and assert `text.count(".env.example") <= 1`. (After this plan: either zero mentions or one mention with the CI-secret framing.) Failure message: "README still references .env.example multiple times — the file's narrow purpose can't survive multiple framings."

    Use the existing `_repo_root()` helper at the top of the file (do NOT redefine).

    Per Nyquist rule: tests MUST FAIL on the current state of README.md (multiple mentions, has `cp .env.example .env`) and EXTENDING.md (zero mentions). After Task 2, all three MUST PASS without modifying the tests.
  </behavior>
  <action>
    Append three new test functions to tests/unit/test_dotenv_example.py per the behavior block above. Reuse the existing `_repo_root()` helper. Do NOT modify any existing test. Do NOT add new top-level constants — define README/EXTENDING paths inside each new test using `_repo_root() / "README.md"` and `_repo_root() / "docs" / "EXTENDING.md"` to keep the diff small.

    After writing, run the new tests in isolation to confirm they FAIL on current trunk (RED state).

    Commit message: `test(12): add regression guards for .env.example doc reconciliation`
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_dotenv_example.py::test_readme_does_not_imperatively_cp_dotenv_example tests/unit/test_dotenv_example.py::test_extending_mentions_dotenv_example_with_ci_secret_framing tests/unit/test_dotenv_example.py::test_readme_mentions_dotenv_example_at_most_once -v</automated>
    Expected: ALL THREE FAIL (RED). The first fails because README.md line 32 has `cp .env.example .env`. The second fails because EXTENDING.md has zero `.env.example` mentions. The third fails because README has 5 mentions.
  </verify>
  <done>
    Three new test functions appended to tests/unit/test_dotenv_example.py. Existing 7 tests still pass (`uv run pytest tests/unit/test_dotenv_example.py -v` shows 7 passed + 3 failed). Commit lands cleanly with message above.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — reconcile README.md and add EXTENDING.md CI-secret subsection</name>
  <files>README.md, docs/EXTENDING.md</files>
  <action>
    **README.md edits:**

    1. **Line 25** ("uvx (the .env.example default) or installed on PATH"): rewrite the prereq bullet to drop the .env.example reference. New text: "`homelab-mcp` runnable via `uvx`, or installed on `PATH`". The "default" claim is outdated — .env.example v1.2 no longer declares MCP_SERVER_COMMAND.

    2. **Lines 31-34** (the `bash` Setup block with `cp .env.example .env`): delete the `cp .env.example .env` line entirely. The block becomes:

       ```bash
       git clone <repo-url>
       cd mvp_test_framework
       uv sync
       ```

       (Three lines — clone, cd, sync. No copy step.)

    3. **Lines 85-86** (env-var table rows mentioning ".env.example ships uvx" and ".env.example ships [\"homelab-mcp\"]"): rewrite the `Purpose` cells to describe the variable's runtime role only — drop the .env.example shipping claims. Suggested replacements: row for `MCP_SERVER_COMMAND` → "MCP server launcher binary."; row for `MCP_SERVER_ARGS` → "Args passed to the launcher (JSON list)."

    4. **Lines 90-93** (the prose paragraph starting "Copy .env.example to .env and edit..."): rewrite to drop the copy-imperative. Replacement paragraph (THIS PLAN OWNS THIS PARAGRAPH; Plan 12-09 will append one additional sentence to the end):

       > "Configure the framework via `config.yaml` — generate a starter with `mcp-test-framework config-init -o config.yaml` and pass it via `--config config.yaml`. Env vars are reserved for CI-secret passthrough only (see `.env.example`); they no longer override config values."

       This is the SOLE permitted mention of .env.example in README after this plan — it appears exactly once, framed as CI-secret-only.

       **Coordination note for the executor:** Plan 12-09 will land AFTER this plan and APPEND one additional sentence to the END of this paragraph (about no cwd auto-discovery). DO NOT pre-emptively include that sentence here — let 12-09 add it. The two plans together produce a three-sentence paragraph: (1) generate via config-init, (2) env vars are CI-secret only, (3) no cwd auto-discovery.

    5. **Lines 229-231** (Troubleshooting block citing ".env (the default uses `uvx homelab-mcp`...)"): rewrite to point at `mcp_server.command` / `mcp_server.args` in `config.yaml` instead of `.env`. Suggested: "If `homelab-mcp` is not on `PATH` and you see `[WinError 2]`: confirm `mcp_server.command` and `mcp_server.args` in your `config.yaml` (e.g. `command: uvx, args: [homelab-mcp]`)."

    After these edits, run a self-check: `text.count(".env.example") == 1` (the CI-secret reference in step 4), and `"cp .env.example" not in text`.

    **docs/EXTENDING.md edit:**

    Add a new short subsection AFTER Step 4 ("run") and BEFORE the closing paragraph "You never read your server's source." Title it `### CI secrets (.env.example)`. Body (3-5 sentences):

    > If your judge backend reads a secret from the environment (an HTTP-backed judge with an API key, for example), set it in `.env` rather than `config.yaml` — secrets do not belong in version-controlled config. The framework ships a `.env.example` at the repo root documenting this CI-secret passthrough convention; copy it to `.env` ONLY if you have such a secret to set. Env vars do not override `config.yaml` values; `.env.example` is not part of normal local setup.

    The mention must use one of the framings the test scans for: "CI secret", "CI secrets", "secret passthrough", or "CI-secret". (The example text above contains "CI secret" and "CI-secret".)

    Do NOT introduce any banned tokens (Phase \d, Plan \d-\d, TOOLCFG-, D-\d+, CLEAN-\d, PERSONA-\d, etc.).

    Commit message: `docs(12): reconcile README and EXTENDING around .env.example CI-secret framing (closes UAT gap 1)`
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_dotenv_example.py tests/unit/test_doc_scrub.py -v</automated>
    Expected: ALL tests pass (the 3 new tests from Task 1 flip RED→GREEN; existing 7 dotenv-example tests + all test_doc_scrub.py tests stay green). Specifically verifies (a) README no longer has `cp .env.example .env`, (b) EXTENDING mentions .env.example exactly once with CI-secret framing, (c) README mentions .env.example at most once, (d) no banned tokens introduced into either file, (e) PERSONA-01 heading still present and unique in both files, (f) "treats your MCP server as a black box" locked phrase still in README.
  </verify>
  <done>
    README.md edits complete: `cp .env.example .env` removed; single CI-secret-framed mention of .env.example remains. docs/EXTENDING.md gains a "CI secrets (.env.example)" subsection between Step 4 and the closing paragraph. All 10 tests in test_dotenv_example.py pass; all tests in test_doc_scrub.py still pass. Commit lands cleanly with the message above.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/unit/test_dotenv_example.py tests/unit/test_doc_scrub.py -v` — 17 tests pass (10 dotenv + 7 doc-scrub).
- Manual: open README.md and confirm the Setup block no longer copies .env.example, and `.env.example` appears at most once in the file with CI-secret framing.
- Manual: open docs/EXTENDING.md and confirm a short subsection between Step 4 and "You never read your server's source." names .env.example with the locked CI-secret framing.
- Banned-token grep over README.md and docs/EXTENDING.md returns zero matches (covered by test_doc_scrub.py).
</verification>

<success_criteria>
The UAT gap-1 truth ("`.env.example is visible/discoverable to the operator opening the workspace`") is satisfied via the documentation surface change: an operator who follows docs/EXTENDING.md (the canonical entry point per PERSONA-01) is now told the file exists and what it's for. An operator who reads README.md is no longer instructed to copy it as setup. Both fixes are mechanically guarded by the three new tests in tests/unit/test_dotenv_example.py.

The editor-side dotfile-hiding contributing factor is acknowledged in the UAT diagnosis but explicitly out of scope (per `<scope_guardrails>`); a `.vscode/settings.json` change would be environmental and is deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/12-doc-persona-foundation/12-07-SUMMARY.md` per @$HOME/.claude/get-shit-done/templates/summary.md, recording:
- The two doc edits (README, EXTENDING) with exact section/line numbers touched.
- The three new tests added to test_dotenv_example.py.
- Confirmation of the gap-1 truth ("`.env.example is visible/discoverable...`") flipping from `failed` → satisfied via the doc reconciliation.
- Forward note: editor-side `.vscode/settings.json` change remains environmental and was explicitly out of scope.
- Cross-plan coordination: this plan OWNS the README.md ~lines 90-93 paragraph rewrite. Plan 12-09 will append ONE additional sentence after this plan lands (about no cwd auto-discovery). Both plans together produce a three-sentence paragraph; this plan must not pre-emptively include 12-09's sentence.
</output>
