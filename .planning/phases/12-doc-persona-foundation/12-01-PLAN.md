---
phase: 12-doc-persona-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/ERROR-STYLE.md
  - examples/README.md
  - tests/unit/test_error_style.py
  - tests/unit/test_examples_dir.py
autonomous: true
requirements: [PERSONA-03]
tags: [docs, error-style, examples, persona]

must_haves:
  outcomes:
    - "An operator opening docs/ERROR-STYLE.md sees four rules, an exit-code table, and the verbatim SAFE-03/SAFE-06 reference messages — discoverable by name and citable from rewritten error sites."
    - "An operator browsing the repo finds examples/ as a one-file-plus-README directory whose convention reads in under a minute."
  truths:
    - "docs/ERROR-STYLE.md exists and is the citation target for every Phase 12 error rewrite (D-16)."
    - "docs/ERROR-STYLE.md contains the SAFE-03 reference message verbatim (D-17) — Phase 13 implements it without rewording."
    - "docs/ERROR-STYLE.md contains the SAFE-06 reference message verbatim (D-17) — Phase 13 implements it without rewording."
    - "examples/README.md exists and explains the naming convention (server-name-as-filename) in 10–20 lines (D-11)."
    - "examples/ is a real directory in the repo (not just a planning idea)."
  proofs:
    - "test -f docs/ERROR-STYLE.md && test -f examples/README.md returns 0"
    - "grep -c 'SAFE-03 — no config found' docs/ERROR-STYLE.md returns 1"
    - "grep -c 'SAFE-06 — config uses an older schema version' docs/ERROR-STYLE.md returns 1"
    - "grep -c 'next: run \\`mcp-test-framework config-init -o config.yaml\\`' docs/ERROR-STYLE.md returns 1 (SAFE-03 next-step)"
    - "uv run pytest tests/unit/test_error_style.py -x exits 0"
---

<objective>
Land the Phase 12 error-message style guide and the empty-shape `examples/` directory README.

Purpose: Plans 03–06 cite docs/ERROR-STYLE.md when rewriting error sites and reference `examples/` when linking from README/CLEAN-04. Both files are pure prose with no upstream dependencies — Wave 1 work that unblocks every other plan.

Output:
- `docs/ERROR-STYLE.md` (NEW) — four rules, exit-code table, SAFE-03 + SAFE-06 reference messages locked verbatim, banned-strings checklist.
- `examples/README.md` (NEW) — directory convention doc.
- `tests/unit/test_error_style.py` (NEW, Wave-0 scaffold) — guarantees the SAFE-03 / SAFE-06 reference text never drifts.
- `tests/unit/test_examples_dir.py` (NEW, Wave-0 scaffold) — guarantees `examples/README.md` exists and links to `homelab-mcp.yaml` (Plan 03 creates the YAML).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-doc-persona-foundation/12-CONTEXT.md
@.planning/phases/12-doc-persona-foundation/12-RESEARCH.md
@.planning/phases/12-doc-persona-foundation/12-PATTERNS.md

<interfaces>
<!-- No code interfaces — pure docs + test scaffold. -->
<!-- The ERROR-STYLE.md "next:" line format is the contract Plan 04 implements via _emit_operator_error. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/ERROR-STYLE.md (D-15, D-16, D-17)</name>
  <files>docs/ERROR-STYLE.md</files>
  <read_first>
    - .planning/phases/12-doc-persona-foundation/12-RESEARCH.md (lines 590-678 — the literal skeleton)
    - .planning/phases/12-doc-persona-foundation/12-CONTEXT.md (D-15, D-16, D-17 sections)
  </read_first>
  <action>
Create `docs/ERROR-STYLE.md` (new file) with the literal content below. Do NOT add a preamble, do NOT alter the bullet wording, do NOT reorder the rules — Phase 13 reads SAFE-03 and SAFE-06 verbatim.

```markdown
# Error message style guide

Operator-facing error messages in `mcp-test-framework` follow four rules.
This guide is the citation target for every rewritten error site (PERSONA-03)
and the canonical source for the SAFE-03 / SAFE-06 reference messages that
Phase 13 will implement verbatim.

## Rules

1. **Operator terms only.** No spec IDs, no internal jargon, no
   `Phase N` / `Plan N-N` / `TOOLCFG-`/`D-`/`CD-` / file:line references.
   The operator does not have your planning artefacts.
2. **Name the actionable next step inline.** Every error ends with a
   `next: <action verb> <command-or-instruction>` line. The verb must
   be imperative (`run`, `check`, `update`, `verify`).
3. **One-line summary + multi-line detail block.** Not a wall of text.
   Format:

       <summary>

       <detail line 1>
       <detail line 2>
       ...

       next: <action> <instruction>

4. **Preserve exit codes.** Don't change behaviour, only words. The
   table below records the contract.

| Error class                              | Exit code |
|------------------------------------------|-----------|
| Config not found / typo'd path           | 2         |
| Config schema mismatch / version error   | 2         |
| MCP server command not on PATH           | 2         |
| MCP server fails handshake               | 2         |
| Judge endpoint unreachable               | 2         |
| Test failure                             | 1         |
| All pass                                 | 0         |
| SIGINT (Ctrl+C)                          | 130       |

## Reference messages (locked for downstream phases)

The following messages are LOCKED in this style guide. Phase 13 implements
them verbatim — copy exactly, do not reword.

### SAFE-03 — no config found, framework refuses to run

    no config file found: ./config.yaml

    the framework refuses to run without a config file because it would
    otherwise call every tool the server advertises -- including any
    destructive ones. you must explicitly opt in to which tools run.

    next: run `mcp-test-framework config-init -o config.yaml` to generate
          a starter config, then edit it to enable the tools you want to test

### SAFE-06 — config uses an older schema version

    config file uses an older format: <path>

    this release of mcp-test-framework expects schema version 2 (opt-in
    tool selection); your config is version 1 (opt-out). the difference
    matters: in v1 a tool with no entry runs by default, in v2 it skips
    by default.

    your existing per-tool settings (`call_arguments`, `judges`,
    `skip_reason`) port forward unchanged -- only the implicit default
    flips. the migration walkthrough at docs/MIGRATION-v1-to-v2.md shows
    the steps.

    next: run `mcp-test-framework config-init -o config.yaml.new` to see
          the v2 layout, port your tool entries across, then replace your
          existing config

## Banned strings (manual review checklist)

When rewriting an error message, grep the rewritten output for these
patterns -- any match is a regression:

    Phase \d
    Plan \d-\d
    [A-Z]{2,}-\d{2}      # spec IDs like TOOLCFG-01, ISOL-02, D-03
    \d{6}-[a-z0-9]{3}    # quick-task IDs like 260507-j6i
    src/.*\.py:\d+       # file:line references
    \(.*\.md\)           # parenthetical file references inside the message
```

Notes:
- The exact heading levels (`#`, `##`, `###`) above are part of the contract — Plan 03/04 tests grep for them.
- The SAFE-03 and SAFE-06 message bodies are indented 4 spaces inside the markdown (rendered as code blocks). Preserve the indentation exactly.
- The SAFE-03 detail line "you must explicitly opt in to which tools run." is the operator-tone replacement for the planner-tone "tools become an allowlist." Do not reword.
  </action>
  <verify>
    <automated>test -f docs/ERROR-STYLE.md && grep -c 'SAFE-03 — no config found' docs/ERROR-STYLE.md && grep -c 'SAFE-06 — config uses an older schema version' docs/ERROR-STYLE.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/ERROR-STYLE.md exists
    - File contains literal heading `### SAFE-03 — no config found, framework refuses to run`
    - File contains literal heading `### SAFE-06 — config uses an older schema version`
    - File contains literal line `next: run \`mcp-test-framework config-init -o config.yaml\` to generate`
    - File contains the four-row Rules section (1. Operator terms 2. Name next step 3. Summary+detail 4. Preserve exit codes)
    - File contains exit-code table with 8 rows (Config not found through SIGINT)
    - `grep -E '\b(Phase [0-9]|Plan [0-9]-[0-9]|TOOLCFG-|ISOL-|OUTPUT-)' docs/ERROR-STYLE.md` returns ZERO matches outside the "Banned strings" checklist code block (the checklist itself contains those patterns by design — exempt the fenced block from the check, or use `grep -v 'Banned strings' | grep -v 'Phase \\\\d'`)
  </acceptance_criteria>
  <done>docs/ERROR-STYLE.md exists, contains both reference messages verbatim, and Plan 04 has a stable copy-paste target.</done>
</task>

<task type="auto">
  <name>Task 2: Write examples/README.md (D-11)</name>
  <files>examples/README.md</files>
  <read_first>
    - .planning/phases/12-doc-persona-foundation/12-RESEARCH.md (section "examples/ directory layout")
    - .planning/phases/12-doc-persona-foundation/12-CONTEXT.md (D-11 — one-file directory + brief README, 10–20 lines)
  </read_first>
  <action>
The directory `examples/` does not yet exist (Plan 03 will populate `examples/homelab-mcp.yaml` via `git mv config.example.yaml examples/homelab-mcp.yaml`). Create the directory implicitly by writing this file.

Write `examples/README.md` with the literal text below — 10–20 lines per D-11, naming convention paragraph mirrors the doc-tree intro shape from `docs/EXTENDING.md`.

```markdown
# Examples

This directory contains real-world configs for specific MCP servers.
Each file is a complete, runnable `config.yaml` you can copy to your
working directory and pass to `mcp-test-framework run --config <path>`.

## Files

- `homelab-mcp.yaml` — config for the
  [`homelab-mcp`](https://github.com/washyu/homelab-mcp) server. The
  reference example used during development of this framework.

## Naming convention

Files are named after the MCP server they target (lowercase, dashes match
the upstream package name). To add an example for another server, drop a
file named after it here and link it from this README.
```

Notes:
- The `homelab-mcp.yaml` link will resolve only after Plan 03 lands `git mv config.example.yaml examples/homelab-mcp.yaml`. That is acceptable — Plan 03 is wave 2 and will close the loop.
- Do NOT include any spec IDs (`Phase \d`, `Plan \d-\d`, `D-\d+`) in this file — it is operator-facing.
  </action>
  <verify>
    <automated>test -f examples/README.md && grep -c 'homelab-mcp.yaml' examples/README.md</automated>
  </verify>
  <acceptance_criteria>
    - examples/README.md exists at repo root path `examples/README.md`
    - File is between 10 and 25 lines (D-11 says "10-20 lines"; allow +5 slack for blank lines)
    - File contains literal heading `# Examples`
    - File contains literal heading `## Files`
    - File contains literal heading `## Naming convention`
    - File mentions `homelab-mcp.yaml` (link target)
    - `grep -E '\b(Phase [0-9]|Plan [0-9]-[0-9]|TOOLCFG-|D-[0-9]+)' examples/README.md` returns 0 matches
  </acceptance_criteria>
  <done>examples/README.md is the discoverable convention doc; Plan 03 will drop homelab-mcp.yaml beside it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write tests/unit/test_error_style.py + tests/unit/test_examples_dir.py (Wave-0 scaffolds)</name>
  <files>tests/unit/test_error_style.py, tests/unit/test_examples_dir.py</files>
  <read_first>
    - tests/unit/test_config.py (existing test pattern — pure-python unit, no markers, no fixtures)
    - .planning/phases/12-doc-persona-foundation/12-RESEARCH.md (Validation Architecture / Phase Requirements → Test Map table)
  </read_first>
  <behavior>
    - test_error_style_md_exists: asserts docs/ERROR-STYLE.md exists at repo root
    - test_error_style_contains_safe_03_message: asserts the file contains the literal SAFE-03 summary line "no config file found: ./config.yaml" and the literal next-step line "next: run `mcp-test-framework config-init -o config.yaml`"
    - test_error_style_contains_safe_06_message: asserts the file contains the literal SAFE-06 summary line "config file uses an older format:" and the literal "schema version 2 (opt-in" detail
    - test_error_style_no_banned_tokens: greps the file for `Phase \d`, `Plan \d-\d`, `TOOLCFG-\d`, `\d{6}-[a-z0-9]{3}` OUTSIDE the "## Banned strings" section (where they appear as documentation of the patterns themselves) — count must be 0
    - test_examples_readme_exists: asserts examples/README.md exists and contains literal text "homelab-mcp.yaml" and "## Naming convention"
  </behavior>
  <action>
Create both test files. Use `pathlib.Path` to find the repo root (walk up from `__file__` until a directory containing `pyproject.toml` is found — the standard pattern in this codebase per tests/unit/test_config.py).

`tests/unit/test_error_style.py`:

```python
"""Wave-0 banned-token + reference-message scaffold for Phase 12 PERSONA-03.

Phase 13 reads SAFE-03 and SAFE-06 reference messages from docs/ERROR-STYLE.md
verbatim. This test guarantees they never drift.
"""
from __future__ import annotations

import re
from pathlib import Path

import pytest


def _repo_root() -> Path:
    p = Path(__file__).resolve()
    for parent in p.parents:
        if (parent / "pyproject.toml").is_file():
            return parent
    raise RuntimeError("repo root (pyproject.toml) not found")


ERROR_STYLE = _repo_root() / "docs" / "ERROR-STYLE.md"


def test_error_style_md_exists() -> None:
    assert ERROR_STYLE.is_file(), f"missing {ERROR_STYLE}"


def test_error_style_contains_safe_03_message() -> None:
    text = ERROR_STYLE.read_text(encoding="utf-8")
    assert "### SAFE-03 — no config found, framework refuses to run" in text
    assert "no config file found: ./config.yaml" in text
    assert "next: run `mcp-test-framework config-init -o config.yaml`" in text


def test_error_style_contains_safe_06_message() -> None:
    text = ERROR_STYLE.read_text(encoding="utf-8")
    assert "### SAFE-06 — config uses an older schema version" in text
    assert "config file uses an older format:" in text
    assert "schema version 2 (opt-in" in text
    assert "docs/MIGRATION-v1-to-v2.md" in text


def test_error_style_no_banned_tokens_outside_checklist() -> None:
    """Operator-facing prose has no spec IDs / phase IDs / file:line refs.

    The "## Banned strings" section is exempt — it documents the patterns.
    """
    text = ERROR_STYLE.read_text(encoding="utf-8")
    # Split off the Banned strings section (everything from that heading onward).
    parts = text.split("## Banned strings", 1)
    body = parts[0]  # everything BEFORE the banned-strings checklist

    banned = [
        (r"\bPhase \d", "Phase N reference"),
        (r"\bPlan \d-\d", "Plan N-N reference"),
        (r"\bTOOLCFG-\d", "TOOLCFG- spec ID"),
        (r"\bISOL-\d", "ISOL- spec ID"),
        (r"\bOUTPUT-\d", "OUTPUT- spec ID"),
        (r"\d{6}-[a-z0-9]{3}", "quick-task ID"),
    ]
    found: list[str] = []
    for pattern, label in banned:
        for m in re.finditer(pattern, body):
            found.append(f"{label}: {m.group(0)!r} at offset {m.start()}")
    assert not found, "banned tokens in operator-facing prose:\n" + "\n".join(found)
```

`tests/unit/test_examples_dir.py`:

```python
"""Wave-0 scaffold for Phase 12 CLEAN-03.

Plan 01 creates examples/README.md; Plan 03 creates examples/homelab-mcp.yaml
via `git mv`. This test asserts both surface for operator browsing.
"""
from __future__ import annotations

from pathlib import Path

import pytest


def _repo_root() -> Path:
    p = Path(__file__).resolve()
    for parent in p.parents:
        if (parent / "pyproject.toml").is_file():
            return parent
    raise RuntimeError("repo root (pyproject.toml) not found")


EXAMPLES_DIR = _repo_root() / "examples"


def test_examples_readme_exists() -> None:
    readme = EXAMPLES_DIR / "README.md"
    assert readme.is_file(), f"missing {readme}"


def test_examples_readme_links_to_homelab_mcp_yaml() -> None:
    readme = (EXAMPLES_DIR / "README.md").read_text(encoding="utf-8")
    assert "homelab-mcp.yaml" in readme
    assert "## Naming convention" in readme


def test_examples_readme_no_spec_ids() -> None:
    """examples/README.md is operator-facing — no planning artefacts."""
    import re
    body = (EXAMPLES_DIR / "README.md").read_text(encoding="utf-8")
    banned = [r"\bPhase \d", r"\bPlan \d-\d", r"\bTOOLCFG-\d", r"\bD-\d{2}"]
    for p in banned:
        assert not re.search(p, body), f"banned pattern {p!r} in examples/README.md"


@pytest.mark.skipif(
    not (EXAMPLES_DIR / "homelab-mcp.yaml").is_file(),
    reason="Plan 03 lands examples/homelab-mcp.yaml; until then this skips.",
)
def test_examples_homelab_mcp_yaml_exists() -> None:
    """Asserts Plan 03's git-mv landed. Skips until the move happens."""
    yaml_file = EXAMPLES_DIR / "homelab-mcp.yaml"
    assert yaml_file.is_file()
    assert yaml_file.stat().st_size > 100, "yaml looks empty/truncated"
```

Run both: `uv run pytest tests/unit/test_error_style.py tests/unit/test_examples_dir.py -x`. The homelab-mcp.yaml test should SKIP (not fail) because Plan 03 hasn't run yet.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_error_style.py tests/unit/test_examples_dir.py -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/unit/test_error_style.py exists and contains 4 test functions (md_exists, safe_03, safe_06, no_banned_tokens)
    - tests/unit/test_examples_dir.py exists and contains 4 test functions
    - `uv run pytest tests/unit/test_error_style.py tests/unit/test_examples_dir.py -x` exits 0 (the homelab-mcp.yaml test SKIPS until Plan 03)
    - All non-skipped tests PASS
  </acceptance_criteria>
  <done>Two test scaffolds in place; banned-token regression guard active; Plan 04's error-helper rewrites have a working test surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/ERROR-STYLE.md → operator | Untrusted: an operator reads this file to understand error UX |
| examples/README.md → operator | Untrusted: an operator reads this file to find example configs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | docs/ERROR-STYLE.md | accept | Both reference messages and the example error format describe public CLI behaviour; nothing leaked is sensitive. The "banned strings" checklist explicitly forbids file:line refs and spec IDs so internal architecture is not echoed in operator-visible text. |
| T-12-02 | Information Disclosure | examples/README.md | accept | Single external link points at a public github.com URL (homelab-mcp). No env var values, paths, or absolute-path leaks. |
| T-12-03 | Tampering | tests/unit/test_error_style.py | mitigate | Test asserts the SAFE-03/SAFE-06 reference text is present verbatim — guards against a future maintainer silently rewording a locked operator message. Mitigation = the test itself. |
</threat_model>

<verification>
Run all tests created in this plan:
```
uv run pytest tests/unit/test_error_style.py tests/unit/test_examples_dir.py -x
```
Plus a manual grep sweep for banned tokens in the new docs:
```
grep -nE '\b(Phase [0-9]|Plan [0-9]-[0-9]|TOOLCFG-|ISOL-|OUTPUT-|[0-9]{6}-[a-z0-9]{3})' docs/ERROR-STYLE.md examples/README.md
```
The first match (if any) MUST be inside the "## Banned strings" checklist of ERROR-STYLE.md (the literal pattern table). Any match in `examples/README.md` is a regression.
</verification>

<success_criteria>
1. `docs/ERROR-STYLE.md` exists with four rules, exit-code table, SAFE-03 and SAFE-06 reference messages verbatim, banned-strings checklist.
2. `examples/README.md` exists with naming convention, link to `homelab-mcp.yaml` (resolves after Plan 03).
3. `tests/unit/test_error_style.py` and `tests/unit/test_examples_dir.py` exist and pass.
4. Banned-token grep over both new doc files finds matches ONLY inside the explicit "## Banned strings" checklist.
</success_criteria>

<output>
After completion, create `.planning/phases/12-doc-persona-foundation/12-01-SUMMARY.md`.
</output>
