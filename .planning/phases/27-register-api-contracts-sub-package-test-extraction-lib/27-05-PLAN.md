---
phase: 27-register-api-contracts-sub-package-test-extraction-lib
plan: 05
type: execute
wave: 3
depends_on: [27-02, 27-03, 27-04]
files_modified:
  - pyproject.toml
  - tests/contract/test_mcp_tool_contract.py
  - tests/conftest.py
  - src/mcp_test_framework/contracts/__init__.py
  - src/mcp_test_framework/fixtures.py
  - tests/framework/test_wheel_shape.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [LIB-03, LIB-05, LIB-06]
must_haves:
  truths:
    - "Framework's own `pyproject.toml` has `mcp_config_file = \"./config.test.yaml\"` set under `[tool.pytest.ini_options]` per D-06 — the framework dogfoods library mode; every CI run is a live operator simulation."
    - "Framework's CI exercises the plugin-driven injection path end-to-end: `uv run pytest` against the framework root produces parametrized contract tests with `<mcp-contracts>::test_<name>[<tool>]` nodeids and zero references to `tests/contract/test_mcp_tool_contract.py` in the collection output."
    - "`tests/contract/test_mcp_tool_contract.py` is DELETED per D-05 — the file no longer exists on disk."
    - "`tests/conftest.py` has the `pytest_plugins=[...]` line, the `pytest_configure` black-box guard body, the `_discover_tools` helper, the `_resolve_tool_names` helper, and the `pytest_generate_tests` body all DELETED per D-05 + D-17 + D-18. The file is either empty (docstring-only) or fully deleted — pytest tolerates either."
    - "`src/mcp_test_framework/contracts/__init__.py` docstring is rewritten to document the ini-driven route; no `register()` reference; no public exports; no public-facing imports."
    - "`src/mcp_test_framework/fixtures.py`'s `_LIVE_PREFIXES` tuple has the transitional `tests/contract/` entry REMOVED — Plan 27-04 added it as a transitional entry; Plan 27-05 removes it now that the legacy collection path is gone."
    - "`tests/framework/test_wheel_shape.py:test_wheel_source_has_no_homelab_mcp_imports` (PACK-04 work) is verified or extended to scan EVERY `.py` file in the built wheel for `import homelab_mcp` / `from homelab_mcp` and fail-fast per D-18 / LIB-08."
    - "REQUIREMENTS.md LIB-01..08 rewritten per CONTEXT.md `<downstream_impact>`; LIB-05 is EXPLICITLY marked as 'Removed — register() API dropped per D-01' (this is how Phase 27 satisfies LIB-05's traceability)."
    - "REQUIREMENTS.md CFG-01 + CFG-02 rewritten per `<downstream_impact>`: CFG-01 collapses to 'env var killed in v1.4 with one-milestone deprecation'; CFG-02 marked as Removed."
    - "ROADMAP.md Phase 27 title, Goal text, Success Criteria 1+4 (the `register()` references), Phase 28 scope, Phase 30 CLOSE-01 amended per `<downstream_impact>`."
  artifacts:
    - path: "pyproject.toml"
      provides: "Framework's own `[tool.pytest.ini_options]` block has `mcp_config_file = \"./config.test.yaml\"` set"
      contains: "mcp_config_file ="
    - path: "src/mcp_test_framework/contracts/__init__.py"
      provides: "Rewritten docstring documenting the ini-driven route (no register())"
      contains: "[tool.pytest.ini_options]"
      contains: "mcp_config_file"
    - path: ".planning/REQUIREMENTS.md"
      provides: "LIB-01..08 + CFG-01..02 amended per CONTEXT.md downstream_impact; LIB-05 marked Removed"
      contains: "LIB-05"
      contains: "Removed"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 27 title + Goal + SC1..5 amended; Phase 28 scope amended; Phase 30 CLOSE-01 amended"
      contains: "pytest-native"
  key_links:
    - from: "pyproject.toml [tool.pytest.ini_options]"
      to: "src/mcp_test_framework/_plugin.py:pytest_configure"
      via: "parser.addini reads the mcp_config_file value at framework-CI runtime"
      pattern: "mcp_config_file\\s*="
    - from: ".planning/REQUIREMENTS.md LIB-05"
      to: "CONTEXT.md D-01 (register API dropped)"
      via: "LIB-05 traceability entry explicitly marked Removed"
      pattern: "LIB-05.*Removed"
---

<objective>
Final plan of Phase 27. Three deliverables:

1. **Move-and-dogfood execution** (D-04 / D-05 / D-06 / D-07):
   - Add `mcp_config_file = "./config.test.yaml"` under `[tool.pytest.ini_options]` in the framework's own `pyproject.toml`.
   - DELETE `tests/contract/test_mcp_tool_contract.py` (its body now lives in `src/mcp_test_framework/contracts/_tests.py` per Plan 27-01).
   - DELETE the now-dead sections of `tests/conftest.py`: the `pytest_plugins` line, the `pytest_configure` black-box guard body, the `_discover_tools` helper, the `_resolve_tool_names` helper, and the `pytest_generate_tests` body. After this, `tests/conftest.py` is either empty + docstring or fully deleted — either is acceptable (D-17 zero-ceremony invariant).
   - REMOVE the transitional `tests/contract/` entry from `_LIVE_PREFIXES` in `src/mcp_test_framework/fixtures.py` (Plan 27-04 added it as transitional; with the legacy collection path now gone, the entry must come out).

2. **Plugin housekeeping**:
   - Rewrite `src/mcp_test_framework/contracts/__init__.py` docstring per CONTEXT.md `<canonical_refs>` to document the ini-driven route — no `register()` reference, no public exports (D-17).
   - Verify (and EXTEND if needed) the wheel-introspection AST guard `tests/framework/test_wheel_shape.py:test_wheel_source_has_no_homelab_mcp_imports` to scan every `.py` file in the wheel for `import homelab_mcp` / `from homelab_mcp` (D-18 / LIB-08). Per 27-PATTERNS.md "Wheel-introspection AST guard" the existing test already covers `src/` per-file; this task confirms via a quick read + adds a regression test specifically asserting `_black_box_guard.py` is scanned (ironic-leak prevention per Pitfall 6 warning sign).

3. **Documentation amendments** per CONTEXT.md `<downstream_impact>` — single docs-sweep landing here so the orchestrator does not have to spawn a separate quick task before Phase 28:
   - REQUIREMENTS.md LIB-01..08: rewrite per the post-pivot specification in `<downstream_impact>`. LIB-05 explicitly marked "Removed — register() API dropped per Phase 27 D-01"; that's how Phase 27 covers the LIB-05 requirement-ID. LIB-01 ("three lines in conftest.py") → "one line in `[tool.pytest.ini_options]`". LIB-02 nodeid shape preserves but with `<mcp-contracts>` prefix per D-08. LIB-03 test-extraction language preserved. LIB-04 marker semantics preserved. LIB-06 fixture-prefix landed in Phase 26. LIB-07 / LIB-08 preserved per D-19 / D-18.
   - REQUIREMENTS.md CFG-01: "library mode IGNORES MCPTF_CONFIG_FILE" → "framework KILLS MCPTF_CONFIG_FILE in v1.4 with one-milestone deprecation, removes in v1.5"; precedence ladder updated.
   - REQUIREMENTS.md CFG-02: `register(config_file=PATH)` escape hatch → Removed.
   - ROADMAP.md Phase 27 entry: title change, Goal text rephrase, SC1+SC4 rewrite per CONTEXT.md.
   - ROADMAP.md milestone goal text: "three lines in conftest.py — Playwright-for-MCPs" → "one line in pyproject.toml — pytest-native MCP contract testing" (or similar; pick the wording).
   - ROADMAP.md Phase 28 entry: scope shrink — CFG-01/CFG-02 collapse; Phase 28 retains CODEGEN-LIB-01/02 only.
   - ROADMAP.md Phase 30 CLOSE-01: "`tests/contract/conftest.py` calls register()" → "framework's pyproject already uses `mcp_config_file`; verify still green at v1.4 close" (D-07 pre-empt).

This is also a CI dogfood proof point — after this plan ships, `uv run pytest` against the framework root MUST inject contract tests via the plugin (the legacy `tests/contract/` path is gone) AND end-to-end behavior matches today's `mcp-test-framework run`.

Plan 27-02/03/04 must have shipped first.

Purpose: Close Phase 27 by completing the move-and-dogfood loop, documenting the new public surface, and updating the source-of-truth artifacts to reflect the pivot.

Output: 8 files modified (1 new ini line, 2 deletions, 5 amendments).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-CONTEXT.md
@.planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-RESEARCH.md
@.planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-PATTERNS.md

@.planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-02-SUMMARY.md
@.planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-03-SUMMARY.md
@.planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-04-SUMMARY.md

@pyproject.toml
@tests/conftest.py
@tests/contract/test_mcp_tool_contract.py
@src/mcp_test_framework/contracts/__init__.py
@src/mcp_test_framework/fixtures.py
@tests/framework/test_wheel_shape.py
@config.test.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `mcp_config_file = "./config.test.yaml"` to framework's `[tool.pytest.ini_options]` (D-06 dogfood activation)</name>
  <files>pyproject.toml</files>

  <read_first>
    - pyproject.toml (READ the entire `[tool.pytest.ini_options]` block — currently lines 52-71 per 27-PATTERNS.md; verify whether `config.test.yaml` is the correct framework-CI config name vs `config.scratch.yaml`. Per 27-RESEARCH.md Open Question O-1, the planner already verified `config.test.yaml` exists in the working tree.)
    - config.test.yaml (READ the top 30 lines to confirm it conforms to the v2 schema the plugin's `Config(yaml_file=...)` loader expects. If `config.test.yaml` still has v1 schema fields (`version: 1`, `target:`), it MUST be migrated to v2 first — the executor reports back and the orchestrator decides whether to inline the migration into this task or punt to a quick task.)
    - .planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-PATTERNS.md (READ the "pyproject.toml" section — contains the exact target shape for the `[tool.pytest.ini_options]` block after the addition)
    - .planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-CONTEXT.md (READ D-06 — the dogfood activation contract)
  </read_first>

  <action>
1. Confirm `config.test.yaml` is v2-schema-compliant (the plugin will run `Config(yaml_file="./config.test.yaml")` on every framework CI run; v1 schema would fail-loud per D-15). If it is v1 schema, FIRST run `uv run mcp-contracts config-init` to inspect the v2 expectation, then either migrate `config.test.yaml` in-place to v2 OR write a new minimal v2 `config.test.yaml` purpose-built for the framework's own CI. Document the choice in the SUMMARY. Acceptable minimal contents for framework CI:

```
ollama:
  base_url: http://127.0.0.1:11434
  model: qwen3.6:latest
  timeout_seconds: 120
mcp_server:
  command: uvx
  args: [homelab-mcp]
  timeout_seconds: 30
tools: {}
```

`tools: {}` is intentional for framework CI: the empty allowlist triggers D-16 silent no-inject, and the framework's CI gets a working plugin path WITHOUT actually spawning the MCP server (since `pytest_collection` short-circuits before the handshake when `allowed` is empty). This is the strongest dogfood proof — exercises every plugin code path that doesn't need a real MCP server.

If the framework's CI WANTS to run live contract tests against a tool, add the tool to `tools:` with `skip: false` — the planner does not lock this; document the choice in the SUMMARY.

2. EDIT `pyproject.toml`'s `[tool.pytest.ini_options]` block. Add a single line: `mcp_config_file = "./config.test.yaml"`. The exact target block (per 27-PATTERNS.md):

```
[tool.pytest.ini_options]
asyncio_mode = "strict"
asyncio_default_fixture_loop_scope = "session"
testpaths = ["tests"]
# Phase 27 D-06: framework dogfoods library mode — contract tests are
# injected via the plugin reading this ini value, not via tests/contract/.
mcp_config_file = "./config.test.yaml"
markers = [
  "live_homelab: requires homelab-mcp runnable via uvx (or on PATH)",
  "live_ollama: requires reachable Ollama at OLLAMA_BASE_URL with the configured model",
]
addopts = "-m 'not live_homelab and not live_ollama'"
filterwarnings = [
    "always::DeprecationWarning:mcp_test_framework",
]
```

Place the new line between `testpaths` and `markers`; keep the `# Phase 27 D-06: ...` comment.

3. **Verify** by running `uv run pytest --collect-only tests/framework/ -q 2>&1 | head -20`. Expected outcome:
   - No crash, no `mcp_config_file points at ... which does not exist` error.
   - If `tools: {}` in config.test.yaml: zero `<mcp-contracts>` items are injected (D-16 silent no-inject); framework self-tests collect as today.
   - If `tools:` has at least one `skip: false` entry: `<mcp-contracts>::test_<name>[<tool>]` items appear in the collection output.
  </action>

  <verify>
    <automated>uv run pytest --collect-only -q 2>&1 | tail -20 && grep -c "mcp_config_file = " pyproject.toml</automated>
  </verify>

  <acceptance_criteria>
    - Grep: `grep -c 'mcp_config_file = "./config.test.yaml"' pyproject.toml` returns `1`.
    - Grep: `grep -B1 -A1 "Phase 27 D-06" pyproject.toml` shows the documenting comment immediately above the new ini line.
    - `uv run pytest --collect-only tests/framework/ -q 2>&1 | grep -c "ERROR\|error\|Error" | head -5` returns `0` lines (no collection-time errors from the new ini value triggering plugin behavior).
    - `uv run pytest tests/framework/ -q -x` exits with code 0.
    - The plugin's `pytest_configure` exercise: if `config.test.yaml` `tools:` is empty, `uv run pytest --collect-only 2>&1 | grep -c "<mcp-contracts>"` returns `0`. If `tools:` has entries with skip=false AND `uvx homelab-mcp` is on PATH, the count is `> 0`. Either is acceptable for framework CI — document which case applies in the SUMMARY.
  </acceptance_criteria>

  <done>
    `pyproject.toml` has the `mcp_config_file` ini line; framework CI exercises the plugin path; collection is clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete `tests/contract/test_mcp_tool_contract.py` and hollow out `tests/conftest.py` per D-05 / D-17 / D-18</name>
  <files>tests/contract/test_mcp_tool_contract.py, tests/conftest.py</files>

  <read_first>
    - tests/contract/test_mcp_tool_contract.py (final read before deletion — confirm the file content matches what was extracted into `src/mcp_test_framework/contracts/_tests.py` in Plan 27-01; if there's drift, the executor stops and reports back)
    - tests/conftest.py (READ the entire file — identify the four sections to delete: line 18 `pytest_plugins`, lines 30-55 `pytest_configure` black-box guard body, lines 72-85 `_discover_tools`, lines 88-143 `_resolve_tool_names`, lines 146-158 `pytest_generate_tests`. Anything else in the file — fixtures, helpers, imports — stays UNLESS it was only there to support those deleted sections)
    - src/mcp_test_framework/contracts/_tests.py (Plan 27-01 product — confirm the extracted bodies are present and unchanged; verify with `grep -c "^async def test_" src/mcp_test_framework/contracts/_tests.py` returning `10`)
    - src/mcp_test_framework/_black_box_guard.py (Plan 27-01 product — confirm `check_black_box` is wired into `_plugin.py:pytest_configure` per Plan 27-02; verify with `grep -c "check_black_box()" src/mcp_test_framework/_plugin.py` returning `1`)
    - .planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-CONTEXT.md (READ D-05 — hard delete; D-17 zero-ceremony; D-18 black-box-guard relocation)
    - .planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-PATTERNS.md (READ "tests/conftest.py" subsection — itemizes the four sections to delete and confirms pytest tolerates an absent `tests/conftest.py`)
  </read_first>

  <action>
1. **DELETE** `tests/contract/test_mcp_tool_contract.py` entirely. Use `git rm tests/contract/test_mcp_tool_contract.py` to track the deletion in the next commit.

2. **HOLLOW OUT** `tests/conftest.py`. Delete the following sections:
   - The `pytest_plugins = [...]` line (around line 18).
   - The `pytest_configure(config)` function body containing the `sys.modules` `homelab_mcp` black-box guard (around lines 30-55) — relocated to `_black_box_guard.py` and invoked by `_plugin.py:pytest_configure`.
   - The `async def _discover_tools(config: Config) -> list[str]` helper (around lines 72-85) — relocated to `_plugin.py:_discover_tools_live` by Plan 27-03.
   - The `def _resolve_tool_names(config: Config) -> list[str]` helper (around lines 88-143) — SAFE-01 filter logic now lives inline in `_plugin.py:pytest_collection` by Plan 27-03.
   - The `def pytest_generate_tests(metafunc: pytest.Metafunc) -> None:` body (around lines 146-158) — replaced by `_plugin.py:pytest_generate_tests` (Plan 27-03), which is gated on the synthetic-module sentinel.
   - Any unused imports left after the deletions (e.g., `import sys`, `import asyncio`, `from mcp_test_framework.config import Config`, `from mcp_test_framework.mcp_client import McpTestClient`, the `_DISCOVERED_TOOL_NAMES` cache if it lives in this file).

After hollowing, decide between two options:
   - **Option A (preferred for D-17 zero-ceremony demonstration)**: keep `tests/conftest.py` as a docstring-only file with no executable code. The docstring should read:
     ```
     """No fixture-loading ceremony required.

     Phase 27 D-17: the framework's pytest plugin (loaded via
     `[project.entry-points.pytest11]`) owns ALL contract-test
     parametrization, marker application, black-box guard, and config
     loading. The operator's `tests/conftest.py` mirror is zero-ceremony —
     this file is the framework's own demonstration of the same.
     """
     ```
   - **Option B**: delete `tests/conftest.py` entirely via `git rm`. Pytest tolerates an absent `tests/conftest.py` (27-PATTERNS.md confirms). Slightly more aggressive proof of D-17.

Pick Option A unless 27-04's SUMMARY recorded a downstream dependency on `tests/conftest.py` existing (e.g., a tests/framework test that imports from it). Document the choice and rationale in the 27-05 SUMMARY.

3. **DO NOT** delete other framework helper files referenced by `tests/conftest.py` — confirm with `grep -rn "from tests.conftest\|from \\.\\.conftest\\|tests/conftest" tests/framework/ src/` before committing. If any framework self-test imports anything from `tests/conftest.py`, the executor stops and reports back (those imports would need to be repointed to the relocated symbols in `_plugin.py` / `_black_box_guard.py` first).

4. **DO NOT** modify `src/mcp_test_framework/_plugin.py` or any other file in this task — Plans 27-02/03/04 are complete; this task only deletes legacy paths.
  </action>

  <verify>
    <automated>test ! -f tests/contract/test_mcp_tool_contract.py && uv run pytest tests/framework/ -q -x --no-header 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `tests/contract/test_mcp_tool_contract.py` does NOT exist on disk (verify with `test ! -f tests/contract/test_mcp_tool_contract.py` returning exit code 0).
    - `git status tests/contract/test_mcp_tool_contract.py` shows the file as deleted (not just absent).
    - Either: `tests/conftest.py` is a docstring-only no-op (Option A — grep `grep -cE "^(def|async def|class|import|from)" tests/conftest.py` returns `0`), OR `tests/conftest.py` does NOT exist (Option B).
    - Grep: `grep -c "pytest_plugins" tests/conftest.py 2>/dev/null || echo 0` returns `0`.
    - Grep: `grep -c "def pytest_generate_tests" tests/conftest.py 2>/dev/null || echo 0` returns `0`.
    - Grep: `grep -c "def pytest_configure" tests/conftest.py 2>/dev/null || echo 0` returns `0`.
    - `uv run pytest tests/framework/ -q -x` exits with code 0 — no framework self-test breaks from the deletions.
    - `uv run pytest --collect-only -q 2>&1 | grep -c "tests/contract/"` returns `0` — the legacy contract path is no longer in collection.
    - The dogfood proof: `uv run pytest tests/framework/ -q -x` AND `uv run pytest --collect-only tests/framework/ 2>&1 | head -30` produce clean output with no `mcp_config_file` errors and no orphan-import errors from the deletions.
  </acceptance_criteria>

  <done>
    Legacy `tests/contract/test_mcp_tool_contract.py` is deleted; `tests/conftest.py` is hollowed (Option A) or absent (Option B); framework self-tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite `src/mcp_test_framework/contracts/__init__.py` docstring; remove transitional `tests/contract/` from `_LIVE_PREFIXES`; verify wheel-introspection AST guard scope (D-17 / D-18 / D-19 follow-through)</name>
  <files>src/mcp_test_framework/contracts/__init__.py, src/mcp_test_framework/fixtures.py, tests/framework/test_wheel_shape.py</files>

  <read_first>
    - src/mcp_test_framework/contracts/__init__.py (READ the current docstring stub from Phase 26 — confirm it says "future library-mode milestone fills the register() API" and has NO public exports)
    - src/mcp_test_framework/fixtures.py (READ around `_LIVE_PREFIXES` tuple — Plan 27-04 added `tests/contract/` as a transitional entry; Task 2 of THIS plan deleted the legacy `tests/contract/` collection path, so the entry now comes out)
    - tests/framework/test_wheel_shape.py (READ the wheel-introspection test, particularly `test_wheel_source_has_no_homelab_mcp_imports` at lines 142-166 per 27-PATTERNS.md — verify it scans every `.py` in the wheel for `import homelab_mcp` / `from homelab_mcp`)
    - .planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-PATTERNS.md (READ the "contracts/__init__.py" section — contains the literal target docstring for the rewrite; READ "Wheel-introspection AST guard" subsection — confirms the existing test already scans `src/` per-file)
    - .planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-RESEARCH.md (READ "Pitfall 6: Wheel-introspection AST guard scope expansion" — describes the ironic-leak risk where `_black_box_guard.py` itself imports homelab_mcp)
    - .planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-CONTEXT.md (READ D-17 zero-ceremony, D-18 black-box-guard relocation + wheel AST guard, D-19 preflight predicate)
  </read_first>

  <action>
1. **Rewrite `src/mcp_test_framework/contracts/__init__.py`** entirely. Replace the current Phase 26 stub docstring with the new docstring documenting the ini-driven route. Per 27-PATTERNS.md the target shape is exactly:

```
"""mcp_test_framework.contracts — pytest-injected contract test surface.

This subpackage contains the 10 contract-test bodies (TEST-01..TEST-10)
that the framework's pytest plugin synthesizes into operator test
sessions. Operators do NOT import from this module directly — the
library-mode entry point is a single ini line:

    # operator's pyproject.toml
    [tool.pytest.ini_options]
    mcp_config_file = "./config.yaml"

When `mcp_config_file` is set, the plugin (`mcp_test_framework._plugin`)
reads the YAML, runs the black-box guard, parametrizes the test bodies
in `_tests.py` over `config.tools` (Phase 13 SAFE-01 opt-in semantics),
applies the `mcp_contract` marker, and injects them under the synthetic
nodeid `<mcp-contracts>::test_<name>[<tool>]`.

When `mcp_config_file` is unset, this subpackage is inert — no contract
tests are injected. Operators who installed `mcp-contracts` solely for
`gen-test-classes` see no behavioral change.

There is intentionally NO `register()` API. Phase 27 pivoted away from
the register() approach in favor of a pytest-native ini route (CONTEXT.md D-01).
"""
```

The file MUST contain NO imports, NO public exports, NO function or class definitions — JUST the docstring. (`py.typed` already ships in this directory per Phase 26.)

2. **Remove the transitional `tests/contract/` entry from `_LIVE_PREFIXES` in `src/mcp_test_framework/fixtures.py`**. Plan 27-04 added it as a stopgap; with Task 2 of this plan deleting the legacy collection path, the entry must come out. The final `_LIVE_PREFIXES` should be:

```
_LIVE_PREFIXES: tuple[str, ...] = (
    "tests/test_code/",
    "tests/sdet/",  # noqa: sdet-rename-shim
)
```

Update the leading comment block to reflect that `tests/contract/` is fully replaced by the `mcp_contract` marker iteration (no transitional concern remains). Replace the Plan 27-04 transitional comment with:

```
# Phase 27 D-19: `tests/contract/` removed — Plan 27-03's plugin applies the
# `mcp_contract` marker to every injected contract test; `_session_needs_preflight`
# detects them via marker iteration. The test-code-author paths (tests/test_code/
# + legacy tests/sdet/) stay on path-prefix detection because they do NOT carry
# the contract marker — see 27-RESEARCH.md Pitfall 5 / O-5.
```

3. **Verify (and EXTEND if needed) the wheel-introspection AST guard.** Per 27-PATTERNS.md and 27-RESEARCH.md Pitfall 6:
   - READ `tests/framework/test_wheel_shape.py` around `test_wheel_source_has_no_homelab_mcp_imports` (lines 142-166 per the pattern map).
   - The existing test SHOULD already iterate every `.py` file in the built wheel and assert no `import homelab_mcp` / `from homelab_mcp` line-prefixes. If it does, no extension is required; document confirmation in the SUMMARY.
   - If the existing test does NOT scan every `.py` file (e.g., it only checks specific glob patterns), EXTEND it to walk every `.py` file in `mcp_test_framework/` inside the wheel and grep for `import homelab_mcp` or `from homelab_mcp` line prefixes. The grep MUST exclude commented occurrences (`grep -v '^[[:space:]]*#'`).
   - Confirm specifically that `src/mcp_test_framework/_black_box_guard.py` IS in the wheel and IS scanned by the test (Pitfall 6 ironic-leak risk). Add a regression assertion: a sub-check inside the test that finds `_black_box_guard.py` in the wheel's `RECORD` AND scans it.
   - **LIB-08 AST-walk decision branch (mandatory check):** run `grep -cE 'ast\.(parse|walk|NodeVisitor)' tests/framework/test_wheel_shape.py`. If the count is `>= 1`, the existing test already uses an AST walk and satisfies LIB-08's "AST-walk" language verbatim — no upgrade required; record the grep output in the SUMMARY.
     - **If the grep returns `0`**, the existing test uses a fragile line-prefix-only grep and does NOT satisfy LIB-08's AST-walk language. The upgrade is NOT optional. The executor MUST upgrade `tests/framework/test_wheel_shape.py:test_wheel_source_has_no_homelab_mcp_imports` to use `ast.parse` + a `NodeVisitor` (or `ast.walk`) on each `.py` file in the wheel, checking `ast.Import` and `ast.ImportFrom` nodes for `homelab_mcp` references. After the upgrade, re-run the grep and confirm `>= 1`. Record the before/after grep counts in the SUMMARY.
     - LIB-08's spec language is "wheel-introspection CI test fails on banned SUT imports inside `src/`" with explicit AST-walk semantics — the line-prefix grep does NOT cover string-concatenated imports, conditional imports, or aliased imports that an `ast.walk` over `ast.Import`/`ast.ImportFrom` catches.

4. **Do NOT modify** `_plugin.py`, `_runner.py`, `cli.py`, or any other file in this task.
  </action>

  <verify>
    <automated>uv run pytest tests/framework/test_wheel_shape.py -q -x --no-header 2>&1 | tail -10 && grep -c "register()" src/mcp_test_framework/contracts/__init__.py && grep -c "tests/contract/" src/mcp_test_framework/fixtures.py</automated>
  </verify>

  <acceptance_criteria>
    - Grep: `grep -c "register()" src/mcp_test_framework/contracts/__init__.py` returns `1` (the single mention is in the line "There is intentionally NO `register()` API" — confirms the explicit-no-register documentation).
    - Grep: `grep -c "mcp_config_file" src/mcp_test_framework/contracts/__init__.py` returns at least `2` (mentioned in the docstring's ini example and prose).
    - Grep: `grep -cE "^(def |class |import |from |async def )" src/mcp_test_framework/contracts/__init__.py` returns `0` — the file is docstring-only.
    - Grep: `grep -c "tests/contract/" src/mcp_test_framework/fixtures.py` returns `0` — the transitional entry is gone.
    - Grep: `grep -A4 '_LIVE_PREFIXES: tuple\[str, ...\] = ' src/mcp_test_framework/fixtures.py | grep -c 'tests/test_code/'` returns `1`.
    - Grep: `grep -A4 '_LIVE_PREFIXES: tuple\[str, ...\] = ' src/mcp_test_framework/fixtures.py | grep -c 'tests/sdet/'` returns `1`.
    - `uv run pytest tests/framework/test_wheel_shape.py -q -x` exits with code 0.
    - `uv run pytest tests/framework/ -q -x` exits with code 0.
    - Confirm `_black_box_guard.py` is in the test's scope: `uv run python -c "import zipfile, pathlib, glob; w = sorted(pathlib.Path('dist').glob('mcp_contracts-*.whl')); assert w, 'no wheel built'; print(any('_black_box_guard.py' in n for n in zipfile.ZipFile(w[-1]).namelist()))"` (only runs if a wheel exists in `dist/` — otherwise document the confirmation deferral to the next CI build). Print `True` if the file is in the wheel.
    - **LIB-08 AST-walk verification (mandatory):** `grep -cE 'ast\.(parse|walk|NodeVisitor)' tests/framework/test_wheel_shape.py` returns `>= 1`. If this fails, the executor missed the LIB-08 upgrade — apply it now per Task 3 action's mandatory-branch instructions. The SUMMARY MUST record both the pre-edit and post-edit grep output (with the post-edit count `>= 1`).
  </acceptance_criteria>

  <done>
    `contracts/__init__.py` documents the ini-driven route with no public exports; `_LIVE_PREFIXES` no longer contains the transitional `tests/contract/`; wheel-introspection guard scope confirmed.
  </done>
</task>

<task type="auto">
  <name>Task 4: Amend REQUIREMENTS.md and ROADMAP.md per CONTEXT.md `<downstream_impact>` — LIB-01..08, CFG-01..02, Phase 27/28/30 entries + milestone goal text</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md (READ the full file, specifically the LIB-01..08, CFG-01..02 entries — lines 31-43 per the planner's pre-read of REQUIREMENTS.md — and the Traceability table at the bottom)
    - .planning/ROADMAP.md (READ Phase 27, Phase 28, Phase 30 entries — currently at lines 121-141 (Phase 27), 133-142 (Phase 28), 155-164 (Phase 30); also the milestone goal text around line 68: "Reframe the framework as an importable Python test package — operator adds it to their MCP server's `pyproject.toml`, writes three lines in `conftest.py`, runs their existing `pytest`. Playwright-for-MCPs.")
    - .planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-CONTEXT.md (READ `<downstream_impact>` section in its entirety — this is the literal source for every amendment in this task)
  </read_first>

  <action>
This task is pure documentation surgery. Apply each amendment listed in CONTEXT.md `<downstream_impact>`. Do NOT add new requirements or success criteria; only amend existing ones.

1. **REQUIREMENTS.md — LIB-01..08 rewrite:**

   - **LIB-01**: rewrite text to "Operator adds one line in `[tool.pytest.ini_options]` (`mcp_config_file = \"./config.yaml\"`) in `pyproject.toml` and parametrized contract tests appear in their `pytest` collection. No `register()` call; no `conftest.py` edit; no `pytest_plugins` declaration."
   - **LIB-02**: text preserves but with `<mcp-contracts>` prefix per D-08. Rewrite to "Operator's `pytest --collect-only` lists every injected contract test with stable nodeids of the form `<mcp-contracts>::test_<name>[<tool>]` — without spawning the MCP server (collection phase only)."
   - **LIB-03**: text preserved per D-04 (move-and-dogfood verbatim).
   - **LIB-04**: text preserved; remove the `_REGISTRATIONS` / `_INJECTED` references — replace with: "Operator's pytest selects framework-injected contract tests with `pytest -m mcp_contract` (or excludes with `-m \"not mcp_contract\"`); marker is auto-applied at injection time via the plugin's `pytest_collection`. The plugin's `_ContractsModule(_PytestModule)` subclass with overridden `nodeid` synthesizes the injected items."
   - **LIB-05**: REPLACE the entire entry with: "**LIB-05** (REMOVED): RegistrationError, frame validation, and double-call detection are moot — Phase 27 D-01 dropped the `register()` API entirely in favor of the pytest-native ini route. LIB-05 closes by virtue of the underlying mechanism it described no longer existing."
   - **LIB-06**: text preserved (Phase 26 work).
   - **LIB-07**: text preserved per D-19 (marker-based detection with test-code-author carveout).
   - **LIB-08**: text preserved per D-18 (relocation to `_black_box_guard.py`); update the location reference to point at `src/mcp_test_framework/_black_box_guard.py:check_black_box`.

2. **REQUIREMENTS.md — CFG-01 + CFG-02 rewrite:**

   - **CFG-01**: REPLACE with: "Operator's `[tool.pytest.ini_options] mcp_config_file = PATH` is the sole library-mode config source. The framework KILLS `MCPTF_CONFIG_FILE` env var in v1.4 with a one-milestone DeprecationWarning emitted by the plugin's `pytest_configure`; removal lands in v1.5 cleanup. Precedence ladder: `pytest -o \"mcp_config_file=...\"` (CLI subprocess + runtime override) > `[tool.pytest.ini_options]` (ini) > defaults. CLI mode (`mcp-contracts run --config PATH`) internally subprocesses `pytest -o \"mcp_config_file=PATH\"` — single config-resolution route end-to-end."
   - **CFG-02**: REPLACE with: "**CFG-02** (REMOVED): `register(config_file=PATH)` escape hatch dropped — the ini value IS the explicit escape hatch. Multi-config (list of paths) deferred to v1.5 if a real use case emerges (CONTEXT.md `<downstream_impact>`)."

3. **REQUIREMENTS.md Traceability table:**

   - Mark LIB-05 Status as `Removed (Phase 27 D-01)` instead of `Pending`.
   - Mark CFG-02 Status as `Removed (Phase 27 D-01)` instead of `Pending`.
   - Mark LIB-01 through LIB-08 (except LIB-05) and CFG-01 as `Complete (Phase 27)` after this plan ships — the executor adjusts these to `Pending`/`Complete` based on the actual milestone position; the planner expects these to flip to Complete during Phase 27 close, not in this task.

4. **ROADMAP.md — Phase 27 entry:**

   - **Title**: "Phase 27: `register()` API + contracts sub-package + test extraction (LIB)" → "Phase 27: pytest-native ini config + contracts test injection + dogfood (LIB)" (or shorter variant; matches CONTEXT.md `<downstream_impact>` proposal).
   - **Goal**: rewrite from "Operator writes three lines in `tests/conftest.py` — `from mcp_test_framework.contracts import register; register(server_command=[...], tools=[...], judge=...)` — and parametrized contract tests appear..." to "Operator sets one line in `pyproject.toml` — `[tool.pytest.ini_options] mcp_config_file = \"./config.yaml\"` — and parametrized contract tests appear in their pytest collection with the same pass/fail signal as today's CLI. Load-bearing technical bet of v1.4."
   - **Success Criteria SC1**: replace "writes `register(...)` in `tests/conftest.py`" with "sets `[tool.pytest.ini_options] mcp_config_file = PATH` in `pyproject.toml`"; preserve the rest of the SC1 text (no MCP server spawn during collection).
   - **SC2**: nodeid shape "`<contracts-module>::test_<name>[<tool>]`" → "`<mcp-contracts>::test_<name>[<tool>]`" (matches D-08).
   - **SC3**: marker semantics preserved (text mostly fine).
   - **SC4**: REPLACE the double-call / frame-validation paragraph with: "Operator setting `mcp_config_file` to a non-existent path sees a friendly `pytest.exit` operator-tone error with `returncode=2` (D-14). Operator with a malformed YAML sees a friendly validation error (D-15). Operator with `MCPTF_CONFIG_FILE` set in their environment sees a one-time `DeprecationWarning` pointing at the new ini route (D-09)."
   - **SC5**: wheel-introspection AST-walk text preserved per D-18.

5. **ROADMAP.md — Phase 28 entry scope shrink:**

   - **Requirements**: REMOVE CFG-01 and CFG-02 from Phase 28's `**Requirements:**` line — they're now closed in Phase 27. Phase 28 retains CODEGEN-LIB-01, CODEGEN-LIB-02.
   - **Goal text**: rewrite to reflect that the config seam is closed; Phase 28 is now codegen-output-path policy only.
   - **Success Criteria**: REMOVE SC1 and SC2 (the env-var-ignore and `register(config_file=)` escape hatch). Keep SC3 + SC4 (codegen output path defaults + refuse-to-write-under-site-packages).

6. **ROADMAP.md — Phase 30 CLOSE-01 amendment:**

   - **CLOSE-01**: rewrite from "Framework's own `tests/contract/conftest.py` calls `register(config=Config())` — proving the library-mode dogfood loop end-to-end. `tests/conftest.py`'s `pytest_generate_tests` is removed..." to "Framework's own `pyproject.toml` already sets `[tool.pytest.ini_options] mcp_config_file = \"./config.test.yaml\"` (landed in Phase 27 D-06); verify the dogfood loop still green at v1.4 close. CLI-mode path (`mcp-contracts run`) continues to subprocess pytest with JUnit XML round-trip and shares the same `ParsedRun` domain model and renderer helpers."
   - Add a note that CLOSE-01 was pre-empted by Phase 27 D-07.

7. **ROADMAP.md — milestone goal text amendment:**

   - REPLACE the current goal text: "Reframe the framework as an importable Python test package — operator adds it to their MCP server's `pyproject.toml`, writes three lines in `conftest.py`, runs their existing `pytest`. Playwright-for-MCPs."
   - WITH: "Reframe the framework as an importable Python test package — operator adds it to their MCP server's `pyproject.toml`, sets one line in `[tool.pytest.ini_options]` pointing at their YAML config, runs their existing `pytest`. Pytest-native MCP contract testing."

7.5. **Cross-phase ROADMAP audit (BLOCKER fix from revision iteration 1)**:

   The planner-side revision restored Phase 28, 29, and 30 `**Plans**` blocks to `**Plans**: TBD`. The executor MUST confirm those blocks remain at `TBD` after applying the Phase 27 / 28 / 30 scope amendments — the amendments edit Title / Goal / Requirements / Success Criteria, NOT the Plans line. Specifically:

   - **Before editing Phase 28/29/30 sections, read ROADMAP.md lines 145-185 (or grep `grep -n "^### Phase 2[789]" .planning/ROADMAP.md` and `grep -n "^### Phase 30" .planning/ROADMAP.md` to find current line numbers) and record the current Plans line for each.**
   - **After editing**, read the same line ranges and confirm Phase 28, 29, 30 `**Plans**:` lines are STILL `TBD` (the Phase 27 entry is the only one with a populated 5-item plan list).
   - If any of Phase 28/29/30 `**Plans**` blocks show 27-0X plan IDs again, the executor introduced a regression — revert the Plans block back to `TBD` and re-run the post-edit grep.

8. **Do NOT** update PROJECT.md or any other documentation file in this task — those live in Phase 30 CLOSE-03 / CLOSE-04 scope.

Note on tone: every rewritten requirement and success-criterion is OUTCOME-shaped (what the operator observes), not implementation-shaped (what the code does internally). Match the existing REQUIREMENTS.md / ROADMAP.md voice — operator-perspective, no "We will refactor..." language.
  </action>

  <verify>
    <automated>grep -c "mcp_config_file" .planning/REQUIREMENTS.md && grep -c "register()" .planning/REQUIREMENTS.md && grep -c "pytest-native" .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - Grep: `grep -c "LIB-05" .planning/REQUIREMENTS.md` returns at least `2` (the active-requirements entry + the traceability-table entry).
    - Grep: `grep -A2 "LIB-05" .planning/REQUIREMENTS.md | grep -ci "Removed"` returns at least `1` — confirms LIB-05 is explicitly marked Removed.
    - Grep: `grep -c "CFG-02" .planning/REQUIREMENTS.md` returns at least `2`.
    - Grep: `grep -A2 "CFG-02" .planning/REQUIREMENTS.md | grep -ci "Removed"` returns at least `1` — confirms CFG-02 is explicitly marked Removed.
    - Grep: `grep -c "mcp_config_file" .planning/REQUIREMENTS.md` returns at least `3` (LIB-01 + CFG-01 + at least one Future-Requirements / Out-of-Scope reference).
    - Grep: `grep -c "register()" .planning/REQUIREMENTS.md` returns `0` outside the explicit "**LIB-05** (REMOVED)" and "**CFG-02** (REMOVED)" entries documenting the drop. (Spot-check `grep -n "register()" .planning/REQUIREMENTS.md` — every match should be inside an explicit Removed-marker entry.)
    - Grep: `grep -c "Phase 27: pytest-native ini config" .planning/ROADMAP.md` returns `1` — confirms the title amendment.
    - Grep: `grep -c "Phase 27.*register()" .planning/ROADMAP.md` returns `0` outside contexts explicitly noting the drop.
    - Grep: `grep -c "<mcp-contracts>::test_" .planning/ROADMAP.md` returns at least `1` — confirms the nodeid shape SC update.
    - Grep: `grep -c "Pytest-native MCP contract testing" .planning/ROADMAP.md` returns `1` (the milestone goal amendment).
    - Grep: `grep -A2 "CLOSE-01" .planning/ROADMAP.md | grep -c "Phase 27 D-06\|Phase 27 D-07"` returns at least `1` — confirms CLOSE-01 records the D-07 pre-emption.
    - `gsd-sdk query verify.plan-structure ".planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-05-PLAN.md"` returns `valid: true` (validates the plan file we created, not the amended docs — sanity check that the SDK still validates).
    - **Cross-phase ROADMAP audit (mandatory)**:
      - `grep -A8 "^### Phase 28" .planning/ROADMAP.md | grep -c "27-0"` returns `0` — no Phase 27 plan IDs leaked into Phase 28's block.
      - `grep -A8 "^### Phase 29" .planning/ROADMAP.md | grep -c "27-0"` returns `0` — no Phase 27 plan IDs leaked into Phase 29's block.
      - `grep -A8 "^### Phase 30" .planning/ROADMAP.md | grep -c "27-0"` returns `0` — no Phase 27 plan IDs leaked into Phase 30's block.
      - `grep -A2 "^### Phase 28" .planning/ROADMAP.md | grep -c "\*\*Plans\*\*: TBD"` returns `1` (or appears within the first ~10 lines after the Phase 28 header — adjust the `-A` count if Goal/Depends-on/Requirements lines push the Plans line further down).
      - Same TBD-confirmation grep for Phase 29 and Phase 30.
      - The SUMMARY MUST record the before-edit and after-edit output of `grep -B1 -A2 "^### Phase 28" .planning/ROADMAP.md` (and the same for Phase 29/30) so a reviewer can audit that the Plans line stayed at `TBD` end-to-end.
  </acceptance_criteria>

  <done>
    REQUIREMENTS.md LIB-01..08 + CFG-01..02 are amended per CONTEXT.md `<downstream_impact>`; LIB-05 + CFG-02 explicitly marked Removed; ROADMAP.md Phase 27/28/30 entries amended; milestone goal text updated.
  </done>
</task>

</tasks>

<verification>
- `pyproject.toml` has `mcp_config_file = "./config.test.yaml"` under `[tool.pytest.ini_options]`.
- `tests/contract/test_mcp_tool_contract.py` is DELETED.
- `tests/conftest.py` is hollowed (Option A docstring-only) or deleted (Option B).
- `src/mcp_test_framework/contracts/__init__.py` documents the ini route; no `register()` API; no public exports.
- `src/mcp_test_framework/fixtures.py:_LIVE_PREFIXES` no longer contains `tests/contract/`.
- `tests/framework/test_wheel_shape.py:test_wheel_source_has_no_homelab_mcp_imports` scope confirmed to include every `.py` in the wheel (including `_black_box_guard.py`).
- REQUIREMENTS.md LIB-05 + CFG-02 explicitly marked Removed; LIB-01..08 + CFG-01 rewritten per `<downstream_impact>`.
- ROADMAP.md Phase 27 title + Goal + SC1..5 amended; Phase 28 scope shrunk; Phase 30 CLOSE-01 amended; milestone goal text rephrased.
- `uv run pytest tests/framework/ -q -x` is GREEN.
- `uv run pytest --collect-only -q 2>&1 | head -40` shows clean collection (no `tests/contract/` items; `<mcp-contracts>` items only if `tools:` is populated in `config.test.yaml`).
</verification>

<success_criteria>
1. Framework dogfoods library mode end-to-end — every CI run exercises the plugin path via the ini value.
2. Legacy `tests/contract/` collection path is GONE; the framework's contract tests flow EXCLUSIVELY through the plugin.
3. `contracts/__init__.py` documents the ini route with zero public exports — operators don't import from here.
4. REQUIREMENTS.md + ROADMAP.md reflect the Phase 27 pivot — Phase 28 / 30 amended; LIB-05 + CFG-02 explicitly marked Removed; milestone goal text updated.
5. Wheel-introspection AST guard scope confirmed (or extended) to cover `_black_box_guard.py`.
</success_criteria>

<output>
After completion, create `.planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-05-SUMMARY.md` recording:
- Which option (A docstring-only vs B fully deleted) was chosen for `tests/conftest.py` and why.
- Whether `config.test.yaml` needed v2 schema migration before D-06 activation, and the final contents.
- The output of `uv run pytest --collect-only -q 2>&1 | head -40` after the move — confirming no `tests/contract/` items, plus zero / N `<mcp-contracts>` items depending on `tools:` population.
- Whether the wheel-introspection AST guard required extension or was already PACK-04-complete (per Pitfall 6).
- A list of every requirement-traceability cell flipped to `Complete` vs `Removed` so the orchestrator's phase-close verification can audit.
- Whether any framework self-tests had to be repaired (the Rule-1 deviation precedent — if Plan 27-02's `MCPTF_CONFIG_FILE` env-var removal in `cli.py` broke any framework self-test asserting on the env-var-write, record what was repaired).
</output>
