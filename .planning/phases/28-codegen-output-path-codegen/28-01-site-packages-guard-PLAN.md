---
phase: 28-codegen-output-path-codegen
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_test_framework/cli.py
  - tests/framework/unit/test_gen_test_classes_site_packages_guard.py
autonomous: true
requirements:
  - CODEGEN-LIB-02
must_haves:
  truths:
    - "D-07: Strict guard — resolve `out_root` absolute, compare against `Path(mcp_test_framework.__file__).parent.parent`, abort with operator-tone error if `out_root` is a descendant. No special-casing of `site-packages` / `dist-packages` directory names; uniform across editable installs, site-packages, vendored copies, Windows/Linux/venv/uv."
    - "D-08: Guard fires BEFORE the MCP handshake — at command start, immediately after `cfg` is loaded and `out_root` resolved. Server subprocess is never spawned if the target is unsafe."
    - "D-09: No bypass — no `allow_unsafe_generated_root` config knob, no `--unsafe` flag. Operators who hit the guard must change `generated_root` to a path outside the framework install root."
    - "D-10: Guard error is operator-tone per `docs/ERROR-STYLE.md`, names the offending field (`test_code.generated_root`), shows the resolved-absolute target path, names the detected framework install root, and tells the operator to set the field to a path inside their own project tree. Returncode 2."
  artifacts:
    - path: "src/mcp_test_framework/cli.py"
      provides: "`_guard_against_site_packages_target(out_root)` helper called from `gen_test_classes` BEFORE the MCP handshake."
      contains: "def _guard_against_site_packages_target("
    - path: "tests/framework/unit/test_gen_test_classes_site_packages_guard.py"
      provides: "Unit tests covering: (a) descendant-of-install-root aborts exit 2; (b) sibling-of-install-root passes; (c) error names the offending field + paths; (d) guard fires before any subprocess spawn."
      contains: "def test_guard_aborts_when_target_inside_framework_install_root"
  key_links:
    - from: "src/mcp_test_framework/cli.py::gen_test_classes"
      to: "src/mcp_test_framework/cli.py::_guard_against_site_packages_target"
      via: "direct call after `out_root` resolution, BEFORE `asyncio.Runner()` / `_run_codegen_handshake` invocation"
      pattern: "_guard_against_site_packages_target\\(out_root\\)"
---

<objective>
Add CODEGEN-LIB-02 pre-handshake site-packages guard to `gen-test-classes`. The guard resolves the configured `cfg.test_code.generated_root` to absolute, compares against the framework's detected install root (`Path(mcp_test_framework.__file__).parent.parent`), and aborts with an operator-tone error BEFORE attempting the MCP handshake if the target is a descendant of the framework install root. No bypass flag; per D-09.

Purpose: prevent `gen-test-classes` from writing generated Python under the framework's own install tree (site-packages, editable install root, vendored copy). Failure must be cheap and fast — server subprocess never starts.

Output: new `_guard_against_site_packages_target` helper in cli.py, wired into `gen_test_classes` at command start (after `out_root` resolution, before handshake). Unit test file covering descendant detection, sibling no-op, error tone, and pre-handshake timing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-codegen-output-path-codegen/28-CONTEXT.md
@docs/ERROR-STYLE.md
@src/mcp_test_framework/cli.py
@src/mcp_test_framework/models.py

<interfaces>
<!-- Existing operator-tone error helper Phase 28 reuses verbatim. -->
<!-- Located at src/mcp_test_framework/_runner.py and re-exported from cli.py. -->

From src/mcp_test_framework/cli.py (line ~97):
```python
from mcp_test_framework._runner import _emit_operator_error  # noqa: E402
```

`_emit_operator_error` signature (used everywhere in cli.py — copy the call shape):
```python
_emit_operator_error(
    summary="<one-line summary, lowercase per docs/ERROR-STYLE.md>",
    detail=[
        "<detail line 1>",
        "<detail line 2>",
    ],
    next_step="<imperative verb + instruction>",
)
# Raises typer.Exit(code=2). Never returns. Confirmed at cli.py:194-203 reference site.
```

`gen_test_classes` insertion site (cli.py:1079-1082 today):
```python
out_root = cfg.test_code.generated_root
if not out_root.is_absolute():
    out_root = Path.cwd() / out_root
```
The guard call goes IMMEDIATELY after this block and BEFORE the `try: with asyncio.Runner() as runner: ...` block on line 1083.

Framework install root resolution (the pattern this plan implements):
```python
import mcp_test_framework
framework_install_root = Path(mcp_test_framework.__file__).resolve().parent.parent
# For an editable install:    .../mvp_test_framework/src
# For a site-packages install: .../site-packages
# Either way, no path that contains the framework code should also contain
# operator-generated test code. Descendant check covers both.
```

`Path.is_relative_to(other)` (Python 3.9+) is the cross-platform descendant check used elsewhere in the framework.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add _guard_against_site_packages_target helper to cli.py with unit tests</name>
  <files>src/mcp_test_framework/cli.py, tests/framework/unit/test_gen_test_classes_site_packages_guard.py</files>
  <read_first>
    - src/mcp_test_framework/cli.py lines 1-100 (imports + _emit_operator_error re-export) — confirm import surface
    - src/mcp_test_framework/cli.py lines 990-1110 (gen_test_classes body) — locate the insertion point
    - docs/ERROR-STYLE.md (full file) — error tone rules (operator-terms-only, lowercase summary, no spec IDs, `next:` imperative verb)
    - .planning/phases/28-codegen-output-path-codegen/28-CONTEXT.md sections `<decisions>` D-07 / D-08 / D-09 / D-10 and `<canonical_refs>` "Operator-tone error references"
    - src/mcp_test_framework/cli.py lines 188-203 (_emit_operator_error call-site example for the missing-field-style error) — copy the shape
    - tests/framework/unit/test_gen_sdet_classes_cli.py lines 1-50 — CliRunner test pattern to mirror
  </read_first>
  <behavior>
    - Test 1: When `cfg.test_code.generated_root` resolves to a descendant of the framework's install root, `_guard_against_site_packages_target` raises `typer.Exit` with `code=2`. The captured stderr contains the literal substring `"test_code.generated_root"` AND the resolved-absolute target path AND the framework install root path.
    - Test 2: When `cfg.test_code.generated_root` resolves to a path that is NOT a descendant of the framework install root (e.g. tmp_path), the helper returns `None` without raising.
    - Test 3: When the guard is invoked via the full `gen_test_classes` command with a config whose `test_code.generated_root` points inside the framework install root, the command exits with code 2 AND no MCP server subprocess is spawned (assert by setting `mcp_server.command` to a sentinel-only-fail-on-spawn value and verifying that the `FileNotFoundError: MCP server command not on PATH` branch is NEVER reached — i.e. the guard fires first).
    - Test 4 (boundary): When `cfg.test_code.generated_root` points at a sibling-of-install-root path (e.g. install_root.parent / "tests_generated"), the helper returns None (descendant check only; siblings are allowed).
  </behavior>
  <action>
    Step 1 — Add helper to `src/mcp_test_framework/cli.py`. Insert the helper definition AFTER the existing `_emit_operator_error` re-export block (around line 100, before `_emit_operator_error_for_validation`):

```python
def _guard_against_site_packages_target(out_root: Path) -> None:
    """Pre-handshake site-packages guard (CODEGEN-LIB-02).

    Aborts `gen-test-classes` BEFORE the MCP handshake if the resolved
    target output directory is a descendant of the framework's own
    install root. Single robust check that works uniformly across:
    standard site-packages installs, editable installs (`pip install -e .`),
    vendored copies, Windows + Linux + venv + uv environments. No
    special-casing of `site-packages` / `dist-packages` directory names.

    No bypass flag and no config knob; operators who hit the guard must
    change `cfg.test_code.generated_root` to a path outside the
    framework's install root.

    Args:
        out_root: Fully resolved, absolute output root path (caller
            already converted relative paths against the chosen base).

    Raises:
        typer.Exit (via _emit_operator_error) with code 2 if out_root
        is a descendant of the framework install root.
    """
    import mcp_test_framework
    framework_install_root = Path(mcp_test_framework.__file__).resolve().parent.parent
    target_resolved = out_root.resolve()
    if target_resolved == framework_install_root or target_resolved.is_relative_to(framework_install_root):
        _emit_operator_error(
            summary="gen-test-classes: refusing to write inside the framework's install tree",
            detail=[
                f"the resolved target path `{target_resolved}` is inside the "
                f"framework's own install root `{framework_install_root}`.",
                "the framework refuses to generate operator test code under its "
                "own install tree -- generated files would be lost on the next "
                "package upgrade and would shadow framework modules during import.",
                "",
                f"the offending config field is `test_code.generated_root`.",
            ],
            next_step=(
                "set `test_code.generated_root` in your config.yaml to a path "
                "inside your own project tree (e.g. `tests/test_code/_generated`)"
            ),
        )
```

Step 2 — Wire the helper into `gen_test_classes` in `src/mcp_test_framework/cli.py`. Find the existing block (currently around line 1079-1082):

```python
    # `out_root` is config-driven; the framework never writes generated
    # Python code inside its own `src/` tree. Relative paths are
    # resolved against CWD (mirrors the MCPTF_CONFIG_FILE convention).
    out_root = cfg.test_code.generated_root
    if not out_root.is_absolute():
        out_root = Path.cwd() / out_root
```

This block currently sits AFTER the handshake (which is wrong for D-08 timing). Relocate the `out_root` resolution + guard call BEFORE the handshake. Replace the existing structure so the new ordering is:

```python
    # Resolve out_root BEFORE handshake so the site-packages guard
    # (D-07/D-08) can abort without starting an MCP subprocess.
    out_root = cfg.test_code.generated_root
    if not out_root.is_absolute():
        out_root = Path.cwd() / out_root
    _guard_against_site_packages_target(out_root)

    try:
        with asyncio.Runner() as runner:
            server_info, tools = runner.run(_run_codegen_handshake(cfg))
    except KeyboardInterrupt:
        # ... existing body unchanged
```

Remove the OLD post-handshake `out_root = cfg.test_code.generated_root / if not out_root.is_absolute() / out_root = Path.cwd() / out_root` block (currently around line 1079) since the resolution has been hoisted upward. The subsequent `_codegen.generate(..., out_root=out_root)` call (currently ~line 1088) keeps using the same `out_root` variable — only the assignment site moves.

Note on cwd-relative semantics: Plan 28-02 will revisit relative-path resolution once pyproject.toml-based config lookup lands (Discretion item per CONTEXT.md). For this plan, preserve cwd-relative resolution unchanged.

Step 3 — Create `tests/framework/unit/test_gen_test_classes_site_packages_guard.py`. Use CliRunner pattern from `tests/framework/unit/test_gen_sdet_classes_cli.py` for end-to-end tests, plus direct-call unit tests for the helper:

```python
"""Unit + CLI tests for the CODEGEN-LIB-02 pre-handshake site-packages guard."""
from __future__ import annotations

import textwrap
from pathlib import Path

import pytest
import typer
import yaml
from typer.testing import CliRunner

import mcp_test_framework
from mcp_test_framework.cli import _guard_against_site_packages_target, app


def _framework_install_root() -> Path:
    return Path(mcp_test_framework.__file__).resolve().parent.parent


def test_guard_aborts_when_target_inside_framework_install_root() -> None:
    install_root = _framework_install_root()
    inside = install_root / "tests_generated"
    with pytest.raises(typer.Exit) as exc_info:
        _guard_against_site_packages_target(inside)
    assert exc_info.value.exit_code == 2


def test_guard_passes_when_target_outside_framework_install_root(tmp_path: Path) -> None:
    # tmp_path is guaranteed outside the install root.
    result = _guard_against_site_packages_target(tmp_path / "_generated")
    assert result is None


def test_guard_passes_when_target_is_sibling_of_install_root() -> None:
    install_root = _framework_install_root()
    sibling = install_root.parent / "_sibling_generated"
    result = _guard_against_site_packages_target(sibling)
    assert result is None


def test_guard_error_message_names_field_and_paths(
    capsys: pytest.CaptureFixture[str],
) -> None:
    install_root = _framework_install_root()
    inside = install_root / "leak_target"
    with pytest.raises(typer.Exit):
        _guard_against_site_packages_target(inside)
    captured = capsys.readouterr()
    combined = captured.err + captured.out
    assert "test_code.generated_root" in combined
    assert str(install_root) in combined
    assert str(inside.resolve()) in combined


def _write_config_with_generated_root(tmp_path: Path, generated_root: Path) -> Path:
    cfg_path = tmp_path / "config.yaml"
    payload = {
        "version": 2,
        "mcp_server": {
            # Sentinel that would otherwise raise FileNotFoundError if the
            # handshake were attempted. The guard MUST fire first.
            "command": "this-command-never-exists-and-must-not-be-spawned",
            "args": [],
        },
        "test_code": {
            "generated_root": str(generated_root),
        },
        "tools": {},
    }
    cfg_path.write_text(yaml.safe_dump(payload), encoding="utf-8")
    return cfg_path


def test_gen_test_classes_cli_aborts_before_handshake_when_target_inside_install_root(
    tmp_path: Path,
) -> None:
    install_root = _framework_install_root()
    inside = install_root / "blocked_target"
    cfg_path = _write_config_with_generated_root(tmp_path, inside)
    runner = CliRunner()
    result = runner.invoke(app, ["gen-test-classes", "--config", str(cfg_path)])
    assert result.exit_code == 2
    combined = result.stdout + (result.stderr if hasattr(result, "stderr") else "")
    # Guard message present:
    assert "test_code.generated_root" in combined
    # MCP-server-not-on-PATH message absent (handshake never attempted):
    assert "MCP server command not found" not in combined
    assert "MCP server failed to start" not in combined
```

Step 4 — Run the tests to confirm GREEN:

```
uv run pytest tests/framework/unit/test_gen_test_classes_site_packages_guard.py -x -v
```

All five tests must pass. If any test fails, fix the helper or the wiring until GREEN.
  </action>
  <verify>
    <automated>uv run pytest tests/framework/unit/test_gen_test_classes_site_packages_guard.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - src/mcp_test_framework/cli.py contains the literal string `def _guard_against_site_packages_target(`
    - src/mcp_test_framework/cli.py contains the literal string `_guard_against_site_packages_target(out_root)` inside the `gen_test_classes` function body (use Grep with `multiline: true` if needed to confirm placement)
    - The line `_guard_against_site_packages_target(out_root)` appears BEFORE `with asyncio.Runner()` inside `gen_test_classes` (verify by reading lines around the gen_test_classes body)
    - tests/framework/unit/test_gen_test_classes_site_packages_guard.py exists with at least five `def test_` functions covering the four behaviors above plus the error-tone assertion
    - `uv run pytest tests/framework/unit/test_gen_test_classes_site_packages_guard.py -x -v` exits 0
    - The error message contains the literal substring `"test_code.generated_root"` (grep the test output or the helper source) AND mentions the resolved absolute path (verify in `test_guard_error_message_names_field_and_paths`)
    - The CLI test confirms NO subprocess spawn occurred (no `MCP server command not found` or `MCP server failed to start` text in the output)
    - `uv run pytest tests/framework/ -x` continues to pass (no regression in existing framework tests)
  </acceptance_criteria>
  <done>
    - Helper defined, wired into gen_test_classes BEFORE the handshake, tests pass GREEN, no regression in the rest of the framework test suite.
  </done>
</task>

</tasks>

<verification>
- `uv run pytest tests/framework/unit/test_gen_test_classes_site_packages_guard.py -x -v` exits 0
- `uv run pytest tests/framework/ -x` exits 0
- Grep confirms `_guard_against_site_packages_target(out_root)` call sits inside `gen_test_classes` BEFORE the `asyncio.Runner()` block
- No `--unsafe`, `--allow-site-packages`, or `allow_unsafe_generated_root` token introduced anywhere in src/ (D-09: no bypass)
</verification>

<success_criteria>
The site-packages guard fires at command start, before any MCP subprocess. Operator-tone error names `test_code.generated_root`, the resolved target, and the framework install root. No bypass exists. CODEGEN-LIB-02 satisfied with a unit-tested helper.
</success_criteria>

<output>
After completion, create `.planning/phases/28-codegen-output-path-codegen/28-01-SUMMARY.md` documenting the helper signature, the wiring point in `gen_test_classes`, and the test file path.
</output>
