---
phase: 28-codegen-output-path-codegen
plan: 01
subsystem: cli

tags: [typer, gen-test-classes, site-packages-guard, pre-handshake-safety]

# Dependency graph
requires:
  - phase: 27-register-api-contracts-sub-package-test-extraction-lib
    provides: "_load_config tuple-return + Config(yaml_file=...) explicit kwarg routing reused by gen-test-classes pre-handshake gate."
  - phase: 21-1-sdet-generated-output-relocation
    provides: "cfg.test_code.generated_root required-field semantics; cwd-relative resolution convention."
provides:
  - "Pre-handshake site-packages guard on gen-test-classes (rejects descendants of Path(mcp_test_framework.__file__).parent.parent before any MCP subprocess is spawned)."
  - "_guard_against_site_packages_target(out_root) helper exported from mcp_test_framework.cli."
  - "Hoisted out_root resolution above the asyncio.Runner() handshake in gen_test_classes."
affects:
  - "28-02 (pyproject ini route): out_root resolution semantics may switch from cwd-relative to pyproject-relative; guard call site stays unchanged."
  - "28-03 (overwrite prompt): runs immediately after the guard, against the same already-resolved out_root."
  - "28-04 (docs sweep): error copy + helper name should appear in operator-facing docs."

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Pre-handshake fail-fast safety check pattern: resolve config-derived target -> compare against framework install root -> abort BEFORE expensive subprocess setup."
    - "is_relative_to(framework_install_root) descendant check (Python 3.9+) for uniform site-packages / editable / vendored detection without directory-name special-casing."

key-files:
  created:
    - tests/framework/unit/test_gen_test_classes_site_packages_guard.py
  modified:
    - src/mcp_test_framework/cli.py

key-decisions:
  - "Helper inserted immediately AFTER the _emit_operator_error re-export block in cli.py (matches the 'operator-error helper cluster' grouping; keeps the guard discoverable next to its only error-emission dependency)."
  - "out_root resolution hoisted ABOVE the asyncio.Runner() block in gen_test_classes; the duplicate post-handshake resolution was deleted (single resolution site, same variable consumed by _codegen.generate)."
  - "Planning-ID literals (`CODEGEN-LIB-02`) removed from docstring + comment per the Phase 27 leak-gate precedent (`Plan 27-01 deviation: planning-ID gate takes precedence over plan's verbatim docstring instructions in src/`)."
  - "Cwd-relative resolution semantics preserved verbatim; 28-02 owns the pyproject-relative pivot."

patterns-established:
  - "gen-test-classes safety-check ordering: _load_config -> resolve out_root -> _guard_against_site_packages_target -> handshake. New safety checks added later (e.g. 28-03 non-empty-dir prompt) slot in AFTER the guard and BEFORE the handshake."

requirements-completed: [CODEGEN-LIB-02]

# Metrics
duration: ~8min
completed: 2026-05-17
---

# Phase 28 Plan 01: Site-packages Guard Summary

**Pre-handshake site-packages guard rejects gen-test-classes targets that descend from Path(mcp_test_framework.__file__).parent.parent (uniform across site-packages / editable / vendored installs) with operator-tone exit 2 before any MCP subprocess is spawned.**

## Performance

- **Duration:** ~8 min
- **Started:** 2026-05-17T04:08Z (approx)
- **Completed:** 2026-05-17T04:11:28Z
- **Tasks:** 1 (TDD)
- **Files modified:** 1 (cli.py)
- **Files created:** 1 (test_gen_test_classes_site_packages_guard.py)

## Accomplishments

- `_guard_against_site_packages_target(out_root)` helper added to `src/mcp_test_framework/cli.py` (line 100). Resolves target absolute, compares against `Path(mcp_test_framework.__file__).resolve().parent.parent`, aborts via `_emit_operator_error` (exit 2) on descendant match.
- `gen_test_classes` reordered: `out_root = cfg.test_code.generated_root` + relative-to-cwd resolution + guard call now run BEFORE `asyncio.Runner()` (line 1078-1081). The previous post-handshake duplicate block was removed.
- Error message names the offending config field (`test_code.generated_root`), the resolved-absolute target, the detected framework install root, and tells the operator to set `test_code.generated_root` to a path inside their own project tree — per D-10 + `docs/ERROR-STYLE.md`.
- No bypass surface introduced (no `--unsafe`, no `allow_unsafe_generated_root` config knob) — confirmed via grep on `src/`.
- 5/5 new unit + CLI tests pass; broader 607-test `tests/framework/` suite stays green.

## Task Commits

Each task was committed atomically (TDD RED -> GREEN):

1. **Task 1 RED: failing site-packages guard tests** - `229a851` (test)
2. **Task 1 GREEN: helper + gen_test_classes wiring** - `4a88ba9` (feat)

_Final plan-metadata commit (SUMMARY + STATE + ROADMAP) follows this file._

## Files Created/Modified

- `src/mcp_test_framework/cli.py` - Added `_guard_against_site_packages_target` helper (line 100); hoisted out_root resolution + guard call above the handshake in `gen_test_classes` (line 1078-1081); removed the duplicate post-handshake out_root resolution block.
- `tests/framework/unit/test_gen_test_classes_site_packages_guard.py` - 5 tests covering: (a) descendant abort exit 2; (b) tmp_path passes; (c) sibling-of-install-root passes; (d) error message names field + paths; (e) CLI aborts BEFORE handshake (asserts neither `MCP server command not found` nor `MCP server failed to start` text appears).

## Decisions Made

- **Helper placement (cluster with `_emit_operator_error`):** Inserted directly after the `_emit_operator_error` re-export at line 97 to keep all operator-error emission helpers in one block. Easier to find for the next implementer adding a related safety check.
- **`is_relative_to` over directory-name match (D-07 lock):** No `"site-packages"` / `"dist-packages"` string check anywhere in the helper — the descendant check covers all three install topologies uniformly (standard, editable, vendored) on both Windows + POSIX.
- **Cwd-relative semantics preserved (Discretion item locked):** 28-02 (pyproject ini route) owns the pivot from cwd-relative to pyproject-relative resolution. This plan keeps `Path.cwd() / out_root` verbatim.
- **`out_root` resolution moved, NOT duplicated:** Resolution lives at ONE site only (pre-handshake). The previous post-handshake block was deleted; the same `out_root` variable flows into `_codegen.generate()` further down.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Planning-ID leak gate failure in src/**
- **Found during:** Task 1 GREEN (regression run via `uv run pytest tests/framework/ -x`)
- **Issue:** The plan's verbatim helper docstring + comment included `(CODEGEN-LIB-02)` literals. `tests/framework/test_sdet_rename_leak_gate.py::test_no_residual_match_in_src_python_strings[planning_id-...]` flagged both occurrences (cli.py:101 and cli.py:1074) with a hard assertion. STATE.md `Plan 27-01 deviation` explicitly captures this precedent: "Planning-ID gate takes precedence over plan's verbatim docstring instructions in src/; scrub before commit (repeat of Plan 27-01 deviation)."
- **Fix:** Stripped the `CODEGEN-LIB-02` parenthetical from both the helper docstring and the wiring-site comment. Wording otherwise preserved. No `# noqa: sdet-rename-shim` tag used (the IDs are not legacy compat shims; they were authored fresh in this plan).
- **Files modified:** src/mcp_test_framework/cli.py (docstring line 101, comment line 1074)
- **Verification:** Full `uv run pytest tests/framework/ -x` then exited 0 (607 passed, 1 skipped, 17 deselected, 1 xfailed).
- **Committed in:** 4a88ba9 (rolled into the GREEN commit alongside the helper + wiring).

---

**Total deviations:** 1 auto-fixed (1 Rule 1 bug — planning-ID leak gate)
**Impact on plan:** Cosmetic only — helper semantics + error tone + acceptance criteria are unchanged. The plan's "literal docstring instruction" carried planning IDs that the project's own leak gate rejects; the scrub is the established convention for src/ per STATE.md Plan 27-01 deviation log.

## Issues Encountered

None beyond the deviation above. RED gate fired on ImportError (expected); GREEN gate passed on first run after helper + wiring landed.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- 28-02 (pyproject ini config route) unblocked: the guard call site is a stable anchor for the resolution-semantics swap (cwd-relative -> pyproject-relative). No change to the guard helper expected.
- 28-03 (overwrite prompt) unblocked: the prompt slots in AFTER the guard call and BEFORE the handshake; same `out_root` variable flows through.
- 28-04 (docs sweep): operator-facing docs that reference `gen-test-classes` should mention the guard's existence + the operator-tone error copy once the full Phase 28 surface is locked.
- No blockers carried into 28-02.

## Self-Check: PASSED

- FOUND: src/mcp_test_framework/cli.py (`_guard_against_site_packages_target` at line 100; call at line 1081 before `asyncio.Runner()` at line 1084)
- FOUND: tests/framework/unit/test_gen_test_classes_site_packages_guard.py (5 test functions; all PASS via `uv run pytest`)
- FOUND: commit 229a851 (test RED)
- FOUND: commit 4a88ba9 (feat GREEN)
- Verified: `uv run pytest tests/framework/` -> 607 passed, 1 skipped, 17 deselected, 1 xfailed
- Verified: no `--unsafe` / `allow_unsafe_generated_root` / `allow-site-packages` tokens introduced in `src/` (D-09 lock holds)

## TDD Gate Compliance

- RED gate: `229a851` — test commit precedes GREEN; ImportError on `_guard_against_site_packages_target` confirmed RED.
- GREEN gate: `4a88ba9` — feat commit follows RED; all 5 tests pass.
- REFACTOR gate: not required (helper landed in final shape on first GREEN; the planning-ID scrub was rolled into the GREEN commit as a Rule 1 bug fix, not a separate refactor).

---
*Phase: 28-codegen-output-path-codegen*
*Completed: 2026-05-17*
