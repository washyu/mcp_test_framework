---
phase: 05-cli-readme-acceptance
plan: 05
type: execute
wave: 5
depends_on: [05-04]
files_modified:
  - README.md
  - .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md
autonomous: false
requirements: [CLI-01, CLI-02, CLI-03, OPS-03, DOCS-01]
requirements_addressed: [CLI-01, CLI-02, CLI-03, OPS-03, DOCS-01]
tags: [acceptance, uat, cli, ops-03, sample-output]

must_haves:
  truths:
    - "From a clean checkout, `uv sync` then `uv run mcp-test-framework run` produces standard pytest terminal output and exits 0 against live homelab-mcp + Ollama (SC#6)"
    - "`uv run mcp-test-framework list-tools` against live homelab-mcp prints the registered tools in indented-block format (CLI-02 live verification)"
    - "`uv run mcp-test-framework list-tools --json | python -m json.tool` parses as valid JSON (CLI-02 --json live verification)"
    - "`uv run mcp-test-framework version` prints the version (CLI-03 live verification)"
    - "Ctrl+C during `uv run mcp-test-framework list-tools` exits with code 130 and `Get-Process homelab-mcp` returns no matches (OPS-03 manual UAT — D-teardown-2)"
    - "README's `## Sample green run` placeholder is replaced with a real captured 10-15 line excerpt from the Wave 5 green run (D-readme-3)"
  artifacts:
    - path: ".planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md"
      provides: "Verbatim capture of all 6 acceptance commands with exit codes and stdout/stderr excerpts"
      min_lines: 80
    - path: "README.md"
      provides: "README with the `<!-- TODO Plan 05 -->` placeholder replaced by a real captured pytest output excerpt"
      contains: "passed in"
  key_links:
    - from: "Acceptance walkthrough"
      to: "All 6 ROADMAP.md Phase 5 success criteria"
      via: "manual command execution + verbatim capture"
      pattern: "uv run mcp-test-framework"
    - from: "Plan 05 sample-output capture"
      to: "README.md `## Sample green run` section"
      via: "human paste of 10-15 lines from a real green run"
      pattern: "passed in"
---

<objective>
Walk through the 6 ROADMAP.md Phase 5 success criteria with verbatim command capture, including the manual OPS-03 UAT (Ctrl+C teardown of `list-tools` with `Get-Process homelab-mcp` confirmation). Replace the README's `<!-- TODO Plan 05 -->` placeholder with the real captured pytest output (10-15 lines) from this walkthrough.

Purpose: This is the close-out plan for Milestone v1.0. SC#6 ("from a clean clone, `uv sync` then `uv run mcp-test-framework run` produces standard pytest terminal output and exits 0") cannot be verified by automated test alone — it requires live homelab-mcp + Ollama, a Windows machine for the OPS-03 UAT, and a human to send SIGINT and check process tables. This plan stitches all of those together into one capture artifact.

Output:
- `.planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md`: a checklist artifact with verbatim stdout/stderr capture for each of the 6 SCs.
- `README.md` updated: the `<!-- TODO Plan 05 -->` placeholder replaced by a 10-15 line excerpt from the live green run captured in this plan.
- A signed-off acceptance trail that `/gsd-verify-work` can consume directly.

This plan has `autonomous: false` because Tasks 1 and 3 contain `checkpoint:human-verify` and `checkpoint:human-action` steps respectively (Ctrl+C and `Get-Process homelab-mcp` cannot be automated without cross-platform process-enumeration scaffolding deferred per CONTEXT.md `<deferred>`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-cli-readme-acceptance/05-CONTEXT.md
@.planning/phases/05-cli-readme-acceptance/05-PATTERNS.md
@.planning/phases/05-cli-readme-acceptance/05-01-PLAN.md
@.planning/phases/05-cli-readme-acceptance/05-02-PLAN.md
@.planning/phases/05-cli-readme-acceptance/05-03-PLAN.md
@.planning/phases/05-cli-readme-acceptance/05-04-PLAN.md
@CLAUDE.md
@README.md
@src/mcp_test_framework/cli.py
@.planning/phases/04.1-mcp-client-teardown-fix/04.1-01-SUMMARY.md
</context>

<interfaces>
<!-- Reference contracts for the acceptance walkthrough. -->

The 6 ROADMAP.md Phase 5 success criteria (this plan walks through each):

| SC# | Criterion | How verified in this plan |
|-----|-----------|---------------------------|
| SC#1 | `mcp-test-framework run` resolves config, invokes pytest.main(), exits with pytest's exit code (CLI-01) | Task 1 step 1 (live green run capture) |
| SC#2 | `mcp-test-framework list-tools` connects via stdio + prints tools (CLI-02) | Task 1 step 2 (live list-tools text + --json) |
| SC#3 | `mcp-test-framework version` prints the package version (CLI-03) | Task 1 step 3 |
| SC#4 | KeyboardInterrupt at the CLI level cleanly tears down the MCP subprocess (OPS-03) | Task 3 (manual Ctrl+C UAT + Get-Process check) |
| SC#5 | README explains setup, configuration, run instructions, Windows troubleshooting (DOCS-01) | Task 1 step 4 (README structure check; Plan 04 already verified content) |
| SC#6 | Clean-clone walkthrough: uv sync + run produces standard pytest output and exits 0 | Task 1 step 1 + step 5 (clean-clone simulation via fresh tmp dir) |

Phase 04.1 contract (load-bearing for OPS-03):
- `tests/smoke/test_smoke_homelab_mcp.py` regression smoke (added in 04.1-01) confirms full mcp_client lifecycle clean teardown for the FIXTURE path.
- Plan 03's `list-tools` reuses the same asyncio.Runner + AsyncExitStack pattern at the CLI surface.
- D-teardown-2: OPS-03 verified by manual UAT in this plan (NOT by automated cross-platform process-enum test).

Sample-capture target for README (D-readme-3):
- 10-15 lines from `uv run mcp-test-framework run` against live homelab-mcp + Ollama
- Should include the test-session header, pass markers (10 PASSED expected after Phase 4), and the summary line
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Acceptance walkthrough (5 of 6 success criteria) — live green run + capture sample</name>
  <files>
    - .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md
    - README.md
  </files>
  <read_first>
    - .planning/ROADMAP.md `### Phase 5: CLI, README & Acceptance` Success Criteria block (the 6 SCs this plan walks through)
    - .planning/phases/04.1-mcp-client-teardown-fix/04.1-01-SUMMARY.md (verified state: "EXIT_CODE=0, 10 passed in 14.95s, no leftover homelab-mcp.exe")
    - README.md (current state after Plan 04 — the `<!-- TODO Plan 05 -->` placeholder is what gets replaced)
    - src/mcp_test_framework/cli.py (current state after Plans 01-03)
  </read_first>
  <what-built>
    Plans 01-04 shipped:
    - `src/mcp_test_framework/cli.py` with three commands (`run`, `list-tools`, `version`), `_load_config` helper, and three formatting helpers
    - `pyproject.toml` `[project.scripts]` entry uncommented
    - `README.md` with quickstart content, env-var table, troubleshooting, and a `<!-- TODO Plan 05 -->` placeholder for the sample-green-run section
    - `docs/EXTENDING.md` with two extension recipes
    - Sync-check artifact confirming `.env.example` ↔ README parity

    This task drives the live walkthrough: ensure homelab-mcp and Ollama are reachable, run all 5 non-OPS-03 SCs end-to-end, capture the output, paste a 10-15 line excerpt into the README's sample-output section.
  </what-built>
  <action>
    Open a PowerShell terminal in the repo root. Run the following sequence and capture verbatim into `.planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md`. Use a fenced code block per command with the command line, the captured stdout/stderr, and the exit code (`echo "exit=$LASTEXITCODE"` after each command in PowerShell, or `echo "exit=$?"` in bash).

    **Pre-flight:** Confirm Ollama is reachable and the model is loaded.
    ```powershell
    curl -s http://127.0.0.1:11434/api/tags | findstr qwen3.6
    # expect a JSON line containing "qwen3.6:latest" -- if missing, ollama pull first
    ```

    Confirm `uvx homelab-mcp` is invocable (the default in `.env.example`):
    ```powershell
    uvx homelab-mcp --help
    # expect MCP server help output; if "command not found", install uv first
    ```

    **Step 1 — SC#1 + SC#6 (clean-clone walkthrough + green pytest run):**

    Simulate a clean clone: in a fresh tmp dir, clone the repo (or `git worktree add`), `uv sync`, then run. If `git clone` of the local repo is impractical, run from the working tree but ensure `uv sync` is freshly executed.

    ```powershell
    cd <home>\projects\mvp_test_framework
    uv sync
    echo "uv-sync-exit=$LASTEXITCODE"
    uv run mcp-test-framework run 2>&1 | Tee-Object -FilePath "$env:TEMP\\plan05-run-output.txt"
    echo "run-exit=$LASTEXITCODE"
    ```

    Expected:
    - `uv-sync-exit=0`
    - `run-exit=0`
    - stdout contains pytest's standard output: test session header, pass markers, summary line like `============= 10 passed in X.XXs =============`

    Capture the full output verbatim into the walkthrough doc under SC#1/SC#6.

    **Step 1b — Capture sample-output excerpt for README:**

    From `$env:TEMP\plan05-run-output.txt`, extract a 10-15 line excerpt that includes the session header line (`============================= test session starts =============================`), a few PASSED markers (or the dot-progress line), and the final summary (`========= N passed in X.Xs =========`). Paste this excerpt into `README.md` under the `## Sample green run` section, REPLACING the placeholder block (the `<!-- TODO Plan 05 -->` HTML comment + the synthetic `...` lines).

    Required README edit (in `## Sample green run`):
    - Remove the `<!-- TODO Plan 05: replace this block ... -->` HTML comment line
    - Remove the synthetic `============================= test session starts ==============================`/`...`/`============== 10 passed in X.XXs ===============` placeholder block
    - Insert the captured 10-15 line excerpt inside a fenced code block (use ```text language hint)
    - Keep the trailing one-sentence "Captured from a real green run; replace if your output drifts." caption

    **Step 2 — SC#2 (list-tools text + --json):**

    ```powershell
    uv run mcp-test-framework list-tools 2>&1 | Tee-Object -FilePath "$env:TEMP\\plan05-list-tools-text.txt"
    echo "list-tools-text-exit=$LASTEXITCODE"
    uv run mcp-test-framework list-tools --json 2>&1 | Tee-Object -FilePath "$env:TEMP\\plan05-list-tools-json.txt"
    echo "list-tools-json-exit=$LASTEXITCODE"
    Get-Content "$env:TEMP\\plan05-list-tools-json.txt" | ConvertFrom-Json | Select-Object -First 1
    ```

    Expected:
    - Both exits = 0
    - Text output: at least one tool block (e.g., `list_registered_servers` followed by indented description); alphabetically sorted if more than one tool
    - JSON output: parses via `ConvertFrom-Json` without error; first element has `name`, `description`, `inputSchema`, `outputSchema` keys

    Capture both outputs verbatim into the walkthrough doc under SC#2.

    **Step 3 — SC#3 (version):**

    ```powershell
    uv run mcp-test-framework version
    echo "version-exit=$LASTEXITCODE"
    ```

    Expected: `version-exit=0`, stdout matches `^[0-9]+\.[0-9]+\.[0-9]+$` (currently `0.1.0`).

    Capture into walkthrough doc under SC#3.

    **Step 4 — SC#5 (README structure spot-check):**

    Plan 04 already verified the README content via grep and the sync-check artifact. This step is a final eyeball: open `README.md` and confirm the captured sample (from Step 1b) renders cleanly under `## Sample green run`. Capture into walkthrough doc as a one-line acknowledgment ("README sample-output section updated; structure preserved from Plan 04").

    **Step 5 — Walkthrough artifact structure:**

    `.planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md` MUST contain (in order):
    - H1: `# Phase 5 Acceptance Walkthrough`
    - Frontmatter or header line with timestamp + executor (you / human verifier)
    - One H2 per SC (`## SC#1: ...`, `## SC#2: ...`, etc.) with the verbatim command + stdout/stderr capture + exit code
    - `## Pre-flight` section above SC#1 with the Ollama-reachability and uvx-homelab-mcp checks
    - `## OPS-03 (SC#4)` section RESERVED but empty — Task 3 fills it in

    Once Steps 1-5 pass, mark this checkpoint complete with `approved` and proceed to Task 2.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md && grep -q "run-exit=0" .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md && grep -q "version-exit=0" .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md && grep -q "passed in" README.md && ! grep -q "TODO Plan 05" README.md</automated>
  </verify>
  <done>
    Walkthrough artifact has captures for SC#1, SC#2, SC#3, SC#5 with verbatim exit codes. README's `## Sample green run` placeholder is replaced with a real 10-15 line excerpt from the live green run. The `## OPS-03 (SC#4)` section exists in the walkthrough doc as a reserved placeholder for Task 3 to fill in.
  </done>
  <acceptance_criteria>
    - File `.planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md` exists with `# Phase 5 Acceptance Walkthrough` H1
    - The walkthrough doc contains H2 sections for SC#1, SC#2, SC#3, SC#5 (and a placeholder/empty H2 for SC#4 / OPS-03)
    - Each SC section captures the command, the stdout/stderr, and the exit code verbatim
    - SC#1 / SC#6 capture shows `run-exit=0` and a final pytest summary line containing `passed in`
    - SC#2 capture shows both text and JSON outputs with `list-tools-text-exit=0` and `list-tools-json-exit=0`; JSON output parses via ConvertFrom-Json (`Select-Object -First 1` shows a tool record with at least `name` and `description` keys)
    - SC#3 capture shows `version-exit=0` and stdout matching `^[0-9]+\.[0-9]+\.[0-9]+$`
    - `README.md` `## Sample green run` section now contains an excerpt with the substring `passed in` (real captured output, NOT the placeholder)
    - `README.md` no longer contains the `<!-- TODO Plan 05` substring
    - Grep: `grep -c "passed in" README.md` returns at least `1`
    - Grep: `grep -c "TODO Plan 05" README.md` returns `0`
  </acceptance_criteria>
  <resume-signal>Type "approved" once SC#1, SC#2, SC#3, SC#5 capture is complete and the README sample-output is updated. Or describe any failure and the executor will iterate.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Confirm acceptance walkthrough captures all five non-OPS-03 SCs</name>
  <files>(no files modified — verification-only)</files>
  <read_first>
    - .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md (output of Task 1)
    - README.md (post-Task 1 state with real captured sample)
  </read_first>
  <action>
    Run automated grep checks on the walkthrough artifact and the README to confirm Task 1's captures landed correctly.

    ```bash
    test -f .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md
    # Each SC heading present
    for SC in "SC#1" "SC#2" "SC#3" "SC#5"; do
      grep -q "## .*$SC" .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md || echo "MISSING: $SC heading"
    done
    # Exit-code captures present
    grep -q "run-exit=0" .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md || echo "MISSING: run-exit=0"
    grep -q "list-tools-text-exit=0" .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md || echo "MISSING: list-tools-text-exit=0"
    grep -q "list-tools-json-exit=0" .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md || echo "MISSING: list-tools-json-exit=0"
    grep -q "version-exit=0" .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md || echo "MISSING: version-exit=0"
    # README sample updated
    grep -q "TODO Plan 05" README.md && echo "FAIL: TODO Plan 05 marker still present" || echo "OK: README sample updated"
    grep -q "passed in" README.md || echo "MISSING: real pytest summary in README sample"
    echo "Task 2 checks complete"
    ```

    If any `MISSING:` or `FAIL:` line appears, return to Task 1 (the human-verify checkpoint) and re-run the failing step. The executor surfaces the failure to the user; the user re-runs the live capture and edits the artifact.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md && grep -q "run-exit=0" .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md && grep -q "version-exit=0" .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md && grep -q "passed in" README.md && ! grep -q "TODO Plan 05" README.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md` exists
    - Walkthrough contains an H2 heading mentioning SC#1
    - Walkthrough contains an H2 heading mentioning SC#2
    - Walkthrough contains an H2 heading mentioning SC#3
    - Walkthrough contains an H2 heading mentioning SC#5
    - Walkthrough contains the verbatim string `run-exit=0`
    - Walkthrough contains the verbatim string `list-tools-text-exit=0`
    - Walkthrough contains the verbatim string `list-tools-json-exit=0`
    - Walkthrough contains the verbatim string `version-exit=0`
    - `README.md` no longer contains `TODO Plan 05`
    - `README.md` contains the substring `passed in` (real pytest summary)
  </acceptance_criteria>
  <done>
    Five of six SCs are verified and captured. The walkthrough artifact has placeholders for SC#4 / OPS-03 awaiting Task 3.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: OPS-03 manual UAT — Ctrl+C `list-tools` + Get-Process homelab-mcp confirmation</name>
  <files>
    - .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md
  </files>
  <read_first>
    - .planning/phases/05-cli-readme-acceptance/05-CONTEXT.md `<decisions>` D-teardown-1, D-teardown-2, D-teardown-3
    - .planning/research/PITFALLS.md Pitfall 4 (Windows ProactorEventLoop)
    - .planning/phases/04.1-mcp-client-teardown-fix/04.1-01-SUMMARY.md (verified state: "no leftover homelab-mcp.exe" for the FIXTURE path; this UAT proves the same for the CLI path)
  </read_first>
  <what-built>
    The CLI's `list-tools` command (Plan 03) uses asyncio.Runner + AsyncExitStack to own the `McpTestClient` lifecycle in a single task. KeyboardInterrupt should propagate through the runner, `__aexit__` runs in the same task, `stdio_client._terminate_process_tree` kills the homelab-mcp subprocess, and Python exits 130. D-teardown-3: no message printed.

    This is the OPS-03 contract: no zombie `homelab-mcp.exe` after Ctrl+C. Phase 04.1 hardened the same pattern for the test path; Plan 05 verifies it at the CLI surface.
  </what-built>
  <action>
    This task requires manual interaction in two PowerShell terminals on the Windows 11 dev machine. Automated SIGINT + cross-platform process enumeration is deferred per CONTEXT.md `<deferred>` ("Automated regression test for OPS-03"). Capture the verbatim sequence into `.planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md` under the `## OPS-03 (SC#4)` section reserved by Task 1.

    **Pre-condition:** Ensure no `homelab-mcp.exe` is currently running:
    ```powershell
    Get-Process homelab-mcp -ErrorAction SilentlyContinue
    # expect: empty output (no rows). If any rows appear, kill them: Stop-Process -Name homelab-mcp -Force
    ```

    Capture the pre-condition output (should be empty).

    **Terminal A — invoke `list-tools` and Ctrl+C mid-call:**

    To create a reliable interrupt window, the MCP server takes ~1-3 seconds to initialize. Hit Ctrl+C as soon as you see the command spawn the subprocess (or before it returns the first tool block). If the call completes too fast, restart with a slower MCP server (uvx cold-start path is naturally slower than a globally-installed binary):
    ```powershell
    # Make sure MCP_SERVER_COMMAND uses uvx (cold-start makes the interrupt window wider)
    $env:MCP_SERVER_COMMAND = "uvx"
    $env:MCP_SERVER_ARGS = '["homelab-mcp"]'
    uv run mcp-test-framework list-tools
    # ...as soon as the command spawns the subprocess, hit Ctrl+C...
    echo "list-tools-sigint-exit=$LASTEXITCODE"
    # expected: 130 (Python's standard SIGINT exit code) or -1073741510 on Windows
    ```

    Expected:
    - Exit code is non-zero AND specifically NOT pytest's success code (0)
    - On Windows PowerShell, `$LASTEXITCODE` after Ctrl+C is typically `-1073741510` (`0xC000013A`, Windows STATUS_CONTROL_C_EXIT) — this is the Windows equivalent of POSIX 130 and is CORRECT
    - No exception traceback printed (D-teardown-3: silent exit). A bare KeyboardInterrupt traceback IS acceptable since CPython prints `KeyboardInterrupt` by default; what we forbid is a try/except-printed message like "Interrupted before tools could be listed."

    **Terminal B (or same terminal after Ctrl+C) — confirm no zombie subprocess:**

    Immediately after the Ctrl+C, check for leftover homelab-mcp processes:
    ```powershell
    Get-Process homelab-mcp -ErrorAction SilentlyContinue
    echo "get-process-exit=$LASTEXITCODE"
    # expected: empty output, get-process-exit=0
    ```

    Also check for any `python.exe` instances spawned by uvx that might be hosting homelab-mcp:
    ```powershell
    Get-Process python -ErrorAction SilentlyContinue | Where-Object { $_.MainWindowTitle -match "homelab" -or $_.CommandLine -match "homelab-mcp" }
    # expected: empty output
    ```

    **Repeat the test 3 times** to confirm reproducibility (the cancel-scope teardown bug from Phase 04.1 was intermittent; multiple attempts catch flakiness).

    **Capture into the walkthrough artifact** under `## OPS-03 (SC#4)`:
    - The pre-condition empty `Get-Process` output
    - For each of the 3 attempts: the `list-tools-sigint-exit` value and the post-Ctrl+C `Get-Process homelab-mcp` output (must be empty)
    - A summary line: `OPS-03: 3/3 attempts passed -- no zombie homelab-mcp.exe after Ctrl+C`

    If any attempt leaves a zombie:
    - Capture the zombie-process row in the walkthrough
    - Mark OPS-03 as FAILING in the artifact
    - Stop and route back to Plan 03 for investigation (the cancel-scope discipline must have regressed)

    **D-teardown-3 sanity check:** The walkthrough should NOT contain any custom CLI-printed message about being interrupted. CPython's default `KeyboardInterrupt` traceback is acceptable; a hand-rolled "Interrupted before tools could be listed" line is a violation.

    Once 3/3 attempts pass, mark this checkpoint complete and proceed to Task 4.
  </action>
  <verify>
    <automated>grep -c "OPS-03: 3/3 attempts passed" .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md</automated>
  </verify>
  <done>
    OPS-03 verified at the CLI surface: 3/3 manual Ctrl+C attempts on `uv run mcp-test-framework list-tools` produce exit code 130 (or Windows STATUS_CONTROL_C_EXIT) AND `Get-Process homelab-mcp` returns no rows. The walkthrough doc captures all three attempts verbatim. No custom "Interrupted" message printed (D-teardown-3 honored).
  </done>
  <acceptance_criteria>
    - Walkthrough doc has an H2 section `## OPS-03 (SC#4)` (or similar — must mention SC#4 or OPS-03)
    - Walkthrough captures the pre-condition `Get-Process homelab-mcp` empty output
    - Walkthrough captures 3 separate Ctrl+C attempts with the exit code (130 or -1073741510 on Windows)
    - Walkthrough captures 3 separate post-Ctrl+C `Get-Process homelab-mcp` outputs, all empty
    - Walkthrough contains the line `OPS-03: 3/3 attempts passed -- no zombie homelab-mcp.exe after Ctrl+C`
    - Walkthrough does NOT contain any captured "Interrupted before tools could be listed" message (would violate D-teardown-3)
  </acceptance_criteria>
  <resume-signal>Type "approved" once 3/3 OPS-03 attempts pass with no zombie subprocesses. If any attempt fails, describe the failure (zombie process row, unexpected message printed, etc.) and revert to Plan 03 for fix.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Final acceptance summary — all 6 SCs verified</name>
  <files>
    - .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md
  </files>
  <read_first>
    - .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md (output of Tasks 1 + 3)
    - .planning/ROADMAP.md (Phase 5 success criteria block)
  </read_first>
  <action>
    Append a final summary table to `.planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md` confirming all 6 SCs verified.

    Append (DO NOT replace existing content):
    ```markdown

    ---

    ## Final acceptance summary

    | SC# | Criterion | Status | Captured in section |
    |-----|-----------|--------|---------------------|
    | SC#1 | `run` invokes pytest.main() and exits with pytest's exit code (CLI-01) | PASS | `## SC#1` |
    | SC#2 | `list-tools` connects via stdio + prints tools (CLI-02) | PASS | `## SC#2` |
    | SC#3 | `version` prints the package version (CLI-03) | PASS | `## SC#3` |
    | SC#4 | KeyboardInterrupt cleanly tears down MCP subprocess (OPS-03) | PASS (3/3 attempts) | `## OPS-03 (SC#4)` |
    | SC#5 | README documents setup, configuration, run, troubleshooting (DOCS-01) | PASS | `## SC#5` |
    | SC#6 | Clean-clone walkthrough produces standard pytest output and exits 0 | PASS | `## SC#1` (combined) |

    **Milestone v1.0 acceptance:** all 6 Phase 5 success criteria captured and verified live. Ready for `/gsd-verify-work`.

    Captured: <YYYY-MM-DD> by <executor name>
    ```

    Replace `<YYYY-MM-DD>` with the current date (2026-05-06) and `<executor name>` with the human verifier's identifier (e.g., "washyu" or "Claude Code execute-phase").
  </action>
  <verify>
    <automated>grep -c "Final acceptance summary" .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md && grep -c "Milestone v1.0 acceptance" .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md</automated>
  </verify>
  <acceptance_criteria>
    - Walkthrough doc contains the H2 `## Final acceptance summary`
    - Walkthrough doc contains a table row with `SC#1` and `PASS`
    - Walkthrough doc contains a table row with `SC#2` and `PASS`
    - Walkthrough doc contains a table row with `SC#3` and `PASS`
    - Walkthrough doc contains a table row with `SC#4` and `PASS (3/3 attempts)`
    - Walkthrough doc contains a table row with `SC#5` and `PASS`
    - Walkthrough doc contains a table row with `SC#6` and `PASS`
    - Walkthrough doc contains the line `Milestone v1.0 acceptance: all 6 Phase 5 success criteria captured and verified live. Ready for /gsd-verify-work.`
  </acceptance_criteria>
  <done>
    The acceptance walkthrough artifact is complete with all 6 SCs verified. The artifact is the input `/gsd-verify-work` will consume to flip Phase 5 status to `passed` and close Milestone v1.0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live homelab-mcp subprocess -> CLI | Already exercised in prior phases; Plan 05 only re-verifies at the CLI surface |
| Live Ollama HTTP endpoint -> CLI | Plan 05 SC#1 path only; httpx.Timeout already in place from Phase 3 |
| Human Ctrl+C -> Python interpreter | The OPS-03 UAT relies on the OS delivering SIGINT to the foreground process; standard Windows behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Repudiation | Live UAT capture authenticity | mitigate | The walkthrough doc includes verbatim stdout/stderr captures with exit codes; `/gsd-verify-work` cross-references against the actual exit codes the CI would produce. The capture is signed off by the human verifier in the final summary block |
| T-05-05-02 | Denial of Service | Live homelab-mcp subprocess hang | mitigate | McpTestClient.list_tools() wraps session.list_tools() in asyncio.timeout(30s default). A hanging subprocess raises TimeoutError; the OPS-03 UAT covers the SIGINT-mid-call case explicitly |
| T-05-05-03 | OPS-03 (Pitfall 1 regression) | asyncio.Runner + AsyncExitStack lifecycle | mitigate | 3/3 manual UAT attempts confirm no zombie subprocess. If any attempt fails, the cancel-scope discipline has regressed; Plan 03's body is the single point of failure to fix |
| T-05-05-04 | Information Disclosure | Captured stdout in walkthrough doc | accept | The walkthrough captures real tool descriptions and possibly test output; this is local-dev-machine output and the doc lives in `.planning/` (private to the repo). If the repo is open-sourced, scrub homelab-specific details before publishing |
</threat_model>

<verification>
After all tasks complete:

```bash
# Walkthrough doc exists with all 6 SCs verified
test -f .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md
grep -q "## Final acceptance summary" .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md
grep -q "Milestone v1.0 acceptance" .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md

# README sample-output is real, not placeholder
grep -q "passed in" README.md
! grep -q "TODO Plan 05" README.md

# OPS-03 captured 3/3 attempts
grep -q "3/3 attempts passed" .planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md
```
</verification>

<success_criteria>
- All 6 ROADMAP.md Phase 5 success criteria have verbatim capture in `.planning/phases/05-cli-readme-acceptance/05-05-ACCEPTANCE-WALKTHROUGH.md`
- README's `## Sample green run` placeholder is replaced with a real captured pytest output excerpt
- OPS-03 UAT confirms 3/3 Ctrl+C attempts produce no zombie `homelab-mcp.exe`
- Final acceptance summary table marks all 6 SCs as PASS
- `/gsd-verify-work` can consume the walkthrough doc directly to flip Phase 5 status to `passed`
- Milestone v1.0 ready for close-out
</success_criteria>

<output>
After completion, create `.planning/phases/05-cli-readme-acceptance/05-05-SUMMARY.md` per the standard summary template, including a reference to the walkthrough artifact and a one-line confirmation of the OPS-03 outcome (3/3 attempts passed).
</output>
</content>
