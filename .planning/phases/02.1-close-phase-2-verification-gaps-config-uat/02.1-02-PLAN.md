---
phase: 02.1-close-phase-2-verification-gaps-config-uat
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - config.example.yaml
  - pyproject.toml
  - .gitignore
  - docs/mcp_test_framework_mvp_spec.md
autonomous: true
requirements:
  - CORE-03
must_haves:
  truths:
    - "D-01: uvx homelab-mcp is the documented canonical invocation; direct-binary remains a valid alternative"
    - ".env.example documents MCP_SERVER_COMMAND=uvx with MCP_SERVER_ARGS=[\"homelab-mcp\"] as the default invocation"
    - "config.example.yaml documents mcp_server.command: uvx with args: [homelab-mcp] as the default invocation"
    - "pyproject.toml live_homelab marker description matches the uvx-or-PATH wording"
    - "config.yaml is gitignored (git check-ignore -v config.yaml exits 0 with a matching .gitignore line)"
    - "config.example.yaml is NOT gitignored (gitignore pattern doesn't accidentally match it)"
    - "docs/mcp_test_framework_mvp_spec.md is tracked in git (git ls-files lists it)"
    - "Both example files carry an inline comment explaining how to switch to a globally-installed homelab-mcp binary"
  artifacts:
    - path: ".env.example"
      provides: "Reconciled env-var example matching the working uvx invocation (D-02)"
      contains: "MCP_SERVER_COMMAND=uvx"
    - path: "config.example.yaml"
      provides: "Reconciled YAML example matching the working uvx invocation (D-02)"
      contains: "command: uvx"
    - path: "pyproject.toml"
      provides: "live_homelab marker with corrected description"
      contains: "uvx (or on PATH)"
    - path: ".gitignore"
      provides: "config.yaml ignore rule (D-09)"
      contains: "config.yaml"
    - path: "docs/mcp_test_framework_mvp_spec.md"
      provides: "Canonical spec committed to git (D-10)"
  key_links:
    - from: ".env.example"
      to: "config.example.yaml"
      via: "both reflect the same canonical uvx invocation -- a developer reading either gets the same story"
      pattern: "uvx"
    - from: "pyproject.toml live_homelab marker description"
      to: ".env.example MCP_SERVER_COMMAND comment"
      via: "marker description references uvx, examples show uvx -- consistent narrative"
      pattern: "uvx"
    - from: ".gitignore"
      to: "config.yaml in working tree"
      via: "git check-ignore matches the new .gitignore line"
      pattern: "^config\\.yaml$"
---

<objective>
Reconcile example-config files and `pyproject.toml` marker description to the actually-working `uvx homelab-mcp` invocation per Phase 02.1 CONTEXT.md D-01/D-02; gitignore `config.yaml` per D-09; commit the canonical spec at `docs/mcp_test_framework_mvp_spec.md` per D-10. Pure text-edit work; no code changes; no test changes.

Purpose: Today a developer who runs `cp .env.example .env && uv run pytest -m live_homelab tests/smoke/` gets `FileNotFoundError("MCP server command not on PATH: 'homelab-mcp'")` because the example points at a binary that may not be installed. The working dev-machine config uses `uvx homelab-mcp` (zero-install); this plan documents that as the canonical default. It also hardens the repo against two footguns: an accidental `git add .` committing the developer's machine-specific `config.yaml`, and the canonical spec at `docs/mcp_test_framework_mvp_spec.md` (cited as source-of-truth by every CONTEXT.md / ROADMAP.md / CLAUDE.md) being absent from the repo.

Output: 4 modified files (.env.example, config.example.yaml, pyproject.toml, .gitignore) + 1 newly-tracked file (docs/mcp_test_framework_mvp_spec.md). Independent of Plan 02.1-01 -- runs in parallel in Wave 1.
</objective>

<execution_context>
@<home>/.claude/get-shit-done/workflows/execute-plan.md
@<home>/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-CONTEXT.md
@.planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-RESEARCH.md
@.env.example
@config.example.yaml
@pyproject.toml
@.gitignore
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile example config files and marker description (D-02)</name>
  <files>.env.example, config.example.yaml, pyproject.toml</files>
  <read_first>
    - .env.example (current 22 lines)
    - config.example.yaml (current 17 lines)
    - pyproject.toml (current 67 lines -- only the `[tool.pytest.ini_options]` markers list at line 37-39 changes)
    - .planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-CONTEXT.md §Implementation Decisions D-02 (lines 34-37 -- exact target strings)
    - .planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-RESEARCH.md §Code Examples (lines 365-389 -- diff snippets per file)
    - .planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-RESEARCH.md §Anti-Patterns to Avoid (lines 244-249) -- DO NOT add `MCPTF_CONFIG_FILE` to a committed file
  </read_first>
  <action>
    Make these three text edits. Each is fully specified -- no interpretation.

    **Edit 1 -- `.env.example`:**
    The current MCP block is:
    ```
    MCP_SERVER_COMMAND=homelab-mcp
    MCP_SERVER_ARGS=
    MCP_SERVER_TIMEOUT_SECONDS=30
    ```
    Replace the first two lines with this 4-line block (a 2-line comment + 2 reconciled value lines):
    ```
    # Default invocation uses uvx for zero-install. To use a globally-installed
    # binary, set MCP_SERVER_COMMAND=homelab-mcp and MCP_SERVER_ARGS=[].
    MCP_SERVER_COMMAND=uvx
    MCP_SERVER_ARGS=["homelab-mcp"]
    ```
    The `MCP_SERVER_TIMEOUT_SECONDS=30` line below it stays unchanged. The OLLAMA_*, TARGET_TOOL_NAME, JUDGE_TIMEOUT_SECONDS, and MCPTF_CONFIG_FILE-comment sections at top and bottom of the file stay byte-for-byte unchanged.

    **Edit 2 -- `config.example.yaml`:**
    The current `mcp_server` block is:
    ```yaml
    mcp_server:
      command: homelab-mcp
      args: []
      timeout_seconds: 30
    ```
    Replace with (note: the comment goes ABOVE the `mcp_server:` line, NOT inside the block):
    ```yaml
    # Default invocation uses uvx for zero-install. To use a globally-installed
    # binary, set command: homelab-mcp and args: [].
    mcp_server:
      command: uvx
      args: [homelab-mcp]
      timeout_seconds: 30
    ```
    The `ollama:`, `target:`, and `judge_timeout_seconds:` blocks stay byte-for-byte unchanged.

    **Edit 3 -- `pyproject.toml` line 37-39:**
    The current marker block is:
    ```toml
    markers = [
      "live_homelab: requires live homelab-mcp on PATH",
    ]
    ```
    Replace ONLY the description string (everything after `live_homelab:`):
    ```toml
    markers = [
      "live_homelab: requires homelab-mcp runnable via uvx (or on PATH)",
    ]
    ```
    The marker name `live_homelab` and the surrounding `addopts = "-m 'not live_homelab'"` line MUST NOT change. Verify with grep below.

    **Anti-actions -- DO NOT:**
    - Add `MCPTF_CONFIG_FILE=./config.yaml` as an active line to `.env.example` (RESEARCH Anti-Pattern: this is a per-machine concern; the comment in the existing file at line 19-21 already documents the opt-in correctly).
    - Change `MCP_SERVER_TIMEOUT_SECONDS=30` (Phase 2 CONTEXT.md D-05 lock).
    - Reflow YAML/env style in either example file beyond the lines listed above (preserves diff readability).
    - Touch any other line in pyproject.toml -- specifically `[project]`, `[dependency-groups]`, `[tool.ruff*]`, `[build-system]`, `[tool.hatch.*]` are LOCKED.
  </action>
  <verify>
    <automated>grep -F "MCP_SERVER_COMMAND=uvx" .env.example && grep -F 'MCP_SERVER_ARGS=["homelab-mcp"]' .env.example && grep -F "command: uvx" config.example.yaml && grep -F "args: [homelab-mcp]" config.example.yaml && grep -F "live_homelab: requires homelab-mcp runnable via uvx (or on PATH)" pyproject.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "MCP_SERVER_COMMAND=uvx" .env.example` returns 1.
    - `grep -c "MCP_SERVER_COMMAND=homelab-mcp" .env.example` returns 0 (the old value is gone) AND `grep -c "MCP_SERVER_COMMAND=homelab-mcp" .env.example` zero matches as a setting line; the string `MCP_SERVER_COMMAND=homelab-mcp` may appear only inside the `# To use a globally-installed binary, set MCP_SERVER_COMMAND=homelab-mcp` comment -- if so, that comment line is the ONLY occurrence.
    - `grep -cF 'MCP_SERVER_ARGS=["homelab-mcp"]' .env.example` returns 1.
    - `grep -c "MCP_SERVER_TIMEOUT_SECONDS=30" .env.example` returns 1 (preserved).
    - `grep -cF "command: uvx" config.example.yaml` returns 1.
    - `grep -cF "args: [homelab-mcp]" config.example.yaml` returns 1.
    - `grep -c "command: homelab-mcp" config.example.yaml` returns 0 as a setting; if it appears in the comment line that's the only occurrence.
    - `grep -cF "live_homelab: requires homelab-mcp runnable via uvx (or on PATH)" pyproject.toml` returns 1.
    - `grep -cF "live_homelab: requires live homelab-mcp on PATH" pyproject.toml` returns 0 (old wording gone).
    - `grep -cF "addopts = \"-m 'not live_homelab'\"" pyproject.toml` returns 1 (addopts unchanged).
    - `uv run python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['tool']['pytest']['ini_options']['markers'])"` parses cleanly and prints a list whose first element ends with `via uvx (or on PATH)` -- this proves the TOML is still valid after the edit.
    - `uv run pytest --collect-only -q tests/ 2>&1 | head -5` does not emit `PytestUnknownMarkWarning` for `live_homelab` (proves the marker registration string still parses).
  </acceptance_criteria>
  <done>
    Three files edited per the diffs in 02.1-RESEARCH §Code Examples. Marker description, env example, and YAML example all reflect `uvx homelab-mcp` as the documented default. TIMEOUT_SECONDS unchanged. pyproject.toml still parses as valid TOML. No PytestUnknownMarkWarning on collection.
  </done>
</task>

<task type="auto">
  <name>Task 2: Gitignore config.yaml and commit canonical spec (D-09, D-10)</name>
  <files>.gitignore, docs/mcp_test_framework_mvp_spec.md</files>
  <read_first>
    - .gitignore (current 22 lines)
    - .planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-CONTEXT.md §D-09 and §D-10 (lines 60-62 -- exact intent)
    - .planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-RESEARCH.md §Pitfall 3 (lines 290-296 -- "Adding config.yaml to .gitignore AFTER staging it")
    - .planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-RESEARCH.md §Pitfall 4 (lines 298-308 -- bare `config.yaml` is fine, doesn't match `config.example.yaml`)
    - .planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-RESEARCH.md §Pitfall 5 (lines 310-315 -- LF line endings on the spec are already correct; no .gitattributes needed)
  </read_first>
  <action>
    **Edit 1 -- `.gitignore`:**
    Insert exactly one new line `config.yaml` directly after the existing `.env` line in the "Local config / secrets" section. The resulting block must read:
    ```
    # Local config / secrets
    .env
    config.yaml
    ```
    Per RESEARCH Pitfall 4, the bare line `config.yaml` is correct and does NOT match `config.example.yaml` -- verified live. Use the bare form (not `/config.yaml`) to match the existing repo style which uses bare `.env` directly above.

    **Edit 2 -- track `docs/mcp_test_framework_mvp_spec.md`:**
    The file already exists in the working tree but is untracked. Stage it so the plan's commit picks it up. Run:
    ```
    git add docs/mcp_test_framework_mvp_spec.md .gitignore .env.example config.example.yaml pyproject.toml
    ```
    DO NOT use `git add .` or `git add -A` (RESEARCH Pitfall 3: ensures the new gitignore rule is staged before any wildcard add can sweep up `config.yaml`). DO NOT modify the contents of `docs/mcp_test_framework_mvp_spec.md` -- D-10 says verbatim from the working tree, no edits.

    **Anti-action -- DO NOT:**
    - Add a `.gitattributes` file (RESEARCH Pitfall 5 verified the spec already has LF line endings; `core.autocrlf` round-trips safely).
    - Add `/config.yaml` (leading-slash form). Plain `config.yaml` matches the existing `.env` pattern in the same section -- consistent style.
    - Add other glob patterns like `*.yaml` or `config*.yaml` (would accidentally match `config.example.yaml`, breaking the committed template).
    - Stage `.env`, `config.yaml`, or any file under `.planning/` in this task's `git add` -- those are owned by other commits / are themselves gitignored.
  </action>
  <verify>
    <automated>git check-ignore -v config.yaml && ! git check-ignore -v config.example.yaml && git ls-files docs/mcp_test_framework_mvp_spec.md | grep -q "docs/mcp_test_framework_mvp_spec.md"</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore -v config.yaml` exits 0 and prints a line referencing `.gitignore:` and the matching pattern `config.yaml`.
    - `git check-ignore -v config.example.yaml` exits 1 (NOT ignored -- the committed template is still tracked).
    - `git check-ignore -v .env` exits 0 (existing rule unchanged).
    - `git ls-files docs/mcp_test_framework_mvp_spec.md` prints `docs/mcp_test_framework_mvp_spec.md` (the file is staged/tracked after `git add`).
    - `grep -c "^config.yaml$" .gitignore` returns 1 (exactly one bare line).
    - `grep -c "^.env$" .gitignore` returns 1 (existing line preserved).
    - The relative ordering inside `.gitignore` is: the new `config.yaml` line appears AFTER the existing `.env` line (verifiable by `grep -n "^\\.env\\|^config\\.yaml" .gitignore` showing `.env` first by line number).
    - `git status --porcelain docs/mcp_test_framework_mvp_spec.md` shows `A  docs/mcp_test_framework_mvp_spec.md` (added, ready to commit). The file's blob hash matches its working-tree content (no edits applied).
  </acceptance_criteria>
  <done>
    `config.yaml` is gitignored; `config.example.yaml` is NOT; `.env` rule preserved. `docs/mcp_test_framework_mvp_spec.md` is staged for commit (no content edits). All five files modified by this plan are staged: `.env.example`, `config.example.yaml`, `pyproject.toml`, `.gitignore`, `docs/mcp_test_framework_mvp_spec.md`.
  </done>
</task>

</tasks>

<verification>
After both tasks land:
- `uv run pytest -q` -- 30 passed, 2 deselected (no behavior change from doc edits; sanity check that pyproject.toml is still valid).
- `uv run ruff check src tests` -- clean.
- `git diff --cached --stat` -- shows exactly 4 modified files + 1 added file: `.env.example`, `config.example.yaml`, `pyproject.toml`, `.gitignore`, `docs/mcp_test_framework_mvp_spec.md`.
- `git check-ignore -v config.yaml` exits 0; `git check-ignore -v config.example.yaml` exits 1.
- `git ls-files docs/` lists the spec file.
- `cat .env.example` shows `MCP_SERVER_COMMAND=uvx` and `MCP_SERVER_ARGS=["homelab-mcp"]` plus the explanatory comment.
- `cat config.example.yaml` shows `command: uvx` and `args: [homelab-mcp]` plus the explanatory comment.
- `grep "live_homelab:" pyproject.toml` shows the new uvx-or-PATH wording.
</verification>

<success_criteria>
1. `.env.example` and `config.example.yaml` document `uvx homelab-mcp` as the default invocation with an inline comment showing how to switch to a globally-installed binary.
2. `pyproject.toml` `live_homelab` marker description matches the new wording without breaking marker registration.
3. `config.yaml` is gitignored; `config.example.yaml` is not; `.env` rule preserved.
4. `docs/mcp_test_framework_mvp_spec.md` is staged for commit verbatim from the working tree (no edits).
5. The default test suite still collects and runs cleanly (no PytestUnknownMarkWarning).
6. `MCP_SERVER_TIMEOUT_SECONDS=30` (Phase 2 D-05 lock) preserved across both example files.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-02-SUMMARY.md` covering: the exact diffs to all 4 modified files, the `git ls-files` confirmation that the spec is now tracked, the `git check-ignore` output for `config.yaml` and `config.example.yaml`, and the `pytest --collect-only` confirmation that the marker still registers cleanly. Cite D-02, D-09, D-10 by name in the summary.
</output>
