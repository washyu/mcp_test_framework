---
phase: 02.1-close-phase-2-verification-gaps-config-uat
plan: 03
type: execute
wave: 2
depends_on: ["02.1-01"]
files_modified:
  - .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md
  - .planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md
autonomous: true
requirements:
  - CORE-03
must_haves:
  truths:
    - "D-04: both live_homelab smoke tests are executed and their pytest output captured verbatim into 02-HUMAN-UAT.md"
    - "D-05: Phase 2 verification frontmatter flips to status: passed only when BOTH tests pass on the same run"
    - "D-06: if any test fails, 02-VERIFICATION.md frontmatter is NOT touched (the abort-on-red gate)"
    - "D-07: a smoke-test failure caused by a localized bug in mcp_client.py / config.py / models.py / tests / example files is absorbed in this phase via a follow-up plan; cross-cutting fixes (interface change, new env var, precedence shift) escalate via /gsd-insert-phase"
    - "Both live_homelab smoke tests run with MCPTF_CONFIG_FILE=./config.yaml prefix and exit 0 in the same run"
    - "02-HUMAN-UAT.md item #1 (SC#1) shows result: passed plus a fenced code block of captured pytest output"
    - "02-HUMAN-UAT.md item #2 (SC#2) shows result: passed plus a fenced code block of captured pytest output"
    - "02-HUMAN-UAT.md summary line tallies as total: 2 / passed: 2 / pending: 0 / issues: 0 / skipped: 0 / blocked: 0"
    - "02-VERIFICATION.md frontmatter status is 'passed' (was 'human_needed')"
    - "02-VERIFICATION.md frontmatter must_haves_passed is 4 (was 2)"
    - "02-VERIFICATION.md frontmatter must_haves_human is 0 (was 2)"
    - "02-VERIFICATION.md frontmatter must_haves_failed is 0 (unchanged)"
    - "02-VERIFICATION.md frontmatter must_haves_total is 4 (unchanged)"
    - "If either smoke test FAILS: 02-VERIFICATION.md frontmatter is NOT touched, 02-HUMAN-UAT.md captures result: failed plus the traceback, and a follow-up phase is recommended per D-07 escalation rules"
  artifacts:
    - path: ".planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md"
      provides: "Filled-in UAT log with captured pytest output for both smoke tests (D-04); abort-on-red gate honored when any test fails (D-06); failure routed via absorb-vs-escalate disposition (D-07)"
      contains: "result: passed"
    - path: ".planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md"
      provides: "Phase 2 verification frontmatter flipped to 'passed' on green double-pass UAT (D-05)"
      contains: "status: passed"
  key_links:
    - from: ".planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md"
      to: "the verbatim pytest stdout/stderr captured during this plan's execution"
      via: "fenced code block under each item"
      pattern: "=+\\s+test session starts\\s+=+"
    - from: ".planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md"
      to: "downstream gsd audits (gsd-progress, gsd-audit-milestone)"
      via: "frontmatter status field flip is the load-bearing signal that Phase 2 is fully verified"
      pattern: "^status: passed$"
    - from: "MCPTF_CONFIG_FILE=./config.yaml"
      to: "Config().mcp_server.command resolving to 'uvx'"
      via: "YAML overlay loaded by pydantic-settings YamlConfigSettingsSource"
      pattern: "MCPTF_CONFIG_FILE=./config.yaml"
---

<objective>
Run the two `live_homelab` smoke tests against the real `homelab-mcp` subprocess (via `uvx`) on the developer's Windows 11 machine, capture the pytest output verbatim into `02-HUMAN-UAT.md`, and on a green double-pass flip the `02-VERIFICATION.md` frontmatter from `human_needed` to `passed` per Phase 02.1 CONTEXT.md D-04 and D-05. This is the load-bearing artifact that closes CORE-03 / Phase 2.

Purpose: Phase 2 currently shows `status: human_needed` with 2 of 4 success criteria pending live verification. Plan 02.1-01 has already removed the Windows-fatal `errlog=` blocker so both tests can now run green. This plan is the actual UAT execution + evidence capture + frontmatter gate flip.

Output: 02-HUMAN-UAT.md filled in with two `result: passed` items + captured pytest stdout/stderr; 02-VERIFICATION.md frontmatter flipped per the exact target values listed in must_haves below.

Wave 2: depends on Plan 02.1-01 (which fixes the Windows errlog crash that would otherwise make SC#2 fail immediately). Independent of Plan 02.1-02 (doc-hygiene plan touches different files).
</objective>

<execution_context>
@<home>/.claude/get-shit-done/workflows/execute-plan.md
@<home>/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-CONTEXT.md
@.planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-RESEARCH.md
@.planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-01-PLAN.md
@.planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md
@.planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md
@tests/smoke/test_smoke_homelab_mcp.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run both live_homelab smoke tests with the YAML overlay env var and capture verbatim output</name>
  <files>.planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md (current 33 lines -- the partial UAT file from Phase 2)
    - .planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-RESEARCH.md §Critical Finding 1 (lines 79-109 -- MCPTF_CONFIG_FILE=./config.yaml is MANDATORY)
    - .planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-RESEARCH.md §Pattern 3 -- HUMAN-UAT.md fill-in (lines 215-241 -- exact format)
    - .planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-RESEARCH.md §Common Pitfalls (lines 277-323 -- esp. Pitfall 1 forgetting the env var, Pitfall 2 confusing errlog crash for env issue, Pitfall 6 env var not persisting)
    - tests/smoke/test_smoke_homelab_mcp.py (the two test functions being run)
    - .planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-CONTEXT.md §D-04, D-05, D-06 (lines 42-49 -- exact pytest commands and result-capture rules)
  </read_first>
  <action>
    **Step 1 -- precondition check.** Verify Plan 02.1-01 has landed:
    ```bash
    grep -c "stdio_client(params)" src/mcp_test_framework/mcp_client.py
    ```
    Must return 1. If 0 (still has `errlog=` kwarg), STOP and surface that 02.1-01 has not landed -- this plan is blocked.

    **Step 2 -- run BOTH smoke tests in a SINGLE pytest invocation.** Per RESEARCH §Common Pitfall 6, separate Bash invocations get separate shells; use one combined command. Per RESEARCH Critical Finding 1, the env var prefix is MANDATORY (otherwise `Config().mcp_server.command` defaults to `'homelab-mcp'`, not `'uvx'`, and `shutil.which('homelab-mcp')` returns None on this machine).

    Use ONE of these two equivalent commands (Bash form is preferred for output-capture simplicity):

    Bash / Git Bash:
    ```bash
    MCPTF_CONFIG_FILE=./config.yaml uv run pytest -m live_homelab tests/smoke/test_smoke_homelab_mcp.py -v 2>&1 | tee uat_capture.txt
    ```

    PowerShell equivalent:
    ```powershell
    $env:MCPTF_CONFIG_FILE = './config.yaml'
    uv run pytest -m live_homelab tests/smoke/test_smoke_homelab_mcp.py -v 2>&1 | Tee-Object -FilePath uat_capture.txt
    Remove-Item env:MCPTF_CONFIG_FILE
    ```

    The pytest invocation collects exactly 2 tests (`test_raw_stdio_lists_target_tool` and `test_wrapper_call_tool_returns_non_error_with_content`) -- verified in 02-VERIFICATION.md line 103. The `-v` flag prints PASSED/FAILED per test on its own line. `2>&1 | tee` preserves both streams in order.

    **Step 3 -- capture and decide outcome.**
    Read `uat_capture.txt`. The pytest summary line at the bottom is one of:
    - `============================== 2 passed in N.NNs ==============================` -- BOTH PASSED -> proceed to Step 4 (green path).
    - `========================= 1 failed, 1 passed in N.NNs =========================` or `2 failed in N.NNs` -- FAILURE -> proceed to Step 5 (red path) per D-06.

    **Step 4 -- green path: fill in 02-HUMAN-UAT.md with passed results.**

    Open `.planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md`. Make these edits:

    (a) Frontmatter `status: partial` -> `status: complete`. Update `updated:` to today's date (2026-05-04 per the system context, or whatever today's actual run date is per the date the executor reads from system).

    (b) The "Current Test" section currently says `[awaiting human testing on a machine with \`homelab-mcp\` installed]`. Replace with: `Closed by Phase 02.1 -- both smoke tests passed against live homelab-mcp via uvx on Windows 11.`

    (c) Item #1 (SC#1) -- replace `result: [pending]` with `result: passed`, then add directly after it:
    ```
    ran: <date> (Windows 11, uvx 0.11.x, MCPTF_CONFIG_FILE=./config.yaml)
    command: MCPTF_CONFIG_FILE=./config.yaml uv run pytest -m live_homelab tests/smoke/test_smoke_homelab_mcp.py::test_raw_stdio_lists_target_tool -v

    <details>
    <summary>Captured pytest output</summary>

    ```
    <PASTE the SC#1-relevant lines from uat_capture.txt here -- include the `test session starts` banner, the collection line, the PASSED line for test_raw_stdio_lists_target_tool, and the trailing summary line. If both tests are in one capture block, this captures the full pytest run for both items here -- that's fine; alternatively split the output by test name. Either is acceptable per Claude's Discretion.>
    ```

    </details>
    ```

    (d) Item #2 (SC#2) -- same pattern as (c) but for `test_wrapper_call_tool_returns_non_error_with_content`. If you used the combined run, you can either (i) paste the same combined capture under both items with a note, or (ii) re-run the SC#2 test individually for a clean per-item capture. Option (i) is fine and saves time.

    (e) Summary block (currently `total: 2 / passed: 0 / issues: 0 / pending: 2 / skipped: 0 / blocked: 0`):
    Update to EXACTLY:
    ```
    total: 2
    passed: 2
    issues: 0
    pending: 0
    skipped: 0
    blocked: 0
    ```

    (f) Gaps section -- leave the `## Gaps` heading; under it add `(None.)` if empty.

    **Step 5 -- red path: fill in 02-HUMAN-UAT.md with failed results, DO NOT touch 02-VERIFICATION.md.**

    If either or both tests FAIL:
    - Update `result: [pending]` to `result: failed` for the failing item(s).
    - Paste the captured pytest output (including full traceback) inside a `<details><summary>Captured pytest output</summary>` block as in Step 4(c)/(d).
    - Update Summary: `passed: <0 or 1>`, `pending: 0`, `issues: <1 or 2>` (the issues count = the failed count).
    - Frontmatter: set `status: blocked`. Do NOT set `status: complete`.
    - Add a new section `## Failure Analysis` after Gaps:
      ```
      ## Failure Analysis

      Failing test: <test name>
      Captured stack signature: <one-line summary, e.g. "io.UnsupportedOperation: fileno at subprocess.py:1420"; or "FileNotFoundError: 'homelab-mcp'"; or "asyncio.TimeoutError after 30s">
      Disposition per 02.1 D-07:
      - <ABSORB | ESCALATE> -- <one-paragraph rationale citing the D-07 fix-locality test: "is the fix localized to mcp_client.py / config.py / models.py / tests / example files? If yes -> absorb in 02.1; if no -> escalate via /gsd-insert-phase">
      Recommended next step: <e.g. "extend Plan 02.1-01 with an additional task" OR "STOP, freeze 02.1, run /gsd-insert-phase 02.2 with this Failure Analysis as the seed">
      ```
    - Skip Task 2 -- do NOT touch 02-VERIFICATION.md frontmatter (D-06 lock).

    **Sanity warnings (per RESEARCH §Pitfalls):**
    - If you see `FileNotFoundError [WinError 2] ... 'homelab-mcp'`, that means MCPTF_CONFIG_FILE was NOT picked up -- this is Pitfall 1, NOT a real SC#1 failure. Re-run with the env var explicitly set inline on the command (avoid the PowerShell session-scoped approach if it's not surviving across the tool boundary).
    - If you see `FileNotFoundError [WinError 2] ... 'uvx'`, that's a real environmental problem -- `uvx` should ship with `uv`. Stop and surface to the user.
    - If you see `io.UnsupportedOperation: fileno`, that means Plan 02.1-01 has NOT landed in the working tree being tested. Verify with `grep -n "errlog" src/mcp_test_framework/mcp_client.py` -- if it shows `errlog=_LoggerWriter(_log)` in the `stdio_client` call, 02.1-01 is missing.

    **Anti-actions -- DO NOT:**
    - Modify `tests/smoke/test_smoke_homelab_mcp.py` (Phase 2 deliverable, locked per 02.1 RESEARCH Anti-Patterns).
    - Update 02-VERIFICATION.md frontmatter in this task (Task 2 owns that, gated on green from this task).
    - Strip `<` or `*` characters from the captured output before pasting (RESEARCH Anti-Pattern: always use ``` fences exactly because tracebacks contain markdown-active characters).
    - Commit `uat_capture.txt` (it's a working file; the canonical record is the fenced block inside 02-HUMAN-UAT.md).
    - Add `MCPTF_CONFIG_FILE` to a tracked file (RESEARCH Anti-Pattern: cwd auto-discovery is locked closed in Phase 1).
  </action>
  <verify>
    <automated>grep -c "result: passed" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    GREEN-PATH (both tests passed):
    - `grep -c "result: passed" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returns 2.
    - `grep -c "result: \[pending\]" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returns 0.
    - `grep -c "result: failed" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returns 0.
    - `grep -c "^total: 2$" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returns 1.
    - `grep -c "^passed: 2$" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returns 1.
    - `grep -c "^pending: 0$" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returns 1.
    - `grep -c "^issues: 0$" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returns 1.
    - `grep -c "<details>" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returns at least 1 (captured-output details block(s) present).
    - `grep -c "MCPTF_CONFIG_FILE=./config.yaml" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returns at least 2 (one per item's command line; documents the env-var requirement for reproducibility).
    - The captured output blocks contain `PASSED` for both `test_raw_stdio_lists_target_tool` and `test_wrapper_call_tool_returns_non_error_with_content` -- verifiable by `grep "PASSED" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returning at least 2 lines.
    - The captured output contains `2 passed` (the pytest summary line) -- verifiable by `grep -c "passed in" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returning at least 1.
    - Frontmatter `status: complete` is present (`grep -c "^status: complete$" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returns 1).

    RED-PATH (any test failed):
    - `grep -c "result: failed" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returns 1 or 2.
    - `grep -c "## Failure Analysis" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returns 1.
    - The Failure Analysis section names the failing test, shows the stack signature, and states ABSORB or ESCALATE with rationale per D-07.
    - `grep -c "^status: blocked$" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returns 1.
    - Task 2 is SKIPPED -- 02-VERIFICATION.md frontmatter still reads `status: human_needed` afterward.
  </acceptance_criteria>
  <done>
    Either: (a) both live tests passed, 02-HUMAN-UAT.md updated with two `result: passed` entries + fenced captured-output blocks + summary `passed: 2 / pending: 0 / issues: 0`, ready for Task 2 to flip 02-VERIFICATION.md; OR (b) at least one test failed, 02-HUMAN-UAT.md updated with `result: failed` + traceback + Failure Analysis section per D-06/D-07/D-08, Task 2 SKIPPED, escalation/absorb decision recorded.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip 02-VERIFICATION.md frontmatter on green UAT (D-05) -- SKIP if any test failed</name>
  <files>.planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md (current 137 lines -- specifically frontmatter at lines 1-12)
    - .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md (post-Task-1 state -- look for `status: complete` and `passed: 2`. If `status: blocked` or `result: failed` appears anywhere, ABORT this task.)
    - .planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-CONTEXT.md §D-05, D-06 (lines 47-50 -- exact gate condition)
    - .planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-RESEARCH.md §Pattern 2 -- Verification frontmatter flip (lines 191-213 -- exact target frontmatter)
  </read_first>
  <action>
    **Pre-flight gate -- D-06 lock.** Run:
    ```bash
    grep -E "^status: complete$|result: failed|^status: blocked$" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md
    ```
    Required output: exactly the line `status: complete` (and nothing else from that pattern). If any line containing `result: failed` or `status: blocked` appears, ABORT this task and surface "UAT did not pass; D-06 prohibits flipping verification frontmatter; see 02-HUMAN-UAT.md Failure Analysis."

    **Frontmatter flip.** Open `.planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md`. The current frontmatter (lines 1-12) is:
    ```yaml
    ---
    status: human_needed
    phase: 02-mcp-client-wrapper
    phase_number: 02
    verified_at: 2026-05-04
    verifier: orchestrator (claude opus 4.7) — manual verification after subagent permission lockouts
    must_haves_total: 4
    must_haves_passed: 2
    must_haves_human: 2
    must_haves_failed: 0
    requirements: [CORE-03]
    ---
    ```

    Apply EXACTLY these field changes (all other fields stay byte-for-byte unchanged):
    - `status: human_needed` -> `status: passed`
    - `must_haves_passed: 2` -> `must_haves_passed: 4`
    - `must_haves_human: 2` -> `must_haves_human: 0`

    Optionally (Claude's Discretion per 02.1 RESEARCH Open Question 2):
    - `verified_at: 2026-05-04` -> bump to today's actual date if different (the file's previous date is fine if the run happened today).
    - `verifier: orchestrator (claude opus 4.7) — manual verification after subagent permission lockouts` -> append ` ; closed by Phase 02.1 (live UAT against uvx homelab-mcp on Windows 11)` to give an audit trail.

    DO NOT change:
    - `phase`, `phase_number`, `must_haves_total`, `must_haves_failed`, `requirements` -- all locked per RESEARCH §Pattern 2.

    **Body update (Claude's Discretion per 02.1 D-Discretion bullet 4).** In the body section "## Items requiring human verification" (currently lines 108-122), append a one-line resolution note as the FIRST line of that section:
    ```
    > Resolved by Phase 02.1 on <date>: both items ran live and passed; see `02-HUMAN-UAT.md`. The two pending items below are kept for historical context.
    ```
    Do not delete the existing "1. SC#1" / "2. SC#2" subsections -- they are the historical record of what needed verifying.

    **Anti-actions -- DO NOT:**
    - Update the `## Test Suite State` section's `30 passed, 2 deselected` line. The default suite (without `-m live_homelab`) still produces that count -- it has not changed. Editing it is wrong.
    - Touch the `## Success Criteria Verification` SC#1 / SC#2 subsections. They are evidence narrative for the Phase 2 verification snapshot; the new evidence lives in 02-HUMAN-UAT.md, which is now the canonical record.
    - Mark the `## Gaps` section (currently `(None.)`) -- still none.
  </action>
  <verify>
    <automated>grep -E "^status: passed$|^must_haves_passed: 4$|^must_haves_human: 0$" .planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^status: passed$" .planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md` returns 1.
    - `grep -c "^status: human_needed$" .planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md` returns 0.
    - `grep -c "^must_haves_passed: 4$" .planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md` returns 1.
    - `grep -c "^must_haves_passed: 2$" .planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md` returns 0.
    - `grep -c "^must_haves_human: 0$" .planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md` returns 1.
    - `grep -c "^must_haves_human: 2$" .planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md` returns 0.
    - `grep -c "^must_haves_total: 4$" .planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md` returns 1 (unchanged).
    - `grep -c "^must_haves_failed: 0$" .planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md` returns 1 (unchanged).
    - `grep -c "^phase_number: 02$" .planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md` returns 1 (unchanged).
    - `grep -c "^requirements: \[CORE-03\]$" .planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md` returns 1 (unchanged).
    - `grep -c "Resolved by Phase 02.1" .planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md` returns 1 (the body resolution note is present).
    - `uv run python -c "import yaml; data = yaml.safe_load(open('.planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md').read().split('---')[1]); assert data['status'] == 'passed' and data['must_haves_passed'] == 4 and data['must_haves_human'] == 0 and data['must_haves_total'] == 4 and data['must_haves_failed'] == 0; print('OK')"` prints `OK` (frontmatter is still valid YAML and contains the exact target values).
  </acceptance_criteria>
  <done>
    02-VERIFICATION.md frontmatter reads `status: passed`, `must_haves_passed: 4`, `must_haves_human: 0` (with `must_haves_total: 4` and `must_haves_failed: 0` preserved). Frontmatter parses as valid YAML. Body has a one-line resolution note pointing to 02-HUMAN-UAT.md. Phase 2 is now fully verified end-to-end; CORE-03 closure is the load-bearing signal that downstream gsd audits will read.
  </done>
</task>

</tasks>

<verification>
After both tasks land (green path):
- 02-HUMAN-UAT.md: 2 `result: passed`, summary `total: 2 / passed: 2 / pending: 0 / issues: 0`, captured pytest output in fenced blocks, frontmatter `status: complete`.
- 02-VERIFICATION.md frontmatter: `status: passed`, `must_haves_passed: 4`, `must_haves_human: 0`, `must_haves_total: 4`, `must_haves_failed: 0`, `requirements: [CORE-03]`.
- The two pytest commands from 02.1-CONTEXT D-04 (with the `MCPTF_CONFIG_FILE=./config.yaml` prefix per 02.1-RESEARCH Critical Finding 1) ran green.
- `uv run python -c "import yaml; ..." ` confirms 02-VERIFICATION.md frontmatter is still valid YAML.
- `grep -c "result: \[pending\]" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returns 0.
- `grep -c "result: failed" .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` returns 0.
- Default suite (`uv run pytest -q`) still reports 30 passed / 2 deselected (sanity: no regression from the live run).

Red-path (any test failed):
- 02-HUMAN-UAT.md captures the failure with `result: failed` + traceback + Failure Analysis section.
- 02-VERIFICATION.md frontmatter UNCHANGED (status still `human_needed`).
- D-08 rationale (failing test name + traceback signature + absorb-vs-escalate choice) recorded inside Failure Analysis.
- Plan SUMMARY.md flags the phase as "uncovered failing UAT, root cause documented" and recommends `/gsd-insert-phase` if escalation chosen.
</verification>

<success_criteria>
1. Both `live_homelab` smoke tests run with `MCPTF_CONFIG_FILE=./config.yaml` set, exit 0 in the same pytest session.
2. 02-HUMAN-UAT.md contains both items as `result: passed` plus verbatim captured pytest output in `<details>`-wrapped fenced code blocks.
3. 02-HUMAN-UAT.md summary tallies are `total: 2 / passed: 2 / pending: 0 / issues: 0 / skipped: 0 / blocked: 0`.
4. 02-VERIFICATION.md frontmatter contains EXACTLY `status: passed`, `must_haves_passed: 4`, `must_haves_human: 0`, `must_haves_total: 4`, `must_haves_failed: 0`. All other frontmatter fields preserved.
5. 02-VERIFICATION.md body has a resolution note pointing to 02-HUMAN-UAT.md.
6. CORE-03 traceability table entry can transition from "Pending" to "Complete" on the next ROADMAP/REQUIREMENTS regen (this is downstream; this plan does not touch those files).
7. RED-PATH ONLY: 02-VERIFICATION.md frontmatter NOT touched; Failure Analysis section in 02-HUMAN-UAT.md cites failing test, captured stack, and absorb/escalate per D-07/D-08.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-03-SUMMARY.md` covering:
- The exact pytest command(s) run, including the `MCPTF_CONFIG_FILE=./config.yaml` prefix (cite RESEARCH Critical Finding 1).
- The full captured pytest summary line (e.g., `2 passed in 9.12s`).
- The before/after frontmatter values for 02-VERIFICATION.md (citing D-05).
- Confirmation of the green-path acceptance criteria (or, on red-path, the Failure Analysis content + escalation decision).
- The closing line: "Phase 2 (CORE-03) is now fully verified end-to-end."
</output>
