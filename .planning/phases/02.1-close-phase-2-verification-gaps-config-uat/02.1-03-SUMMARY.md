---
phase: 02.1-close-phase-2-verification-gaps-config-uat
plan: 03
subsystem: testing

tags: [pytest, pytest-asyncio, mcp, stdio_client, uvx, homelab-mcp, live-uat, verification, frontmatter, pydantic-settings]

# Dependency graph
requires:
  - phase: 02.1-01
    provides: "Removed `errlog=_LoggerWriter(_log)` kwarg from `stdio_client(params)` call in `src/mcp_test_framework/mcp_client.py` (the Windows-fatal `io.UnsupportedOperation: fileno` blocker for SC#2)"
  - phase: 02-mcp-client-wrapper
    provides: "`tests/smoke/test_smoke_homelab_mcp.py` (the two `live_homelab` smoke tests being executed); `McpTestClient` wrapper; `02-VERIFICATION.md` and `02-HUMAN-UAT.md` partial files awaiting live UAT closure"
  - phase: 01-config-and-cli
    provides: "`MCPTF_CONFIG_FILE` env var loaded by `pydantic-settings` `YamlConfigSettingsSource` to overlay `command: uvx, args: [homelab-mcp]` onto the default `MCP_SERVER` config"
provides:
  - "Live evidence that Phase 2 SC#1 (raw stdio_client lists `list_registered_servers` with non-empty schema) passes end-to-end against `uvx homelab-mcp` on Windows 11"
  - "Live evidence that Phase 2 SC#2 (`McpTestClient.call_tool` returns `isError=False` with non-empty content/structuredContent) passes end-to-end on Windows 11"
  - "`02-HUMAN-UAT.md` filled in (status: complete, total: 2 / passed: 2 / pending: 0 / issues: 0) with verbatim pytest output preserved in `<details>`-wrapped fenced code blocks"
  - "`02-VERIFICATION.md` frontmatter flipped to `status: passed`, `must_haves_passed: 4`, `must_haves_human: 0` per D-05 — the load-bearing audit signal that downstream gsd commands read"
  - "End-to-end CORE-03 closure: Phase 2 (CORE-03) is now fully verified, no human gates remain"
affects: [02.1-orchestrator-phase-complete, gsd-progress, gsd-audit-milestone, REQUIREMENTS.md CORE-03 traceability row, ROADMAP.md Phase 2 progress table]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Live UAT capture pattern: run pytest with `MCPTF_CONFIG_FILE=./config.yaml` prefix + `tee` to scratch file, paste verbatim into HUMAN-UAT.md inside `<details>` blocks, delete scratch file (never commit)"
    - "Verification frontmatter flip pattern: D-06 pre-flight gate (grep HUMAN-UAT.md for status: complete with no result: failed and no status: blocked) before any frontmatter mutation"

key-files:
  created:
    - .planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-03-SUMMARY.md
  modified:
    - .planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md
    - .planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md

key-decisions:
  - "Used the combined-pytest-run path (one invocation collecting both tests) per Plan Step 2 — saves cold-start time and produces a single canonical capture block; both items in HUMAN-UAT.md reference the same capture with a clarifying note (option (i) in Plan Step 4(d))"
  - "Bash form of the env-var prefix chosen over the PowerShell session-scoped form to dodge RESEARCH Pitfall 6 (env var not surviving across separate shell invocations)"
  - "Verifier audit-trail line appended (' ; closed by Phase 02.1 (live UAT against uvx homelab-mcp on Windows 11)') per RESEARCH Open Question 2 'Claude's Discretion' — provides traceability without breaking YAML"

patterns-established:
  - "D-06 pre-flight gate: before any frontmatter mutation that depends on UAT, grep the HUMAN-UAT.md file for the exact lines `status: complete` (presence) and `result: failed`/`status: blocked` (absence). The grep output must be exactly `status: complete` and nothing else from that pattern."
  - "Atomic two-task split for UAT-gated frontmatter flips: Task 1 captures evidence (always runs), Task 2 mutates the verification artifact (only runs on green). Each gets its own commit so a red-path partial completion is still a clean checkpoint for downstream agents."

requirements-completed: [CORE-03]

# Metrics
duration: ~6 min
completed: 2026-05-04
---

# Phase 02.1 Plan 03: Live Phase-2 UAT Closure Summary

**Both `live_homelab` smoke tests (`test_raw_stdio_lists_target_tool` + `test_wrapper_call_tool_returns_non_error_with_content`) ran green in 6.90s on Windows 11 against `uvx homelab-mcp`; Phase 2 verification frontmatter flipped to `status: passed` per D-05.**

## Performance

- **Duration:** ~6 min wall-clock
- **Started:** 2026-05-04 (orchestrator hand-off, Wave 2 of Phase 02.1)
- **Completed:** 2026-05-04
- **Tasks:** 2 (both auto, both green)
- **Files modified:** 2 (`02-HUMAN-UAT.md`, `02-VERIFICATION.md`)
- **Pytest live-suite duration:** **6.90s** for both tests in a single session (cold-start cost — first uvx invocation downloads `homelab-mcp` if not cached; this run was warm from prior 02.1-01 work)

## Accomplishments

- **Both Phase 2 live-UAT smoke tests passed in a single pytest session** with the mandatory `MCPTF_CONFIG_FILE=./config.yaml` overlay (RESEARCH Critical Finding 1). Pytest summary line: `============================== 2 passed in 6.90s ==============================`.
- **02-HUMAN-UAT.md filled in per D-04**: both items now show `result: passed` with verbatim pytest stdout preserved in `<details>`-wrapped fenced code blocks; summary tallies updated to `total: 2 / passed: 2 / pending: 0 / issues: 0 / skipped: 0 / blocked: 0`; frontmatter `status: partial` → `status: complete`.
- **02-VERIFICATION.md frontmatter flipped per D-05**: `status: human_needed` → `status: passed`; `must_haves_passed: 2` → `4`; `must_haves_human: 2` → `0`. The four locked fields (`phase`, `phase_number`, `must_haves_total`, `must_haves_failed`, `requirements`) preserved byte-for-byte. YAML validity confirmed by `yaml.safe_load`.
- **D-06 abort-on-red gate honored**: pre-flight `grep -E "^status: complete$|result: failed|^status: blocked$"` against the post-Task-1 HUMAN-UAT.md returned exactly `status: complete` with no failure markers, gating Task 2 to proceed.
- **No regression in default suite**: post-flip, `uv run pytest -q` still reports `30 passed, 2 deselected in 0.61s` (the live tests remain filtered out by `addopts = "-m 'not live_homelab'"`).

## Pytest Commands Executed (Verbatim)

Single combined run (per Plan Step 2 / RESEARCH Pitfall 6 — one shell, both tests):

```
MCPTF_CONFIG_FILE=./config.yaml uv run pytest -m live_homelab tests/smoke/test_smoke_homelab_mcp.py -v 2>&1 | tee uat_capture.txt
```

Captured output (verbatim):

```
============================= test session starts =============================
platform win32 -- Python 3.14.3, pytest-9.0.3, pluggy-1.6.0 -- <home>\projects\mvp_test_framework\.venv\Scripts\python.exe
cachedir: .pytest_cache
rootdir: <home>\projects\mvp_test_framework
configfile: pyproject.toml
plugins: anyio-4.13.0, asyncio-1.3.0
asyncio: mode=Mode.STRICT, debug=False, asyncio_default_fixture_loop_scope=session, asyncio_default_test_loop_scope=function
collecting ... collected 2 items

tests/smoke/test_smoke_homelab_mcp.py::test_raw_stdio_lists_target_tool PASSED [ 50%]
tests/smoke/test_smoke_homelab_mcp.py::test_wrapper_call_tool_returns_non_error_with_content PASSED [100%]

============================== 2 passed in 6.90s ==============================
```

Exit code: 0. Total wall-clock for the pytest session: 6.90s. Both tests collected exactly per `02-VERIFICATION.md` line 103. The scratch `uat_capture.txt` was deleted after the verbatim block was pasted into `02-HUMAN-UAT.md` (per Plan Step 4 anti-action — never commit the scratch file).

## Decision Gates Honored

- **D-04** — both `live_homelab` smoke tests executed and pytest output captured verbatim into `02-HUMAN-UAT.md`. ✓
- **D-05** — Phase 2 verification frontmatter flipped to `status: passed` because BOTH tests passed in the same run. ✓
- **D-06** — abort-on-red gate cleared: pre-flight grep against `02-HUMAN-UAT.md` returned exactly `status: complete` with no `result: failed` or `status: blocked` markers, so the frontmatter flip in Task 2 was permitted. (Had either test failed, Task 2 would have aborted and `02-VERIFICATION.md` would remain at `status: human_needed` — green path means D-06 was not triggered.) ✓
- **D-07** — absorb-vs-escalate disposition: not applicable (green path). Had a smoke test failed, the Failure Analysis section in `02-HUMAN-UAT.md` would name the failing test, the stack signature, and pick ABSORB (localized to mcp_client.py / config.py / models.py / tests / example files) or ESCALATE (cross-cutting interface change, new env var, or precedence shift). ✓ (vacuously)

## Before/After Frontmatter (`02-VERIFICATION.md`)

**Before (lines 1–12):**
```yaml
---
status: human_needed
phase: 02-mcp-client-wrapper
phase_number: 02
verified_at: 2026-05-04
verifier: orchestrator (claude opus 4.7) — manual verification after subagent permission lockouts
must_haves_total: 4
must_haves_passed: 2
must_haves_human: 2
must_haves_failed: 0
requirements: [CORE-03]
---
```

**After:**
```yaml
---
status: passed
phase: 02-mcp-client-wrapper
phase_number: 02
verified_at: 2026-05-04
verifier: orchestrator (claude opus 4.7) — manual verification after subagent permission lockouts ; closed by Phase 02.1 (live UAT against uvx homelab-mcp on Windows 11)
must_haves_total: 4
must_haves_passed: 4
must_haves_human: 0
must_haves_failed: 0
requirements: [CORE-03]
---
```

YAML validity confirmed by `uv run python -c "import yaml; data = yaml.safe_load(open('.planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md').read().split('---')[1]); ...; print('OK')"` → `OK`.

## Task Commits

Each task was committed atomically (per `task_commit_protocol`):

1. **Task 1: Run both `live_homelab` smoke tests + capture into 02-HUMAN-UAT.md** — `65185ca` (`test`)
2. **Task 2: Flip 02-VERIFICATION.md frontmatter on green UAT** — `d471027` (`docs`)

Plan-level summary commit will be made by the final-commit step after this SUMMARY.md is written.

## Pre-flight Checks (run before Task 1 pytest invocation)

- `ls config.yaml` — present at repo root ✓
- `grep -E "command|args" config.yaml` — returns `command: uvx` and `args: [homelab-mcp]` ✓ (working invocation; matches RESEARCH Critical Finding 1)
- `uvx --version` — returns `uvx 0.11.3 (45da18ac3 2026-04-01 x86_64-pc-windows-msvc)` ✓ (≥ 0.5)
- `grep -n "stdio_client" src/mcp_test_framework/mcp_client.py` — line 124 shows `stdio_client(params)` (no `errlog=` kwarg), confirming Plan 02.1-01 has landed and the Windows-fatal `io.UnsupportedOperation: fileno` blocker is gone ✓

All four pre-flight checks green; pytest invocation proceeded.

## Files Created/Modified

- `.planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` — replaced placeholder content with two `result: passed` items, verbatim pytest captures inside `<details>` fenced blocks, summary tallies `total: 2 / passed: 2 / pending: 0 / issues: 0 / skipped: 0 / blocked: 0`, frontmatter `status: complete`. (54 insertions, 6 deletions per `git show`.)
- `.planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md` — frontmatter flipped (`status` / `must_haves_passed` / `must_haves_human` / `verifier`), one-line resolution note prepended to `## Items requiring human verification` section pointing to `02-HUMAN-UAT.md`. Locked fields and historical SC#1/SC#2 subsections preserved. (6 insertions, 4 deletions per `git show`.)
- `.planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-03-SUMMARY.md` — this file.

## Decisions Made

- **Combined pytest run instead of two separate runs.** Plan Step 4(d) explicitly permits option (i) — paste the same combined capture under both items with a note. Chosen because (a) cuts cold-start cost in half, (b) produces a single canonical capture, (c) matches the spirit of the success criterion "both tests run with the env-var prefix and exit 0 in the same run".
- **Bash-form env-var prefix.** Plan Step 2 lists Bash and PowerShell as equivalent; I picked Bash to dodge RESEARCH Pitfall 6 (env var not surviving across separate shell invocations) and to match the form used in `02-CONTEXT.md` D-04 which spells the command as `MCPTF_CONFIG_FILE=./config.yaml uv run pytest ...`.
- **Verifier audit-trail line appended.** Per RESEARCH Open Question 2 / Plan Step 4 'Optionally', appended ` ; closed by Phase 02.1 (live UAT against uvx homelab-mcp on Windows 11)` to the verifier field — provides traceability for downstream audits without breaking YAML (semicolon is data, not a YAML special).
- **`verified_at` left at 2026-05-04.** Today's date matches the previous value per the system-context date — no bump needed.

## Deviations from Plan

None — plan executed exactly as written. Both tasks landed clean on the green path; D-06 was not triggered.

**Total deviations:** 0
**Impact on plan:** None — green path executed as designed.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required for this plan. The `homelab-mcp` server runs as a transient `uvx` subprocess each pytest invocation; no daemon, no auth, no env beyond `MCPTF_CONFIG_FILE=./config.yaml`.

## Next Phase Readiness

- **Phase 2 (CORE-03) is now fully verified end-to-end.** `02-VERIFICATION.md` reads `status: passed` with all 4 must-haves passed and 0 human-pending. `02-HUMAN-UAT.md` reads `status: complete` with 2/2 passed.
- **CORE-03 traceability table row** in `REQUIREMENTS.md` can transition from "Pending" to "Complete" on the next regen — that's a downstream concern owned by the Phase 02.1 orchestrator, not this plan (per `<execution_context>` "Do NOT update STATE.md or ROADMAP.md").
- **Wave 2 of Phase 02.1 is complete** — Plans 02.1-01 (Windows blocker fix) and 02.1-02 (doc hygiene) provided the unblocking foundation; this plan (02.1-03) executed the actual verification close. Orchestrator can now proceed to Phase 02.1 phase-complete (which will also patch ROADMAP / STATE / REQUIREMENTS).
- **No blockers, no concerns.** The default suite still reports 30 passed / 2 deselected (no regression); the live suite still collects exactly 2 tests when `-m live_homelab` is passed.

## Self-Check: PASSED

- ✓ `.planning/phases/02-mcp-client-wrapper/02-HUMAN-UAT.md` exists (modified)
- ✓ `.planning/phases/02-mcp-client-wrapper/02-VERIFICATION.md` exists (modified)
- ✓ `.planning/phases/02.1-close-phase-2-verification-gaps-config-uat/02.1-03-SUMMARY.md` exists (this file)
- ✓ Commit `65185ca` exists in git log (Task 1)
- ✓ Commit `d471027` exists in git log (Task 2)
- ✓ All 11 Task 1 acceptance grep checks returned the expected counts
- ✓ All 10 Task 2 acceptance grep checks returned the expected counts; YAML safe_load confirms frontmatter validity
- ✓ Default suite regression check: `30 passed, 2 deselected` — unchanged

## PLAN COMPLETE

Phase 2 (CORE-03) is now fully verified end-to-end.
