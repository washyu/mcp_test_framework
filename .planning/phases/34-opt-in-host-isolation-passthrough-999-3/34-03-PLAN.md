---
phase: 34-opt-in-host-isolation-passthrough-999-3
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_test_framework/_isolation.py
  - tests/framework/unit/test_isolation_dispatcher.py
autonomous: true
requirements: [ISOL-02, ISOL-03]
tags: [isolation-primitive, seed-022, dispatcher]

must_haves:
  truths:
    - "D-05: `_build_passthrough_env()` returns `dict(os.environ)` — a literal copy, nothing stripped, nothing injected"
    - "D-07: Mode-branching dispatcher `_build_subprocess_env(mode, isolated_home)` lives in `_isolation.py` as sibling to existing `_build_isolated_env`; spawn sites call dispatcher only. Existing `_build_isolated_env` keeps its name (no rename churn)."
    - "`_build_subprocess_env(mode='strict', isolated_home)` is byte-for-byte identical to legacy `_build_isolated_env(isolated_home)` (regression invariant)"
    - "`_build_subprocess_env(mode='passthrough', isolated_home=None)` is valid; passthrough does not consume isolated_home"
    - "`_build_subprocess_env(mode='strict', isolated_home=None)` raises AssertionError (defensive guard)"
    - "`_isolation.py` does NOT import `config.py` — dispatcher takes `mode: Literal[...]` (plain data); caller passes data (SEED-022)"
    - "Module docstring's `Isolation is ALWAYS-ON` line is reworded to reflect opt-in passthrough; maintainer warning at L33-37 stays in force"
  artifacts:
    - path: "src/mcp_test_framework/_isolation.py"
      provides: "`_build_passthrough_env()` + `_build_subprocess_env(mode, isolated_home)` dispatcher; legacy `_build_isolated_env` unchanged"
      contains: "_build_subprocess_env"
    - path: "tests/framework/unit/test_isolation_dispatcher.py"
      provides: "Three load-bearing dispatcher contract pins"
      contains: "test_passthrough_returns_dict_of_os_environ"
  key_links:
    - from: "_build_subprocess_env"
      to: "_build_isolated_env"
      via: "strict branch delegates verbatim — no logic duplication"
      pattern: "return _build_isolated_env"
---

<objective>
Add two sibling functions to `src/mcp_test_framework/_isolation.py` per D-07:

1. `_build_passthrough_env() -> dict[str, str]` returns a literal `dict(os.environ)` copy (D-05).
2. `_build_subprocess_env(mode: Literal['strict','passthrough'], isolated_home: Path | None) -> dict[str, str]` is the new public-internal dispatcher.

Existing `_build_isolated_env(isolated_home)` keeps its name and behavior unchanged (no rename churn — deferred per CONTEXT.md). The dispatcher delegates to it for `mode='strict'`. SEED-022-clean: `_isolation.py` does NOT import `config.py`; the dispatcher takes plain string + Path data.

Update the module docstring's `Isolation is ALWAYS-ON. No toggle, no --no-isolation CLI escape hatch.` line (currently false post-Phase-34) to reflect the new opt-in passthrough seam. Maintainer warning at lines 33-37 stays in force for the strict allowlist.

Pin all three contract invariants via a new unit test file `tests/framework/unit/test_isolation_dispatcher.py`.

Purpose: ISOL-02 + ISOL-03 — passthrough mode hands the operator's full env to the subprocess (allowlist + tempdir HOME redirect + null keyring backend all bypassed).

Output:
- Two new functions in `_isolation.py` + docstring rewording.
- One new test file with three pinned dispatcher invariants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-CONTEXT.md
@.planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-RESEARCH.md
@.planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-PATTERNS.md
@src/mcp_test_framework/_isolation.py

<interfaces>
Existing `_isolation.py` surface (preserved unchanged):

- Module constants: `_PASSTHROUGH_ALLOWLIST`, `_MCP_PREFIX`, `_HOME_OVERRIDES`, `_KEYRING_OVERRIDES = {"PYTHON_KEYRING_BACKEND": "keyring.backends.null.Null"}`
- `_build_isolated_env(isolated_home: Path) -> dict[str, str]` at L78-104 — builds env via allowlist + MCP_ prefix + HOME redirect + null keyring backend
- Module docstring (L1-38) — first paragraph at L9 says `Isolation is ALWAYS-ON. No toggle, no --no-isolation CLI escape hatch.` Now FALSE; rewording required.
- Maintainer warning at L33-37 (`DO NOT widen _PASSTHROUGH_ALLOWLIST ...`) — stays in force.

SEED-022 constraint (locked by memory `project_framework_primitives_sdet_safety_principle.md`): `_isolation.py` does NOT import `config.py`. Dispatcher takes `mode: Literal[str]`, never `cfg: Config`. Caller (fixtures.py / mcp_client.py / _plugin.py — handled in plan 34-04) extracts the data and passes it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing dispatcher contract tests (RED phase)</name>
  <files>tests/framework/unit/test_isolation_dispatcher.py</files>
  <read_first>
    - tests/framework/unit/test_error_style.py (import + capsys + monkeypatch idiom that the new file mirrors)
    - .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-PATTERNS.md (section titled "NEW tests/framework/unit/test_isolation_dispatcher.py" — three load-bearing pins listed verbatim)
    - .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-RESEARCH.md (Open Question 6 — defensive assert pin rationale)
    - src/mcp_test_framework/_isolation.py (full file — strict-branch invariant must be byte-for-byte identical to legacy `_build_isolated_env`)
  </read_first>
  <behavior>
    - `test_passthrough_returns_dict_of_os_environ`: under `monkeypatch`-controlled parent env, `_build_subprocess_env('passthrough', None)` returns a dict equal to `dict(os.environ)` (D-05 literal copy invariant).
    - `test_strict_delegates_to_build_isolated_env`: with `isolated_home = tmp_path / "iso"`, `_build_subprocess_env('strict', isolated_home)` returns a dict equal to `_build_isolated_env(isolated_home)` (byte-for-byte regression invariant).
    - `test_strict_with_none_isolated_home_raises_assertion_error`: `_build_subprocess_env('strict', None)` raises `AssertionError` (defensive guard per Open Question 6).
    - All three tests FAIL on first run because the functions don't exist yet. RED phase.
  </behavior>
  <action>
Create the file `tests/framework/unit/test_isolation_dispatcher.py` with exactly the following Python source. Use the Write tool; do not use heredoc.

File contents (copy verbatim):

  - Module docstring describing the three invariants and SEED-022 constraint.
  - `from __future__ import annotations`
  - `import os`
  - `from pathlib import Path`
  - `import pytest`

Three test functions:

(1) `test_passthrough_returns_dict_of_os_environ(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None`
    - Import `_build_subprocess_env` from `mcp_test_framework._isolation` inside the body.
    - `monkeypatch.setenv("MCPTF_TEST_PASSTHROUGH_MARKER", "operator-credential")`
    - `monkeypatch.delenv("PYTHON_KEYRING_BACKEND", raising=False)`
    - `env = _build_subprocess_env("passthrough", None)`
    - `assert env == dict(os.environ)` (the load-bearing invariant)
    - `assert env.get("MCPTF_TEST_PASSTHROUGH_MARKER") == "operator-credential"`
    - Assert that PYTHON_KEYRING_BACKEND is NOT the null-keyring value: `assert env.get("PYTHON_KEYRING_BACKEND") != "keyring.backends.null.Null"`

(2) `test_strict_delegates_to_build_isolated_env(tmp_path: Path, monkeypatch: pytest.MonkeyPatch) -> None`
    - Import both `_build_isolated_env` and `_build_subprocess_env`.
    - `isolated_home = tmp_path / "iso"; isolated_home.mkdir()`
    - `monkeypatch.setenv("MCP_TEST_VAR", "x")` (seeds a deterministic parent env entry that passes through the MCP_ prefix filter)
    - `legacy = _build_isolated_env(isolated_home)`
    - `new = _build_subprocess_env("strict", isolated_home)`
    - `assert new == legacy` (byte-for-byte regression invariant — the load-bearing one)
    - `assert new.get("PYTHON_KEYRING_BACKEND") == "keyring.backends.null.Null"`
    - Cross-platform HOME assertion: `assert new.get("HOME") == str(isolated_home) or new.get("USERPROFILE") == str(isolated_home)` (Windows writes USERPROFILE, POSIX writes HOME; current `_HOME_OVERRIDES` may include both)

(3) `test_strict_with_none_isolated_home_raises_assertion_error() -> None`
    - Import `_build_subprocess_env`.
    - `with pytest.raises(AssertionError): _build_subprocess_env("strict", None)`

The tests MUST fail on first run because `_build_subprocess_env` does not exist yet. The expected failure is an `ImportError` or `ModuleNotFoundError` during collection on the `from mcp_test_framework._isolation import _build_subprocess_env` line — NOT an AssertionError. Verify the failure shape in acceptance criteria below.
  </action>
  <verify>
    <automated>uv run pytest tests/framework/unit/test_isolation_dispatcher.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/framework/unit/test_isolation_dispatcher.py` exits 0
    - `grep -nE "def test_passthrough_returns_dict_of_os_environ" tests/framework/unit/test_isolation_dispatcher.py` exits 0
    - `grep -nE "def test_strict_delegates_to_build_isolated_env" tests/framework/unit/test_isolation_dispatcher.py` exits 0
    - `grep -nE "def test_strict_with_none_isolated_home_raises_assertion_error" tests/framework/unit/test_isolation_dispatcher.py` exits 0
    - `grep -nE "_build_subprocess_env" tests/framework/unit/test_isolation_dispatcher.py | wc -l` returns at least 3
    - `uv run pytest tests/framework/unit/test_isolation_dispatcher.py -x` exits NON-zero (RED phase — functions don't exist yet)
    - The failure output references `_build_subprocess_env` and `ImportError` (or `cannot import name`), NOT AssertionError
  </acceptance_criteria>
  <done>
    Three failing tests exist that pin the dispatcher's contract. Tests fail with ImportError because `_build_subprocess_env` is not yet defined. Task 2 makes them GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add `_build_passthrough_env()` + `_build_subprocess_env()` dispatcher + rework module docstring</name>
  <files>src/mcp_test_framework/_isolation.py</files>
  <read_first>
    - src/mcp_test_framework/_isolation.py (full file — see existing `_build_isolated_env` at L78-104, module docstring at L1-38, and the maintainer warning at L33-37)
    - .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-PATTERNS.md (section "src/mcp_test_framework/_isolation.py" — verbatim function bodies + docstring rewording)
    - .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-CONTEXT.md (D-05, D-07, D-08 — operational decisions the code must implement)
    - .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-RESEARCH.md (Open Question 4 — docstring rewording recommendation; Open Question 6 — assert guard)
    - tests/framework/unit/test_isolation_dispatcher.py (just-written test file — see what behavior the code must satisfy)
  </read_first>
  <behavior>
    - Adding new functions does NOT modify `_build_isolated_env` signature or body — backwards compatibility for plan 34-04's spawn-site swap.
    - `_build_passthrough_env()` returns `dict(os.environ)`. No filtering, no key injection, no override of any kind.
    - `_build_subprocess_env(mode, isolated_home)` branches on `mode`:
      - `mode == 'passthrough'` → return `_build_passthrough_env()` (passes `isolated_home` is unused)
      - `mode == 'strict'` → `assert isolated_home is not None, ...` then return `_build_isolated_env(isolated_home)`
    - `Literal` is imported from `typing` (add the import if not present).
    - `_isolation.py` does NOT import `config.py` (verified by grep in acceptance criteria).
    - Module docstring's first paragraph at L9 reworded; maintainer warning at L33-37 retained verbatim.
    - All three tests from Task 1 now pass (GREEN phase).
  </behavior>
  <action>
Open `src/mcp_test_framework/_isolation.py` and make four coordinated edits:

(A) Imports — verify the following are present at the top of the file; add any that are missing:

  - `from pathlib import Path`
  - `from typing import Literal`

`Literal` may already be imported via `from typing import Literal` or `from typing_extensions import Literal` — use whichever the file already prefers; if neither is present, add `from typing import Literal` alongside the existing typing imports.

(B) Module docstring rewording (line 9 area). The current line reads (approximately):

```
- Isolation is ALWAYS-ON. No toggle, no ``--no-isolation`` CLI escape hatch.
```

Replace it with:

```
- Isolation is ON BY DEFAULT (``host_isolation='strict'``) and disabled
  per-config-file via ``host_isolation='passthrough'``. The maintainer
  warning below stays in force for the strict allowlist; passthrough is
  all-or-nothing by design (no per-var widening, no per-tool toggle).
```

Maintain the bullet shape and surrounding lines exactly. Do NOT touch the maintainer warning at lines 33-37 (`DO NOT widen _PASSTHROUGH_ALLOWLIST ...`) — it remains in force for strict mode.

(C) Add `_build_passthrough_env` immediately after the existing `_build_isolated_env` function (so legacy strict-mode builder stays first; passthrough sibling appears second; dispatcher third). Function body, verbatim:

```python
def _build_passthrough_env() -> dict[str, str]:
    """Return a literal ``dict(os.environ)`` copy -- passthrough mode contract (D-05).

    Operator opted into ``host_isolation='passthrough'``: the spawned MCP subprocess
    sees the operator's full env, including HOME / USERPROFILE / TEMP and the
    operator's keyring backend. Nothing stripped, nothing injected. The hermetic
    property is explicitly traded for credential reachability.

    SEED-022: this primitive takes no Config blob -- the caller passed the mode.
    """
    return dict(os.environ)
```

(D) Add `_build_subprocess_env` dispatcher immediately after `_build_passthrough_env`. Function body, verbatim:

```python
def _build_subprocess_env(
    mode: Literal['strict', 'passthrough'],
    isolated_home: Path | None,
) -> dict[str, str]:
    """Dispatch to the right env builder based on ``host_isolation`` mode (D-07/D-08).

    Spawn-site callers (``fixtures.py``, ``mcp_client.py``, ``_plugin.py``) pass
    ``cfg.host_isolation`` as ``mode`` and the resolved ``_isolated_home`` Path
    (or None under passthrough where the tempdir is unallocated per D-06).

    Strict mode REQUIRES an ``isolated_home`` -- the assertion is a defensive
    guard against caller drift; passthrough mode ignores it.
    """
    if mode == 'passthrough':
        return _build_passthrough_env()
    assert isolated_home is not None, (
        "_build_subprocess_env(mode='strict') requires isolated_home; "
        "caller must allocate the tempdir before invoking strict-mode dispatch"
    )
    return _build_isolated_env(isolated_home)
```

DO NOT change `_build_isolated_env` — its signature, body, and name stay as-is (Deferred Idea: rename to `_build_strict_env` is OUT of scope per CONTEXT.md `<deferred>`). Existing callers continue to call it directly until plan 34-04 reroutes them through `_build_subprocess_env`.

DO NOT import from `src/mcp_test_framework/config.py`. The dispatcher takes plain data (`mode: str`, `isolated_home: Path | None`). Verified by grep in acceptance criteria.

Run the test file from Task 1 — all three tests should now PASS (GREEN phase).
  </action>
  <verify>
    <automated>uv run pytest tests/framework/unit/test_isolation_dispatcher.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "^def _build_passthrough_env\(\) -> dict\[str, str\]:" src/mcp_test_framework/_isolation.py` exits 0
    - `grep -nE "^def _build_subprocess_env\(" src/mcp_test_framework/_isolation.py` exits 0
    - `grep -nE "mode: Literal\['strict', ?'passthrough'\]" src/mcp_test_framework/_isolation.py` exits 0
    - `grep -nE "isolated_home: Path \| None" src/mcp_test_framework/_isolation.py` exits 0
    - `grep -nE "return _build_isolated_env\(isolated_home\)" src/mcp_test_framework/_isolation.py` exits 0 (strict branch delegates verbatim)
    - `grep -nE "return dict\(os\.environ\)" src/mcp_test_framework/_isolation.py` exits 0 (passthrough body is a literal copy)
    - `grep -cE "from mcp_test_framework\.config|from .config|from mcp_test_framework import config" src/mcp_test_framework/_isolation.py` returns 0 (NO config import — SEED-022 invariant)
    - `grep -cE "Isolation is ALWAYS-ON" src/mcp_test_framework/_isolation.py` returns 0 (old docstring removed)
    - `grep -nE "ON BY DEFAULT.*host_isolation='strict'" src/mcp_test_framework/_isolation.py` exits 0 (new docstring present)
    - `grep -nE "DO NOT widen" src/mcp_test_framework/_isolation.py` exits 0 (maintainer warning retained)
    - `grep -nE "^def _build_isolated_env\(isolated_home: Path\) -> dict\[str, str\]:" src/mcp_test_framework/_isolation.py` exits 0 (legacy function unchanged)
    - `uv run pytest tests/framework/unit/test_isolation_dispatcher.py -x` exits 0 (all three tests GREEN)
    - `uv run pytest tests/framework/ -x` exits 0 (no broader regression)
  </acceptance_criteria>
  <done>
    Two new functions exist in `_isolation.py` (passthrough builder + dispatcher). Legacy `_build_isolated_env` is unchanged. Module docstring is reworded; maintainer warning is retained. No `config.py` import. All three Task 1 tests pass (GREEN); no other framework test regresses.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator host env → MCP subprocess env | Crosses spawn-site seam under passthrough; full os.environ is forwarded |
| _isolation.py primitive → caller | Dispatcher takes plain data; SEED-022 prevents framework reasoning about which env vars are "safe" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03-01 | Information Disclosure | `_build_passthrough_env` exposes operator credentials | accept | Operator-opt-in by design (D-05). The hermetic property is explicitly traded for credential reachability. Documented in plan 34-08; warned via plan 34-05 xdist clamp banner; SEED-022 places safety on the operator. |
| T-34-03-02 | Tampering | Caller mis-passes `mode='strict'` without `isolated_home` → silent passthrough | mitigate | `assert isolated_home is not None` guard; pinned by `test_strict_with_none_isolated_home_raises_assertion_error`. Caller error fails fast at the spawn-site, never silently degrades to passthrough. |
| T-34-03-03 | Elevation of Privilege | _isolation.py imports config.py → SEED-022 violation | mitigate | Acceptance criterion `grep -c "from mcp_test_framework.config" src/mcp_test_framework/_isolation.py` returns 0; dispatcher takes plain `Literal[str]` and `Path \| None`. |
| T-34-03-04 | Repudiation | Strict-mode behavior drift from legacy `_build_isolated_env` | mitigate | `test_strict_delegates_to_build_isolated_env` pins byte-for-byte equivalence between dispatcher's strict branch and the legacy builder — any drift in either fails the test. |
</threat_model>

<verification>
- `uv run pytest tests/framework/unit/test_isolation_dispatcher.py -x` passes (three new pins).
- `uv run pytest tests/framework/ -x` passes (no regression from existing isolation consumers — they still call `_build_isolated_env` directly until plan 34-04).
- `grep -cE "from mcp_test_framework\.config|from \.config" src/mcp_test_framework/_isolation.py` returns 0.
- `grep -nE "_build_subprocess_env" src/mcp_test_framework/_isolation.py` shows two occurrences (function def + maybe a docstring reference).
- `grep -nE "Isolation is ALWAYS-ON" src/mcp_test_framework/_isolation.py` returns nothing (old line removed).
</verification>

<success_criteria>
- `_build_passthrough_env()` and `_build_subprocess_env(mode, isolated_home)` exist in `_isolation.py` with the exact signatures specified.
- `_build_isolated_env` is unchanged (callers in `fixtures.py:432` and `mcp_client.py:189` continue working until plan 34-04 swaps them).
- Module docstring reworded; maintainer warning retained.
- `_isolation.py` does NOT import `config.py` (SEED-022 invariant pinned by grep).
- Three load-bearing dispatcher contract tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-03-SUMMARY.md` with:
- Final signatures of `_build_passthrough_env` and `_build_subprocess_env` pasted verbatim.
- New module-docstring line pasted (for plan 34-08 docs to reference the canonical wording).
- Confirmation that `_build_isolated_env` body + signature are byte-for-byte unchanged.
- Confirmation that `_isolation.py` does not import config.py (SEED-022 invariant).
</output>
