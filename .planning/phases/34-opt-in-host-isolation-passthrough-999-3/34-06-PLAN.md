---
phase: 34-opt-in-host-isolation-passthrough-999-3
plan: 06
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - src/mcp_test_framework/test_code/session.py
  - tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py
  - .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md
autonomous: true
requirements: [ISOL-05]
tags: [bare-config-audit, stash-routing, test-code-fixture]

must_haves:
  truths:
    - "Every bare `Config()` call site in `src/` is documented in `34-BARE-CONFIG-AUDIT.md` with category + remediation"
    - "Every bare `Config()` call site in `tests/` is documented in `34-BARE-CONFIG-AUDIT.md` (categorized inventory, not per-site refactor)"
    - "`test_code/session.py:67` is routed through the plugin stash with bare-Config fallback preserved (RESEARCH Finding 2 + Open Question 3 recommendation (b))"
    - "`tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py:67, 196` receive 2-line ISOL-05 inline audit comments"
    - "`tests/framework/unit/test_sdet_fixtures.py:_install_session_config` monkeypatch shim stays load-bearing — bare-Config fallback preserved at session.py:67"
  artifacts:
    - path: ".planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md"
      provides: "Categorized inventory of every bare `Config()` site in `src/` + `tests/` — the ISOL-05 audit deliverable"
      contains: "src/mcp_test_framework/test_code/session.py:67"
    - path: "src/mcp_test_framework/test_code/session.py"
      provides: "Stash-routing remediation; `request: pytest.FixtureRequest` added to `mcp_session` fixture; bare-Config fallback preserved"
      contains: "getattr(request.session.config, \"_mcp_contracts_config\", None)"
    - path: "tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py"
      provides: "Two 2-line inline ISOL-05 audit comments at the bare-Config sites"
      contains: "ISOL-05 audit"
  key_links:
    - from: "src/mcp_test_framework/test_code/session.py:mcp_session"
      to: "src/mcp_test_framework/_plugin.py:247 stash (`config._mcp_contracts_config`)"
      via: "stash-lookup-with-fallback pattern mirrors `fixtures.py:108-111`"
      pattern: "getattr\\(request\\.session\\.config, .._mcp_contracts_config.., None\\)"
---

<objective>
Complete the ISOL-05 bare-`Config()` audit deliverable + remediate the highest-risk site:

1. **Create `.planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md`** — categorized inventory of every bare `Config()` construction site in `src/` and `tests/`. This file IS the audit deliverable per CONTEXT.md Claude's Discretion ISOL-05 ("inventory is the deliverable; NOT a per-site refactor"). Use the inventory verbatim from RESEARCH Finding 4 + the "Bare `Config()` Inventory" tables.

2. **Remediate `src/mcp_test_framework/test_code/session.py:67`** — the highest-risk site (RESEARCH Finding 2). Add `request: pytest.FixtureRequest` to the `mcp_session` fixture signature and route through the stash. PRESERVE the bare-Config fallback so `tests/framework/unit/test_sdet_fixtures.py:_install_session_config` monkeypatch shim continues working (Open Question 3 recommendation (b) — the smaller change).

3. **Annotate `tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py:67, 196`** with 2-line ISOL-05 inline audit comments — operator-authored scenario tests that bare-construct Config and inherit the `strict` default.

Note: `fixtures.py:111` (the other `src/` bare-Config site) is annotated inside Plan 34-04 Task 1 (which already touches `fixtures.py`). Plan 34-06 does NOT modify `fixtures.py`.

Purpose: ISOL-05 — every bare `Config()` site in `src/` + `tests/` is identified, documented (which mode it implicitly assumes), and the highest-risk site routes through the resolved-config seam so neither mode silently leaks operator env across the spawn seam.

Output:
- One new audit document (`34-BARE-CONFIG-AUDIT.md`).
- `test_code/session.py:67` stash-routing remediation.
- Two 2-line inline comments in `test_proxmox_vm_lifecycle_readme_sample.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-CONTEXT.md
@.planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-RESEARCH.md
@.planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-PATTERNS.md
@.planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-01-SUMMARY.md
@src/mcp_test_framework/test_code/session.py
@src/mcp_test_framework/fixtures.py
@tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py
@tests/framework/unit/test_sdet_fixtures.py

<interfaces>
**Current `mcp_session` fixture surface** (`test_code/session.py:54-67`):

```python
@pytest_asyncio.fixture(loop_scope="session", scope="session")
async def mcp_session(mcp_client: McpTestClient):
    """Live ClientSession driver + active test-code registry."""
    ...
    # Step 3: load the generated package from cfg.test_code.generated_root/<slug>/.
    cfg = Config()  # ← BARE — bypasses operator YAML's host_isolation under passthrough
    generated_root = cfg.test_code.generated_root
    ...
```

**Stash-lookup-with-fallback canonical pattern** (`fixtures.py:108-111`):

```python
cfg = getattr(request.session.config, "_mcp_contracts_config", None)
if cfg is not None:
    return cfg
return Config()  # framework-self-test fallback
```

**Why the fallback must be preserved** (RESEARCH Open Question 3 + Finding 4):

`tests/framework/unit/test_sdet_fixtures.py` has a monkeypatch shim `_install_session_config` at L142-163 that stubs out the bare-Config import path. If the stash-routing path makes bare-Config dead code, the shim's pre-Phase-34 contract breaks. Recommendation (b) — keep the bare-Config fallback alive at session.py:67 — is the smaller change AND matches `fixtures.py:108-111`'s pattern.

**Inventory sources (RESEARCH Finding 4)**:

| `src/` actionable sites | 2 (not 3 — cli.py:15 is docstring, not a call site) |
| `tests/` actionable sites | 1 new defaults pin (already lands in plan 34-01) + 1 conditional update (test_sdet_fixtures — keep fallback alive avoids it) + 2 inline comments (test_proxmox_vm_lifecycle_readme_sample.py:67, 196) |
| Total document sites | 6 `src/` rows + 9 `tests/` rows in the inventory tables |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the `34-BARE-CONFIG-AUDIT.md` deliverable</name>
  <files>.planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md</files>
  <read_first>
    - .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-RESEARCH.md (FULL section "Bare `Config()` Inventory" — `src/` table + `tests/` table — these are the verbatim source rows)
    - .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-CONTEXT.md (Claude's Discretion ISOL-05 — "inventory is the deliverable, NOT a per-site refactor")
  </read_first>
  <behavior>
    - Audit document exists at `.planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md`.
    - Document contains two inventory tables: one for `src/` sites, one for `tests/` sites.
    - Each row carries: `file:line`, `Category` (e.g., "Mode-agnostic", "HIGH RISK", "Not a call site (docstring)"), `Recommended Remediation`.
    - Net `src/` actionable count documented as 2 (NOT 3 — cli.py:15 is a docstring, not a call site).
    - Net `tests/` actionable count documented as 1 add (defaults pin — landed in plan 34-01) + 2 inline-comment additions (the operator-authored scenario file — landed in this plan's Task 3) + 1 conditional update (test_sdet_fixtures — explicitly NOT updated because fallback preserved).
    - Document references the file paths of remediation plans (`34-01-PLAN.md` for the defaults pin, `34-04-PLAN.md` for `fixtures.py:111` annotation, `34-06-PLAN.md` Task 2/3 for `test_code/session.py:67` + scenario annotations).
  </behavior>
  <action>
Create the file `.planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md` with the following content:

A markdown file with these sections in order:

(1) **Front matter / header**: title `# Phase 34 ISOL-05 — Bare \`Config()\` Caller Audit`, audit date `**Audited:** 2026-05-26 (Phase 34 plan-time research)`, source pointer `**Source of truth:** \`34-RESEARCH.md\` Finding 4 + Bare \`Config()\` Inventory section.`

(2) **Summary paragraph**: explain that the audit is the ISOL-05 deliverable (CONTEXT.md Claude's Discretion locks "inventory is the deliverable, NOT a per-site refactor"), distinguishes 2 actionable `src/` call sites from doc-string references, and points at the four remediation plans (34-01, 34-04, 34-06 Tasks 2 + 3).

(3) **`src/` inventory table** — copy verbatim from RESEARCH Finding 4's `src/` table:

| file:line | Category | Recommended Remediation | Landing Plan |
|-----------|----------|-------------------------|--------------|
| `src/mcp_test_framework/fixtures.py:111` | Mode-agnostic; framework-self-test fallback when plugin stash absent | 2-line inline audit comment naming the implicit `strict` assumption | Plan 34-04 Task 1 |
| `src/mcp_test_framework/test_code/session.py:67` | **HIGH RISK** — runs after stash population during fixture resolution; bypasses operator YAML under passthrough | Add `request: pytest.FixtureRequest` to fixture signature; route through stash with bare-Config fallback (mirror `fixtures.py:108-111`) | Plan 34-06 Task 2 |
| `src/mcp_test_framework/cli.py:15` (docstring) | Not a call site — docstring reference inside `_load_config`'s docstring | No remediation | n/a |
| `src/mcp_test_framework/cli.py:471` (docstring) | Not a call site | No remediation | n/a |
| `src/mcp_test_framework/cli.py:551` (comment) | Not a call site | No remediation | n/a |
| `src/mcp_test_framework/fixtures.py:100, 208` (docstrings) | Not call sites | No remediation | n/a |

Footer: "Net `src/` actionable count: **2** (CONTEXT.md initially flagged 3; cli.py:15 is doc text, not a call site)."

(4) **`tests/` inventory table** — copy verbatim from RESEARCH Finding 4's `tests/` table:

| file:line | Category | Recommended Remediation | Landing Plan |
|-----------|----------|-------------------------|--------------|
| `tests/framework/smoke/test_smoke_homelab_mcp.py:40` (comment) | Not a call site | No remediation | n/a |
| `tests/framework/test_config_init_cli.py:104, 133` (comments / inline kwargs) | Tests mode directly via config-init scaffold round-trip | After plan 34-07 scaffold edit, verify the round-trip still passes | Plan 34-07 smoke check |
| `tests/framework/test_tool_config.py:36, 299` | Mode-agnostic; constructs `Config(test_code=stub)` for ToolConfig table tests | No remediation (strict default preserves behavior) | n/a |
| `tests/framework/unit/test_config.py:280, 287, 300, 314, 358, 365` | Mode-agnostic; pure Config-defaults validation | Add `test_host_isolation_default_is_strict` (Phase 35 SHIM-09 capstone pin) | Plan 34-01 Task 2 |
| `tests/framework/unit/test_mcp_config_fixture.py:11, 76, 100` (test bodies + comments) | Tests mode directly — Tier 1 stash hit, Tier 2 bare fallback | No remediation | n/a |
| `tests/framework/unit/test_runner_migration.py:101, 105, 118, 150, 166, 209` | Mode-agnostic; verifies env-var fallback removal (Phase 31 SHIM-05) | No remediation | n/a |
| `tests/framework/unit/test_sdet_fixtures.py:122, 147` | Tests `test_code/session.py:67`'s bare Config() via `_install_session_config` monkeypatch shim | CONDITIONAL — NOT updated because plan 34-06 Task 2 preserves bare-Config fallback at session.py:67 (Open Question 3 recommendation (b)) | n/a |
| `tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py:67, 196` | Operator-authored test-code scenario; bare Config() inside scenario body | 2-line inline ISOL-05 audit comment | Plan 34-06 Task 3 |
| `tests/framework/unit/test_config_init.py:100` (comment) | Not a call site | No remediation | n/a |

Footer: "Net `tests/` actionable count: **1 add** (defaults pin — Plan 34-01) + **2 inline comments** (Plan 34-06 Task 3) + **0 conditional updates** (test_sdet_fixtures preserved by fallback-alive recommendation (b))."

(5) **Closing section** — "## Anti-goals" — list the off-table directions per memory locks: no keyring faking, no per-env-var allowlist, no per-tool isolation mode, no SUT-aware safety reasoning. Cite the three memory entries (`project_isolation_blocks_live_uat.md`, `project_framework_primitives_sdet_safety_principle.md`, `feedback_phase_scope_intent.md`).

(6) **Cross-references**: list paths to the four landing plans + CONTEXT.md + RESEARCH.md + PATTERNS.md.

DO NOT add a per-site refactor proposal — that's explicitly out of scope per CONTEXT.md. DO NOT propose refactoring `fixtures.py:111` to remove the fallback — it's the framework-self-test path and the recommendation (b) preserves it. DO NOT propose deferring `test_code/session.py:67` to a separate audit-only phase — memory `feedback_phase_scope_intent.md` locks this audit IN-scope.
  </action>
  <verify>
    <automated>test -f .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md && wc -l .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md` exits 0
    - `grep -nE "^# Phase 34 ISOL-05" .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md` exits 0 (title present)
    - `grep -nE "test_code/session\.py:67" .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md` exits 0 (highest-risk site documented)
    - `grep -nE "fixtures\.py:111" .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md` exits 0 (mode-agnostic site documented)
    - `grep -nE "HIGH RISK" .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md` exits 0 (risk categorization present)
    - `grep -cE "Plan 34-(01|04|06|07)" .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md` returns at least 4 (cross-references to landing plans)
    - `grep -nE "no keyring faking" .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md` exits 0 (anti-goal cited)
    - `grep -cE "Not a call site" .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md` returns at least 4 (CONTEXT.md miscount distinguished from real call sites)
  </acceptance_criteria>
  <done>
    Audit deliverable exists at the canonical path with both inventory tables, anti-goals, cross-references, and the explicit "2 actionable src/ sites" finding that corrects CONTEXT.md's count of 3.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route `mcp_session` fixture through plugin stash with bare-Config fallback</name>
  <files>src/mcp_test_framework/test_code/session.py</files>
  <read_first>
    - src/mcp_test_framework/test_code/session.py (FULL file — see `mcp_session` fixture at L54-67 and existing imports)
    - src/mcp_test_framework/fixtures.py L100-111 (canonical stash-lookup-with-fallback pattern — copy idiom verbatim)
    - .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-PATTERNS.md (section "src/mcp_test_framework/test_code/session.py" — verbatim remediation snippet)
    - .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-RESEARCH.md (Finding 2 — stash timing safety; Open Question 3 recommendation (b) — keep fallback alive)
    - tests/framework/unit/test_sdet_fixtures.py L122-163 (verify the `_install_session_config` monkeypatch shim — fallback must keep it green)
  </read_first>
  <behavior>
    - `mcp_session` fixture signature gains `request: pytest.FixtureRequest` as the FIRST positional parameter (mcp_client moves to second).
    - Body's `cfg = Config()` is replaced with stash-lookup pattern:
      ```python
      cfg = getattr(request.session.config, "_mcp_contracts_config", None)
      if cfg is None:
          cfg = Config()  # framework-self-test fallback; mirrors fixtures.py:108-111
      ```
    - Bare `Config()` fallback is PRESERVED so `tests/framework/unit/test_sdet_fixtures.py:_install_session_config` monkeypatch shim continues working unchanged.
    - All existing test_code tests pass under default strict mode AND under the `_install_session_config` monkeypatch shim.
  </behavior>
  <action>
Edit `src/mcp_test_framework/test_code/session.py`:

(A) Imports — verify `pytest` is imported. The fixture currently uses `pytest_asyncio.fixture` so `pytest_asyncio` is imported; add `import pytest` if not present at module top.

(B) Locate the `mcp_session` fixture at L54 area:

```python
@pytest_asyncio.fixture(loop_scope="session", scope="session")
async def mcp_session(mcp_client: McpTestClient):
    """..."""
    ...
    # Step 3: load the generated package from cfg.test_code.generated_root/<slug>/.
    cfg = Config()
    generated_root = cfg.test_code.generated_root
    ...
```

(C) Modify the signature to add `request: pytest.FixtureRequest` as the first parameter:

```python
@pytest_asyncio.fixture(loop_scope="session", scope="session")
async def mcp_session(
    request: pytest.FixtureRequest,
    mcp_client: McpTestClient,
):
```

(D) Replace the bare `cfg = Config()` line with the stash-lookup-with-fallback pattern. The exact lines to replace:

Before:
```python
    # Step 3: load the generated package from cfg.test_code.generated_root/<slug>/.
    cfg = Config()
    generated_root = cfg.test_code.generated_root
```

After:
```python
    # Step 3: load the generated package from cfg.test_code.generated_root/<slug>/.
    # ISOL-05 (Phase 34): route through plugin stash so operator's host_isolation
    # (and other YAML overrides) reach the fixture. Bare-Config fallback preserved
    # for framework self-tests + test_sdet_fixtures._install_session_config shim.
    cfg = getattr(request.session.config, "_mcp_contracts_config", None)
    if cfg is None:
        cfg = Config()  # framework-self-test fallback; mirrors fixtures.py:108-111
    generated_root = cfg.test_code.generated_root
```

DO NOT delete the bare `Config()` fallback — Open Question 3 recommendation (b) explicitly preserves it. Recommendation (a) would require updating `tests/framework/unit/test_sdet_fixtures.py:_install_session_config`; (b) is the smaller change.

DO NOT add a `mcp_config` fixture dependency — Open Question 3 explicitly rejected this alternative because it changes the public dependency list of the fixture.

DO NOT change the fixture's `scope` or `loop_scope` — both remain `"session"`.

After editing, run `tests/framework/unit/test_sdet_fixtures.py` to verify the monkeypatch shim still works.
  </action>
  <verify>
    <automated>uv run pytest tests/framework/ -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "async def mcp_session\(" src/mcp_test_framework/test_code/session.py` exits 0 (fixture still exists)
    - `grep -nE "request: pytest\.FixtureRequest" src/mcp_test_framework/test_code/session.py` exits 0 (new parameter present)
    - `grep -nE 'getattr\(request\.session\.config, "_mcp_contracts_config", None\)' src/mcp_test_framework/test_code/session.py` exits 0 (stash-lookup added)
    - `grep -nE "framework-self-test fallback" src/mcp_test_framework/test_code/session.py` exits 0 (fallback comment preserved)
    - `grep -cE "cfg = Config\(\)" src/mcp_test_framework/test_code/session.py` returns 1 (one bare-Config remains — the fallback inside the `if cfg is None:` branch)
    - `grep -nE "ISOL-05" src/mcp_test_framework/test_code/session.py` exits 0 (audit reference present in comment)
    - `uv run pytest tests/framework/unit/test_sdet_fixtures.py -x` exits 0 (monkeypatch shim still works — Open Question 3 recommendation (b) verified)
    - `uv run pytest tests/framework/ -x` exits 0 (no broader regression)
  </acceptance_criteria>
  <done>
    `mcp_session` fixture takes `request: pytest.FixtureRequest`, routes through stash for the operator-real path, falls back to bare `Config()` for framework self-tests. `_install_session_config` monkeypatch shim remains load-bearing. No framework test regresses.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add 2-line ISOL-05 audit comments to operator-authored Proxmox scenario test</name>
  <files>tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py</files>
  <read_first>
    - tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py (FULL file — see L67 and L196 where bare `Config()` appears)
    - .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-PATTERNS.md (section "tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py" — verbatim comment template)
    - .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md (will be created by Task 1 — cross-reference the table row)
  </read_first>
  <behavior>
    - The bare-Config sites at L67 and L196 each receive a 2-line inline comment immediately above the `Config()` call.
    - The comment names the ISOL-05 audit, states the implicit `'strict'` mode assumption, and notes that the scenario does NOT consume the `host_isolation` field (so the latent bug under passthrough is harmless for this scenario).
    - No behavior change; pure documentation annotation.
  </behavior>
  <action>
Open `tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py` and locate the bare `Config()` calls. Per RESEARCH Finding 4, they appear at L67 and L196. Grep first to confirm exact line numbers:

```
grep -nE "= Config\(\)" tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py
```

For EACH `= Config()` site found, insert these two comment lines immediately above (matching the indent of the `Config()` line):

```python
    # NOTE (ISOL-05 audit, Phase 34): bare Config() in operator-authored scenario.
    # Inherits host_isolation='strict' default; scenario does not consume the field.
```

Match the existing indentation precisely. If the `Config()` call is at the function-body indent (4 spaces), the comments are at 4 spaces. If it's at an inner block indent (8 spaces), match that.

DO NOT modify the `Config()` call itself. DO NOT add a `request: pytest.FixtureRequest` parameter to the test function — operator-authored scenarios are intentionally simple per CONTEXT.md ("scenarios construct bare Config() ... and are intentionally simple"). DO NOT route through the stash — this is operator persona code, not framework code.

If grep reveals MORE than two bare `Config()` sites (e.g., due to file growth since RESEARCH was written), annotate ALL of them with the same 2-line comment block. If grep reveals FEWER than two (e.g., one was already removed), annotate the remaining ones and note the discrepancy in the SUMMARY file.
  </action>
  <verify>
    <automated>uv run pytest tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py -x --co</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "ISOL-05 audit, Phase 34" tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py` returns at least 2 (one per bare-Config site)
    - `grep -cE "Inherits host_isolation='strict' default" tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py` returns at least 2
    - The number of `ISOL-05 audit` occurrences equals the number of `= Config()` occurrences in the file (each bare-Config site is annotated): `[ "$(grep -cE 'ISOL-05 audit' tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py)" = "$(grep -cE '= Config\(\)' tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py)" ]` succeeds
    - `uv run pytest tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py -x --co` exits 0 (test file still collects cleanly; no syntax error introduced by comment placement)
  </acceptance_criteria>
  <done>
    Every bare `Config()` call site in the Proxmox scenario file carries a 2-line ISOL-05 audit comment. Test file still collects without error. No behavior change.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `test_code/session.py` fixture → operator YAML's host_isolation | Bare `Config()` bypass under passthrough = missed operator opt-in (latent before Phase 34; remediated here) |
| Framework self-test path → `mcp_session` fixture | Monkeypatch shim `_install_session_config` must keep working; fallback path preserves this contract |
| Operator-authored scenarios → bare `Config()` | Inherits strict default; latent passthrough bug acceptable because scenarios don't consume the field — annotated, not refactored |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-06-01 | Information Disclosure | `test_code/session.py:67` bare Config() bypasses operator YAML; under passthrough, scenarios silently run with strict-mode `cfg.test_code.generated_root` instead of operator-overridden value | mitigate | Task 2 routes through plugin stash; bare-Config fallback preserved for framework self-tests. RESEARCH Finding 2 confirms stash is populated by the time `mcp_session` resolves. |
| T-34-06-02 | Tampering | Bare-Config fallback removed → `_install_session_config` monkeypatch shim in `test_sdet_fixtures.py` breaks → CI red | mitigate | Open Question 3 recommendation (b) — keep fallback alive — explicitly preserves the shim. Acceptance criterion runs `pytest tests/framework/unit/test_sdet_fixtures.py` to verify. |
| T-34-06-03 | Repudiation | Operator-authored scenarios silently miss passthrough opt-in for fields they don't consume → no visible harm, but cumulative audit drift | accept | 2-line inline comments at each bare-Config site document the implicit assumption + the field-non-consumption clause. Annotation, not refactor, per CONTEXT.md "audit is a deliverable, not a refactor." |
| T-34-06-04 | Tampering | Audit document drift — new bare-Config sites land in `src/` post-Phase-34 without inventory update | accept | Phase 35 SHIM-09 regression gate is the cross-milestone enforcement; Phase 34's deliverable is the v1.5-shipped inventory. Future phases own future-site tracking. |
</threat_model>

<verification>
- `uv run pytest tests/framework/unit/test_sdet_fixtures.py -x` passes (Open Question 3 recommendation (b) verified — monkeypatch shim still works).
- `uv run pytest tests/framework/ -x` passes (no broader regression).
- `test -f .planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-BARE-CONFIG-AUDIT.md` exits 0.
- `grep -cE "= Config\(\)" src/mcp_test_framework/test_code/session.py` returns 1 (only the fallback, not the original bare site).
- `grep -nE "ISOL-05 audit" tests/test_code/test_proxmox_vm_lifecycle_readme_sample.py` finds at least 2 sites.
</verification>

<success_criteria>
- Audit deliverable (`34-BARE-CONFIG-AUDIT.md`) exists with both inventory tables, anti-goals, cross-references.
- `test_code/session.py:67` routes through plugin stash with bare-Config fallback preserved.
- `_install_session_config` monkeypatch shim in `test_sdet_fixtures.py` still works (recommendation (b) verified).
- Operator-authored Proxmox scenario file carries 2-line ISOL-05 audit comments at every bare-Config site.
- Net actionable count documented: 2 src/ sites (NOT 3 — CONTEXT.md miscount corrected).
</success_criteria>

<output>
After completion, create `.planning/phases/34-opt-in-host-isolation-passthrough-999-3/34-06-SUMMARY.md` with:
- Path to the audit deliverable + count of inventory rows (src/ + tests/).
- Final `mcp_session` fixture signature pasted (showing `request: pytest.FixtureRequest`).
- Final stash-lookup block pasted (showing both branches — stash hit + bare fallback).
- Number of bare-Config sites annotated in `test_proxmox_vm_lifecycle_readme_sample.py` (should be 2, or note the discrepancy).
- Confirmation that `_install_session_config` monkeypatch shim works unchanged (recommendation (b) verified).
</output>
