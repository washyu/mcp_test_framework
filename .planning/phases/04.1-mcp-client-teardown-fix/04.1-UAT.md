---
status: complete
phase: 04.1-mcp-client-teardown-fix
source: [04.1-01-SUMMARY.md]
started: 2026-05-06
updated: 2026-05-06
---

## Current Test

[testing complete]

## Tests

### 1. Green-path live sweep exits 0
expected: |
  `uv run pytest tests/test_homelab_list_registered_servers.py -v` against the
  green-path target (`TARGET_TOOL_NAME=list_keyring_credentials`) exits with
  `EXIT_CODE=0`. Final summary line shows `10 passed in NN.NNs`. The captured
  output contains zero occurrences of `Attempted to exit cancel scope` and no
  `ERROR at teardown of` lines. (Mirrors Phase 04.1 Plan success criterion #1
  via the optional retry path.)
result: pass

### 2. No leftover homelab-mcp.exe after a run (Windows SC#1)
expected: |
  Immediately after the run from Test 1 (or any pytest invocation that uses the
  `mcp_client` fixture), `Get-Process homelab-mcp -ErrorAction SilentlyContinue`
  returns no output (no matching processes). No zombie subprocess survives
  fixture teardown. (Phase 04.1 success criterion #3.)
result: pass

### 3. Regression smoke test guards against future fixture reshuffles
expected: |
  `uv run pytest tests/smoke/test_mcp_client_teardown_regression.py::test_mcp_client_teardown_no_cancel_scope_error -v`
  with `MCPTF_CONFIG_FILE` set EITHER passes (`1 passed`, fully exercising the
  outer subprocess assertion against a clean child run) OR skips with a clear
  reason (`uvx not on PATH`, `config.yaml absent`, or child returncode 2 from
  `_preflight`). It MUST NOT fail or error.
result: pass

### 4. Existing unit suite still green after the rewrite
expected: |
  `uv run pytest tests/unit/ -x -q` exits 0 with all 56 collected tests
  passing. The fixture rewrite touches no unit-level code, but this is the
  fast belt-and-suspenders check for unintended import-side regressions in
  `src/mcp_test_framework/fixtures.py` or `src/mcp_test_framework/mcp_client.py`.
result: pass

### 5. DEF-04-03-B is documented as resolved
expected: |
  Open `.planning/phases/04-fixtures-test-cases/04-03-SUMMARY.md`. The Deferred
  Issues table row for `DEF-04-03-B` shows Status `**resolved (Phase 04.1)**`
  with a 2026-05-06 resolution sentence appended to the description, and the
  YAML frontmatter contains `requirements-resolved-by-followup: [DEF-04-03-B]`
  with no remaining `**reassigned to Phase 04.1**` text in the file. (Phase
  04.1 success criterion #5.)
result: pass

## Summary

total: 5
passed: 5
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none yet]
