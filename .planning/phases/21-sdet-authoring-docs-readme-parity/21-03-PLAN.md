---
phase: 21-sdet-authoring-docs-readme-parity
plan: "03"
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [DOC-SDET-02]
tags: [docs, claude-md, persona-note, dual-persona]

must_haves:
  truths:
    - "CLAUDE.md's '## What This Project Is' section, which Claude reads first when entering this repo, mentions the dual operator+SDET persona model and points to docs/SDET-AUTHORING.md."
    - "The note is 1-3 sentences appended to the existing section (no new H2 added — D-03)."
    - "Reading CLAUDE.md gives Claude enough signal to route an SDET-flavored question to docs/SDET-AUTHORING.md instead of treating tests/sdet/ as untyped territory."
  artifacts:
    - path: "CLAUDE.md"
      provides: "Dual-persona note appended to ## What This Project Is"
      contains: "SDET"
      contains_link: "docs/SDET-AUTHORING.md"
  key_links:
    - from: "CLAUDE.md ## What This Project Is"
      to: "docs/SDET-AUTHORING.md"
      via: "inline reference in appended note"
      pattern: "docs/SDET-AUTHORING\\.md"
---

<objective>
Append a 1–3 sentence note to CLAUDE.md's existing `## What This Project Is` section noting the v1.3 SDET persona addition and pointing to `docs/SDET-AUTHORING.md` (D-03).

Purpose: Claude entering this repo reads CLAUDE.md first. Without this note, an SDET-flavored question ("how do I write a tests/sdet/ scenario?") gets answered from general knowledge or by scanning the codebase. With this note, Claude knows there's a canonical authoring doc and routes the user there.

Output: Updated `CLAUDE.md` with the note appended to the existing section. No new H2. No new section heading. Minimal churn.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-sdet-authoring-docs-readme-parity/21-CONTEXT.md
@CLAUDE.md
@docs/SDET-AUTHORING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append dual-persona note to CLAUDE.md '## What This Project Is' section</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md (specifically lines 9–17, the `## What This Project Is` section — must not disturb the existing 3 paragraphs or the numbered list of test categories)
    - .planning/phases/21-sdet-authoring-docs-readme-parity/21-CONTEXT.md (D-03 — 1–3 sentence budget, append to existing section, no new H2)
    - docs/SDET-AUTHORING.md (verify the link target exists)
  </read_first>
  <action>
In `CLAUDE.md`, locate the `## What This Project Is` section (line 9). The section currently ends at line 17 with the paragraph: "The framework is deliberately narrow for the MVP — see the 'Out of Scope' and 'Future Work' sections of the spec before suggesting generalizations."

Append the following paragraph IMMEDIATELY AFTER that closing paragraph (line 17), and BEFORE the blank line that precedes `## Tooling` (line 19). The new paragraph stands as its own paragraph (preceded by a blank line).

Exact text:

```
v1.3 added the **SDET** persona as a second first-class user alongside the operator. The operator runs the contract pass (schema + description + output checks); the SDET authors stateful scenarios under `tests/sdet/` that exercise the MCP server end-to-end. Both personas share the same CLI surface — SDET adds the `--sdet` flag, the `mcp_test_framework.sdet` import surface (`mcp_session`, `tool()`, `ToolCallError`), and the `tests/sdet/` discovery scope. See [`docs/SDET-AUTHORING.md`](docs/SDET-AUTHORING.md) for the authoring walkthrough.
```

Length check: this is 3 sentences (counting the imperative final sentence as a sentence). D-03 budget is 1–3 sentences — this is at the upper bound. Do NOT exceed 3 sentences; if rewording for flow makes it 4, drop the second sentence's clause about "schema + description + output checks" to keep the budget.

Constraints:
- Do NOT add a new H2 like `## Personas` (D-03 explicit).
- Do NOT modify any other section of CLAUDE.md (operator might run `/gsd-profile-user` later; that section is reserved).
- Do NOT modify the existing 3 paragraphs of `## What This Project Is` (the MVP framing is still accurate — v1.3 builds on top, doesn't replace).
- The link uses the same markdown style as docs/EXTENDING.md references elsewhere in the repo.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$c = Get-Content CLAUDE.md -Raw ; $c -match 'v1\.3 added the \*\*SDET\*\* persona' ; $c -match 'docs/SDET-AUTHORING\.md' ; $c -match 'mcp_test_framework\.sdet' ; -not ($c -match '## Personas')"</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` `## What This Project Is` section contains literal `SDET` (the persona name)
    - `CLAUDE.md` contains literal markdown link `(docs/SDET-AUTHORING.md)`
    - `CLAUDE.md` contains literal `mcp_test_framework.sdet` (the public import surface namespace)
    - `CLAUDE.md` does NOT contain a new H2 `## Personas` or similar (D-03)
    - The appended paragraph is positioned AFTER `## What This Project Is` content and BEFORE `## Tooling` H2 (line-number check)
    - The appended paragraph is 3 sentences or fewer (D-03 budget — verifiable by counting `.` or `!` end-of-sentence markers in the appended block, allowing for inline code that contains `.`)
    - No other section of CLAUDE.md changed (verifiable via `git diff CLAUDE.md` showing only the insertion between `## What This Project Is` and `## Tooling`)
  </acceptance_criteria>
  <done>CLAUDE.md `## What This Project Is` includes the dual-persona note. Claude entering this repo learns about the SDET persona surface within the first section it reads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Doc-only change to a Claude-instructions file. No executable code paths added, no input handling, no I/O, no secrets, no auth, no schema changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | n/a | No threat surface — doc-only changes. CLAUDE.md modifies Claude's reading context only; no runtime impact on the framework. Threats: none. |

</threat_model>

<verification>
- `CLAUDE.md` contains the new paragraph between `## What This Project Is` and `## Tooling`
- Paragraph mentions SDET, mcp_test_framework.sdet, and links docs/SDET-AUTHORING.md
- Paragraph is 1–3 sentences (D-03 budget)
- No new H2 added
- `git diff --name-only -- src/ | wc -l` returns 0 (no src/ changes)
</verification>

<success_criteria>
- D-03 satisfied: dual-persona note appended to existing section (no new H2, 1–3 sentence budget honored).
- Claude reading CLAUDE.md first now has a routing signal to docs/SDET-AUTHORING.md for SDET-flavored questions.
</success_criteria>

<output>
After completion, create `.planning/phases/21-sdet-authoring-docs-readme-parity/21-03-SUMMARY.md` describing: the exact text appended (paste it verbatim), the line range it occupies, and confirmation that no other CLAUDE.md section was touched.
</output>
