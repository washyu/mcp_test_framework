---
phase: 30-cli-demotion-carry-forward-uat-closure-docs-rewrite
plan: 04
type: execute
wave: 2
depends_on: [30-01, 30-02, 30-03]
files_modified:
  - .planning/STATE.md
autonomous: true
requirements: [CLOSE-01]
tags: [dogfood, verification, pytest-run, state-update]

must_haves:
  truths:
    - "Framework's own `pyproject.toml` still carries `[tool.pytest.ini_options] mcp_config_file = \"./config.test.yaml\"` (Phase 27 D-06 dogfood line intact)"
    - "`uv run pytest` from repo root returns a green/zero-failures result against the framework's own test suite under the dogfood ini route"
    - "Plan 01's new parity test is collected but default-deselected (default addopts: `-m 'not live_homelab and not live_ollama'`) — does NOT spawn subprocesses in the dogfood run"
    - "Plan 02's REQUIREMENTS amendments do not break any framework self-test that references CLOSE-NN markers"
    - "STATE.md notes the dogfood-loop-green verification at v1.4 close"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Updated session continuity record + Performance Metrics row for Plan 30-04 + Accumulated Context dogfood-green note"
      contains: "Phase 30"
  key_links:
    - from: "pyproject.toml"
      to: "config.test.yaml"
      via: "mcp_config_file ini key"
      pattern: "mcp_config_file = \"./config\\.test\\.yaml\""
    - from: ".planning/STATE.md"
      to: "Phase 30 verification result"
      via: "Performance Metrics + Accumulated Context entries"
      pattern: "Phase 30 P04"
---

<objective>
Deliver SC1 / CLOSE-01 (verification act) — the dogfood-loop-still-green confirmation at v1.4 close.

Per CONTEXT.md `<domain>` SC1: "no new code; a pytest-run + assertion + STATE.md note." This plan:
1. Verifies the Phase 27 D-06 dogfood line (`mcp_config_file = "./config.test.yaml"`) is still present in `pyproject.toml`.
2. Runs `uv run pytest` end-to-end under default addopts (Plan 01's parity test default-skips via `live_homelab` + `live_ollama` markers — verification per Plan 01 acceptance criteria).
3. Asserts the run is green (no unexpected failures).
4. Records the dogfood-green status in `.planning/STATE.md`.

This plan is in WAVE 2 because the dogfood pytest run is expected to exercise the parity test file from Plan 01 (under default-skip path) and may also exercise framework tests that touch the LIBRARY-MODE.md or REQUIREMENTS.md surfaces from Plan 02 (none expected, but transitively safe to run after). Plan 03 (30-UAT.md) is a planning-artifact-only deliverable and has no pytest-execution impact, but is listed in `depends_on` because Phase 30 dogfood-green is conceptually "everything Plans 01-03 produced still doesn't break the framework's self-tests."

Output:
- A documented pytest run with green result.
- Updated `.planning/STATE.md` recording the dogfood verification + Performance Metrics row for Plan 30-04.
- NO code or test changes (this plan is verification-only — investigative-tasks-only if any assertion fails).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-CONTEXT.md
@.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-RESEARCH.md

# Plan dependencies
@.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-01-PLAN.md
@.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-02-PLAN.md
@.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-03-PLAN.md

# The dogfood substrate
@pyproject.toml
@config.test.yaml

<interfaces>
<!-- Verified pyproject.toml ini state (must remain at verification time). -->

`pyproject.toml [tool.pytest.ini_options]` MUST still contain at verification time:
```toml
[tool.pytest.ini_options]
asyncio_mode = "strict"
asyncio_default_fixture_loop_scope = "session"
testpaths = ["tests"]
# Phase 27 D-06: framework dogfoods library mode — contract tests are
# (a comment block followed by:)
mcp_config_file = "./config.test.yaml"
markers = [
  "live_homelab: ...",
  "live_ollama: ...",
]
addopts = "-m 'not live_homelab and not live_ollama'"
```

(The full block may include `filterwarnings` and other keys — those are not asserted here.)

Plan 01's marker `parity` is registered in `tests/framework/conftest.py` `pytest_configure`, NOT in pyproject's `markers = [...]` list — verified by acceptance criterion.

`config.test.yaml` has `tools: {}` (Phase 27 D-13/D-16 intentional empty allowlist). The plugin no-injects under empty allowlist — `pytest --collect-only` shows zero `<mcp-contracts>::test_*[*]` items in the dogfood run. This is correct CI state.
</interfaces>

<expected_pytest_baseline>
<!-- What "dogfood green" looks like. Use this as the bar for the verification task. -->

A green dogfood run from repo root with:
```bash
uv run pytest
```

is expected to produce:
- Exit code 0
- No FAILED test cases
- No errored test cases
- Zero `<mcp-contracts>::test_*[*]` items collected (because `config.test.yaml` has empty `tools:`)
- Zero `parity`-marked items collected (default addopts deselects via `live_homelab + live_ollama`)
- Some count of framework self-tests passed (`tests/framework/`) — the exact number depends on accumulated framework state, but should be a positive integer

If the run is NOT green, this plan switches to investigative mode: capture the failure summary, surface to user, do NOT silently patch — the planner's contract is verification, not repair.
</expected_pytest_baseline>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify pyproject.toml dogfood ini line is still present</name>
  <files></files>
  <read_first>
    - pyproject.toml (the `[tool.pytest.ini_options]` block — confirm Phase 27 D-06 line intact)
    - .planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-CONTEXT.md (Phase 27 D-06 reference — this is the line being verified)
  </read_first>
  <action>
Read `pyproject.toml`. Confirm the dogfood line `mcp_config_file = "./config.test.yaml"` is present inside the `[tool.pytest.ini_options]` block. No edits — verification only.

If the line is MISSING or ALTERED (different path), STOP and surface to the operator. Do NOT patch silently — the Phase 27 D-06 line is a load-bearing artifact; a missing line is a regression that needs explicit attention, not a planner-side fix.

Also confirm the `parity` marker is NOT in the pyproject `markers = [...]` list (Plan 01 registered it in framework conftest scope per CONTEXT.md `<decisions>` Claude's-discretion default).
  </action>
  <verify>
    <automated>grep -F 'mcp_config_file = "./config.test.yaml"' pyproject.toml && ! (grep -A 10 'markers = \[' pyproject.toml | grep -F '"parity:')</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'mcp_config_file = "./config.test.yaml"' pyproject.toml` returns 1 match (Phase 27 D-06 dogfood line intact)
    - `grep -B 2 'mcp_config_file' pyproject.toml | grep -F 'tool.pytest.ini_options'` returns at least 1 match (the dogfood line lives inside the `[tool.pytest.ini_options]` block)
    - `grep -A 8 'markers = \[' pyproject.toml | grep -F '"parity:'` returns ZERO matches (parity marker correctly NOT in pyproject — lives in tests/framework/conftest.py per Plan 01)
    - `grep -F 'addopts = "-m ' pyproject.toml` returns at least 1 match (default-deselection addopts still in place)
    - `grep -F 'not live_homelab' pyproject.toml` returns at least 1 match (live_homelab deselected by default)
  </acceptance_criteria>
  <done>pyproject.toml dogfood ini line intact; parity marker correctly framework-scoped (not pyproject-scoped); default addopts still deselect live_homelab/live_ollama.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run the full framework pytest suite and confirm green</name>
  <files></files>
  <read_first>
    - .planning/STATE.md `## Performance Metrics` table (for the row format precedent)
    - .planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/*-SUMMARY.md (any one — for the green-baseline shape framework self-tests usually emit; the exact pass count is not asserted, only "no failures")
  </read_first>
  <action>
Run the full framework pytest suite from the repo root:

```powershell
uv run pytest
```

Expected behavior:
- Exit code 0
- No failed test cases; no errored test cases
- Zero `<mcp-contracts>::test_*[*]` items collected (because `config.test.yaml` has empty `tools:` per Phase 27 D-13/D-16 design)
- Zero `parity`-marked items collected (default addopts deselects them via `live_homelab + live_ollama`)
- Some positive count of framework self-tests passes

Record the exact `passed=N, failed=0, errored=0, skipped=M` numbers from the pytest output for the STATE.md update in Task 3.

If the run is GREEN: proceed to Task 3.

If the run is NOT GREEN:
1. STOP. Do NOT patch any test or production code as part of this plan.
2. Capture the failure summary from the pytest output (test names, brief error messages — first 50 lines of failure detail).
3. Determine whether the failure is caused by:
   - Plan 01 changes (`tests/framework/parity/test_cli_vs_pytest_route.py` collected unexpectedly, or `tests/framework/conftest.py` change broke an existing test) → surface to operator with "Plan 01 verification artifact regressed framework self-tests"
   - Plan 02 changes (REQUIREMENTS.md amendment broke a framework self-test that scrapes the file, OR README/LIBRARY-MODE.md changes broke a doc-link gate) → surface as "Plan 02 doc rewrite regressed a framework self-test"
   - Plan 03 changes (planning-artifact-only — should not affect pytest) → surface as "Plan 03 unexpectedly affected pytest" (highly unlikely; double-check)
   - Pre-existing red (not caused by Plans 01-03) → surface as "Phase 30 inherited a red baseline; Plans 01-03 not implicated"
4. Surface to operator with a `## DOGFOOD GREEN REGRESSED` summary and let the operator decide whether to insert a Phase 30.1 fix plan, revert one of Plans 01-03, or accept the red baseline (unlikely given v1.4 close gating).

Note: the parity test (Plan 01) MUST be collected (`pytest --collect-only -m "parity"` returns 1 item — Plan 01 Task 2 acceptance criterion) but MUST be default-deselected (zero items collected under default addopts — Plan 01 Task 2 acceptance criterion). If the dogfood run unexpectedly tries to spawn the parity test's subprocesses, that's a marker-registration regression and the operator must be informed.
  </action>
  <verify>
    <automated>uv run pytest --co -q 2>&1 | tail -3 | grep -E 'error[s]? in' && echo "COLLECT ERRORS DETECTED" && exit 1; uv run pytest -q 2>&1 | tail -5 | grep -E '^=+.*(passed|skipped).*=+$' | grep -v 'failed\|error'</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q` returns exit code 0
    - `uv run pytest --collect-only -m "parity"` returns 1 collected item (Plan 01 parity test, opt-in selectable)
    - `uv run pytest --collect-only -q 2>&1 | tail -1 | grep -E 'tests? collected'` reports at least 1 test collected under default addopts
    - `uv run pytest --collect-only -q 2>&1 | grep -F '<mcp-contracts>::test_'` returns ZERO matches (config.test.yaml has empty `tools:`, plugin no-injects per Phase 27 D-13/D-16)
    - The summary line `=== N passed ... ===` reports zero `failed` and zero `error` totals (regex-match: `=+ \d+ passed(, \d+ skipped)?(, \d+ warning[s]?)? in [\d.]+s =+` — no `failed` or `error` keyword)
    - Captured pytest output includes a line like `passed: N, failed: 0, errored: 0, skipped: M` (or equivalent pytest format) — record N and M for Task 3
  </acceptance_criteria>
  <done>Full framework pytest suite runs green from repo root under default addopts. Parity test is collected (1 item under `-m parity`) but default-deselected. No `<mcp-contracts>::test_*` items collected (empty allowlist correct). Pass/skip counts captured for STATE.md.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update STATE.md with dogfood-green verification record + Performance Metrics row</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (the full file — needed for Performance Metrics table position + Accumulated Context insertion point + Session Continuity update)
    - The captured pytest output from Task 2 (pass/skip counts)
  </read_first>
  <action>
Update `.planning/STATE.md` with three edits:

**Edit 1 — Append a Performance Metrics row for Plan 30-04:**

Add after the last existing row in the `## Performance Metrics` table. Format (mirror the Phase 29 P29-03 row precedent):

```
| Phase 30 P04 | <time-or-noop-marker>min | 3 tasks | 1 files | dogfood verification: passed=N, skipped=M, failed=0, errored=0; config.test.yaml empty tools: confirmed; parity test collected (1 under -m parity), default-deselected (0 under default addopts) |
```

Substitute `N` and `M` with the actual pass/skip counts from Task 2.

**Edit 2 — Append an Accumulated Context "Decisions" or end-of-list note:**

Append a new entry to the appropriate accumulated-context section (after the last `- [Phase ?]: ...` style bullet near the bottom of `## Accumulated Context > ### Decisions`). Format:

```
- [Phase 30 P04]: dogfood verification green at v1.4 close -- Phase 27 D-06 ini line (`mcp_config_file = "./config.test.yaml"`) intact; `uv run pytest` returns exit 0 with N passed / M skipped / 0 failed / 0 errored; Plan 01 parity test correctly collected under -m parity (1 item) and default-deselected under pyproject addopts (0 items); empty allowlist in config.test.yaml verified (zero `<mcp-contracts>::test_*` items collected). CLOSE-01 dogfood verification thread closed (text amendment lives in Plan 30-02).
```

**Edit 3 — Update `## Session Continuity` block:**

Update the existing `Last session:`, `Stopped at:`, and `Resume next:` lines at the bottom of the file:
- `Last session:` → today's UTC date in the existing ISO format (e.g., `2026-05-17T<HH:MM:SS>.<ms>Z` — match the existing format).
- `Stopped at:` → `Phase 30 Plan 30-04 dogfood verification complete`
- `Resume next:` → `/gsd-verify-phase 30` then `/gsd-complete-milestone v1.4`

Do NOT modify the frontmatter at the top of STATE.md (`status:`, `stopped_at:`, `last_updated:`, etc.) — those are managed by the orchestrator at phase-close time, not by individual plans.

Do NOT modify the `## Project Reference` section, the `## Current Position` section's `Phase:`/`Plan:`/`Status:` fields, the `## Performance Metrics` headers, the `## Accumulated Context > ### Decisions` heading, or any other existing content.
  </action>
  <verify>
    <automated>grep -F 'Phase 30 P04' .planning/STATE.md && grep -F 'dogfood verification green at v1.4 close' .planning/STATE.md && grep -F 'Phase 30 Plan 30-04 dogfood verification complete' .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F '| Phase 30 P04 |' .planning/STATE.md` returns 1 match (Performance Metrics row added)
    - `grep -F 'dogfood verification: passed=' .planning/STATE.md` returns 1 match (verification context in the metrics row)
    - `grep -F '[Phase 30 P04]:' .planning/STATE.md` returns 1 match (Accumulated Context note added)
    - `grep -F 'dogfood verification green at v1.4 close' .planning/STATE.md` returns 1 match (context note prose)
    - `grep -F 'mcp_config_file = "./config.test.yaml"' .planning/STATE.md` returns at least 1 match (Phase 27 D-06 line cited in context note)
    - `grep -F 'Phase 30 Plan 30-04 dogfood verification complete' .planning/STATE.md` returns 1 match (Session Continuity updated)
    - `grep -F '/gsd-verify-phase 30' .planning/STATE.md` returns 1 match (Resume next: pointer)
    - `grep -F '/gsd-complete-milestone v1.4' .planning/STATE.md` returns 1 match (Resume next: chain pointer)
    - The frontmatter `last_updated:` line was NOT modified by this task (orchestrator owns it): `head -n 15 .planning/STATE.md | grep -F '"2026-05-17T20:36:47.213Z"'` returns 1 match (the original frontmatter timestamp from before this plan ran)
    - `grep -F 'milestone: v1.4' .planning/STATE.md` returns 1 match (frontmatter milestone field unchanged)
  </acceptance_criteria>
  <done>STATE.md records the Phase 30 P04 dogfood verification with pass/skip counts in Performance Metrics, a context note in Accumulated Context describing the green state of the dogfood loop + Plan 01 parity-marker behavior, and an updated Session Continuity block pointing at next steps. Frontmatter unchanged.</done>
</task>

</tasks>

<verification>
- pyproject.toml dogfood line `mcp_config_file = "./config.test.yaml"` intact
- `parity` marker correctly NOT in pyproject's `markers = [...]` (lives in framework conftest per Plan 01)
- `uv run pytest` exits 0 with zero failures and zero errors
- Plan 01 parity test collected under `-m parity` (1 item) and default-deselected (0 items under default addopts)
- Zero `<mcp-contracts>::test_*` items collected (config.test.yaml empty allowlist correct)
- STATE.md updated with Performance Metrics row + Accumulated Context note + Session Continuity update
</verification>

<success_criteria>
Plan complete when:
- [ ] pyproject.toml dogfood line intact (Phase 27 D-06 still in place).
- [ ] `uv run pytest` from repo root returns green: exit 0, zero failures, zero errors.
- [ ] Plan 01 parity test correctly registered (collected under `-m parity`, default-deselected otherwise).
- [ ] config.test.yaml empty allowlist still produces zero injected contract tests.
- [ ] STATE.md records the dogfood verification (Performance Metrics + Accumulated Context + Session Continuity).
- [ ] CLOSE-01 acceptance criteria satisfied (the verification-act portion — text amendment portion lives in Plan 30-02).
</success_criteria>

<output>
After completion, create `.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-04-SUMMARY.md` documenting:
- Verification commands run + pytest exit code + pass/skip counts
- Confirmation of dogfood-green at v1.4 close
- pyproject.toml dogfood line intact (Phase 27 D-06)
- Plan 01 parity test marker behavior verified (1-collected-under-marker, 0-collected-under-default)
- STATE.md updates (Performance Metrics row + Accumulated Context note + Session Continuity)
- Pointer to next steps: `/gsd-verify-phase 30`, then `/gsd-complete-milestone v1.4`
- Note: live-UAT execution (Plan 30-03's 30-UAT.md) NOT done here — separate user-driven session per memory `feedback_uat_must_be_user_driven`
</output>
