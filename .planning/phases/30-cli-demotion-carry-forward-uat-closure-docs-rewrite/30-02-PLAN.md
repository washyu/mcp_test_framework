---
phase: 30-cli-demotion-carry-forward-uat-closure-docs-rewrite
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/LIBRARY-MODE.md
  - README.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [CLOSE-01, CLOSE-03]
tags: [docs, library-mode, readme-rewrite, requirements-amendment]

must_haves:
  truths:
    - "A new operator reading README.md sees library-mode usage (one ini line + pytest) BEFORE any CLI invocation copy"
    - "CLI usage continues to ship and is documented, but lives in an 'Appendix: CLI usage' section AT THE END of README.md"
    - "docs/LIBRARY-MODE.md exists as the primary library-mode reference (ini value, plugin auto-discovery, marker semantics, fixture surface, reporter opt-in, gen-test-classes, env-var deprecation, parity gate)"
    - "REQUIREMENTS.md CLOSE-01 and CLOSE-03 text matches Phase 27 D-01/D-07 reality (no more `register(config=Config())` references)"
    - "docs/LIBRARY-MODE.md links to (does not duplicate) docs/TEST-CODE-AUTHORING.md for the test-code-author surface"
  artifacts:
    - path: "docs/LIBRARY-MODE.md"
      provides: "Primary operator-facing library-mode reference doc"
      min_lines: 220
      contains: "mcp_config_file"
    - path: "README.md"
      provides: "Top-of-funnel operator doc with library-mode quickstart leading"
      contains: "[tool.pytest.ini_options]"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Amended CLOSE-01 and CLOSE-03 line items reflecting Phase 27 D-01/D-07 pivot"
      contains: "mcp_config_file"
  key_links:
    - from: "README.md"
      to: "docs/LIBRARY-MODE.md"
      via: "markdown relative link in Links section + inline references"
      pattern: "docs/LIBRARY-MODE\\.md"
    - from: "docs/LIBRARY-MODE.md"
      to: "docs/TEST-CODE-AUTHORING.md"
      via: "markdown relative link in §'Test-code scenarios' section"
      pattern: "TEST-CODE-AUTHORING\\.md"
    - from: "docs/LIBRARY-MODE.md"
      to: "docs/ERROR-STYLE.md"
      via: "markdown relative link in §'Error tone' section"
      pattern: "ERROR-STYLE\\.md"
---

<objective>
Deliver CLOSE-01 + CLOSE-03 — the docs-rewrite thread of Phase 30.

Three deliverables in one cohesive operator-facing sweep:
1. Create `docs/LIBRARY-MODE.md` as the new primary library-mode reference (~250-400 lines per research §"`docs/LIBRARY-MODE.md` outline").
2. Rewrite `README.md` top — lead with library-mode quickstart; demote CLI to an "Appendix: CLI usage" section at the END of the file; demote the SUT-specific homelab-mcp / Ollama setup to a subsection inside the Appendix.
3. Amend `.planning/REQUIREMENTS.md` — rewrite CLOSE-01 and CLOSE-03 text per Phase 27 D-01 / D-07 pivot (verbatim replacement text supplied in CONTEXT.md `<downstream_impact>`). CLOSE-02 and CLOSE-04 stay current.

Purpose:
- CLOSE-03: New operator reads README → sees library mode first; CLI usage demoted to Appendix; LIBRARY-MODE.md is the primary library-mode reference doc.
- CLOSE-01 (text amendment portion): REQUIREMENTS.md catches up to ROADMAP.md, which already reflects the Phase 27 D-06/D-07 pre-emption.

Output:
- New file `docs/LIBRARY-MODE.md`.
- Rewritten `README.md` (top half new, current CLI content preserved verbatim in the new Appendix).
- Two amended line items in `.planning/REQUIREMENTS.md` (lines 57 + 59 in current revision).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-CONTEXT.md
@.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-RESEARCH.md
@.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-PATTERNS.md

# Phase 27/28/29 surfaces this doc describes
@.planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-CONTEXT.md
@.planning/phases/28-codegen-output-path-codegen/28-CONTEXT.md
@.planning/phases/29-live-domain-ui-reporter-plugin/29-CONTEXT.md

# Style anchors for doc voice and structure
@docs/TEST-CODE-AUTHORING.md
@docs/ERROR-STYLE.md
@README.md

<interfaces>
<!-- Verbatim replacement text for REQUIREMENTS.md amendments — copy these without editing. -->

REQUIREMENTS.md current text (line 57 — CLOSE-01):
```
- [ ] **CLOSE-01**: Framework's own `tests/contract/conftest.py` calls `register(config=Config())` — proving the library-mode dogfood loop end-to-end. `tests/conftest.py`'s `pytest_generate_tests` is removed; all contract-test parametrization flows through the plugin's hooks. CLI-mode path (`mcp-test-framework run`) continues to subprocess pytest with JUnit XML round-trip and shares the same `ParsedRun` domain model and renderer helpers.
```

REPLACE with (per CONTEXT.md `<downstream_impact>` line 162):
```
- [ ] **CLOSE-01**: Framework's own `pyproject.toml` sets `[tool.pytest.ini_options] mcp_config_file = "./config.test.yaml"` (landed in Phase 27 D-06); Phase 30 verifies the dogfood loop is still green at v1.4 close. CLI-mode path (`mcp-contracts run`) continues to subprocess pytest with JUnit XML round-trip and shares the same `ParsedRun` domain model and renderer helpers.
```

REQUIREMENTS.md current text (line 59 — CLOSE-03):
```
- [ ] **CLOSE-03**: New operator reading the README sees library-mode usage first ("Add to your `pyproject.toml`, write three lines in `conftest.py`, run pytest"); CLI usage demotes to an "Appendix: CLI usage" section; `docs/LIBRARY-MODE.md` is the primary reference document for the library API surface.
```

REPLACE with (per CONTEXT.md `<downstream_impact>` line 163):
```
- [ ] **CLOSE-03**: New operator reading the README sees library-mode usage first ("Add to your `pyproject.toml`, set one line in `[tool.pytest.ini_options]`, run pytest"); CLI usage demotes to an "Appendix: CLI usage" section; `docs/LIBRARY-MODE.md` is the primary reference document for the library API surface.
```

(The diff between CLOSE-01 old/new is: drops `register(config=Config())` + `tests/contract/conftest.py` + `tests/conftest.py`'s `pytest_generate_tests` references; replaces with `mcp_config_file` ini route. The diff between CLOSE-03 old/new is: replaces "write three lines in `conftest.py`" with "set one line in `[tool.pytest.ini_options]`".)

CLOSE-02 (line 58) and CLOSE-04 (lines 60-64) and traceability rows (lines 129-132) STAY VERBATIM.
</interfaces>

<readme_rewrite_outline>
<!-- Recommended target structure for the rewritten README. Section IDs are advisory. -->

Final section order (top to bottom):
1. `# mcp_test_framework` (H1 title — keep)
2. One-line value prop: "Pytest-native MCP contract testing — for any MCP server, configured via a single ini line."
3. `## Quickstart` — 3-5 lines:
   ```bash
   uv add mcp-contracts
   ```
   ```toml
   # In your pyproject.toml:
   [tool.pytest.ini_options]
   mcp_config_file = "./config.yaml"
   ```
   ```bash
   uv run pytest
   ```
4. `## Library mode (recommended)` — ~50 lines linking inward to `docs/LIBRARY-MODE.md` for depth. Cover:
   - "Add one line to pyproject.toml"
   - "Write a minimal config.yaml" (3-line shape — point at `config.example.yaml` for full surface)
   - "Run pytest" — show the injected `<mcp-contracts>::test_*[<tool>]` collection output sample
   - Brief mention of `pytest --mcp-domain-ui` opt-in (link to LIBRARY-MODE.md § Reporter plugin)
   - Brief mention of `gen-test-classes` (link to LIBRARY-MODE.md § Codegen CLI)
5. (Optional) Sample green-run output — keep the existing canonical green-run sample if present, but moved DOWN below library-mode section.
6. `## Test-code scenarios` — keep current section content but rephrase intro paragraph to point library-mode operators at `docs/TEST-CODE-AUTHORING.md`. The current PASS-sample snapshot block stays as-is (Phase 30 UAT-1 captures the re-snapshot in a follow-up user-driven session, NOT here).
7. `## Configuration` — current Configuration section content, light edits to lead with `mcp_config_file` ini route; existing config field documentation kept verbatim.
8. `## Appendix: CLI usage` — DEMOTED. Contains:
   - All current `## Commands` content (currently README L45-150 area) — PRESERVE VERBATIM
   - All current Setup content for the example homelab-mcp stack (currently README L19-43 area) — moved into a subsection like `### Run against the example homelab-mcp stack` — PRESERVE VERBATIM where possible
   - Note that the legacy `mcp-test-framework run` alias also works in v1.4 (drops in v1.5)
9. `## Links` — final list:
   - [Library mode reference](docs/LIBRARY-MODE.md) — primary library-mode doc
   - [Test-code authoring](docs/TEST-CODE-AUTHORING.md) — scenario-author walkthrough
   - [Extending the framework](docs/EXTENDING.md) — internals + bootstrap flow
   - [Error tone style](docs/ERROR-STYLE.md) — operator-facing error patterns

NOT in scope for Phase 30 (per `<scope_guardrails>`):
- ID-leak scrub of README/config.example.yaml/.env.example/docs/EXTENDING.md — VERIFIED CLEAN at planning time (research §"Surfaces verified clean of planning-ID leaks"). Add a single grep-verification task confirming each surface still has zero planning-ID matches; do NOT spend plan-time on a scrub pass.
- README badge/branding/marketing rewrites — explicitly OUT per REQUIREMENTS.md "Out of Scope".
- Removing or altering the legacy `mcp-test-framework run` alias documentation — alias still ships in v1.4 (drops in v1.5).
- Removing `docs/SDET-AUTHORING.md` — stays as v1.4 deprecation alias (drops in v1.5).
</readme_rewrite_outline>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create docs/LIBRARY-MODE.md (the primary library-mode reference)</name>
  <files>docs/LIBRARY-MODE.md</files>
  <read_first>
    - .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-RESEARCH.md §"`docs/LIBRARY-MODE.md` outline" (lines 612-753 — the complete target structure with section bodies)
    - .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-PATTERNS.md §`docs/LIBRARY-MODE.md (CREATE)` (heading + intro pattern from TEST-CODE-AUTHORING.md)
    - docs/TEST-CODE-AUTHORING.md (style anchor — H1 + one-paragraph scope + Prerequisites bullet list + H2 sections)
    - docs/ERROR-STYLE.md (link target; LIBRARY-MODE.md § "Error tone" references it)
    - .planning/phases/27-register-api-contracts-sub-package-test-extraction-lib/27-CONTEXT.md §`<decisions>` (D-02 ini key resolution, D-09 MCPTF_CONFIG_FILE deprecation, D-11 CLI internal subprocess shape — facts cited in LIBRARY-MODE.md)
    - .planning/phases/28-codegen-output-path-codegen/28-CONTEXT.md §`<decisions>` (D-11 pyproject ini resolution, D-01 no smart default for generated_root)
    - .planning/phases/29-live-domain-ui-reporter-plugin/29-CONTEXT.md §`<decisions>` (D-04 `--mcp-domain-ui=auto|force|off`, D-05 CLI mode passes force)
  </read_first>
  <action>
Create `docs/LIBRARY-MODE.md`. Use the outline in 30-RESEARCH.md §"`docs/LIBRARY-MODE.md` outline" lines 612-753 VERBATIM AS THE STRUCTURAL TEMPLATE — keep the section ordering and headers exactly. Fill in section bodies with the specifics from the cited Phase 27/28/29 CONTEXT.md files.

Required H2 sections in order:
1. `# Library Mode — pytest-native MCP contract testing` (H1, with one-paragraph framing immediately below per docs/TEST-CODE-AUTHORING.md style)
2. `## Quickstart (90 seconds)` — install + ini line + pytest invocation; show injected `<mcp-contracts>::test_*[<tool>]` collection output as the success signal
3. `## What library mode IS` — bullet list (4 bullets per outline)
4. `## What library mode IS NOT` — bullet list (3 bullets per outline)
5. `## The mcp_config_file ini value` — toml block showing the ini key + a 3-row table mapping resolution priority (-o override > ini > unset → no-op). Cite Phase 27 D-02 mechanism in prose, but do NOT mention the planning ID — paraphrase or use phrasing like "the ini-route mechanism described in `docs/EXTENDING.md`".
6. `## Plugin auto-discovery` — explain `[project.entry-points.pytest11]` auto-load; note operator does NOT add `pytest_plugins=[...]`
7. `## Injected test surface` — show sample `pytest --collect-only` output with the `<mcp-contracts>::test_*[<tool>]` nodeid shape; explain virtual nodes
8. `## Markers` — table covering `mcp_contract` (auto-applied), `live_homelab` and `live_ollama` (operator opt-in). Note the framework-internal `parity` marker is intentionally NOT documented here (it is framework-self-test-scope per Phase 30 Plan 01).
9. `## Fixture surface` — list of `mcp_*` prefixed fixtures (`mcp_config`, `mcp_client`, `mcp_judge`, `mcp_target_tool`, `mcp_rubric_*`); note unprefixed compat aliases still work in v1.4 with `DeprecationWarning`.
10. `## Reporter plugin (--mcp-domain-ui)` — show the four invocations (default, `--mcp-domain-ui`, `--mcp-domain-ui=force`, `--mcp-domain-ui=off`); explain TTY auto-detection; note `-p no:mcp_test_framework_reporter` disables it while keeping contract fixtures.
11. `## Codegen CLI (gen-test-classes)` — show the invocation; note it reads the SAME pyproject ini value as pytest; reference `--config PATH` override; cite the `test_code.generated_root` requirement (no smart default).
12. `## Migration from MCPTF_CONFIG_FILE env var` — before/after toml block per outline; note `DeprecationWarning` in v1.4, removed in v1.5.
13. `## Error tone` — link to `docs/ERROR-STYLE.md`; bullet examples (missing config, empty tools allowlist, missing generated_root).
14. `## Running the parity gate locally` — NEW section (NOT in outline; required by Plan 01 D-02a doc handoff). 4-6 sentences explaining: parity gate lives at `tests/framework/parity/test_cli_vs_pytest_route.py`; deselected by default via `live_homelab` + `live_ollama` markers; running it requires (a) populated `tools:` in `config.test.yaml` (or a different config path), (b) reachable homelab-mcp + Ollama; invoke via `pytest -m "parity and live_homelab and live_ollama" tests/framework/parity/`.
15. `## Test-code scenarios (test-code-author surface)` — short paragraph + relative link to `docs/TEST-CODE-AUTHORING.md`. Do NOT duplicate that doc's content.

Style rules (project-wide, from CLAUDE.md):
- No emojis.
- Triple-backtick fenced code blocks with language tag (`bash`, `toml`, `python`).
- Inline-backtick + relative path for cross-doc links.
- Em-dash for prose continuation (`—` not `--`).
- Tables for resolution / surface enumerations.
- Heading depth: H1 for title, H2 for sections, H3 sparingly within long sections only.

Length target: 220-400 lines (research outline aims ~250-400; defensive lower bound 220 to allow for compact writing).

Operator persona reminder (from `<readme_rewrite_outline>` + memory `project_vibe_coded_persona`):
- Operator may not know homelab-mcp internals — the doc must work for any compatible MCP server.
- Default examples use `./config.yaml` (operator's project file) NOT `./config.test.yaml` (the framework's CI substrate). Use `./config.test.yaml` ONLY in the parity-gate section where it is the correct context.

Source authority hierarchy:
- Phase 27/28/29 CONTEXT.md files are the source of truth for facts (D-XX decisions); paraphrase, do not lift planning-ID strings into this operator-facing doc.
- Research outline (lines 612-753) is the structural template; section bodies fill in with paraphrased facts.
- Verified planning-IDs MUST NOT appear in operator-facing prose (per memory `project_doc_scrub_planning_artifacts`).
  </action>
  <verify>
    <automated>test -f docs/LIBRARY-MODE.md && wc -l docs/LIBRARY-MODE.md | awk '{print $1}' | xargs -I{} test {} -ge 220</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/LIBRARY-MODE.md`
    - `wc -l docs/LIBRARY-MODE.md` reports at least 220 lines
    - `grep -F '# Library Mode' docs/LIBRARY-MODE.md` returns 1 match
    - `grep -F '## Quickstart' docs/LIBRARY-MODE.md` returns 1 match
    - `grep -F '## What library mode IS' docs/LIBRARY-MODE.md` returns 1 match
    - `grep -F '## What library mode IS NOT' docs/LIBRARY-MODE.md` returns 1 match
    - `grep -F 'mcp_config_file' docs/LIBRARY-MODE.md` returns at least 5 matches
    - `grep -F '[tool.pytest.ini_options]' docs/LIBRARY-MODE.md` returns at least 2 matches
    - `grep -F '[project.entry-points.pytest11]' docs/LIBRARY-MODE.md` returns at least 1 match
    - `grep -F '<mcp-contracts>::test_' docs/LIBRARY-MODE.md` returns at least 1 match (collection-output sample)
    - `grep -F 'mcp_contract' docs/LIBRARY-MODE.md` returns at least 1 match (markers section)
    - `grep -F 'mcp_config' docs/LIBRARY-MODE.md` returns at least 1 match (fixtures section)
    - `grep -F '--mcp-domain-ui' docs/LIBRARY-MODE.md` returns at least 2 matches (reporter section)
    - `grep -F 'gen-test-classes' docs/LIBRARY-MODE.md` returns at least 1 match (codegen section)
    - `grep -F 'MCPTF_CONFIG_FILE' docs/LIBRARY-MODE.md` returns at least 1 match (migration section)
    - `grep -F 'TEST-CODE-AUTHORING.md' docs/LIBRARY-MODE.md` returns at least 1 match (link to test-code authoring)
    - `grep -F 'ERROR-STYLE.md' docs/LIBRARY-MODE.md` returns at least 1 match (link in error-tone section)
    - `grep -F 'parity' docs/LIBRARY-MODE.md` returns at least 1 match (running-parity-gate section per D-02a doc handoff)
    - `grep -F 'config.test.yaml' docs/LIBRARY-MODE.md` returns at least 1 match (parity-gate section)
    - `grep -E '(Phase [0-9]|D-[0-9]|REQ-|SEED-|SC[0-9]|CLOSE-|LIB-|CFG-|CODEGEN-LIB-|REPORTER-|RENAME-|PACK-)' docs/LIBRARY-MODE.md` returns ZERO matches (no planning-ID leaks per memory `project_doc_scrub_planning_artifacts`)
    - `grep -P '[\x{1F300}-\x{1FAFF}]|[\x{2600}-\x{27BF}]' docs/LIBRARY-MODE.md` returns ZERO matches (no emojis per CLAUDE.md)
  </acceptance_criteria>
  <done>docs/LIBRARY-MODE.md exists as a 220+ line primary library-mode reference following the research-supplied outline; cross-links to TEST-CODE-AUTHORING.md and ERROR-STYLE.md; no planning-ID leaks; no emojis.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite README.md — lead with library mode, demote CLI + homelab-mcp setup to Appendix</name>
  <files>README.md</files>
  <read_first>
    - README.md (full file — needed to preserve current CLI command docs and Configuration section content VERBATIM in the new Appendix)
    - .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-RESEARCH.md §"README rewrite — operator-onboarding-first structure" (lines 755-781)
    - .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-PATTERNS.md §`README.md (MODIFY: heavy rewrite)`
    - docs/LIBRARY-MODE.md (Task 1's output — README links inward to it and must use consistent terminology)
    - docs/TEST-CODE-AUTHORING.md (style anchor for tone and fence-block style)
  </read_first>
  <action>
Rewrite `README.md` per the section order in `<readme_rewrite_outline>` above (and 30-RESEARCH.md lines 755-781).

The rewrite is "heavy, not polish" (per 30-CONTEXT.md `<specifics>`):
- Current README leads with MVP-shape framing (homelab-mcp, list_keyring_credentials, Ollama).
- New README leads with library-shape framing (any compatible MCP server, ini-route config, opt-in tools).
- Current CLI docs MUST be preserved VERBATIM in the new Appendix — do not paraphrase or "clean up" CLI command documentation.

Strict preservation requirements (move-not-rewrite):
1. All current `## Commands` content moves to `## Appendix: CLI usage` and is preserved char-for-char where possible (one section heading change, body kept verbatim).
2. The legacy `mcp-test-framework` v1.4 deprecation reference (the literal string `mcp-test-framework command is deprecated since v1.4 and will be removed in v1.5 — use mcp-contracts instead.` IF present in current README) stays in the Appendix.
3. The `## Test-code scenarios` section content stays — including the pre-Phase-24 FAIL-sample snapshot. Phase 30 UAT-1 captures the re-snapshot in a follow-up session; do NOT touch the snapshot block in this plan.
4. The `## Configuration` section technical content (config field descriptions, examples) is preserved; only the lead-in paragraph is edited to reference `mcp_config_file` ini route.
5. Existing canonical green-run sample block (if present in current README around L222) is preserved; only its position shifts down.

New content to author:
- New H1 area: one-line value prop (e.g., "Pytest-native MCP contract testing — for any MCP server, configured via one ini line.")
- New `## Quickstart` section: 3-block sample (bash install, toml ini, bash pytest).
- New `## Library mode (recommended)` section: ~30-50 lines linking inward to `docs/LIBRARY-MODE.md` for depth. Sample injected test collection output. Brief mentions of `--mcp-domain-ui` and `gen-test-classes` (each linking to LIBRARY-MODE.md).
- New `## Links` section at end: relative links to docs/LIBRARY-MODE.md, docs/TEST-CODE-AUTHORING.md, docs/EXTENDING.md, docs/ERROR-STYLE.md.

Section order (top to bottom):
1. H1 title
2. One-line value prop
3. `## Quickstart`
4. `## Library mode (recommended)`
5. (Existing) Sample green-run if present — moved down
6. (Existing) `## Test-code scenarios` — content unchanged
7. (Existing) `## Configuration` — lead paragraph edited
8. `## Appendix: CLI usage` — DEMOTED current Commands content + Setup-for-homelab-mcp content as subsection
9. `## Links`

Operator persona reminder (memory `project_vibe_coded_persona`):
- Operator may not know homelab-mcp internals. The new top-of-funnel sections (1-4) must work for any compatible MCP server. The homelab-mcp / Ollama-specific setup moves into the Appendix as "Run against the example homelab-mcp stack" subsection.
- Default examples in the new top sections use `./config.yaml` (the operator's project config), NOT `./config.test.yaml` (which is framework-CI-specific).

Style rules (CLAUDE.md):
- No emojis.
- Triple-backtick code fences with language tags.
- Em-dash for prose continuation.
- No planning-ID leaks (`Phase \d`, `D-\d`, `SC\d`, `CLOSE-`, `LIB-`, etc. — zero matches required in the operator-facing file).

What NOT to do:
- Do NOT remove or alter the `mcp-test-framework run` legacy alias documentation — the alias still ships in v1.4 (drops in v1.5, NOT Phase 30 scope).
- Do NOT add deprecation warnings to the CLI invocation copy itself — CLOSE keeps the CLI shipping (per REQUIREMENTS.md "Out of Scope" §1).
- Do NOT re-snapshot the README §"Test-code scenarios" PASS-sample block — that is Phase 30 UAT-1's deliverable (user-driven session).
- Do NOT touch `config.example.yaml`, `.env.example`, `docs/EXTENDING.md`. They are VERIFIED CLEAN (Task 3 only re-verifies; no edits).
- Do NOT add badges, branding, or marketing copy (out of scope per REQUIREMENTS.md).
  </action>
  <verify>
    <automated>head -n 30 README.md | grep -F -q 'mcp_config_file' && grep -F -q '## Appendix: CLI usage' README.md && echo "REWRITE SHAPE OK"</automated>
  </verify>
  <acceptance_criteria>
    - `head -n 40 README.md` contains the string `mcp_config_file` (library-mode quickstart leads — within first 40 lines)
    - `grep -F '[tool.pytest.ini_options]' README.md` returns at least 1 match (Quickstart toml block)
    - `grep -F '## Quickstart' README.md` returns 1 match
    - `grep -F '## Library mode' README.md` returns at least 1 match
    - `grep -F '## Appendix: CLI usage' README.md` returns 1 match
    - `grep -F '## Test-code scenarios' README.md` returns 1 match (existing section preserved)
    - `grep -F '## Configuration' README.md` returns 1 match (existing section preserved)
    - `grep -F 'docs/LIBRARY-MODE.md' README.md` returns at least 1 match (links section + inline)
    - `grep -F 'docs/TEST-CODE-AUTHORING.md' README.md` returns at least 1 match
    - `grep -F 'mcp-contracts run' README.md` returns at least 1 match (CLI usage in Appendix preserved)
    - The line index of `## Appendix: CLI usage` is GREATER than the line index of `## Quickstart`: `awk '/## Quickstart/{q=NR} /## Appendix: CLI usage/{a=NR} END{exit (a>q)?0:1}' README.md`
    - The line index of `## Library mode` is LESS than the line index of `## Appendix: CLI usage`: `awk '/## Library mode/{l=NR} /## Appendix: CLI usage/{a=NR} END{exit (a>l)?0:1}' README.md`
    - `grep -E '(Phase [0-9]|D-[0-9]|SC[0-9]|REQ-|SEED-|CLOSE-|LIB-|CFG-|CODEGEN-LIB-|REPORTER-|RENAME-|PACK-)' README.md` returns ZERO matches (no planning-ID leaks)
    - `grep -P '[\x{1F300}-\x{1FAFF}]|[\x{2600}-\x{27BF}]' README.md` returns ZERO matches (no emojis)
    - `grep -F 'register(config=Config())' README.md` returns ZERO matches (Phase 27 D-01 dropped register() API; must not appear in README)
  </acceptance_criteria>
  <done>README.md leads with library-mode Quickstart, demotes CLI usage to a clearly-labeled Appendix at the END of the file, preserves the existing CLI command documentation verbatim in the Appendix, and links inward to docs/LIBRARY-MODE.md. No planning-ID leaks, no emojis, no `register(config=Config())` ghosts.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Amend REQUIREMENTS.md CLOSE-01 and CLOSE-03 + verify clean-of-ID-leaks in adjacent operator-facing files</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — line 57 = CLOSE-01, line 59 = CLOSE-03; rest stays verbatim)
    - .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-CONTEXT.md §`<downstream_impact>` (verbatim replacement text)
    - The `<interfaces>` block above (replacement text copied character-for-character)
  </read_first>
  <action>
Two distinct sub-actions:

**Sub-action A — REQUIREMENTS.md CLOSE-01 + CLOSE-03 text amendment.**

Find the current CLOSE-01 line (currently line 57 of `.planning/REQUIREMENTS.md`):
```
- [ ] **CLOSE-01**: Framework's own `tests/contract/conftest.py` calls `register(config=Config())` — proving the library-mode dogfood loop end-to-end. `tests/conftest.py`'s `pytest_generate_tests` is removed; all contract-test parametrization flows through the plugin's hooks. CLI-mode path (`mcp-test-framework run`) continues to subprocess pytest with JUnit XML round-trip and shares the same `ParsedRun` domain model and renderer helpers.
```

Replace with (verbatim from `<interfaces>` above):
```
- [ ] **CLOSE-01**: Framework's own `pyproject.toml` sets `[tool.pytest.ini_options] mcp_config_file = "./config.test.yaml"` (landed in Phase 27 D-06); Phase 30 verifies the dogfood loop is still green at v1.4 close. CLI-mode path (`mcp-contracts run`) continues to subprocess pytest with JUnit XML round-trip and shares the same `ParsedRun` domain model and renderer helpers.
```

Find the current CLOSE-03 line (currently line 59 of `.planning/REQUIREMENTS.md`):
```
- [ ] **CLOSE-03**: New operator reading the README sees library-mode usage first ("Add to your `pyproject.toml`, write three lines in `conftest.py`, run pytest"); CLI usage demotes to an "Appendix: CLI usage" section; `docs/LIBRARY-MODE.md` is the primary reference document for the library API surface.
```

Replace with (verbatim from `<interfaces>` above):
```
- [ ] **CLOSE-03**: New operator reading the README sees library-mode usage first ("Add to your `pyproject.toml`, set one line in `[tool.pytest.ini_options]`, run pytest"); CLI usage demotes to an "Appendix: CLI usage" section; `docs/LIBRARY-MODE.md` is the primary reference document for the library API surface.
```

KEEP UNCHANGED:
- The `- [ ]` checkbox state — flipping to `- [x]` happens at phase-close time, NOT now.
- Line 58 (CLOSE-02) — already current.
- Lines 60-64 (CLOSE-04 multi-line bullet) — already current.
- Traceability table rows 129-132 — keep `Pending`.
- All other content in REQUIREMENTS.md.

**Sub-action B — verify ID-leak-clean status of the four operator-facing files** (per research §"Surfaces verified clean of planning-ID leaks" — research already verified, this task re-confirms in case Task 2 introduced regressions or stale content snuck in).

Run grep verifications and capture results in the plan's summary. NO file edits.
- `README.md`
- `config.example.yaml`
- `.env.example`
- `docs/EXTENDING.md`

If any of the four shows planning-ID leaks (matches for `(Phase [0-9]|D-[0-9]|SC[0-9]|REQ-|SEED-|CLOSE-|LIB-|CFG-|CODEGEN-LIB-|REPORTER-|RENAME-|PACK-)`), STOP and surface the finding to the operator — DO NOT silently scrub (research §"v1.2-deferred ID-leak scrub" is closed by Phase 25 P25-05/P25-06; any new leak is a regression that needs explicit attention, not a scrub pass).
  </action>
  <verify>
    <automated>grep -F 'mcp_config_file = "./config.test.yaml"' .planning/REQUIREMENTS.md && grep -F 'set one line in `[tool.pytest.ini_options]`' .planning/REQUIREMENTS.md && ! grep -F 'register(config=Config())' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'mcp_config_file = "./config.test.yaml"' .planning/REQUIREMENTS.md` returns at least 1 match (new CLOSE-01 text contains the dogfood line)
    - `grep -F 'Phase 30 verifies the dogfood loop is still green at v1.4 close' .planning/REQUIREMENTS.md` returns 1 match (new CLOSE-01 prose)
    - `grep -F 'set one line in `[tool.pytest.ini_options]`' .planning/REQUIREMENTS.md` returns 1 match (new CLOSE-03 prose)
    - `grep -F 'register(config=Config())' .planning/REQUIREMENTS.md` returns ZERO matches (stale text removed from CLOSE-01)
    - `grep -F 'write three lines in `conftest.py`' .planning/REQUIREMENTS.md` returns ZERO matches (stale text removed from CLOSE-03)
    - `grep -F 'tests/contract/conftest.py' .planning/REQUIREMENTS.md` returns ZERO matches (Phase 27 D-05 deleted this dir; stale CLOSE-01 reference removed)
    - `grep -F 'pytest_generate_tests' .planning/REQUIREMENTS.md` returns ZERO matches (Phase 27 D-04 stale reference removed)
    - `grep -F 'mcp-test-framework run' .planning/REQUIREMENTS.md` returns ZERO matches in CLOSE-01 (replaced with `mcp-contracts run`)
    - `grep -F '- [ ] **CLOSE-02**' .planning/REQUIREMENTS.md` returns 1 match (CLOSE-02 checkbox + text unchanged)
    - `grep -F '- [ ] **CLOSE-04**' .planning/REQUIREMENTS.md` returns 1 match (CLOSE-04 checkbox + text unchanged)
    - `grep -F '| CLOSE-01 | Phase 30 | Pending |' .planning/REQUIREMENTS.md` returns 1 match (traceability table row unchanged — flip to Complete happens at phase close, not now)
    - `grep -F '| CLOSE-03 | Phase 30 | Pending |' .planning/REQUIREMENTS.md` returns 1 match (same)
    - Sub-action B grep checks (re-verify operator-facing files are clean):
      - `grep -E '(Phase [0-9]|D-[0-9]|SC[0-9]|CLOSE-|LIB-|CFG-|CODEGEN-LIB-|REPORTER-|RENAME-|PACK-)' README.md` returns ZERO matches
      - `grep -E '(Phase [0-9]|D-[0-9]|SC[0-9]|CLOSE-|LIB-|CFG-|CODEGEN-LIB-|REPORTER-|RENAME-|PACK-)' config.example.yaml` returns ZERO matches
      - `grep -E '(Phase [0-9]|D-[0-9]|SC[0-9]|CLOSE-|LIB-|CFG-|CODEGEN-LIB-|REPORTER-|RENAME-|PACK-)' .env.example` returns ZERO matches
      - `grep -E '(Phase [0-9]|D-[0-9]|SC[0-9]|CLOSE-|LIB-|CFG-|CODEGEN-LIB-|REPORTER-|RENAME-|PACK-)' docs/EXTENDING.md` returns ZERO matches
  </acceptance_criteria>
  <done>REQUIREMENTS.md CLOSE-01 and CLOSE-03 text reflects Phase 27 D-01/D-07 reality (no more `register(config=Config())` or `tests/contract/conftest.py` ghosts); CLOSE-02 and CLOSE-04 unchanged; traceability table unchanged; four operator-facing surfaces (README, config.example.yaml, .env.example, docs/EXTENDING.md) re-verified clean of planning-ID leaks.</done>
</task>

</tasks>

<verification>
- Library-mode reference doc exists at expected path with sufficient depth: `wc -l docs/LIBRARY-MODE.md` >= 220
- README leads with library mode: top 40 lines of README.md include `mcp_config_file` and `[tool.pytest.ini_options]`
- CLI usage demoted (not removed): `## Appendix: CLI usage` heading exists at a line below `## Library mode`
- REQUIREMENTS amendments shipped: CLOSE-01 + CLOSE-03 new text present, stale text gone, CLOSE-02 + CLOSE-04 unchanged
- No regression in ID-leak-clean status: 4 operator-facing surfaces still match-free
- No emojis introduced in any of the 4 modified files
</verification>

<success_criteria>
Plan complete when:
- [ ] `docs/LIBRARY-MODE.md` exists (220+ lines, follows research outline, links to TEST-CODE-AUTHORING.md and ERROR-STYLE.md, covers all 14 required H2 sections, includes the "Running the parity gate locally" section per Plan 01 D-02a doc handoff).
- [ ] `README.md` leads with library-mode Quickstart, has `## Appendix: CLI usage` at the end, preserves current CLI command docs verbatim in the Appendix, contains no planning-ID leaks.
- [ ] `.planning/REQUIREMENTS.md` CLOSE-01 + CLOSE-03 amended verbatim per CONTEXT.md `<downstream_impact>`; CLOSE-02 + CLOSE-04 unchanged; traceability rows still `Pending`.
- [ ] Re-verification grep confirms README.md, config.example.yaml, .env.example, docs/EXTENDING.md all remain free of planning-ID leaks.
- [ ] No emojis in any modified file.
- [ ] CLOSE-03 acceptance criteria satisfied (new operator reading README sees library-mode usage first; CLI demoted to Appendix; LIBRARY-MODE.md is the primary library API reference).
- [ ] CLOSE-01 acceptance criteria PARTIALLY satisfied (text amendment portion — the verification-of-green-dogfood-loop portion lands in Plan 04).
</success_criteria>

<output>
After completion, create `.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-02-SUMMARY.md` documenting:
- Files created (docs/LIBRARY-MODE.md)
- Files modified (README.md, .planning/REQUIREMENTS.md)
- Section structure of new LIBRARY-MODE.md (one-line per H2)
- Confirmation grep counts for the 6 critical assertions (replacement text present; stale text gone; CLOSE-02 + CLOSE-04 unchanged)
- ID-leak verification status for the 4 operator-facing surfaces
- Any deviations from the locked outline (expected: none)
</output>
