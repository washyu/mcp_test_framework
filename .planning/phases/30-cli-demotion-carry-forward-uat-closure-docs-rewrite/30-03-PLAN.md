---
phase: 30-cli-demotion-carry-forward-uat-closure-docs-rewrite
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md
autonomous: true
requirements: [CLOSE-04]
tags: [uat, capture-protocol, user-driven, carry-forward, live-stack]

must_haves:
  truths:
    - "A 30-UAT.md capture-protocol document exists with one section per carry-forward UAT (four total)"
    - "Each UAT section is a CAPTURE PROTOCOL — exact commands + pass criteria + paste-evidence block — NOT an executed test result"
    - "The document declares status: pending in frontmatter and 'Phase 30 VERIFICATION.md does NOT block on UAT closure' in prose"
    - "UAT-1 (README test-code-scenarios re-capture), UAT-2 (Phase 17 ~70-tool codegen + pyright), UAT-3 (v1.2 v2-config walkthrough with library-mode example), UAT-4 (v1.2 Phase 14 live-smoke under CLI + library modes) each have their own section"
    - "Per memory feedback_uat_must_be_user_driven: these are user-driven captures; no task in this plan executes the UATs"
  artifacts:
    - path: ".planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md"
      provides: "Capture-protocol document for the four CLOSE-04 carry-forward UATs"
      min_lines: 130
      contains: "UAT-1"
  key_links:
    - from: ".planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md"
      to: "README.md §test-code-scenarios snapshot (UAT-1 target)"
      via: "carry-forward reference"
      pattern: "README"
    - from: ".planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md"
      to: "Phase 17 17-VERIFICATION.md (UAT-2 carry-from)"
      via: "carry-forward reference"
      pattern: "Phase 17"
    - from: ".planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md"
      to: "Phase 13 13-VERIFICATION.md + Phase 14 14-VERIFICATION.md (UAT-3 + UAT-4 carry-from)"
      via: "carry-forward references"
      pattern: "Phase 1[34]"
---

<objective>
Deliver CLOSE-04 — author the carry-forward live-UAT capture protocol document.

Per memory `feedback_uat_must_be_user_driven` and CONTEXT.md `<decisions>` Claude's-discretion default: this plan ships the CAPTURE PROTOCOL only. The user runs the four UATs in a follow-up session against live homelab-mcp + Proxmox keyring access; PASS evidence pastes into the document then. Phase 30 VERIFICATION.md does NOT block on UAT closure — UAT execution is tracked separately.

Output:
- New file `.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` with:
  - YAML frontmatter (status: pending, source: [30-CONTEXT.md, 30-VERIFICATION.md])
  - Four UAT sections — one per carry-forward item (UAT-1..UAT-4 from research §"Live-UAT capture protocol shape" lines 472-611)
  - Summary block (total: 4 / pending: 4)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-CONTEXT.md
@.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-RESEARCH.md
@.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-PATTERNS.md

# Existing UAT shape analogs
@.planning/phases/29-live-domain-ui-reporter-plugin/29-HUMAN-UAT.md

<uat_bodies>
<!-- Verbatim source for each UAT section. Copy these directly into 30-UAT.md, formatted per the template below. -->

## UAT-1 source (research §"UAT-1" lines 504-528)

**Title:** README test-code-scenarios PASS-sample re-capture
**Background:** README §"test-code scenarios" still shows pre-Phase-24 FAIL output for the Proxmox VM lifecycle scenario. Phase 24 shipped the `exclude_unset=True` fix; the snapshot needs re-capture against live Proxmox + homelab-mcp.
**Carried from:** Phase 24 Plan 24-02 Task 3a/3b — deferred at Plan 24-02 close (live-uat row in STATE.md Deferred Items).
**Pre-reqs:**
- Operator shell with Proxmox keyring access (agent's PowerShell session cannot reach the keyring per STATE.md deferred-items)
- `MCPTF_DOGFOOD_PROXMOX_HOST=192.168.10.20` set
- `homelab-mcp` reachable via `uvx`
- Ollama reachable for any contract pass (separate run; UAT-1 is test-code only, no judges)

**Commands:**
```powershell
$env:MCPTF_DOGFOOD_PROXMOX_HOST = "192.168.10.20"
uv run mcp-contracts run --test-code --config config.yaml
```

**Expected observable outcome:** All test-code scenarios pass; output shape matches the README §"test-code scenarios" snapshot format with PASS rows instead of FAIL.

**Pass criteria:**
- [ ] No `ToolCallError` from upstream homelab-mcp `inputSchema` bug surfaces (Phase 24 fix holds)
- [ ] Output matches README §"test-code scenarios" snapshot shape (headers, per-tool rows, summary)
- [ ] Re-snapshot pasted verbatim into README §"test-code scenarios", replacing the pre-Phase-24 FAIL block
- [ ] The HTML sentinel comment at README §"test-code scenarios" (`<!-- noqa: sdet-rename-shim — ... -->` if present) is removed once the snapshot is current

---

## UAT-2 source (research §"UAT-2" lines 529-550)

**Title:** Phase 17 SC1 — `gen-test-classes` at ~70-tool scale + pyright clean
**Background:** Phase 17 codegen target was ~70 tools. Live UAT was deferred at v1.3 close; needs live homelab-mcp to enumerate the full tool surface and pyright run on the generated output.
**Carried from:** Phase 17 17-VERIFICATION.md `human_needed` row (STATE.md "Acknowledged at v1.3 milestone close").
**Pre-reqs:**
- `homelab-mcp` runnable via `uvx`, exposing ~70 tools
- `pyright` installed (dev dep)
- A `config.yaml` with `test_code.generated_root` set to a writable path

**Commands:**
```bash
uv run mcp-contracts gen-test-classes --config config.yaml
uv run pyright <path-from-cfg.test_code.generated_root>/<server_slug>/
```

**Expected observable outcome:** `gen-test-classes` writes ~70 typed Pydantic Params + Response class pairs; `pyright` reports zero errors / zero warnings.

**Pass criteria:**
- [ ] Generated file count matches live tool count (e.g., `Get-ChildItem <path> -Recurse -Filter 'test_*.py' | Measure-Object -Line` ≈ 70 on PowerShell, or `find <path> -name 'test_*.py' | wc -l` on POSIX)
- [ ] `pyright` exit code 0
- [ ] No `# type: ignore` or `# pyright: ignore` lines in generated code

---

## UAT-3 source (research §"UAT-3" lines 551-578)

**Title:** v1.2 Phase 13 v2 config + migration walkthrough — library-mode example
**Background:** Phase 13 v1→v2 schema migration; live UAT was deferred at v1.2 close. Phase 30 closes it by demonstrating the migration walkthrough using the library-mode `mcp_config_file` ini route.
**Carried from:** Phase 13 13-VERIFICATION.md `human_needed` row.
**Pre-reqs:**
- A v1-schema `config.yaml` to migrate (legacy fixture or hand-crafted)
- pyproject.toml with `[tool.pytest.ini_options] mcp_config_file = "./config.yaml"` set

**Commands:**
```bash
# Start with v1-schema config; observe migration error
uv run pytest --collect-only

# Apply the migration per docs/MIGRATION-v1-to-v2.md
uv run mcp-contracts config-init -o config.yaml.new

# Compare new vs old; merge by hand or replace
uv run pytest --collect-only
```

**Expected observable outcome:** First `pytest --collect-only` emits a fail-loud v1→v2 migration error naming `version: 1` and pointing at the migration doc; second `pytest --collect-only` succeeds and shows injected contract tests.

**Pass criteria:**
- [ ] Migration error names the schema version field
- [ ] Migration error references `mcp-contracts config-init` as the next step
- [ ] After migration, contract tests collect under `<mcp-contracts>::test_*[*]` nodeids
- [ ] No `MCPTF_CONFIG_FILE` env var set during the walkthrough (proves library-mode is the documented route)

---

## UAT-4 source (research §"UAT-4" lines 579-611)

**Title:** v1.2 Phase 14 live-smoke + visual domain UI under both CLI and library modes
**Background:** Phase 14 hybrid runner + visual domain UI; live UAT deferred at v1.2 close. Phase 30 closes it by capturing the domain UI output under BOTH CLI mode (`mcp-contracts run`) and library mode (`pytest --mcp-domain-ui=force`) and confirming visual parity.
**Carried from:** Phase 14 14-VERIFICATION.md `human_needed` row.
**Pre-reqs:**
- Live homelab-mcp + Ollama reachable
- `config.yaml` with at least 2-3 enabled tools

**Commands:**
```powershell
# CLI mode (PowerShell — Windows-first dev environment per CLAUDE.md)
uv run mcp-contracts run --config config.yaml *>&1 | Tee-Object -FilePath cli_output.txt

# Library mode (force the reporter even in non-TTY contexts)
uv run pytest -o "mcp_config_file=./config.yaml" --mcp-domain-ui=force *>&1 | Tee-Object -FilePath lib_output.txt

# Visual diff
Compare-Object (Get-Content cli_output.txt) (Get-Content lib_output.txt)
```

Or POSIX equivalent:
```bash
uv run mcp-contracts run --config config.yaml > cli_output.txt 2>&1
uv run pytest -o "mcp_config_file=./config.yaml" --mcp-domain-ui=force > lib_output.txt 2>&1
diff -u cli_output.txt lib_output.txt
```

**Expected observable outcome:**
- Both outputs show the MCP domain UI header / per-tool rows / `Result:` summary
- CLI mode wraps pytest's native output (none visible by default)
- Library mode emits domain UI ADDITIVE to pytest's native output (per Phase 29 reporter design)
- Per-tool rows + summary line agree between the two runs (same tools, same outcomes)

**Pass criteria:**
- [ ] CLI mode shows the domain UI without pytest framing
- [ ] Library mode shows BOTH pytest native output AND the domain UI
- [ ] Per-tool verdicts (PASS / FAIL / SKIP) match across both routes
- [ ] `Result:` summary line agrees (same PASS / FAIL / SKIP counts) between the two
</uat_bodies>

<frontmatter_and_section_template>
<!-- Document shell to author. -->

```yaml
---
status: pending
phase: 30-cli-demotion-carry-forward-uat-closure-docs-rewrite
source: [30-CONTEXT.md, 30-VERIFICATION.md]
started: 2026-05-17T00:00:00Z
updated: 2026-05-17T00:00:00Z
---
```

After frontmatter, an intro paragraph that establishes:
- This is a CAPTURE PROTOCOL document — user-driven (memory `feedback_uat_must_be_user_driven`).
- Phase 30 VERIFICATION.md does NOT block on UAT closure.
- UATs close in a follow-up session when the user has Proxmox keyring + live homelab-mcp + Ollama access.
- Closing each UAT means pasting evidence into the relevant `Evidence (paste here after running)` block and flipping the `Status:` line.

Then an H2 `## Tests` (or equivalent) heading.

Each UAT renders as:
```markdown
### UAT-N: <title>
**Background:** <one-line context>
**Carried from:** <Phase/Plan reference>
**Pre-reqs:**
- <bullet 1>
- <bullet 2>

**Commands:**
\`\`\`<shell>
<command lines>
\`\`\`

**Expected observable outcome:** <prose>

**Pass criteria:**
- [ ] <criterion 1>
- [ ] <criterion 2>

**Evidence (paste here after running):**
\`\`\`
<placeholder for paste — leave a hint like "<paste stdout / screenshot reference / file snapshot here>">
\`\`\`

**Status:** [ ] pending / [ ] pass / [ ] fail / [ ] blocked
**Notes:** <empty for now>
```

Closing summary block at bottom:
```yaml
## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0
```
</frontmatter_and_section_template>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author 30-UAT.md with all four carry-forward UAT capture protocols</name>
  <files>.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md</files>
  <read_first>
    - .planning/phases/29-live-domain-ui-reporter-plugin/29-HUMAN-UAT.md (analog for YAML frontmatter + section style — note 29 uses a simpler 3-field shape; Phase 30 uses the richer template from 30-RESEARCH.md per CONTEXT.md `<decisions>`)
    - .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-RESEARCH.md §"Live-UAT capture protocol shape" (lines 472-611, full section including UAT-1..UAT-4 bodies)
    - .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-PATTERNS.md §`.planning/phases/30-.../30-UAT.md (CREATE: capture-protocol document)`
    - The `<uat_bodies>` and `<frontmatter_and_section_template>` blocks above (verbatim source for each UAT section)
  </read_first>
  <action>
Create `.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` with this exact structural layout:

1. YAML frontmatter (per template above):
   ```yaml
   ---
   status: pending
   phase: 30-cli-demotion-carry-forward-uat-closure-docs-rewrite
   source: [30-CONTEXT.md, 30-VERIFICATION.md]
   started: 2026-05-17T00:00:00Z
   updated: 2026-05-17T00:00:00Z
   ---
   ```

2. Intro section (`## About this document` or similar) with 3-4 short paragraphs covering:
   - This is a CAPTURE PROTOCOL — user-driven.
   - Phase 30 VERIFICATION.md does NOT block on UAT closure.
   - UATs require live homelab-mcp + Ollama + Proxmox keyring access (per UAT pre-reqs).
   - Closing a UAT means: run commands → paste output into Evidence block → check Pass criteria boxes → flip Status line → bump Summary block counts.
   - Note: per memory `feedback_uat_must_be_user_driven`, these are user intent checks ("does the operator perceive PASS?"), NOT framework contract assertions.

3. `## Tests` H2 heading.

4. Four UAT sections (UAT-1, UAT-2, UAT-3, UAT-4) — render each per the template in `<frontmatter_and_section_template>` using the verbatim source content from `<uat_bodies>`.

   Each section MUST include:
   - `### UAT-N: <title>` heading
   - `**Background:**` line
   - `**Carried from:**` line
   - `**Pre-reqs:**` bullet list
   - `**Commands:**` fenced code block (PowerShell-first for UAT-1 + UAT-4 since user is Windows-primary per env metadata; bash where applicable per source)
   - `**Expected observable outcome:**` prose
   - `**Pass criteria:**` bullet checklist (matches source verbatim)
   - `**Evidence (paste here after running):**` empty fenced block with a placeholder hint
   - `**Status:** [ ] pending / [ ] pass / [ ] fail / [ ] blocked` line
   - `**Notes:** <empty>` line

5. Summary block at end:
   ```yaml
   ## Summary

   total: 4
   passed: 0
   issues: 0
   pending: 4
   skipped: 0
   blocked: 0
   ```

Strict requirements:
- The exact source content for each UAT-N is in `<uat_bodies>` above. Use those bodies verbatim — do NOT re-author or paraphrase the Background/Carried-from/Pre-reqs/Commands/Expected/Pass-criteria.
- UAT-1 commands use PowerShell syntax (Windows-first env per CLAUDE.md). UAT-4 provides BOTH PowerShell AND POSIX command blocks.
- The Evidence blocks MUST be empty (with a placeholder hint inside) — DO NOT pre-fill with sample output. The user pastes their own evidence at execution time.
- The Status lines MUST be `[ ] pending / [ ] pass / [ ] fail / [ ] blocked` (all four checkboxes unchecked) — DO NOT pre-flip any state.
- The intro section must explicitly state Phase 30 VERIFICATION.md does NOT block on UAT closure.
- No emojis.
- No planning-ID leaks beyond the natural Phase-NN references in the "Carried from:" lines (which are required for traceability).

What NOT to do:
- Do NOT execute any of the UATs — they require Proxmox keyring + live homelab-mcp + Ollama, which the agent's shell cannot access (per STATE.md deferred-items + memory).
- Do NOT add gating or "must close before phase verification" language — UATs explicitly DO NOT block phase close per CONTEXT.md `<decisions>` Claude's-discretion default.
- Do NOT include CLOSE-04 as a planning ID in the operator-readable prose — paraphrase (e.g., "carry-forward live-UAT items from v1.2 / v1.3 close"). The frontmatter and the per-UAT "Carried from:" lines are the correct surfaces for traceability identifiers.

Length target: 130-250 lines (4 UATs × ~30 lines each = 120, plus frontmatter + intro + summary).
  </action>
  <verify>
    <automated>test -f .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md && grep -cF '### UAT-' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md | grep -F -q 4</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md`
    - `wc -l .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` reports at least 130 lines
    - `head -n 10 .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` contains `status: pending`
    - `head -n 10 .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` contains `phase: 30-cli-demotion-carry-forward-uat-closure-docs-rewrite`
    - `grep -c -F '### UAT-' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns exactly 4
    - `grep -F '### UAT-1:' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns 1 match
    - `grep -F '### UAT-2:' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns 1 match
    - `grep -F '### UAT-3:' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns 1 match
    - `grep -F '### UAT-4:' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns 1 match
    - `grep -c -F '**Background:**' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns exactly 4
    - `grep -c -F '**Carried from:**' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns exactly 4
    - `grep -c -F '**Pre-reqs:**' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns exactly 4
    - `grep -c -F '**Commands:**' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns exactly 4
    - `grep -c -F '**Pass criteria:**' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns exactly 4
    - `grep -c -F '**Evidence (paste here after running):**' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns exactly 4
    - `grep -c -F '**Status:** [ ] pending / [ ] pass / [ ] fail / [ ] blocked' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns exactly 4 (no status pre-flipped)
    - `grep -F 'MCPTF_DOGFOOD_PROXMOX_HOST' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns at least 1 match (UAT-1 pre-req)
    - `grep -F 'gen-test-classes' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns at least 1 match (UAT-2 command)
    - `grep -F 'config-init' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns at least 1 match (UAT-3 command + pass criterion)
    - `grep -F '--mcp-domain-ui=force' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns at least 1 match (UAT-4 library-mode command)
    - `grep -F 'VERIFICATION.md does NOT block' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns at least 1 match (intro language)
    - `grep -F 'user-driven' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns at least 1 match (intro language)
    - `grep -F 'total: 4' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns 1 match (summary block)
    - `grep -F 'pending: 4' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns 1 match
    - `grep -F 'passed: 0' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns 1 match (no UATs pre-marked passed)
    - `grep -P '[\x{1F300}-\x{1FAFF}]|[\x{2600}-\x{27BF}]' .planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-UAT.md` returns ZERO matches (no emojis)
  </acceptance_criteria>
  <done>30-UAT.md exists as a capture-protocol document with four UAT sections (UAT-1..UAT-4) each containing Background / Carried-from / Pre-reqs / Commands / Expected / Pass-criteria / Evidence / Status fields per template. All Status lines remain unflipped (pending). Summary block reads total: 4 / pending: 4. Intro establishes user-driven, non-blocking nature of UAT closure.</done>
</task>

</tasks>

<verification>
- 30-UAT.md exists at expected path with frontmatter, 4 UAT sections, and summary block
- Each UAT section carries all 8 required fields (Background, Carried from, Pre-reqs, Commands, Expected, Pass criteria, Evidence, Status)
- Status fields show all checkboxes empty (no pre-flipping)
- Summary block agrees with section count
- Intro explicitly notes user-driven nature and that Phase 30 VERIFICATION.md does NOT block on UAT closure
- No emojis
- No tasks in this plan execute any UAT (per memory `feedback_uat_must_be_user_driven`)
</verification>

<success_criteria>
Plan complete when:
- [ ] `30-UAT.md` exists in the phase directory.
- [ ] All four UAT sections (UAT-1: README scenarios re-capture, UAT-2: ~70-tool codegen + pyright, UAT-3: v2-config walkthrough, UAT-4: live-smoke under both modes) are present with verbatim source content.
- [ ] All Status lines are `[ ] pending / [ ] pass / [ ] fail / [ ] blocked` (no checkboxes pre-flipped).
- [ ] Evidence blocks are empty (placeholders only) — the user pastes evidence at UAT execution time.
- [ ] Summary block matches state: total: 4, pending: 4, passed: 0.
- [ ] Intro explicitly states UATs are user-driven AND that Phase 30 VERIFICATION.md does NOT block on UAT closure.
- [ ] CLOSE-04 acceptance criteria satisfied (capture protocol exists for all four carry-forward UAT items).
</success_criteria>

<output>
After completion, create `.planning/phases/30-cli-demotion-carry-forward-uat-closure-docs-rewrite/30-03-SUMMARY.md` documenting:
- File created (30-UAT.md)
- UAT count verification (4 sections present)
- Confirmation that no UATs were executed — protocols only
- Summary block initial state (total: 4, pending: 4)
- Note for user: follow-up session needed to run UATs against live homelab-mcp + Proxmox + Ollama (no commitment to that timing here)
</output>
