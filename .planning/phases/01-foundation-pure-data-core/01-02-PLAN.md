---
phase: 01-foundation-pure-data-core
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/mcp_test_framework/models.py
  - src/mcp_test_framework/config.py
  - tests/unit/test_config.py
  - .env.example
  - config.example.yaml
autonomous: true
requirements: [CORE-01, DOCS-02]
tags: [pydantic, pydantic-settings, config, env, yaml]

must_haves:
  truths:
    - "Config() returns a frozen, hashable Pydantic Config model with ollama, mcp_server, target sub-models populated from defaults"
    - "Setting OLLAMA_BASE_URL in env overrides the default in Config().ollama.base_url"
    - "A YAML file pointed to by MCPTF_CONFIG_FILE overlays defaults but is overridden by env vars"
    - "An init kwarg passed to Config(target=TargetConfig(tool_name='X')) overrides env, .env, and YAML — verifying CLI > env > .env > YAML > default precedence"
    - "Mutating a frozen Config field raises pydantic.ValidationError (or pydantic_core.ValidationError)"
    - "Mutating a frozen sub-model field (e.g. config.ollama.model = 'X') also raises — verifies Assumption A5"
    - ".env.example enumerates every spec env var; config.example.yaml mirrors them in YAML form"
  artifacts:
    - path: "src/mcp_test_framework/models.py"
      provides: "OllamaConfig, McpServerConfig, TargetConfig — frozen Pydantic BaseModel sub-models with validation_alias on every spec env-var-mapped field"
      exports: ["OllamaConfig", "McpServerConfig", "TargetConfig"]
    - path: "src/mcp_test_framework/config.py"
      provides: "Config(BaseSettings) with settings_customise_sources implementing CLI > env > .env > YAML > default precedence; bare env names resolved via validation_alias on sub-model fields (NOT env_nested_delimiter)"
      exports: ["Config"]
    - path: "tests/unit/test_config.py"
      provides: "Unit tests for default values, env override, YAML overlay, init-kwarg override, frozen enforcement (top-level + sub-models)"
      min_lines: 80
    - path: ".env.example"
      provides: "Template enumerating every env var from spec §Configuration"
      contains: "OLLAMA_BASE_URL="
    - path: "config.example.yaml"
      provides: "Template YAML overlay mirroring env vars under nested keys"
      contains: "ollama:"
  key_links:
    - from: "src/mcp_test_framework/config.py"
      to: "src/mcp_test_framework/models.py"
      via: "from mcp_test_framework.models import OllamaConfig, McpServerConfig, TargetConfig"
      pattern: "from mcp_test_framework\\.models import"
    - from: "Config.settings_customise_sources"
      to: "MCPTF_CONFIG_FILE env var → YamlConfigSettingsSource"
      via: "os.environ.get('MCPTF_CONFIG_FILE') + Path.is_file() guard"
      pattern: "MCPTF_CONFIG_FILE"
    - from: "tests/unit/test_config.py"
      to: "Config and sub-models"
      via: "from mcp_test_framework.config import Config; from mcp_test_framework.models import ..."
      pattern: "from mcp_test_framework"
---

<objective>
Implement the Phase 1 configuration layer: frozen Pydantic sub-models in models.py, a frozen Config(BaseSettings) in config.py with explicit `settings_customise_sources` returning the user-locked precedence (CLI > env > .env > YAML > defaults), unit tests that prove every level of that precedence, and the DOCS-02 example files (.env.example, config.example.yaml).

Purpose: `Config` is the only Phase 1 export consumed by every downstream phase (Phase 2 reads `mcp_server.{command,args}`, Phase 3 reads `ollama.{base_url,model,timeout_seconds}`, Phase 4 reads `target.tool_name`). Phase 1 must lock the schema and prove the precedence works — including the non-obvious `error.absolute_path`-style behaviors.

Output: A frozen `Config()` callable that resolves layered sources correctly, plus passing tests, plus example config files developers can copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-pure-data-core/01-CONTEXT.md
@.planning/phases/01-foundation-pure-data-core/01-RESEARCH.md
@docs/mcp_test_framework_mvp_spec.md
@pyproject.toml

<interfaces>
<!-- The downstream consumer contracts. After this plan ships, these are stable. -->

src/mcp_test_framework/models.py exports:
```python
class OllamaConfig(BaseModel):
    base_url: str = Field(default="http://127.0.0.1:11434",
                          validation_alias=AliasChoices("OLLAMA_BASE_URL"))
    model: str = Field(default="qwen3.6:latest",
                       validation_alias=AliasChoices("OLLAMA_MODEL"))
    timeout_seconds: int = Field(default=120, ge=1,
                                 validation_alias=AliasChoices("OLLAMA_TIMEOUT_SECONDS"))

class McpServerConfig(BaseModel):
    command: str = Field(default="homelab-mcp",
                         validation_alias=AliasChoices("MCP_SERVER_COMMAND"))
    args: list[str] = Field(default_factory=list,
                            validation_alias=AliasChoices("MCP_SERVER_ARGS"))

class TargetConfig(BaseModel):
    tool_name: str = Field(default="list_registered_servers",
                           validation_alias=AliasChoices("TARGET_TOOL_NAME"))
```

src/mcp_test_framework/config.py exports:
```python
class Config(BaseSettings):
    ollama: OllamaConfig
    mcp_server: McpServerConfig
    target: TargetConfig
    judge_timeout_seconds: int = 120  # bare name JUDGE_TIMEOUT_SECONDS works without alias (top-level field)
    # frozen=True; env_file=".env"; extra="ignore"
    # NO env_nested_delimiter — bare env names are routed to sub-model fields via validation_alias
    # settings_customise_sources returns
    #   (init_settings, env_settings, dotenv_settings,
    #    [YamlConfigSettingsSource(settings_cls, yaml_file=MCPTF_CONFIG_FILE) if set],
    #    file_secret_settings)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement models.py and config.py with layered precedence</name>
  <files>src/mcp_test_framework/models.py, src/mcp_test_framework/config.py</files>
  <read_first>
    - .planning/phases/01-foundation-pure-data-core/01-RESEARCH.md `### Pattern 2: models.py — Cross-Cutting Config Sub-Models` (verbatim shape)
    - .planning/phases/01-foundation-pure-data-core/01-RESEARCH.md `### Pattern 3: config.py — BaseSettings with Layered Precedence` (verbatim shape including settings_customise_sources signature)
    - .planning/phases/01-foundation-pure-data-core/01-RESEARCH.md `### Pitfall 1: pydantic-settings Source Ordering Inverted` (leftmost-wins reminder)
    - .planning/phases/01-foundation-pure-data-core/01-RESEARCH.md `### Assumptions Log` (A5: sub-models need their own ConfigDict(frozen=True))
    - .planning/phases/01-foundation-pure-data-core/01-CONTEXT.md `### Decisions > Env var naming convention` (LOCKED to bare names — drives the validation_alias design)
    - docs/mcp_test_framework_mvp_spec.md §Configuration (env var list — source of truth)
  </read_first>
  <behavior>
    Tests in Task 2 will assert:
    - Test 1: `Config()` with no env / no YAML returns defaults: ollama.base_url=="http://127.0.0.1:11434", ollama.model=="qwen3.6:latest", ollama.timeout_seconds==120, mcp_server.command=="homelab-mcp", mcp_server.args==[], target.tool_name=="list_registered_servers", judge_timeout_seconds==120.
    - Test 2: `monkeypatch.setenv("OLLAMA_BASE_URL","http://other:1")` → Config().ollama.base_url == "http://other:1" (env beats default — bare name routed via validation_alias).
    - Test 3: tmp YAML file with `ollama: { base_url: http://yaml:1 }` + `monkeypatch.setenv("MCPTF_CONFIG_FILE", str(yaml_path))` → Config().ollama.base_url == "http://yaml:1" (YAML beats default).
    - Test 4: tmp YAML sets ollama.base_url=="http://yaml:1" AND monkeypatch sets OLLAMA_BASE_URL="http://env:1" → Config().ollama.base_url == "http://env:1" (env beats YAML — the locked precedence inversion vs spec text).
    - Test 5: `Config(ollama=OllamaConfig(base_url="http://init:1"))` with both env and YAML set → result is "http://init:1" (init/CLI beats env beats YAML).
    - Test 6: `cfg = Config(); cfg.judge_timeout_seconds = 999` raises ValidationError (top-level frozen).
    - Test 7: `cfg = Config(); cfg.ollama.model = "X"` raises ValidationError (sub-model frozen — verifies Assumption A5).
  </behavior>
  <action>
    1. Create `src/mcp_test_framework/models.py` based on Pattern 2 in 01-RESEARCH.md, **with the following deviation from RESEARCH** (per plan-checker iter 1 BLOCKER #1): every sub-model field that has a corresponding spec env var carries an explicit `Field(..., validation_alias=AliasChoices("<BARE_NAME>"))` annotation. This is required because CONTEXT.md "Env var naming convention" LOCKS bare env names (`OLLAMA_BASE_URL`, etc.), and bare names are incompatible with `pydantic-settings`' default nested-env-var routing. Concrete mapping:

       - `OllamaConfig.base_url` → `Field(default="http://127.0.0.1:11434", validation_alias=AliasChoices("OLLAMA_BASE_URL"))`
       - `OllamaConfig.model` → `Field(default="qwen3.6:latest", validation_alias=AliasChoices("OLLAMA_MODEL"))`
       - `OllamaConfig.timeout_seconds` → `Field(default=120, ge=1, validation_alias=AliasChoices("OLLAMA_TIMEOUT_SECONDS"))`
       - `McpServerConfig.command` → `Field(default="homelab-mcp", validation_alias=AliasChoices("MCP_SERVER_COMMAND"))`
       - `McpServerConfig.args` → `Field(default_factory=list, validation_alias=AliasChoices("MCP_SERVER_ARGS"))`
       - `TargetConfig.tool_name` → `Field(default="list_registered_servers", validation_alias=AliasChoices("TARGET_TOOL_NAME"))`

       Each class still has `model_config = ConfigDict(frozen=True)` (Pattern 2 + Assumption A5). Imports: `from pydantic import AliasChoices, BaseModel, ConfigDict, Field`.

    2. Create `src/mcp_test_framework/config.py` based on Pattern 3 in 01-RESEARCH.md, **with the following deviation** (per plan-checker iter 1 BLOCKER #1): the `SettingsConfigDict(...)` call in `Config.model_config` MUST NOT include `env_nested_delimiter`. Keep `frozen=True`, `env_file=".env"`, `env_file_encoding="utf-8"`, `extra="ignore"`, and any case-sensitivity / `populate_by_name` setting Pattern 3 specifies. Reason: bare env names (locked in CONTEXT.md) are routed to sub-model fields via `validation_alias` on each sub-model field (Task 1 step above), not via the nested-delimiter mechanism. `env_nested_delimiter="__"` would force `OLLAMA__BASE_URL` instead of the locked `OLLAMA_BASE_URL`.

       Concretely:
       - `from __future__ import annotations`
       - imports: `os`, `pathlib.Path`, `pydantic.Field`, `pydantic_settings.{BaseSettings, SettingsConfigDict, PydanticBaseSettingsSource, YamlConfigSettingsSource}`, and the three sub-models from `mcp_test_framework.models`.
       - `class Config(BaseSettings)` with:
         - `model_config = SettingsConfigDict(frozen=True, env_file=".env", env_file_encoding="utf-8", extra="ignore")`  # NO env_nested_delimiter
         - Fields: `ollama: OllamaConfig = Field(default_factory=OllamaConfig)`, `mcp_server: McpServerConfig = Field(default_factory=McpServerConfig)`, `target: TargetConfig = Field(default_factory=TargetConfig)`, `judge_timeout_seconds: int = 120`. (`JUDGE_TIMEOUT_SECONDS` works without alias because pydantic-settings uppercases top-level field names by default.)
         - `@classmethod settings_customise_sources(cls, settings_cls, init_settings, env_settings, dotenv_settings, file_secret_settings)` returning `tuple(...)` exactly per Pattern 3 — leftmost-wins order is `(init_settings, env_settings, dotenv_settings, YamlConfigSettingsSource(settings_cls, yaml_file=yaml_path) if MCPTF_CONFIG_FILE set and exists, file_secret_settings)`.

       Lift Pattern 3 verbatim from RESEARCH for the `settings_customise_sources` body (the source-ordering logic) including the inline comment "Precedence: CLI/init > env > .env > YAML > default. In pydantic-settings, leftmost source wins." This comment is itself an acceptance signal for the precedence decision being honored consciously. Then apply the two deviations above (drop `env_nested_delimiter`; document the alias-driven approach in a comment).

       Add an inline comment in config.py near `model_config`: `# env_nested_delimiter intentionally omitted — bare env names (per CONTEXT.md lock) are routed to sub-model fields via validation_alias on each sub-model field. See plan-checker iter 1 BLOCKER #1.`

    3. **Deviation note (record in SUMMARY):** This plan deviates from RESEARCH Pattern 2 (adds `validation_alias` on every sub-model field) and RESEARCH Pattern 3 (drops `env_nested_delimiter` from `SettingsConfigDict`). Reason: CONTEXT.md "Env var naming convention" locks bare env-var names, which are incompatible with `env_nested_delimiter='__'`. Resolution chosen at planning time per plan-checker iter 1 BLOCKER #1 (Option A: validation_alias).

    Use the Write tool. Both files are new.
  </action>
  <verify>
    <automated>uv run python -c "from mcp_test_framework.config import Config; from mcp_test_framework.models import OllamaConfig, McpServerConfig, TargetConfig; cfg = Config(); assert cfg.ollama.base_url=='http://127.0.0.1:11434'; assert cfg.target.tool_name=='list_registered_servers'; assert cfg.mcp_server.args==[]; assert cfg.judge_timeout_seconds==120; print('OK')" && grep -q 'validation_alias=AliasChoices("OLLAMA_BASE_URL")' src/mcp_test_framework/models.py && grep -q 'validation_alias=AliasChoices("OLLAMA_MODEL")' src/mcp_test_framework/models.py && grep -q 'validation_alias=AliasChoices("OLLAMA_TIMEOUT_SECONDS")' src/mcp_test_framework/models.py && grep -q 'validation_alias=AliasChoices("MCP_SERVER_COMMAND")' src/mcp_test_framework/models.py && grep -q 'validation_alias=AliasChoices("MCP_SERVER_ARGS")' src/mcp_test_framework/models.py && grep -q 'validation_alias=AliasChoices("TARGET_TOOL_NAME")' src/mcp_test_framework/models.py && ! grep -q 'env_nested_delimiter' src/mcp_test_framework/config.py && echo OK
    # Frozen-Config check is exercised by tests/unit/test_config.py::test_top_level_config_is_frozen in Task 2 (PowerShell-incompatible multi-line python -c removed per plan-checker iter 1 WARNING #1)</automated>
  </verify>
  <acceptance_criteria>
    - `src/mcp_test_framework/models.py` exists and exports `OllamaConfig`, `McpServerConfig`, `TargetConfig` (verified via `uv run python -c "from mcp_test_framework.models import OllamaConfig, McpServerConfig, TargetConfig"` exits 0)
    - `src/mcp_test_framework/config.py` exists and exports `Config` (verified via `uv run python -c "from mcp_test_framework.config import Config"` exits 0)
    - Each sub-model has `model_config = ConfigDict(frozen=True)` (verified via `grep -c 'frozen=True' src/mcp_test_framework/models.py` returns >= 3)
    - `Config` has `settings_customise_sources` classmethod (verified via `grep -q 'def settings_customise_sources' src/mcp_test_framework/config.py`)
    - Source tuple in `settings_customise_sources` lists `init_settings, env_settings, dotenv_settings` BEFORE the optional `YamlConfigSettingsSource` (verified via `grep -A 20 'sources: list' src/mcp_test_framework/config.py | grep -E 'init_settings|env_settings|dotenv_settings|YamlConfigSettingsSource'` shows them in that order)
    - `MCPTF_CONFIG_FILE` env var is the only YAML path source — no auto-discovery (verified via `grep -q 'MCPTF_CONFIG_FILE' src/mcp_test_framework/config.py` AND `! grep -E "(cwd|getcwd|Path\\(\\.\\)\\.glob).*config" src/mcp_test_framework/config.py`)
    - `Config()` instantiates without raising (verified via the inline python -c above)
    - `src/mcp_test_framework/models.py` contains `validation_alias=AliasChoices("OLLAMA_BASE_URL")` (verified via `grep -q 'validation_alias=AliasChoices("OLLAMA_BASE_URL")' src/mcp_test_framework/models.py`)
    - `src/mcp_test_framework/models.py` contains `validation_alias=AliasChoices("OLLAMA_MODEL")` (verified via `grep -q 'validation_alias=AliasChoices("OLLAMA_MODEL")' src/mcp_test_framework/models.py`)
    - `src/mcp_test_framework/models.py` contains `validation_alias=AliasChoices("OLLAMA_TIMEOUT_SECONDS")` (verified via `grep -q 'validation_alias=AliasChoices("OLLAMA_TIMEOUT_SECONDS")' src/mcp_test_framework/models.py`)
    - `src/mcp_test_framework/models.py` contains `validation_alias=AliasChoices("MCP_SERVER_COMMAND")` (verified via `grep -q 'validation_alias=AliasChoices("MCP_SERVER_COMMAND")' src/mcp_test_framework/models.py`)
    - `src/mcp_test_framework/models.py` contains `validation_alias=AliasChoices("MCP_SERVER_ARGS")` (verified via `grep -q 'validation_alias=AliasChoices("MCP_SERVER_ARGS")' src/mcp_test_framework/models.py`)
    - `src/mcp_test_framework/models.py` contains `validation_alias=AliasChoices("TARGET_TOOL_NAME")` (verified via `grep -q 'validation_alias=AliasChoices("TARGET_TOOL_NAME")' src/mcp_test_framework/models.py`)
    - `src/mcp_test_framework/models.py` imports `AliasChoices` from `pydantic` (verified via `grep -E 'from pydantic import.*AliasChoices' src/mcp_test_framework/models.py`)
    - `src/mcp_test_framework/config.py` does NOT contain `env_nested_delimiter` (verified via `! grep -q 'env_nested_delimiter' src/mcp_test_framework/config.py`)
    - Frozen-Config enforcement is exercised by Task 2's `test_top_level_config_is_frozen` (no inline multi-line python -c here — PowerShell-incompatible per plan-checker iter 1 WARNING #1)
  </acceptance_criteria>
  <done>models.py and config.py exist; Config() returns defaults; bare env names route to sub-model fields via validation_alias; env_nested_delimiter is absent; all imports succeed; ready for tests in Task 2 to prove behavior end-to-end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write tests/unit/test_config.py covering all 5 precedence levels + frozen enforcement (top-level and sub-model)</name>
  <files>tests/unit/test_config.py</files>
  <read_first>
    - tests/unit/__init__.py (verify package marker exists from Plan 01)
    - src/mcp_test_framework/config.py (the module under test — written in Task 1)
    - src/mcp_test_framework/models.py (sub-model imports for init-kwarg test; confirm validation_alias annotations are present)
    - .planning/phases/01-foundation-pure-data-core/01-RESEARCH.md `### Pitfall 1` (test_config_precedence is essentially mandated — covers the source-ordering inversion bug)
    - .planning/phases/01-foundation-pure-data-core/01-RESEARCH.md `### Assumptions Log` (A5 — sub-model frozen test is what verifies the assumption)
  </read_first>
  <behavior>
    Test functions to ship (use plain pytest — no @pytest.mark.asyncio per RESEARCH anti-patterns):

    1. `test_defaults` — `Config()` with `monkeypatch.delenv` cleared for all OLLAMA_*, MCP_*, TARGET_*, MCPTF_* asserts every default value listed in models.py.

    2. `test_env_overrides_default` — `monkeypatch.setenv("OLLAMA_BASE_URL", "http://env:1")` then `Config().ollama.base_url == "http://env:1"`.

    3. `test_yaml_overrides_default` — write a tmp YAML file via tmp_path with `ollama:\n  base_url: http://yaml:1\n`, `monkeypatch.setenv("MCPTF_CONFIG_FILE", str(yaml_path))`, `monkeypatch.delenv("OLLAMA_BASE_URL", raising=False)`, then `Config().ollama.base_url == "http://yaml:1"`.

    4. `test_env_overrides_yaml` — same YAML file, AND `monkeypatch.setenv("OLLAMA_BASE_URL", "http://env:1")`, then `Config().ollama.base_url == "http://env:1"` (the locked precedence: env beats YAML).

    5. `test_init_overrides_env_and_yaml` — same YAML, same env var, AND construct via `Config(ollama=OllamaConfig(base_url="http://init:1"))`, assert `result.ollama.base_url == "http://init:1"`.

    6. `test_top_level_config_is_frozen` — `cfg = Config()`, `with pytest.raises((pydantic_core.ValidationError, ValueError, TypeError)): cfg.judge_timeout_seconds = 999`.

    7. `test_sub_model_is_frozen` — `cfg = Config()`, `with pytest.raises((pydantic_core.ValidationError, ValueError, TypeError)): cfg.ollama.model = "X"`. (Verifies Assumption A5.)

    8. `test_no_yaml_path_skips_yaml_overlay` — `monkeypatch.delenv("MCPTF_CONFIG_FILE", raising=False)`, no YAML file written, `Config()` instantiates and uses defaults; specifically `Config().ollama.base_url == "http://127.0.0.1:11434"`.

    9. `test_invalid_yaml_path_skips_yaml_overlay` — `monkeypatch.setenv("MCPTF_CONFIG_FILE", "/does/not/exist.yaml")`, `Config()` does not raise; uses defaults (Pattern 3's `Path(yaml_path).is_file()` guard).

    Use `monkeypatch` fixture for env. Use `tmp_path` for YAML files. Import `pydantic_core` for ValidationError. Standard `import pytest`, no asyncio marker.
  </behavior>
  <action>
    Write `tests/unit/test_config.py` with the 9 tests described in <behavior>. Each test must:
    - Clear all relevant env vars at start via monkeypatch.delenv (raising=False) for: OLLAMA_BASE_URL, OLLAMA_MODEL, OLLAMA_TIMEOUT_SECONDS, MCP_SERVER_COMMAND, MCP_SERVER_ARGS, TARGET_TOOL_NAME, JUDGE_TIMEOUT_SECONDS, MCPTF_CONFIG_FILE — to avoid leakage between tests.
    - Use synchronous test functions (no async marker — RESEARCH anti-pattern).
    - For YAML tests, use `yaml_path = tmp_path / "config.yaml"; yaml_path.write_text("...")` then setenv.

    Bare env names (`OLLAMA_BASE_URL` etc.) are correct as written — Task 1 already lands the `validation_alias=AliasChoices("OLLAMA_BASE_URL")` (and equivalent) annotations on each sub-model field per the planning-time resolution of plan-checker iter 1 BLOCKER #1, so `monkeypatch.setenv("OLLAMA_BASE_URL", "...")` reaches `cfg.ollama.base_url` directly. Task 2 only writes tests against the as-shipped config; do NOT add adjustments to Task 1, do NOT use double-underscore (`OLLAMA__BASE_URL`) forms.

    Use the Write tool.
  </action>
  <verify>
    <automated>uv run pytest tests/unit/test_config.py -v 2>&1 | tee /tmp/test_config.log; grep -E "passed|failed" /tmp/test_config.log; uv run pytest tests/unit/test_config.py -v --tb=short && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/test_config.py` exists
    - File contains 9 test functions (verified via `grep -c '^def test_' tests/unit/test_config.py` returns 9)
    - All 9 tests pass: `uv run pytest tests/unit/test_config.py -v` exits 0
    - File does NOT contain `@pytest.mark.asyncio` (verified via `! grep -q '@pytest.mark.asyncio' tests/unit/test_config.py` — these are sync tests per RESEARCH anti-pattern)
    - File uses `monkeypatch` fixture (verified via `grep -q 'monkeypatch' tests/unit/test_config.py`)
    - File covers env-overrides-yaml case (verified via `grep -q 'test_env_overrides_yaml' tests/unit/test_config.py`)
    - File covers sub-model frozen case (verified via `grep -q 'test_sub_model_is_frozen' tests/unit/test_config.py`)
    - Specifically `test_init_overrides_env_and_yaml` exists and passes (verified by pytest output containing `test_init_overrides_env_and_yaml PASSED`)
    - Tests use bare env-var names (verified via `grep -q 'OLLAMA_BASE_URL' tests/unit/test_config.py` AND `! grep -q 'OLLAMA__BASE_URL' tests/unit/test_config.py` — no double-underscore form)
  </acceptance_criteria>
  <done>9 tests pass; precedence levels CLI > env > YAML > default each have a dedicated assertion; frozen enforcement verified for top level AND sub-model; bare env-var names work end-to-end via Task 1's validation_alias annotations.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write .env.example and config.example.yaml enumerating every spec env var</name>
  <files>.env.example, config.example.yaml</files>
  <read_first>
    - .planning/phases/01-foundation-pure-data-core/01-RESEARCH.md `### Example D: .env.example` (verbatim content)
    - .planning/phases/01-foundation-pure-data-core/01-RESEARCH.md `### Example E: config.example.yaml` (verbatim content)
    - .planning/phases/01-foundation-pure-data-core/01-CONTEXT.md `### Decisions > Env var naming convention` (the 7 bare names listed)
    - docs/mcp_test_framework_mvp_spec.md §Configuration (the spec list of env vars and YAML schema — source of truth)
    - src/mcp_test_framework/models.py (defaults must match)
  </read_first>
  <action>
    1. Write `.env.example` at repo root verbatim from RESEARCH Example D. It enumerates exactly:
       - `OLLAMA_BASE_URL=http://127.0.0.1:11434`
       - `OLLAMA_MODEL=qwen3.6:latest`
       - `OLLAMA_TIMEOUT_SECONDS=120`
       - `MCP_SERVER_COMMAND=homelab-mcp`
       - `MCP_SERVER_ARGS=`
       - `TARGET_TOOL_NAME=list_registered_servers`
       - `JUDGE_TIMEOUT_SECONDS=120`
       - A commented `# MCPTF_CONFIG_FILE=./config.yaml` line documenting the YAML overlay opt-in
       - Header comment: "copy to .env and edit. Never commit .env."

    2. Write `config.example.yaml` at repo root verbatim from RESEARCH Example E. Top-level YAML keys: `ollama:`, `mcp_server:`, `target:`, `judge_timeout_seconds:`. Values mirror the .env.example defaults. Include the precedence reminder comment: `# YAML overlay sits BELOW env in precedence: CLI > env > .env > YAML > defaults.`

    Use the Write tool. Both files are new.
  </action>
  <verify>
    <automated>test -f .env.example && test -f config.example.yaml && grep -q 'OLLAMA_BASE_URL=' .env.example && grep -q 'OLLAMA_MODEL=' .env.example && grep -q 'OLLAMA_TIMEOUT_SECONDS=' .env.example && grep -q 'MCP_SERVER_COMMAND=' .env.example && grep -q 'TARGET_TOOL_NAME=' .env.example && grep -q 'JUDGE_TIMEOUT_SECONDS=' .env.example && grep -q '^ollama:' config.example.yaml && grep -q '^mcp_server:' config.example.yaml && grep -q '^target:' config.example.yaml && uv run python -c "import yaml; d=yaml.safe_load(open('config.example.yaml')); assert 'ollama' in d and 'mcp_server' in d and 'target' in d; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` exists at repo root
    - `.env.example` contains lines starting with each of: `OLLAMA_BASE_URL=`, `OLLAMA_MODEL=`, `OLLAMA_TIMEOUT_SECONDS=`, `MCP_SERVER_COMMAND=`, `MCP_SERVER_ARGS=`, `TARGET_TOOL_NAME=`, `JUDGE_TIMEOUT_SECONDS=` (each verified via `grep -q`)
    - `.env.example` mentions `MCPTF_CONFIG_FILE` somewhere (commented opt-in OK; verified via `grep -q 'MCPTF_CONFIG_FILE' .env.example`)
    - `config.example.yaml` exists at repo root
    - `config.example.yaml` parses as valid YAML (verified via `python -c "import yaml; yaml.safe_load(open('config.example.yaml'))"` exits 0)
    - `config.example.yaml` has top-level keys `ollama`, `mcp_server`, `target` (verified via the python -c yaml.safe_load assertion above)
    - All 7 spec env vars from CONTEXT.md "Env var naming convention" are present in `.env.example`
  </acceptance_criteria>
  <done>Both example files exist with every spec env var enumerated; YAML parses; bare-name and nested-YAML representations match.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem (.env, config.yaml) → Config | YAML and .env files are local, dev-controlled, but malicious YAML payloads (anchors, !!python/object) are still a vector |
| OS environment → Config | env vars are dev-controlled; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Malicious YAML payload (e.g. !!python/object/apply:os.system) loaded via MCPTF_CONFIG_FILE | mitigate | `pydantic-settings`'s YamlConfigSettingsSource uses `yaml.safe_load` under the hood (PyYAML default for the source class). No custom `yaml.load` calls in our code. Verified by inspecting RESEARCH Pattern 3 — only YamlConfigSettingsSource is invoked |
| T-02-02 | Information Disclosure | `.env` committed to git accidentally | mitigate (cross-plan) | `.gitignore` from Plan 01 contains `.env`; this plan only ships `.env.example` (template, no secrets) |
| T-02-03 | Tampering | Untrusted env var value injected into Config (e.g. OLLAMA_TIMEOUT_SECONDS=evil) | mitigate | Pydantic v2 `Field(ge=1)` on int fields (per Pattern 2); `extra="ignore"` rejects unknown keys silently. Type coercion errors raise ValidationError at Config() construction — fail-fast |
| T-02-04 | Information Disclosure | Config value (future API key) printed in logs | accept (deferred) | Phase 1 doesn't log; documented in RESEARCH Security Domain that all future logging must elide values. Not actionable in this plan |
</threat_model>

<verification>
- `uv run pytest tests/unit/test_config.py -v` exits 0 with 9 tests passing
- `uv run python -c "from mcp_test_framework.config import Config; c = Config(); print(c.model_dump_json())"` runs cleanly
- `python -c "import yaml; yaml.safe_load(open('config.example.yaml'))"` exits 0
- `.env.example` is human-readable and lists all 7 spec env vars
</verification>

<success_criteria>
1. `Config()` resolves a setting with precedence `CLI flag (init kwarg) > env var > .env > YAML > default` — verified by 5 dedicated unit tests (CORE-01 success criterion 4)
2. `Config` exposes a frozen Pydantic model — verified by both top-level and sub-model frozen tests (CORE-01)
3. `.env.example` and `config.example.yaml` enumerate every configurable setting with example values (DOCS-02)
4. `Config` is consumable by the rest of the framework via `from mcp_test_framework.config import Config`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-pure-data-core/01-02-SUMMARY.md` documenting:
- Confirmation that bare env names worked via `validation_alias` annotations on sub-model fields (per plan-checker iter 1 BLOCKER #1 resolution — Option A)
- Confirmation that `env_nested_delimiter` was NOT set on `Config.model_config`
- Final Config schema shape (the dump of `Config().model_dump()`)
- Test count + pass/fail
- Whether Assumption A5 held (sub-model frozen propagation behavior observed)
</output>
