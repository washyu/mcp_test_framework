---
phase: 01-foundation-pure-data-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - .gitignore
  - uv.lock
  - src/mcp_test_framework/__init__.py
  - tests/__init__.py
  - tests/unit/__init__.py
  - main.py
autonomous: true
requirements: [SETUP-01, SETUP-02, SETUP-03]
tags: [python, uv, pyproject, ruff, pytest-asyncio]

must_haves:
  truths:
    - "uv sync on a fresh checkout produces a clean install with uv.lock committed and Python 3.14 honored"
    - "pyproject.toml configures pytest-asyncio asyncio_mode='strict' and asyncio_default_fixture_loop_scope='session'"
    - "ruff TID251 banned-api rule for homelab_mcp is declared in pyproject.toml under [tool.ruff.lint.flake8-tidy-imports.banned-api]"
    - "src/mcp_test_framework/ package is importable (uv run python -c 'import mcp_test_framework' exits 0)"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project metadata, runtime deps, dev deps, pytest+ruff config"
      contains: "[dependency-groups]"
    - path: "uv.lock"
      provides: "Reproducible dependency lockfile (committed)"
      min_lines: 50
    - path: "src/mcp_test_framework/__init__.py"
      provides: "Package marker, makes mcp_test_framework importable"
    - path: "tests/__init__.py"
      provides: "Tests package marker"
    - path: "tests/unit/__init__.py"
      provides: "tests/unit subpackage marker for Phase 1 unit tests"
    - path: ".gitignore"
      provides: "Excludes .env, .venv, __pycache__; explicitly KEEPS uv.lock"
      contains: ".env"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv sync resolves [project.dependencies] + [dependency-groups].dev into uv.lock"
      pattern: "dependency-groups"
    - from: "pyproject.toml [tool.ruff.lint]"
      to: "TID251 enforcement on src/ and tests/"
      via: "select = [..., 'TID'] + flake8-tidy-imports.banned-api['homelab_mcp']"
      pattern: "homelab_mcp"
    - from: "pyproject.toml [tool.pytest.ini_options]"
      to: "tests/ discovery"
      via: "testpaths = ['tests']"
      pattern: "testpaths"
---

<objective>
Bootstrap the project skeleton: rewrite pyproject.toml with runtime deps + dev deps + pytest-asyncio strict config + ruff TID251 banned-api rule, run uv sync to generate and commit uv.lock, create the src package and tests package directories, write .gitignore, delete the obsolete main.py stub.

Purpose: Every other Phase 1 plan depends on this scaffolding. Plans 02 and 03 cannot run their unit tests until uv sync produces a working venv with pytest installed; the ruff TID251 rule declared here is what Plan 04 will smoke-test.

Output: A repo where `uv sync` succeeds, `uv run pytest --collect-only tests/` exits 0 (zero tests, but discovery works), `uv run ruff check` exits 0 against the existing tree, and `import mcp_test_framework` succeeds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-foundation-pure-data-core/01-CONTEXT.md
@.planning/phases/01-foundation-pure-data-core/01-RESEARCH.md
@CLAUDE.md
@docs/mcp_test_framework_mvp_spec.md
@pyproject.toml
@.python-version
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite pyproject.toml with runtime deps, dev deps, pytest-asyncio config, ruff TID251 rule</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current stub — see what already exists)
    - .planning/phases/01-foundation-pure-data-core/01-RESEARCH.md `## Pattern 1: pyproject.toml — Complete Phase 1 Shape` (verbatim shape to apply)
    - .planning/phases/01-foundation-pure-data-core/01-CONTEXT.md (locked decisions on env naming, frozen Config, TID251 mechanism)
    - .python-version (confirm 3.14 pin)
  </read_first>
  <action>
    Apply Pattern 1 from 01-RESEARCH.md verbatim, with these concrete keys/values:

    [project]:
    - name = "mvp-test-framework" (KEEP existing — note hyphen, package dir uses underscore)
    - version = "0.1.0"
    - description = "Pytest framework for testing MCP servers end-to-end."
    - readme = "README.md"
    - requires-python = ">=3.14"
    - dependencies = ["mcp>=1.27", "pydantic>=2.13,<3", "pydantic-settings[yaml]>=2.14", "jsonschema>=4.26"]

    [dependency-groups] (PEP 735 — NOT [tool.uv].dev-dependencies and NOT [project.optional-dependencies].dev):
    - dev = ["pytest>=9.0", "pytest-asyncio>=1.3", "ruff>=0.8"]

    [tool.pytest.ini_options]:
    - asyncio_mode = "strict"
    - asyncio_default_fixture_loop_scope = "session"
    - testpaths = ["tests"]

    [tool.ruff]:
    - line-length = 100
    - target-version = "py314"

    [tool.ruff.lint]:
    - select = ["E", "F", "I", "TID"]

    [tool.ruff.lint.flake8-tidy-imports.banned-api]:
    - "homelab_mcp" = { msg = "Do not import homelab_mcp -- it is a black-box subprocess under test (see PROJECT.md). Drive it via mcp.client.stdio.stdio_client only." }

    Add a comment in the file documenting Pitfall 4 from RESEARCH.md: "TID251 does not catch submodule imports (e.g. from homelab_mcp.client import X). Belt-and-suspenders: tests/conftest.py sys.modules guard catches this gap."

    [build-system]: keep existing hatchling block as-is (whatever uv init produced).

    DO NOT add [project.scripts] mcp-test-framework entry — that lands in Phase 5. RESEARCH Pattern 1 has it commented out; keep it commented or omit.

    DO NOT declare pyyaml directly (pulled in by pydantic-settings[yaml]).
    DO NOT declare python-dotenv (pydantic-settings has built-in dotenv support).
    DO NOT declare httpx, typer, or homelab-mcp.

    Use the Write tool, not heredoc.
  </action>
  <verify>
    <automated>uv run python -c "import tomllib; d=tomllib.loads(open('pyproject.toml','rb').read().decode()); assert d['tool']['pytest']['ini_options']['asyncio_mode']=='strict'; assert d['tool']['pytest']['ini_options']['asyncio_default_fixture_loop_scope']=='session'; assert d['tool']['pytest']['ini_options']['testpaths']==['tests']; assert 'homelab_mcp' in d['tool']['ruff']['lint']['flake8-tidy-imports']['banned-api']; assert any(x.startswith('mcp>=') for x in d['project']['dependencies']); assert any(x.startswith('pydantic-settings') for x in d['project']['dependencies']); assert any(x.startswith('jsonschema') for x in d['project']['dependencies']); assert 'pytest>=9.0' in d['dependency-groups']['dev'] or any(x.startswith('pytest>=') for x in d['dependency-groups']['dev']); print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import tomllib; ..."` confirms `[tool.pytest.ini_options].asyncio_mode == "strict"`
    - `python -c "import tomllib; ..."` confirms `[tool.pytest.ini_options].asyncio_default_fixture_loop_scope == "session"`
    - `python -c "import tomllib; ..."` confirms `[tool.pytest.ini_options].testpaths == ["tests"]`
    - `python -c "import tomllib; ..."` confirms `"homelab_mcp"` key exists under `[tool.ruff.lint.flake8-tidy-imports.banned-api]`
    - `python -c "import tomllib; ..."` confirms `[project].dependencies` contains entries starting with `mcp>=`, `pydantic`, `pydantic-settings`, `jsonschema`
    - `python -c "import tomllib; ..."` confirms `[dependency-groups].dev` contains `pytest`, `pytest-asyncio`, `ruff`
    - File does NOT contain the literal string `[tool.uv]` followed by `dev-dependencies` (legacy form)
    - File does NOT declare `pyyaml` or `python-dotenv` directly (search: `grep -E '^(pyyaml|python-dotenv)' pyproject.toml | grep -v '^#'` returns no matches)
    - `target-version = "py314"` is present under `[tool.ruff]`
  </acceptance_criteria>
  <done>pyproject.toml is rewritten and parses cleanly; all asserted keys present; ready for uv sync.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create src/tests directory scaffolding, .gitignore, run uv sync, delete main.py</name>
  <files>src/mcp_test_framework/__init__.py, tests/__init__.py, tests/unit/__init__.py, .gitignore, uv.lock, main.py</files>
  <read_first>
    - pyproject.toml (after Task 1 — confirms deps to be locked)
    - .planning/phases/01-foundation-pure-data-core/01-RESEARCH.md `### Recommended Project Structure` (file tree to create)
    - .planning/phases/01-foundation-pure-data-core/01-RESEARCH.md `### Pitfall 5: uv.lock Not Committed` (gitignore must NOT include uv.lock)
    - .planning/phases/01-foundation-pure-data-core/01-CONTEXT.md `### Reusable Assets` (delete main.py — nothing depends on it)
    - main.py (confirm it is the hello-world stub before deleting)
  </read_first>
  <action>
    1. Create `src/mcp_test_framework/__init__.py` — empty file (or with `__version__ = "0.1.0"` exported; either is acceptable per RESEARCH).

    2. Create `tests/__init__.py` — empty file.

    3. Create `tests/unit/__init__.py` — empty file.

    4. Create `.gitignore` at repo root with these entries (one per line):
       ```
       # Python
       __pycache__/
       *.py[cod]
       *$py.class
       *.egg-info/
       .pytest_cache/

       # Virtual envs
       .venv/
       venv/

       # Local config / secrets
       .env

       # IDE
       .vscode/
       .idea/

       # OS
       .DS_Store
       Thumbs.db
       ```
       CRITICAL: Do NOT include `uv.lock` in .gitignore (RESEARCH Pitfall 5). The lockfile MUST be committed.

    5. Run `uv sync` from the repo root. This generates `uv.lock` and creates `.venv/`. If uv complains about target-version="py314" on ruff, downgrade to `target-version = "py313"` in pyproject.toml (RESEARCH Open Question 1) and re-run. If `uv sync` fails on `mcp` 1.27 wheel for Python 3.14 (RESEARCH Open Question 2), surface the failure verbatim — do NOT downgrade Python; report and stop.

    6. Delete `main.py` (the hello-world stub from `uv init`).

    7. Confirm package imports: `uv run python -c "import mcp_test_framework; print('ok')"` must print `ok`.
  </action>
  <verify>
    <automated>test -f uv.lock && test -f src/mcp_test_framework/__init__.py && test -f tests/__init__.py && test -f tests/unit/__init__.py && test -f .gitignore && test ! -f main.py && grep -q '^\.env$' .gitignore && ! grep -E '^uv\.lock$' .gitignore && uv run python -c "import mcp_test_framework" && uv run pytest --collect-only tests/ 2>&1 | grep -E 'collected [0-9]+ items|no tests ran' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `uv.lock` exists at repo root and is non-empty (`wc -l uv.lock` > 50)
    - `src/mcp_test_framework/__init__.py` exists
    - `tests/__init__.py` exists
    - `tests/unit/__init__.py` exists
    - `.gitignore` exists and contains a line that is exactly `.env` (verified via `grep -q '^\.env$' .gitignore`)
    - `.gitignore` does NOT contain a line that is exactly `uv.lock` (verified via `! grep -E '^uv\.lock$' .gitignore`)
    - `main.py` does NOT exist (verified via `test ! -f main.py`)
    - `uv run python -c "import mcp_test_framework"` exits 0
    - `uv run pytest --collect-only tests/` exits 0 (zero tests collected is fine; discovery itself must succeed)
    - `uv run ruff check src tests` exits 0 (no offenders yet — empty packages pass)
  </acceptance_criteria>
  <done>uv.lock generated and committed; src and tests package skeletons exist; main.py deleted; package importable; pytest discovery works; ruff is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workstation → repo files | pyproject.toml is hand-edited; uv.lock generated by tool — both are committed source |
| `uv sync` → PyPI | Dependency resolution pulls wheels over HTTPS; supply-chain risk lives here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | uv.lock not committed → drift between machines | mitigate | `.gitignore` explicitly does NOT include `uv.lock`; acceptance check `! grep -E '^uv\.lock$' .gitignore` enforces |
| T-01-02 | Information Disclosure | `.env` file accidentally committed (will hold dev-local config in later phases) | mitigate | `.gitignore` contains `.env`; verified via `grep -q '^\.env$' .gitignore` |
| T-01-03 | Tampering | Compromised PyPI package pulled by `uv sync` | accept | LOW for MVP — single dev workstation; pinned versions in `uv.lock`; standard supply-chain risk no different from any Python project. Revisit with `uv sync --frozen` discipline in Phase 5 README |
| T-01-04 | Elevation of Privilege | Black-box rule bypassed via direct import of `homelab_mcp` | mitigate | ruff TID251 rule declared here; belt (sys.modules guard) ships in Plan 04 |
</threat_model>

<verification>
- `uv sync` produces a clean Python 3.14 install with no errors
- `uv.lock` is non-empty and present in working tree
- `pyproject.toml` parses with `tomllib` and contains all required keys
- `uv run pytest --collect-only tests/` exits 0 (discovery works even with zero tests)
- `uv run python -c "import mcp_test_framework"` exits 0
- `uv run ruff check src tests` exits 0
</verification>

<success_criteria>
1. `uv sync` on a fresh checkout produces a clean install with `uv.lock` committed and Python 3.14 honored (SETUP-01)
2. `pyproject.toml` configures `pytest-asyncio` with `asyncio_mode = "strict"` and `asyncio_default_fixture_loop_scope = "session"` (SETUP-02)
3. `pyproject.toml` declares the ruff `TID251` `banned-api` rule for `homelab_mcp` (partial SETUP-03 — runtime guard ships in Plan 04)
4. `src/mcp_test_framework` is an importable Python package
5. `tests/` directory tree exists and is discovered by pytest
6. `main.py` (hello-world stub) is removed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-pure-data-core/01-01-SUMMARY.md` documenting:
- Versions actually pinned in uv.lock (mcp, pydantic, pydantic-settings, jsonschema, pytest, pytest-asyncio, ruff)
- Whether `target-version = "py314"` accepted on ruff or fallback to py313 was needed
- Any wheel-build issues encountered for mcp 1.27 on Python 3.14
- Confirmation that the package and tests directories are scaffolded
</output>
