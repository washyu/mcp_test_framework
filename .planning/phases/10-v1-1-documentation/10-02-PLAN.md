---
phase: 10-v1-1-documentation
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified:
  - docs/EXTENDING.md
autonomous: true
requirements: [DOC-07]
must_haves:
  truths:
    - "`docs/EXTENDING.md` has a new `## Add a new MCP tool target` section (D-04)"
    - "The new section is placed AFTER the existing `## Swap the judge backend` section and BEFORE `## Further reading` (D-04b -- recommended placement: lighter-weight extension comes after code-extension seams)"
    - "The new section uses a 4-step numbered walkthrough: (1) `mcp-test-framework list-tools`, (2) pick a tool + decide skip/judges/call_arguments, (3) add `tools.<tool_name>:` block, (4) re-run `mcp-test-framework run` and verify per-tool summary (D-04 explicit list)"
    - "The new section's worked example uses the skip-with-reason pattern with abstract placeholder names (D-04c, D-01)"
    - "The new section preceeds the worked YAML block with the D-01b reader-substitution callout"
    - "The new section links back to README's per-tool config schema via `[Per-tool configuration](../README.md#per-tool-configuration)` (D-04a)"
    - "The new section explicitly states 'no code changes required' / 'config-only' (D-04 framing -- this is the lighter-weight seam)"
    - "The new section references README's Block B (judges-subset) rather than re-presenting it (D-04c)"
    - "The new section's opening prose cites `src/mcp_test_framework/models.py` `ToolConfig` as the schema source (PATTERNS.md analog opening pattern)"
    - "EXTENDING.md `## Further reading` gains a link back to README's `#per-tool-configuration` anchor (CD-03)"
    - "EXTENDING.md `## Further reading` gains a link to `config.example.yaml` (CD-03)"
    - "Existing EXTENDING.md sections (rubric, judge backend) are not modified beyond the insertion of the new section"
    - "Anchor `#per-tool-configuration` resolves: the heading `## Per-tool configuration` exists in README.md (cross-document key link verified)"
    - "Anchor `#add-a-new-mcp-tool-target` is generated by the new heading in EXTENDING.md (consumed by README's Further reading link from Plan 10-01)"
  artifacts:
    - path: "docs/EXTENDING.md"
      provides: "New `## Add a new MCP tool target` section with 4-step walkthrough and one complete worked YAML block"
      contains: "## Add a new MCP tool target"
    - path: "docs/EXTENDING.md"
      provides: "Updated Further reading with cross-link back to README per-tool config schema and to config.example.yaml"
      contains: "../README.md#per-tool-configuration"
  key_links:
    - from: "docs/EXTENDING.md `## Add a new MCP tool target` section"
      to: "README.md `## Per-tool configuration` heading (anchor `#per-tool-configuration`)"
      via: "relative Markdown link `[Per-tool configuration](../README.md#per-tool-configuration)`"
      pattern: "\\(\\.\\./README\\.md#per-tool-configuration\\)"
    - from: "README.md Further reading bullet (added in Plan 10-01)"
      to: "docs/EXTENDING.md `## Add a new MCP tool target` heading (anchor `#add-a-new-mcp-tool-target`)"
      via: "GitHub auto-anchor generated from the heading text"
      pattern: "^## Add a new MCP tool target$"
    - from: "docs/EXTENDING.md walkthrough"
      to: "config.example.yaml"
      via: "Further reading bullet"
      pattern: "config\\.example\\.yaml"
---

<objective>
Add ONE new section (`## Add a new MCP tool target`) to `docs/EXTENDING.md` that walks a contributor through covering a new MCP tool via per-tool config alone -- no code changes. Update `## Further reading` with cross-links to the README schema and to `config.example.yaml`.

Purpose: A contributor who has read README's per-tool config schema can use EXTENDING.md to learn the config-only workflow for adopting a new tool target (DOC-07).
Output: docs/EXTENDING.md gains one new section (~40-55 lines) and 2 new Further-reading links. README and other files unchanged.

Cross-document dependency: this plan REQUIRES Plan 10-01 to have shipped, because the new section links into README's `#per-tool-configuration` anchor (D-04a). Plan 10-01 also added a Further-reading bullet in README that points at this section's `#add-a-new-mcp-tool-target` anchor; this plan is what makes that anchor valid.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-v1-1-documentation/10-CONTEXT.md
@.planning/phases/10-v1-1-documentation/10-PATTERNS.md
@.planning/phases/10-v1-1-documentation/10-01-SUMMARY.md
@docs/EXTENDING.md
@README.md
@config.example.yaml
@src/mcp_test_framework/models.py
@src/mcp_test_framework/fixtures.py

<interfaces>
<!-- The schema source the new section's opening prose must cite. -->
<!-- Source: src/mcp_test_framework/models.py L101 -->
class ToolConfig in src/mcp_test_framework/models.py

<!-- The collection-time consumer the closing prose may cite. -->
<!-- Source: src/mcp_test_framework/fixtures.py L405-L418 -->
def tool_config(config: Config, target_tool) -> ToolConfig:
    """Resolve `config.tools.get(target_tool.name, ToolConfig())` per test."""
    return config.tools.get(target_tool.name, ToolConfig())
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert "Add a new MCP tool target" section into docs/EXTENDING.md (DOC-07)</name>
  <files>docs/EXTENDING.md</files>
  <read_first>
    - docs/EXTENDING.md (current state -- 121 lines, sections: Add a new rubric -> Swap the judge backend -> Further reading)
    - README.md (final state from Plan 10-01 -- the new section MUST link into `#per-tool-configuration` and reference Block B by the exact heading `### Block B: judges subset`)
    - .planning/phases/10-v1-1-documentation/10-CONTEXT.md (D-04, D-04a, D-04b, D-04c; D-01, D-01b)
    - .planning/phases/10-v1-1-documentation/10-PATTERNS.md (Add a new MCP tool target pattern assignment, lines 170-254)
    - src/mcp_test_framework/fixtures.py (read the `tool_config` fixture at L405-L418 -- the closing prose names this as the collection-time consumer)
    - src/mcp_test_framework/models.py (confirm `ToolConfig` location -- the opening prose names this as the schema source)
  </read_first>
  <action>
Insert one new section into `docs/EXTENDING.md` immediately AFTER the existing `## Swap the judge backend` section (which ends after the paragraph "...secrets from your environment or a secret manager inside `__init__`.") and BEFORE the existing `## Further reading` section.

The new section MUST contain, in this exact order:

1. The heading: `## Add a new MCP tool target`

2. Opening prose (3-4 lines) -- name the seam, cite the schema source, link forward to README. Use exactly:

```markdown
The per-tool config registry (`tools.<tool_name>:` blocks in your config YAML)
is the lightest-weight way to extend coverage: zero code changes. The schema is
`ToolConfig` (`src/mcp_test_framework/models.py`); see
[Per-tool configuration](../README.md#per-tool-configuration) in the README for
the field reference. This section walks the workflow.
```

3. The "where to drop the recipe" callout on its own line (one bolded inline line, mirroring EXTENDING.md's existing analog at line 24):

```markdown
**Where to drop the recipe:** `config.yaml` (or whichever YAML overlay your `MCPTF_CONFIG_FILE` / `--config` points at). No edits to `tests/conftest.py` or framework source are required.
```

4. A 4-step Markdown ordered list (D-04 explicit list). Each item is 1-2 lines. Use exactly these four steps:

```markdown
1. **Discover.** Run `uv run mcp-test-framework list-tools` to see every tool the connected server advertises.
2. **Decide.** For each tool, decide whether to `skip`, restrict the `judges` subset, or pre-fill `call_arguments`. Tools you say nothing about run with all rubrics and an empty argument map (TOOLCFG-06 safe defaults).
3. **Add a `tools.<tool_name>:` block** under the top-level `tools:` key in your config YAML. See [Per-tool configuration](../README.md#per-tool-configuration) for the field reference; the worked example below uses the skip-with-reason pattern.
4. **Verify.** Re-run `uv run mcp-test-framework run`. The per-tool summary printed at the end of the session shows `<tool_name>: PASS|FAIL|SKIP -- <reason>` so you can confirm the new entry took effect.
```

5. A reader-substitution callout (D-01b) on its own line, immediately before the worked example: `Replace the placeholder tool name below with one from your `mcp-test-framework list-tools` output.`

6. ONE complete worked YAML example using the skip-with-reason pattern (D-04c). Fenced ```yaml```. Exactly:

```yaml
# config.yaml -- per-tool config overlay
tools:
  <your_destructive_tool>:
    skip: true
    skip_reason: "Tool performs writes against real hosts; opted out for CI."
```

7. A one-line cross-reference (D-04c) to README Block B for the judges-subset pattern: `For the judges-subset pattern (`judges: [clarity]`), see [Block B in the README](../README.md#block-b-judges-subset).`

8. Closing-prose pattern (3-5 lines, anchors example to runtime) -- name the resolver fixture from `fixtures.py`. Use exactly:

```markdown
At test-collection time, the `tool_config` fixture
(`src/mcp_test_framework/fixtures.py`) resolves
`config.tools.get(target_tool.name, ToolConfig())` for the active tool. Tools
without an entry receive a default `ToolConfig()` (no skip, no fixed
arguments, all rubrics). Typos in field names are caught at config load by
`extra="forbid"`, so a misspelled `srtip:` does not silently disable the safety
of an explicit `skip: true`.
```

Use the Edit tool. Choose `old_string` as the verbatim final paragraph of the `## Swap the judge backend` section (the paragraph beginning "Pytest's fixture resolution picks up the most specific fixture..." through "...inside `__init__`.") followed by the trailing blank line and the `## Further reading` heading. Choose `new_string` as that same content with the new section inserted between the "Swap the judge backend" closing paragraph and the `## Further reading` heading.

Do NOT modify any existing prose in the rubric section, judge-backend section, or Further reading bullets except via the dedicated Task 2 below.
  </action>
  <verify>
    <automated>python -c "import re; t=open('docs/EXTENDING.md', encoding='utf-8').read(); assert re.search(r'^## Add a new MCP tool target$', t, re.M), 'heading missing'; idx_new=t.index('## Add a new MCP tool target'); idx_judge=t.index('## Swap the judge backend'); idx_fr=t.index('## Further reading'); assert idx_judge < idx_new < idx_fr, 'placement order wrong (must be: Swap judge -> new section -> Further reading)'; print('placement OK')"</automated>
    <automated>python -c "t=open('docs/EXTENDING.md', encoding='utf-8').read(); assert '../README.md#per-tool-configuration' in t, 'README anchor link missing (D-04a)'; assert '../README.md#block-b-judges-subset' in t, 'README Block B anchor link missing (D-04c)'; assert 'src/mcp_test_framework/models.py' in t, 'schema source citation missing'; assert 'src/mcp_test_framework/fixtures.py' in t, 'fixture citation missing'; assert '<your_destructive_tool>' in t, 'placeholder name missing (D-01)'; print('cross-links OK')"</automated>
    <automated>python -c "import re,yaml; t=open('docs/EXTENDING.md', encoding='utf-8').read(); blocks=re.findall(r'```yaml\n(.*?)```', t, re.S); per_tool=[b for b in blocks if 'tools:' in b]; assert len(per_tool)>=1, 'expected at least 1 yaml tools block'; [yaml.safe_load(b) for b in per_tool]; print('yaml-parses OK')"</automated>
    <automated>python -c "t=open('README.md', encoding='utf-8').read(); assert '## Per-tool configuration' in t, 'README per-tool config heading missing -- EXTENDING.md anchor link is broken'; assert '### Block B: judges subset' in t, 'README Block B heading missing -- EXTENDING.md anchor link is broken'; print('cross-document anchors resolve')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Add a new MCP tool target$' docs/EXTENDING.md` returns exactly `1`
    - In docs/EXTENDING.md, the new section appears between `## Swap the judge backend` and `## Further reading` (verified by string-index ordering check above)
    - `grep -c '\.\./README\.md#per-tool-configuration' docs/EXTENDING.md` returns at least `1` (D-04a)
    - `grep -c '\.\./README\.md#block-b-judges-subset' docs/EXTENDING.md` returns at least `1` (D-04c)
    - `grep -c '<your_destructive_tool>' docs/EXTENDING.md` returns at least `1` (D-01 placeholder)
    - `grep -c 'mcp-test-framework list-tools' docs/EXTENDING.md` returns at least `1` (Step 1)
    - `grep -c 'mcp-test-framework run' docs/EXTENDING.md` returns at least `1` (Step 4)
    - `grep -c 'TOOLCFG-06' docs/EXTENDING.md` returns at least `1` (traceability to safe-defaults requirement)
    - `grep -c 'src/mcp_test_framework/models.py' docs/EXTENDING.md` returns at least `1` (schema source citation)
    - `grep -c 'src/mcp_test_framework/fixtures.py' docs/EXTENDING.md` returns at least `1` (closing-prose runtime anchor)
    - All fenced ```yaml blocks in `docs/EXTENDING.md` containing `tools:` parse via `yaml.safe_load`
    - The README cross-references resolve: `## Per-tool configuration` and `### Block B: judges subset` both exist as headings in README.md (cross-document key-link verification)
  </acceptance_criteria>
  <done>
    The new section is present in the correct file location with all required cross-links, the worked YAML parses, and both target anchors in README.md (`#per-tool-configuration`, `#block-b-judges-subset`) exist.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update docs/EXTENDING.md "Further reading" with v1.1 cross-links (CD-03)</name>
  <files>docs/EXTENDING.md</files>
  <read_first>
    - docs/EXTENDING.md (current state with Task 1 applied)
    - .planning/phases/10-v1-1-documentation/10-CONTEXT.md (CD-03)
    - .planning/phases/10-v1-1-documentation/10-PATTERNS.md (Further reading update pattern lines 322-336; cross-link convention lines 257-276)
  </read_first>
  <action>
Append two NEW bullet entries to the existing `## Further reading` section in `docs/EXTENDING.md`. The existing section currently has 3 entries (README link, rubrics.py, judge_protocol.py). Do NOT remove or reorder existing entries.

Add these two entries at the end of the existing list, in this order. Use the EXTENDING.md cross-link convention exactly: backtick-fenced label followed by ` -- ` (two ASCII hyphens, space) and a one-line description.

```markdown
- [`Per-tool configuration`](../README.md#per-tool-configuration) -- README schema reference for the `tools.<name>:` registry
- [`config.example.yaml`](../config.example.yaml) -- complete real-server per-tool config reference
```

Note the link text for the README anchor uses the human-readable phrase `Per-tool configuration` (NOT a backtick-fenced path) because the link target is an in-doc anchor, not a file path. The `config.example.yaml` link uses the standard backtick-fenced-path form because the target IS a file.

Use the Edit tool. Choose `old_string` as the verbatim final bullet of the existing list (the `judge_protocol.py` bullet, currently `- src/mcp_test_framework/judge_protocol.py -- Protocol definition + JudgeResult shape` with surrounding backticks per the existing file format) and `new_string` as that same line followed by the two new bullets above.

Do NOT modify any other text in the file.
  </action>
  <verify>
    <automated>python -c "t=open('docs/EXTENDING.md', encoding='utf-8').read(); assert '[`Per-tool configuration`](../README.md#per-tool-configuration)' in t, 'Per-tool config back-link missing'; assert '[`config.example.yaml`](../config.example.yaml)' in t, 'config.example.yaml link missing'; print('further-reading OK')"</automated>
    <automated>python -c "t=open('docs/EXTENDING.md', encoding='utf-8').read(); count=t.count('## Further reading'); assert count==1, f'expected exactly 1 Further reading section, found {count}'; print('section-uniqueness OK')"</automated>
    <automated>python -c "t=open('docs/EXTENDING.md', encoding='utf-8').read(); assert 'src/mcp_test_framework/rubrics.py' in t, 'rubrics.py original entry removed'; assert 'src/mcp_test_framework/judge_protocol.py' in t, 'judge_protocol.py original entry removed'; print('original entries preserved')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Further reading$' docs/EXTENDING.md` returns exactly `1`
    - `grep -c '\[`Per-tool configuration`\](\.\./README\.md#per-tool-configuration)' docs/EXTENDING.md` returns at least `1`
    - `grep -c '\[`config.example.yaml`\](\.\./config\.example\.yaml)' docs/EXTENDING.md` returns at least `1`
    - The original three Further-reading entries (README.md link, rubrics.py, judge_protocol.py) remain present
    - The new two bullets appear AFTER the original three (string-index ordering)
  </acceptance_criteria>
  <done>
    Further reading has exactly five entries (3 original + 2 new), the back-link to README's per-tool-configuration anchor and the config.example.yaml link are both present, and original entries are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify cross-document anchor integrity (CD-06)</name>
  <files>tests/test_readme_snippets.py</files>
  <read_first>
    - tests/test_readme_snippets.py (created in Plan 10-01 -- contains an xfail-guarded `test_readme_anchor_targets_exist` waiting for this plan)
    - docs/EXTENDING.md (final state after Tasks 1 and 2)
    - README.md (current state -- target of EXTENDING.md cross-links)
  </read_first>
  <action>
This task does NOT modify `tests/test_readme_snippets.py` -- it verifies that the test file from Plan 10-01 now passes (the `xfail`-guarded `test_readme_anchor_targets_exist` should xpass once the EXTENDING.md heading exists).

If `test_readme_anchor_targets_exist` reports `xpassed` (strict-xfail-fail) under `uv run pytest`, edit `tests/test_readme_snippets.py` to remove the `pytest.xfail(...)` runtime call from `test_readme_anchor_targets_exist`. The remaining body should perform the assertion unconditionally: locate every `[..](docs/EXTENDING.md#<anchor>)` link in `README.md`, and for each anchor verify the corresponding heading exists in `docs/EXTENDING.md` (heading text matches `re.sub(r'[^\w\s-]', '', <anchor>).replace('-', ' ')` lower-cased -- standard GitHub anchor rule). At minimum, the anchor `add-a-new-mcp-tool-target` must resolve to the `## Add a new MCP tool target` heading.

If `test_readme_anchor_targets_exist` already passes without modification (Python xfail semantics: a body containing `pytest.xfail(...)` always raises and produces xfail not xpass), then run the suite to confirm and leave the file alone. Update the file ONLY if needed to flip the test from xfail to a real pass.

If a separate `tests/test_extending_snippets.py` would help future-proof the EXTENDING.md side of the invariant, do NOT add it -- Plan 10-01's `test_readme_yaml_snippets_parse` already runs against README only; EXTENDING.md YAML correctness is sufficiently covered by the Task 1 verify command and `test_readme_anchor_targets_exist`.

Final state expected: `uv run pytest tests/test_readme_snippets.py -v` reports 3 passed (no xfail).
  </action>
  <verify>
    <automated>uv run pytest tests/test_readme_snippets.py -v</automated>
    <automated>python -c "import re; t=open('docs/EXTENDING.md', encoding='utf-8').read(); r=open('README.md', encoding='utf-8').read(); links=re.findall(r'\]\(docs/EXTENDING\.md#([^)]+)\)', r); assert links, 'no docs/EXTENDING.md anchor links in README'; headings={h.lower().replace(' ','-') for h in re.findall(r'^##+ (.+)$', t, re.M)}; missing=[a for a in links if a not in headings]; assert not missing, f'broken anchors in README->EXTENDING: {missing}'; print('cross-doc anchors resolve')"</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_readme_snippets.py -v` exits 0 with 3 passed (no xfail, no xpass-strict failure)
    - The cross-document anchor verification (README.md -> docs/EXTENDING.md#add-a-new-mcp-tool-target) succeeds: the heading `## Add a new MCP tool target` exists in `docs/EXTENDING.md`
    - No new test files are created
  </acceptance_criteria>
  <done>
    The test from Plan 10-01 now reports a real pass (not xfail) for the cross-document anchor invariant; the EXTENDING.md side of the v1.1 documentation is fully wired into the regression suite.
  </done>
</task>

</tasks>

<verification>
1. All three tasks' automated `<verify>` commands pass.
2. `uv run pytest tests/test_readme_snippets.py -v` reports 3 passed.
3. `uv run mcp-test-framework run` (full default suite) still exits 0 -- no regressions.
4. Manual scan: docs/EXTENDING.md renders cleanly on GitHub; clicking the README->EXTENDING.md "Add a new MCP tool target" link in Further reading lands on the new section; clicking the EXTENDING.md->README "Per-tool configuration" back-link lands on Plan 10-01's section.
</verification>

<success_criteria>
- docs/EXTENDING.md has exactly four `## ` top-level sections (in order): Add a new description-quality rubric -> Swap the judge backend -> Add a new MCP tool target -> Further reading
- The walkthrough section uses placeholder names per D-01, references README anchors per D-04a/D-04c, includes the 4-step list per D-04, and uses the skip-with-reason worked example per D-04c
- Further reading lists 5 entries (3 original + back-link to README per-tool config + config.example.yaml)
- All YAML in the new section parses via `yaml.safe_load`
- All cross-document anchors resolve (no 404s on GitHub render)
- `tests/test_readme_snippets.py` reports 3 passed (xfail flipped to pass)
- No code changes outside the test-fix-up in Task 3 (which may not require any change)
</success_criteria>

<output>
After completion, create `.planning/phases/10-v1-1-documentation/10-02-SUMMARY.md`
</output>
