---
phase: 10-v1-1-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [DOC-04, DOC-05, DOC-06]
must_haves:
  truths:
    - "README has a `## Per-tool configuration` section with a TOOLCFG field reference table and two worked YAML blocks (D-02, D-04)"
    - "Per-tool examples use abstract placeholder names like `<safe_read_tool_a>` / `<safe_read_tool_b>` (D-01) preceded by a one-line reader-substitution callout (D-01b)"
    - "Per-tool section closes with a one-line cross-link to `config.example.yaml` (D-01a)"
    - "`call_arguments` gets a one-paragraph mention only, not a worked block (D-02a)"
    - "Reserved `setup`/`depends_on` fields get a single-sentence forward-compat callout (D-02b)"
    - "Each worked block shows the COMPLETE `tools.<tool_name>:` entry, copy-pasteable (D-02c)"
    - "README has a `## Isolation guarantee` section opening with the strong, narrowly-scoped claim 'Test runs do not mutate `~/.homelab_mcp/` real-state files' (D-05b)"
    - "Isolation section names the three concrete state files `credential_registry.json`, `known_hosts`, `migration_state.json` (D-05a; documented exception to D-01)"
    - "Isolation section provides the bash recipe `uv run pytest tests/test_isolation.py -v` as the verification (D-05)"
    - "Isolation section includes the keyring null-backend one-line note (D-05b)"
    - "README has a `## CI integration` section with a single GitHub Actions YAML snippet (~25 lines) using `astral-sh/setup-uv@v6`, `actions/checkout@v5`, `dorny/test-reporter@v2` pinned with major-version tags (D-03, D-03d)"
    - "CI snippet runs `uv run mcp-test-framework run --junit-xml=results.xml` and uploads via `dorny/test-reporter@v2` (D-03)"
    - "CI snippet has a leading comment translating to other CI systems (D-03c) and an inline comment noting default `addopts` excludes live markers (D-03b)"
    - "README's `## Further reading` section gains links to EXTENDING.md's new `#add-a-new-mcp-tool-target` anchor and to `config.example.yaml` (CD-03)"
    - "Every fenced YAML block in the new content parses via `yaml.safe_load` (CD-06)"
    - "Every TOOLCFG field documented in the README's reference table exists as an attribute on the `ToolConfig` Pydantic model in `src/mcp_test_framework/models.py` (CD-06 drift check)"
  artifacts:
    - path: "README.md"
      provides: "Three new top-level sections (Per-tool configuration, Isolation guarantee, CI integration) plus updated Further reading"
      contains: "## Per-tool configuration"
    - path: "README.md"
      provides: "Isolation guarantee claim + verification recipe"
      contains: "## Isolation guarantee"
    - path: "README.md"
      provides: "CI integration GHA snippet"
      contains: "## CI integration"
  key_links:
    - from: "README.md `## Per-tool configuration` heading"
      to: "the GitHub-rendered anchor `#per-tool-configuration` (consumed by EXTENDING.md in Plan 10-02)"
      via: "GitHub auto-anchor (lowercase + spaces->hyphens)"
      pattern: "^## Per-tool configuration$"
    - from: "README.md per-tool config section"
      to: "config.example.yaml"
      via: "Markdown link in closing prose"
      pattern: "config\\.example\\.yaml"
    - from: "README.md isolation section"
      to: "tests/test_isolation.py"
      via: "backtick-fenced inline path in bash recipe"
      pattern: "tests/test_isolation\\.py"
    - from: "README.md CI section"
      to: "dorny/test-reporter@v2"
      via: "GHA snippet step"
      pattern: "dorny/test-reporter@v2"
---

<objective>
Insert three new top-level sections into `README.md` covering v1.1's user-facing surface (per-tool config schema, isolation guarantee, CI integration), then update `## Further reading` with the cross-links Plan 10-02 will rely on.

Purpose: A new contributor or CI engineer can adopt v1.1's per-tool config, isolation, and JUnit XML output by reading only `README.md` -- no source-reading required (DOC-04, DOC-05, DOC-06).
Output: README.md gains three new sections (~80-100 new lines total) plus 1-2 new Further-reading links. No other files modified. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-v1-1-documentation/10-CONTEXT.md
@.planning/phases/10-v1-1-documentation/10-PATTERNS.md
@README.md
@config.example.yaml
@src/mcp_test_framework/models.py

<interfaces>
<!-- Reference shape for the TOOLCFG fields. The README's field-reference table -->
<!-- MUST list exactly these fields and types. Source: src/mcp_test_framework/models.py L101-L159. -->

```python
class ToolConfig(BaseModel):
    model_config = ConfigDict(frozen=True, populate_by_name=True, extra="forbid")

    skip: bool = False
    skip_reason: Optional[str] = None
    call_arguments: dict[str, Any] = Field(default_factory=dict)
    judges: Optional[list[str]] = None
    setup: Optional[Any] = None         # reserved (TOOLCFG-03); runtime no-op in v1.1
    depends_on: Optional[list[str]] = None  # reserved (TOOLCFG-03); runtime no-op in v1.1
```

Validator behavior the docs must reflect:
- `skip=True` requires a non-empty `skip_reason` (model_validator raises at load)
- `judges` entries must each resolve against the rubric registry (clarity, disambiguation, parameters)
- `extra="forbid"` -- typos in field names (e.g., `clarty`) fail at load, not silently
- `judges: None` (default) means "run all rubrics"; `judges: []` means "run no rubrics"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert "Per-tool configuration" section into README (DOC-04)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state; new section MUST snap into existing structure without disturbing other sections)
    - .planning/phases/10-v1-1-documentation/10-CONTEXT.md (D-01, D-01a, D-01b, D-02, D-02a, D-02b, D-02c)
    - .planning/phases/10-v1-1-documentation/10-PATTERNS.md (Per-tool configuration pattern assignment, lines 25-77)
    - src/mcp_test_framework/models.py (ToolConfig field shape -- the table values must match this verbatim)
    - config.example.yaml (cross-link target; the table's example values should be consistent in shape)
  </read_first>
  <action>
Insert a new `## Per-tool configuration` section into README.md immediately AFTER the existing `## Configuration` section (which ends at the line beginning with "Copy `.env.example` to `.env` and edit." and the two prose lines that follow it) and BEFORE `## Sample green run`. Use the Edit tool to perform an insertion -- do NOT rewrite untouched sections.

The new section MUST contain, in this exact order:

1. The heading line exactly: `## Per-tool configuration`

2. A single orienting sentence (one line): `Per-tool config lives under the top-level `tools:` key in your YAML overlay; tools with no entry use safe defaults (no skip, all rubrics, empty `call_arguments`).`

3. A field reference table with this exact shape (3 columns, 6 data rows):

```markdown
| Field | Default | Purpose |
|-------|---------|---------|
| `skip` | `false` | If `true`, the tool's tests are skipped via `pytest.skip(reason=skip_reason)`. Requires a non-empty `skip_reason`. |
| `skip_reason` | `null` | Human-readable reason surfaced in pytest output and JUnit XML when `skip: true`. |
| `call_arguments` | `{}` | Fixed `dict[str, Any]` passed to the tool's `call_tool` invocation. |
| `judges` | `null` (all rubrics) | Optional `list[str]` of rubric IDs (`clarity`, `disambiguation`, `parameters`). `[]` means "no rubrics for this tool". |
| `setup` | `null` | Reserved for v1.5+ stateful testing (TOOLCFG-03); runtime no-op in v1.1. |
| `depends_on` | `null` | Reserved for v1.5+ stateful testing (TOOLCFG-03); runtime no-op in v1.1. |
```

4. A single-sentence forward-compat callout (D-02b) immediately after the table: `The `setup` and `depends_on` fields are typed in the model but have no runtime semantics in v1.1; future versions will activate them additively.`

5. The reader-substitution callout (D-01b) on its own line: `Replace the placeholder tool names below with the names from your `mcp-test-framework list-tools` output.`

6. A `### Block A: skip-with-reason` subsection with this complete worked YAML block (D-02 / D-02c). The fenced language tag is exactly ```yaml```:

```yaml
# config.yaml -- per-tool config overlay
tools:
  <safe_read_tool_a>:
    skip: true
    skip_reason: "Tool performs writes; we only exercise read-only tools in CI."
```

7. A `### Block B: judges subset` subsection with this complete worked YAML block (D-02 / D-02c):

```yaml
# config.yaml -- per-tool config overlay
tools:
  <safe_read_tool_b>:
    judges: [clarity]
```

8. A one-paragraph `call_arguments` mention (D-02a) directly after Block B (no `###` heading): `The third per-tool knob, `call_arguments: {key: value}`, lets you pass fixed arguments to the tool's `call_tool` invocation -- useful when the tool requires non-empty input. See `config.example.yaml` for shape.`

9. A closing one-line cross-link (D-01a): `For a complete real-server config, see [`config.example.yaml`](config.example.yaml).`

Use the Edit tool with `old_string` set to the verbatim 3-line closing prose block of the Configuration section ("Copy `.env.example` to `.env` and edit. ..." through "...PowerShell with `$env:VAR = \"...\"` before invoking the CLI.") plus the trailing blank line, and `new_string` set to that same content followed by the new section. Do not change line endings (CRLF or LF) of unmodified text.

After insertion, do NOT modify the existing `## Configuration` section content.
  </action>
  <verify>
    <automated>python -c "import re; t=open('README.md', encoding='utf-8').read(); assert re.search(r'^## Per-tool configuration$', t, re.M), 'heading missing'; assert '<safe_read_tool_a>' in t and '<safe_read_tool_b>' in t, 'placeholders missing'; assert 'config.example.yaml' in t, 'cross-link missing'; assert 'skip_reason' in t and '`call_arguments`' in t and '`judges`' in t, 'fields missing'; print('OK')"</automated>
    <automated>python -c "import re,yaml; t=open('README.md', encoding='utf-8').read(); blocks=re.findall(r'```yaml\n(.*?)```', t, re.S); per_tool=[b for b in blocks if 'tools:' in b]; assert len(per_tool)>=2, f'expected >=2 per-tool yaml blocks, got {len(per_tool)}'; [yaml.safe_load(b) for b in per_tool]; print('yaml-parses OK')"</automated>
    <automated>python -c "from mcp_test_framework.models import ToolConfig; fields=set(ToolConfig.model_fields.keys()); doc='skip skip_reason call_arguments judges setup depends_on'.split(); missing=[f for f in doc if f not in fields]; assert not missing, f'README mentions fields not in model: {missing}'; print('schema-drift OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Per-tool configuration$' README.md` returns `1`
    - `grep -c '^### Block A: skip-with-reason$' README.md` returns `1`
    - `grep -c '^### Block B: judges subset$' README.md` returns `1`
    - `grep -c '<safe_read_tool_a>' README.md` returns at least `1`
    - `grep -c '<safe_read_tool_b>' README.md` returns at least `1`
    - `grep -c 'mcp-test-framework list-tools' README.md` returns at least `1` (reader-substitution callout uses the literal CLI string)
    - `grep -c 'config.example.yaml' README.md` returns at least `1` (closing cross-link)
    - All fenced ```yaml blocks containing `tools:` parse via `yaml.safe_load` without error
    - The 6 documented field names (`skip`, `skip_reason`, `call_arguments`, `judges`, `setup`, `depends_on`) all exist as attributes on `mcp_test_framework.models.ToolConfig`
    - The literal phrase "TOOLCFG-03" appears in the reserved-fields callout (D-02b traceability)
  </acceptance_criteria>
  <done>
    The Per-tool configuration section is present, both worked YAML blocks parse, all documented fields exist on the model, and no existing README sections were modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Insert "Isolation guarantee" and "CI integration" sections into README (DOC-05, DOC-06)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state including the section inserted in Task 1)
    - .planning/phases/10-v1-1-documentation/10-CONTEXT.md (D-03, D-03a, D-03b, D-03c, D-03d, D-05, D-05a, D-05b)
    - .planning/phases/10-v1-1-documentation/10-PATTERNS.md (Isolation pattern assignment lines 80-121; CI pattern assignment lines 124-167)
    - tests/test_isolation.py (verify the file exists and the bash recipe targets it)
    - pyproject.toml (verify `addopts` markers `live_homelab` / `live_ollama` so the CI snippet's reminder comment cites real markers)
  </read_first>
  <action>
Insert TWO new top-level sections into README.md:

**Section 1: `## Isolation guarantee`** -- inserted AFTER the existing `## Sample green run` section (which ends after the paragraph "...the verdict per your project's tolerance.") and BEFORE `## Troubleshooting (Windows)`.

Content (use exactly these literal lines/wording):

1. Heading: `## Isolation guarantee`
2. Strong-claim opening (D-05b, one paragraph, no hedging): `Test runs do not mutate `~/.homelab_mcp/` real-state files. The framework spawns the MCP subprocess with `HOME` and `USERPROFILE` overridden to a per-session temporary directory, so the server reads/writes its state inside the tempdir and never touches your real-state files.`
3. Verification recipe (D-05) -- a fenced ```bash``` block on its own:

```bash
uv run pytest tests/test_isolation.py -v
```

4. Caveat / scope-narrowing prose (D-05a -- list the three CONCRETE files; D-05b -- keyring note). One paragraph, exactly:
`The test in `tests/test_isolation.py` computes sha256 hashes of `~/.homelab_mcp/credential_registry.json`, `~/.homelab_mcp/known_hosts`, and `~/.homelab_mcp/migration_state.json` before and after a full session and asserts byte-identical equality. Test runs also route the OS keyring through a null backend, so credentials are not read or written.`

**Section 2: `## CI integration`** -- inserted AFTER the new `## Isolation guarantee` section and BEFORE `## Troubleshooting (Windows)`.

Content:

1. Heading: `## CI integration`
2. Orienting sentence: `Run the suite in CI with `--junit-xml=` and ingest the result with a JUnit-aware action. The snippet below runs the default unit slice (live markers excluded by `pyproject.toml`'s `addopts`).`
3. The fenced ```yaml``` workflow snippet -- target ~25 lines, use exactly this content (do not change action versions or step ordering):

```yaml
# .github/workflows/test.yml -- GitHub Actions starter.
# On Jenkins / GitLab CI / CircleCI, translate the `runs-on` / `uses` /
# `with` keys to the equivalent runner + action concepts. The CLI invocation
# (`uv run mcp-test-framework run --junit-xml=results.xml`) is portable.
name: tests
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v6
        with:
          python-version: "3.14"
      - run: uv sync
      # Default addopts in pyproject.toml excludes live_homelab + live_ollama markers.
      - run: uv run mcp-test-framework run --junit-xml=results.xml
      - name: Publish test report
        if: always()
        uses: dorny/test-reporter@v2
        with:
          name: pytest
          path: results.xml
          reporter: java-junit
```

4. Post-snippet prose (≤ 6 lines, D-03b reminder + D-03c translate-hint reinforcement): `The snippet pins actions with major-version tags (`@v5`, `@v6`, `@v2`); operators who need SHA-pinning are graduating beyond this starter. The default `addopts` in `pyproject.toml` excludes the `live_homelab` and `live_ollama` markers -- to exercise live tests in CI, add a separate job that opts in to those markers explicitly.`

Use the Edit tool to perform a single insertion. Choose `old_string` as the existing line `## Troubleshooting (Windows)` (with one preceding blank line) and `new_string` as the two new sections followed by the same `## Troubleshooting (Windows)` heading. Preserve all other content verbatim.

Do NOT modify any existing section content. Do NOT remove or relocate the existing `## Sample green run` or `## Troubleshooting (Windows)` content.
  </action>
  <verify>
    <automated>python -c "import re; t=open('README.md', encoding='utf-8').read(); assert re.search(r'^## Isolation guarantee$', t, re.M), 'isolation heading missing'; assert re.search(r'^## CI integration$', t, re.M), 'CI heading missing'; assert 'tests/test_isolation.py' in t, 'isolation test path missing'; assert 'credential_registry.json' in t and 'known_hosts' in t and 'migration_state.json' in t, 'real-state file refs missing (D-05a)'; assert 'null backend' in t, 'keyring callout missing (D-05b)'; print('isolation OK')"</automated>
    <automated>python -c "import re; t=open('README.md', encoding='utf-8').read(); assert 'astral-sh/setup-uv@v6' in t and 'actions/checkout@v5' in t and 'dorny/test-reporter@v2' in t, 'GHA action pins missing'; assert '--junit-xml=results.xml' in t, 'junit-xml flag missing'; assert 'live_homelab' in t and 'live_ollama' in t, 'live-marker reminder missing (D-03b)'; print('CI OK')"</automated>
    <automated>python -c "import re,yaml; t=open('README.md', encoding='utf-8').read(); blocks=re.findall(r'```yaml\n(.*?)```', t, re.S); gha=[b for b in blocks if 'astral-sh/setup-uv' in b]; assert len(gha)==1, f'expected exactly 1 GHA block, got {len(gha)}'; yaml.safe_load(gha[0]); print('GHA yaml-parses OK')"</automated>
    <automated>python -c "import os; assert os.path.exists('tests/test_isolation.py'), 'README references tests/test_isolation.py but file is absent'; print('verification target exists')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Isolation guarantee$' README.md` returns `1`
    - `grep -c '^## CI integration$' README.md` returns `1`
    - `grep -c 'Test runs do not mutate' README.md` returns at least `1` (D-05b strong claim)
    - `grep -c 'credential_registry.json' README.md` returns at least `1` (D-05a)
    - `grep -c 'known_hosts' README.md` returns at least `1` (D-05a)
    - `grep -c 'migration_state.json' README.md` returns at least `1` (D-05a)
    - `grep -c 'uv run pytest tests/test_isolation.py' README.md` returns at least `1` (D-05 recipe)
    - `grep -c 'null backend' README.md` returns at least `1` (D-05b keyring note)
    - `grep -c 'astral-sh/setup-uv@v6' README.md` returns exactly `1`
    - `grep -c 'dorny/test-reporter@v2' README.md` returns exactly `1`
    - `grep -c 'actions/checkout@v5' README.md` returns exactly `1`
    - `grep -c -- '--junit-xml=results.xml' README.md` returns at least `1`
    - `grep -c 'live_homelab' README.md` returns at least `1` (D-03b reminder)
    - The single GHA YAML block parses via `yaml.safe_load` without error
    - `tests/test_isolation.py` file exists on disk (verifies the README's claim is reachable)
  </acceptance_criteria>
  <done>
    Both new sections are inserted in the correct order between Sample green run and Troubleshooting (Windows). Strong claim, file paths, recipe, keyring note, and GHA snippet content all match D-03/D-03b/D-03c/D-03d/D-05/D-05a/D-05b verbatim, and existing README sections are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update README "Further reading" section with v1.1 cross-links (CD-03)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current state with Tasks 1 and 2 applied)
    - .planning/phases/10-v1-1-documentation/10-CONTEXT.md (CD-03)
    - .planning/phases/10-v1-1-documentation/10-PATTERNS.md (Cross-link convention lines 257-276; Further reading update pattern lines 322-336)
  </read_first>
  <action>
Append two NEW bullet entries to the existing `## Further reading` section in README.md (currently 3 entries: spec, EXTENDING.md, PROJECT.md). Do NOT remove or reorder the existing entries.

Add these two entries at the end of the existing list, in this order:

```markdown
- [`docs/EXTENDING.md#add-a-new-mcp-tool-target`](docs/EXTENDING.md#add-a-new-mcp-tool-target) -- add a new MCP tool target via per-tool config (no code changes)
- [`config.example.yaml`](config.example.yaml) -- complete real-server per-tool config reference
```

Use the Edit tool. Choose `old_string` as the verbatim final line of the existing list (the PROJECT.md bullet: `` - [`.planning/PROJECT.md`](.planning/PROJECT.md) -- project mission, constraints, key decisions ``) and `new_string` as that same line followed by the two new bullets above (each on its own line, two-ASCII-hyphen ` -- ` separator matching existing convention).

Do NOT modify any other text in this section or in the file.
  </action>
  <verify>
    <automated>python -c "t=open('README.md', encoding='utf-8').read(); assert '[`docs/EXTENDING.md#add-a-new-mcp-tool-target`](docs/EXTENDING.md#add-a-new-mcp-tool-target)' in t, 'EXTENDING anchor link missing'; assert '[`config.example.yaml`](config.example.yaml)' in t, 'config.example.yaml link missing'; print('further-reading OK')"</automated>
    <automated>python -c "t=open('README.md', encoding='utf-8').read(); count=t.count('## Further reading'); assert count==1, f'expected exactly 1 Further reading section, found {count}'; print('section-uniqueness OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Further reading$' README.md` returns exactly `1` (no duplication)
    - `grep -c 'docs/EXTENDING.md#add-a-new-mcp-tool-target' README.md` returns at least `1`
    - `grep -c '\[`config.example.yaml`\](config.example.yaml)' README.md` returns at least `1`
    - The original three Further-reading entries (spec, EXTENDING.md without anchor, PROJECT.md) remain present and in their original order (spot-check via `grep` for each)
  </acceptance_criteria>
  <done>
    Further reading has exactly five entries (3 original + 2 new), the EXTENDING.md anchor link Plan 10-02 will provide is present, and the original entries are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 4: Plan-level snippet-correctness sanity verification (CD-06)</name>
  <files>tests/test_readme_snippets.py</files>
  <read_first>
    - README.md (final state after Tasks 1-3)
    - .planning/phases/10-v1-1-documentation/10-CONTEXT.md (CD-06)
    - src/mcp_test_framework/models.py (ToolConfig schema for the drift check)
    - pyproject.toml (verify `addopts` and the `tests/` rootdir convention -- the new file lives under tests/)
  </read_first>
  <action>
Create a new test file `tests/test_readme_snippets.py` that bakes CD-06 verification into the suite. The file MUST contain three test functions (no fixtures, no async, no live markers):

1. `test_readme_yaml_snippets_parse` -- extracts every fenced ```yaml block from `README.md` (use `re.findall(r"```yaml\n(.*?)```", text, re.S)`) and asserts each parses via `yaml.safe_load`.

2. `test_readme_per_tool_fields_match_model` -- reads README.md, locates the table row containing each of `skip`, `skip_reason`, `call_arguments`, `judges`, `setup`, `depends_on` (literal substring search inside the Per-tool configuration table is sufficient), and for each documented field asserts it is present in `ToolConfig.model_fields` (imported from `mcp_test_framework.models`).

3. `test_readme_anchor_targets_exist` -- a forward-compat sanity check for Plan 10-02: locate every Markdown link in README.md of the form `[..](docs/EXTENDING.md#<anchor>)`, and for each anchor verify the corresponding heading exists in `docs/EXTENDING.md`. Until Plan 10-02 lands, the only such anchor is `add-a-new-mcp-tool-target`. The check uses `pytest.importorskip`-style guard: if `docs/EXTENDING.md` does not yet contain `## Add a new MCP tool target`, mark the test `pytest.xfail("EXTENDING.md anchor pending Plan 10-02")` (use `pytest.xfail`, not `xfail` decorator -- a runtime xfail call inside the test body). Once 10-02 ships, the xfail flips to xpass and the assertion passes.

Use ONLY `pytest`, `re`, `yaml`, and stdlib imports plus `from mcp_test_framework.models import ToolConfig`. No async. No `live_*` markers (must run by default).

Open files with `pathlib.Path("README.md").read_text(encoding="utf-8")` and `pathlib.Path("docs/EXTENDING.md").read_text(encoding="utf-8")`. Do NOT hardcode absolute paths.
  </action>
  <verify>
    <automated>uv run pytest tests/test_readme_snippets.py -v</automated>
    <automated>python -c "import pathlib; t=pathlib.Path('tests/test_readme_snippets.py').read_text(encoding='utf-8'); assert 'def test_readme_yaml_snippets_parse' in t; assert 'def test_readme_per_tool_fields_match_model' in t; assert 'def test_readme_anchor_targets_exist' in t; print('all 3 tests defined')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_readme_snippets.py` exists
    - The file defines exactly three test functions named `test_readme_yaml_snippets_parse`, `test_readme_per_tool_fields_match_model`, `test_readme_anchor_targets_exist`
    - `uv run pytest tests/test_readme_snippets.py -v` exits 0 with 2 passed + 1 xfailed (or 3 passed if Plan 10-02 has already shipped)
    - The file imports `ToolConfig` from `mcp_test_framework.models`
    - The file does NOT use `pytest.mark.live_homelab` or `pytest.mark.live_ollama`
    - The file does NOT use absolute paths (no `C:\` or `/home/`)
  </acceptance_criteria>
  <done>
    A regression test guards README YAML correctness, schema-drift between README and `ToolConfig`, and the EXTENDING.md anchor target Plan 10-02 will provide. All three tests run by default in `uv run mcp-test-framework run`.
  </done>
</task>

</tasks>

<verification>
1. All four tasks' automated `<verify>` commands pass.
2. `uv run pytest tests/test_readme_snippets.py -v` reports 2 passed + 1 xfailed (or 3 passed once 10-02 ships).
3. `uv run mcp-test-framework run` (full default suite) still exits 0 -- no regressions in the other tests.
4. Manual scan: README renders cleanly on GitHub (no broken Markdown tables, no orphaned headings).
</verification>

<success_criteria>
- README.md contains the three new top-level sections in the order: Per-tool configuration -> Sample green run -> Isolation guarantee -> CI integration -> Troubleshooting (Windows)
- All YAML in new sections parses via `yaml.safe_load`
- All documented TOOLCFG fields exist on `ToolConfig`
- Further reading lists 5 entries with the new EXTENDING.md anchor link and config.example.yaml link
- A regression test (`tests/test_readme_snippets.py`) bakes CD-06 verification into the suite
- No code changes outside `tests/test_readme_snippets.py`
- No existing README sections modified beyond the targeted insertions
</success_criteria>

<output>
After completion, create `.planning/phases/10-v1-1-documentation/10-01-SUMMARY.md`
</output>
