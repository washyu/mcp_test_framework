---
phase: 999.2-codegen-driven-parameter-test-generation-for-required-field-
plan: "02"
subsystem: testing
tags: [codegen, emitter, smoke, pure-data, tdd]

requires:
  - phase: 999.2-codegen-driven-parameter-test-generation-for-required-field-
    plan: "01"
    provides: ToolConfig.examples field + _validate_examples_shape validator (consumed by plan 03 wiring)

provides:
  - _emit_smoke_scenario() pure-data function in src/mcp_test_framework/test_code/_codegen.py
  - Three branches: single-example, multi-example (parametrize), TODO/skip (D-02)
  - 10 framework self-tests pinning GEN-03, GEN-04, GEN-05 behaviors

affects:
  - 999.2-03 (CLI wiring — plan 03 calls _emit_smoke_scenario() from inside generate())

tech-stack:
  added: []
  patterns:
    - "Pure-data emitter pattern: _emit_smoke_scenario() returns str, no I/O (mirrors translate_tool)"
    - "json.dumps() escaping for operator-supplied values into generated Python source (V5/Tampering)"
    - "Single-line @pytest.mark.parametrize with positional example-{i} IDs (Pitfall 4 stability)"

key-files:
  created:
    - tests/framework/unit/test_codegen_smoke_emitter.py
  modified:
    - src/mcp_test_framework/test_code/_codegen.py

key-decisions:
  - "Emitted @pytest.mark.parametrize on a single line (not multi-line split) to match the plan's verbatim assertion and PATTERNS.md sketch"
  - "generate() signature and body UNCHANGED — plan 03 wires _emit_smoke_scenario() from inside generate()"
  - "TODO branch: relative import appears BEFORE pytest.skip() (research Q6 recommendation — ImportError surfaces codegen bug not skip)"

patterns-established:
  - "Pattern: _emit_smoke_scenario() pure-data function appended after generate() — I/O stays in generate(); pure-data functions testable without tmp_path"

requirements-completed: [GEN-03, GEN-04, GEN-05]

duration: ~3min
completed: 2026-05-27
---

# Phase 999.2 Plan 02: _emit_smoke_scenario() Emitter + 10 Framework Tests Summary

**Pure-data `_emit_smoke_scenario()` appended to `_codegen.py` with three branches (single-example, multi-example/parametrize, TODO/skip); 10 framework self-tests pin all GEN-03/GEN-04/GEN-05 behaviors via TDD RED/GREEN cycle**

## Performance

- **Duration:** ~3 min
- **Started:** 2026-05-27T01:19:46Z
- **Completed:** 2026-05-27
- **Tasks:** 2 (TDD: Task 1=RED, Task 2=GREEN)
- **Files modified:** 2

## Accomplishments

- Created `tests/framework/unit/test_codegen_smoke_emitter.py` with 10 tests covering all three emission branches (RED phase)
- Implemented `_emit_smoke_scenario()` in `_codegen.py` — pure-data function, 3 branches (GREEN phase)
- All 10 smoke emitter tests pass; all 12 existing `test_codegen_emitter.py` tests still pass (no regression)
- `generate()` signature and body left UNCHANGED (plan 03 wires the new function)
- Every invariant enforced: 4-line header via `_render_header()` reuse, relative import, `loop_scope="session"`, positional `example-{i}` IDs, `json.dumps()` escaping, exact D-02a skip reason

## Task Commits

1. **Task 1: Add 10 failing tests for _emit_smoke_scenario (RED)** - `65ddb9a` (test)
2. **Task 2: Implement _emit_smoke_scenario() three branches (GREEN)** - `355206c` (feat)

## Files Created/Modified

- `tests/framework/unit/test_codegen_smoke_emitter.py` — new file, 10 unit tests (GEN-03/04/05 coverage)
- `src/mcp_test_framework/test_code/_codegen.py` — appended `_emit_smoke_scenario()` function (~105 lines) after `generate()`

## Decisions Made

- **Single-line @pytest.mark.parametrize:** The plan's verbatim test assertion and PATTERNS.md sketch both show the decorator on a single line. The initial implementation used a multi-line split format causing test 6 to fail (Rule 1 auto-fix). Switched to single-line to satisfy the spec.
- **generate() untouched:** Per plan, `generate()` is not modified here. Plan 03 adds the `tools_config` kwarg and per-tool smoke write.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Single-line vs multi-line @pytest.mark.parametrize in emitter**
- **Found during:** Task 2 (test 6 `test_emit_smoke_multi_example_uses_parametrize` failed immediately)
- **Issue:** Initial implementation emitted `@pytest.mark.parametrize(...)` across 4 lines using the `\n    "example",\n    _EXAMPLES,\n    ids=[...]` split form. The plan's test assertion and PATTERNS.md sketch both show a single-line form.
- **Fix:** Changed the multi-example branch to emit the full decorator on a single line.
- **Files modified:** `src/mcp_test_framework/test_code/_codegen.py`
- **Verification:** All 10 tests pass after fix

---

**Total deviations:** 1 auto-fixed (Rule 1 - format mismatch between initial implementation and plan spec)
**Impact on plan:** Zero scope creep. Single-line form is consistent with PATTERNS.md; multi-line was a misread.

## TDD Gate Compliance

- RED gate: `test(999.2-02)` commit `65ddb9a` — 10 tests failing with ImportError (function absent)
- GREEN gate: `feat(999.2-02)` commit `355206c` — 10 tests passing after implementation
- No REFACTOR commit needed (implementation clean on first attempt after the format fix)

## Known Stubs

None — `_emit_smoke_scenario()` is fully implemented. `generate()` does not yet call it (plan 03 wires it), but the function itself has no stubs.

## Threat Flags

None — `_emit_smoke_scenario()` is pure-data (no network endpoints, no file I/O, no new auth paths). All operator-supplied values are serialized via `json.dumps()` before interpolation into generated source (V5 Tampering mitigation, consistent with existing `_render_header()` + `_render_init()` patterns).

## Self-Check: PASSED

- File `tests/framework/unit/test_codegen_smoke_emitter.py` exists: FOUND
- File `src/mcp_test_framework/test_code/_codegen.py` modified: FOUND
- Commit `65ddb9a` exists: FOUND
- Commit `355206c` exists: FOUND
- `uv run pytest tests/framework/unit/test_codegen_smoke_emitter.py`: 10 passed
- `uv run pytest tests/framework/unit/test_codegen_emitter.py`: 12 passed (no regression)

---
*Phase: 999.2-codegen-driven-parameter-test-generation-for-required-field-*
*Completed: 2026-05-27*
