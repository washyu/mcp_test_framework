---
phase: 999.2-codegen-driven-parameter-test-generation-for-required-field-
plan: 06
type: execute
wave: 3
depends_on: [03, 05]
files_modified:
  - docs/LIBRARY-MODE.md
  - docs/TEST-CODE-AUTHORING.md
  - README.md
autonomous: true
requirements: [GEN-08]
tags: [docs, operator-narrative, cross-reference]

must_haves:
  truths:
    - "docs/LIBRARY-MODE.md gains a Codegen-CLI subsection that documents the examples: field, the TODO/skip branch, and the recommended pairing with Phase 33 skip_buckets: ['output']"
    - "docs/TEST-CODE-AUTHORING.md gains a 'Smoke scaffolds for required-field tools' subsection that documents the per-tool _generated/<server>/<tool>_call_smoke.py output and the lift-out-of-_generated escape hatch"
    - "README.md gains a paragraph in the test-code-scenarios section showing the workflow: examples: in config.yaml -> gen-test-classes -> generated smoke runs under mcp_session, with a cross-link to the Phase 33 skip_buckets walkthrough"
    - "Every doc touch point uses create_proxmox_vm as the worked example (narrative consistency across Phase 33 + 999.2)"
    - "Every doc touch point cross-references the Phase 33 skip_buckets pairing rather than re-explaining it"
  artifacts:
    - path: "docs/LIBRARY-MODE.md"
      provides: "Codegen-CLI smoke-emission subsection + examples: field documentation"
      contains: "examples:"
    - path: "docs/TEST-CODE-AUTHORING.md"
      provides: "Smoke-scaffold subsection for the test-code author persona"
      contains: "_call_smoke.py"
    - path: "README.md"
      provides: "Workflow paragraph linking the codegen smoke to existing test-code-scenarios narrative"
      contains: "gen-test-classes"
  key_links:
    - from: "README.md"
      to: "docs/LIBRARY-MODE.md"
      via: "Cross-reference to the Codegen CLI subsection"
      pattern: "LIBRARY-MODE\\.md"
    - from: "docs/LIBRARY-MODE.md"
      to: "docs/LIBRARY-MODE.md"
      via: "Intra-doc cross-reference to existing Phase 33 skip_buckets walkthrough"
      pattern: "skipping-individual-test-buckets-per-tool|skip_buckets"
---

<objective>
Document the codegen smoke emission across the three operator-facing docs that already discuss
the codegen CLI and the test-code persona. Each doc touch is additive — extends an existing section
rather than adding a new chapter — and each cross-references Phase 33's `skip_buckets` walkthrough
rather than re-explaining the bucket model.

Purpose: When an operator reads README -> LIBRARY-MODE -> TEST-CODE-AUTHORING (the documented
reading order from v1.4 docs rewrite), they encounter the codegen smoke pattern at each level of
depth — overview / library-mode reference / test-code-author detail — with consistent terminology
and a single worked example (`create_proxmox_vm`).

Output: Three doc-file modifications; no new files; no docs/MIGRATION-style standalone files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-CONTEXT.md
@.planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-RESEARCH.md
@.planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-PATTERNS.md
@docs/LIBRARY-MODE.md
@docs/TEST-CODE-AUTHORING.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend docs/LIBRARY-MODE.md Codegen CLI section with examples: + smoke subsection</name>
  <files>docs/LIBRARY-MODE.md</files>
  <read_first>
    - docs/LIBRARY-MODE.md (lines 180-223 — Phase 33 skip_buckets walkthrough; tone reference for the new 999.2 subsection)
    - docs/LIBRARY-MODE.md (lines 296-356 — existing Codegen CLI (gen-test-classes) section; this is where the new 999.2 subsection slots in)
    - .planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-PATTERNS.md (§"`docs/LIBRARY-MODE.md` — extend Codegen CLI section")
    - .planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-RESEARCH.md (§"Config + Docs Touch Points" — third row)
  </read_first>
  <action>
    Open `docs/LIBRARY-MODE.md`. Locate the existing "Codegen CLI (`gen-test-classes`)" section
    (around line 296-356). At the end of that section (after the last existing subsection but
    BEFORE the next top-level "## " heading), add a new H3 subsection titled
    "### Codegen-driven smoke scenarios for required-field tools".

    The subsection MUST include the following content elements (write in the existing doc's tone —
    use the Phase 33 skip_buckets walkthrough at lines 180-223 as the tone reference):

    1. Opening paragraph: explain why required-field tools fail the empty-args output bucket
       (call_arguments defaults to {}; server rejects with `Field required`). Use create_proxmox_vm
       (needs vmid, name, node, host) as the canonical example.

    2. Output path: state that `gen-test-classes` emits
       `tests/test_code/_generated/<server>/<tool>_call_smoke.py` alongside each `<tool>.py`
       for every tool with `len(inputSchema.required) > 0`.

    3. Scenario mechanics: scenario constructs `<ToolName>Params(**example)` and awaits
       `tool().call(params)`, which routes through the Phase 24 `exclude_unset=True` serializer
       (operator-omitted optional fields are not sent to the server).

    4. Operator surface code block (yaml):
       ```yaml
       tools:
         create_proxmox_vm:
           skip_buckets: ["output"]   # see "Skipping individual test buckets per tool" above
           examples:
             - vmid: 9001
               name: smoke-test-vm
               node: pve1
               host: pve1
       ```

    5. Multi-example behavior: each example dict becomes one parametrized scenario invocation
       with positional id `example-<N>`. Keys NOT validated against inputSchema.required at
       config load (D-01b) — surfaces at runtime as a clean Pydantic ValidationError naming
       the missing key.

    6. Recommended pairing with previous subsection: cross-reference the Phase 33 skip_buckets
       walkthrough. Make explicit that the framework does NOT auto-pair these fields
       (SEED-022 — operator decides which combination runs). Use phrasing like "operator
       decides" or "SEED-022" so future readers can grep for the principle.

    7. Missing-examples behavior: include the EXACT grep-able skip reason text:
       `pytest.skip("no examples; populate tools.<name>.examples: in config.yaml to enable this smoke", allow_module_level=True)`.
       Operators reading pytest output should be able to map the message back to this docs section.

    8. Customizing escape hatch: explain that operators needing setup/teardown / multi-step
       lifecycle / dependency-ordered tools should lift the scenario out of `_generated/` into
       a hand-authored sibling under `tests/test_code/`. Cross-link to docs/TEST-CODE-AUTHORING.md.
       State explicitly that files in `_generated/` are wiped and rewritten on every
       `gen-test-classes` run (D-03 unconditional overwrite).

    Match the existing tone (verified at lines 181-223 for Phase 33's `skip_buckets` walkthrough).
    Use `create_proxmox_vm` as the worked example for consistency with Phase 33 docs at the same
    file (line 196) and with README.md (line 404).
  </action>
  <verify>
    <automated>grep -c "Codegen-driven smoke scenarios for required-field tools" docs/LIBRARY-MODE.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "### Codegen-driven smoke scenarios for required-field tools" docs/LIBRARY-MODE.md` exits 0
    - `grep -F "tests/test_code/_generated/<server>/<tool>_call_smoke.py" docs/LIBRARY-MODE.md` exits 0
    - `grep -F "tools.<name>.examples:" docs/LIBRARY-MODE.md` exits 0
    - `grep -c "create_proxmox_vm" docs/LIBRARY-MODE.md` returns >= 2 (Phase 33 ref + new 999.2 subsection)
    - `grep -c "skip_buckets" docs/LIBRARY-MODE.md` returns >= 2 (Phase 33 ref + new pairing example)
    - `grep -F "no examples; populate tools.<name>.examples: in config.yaml to enable this smoke" docs/LIBRARY-MODE.md` exits 0 (D-02a grep-able reason text appears in docs so operators recognize it in pytest output)
    - `grep -E "SEED-022|operator decides" docs/LIBRARY-MODE.md` exits 0 (SEED-022 framing surfaced)
    - `grep -F "exclude_unset=True" docs/LIBRARY-MODE.md` exits 0 (Phase 24 cross-reference)
    - `grep -F "TEST-CODE-AUTHORING.md" docs/LIBRARY-MODE.md` exits 0 (intra-doc cross-link present)
  </acceptance_criteria>
  <done>
    New H3 subsection added inside Codegen CLI section; examples: field documented; TODO/skip branch documented; pairing with Phase 33 cross-referenced; create_proxmox_vm used as worked example.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add smoke-scaffold subsection to docs/TEST-CODE-AUTHORING.md</name>
  <files>docs/TEST-CODE-AUTHORING.md</files>
  <read_first>
    - docs/TEST-CODE-AUTHORING.md (lines 130-156 — the loop-scope invariant block; reference tone for the new subsection AND the canonical citation for why the generated marker uses loop_scope="session")
    - docs/TEST-CODE-AUTHORING.md (around line 414 — near the end of "Conditional skip" section, before "Output ergonomics" — the insertion point)
    - .planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-PATTERNS.md (§"`docs/TEST-CODE-AUTHORING.md` — smoke-scaffold subsection")
    - .planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-RESEARCH.md (§"Config + Docs Touch Points" — fourth row)
  </read_first>
  <action>
    Open `docs/TEST-CODE-AUTHORING.md`. Locate the insertion point near the end of the
    "Conditional skip" section (around line 414 — verify by reading the surrounding context; if
    the line number has shifted from prior edits, use the section header text as the locator).
    Add a new section titled "Smoke scaffolds for required-field tools" at the same heading
    hierarchy level as adjacent sections (likely H2 — match what surrounds the insertion point).

    The new subsection MUST include:

    1. Opening: state that `mcp-contracts gen-test-classes` does double duty — emits both the
       Pydantic Params/Response classes AND a `<tool>_call_smoke.py` scaffold for every tool
       with `inputSchema.required` non-empty. Show the output path:
       `tests/test_code/_generated/<server>/<tool>_call_smoke.py`.

    2. Worked-example Python code block showing the generated scenario shape (use
       create_proxmox_vm — match Phase 33 narrative; reference the verbatim sketches in
       PATTERNS.md §"Branch A — single example"):

       ```python
       @pytest.mark.asyncio(loop_scope="session")
       async def test_create_proxmox_vm_call_smoke(mcp_session):
           params = CreateProxmoxVmParams(**{"vmid": 9001, "name": "smoke", ...})
           response = await tool("create_proxmox_vm").call(params)
           assert response.is_error is False
       ```

    3. loop_scope back-reference: state that the `loop_scope="session"` argument is the same
       invariant documented earlier in the file (lines 130-156). DO NOT duplicate the explanation;
       refer back to it.

    4. Operator surface: populate `tools.<name>.examples:` in `config.yaml`. Cross-link to
       `LIBRARY-MODE.md` with the new anchor from Task 1
       (`LIBRARY-MODE.md#codegen-driven-smoke-scenarios-for-required-field-tools`) for the
       recommended pairing with `skip_buckets: ["output"]`. Each example dict becomes one
       parametrized scenario invocation with positional id `example-<N>`.

    5. Missing-examples behavior: state that without `examples:`, the scaffold still lands as a
       `pytest.skip(..., allow_module_level=True)` module with the literal reason
       `"no examples; populate tools.<name>.examples: in config.yaml to enable this smoke"`. It
       appears in `pytest --collect-only` output so the operator/test-code-author discovers the
       scaffold immediately.

    6. Customizing beyond examples: lift-out-of-_generated escape hatch. Explain that the smoke
       scaffold is a STARTING POINT. For multi-step lifecycle, setup/teardown, or
       dependency-ordered tools, copy the scenario out of `_generated/` into a hand-authored
       sibling under `tests/test_code/` and edit there. Files in `_generated/` are wiped and
       rewritten on every `gen-test-classes` run.

    Match the didactic tone of the existing loop-scope-invariant block at lines 130-156.
  </action>
  <verify>
    <automated>grep -c "Smoke scaffolds for required-field tools" docs/TEST-CODE-AUTHORING.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Smoke scaffolds for required-field tools" docs/TEST-CODE-AUTHORING.md` exits 0
    - `grep -F "_call_smoke.py" docs/TEST-CODE-AUTHORING.md` exits 0
    - `grep -F "loop_scope=\"session\"" docs/TEST-CODE-AUTHORING.md` returns >= 2 occurrences (existing canonical block + new scaffold example)
    - `grep -F "LIBRARY-MODE.md" docs/TEST-CODE-AUTHORING.md` exits 0 (cross-link present)
    - `grep -F "create_proxmox_vm" docs/TEST-CODE-AUTHORING.md` exits 0 (worked example present)
    - `grep -F "allow_module_level=True" docs/TEST-CODE-AUTHORING.md` exits 0 (TODO branch documented)
    - `grep -F "no examples; populate tools.<name>.examples: in config.yaml to enable this smoke" docs/TEST-CODE-AUTHORING.md` exits 0 (D-02a reason text present so operators map pytest output back to docs)
    - `grep -E "lift|copy|hand-authored" docs/TEST-CODE-AUTHORING.md | grep -i -E "_generated|escape hatch"` exits 0 (lift-out escape hatch documented near the new subsection)
  </acceptance_criteria>
  <done>
    Smoke-scaffold subsection added; loop_scope invariant cross-referenced (not duplicated); cross-link to LIBRARY-MODE.md present; escape hatch (lift-out-of-_generated) documented.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add codegen-smoke workflow paragraph to README.md</name>
  <files>README.md</files>
  <read_first>
    - README.md (around line 200 — existing "Test-code scenarios" section; insertion point for the new workflow paragraph)
    - README.md (around lines 398-427 — existing Phase 33 skip_buckets cross-reference; needed for the cross-link in the new paragraph)
    - .planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-PATTERNS.md (§"`README.md` — Phase 33 cross-reference + workflow paragraph")
    - .planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-RESEARCH.md (§"Config + Docs Touch Points" — fifth row)
  </read_first>
  <action>
    Open `README.md`. Locate the "Test-code scenarios" section (around line 200 — verify by
    reading the surrounding context; if section headings have shifted from prior edits, use the
    section title text as the locator).

    Add a single workflow paragraph at the end of that section (after the existing content but
    before the next H2/H3 heading). The paragraph MUST include:

    1. The three-step workflow:
       - operator adds `tools.<name>.examples:` block in `config.yaml`
       - operator runs `mcp-contracts gen-test-classes`
       - generated `tests/test_code/_generated/<server>/<tool>_call_smoke.py` runs under
         the existing `mcp_session` fixture

    2. Use `create_proxmox_vm` as the worked example to match the Phase 33 cross-reference
       at lines 398-427.

    3. Cross-link both:
       - `docs/LIBRARY-MODE.md` (the new subsection from Task 1) for the full reference
       - The existing Phase 33 README cross-reference at lines 398-427 for the recommended
         pairing with `skip_buckets: ["output"]`

    4. State that the pairing is the operator's call (SEED-022 — no auto-pair) so the framing
       matches Phase 33's existing README treatment.

    Keep the paragraph terse — README is the overview; LIBRARY-MODE.md carries the reference
    detail and TEST-CODE-AUTHORING.md carries the test-code-author depth. Target ~5-8 sentences
    or one short paragraph + a single yaml code-fence example block.
  </action>
  <verify>
    <automated>grep -c "gen-test-classes" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "gen-test-classes" README.md` returns >= 2 occurrences (existing + new)
    - `grep -F "_call_smoke.py" README.md` exits 0
    - `grep -F "examples:" README.md` exits 0 (in the context of `tools.<name>.examples:`)
    - `grep -c "create_proxmox_vm" README.md` returns >= 2 occurrences (existing Phase 33 cross-ref + new 999.2 paragraph)
    - `grep -F "LIBRARY-MODE.md" README.md` exits 0 (cross-link to the full reference section)
    - `grep -F "mcp_session" README.md` exits 0 (workflow mentions the fixture)
    - `grep -E "SEED-022|operator decides" README.md` exits 0 (SEED-022 framing surfaced — matches Phase 33's existing treatment)
  </acceptance_criteria>
  <done>
    README test-code-scenarios section gains a workflow paragraph; cross-links to both LIBRARY-MODE.md and the existing Phase 33 README cross-ref present; create_proxmox_vm used; SEED-022 framing matches Phase 33.
  </done>
</task>

</tasks>

<verification>
- All three docs use create_proxmox_vm as the worked example (narrative consistency with Phase 33)
- D-02a grep-able skip reason text appears in both LIBRARY-MODE.md AND TEST-CODE-AUTHORING.md so operators can map pytest output back to docs
- SEED-022 framing surfaced across all three docs (matches Phase 33's existing treatment)
- No new top-level docs files created (only existing files extended)
- Each doc cross-references the others where appropriate (README -> LIBRARY-MODE -> TEST-CODE-AUTHORING reading path preserved)
</verification>

<success_criteria>
- docs/LIBRARY-MODE.md carries the full reference subsection (operator surface + recommended pairing + missing-examples behavior + escape hatch)
- docs/TEST-CODE-AUTHORING.md carries the test-code-author-focused subsection (scaffold shape + loop-scope back-reference + lift-out-of-_generated escape hatch)
- README.md carries the terse workflow paragraph (overview + cross-links)
- All grep-acceptance criteria satisfied across the three files
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-06-SUMMARY.md`.
</output>
