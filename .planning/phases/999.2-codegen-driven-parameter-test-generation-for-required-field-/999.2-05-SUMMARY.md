---
phase: 999.2-codegen-driven-parameter-test-generation-for-required-field-
plan: "05"
subsystem: docs
tags: [config, yaml, examples, codegen, docs, pattern-d]

requires:
  - phase: 999.2-codegen-driven-parameter-test-generation-for-required-field-
    provides: ToolConfig.examples field (plan 01) + _emit_smoke_scenario emitter (plan 02) — Pattern D documents the config shape that drives these

provides:
  - Pattern D scaffold block in config.example.yaml (generic operator template for required-field tools)
  - Commented opt-in template under create_proxmox_vm in examples/homelab-mcp.yaml (real-tool worked walkthrough)

affects:
  - 999.2-06 (docs plan — LIBRARY-MODE + TEST-CODE-AUTHORING references Pattern D shape)
  - Operators copying config.example.yaml or examples/homelab-mcp.yaml as a starting point

tech-stack:
  added: []
  patterns:
    - "Pattern D: required-field tool with skip_buckets: [output] + examples: pairing — the template for operator-side codegen smoke opt-in"

key-files:
  created: []
  modified:
    - config.example.yaml
    - examples/homelab-mcp.yaml

key-decisions:
  - "Pattern D uses <angle_bracket> placeholders in config.example.yaml (generic scaffold, no real tool names)"
  - "examples/homelab-mcp.yaml keeps skip: true as default; commented opt-in block preserves safe-by-default posture per lines 58-66 contract"
  - "Both files use create_proxmox_vm as the canonical worked-example tool for narrative consistency with Phase 33"

patterns-established:
  - "Pattern D: config.example.yaml scaffold block showing skip_buckets + examples pairing for required-field tools"

requirements-completed: [GEN-07]

duration: 5min
completed: 2026-05-27
---

# Phase 999.2 Plan 05: Pattern D config scaffold + homelab-mcp opt-in template Summary

**Pattern D added to config.example.yaml (generic scaffold) and a commented skip_buckets + examples opt-in block added under create_proxmox_vm in examples/homelab-mcp.yaml, preserving safe-by-default posture throughout**

## Performance

- **Duration:** ~5 min
- **Started:** 2026-05-27T01:25:24Z
- **Completed:** 2026-05-27T01:30:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Added Pattern D block to `config.example.yaml` after Pattern C — shows the `skip_buckets: ["output"]` + `examples:` pairing with `<angle_bracket>` placeholders and inline explanatory comments
- Extended `create_proxmox_vm:` block in `examples/homelab-mcp.yaml` with a commented-out opt-in template that cross-references the generated `create_proxmox_vm_call_smoke.py` artifact
- Both files still parse as YAML; neither file flips any operator posture from safe to destructive
- safe-by-default invariant preserved: `cfg['tools']['create_proxmox_vm']['skip']` is `True` on parse; `skip_buckets` and `examples` are absent from the parsed dict (they remain commented out)

## Task Commits

1. **Task 1: Add Pattern D block to config.example.yaml** - `494412b` (docs)
2. **Task 2: Extend create_proxmox_vm block with commented opt-in template** - `5a4f689` (docs)

## Files Created/Modified

- `config.example.yaml` — added Pattern D block (20 lines) after Pattern C, using `<angle_bracket>` placeholders; documents the `_call_smoke.py` generated artifact cross-reference
- `examples/homelab-mcp.yaml` — extended `create_proxmox_vm:` block (21 lines added); `skip: true` + `skip_reason:` remain active defaults; commented template shows the Phase 999.2 opt-in workflow

## Decisions Made

- **Placeholder convention preserved**: Pattern D uses `<required_field_tool>`, `<required_arg_a>`, `<value>` etc. — same `<angle_bracket>` style as Patterns A/B/C in `config.example.yaml`. No real tool name used in the generic scaffold.
- **Narrative consistency**: Both files reference `create_proxmox_vm` as the canonical example tool, matching Phase 33's worked example. Operators see one tool threaded across both phases.
- **Safe-by-default posture unchanged**: In `examples/homelab-mcp.yaml`, `skip: true` and `skip_reason:` are the active keys. The Phase 999.2 template is entirely commented out — operator must explicitly remove the skip lines and uncomment the template to enable.

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None. `python -c "import yaml; ..."` was not available directly (no yaml in system python) but `uv run python -c "import yaml; ..."` worked correctly. All acceptance criteria passed on first attempt.

## Known Stubs

None — these are documentation/scaffolding files. The Pattern D block and the commented opt-in template are intentionally incomplete-by-design (operators fill in real values). This is not a stub — it is the scaffold's purpose.

## Threat Flags

None — config example files contain no new network endpoints, auth paths, file access patterns, or schema changes at trust boundaries.

## Next Phase Readiness

- Pattern D scaffold is available for operators copying `config.example.yaml` as a starting point
- `examples/homelab-mcp.yaml` opt-in template is available for operators wanting to enable `create_proxmox_vm` smoke testing
- Plan 06 (docs: README + LIBRARY-MODE + TEST-CODE-AUTHORING) can reference both files as canonical examples

---
*Phase: 999.2-codegen-driven-parameter-test-generation-for-required-field-*
*Completed: 2026-05-27*
