---
phase: 999.2-codegen-driven-parameter-test-generation-for-required-field-
verified: 2026-05-26T00:00:00Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
gaps: []
human_verification: []
---

# Phase 999.2: Codegen-Driven Parameter Test Generation Verification Report

**Phase Goal:** Extend `mcp-contracts gen-test-classes` to emit `<tool>_call_smoke.py` typed test-code scenarios for every tool whose `inputSchema.required` is non-empty. Operator drops example arg dicts under `tools.<name>.examples:` in `config.yaml`; codegen owns the boilerplate, operator owns the values. Pairs with Phase 33 per-bucket skip.
**Verified:** 2026-05-26T00:00:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth (Requirement) | Status | Evidence |
|---|---------------------|--------|----------|
| 1 | GEN-01: `ToolConfig` gains `examples: list[dict[str, Any]] \| None = None` with shape validator | VERIFIED | `models.py:116-194` — field declared, `_validate_examples_shape` validator present (mode="before"), WR-02 tuple coercion applied; behavioral checks PASS |
| 2 | GEN-02: `gen-test-classes` emits `<tool>_call_smoke.py` for tools with non-empty `required`; tools with empty/missing `required` produce no smoke file | VERIFIED | `_codegen.py:670-693` gate on `isinstance(required, list) and len(required) > 0`; `generate()` carries `tools_config` kwarg; `cli.py:1396` passes `tools_config=cfg.tools`; `test_generate_does_not_emit_smoke_file_for_no_required_field_tool` PASSES |
| 3 | GEN-03: Examples-present branch produces runnable `@pytest.mark.asyncio(loop_scope="session")` scenario with `<ToolName>Params(**example)` + `await tool("<name>").call(params)` | VERIFIED | `_codegen.py:777-790` (Branch A) and `793-808` (Branch B); 10 pure-data tests in `test_codegen_smoke_emitter.py` pass; `test_emit_smoke_uses_loop_scope_session_marker` PASSES |
| 4 | GEN-04: Examples-absent branch emits fully importable TODO/skip module with exact D-02a grep-able reason text; relative import precedes `pytest.skip()` | VERIFIED | `_codegen.py:763-775` (Branch C); `test_emit_smoke_todo_branch_import_precedes_skip` and `test_todo_branch_module_is_importable` PASS; compile() check on generated source confirms valid Python |
| 5 | GEN-05: Multiple examples produce `@pytest.mark.parametrize` with positional `example-{i}` ids | VERIFIED | `_codegen.py:793-808` (Branch B); `test_emit_smoke_multi_example_uses_parametrize` PASSES; `ids=[f"example-{i}" for i in range(len(_EXAMPLES))]` literal in emitter |
| 6 | GEN-06: Every `<tool>_call_smoke.py` carries the 4-line header sentinel via `_render_header()` (D-03a shared sentinel, unconditional overwrite D-03) | VERIFIED | `_codegen.py:741-746` — all three branches start with `_render_header()`; `test_smoke_file_header_sentinel_matches_existing_tool_py_style` and `test_smoke_file_overwritten_unconditionally` PASS |
| 7 | GEN-07: `config.example.yaml` Pattern D block, `examples/homelab-mcp.yaml` opt-in comment, `docs/LIBRARY-MODE.md` subsection, `docs/TEST-CODE-AUTHORING.md` subsection, `README.md` workflow paragraph all delivered | VERIFIED | All 5 doc targets confirmed (detailed below); `examples/homelab-mcp.yaml` loads as `Config` without error after CR-01 fix; SEED-022 framing present in all three docs |
| 8 | GEN-08: Framework self-tests cover all required branches (required+examples, required-no-examples, no-required, multi-example, header sentinel, init.py integrity, config-load rejection) | VERIFIED | 49 tests across 4 test files — all PASS: 6 in `test_tool_config_examples.py`, 15 in `test_codegen_smoke_emitter.py`, 17 in `test_codegen_emitter.py` (5 new), 11 in `test_config_example.py` (1 new) |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/mcp_test_framework/models.py` | `ToolConfig.examples` field + `_validate_examples_shape` validator | VERIFIED | Field at line 116; validator at line 151 (mode="before", WR-02 coercion at line 177); no new `model_validator` added (model_validator count: 3, same as baseline — 2 at lines 215, 246) |
| `src/mcp_test_framework/test_code/_codegen.py` | `_emit_smoke_scenario()` function + `generate()` with `tools_config` kwarg + smoke counters | VERIFIED | `_emit_smoke_scenario` at line 706; `generate()` signature at line 617 with `tools_config: "dict[str, object] | None" = None`; counts dict includes `smoke_scenarios` and `smoke_todos`; NO `from mcp_test_framework.models` import (duck-typed access only) |
| `src/mcp_test_framework/cli.py` | `gen_test_classes` passes `tools_config=cfg.tools` + smoke summary line | VERIFIED | Line 1396: `tools_config=cfg.tools`; lines 1417-1421: smoke summary output |
| `tests/framework/unit/test_tool_config_examples.py` | 6 GEN-01 validator pins | VERIFIED | 6 tests present; D-04 guard + D-01b guard both pinned; no `model_validator` test added |
| `tests/framework/unit/test_codegen_smoke_emitter.py` | 10 pure-data emitter tests + 5 end-to-end integration tests | VERIFIED | 15 tests total; Plans 02 + 04 delivered; all pass |
| `tests/framework/unit/test_codegen_emitter.py` | 5 new `generate()`-level smoke tests + backward-compat | VERIFIED | 5 new tests (lines 247-329); `_ToolCfgShim` shim added; count assertions updated for 4-key dict |
| `tests/framework/unit/test_config_example.py` | Pattern D structural assertion (WR-01 fix) | VERIFIED | `test_config_example_pattern_d_has_examples_and_skip_buckets` at line 119 PASSES |
| `config.example.yaml` | Pattern D block with `<required_field_tool>`, `skip_buckets`, `examples` placeholders | VERIFIED | Lines 75-93; YAML parses cleanly; no real tool name (`create_proxmox_vm` absent); `python -c "import yaml; yaml.safe_load(open('config.example.yaml'))"` exits 0 |
| `examples/homelab-mcp.yaml` | `create_proxmox_vm` block with commented opt-in template; `skip: true` default preserved; `test_code:` key (not `sdet:`) | VERIFIED | CR-01 fix confirmed: `test_code: generated_root: tests/test_code/_generated`; `Config(yaml_file='examples/homelab-mcp.yaml')` loads without error; `skip: true` active; `skip_buckets`/`examples` commented out |
| `docs/LIBRARY-MODE.md` | "Codegen-driven smoke scenarios for required-field tools" H3 subsection | VERIFIED | Line 333; contains D-02a skip reason text, `exclude_unset=True` cross-ref, SEED-022/"operator decides" framing, cross-link to TEST-CODE-AUTHORING.md |
| `docs/TEST-CODE-AUTHORING.md` | "Smoke scaffolds for required-field tools" section | VERIFIED | Line 464; contains `_call_smoke.py`, `allow_module_level=True`, `loop_scope` reference, cross-link to LIBRARY-MODE.md with anchor, D-02a skip reason text, escape hatch mention |
| `README.md` | Workflow paragraph with `gen-test-classes`, `_call_smoke.py`, `mcp_session`, SEED-022 framing | VERIFIED | Lines 325-346; `gen-test-classes` appears >=2 times; `operator decides` framing; LIBRARY-MODE.md cross-link present |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `cli.py` | `_codegen.generate()` | `tools_config=cfg.tools` | WIRED | `cli.py:1396` confirmed |
| `generate()` loop | `_emit_smoke_scenario()` | per-tool call inside loop, before `__init__.py` write | WIRED | `_codegen.py:679`; `__init__.py` write at line 701 (after loop) |
| `_emit_smoke_scenario()` | `_render_header()` | header sentinel reuse (D-03a) | WIRED | `_codegen.py:741`; `_render_header` called in `_emit_smoke_scenario` — confirmed via `grep -F "_render_header(" _codegen.py` returns >= 2 matches |
| `ToolConfig.examples` | `generate()` smoke branch | `getattr(tool_cfg, "examples", None)` duck-typed read | WIRED | `_codegen.py:678`; no direct `models.py` import in `_codegen.py` (verified) |
| `config.example.yaml` | `examples/homelab-mcp.yaml` | Pattern D shape is the model for the homelab-mcp template | WIRED | Both files use `skip_buckets: ["output"]` + `examples:` pairing consistently |

### Data-Flow Trace (Level 4)

Phase 999.2 is a codegen feature, not a data-rendering component. The relevant data flows are:

| Flow | Source | Sink | Real Data? | Status |
|------|--------|------|------------|--------|
| `ToolConfig.examples` → `generate()` → `_emit_smoke_scenario()` → `<tool>_call_smoke.py` | Operator YAML under `tools.<name>.examples:` | Generated Python source on disk | Yes — operator values JSON-escaped into generated file | FLOWING |
| `generate()` counts → CLI output (`smoke: N scenarios, M placeholders`) | Internal counter in `generate()` | `typer.echo` in `gen_test_classes` | Yes — `smoke_scenarios` and `smoke_todos` counters incremented per tool | FLOWING |

### Behavioral Spot-Checks

| Behavior | Result | Status |
|----------|--------|--------|
| `ToolConfig().examples is None` | Confirmed via `uv run python` | PASS |
| `ToolConfig(examples=[123])` raises `ValidationError` with `examples[0]` in message | Confirmed | PASS |
| `ToolConfig(examples=[{'vmid': 9001}], skip_buckets=['output'])` validates (D-04) | Confirmed | PASS |
| `ToolConfig(examples=({'vmid': 9001},))` coerces tuple to list (WR-02) | Confirmed | PASS |
| `_emit_smoke_scenario(..., examples=None)` produces importable Python with import-before-skip | Confirmed | PASS |
| `_emit_smoke_scenario(..., examples=[{'vmid': 1}])` produces `@pytest.mark.asyncio(loop_scope="session")` | Confirmed | PASS |
| `_emit_smoke_scenario(..., examples=[{'name': 'x"inject'}])` escapes dangerous value via json.dumps | Confirmed | PASS |
| `generate(..., tools_config={'t': Shim([{'vmid': 1}])})` returns `smoke_scenarios=1, smoke_todos=0` | Confirmed | PASS |
| `examples/homelab-mcp.yaml` loads as `Config` without error | Confirmed | PASS |
| `config.example.yaml` YAML-parses and Pattern D has `examples` + `skip_buckets` | Confirmed | PASS |

### SEED-022 Principle Audit

The phase explicitly honors the "framework does no tool-safety reasoning; operator owns example values" principle:

- `_codegen.py` never reads `skip_buckets` (confirmed by `grep -n "skip_buckets" _codegen.py` returning only a docstring comment)
- No `model_validator` linking `examples` and `skip_buckets` was added (model_validator count unchanged at 3)
- `test_examples_compatible_with_skip_buckets` explicitly pins that the pairing is NOT enforced
- All three docs describe the pairing as "operator decides" and cite SEED-022

### Requirements Coverage

| Requirement | Plan | Status | Evidence |
|-------------|------|--------|----------|
| GEN-01 | 999.2-01 | SATISFIED | `models.py` `ToolConfig.examples` + validator; 6 unit tests PASS |
| GEN-02 | 999.2-03 | SATISFIED | `generate()` `tools_config` kwarg + emission gate; 5 integration tests PASS |
| GEN-03 | 999.2-02 | SATISFIED | `_emit_smoke_scenario` Branch A; 10 pure-data tests PASS |
| GEN-04 | 999.2-02 | SATISFIED | `_emit_smoke_scenario` Branch C (TODO/skip); import-before-skip confirmed |
| GEN-05 | 999.2-02 | SATISFIED | `_emit_smoke_scenario` Branch B (parametrize); positional `example-{i}` ids |
| GEN-06 | 999.2-02/03 | SATISFIED | `_render_header()` reuse in all branches; unconditional overwrite via `shutil.rmtree` |
| GEN-07 | 999.2-05/06 | SATISFIED | All 5 doc targets delivered; SEED-022 framing present; safe-by-default posture preserved |
| GEN-08 | 999.2-04 | SATISFIED | 49 tests across 4 files; all branches pinned; config-load rejection pinned; `test_config_example_pattern_d_has_examples_and_skip_buckets` added via WR-01 fix |

Note: GEN requirements are phase-derived (from 999.2-RESEARCH.md) and are not listed in REQUIREMENTS.md (which covers SHIM/BUCKET/ISOL/V1DROP for v1.5). This is expected — the phase plan explicitly states "ROADMAP-level requirements remain TBD until v1.6+ promotion."

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `src/mcp_test_framework/cli.py:1396` | `# Phase 999.2 / GEN-02` comment | Info | Internal implementation comment, not operator-facing; acceptable |
| `src/mcp_test_framework/test_code/_codegen.py:670` | `# Phase 999.2 / GEN-02` comment | Info | Same; internal comment |
| `tests/framework/unit/test_codegen_smoke_emitter.py` and `test_codegen_emitter.py` | `_ToolCfgShim` duplicated | Info | Intentional (per IN-03 in REVIEW.md); "self-contained" philosophy; both shims work correctly |

No blocker or warning anti-patterns found. The three Info items from the code review (IN-01 indentation, IN-02 noqa breadth, IN-03 shim duplication) are cosmetic and do not affect correctness. They were explicitly not in scope for the review fix (only CR-01 + WR-01..03 were fixed).

### Human Verification Required

None. Phase 999.2 is a developer-facing codegen feature. All behaviors are verifiable programmatically:
- The generated file content is deterministic string output (pure-data function)
- The operator config surface is Pydantic-validated (load-time errors are testable)
- The CLI wiring is testable without a live MCP server (unit tests use `tmp_path`)
- Doc content is verifiable by grep

The only "live" behavior (running `mcp-contracts gen-test-classes` against actual homelab-mcp and verifying the emitted smoke files would pass if run with a live server) is an integration concern deferred to the normal operator UAT workflow. The framework test suite covers all code paths.

### Gaps Summary

No gaps. All 8 GEN requirements are delivered and verified in the codebase.

The code review found 1 blocker (CR-01: `sdet:` key in homelab-mcp.yaml) and 3 warnings (WR-01..03), all of which were fixed before this verification ran. The 3 info items (IN-01..03) are cosmetic and were intentionally left out of scope for the fix pass.

---

_Verified: 2026-05-26T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
