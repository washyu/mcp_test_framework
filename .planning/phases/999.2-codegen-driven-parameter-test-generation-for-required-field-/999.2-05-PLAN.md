---
phase: 999.2-codegen-driven-parameter-test-generation-for-required-field-
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - config.example.yaml
  - examples/homelab-mcp.yaml
autonomous: true
requirements: [GEN-07]
tags: [docs, config, examples]

must_haves:
  truths:
    - "config.example.yaml gains a 'Pattern D' block showing the recommended examples: + skip_buckets: ['output'] pairing for required-field tools"
    - "examples/homelab-mcp.yaml `create_proxmox_vm:` block carries a commented-out template the operator uncomments to opt in to the codegen smoke (safe-by-default posture preserved per the file's lines 58-66 contract)"
    - "Both files use create_proxmox_vm as the worked-example tool (narrative consistency with Phase 33 — operator sees one tool threaded across both phases)"
    - "Neither file silently flips an operator's posture from safe to destructive — opt-in remains explicit"
  artifacts:
    - path: "config.example.yaml"
      provides: "Pattern D template for operator scaffolding"
      contains: "Pattern D"
    - path: "examples/homelab-mcp.yaml"
      provides: "Commented opt-in template under create_proxmox_vm (real-tool walkthrough)"
      contains: "To opt in to the codegen-driven smoke scenario"
  key_links:
    - from: "config.example.yaml"
      to: "examples/homelab-mcp.yaml"
      via: "Pattern D shape used as the model for the homelab-mcp commented template"
      pattern: "skip_buckets:.*\\[.*output.*\\]"
---

<objective>
Add Pattern D scaffolding to `config.example.yaml` (the generic operator-scaffold file) and a
commented-out opt-in template to `examples/homelab-mcp.yaml`'s `create_proxmox_vm:` block (the
real-tool example). Both reference the recommended `examples:` + `skip_buckets: ["output"]` pairing
from Phase 33's worked example so the narrative threads cleanly.

Purpose: When an operator runs `mcp-contracts config-init` or copies `config.example.yaml` /
`examples/homelab-mcp.yaml` as a starting point, they encounter the codegen-smoke pattern as
discoverable scaffolding — not as undocumented config-yaml lore.

Output: One new Pattern D block in `config.example.yaml`; one extended commented-template block
in `examples/homelab-mcp.yaml`. Both files preserve their existing safety posture (safe-by-default;
operator must explicitly opt in).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-CONTEXT.md
@.planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-RESEARCH.md
@.planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-PATTERNS.md
@config.example.yaml
@examples/homelab-mcp.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Pattern D block to config.example.yaml</name>
  <files>config.example.yaml</files>
  <read_first>
    - config.example.yaml (lines 45-90 — the existing Pattern A/B/C structure; the Pattern D block must MATCH this tone exactly: terse header comment + placeholder `<angle_brackets>` for operator-replaceable values + per-field comment annotation)
    - .planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-PATTERNS.md (§"`config.example.yaml` — Pattern D block" — verbatim template at lines 478-490)
    - .planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-RESEARCH.md (§"Config + Docs Touch Points" — first row)
  </read_first>
  <action>
    Open `config.example.yaml`. Locate the existing "Pattern C" block (around line 65-73 — the
    `<destructive_tool_c>: skip: true, skip_reason: ...` block). Append the following NEW block
    immediately after Pattern C, before any subsequent top-level keys:

    ```yaml
      <required_field_tool>:
        # Pattern D -- required-field tool with examples + bucket skip.
        # Required-field tools cannot satisfy the empty-args output bucket
        # (the deterministic output-conformance check calls the tool with
        # call_arguments defaulting to {}, which the server rejects with
        # `Field required` before the framework can grade the response).
        #
        # Recommended pairing: skip the output bucket AND populate examples:
        # so `mcp-contracts gen-test-classes` emits a real-args smoke scenario
        # under tests/test_code/_generated/<server>/<tool>_call_smoke.py.
        # The schema + judge buckets still run; the output-bucket signal is
        # replaced by the codegen scenario (operator owns the pairing decision
        # per SEED-022 -- the framework does not auto-pair these fields).
        skip_buckets: ["output"]
        examples:
          - <required_arg_a>: <value>
            <required_arg_b>: <value>
          # Add additional dicts to parametrize the smoke scenario; each example
          # becomes one scenario invocation with positional id `example-<N>`.
    ```

    Place Pattern D at the same indentation level as Pattern A/B/C (under the existing `tools:`
    top-level key). Use literal `<angle_bracket>` placeholders for operator-replaceable values
    to match the Pattern A/B/C style (verified at config.example.yaml lines 49, 60-62).

    Do NOT add an actual tool name (e.g. `create_proxmox_vm`) here — `config.example.yaml` is the
    generic scaffold; real tool names belong in `examples/homelab-mcp.yaml`. Do NOT enable the
    Pattern D block by default — placeholders ensure the operator must edit the file before any
    smoke is generated (matches the file's general "scaffold, not config" posture).
  </action>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('config.example.yaml'))" 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "Pattern D -- required-field tool with examples + bucket skip" config.example.yaml` exits 0
    - `grep -F "<required_field_tool>:" config.example.yaml` exits 0 (placeholder convention preserved)
    - `grep -F "<required_arg_a>: <value>" config.example.yaml` exits 0
    - `grep -F "skip_buckets: [\"output\"]" config.example.yaml` exits 0
    - `grep -F "examples:" config.example.yaml` exits 0
    - `grep -F "_call_smoke.py" config.example.yaml` exits 0 (cross-reference to the generated artifact)
    - `python -c "import yaml; yaml.safe_load(open('config.example.yaml'))"` exits 0 (YAML still parses)
    - `grep -F "create_proxmox_vm" config.example.yaml` exits 1 (no real tool name leaked into the generic scaffold)
  </acceptance_criteria>
  <done>
    Pattern D block added after Pattern C; YAML still parses; placeholders used (no real tool name); skip_buckets + examples pairing demonstrated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend examples/homelab-mcp.yaml create_proxmox_vm block with commented opt-in template</name>
  <files>examples/homelab-mcp.yaml</files>
  <read_first>
    - examples/homelab-mcp.yaml (lines 55-100 — the SAFE-BY-DEFAULT POSTURE comment at lines 58-66 AND the existing `create_proxmox_vm:` block at lines 93-95)
    - .planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-PATTERNS.md (§"`examples/homelab-mcp.yaml` — extend `create_proxmox_vm` block" — verbatim template at lines 500-516)
    - .planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-RESEARCH.md (§"Config + Docs Touch Points" — second row + the safety-posture invariant)
  </read_first>
  <action>
    Open `examples/homelab-mcp.yaml`. Locate the existing `create_proxmox_vm:` block (around
    lines 93-95):

    ```yaml
      create_proxmox_vm:
        skip: true
        skip_reason: "destructive: provisions Proxmox VM"
    ```

    Replace ONLY that block (do not touch other tool entries) with the extended block below.
    CRITICAL: keep `skip: true` as the default; the new content is a COMMENTED-OUT template the
    operator uncomments to opt in. This preserves the file's "SAFE-BY-DEFAULT POSTURE" contract
    documented at lines 58-66 — operators must explicitly opt in to anything destructive:

    ```yaml
      create_proxmox_vm:
        skip: true
        skip_reason: "destructive: provisions Proxmox VM"
        # To opt in to the Phase 999.2 codegen-driven smoke scenario, REMOVE the
        # two lines above (skip + skip_reason) and UNCOMMENT the block below.
        # Replace example values with VM specs that are safe to create AND destroy
        # in your environment -- the smoke scenario actually provisions a VM, so
        # the example values must reflect an isolated test cluster, not production.
        #
        # The pairing:
        #   skip_buckets: ["output"] -- the empty-args output bucket cannot succeed
        #                              for required-field tools; skip it.
        #   examples: [...]          -- gen-test-classes reads this and emits
        #                              tests/test_code/_generated/homelab_mcp/
        #                              create_proxmox_vm_call_smoke.py with real
        #                              args -- restoring the output-bucket signal
        #                              via a typed test-code scenario instead.
        #
        # skip_buckets: ["output"]
        # examples:
        #   - vmid: 9001
        #     name: smoke-test-vm
        #     node: pve1
        #     host: pve1
    ```

    Do NOT remove the `skip: true` / `skip_reason:` lines — the safety-posture invariant requires
    they remain the default. Do NOT enable the `skip_buckets` / `examples` lines (leave them
    commented). Use `create_proxmox_vm` as the tool name (matches Phase 33's worked example for
    narrative consistency — see RESEARCH §"Config + Docs Touch Points" closing note + the
    create_proxmox_vm cross-reference in README.md:404 and LIBRARY-MODE.md:196).

    Do NOT modify any other tool entry in the file. Preserve the SAFE-BY-DEFAULT POSTURE comment
    block at lines 58-66.
  </action>
  <verify>
    <automated>python -c "import yaml; cfg = yaml.safe_load(open('examples/homelab-mcp.yaml')); assert cfg['tools']['create_proxmox_vm']['skip'] is True, 'safe-by-default posture regressed'; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "To opt in to the Phase 999.2 codegen-driven smoke scenario" examples/homelab-mcp.yaml` exits 0
    - `grep -F "create_proxmox_vm_call_smoke.py" examples/homelab-mcp.yaml` exits 0 (cross-reference to the generated artifact)
    - `grep -F "# skip_buckets: [\"output\"]" examples/homelab-mcp.yaml` exits 0 (template is COMMENTED OUT)
    - `grep -F "# examples:" examples/homelab-mcp.yaml` exits 0 (template is COMMENTED OUT)
    - `grep -F "create_proxmox_vm:" examples/homelab-mcp.yaml` exits 0 (block still exists)
    - `python -c "import yaml; cfg = yaml.safe_load(open('examples/homelab-mcp.yaml')); assert cfg['tools']['create_proxmox_vm']['skip'] is True"` exits 0 (safe-by-default posture preserved)
    - `python -c "import yaml; cfg = yaml.safe_load(open('examples/homelab-mcp.yaml')); assert 'skip_buckets' not in cfg['tools']['create_proxmox_vm']"` exits 0 (active skip_buckets NOT enabled — operator must uncomment)
    - `python -c "import yaml; cfg = yaml.safe_load(open('examples/homelab-mcp.yaml')); assert 'examples' not in cfg['tools']['create_proxmox_vm']"` exits 0 (active examples NOT enabled — operator must uncomment)
    - The existing SAFE-BY-DEFAULT POSTURE block still appears in the file: `grep -F "SAFE-BY-DEFAULT POSTURE" examples/homelab-mcp.yaml` exits 0
  </acceptance_criteria>
  <done>
    create_proxmox_vm block extended with commented opt-in template; skip: true still wins as the parsed value; file's SAFE-BY-DEFAULT POSTURE invariant preserved; YAML still parses.
  </done>
</task>

</tasks>

<verification>
- Both YAML files still parse
- Neither file flips operator posture from safe to destructive automatically
- Pattern D in config.example.yaml uses placeholders (generic scaffold)
- examples/homelab-mcp.yaml uses create_proxmox_vm (real-tool walkthrough matching Phase 33 narrative)
</verification>

<success_criteria>
- Pattern D block landed in config.example.yaml between Pattern C and the next top-level section
- create_proxmox_vm block in examples/homelab-mcp.yaml gains commented-out skip_buckets + examples template
- safe-by-default posture preserved (skip: true wins on parse)
- create_proxmox_vm is the worked-example tool across both phases (33 + 999.2)
</success_criteria>

<output>
After completion, create `.planning/phases/999.2-codegen-driven-parameter-test-generation-for-required-field-/999.2-05-SUMMARY.md`.
</output>
