---
phase: 17-schema-driven-codegen-surface
verified: 2026-05-13T02:54:47Z
status: human_needed
score: 5/6 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Operator runs `mcp-test-framework gen-sdet-classes` against live homelab-mcp with a real config.yaml, then inspects `src/mcp_test_framework/sdet/generated/homelab_mcp/`"
    expected: "Every advertised tool produces a `<tool>.py` file with PascalCase Params + Response classes; `__init__.py` re-exports + `_REGISTRY` populated; stdout digest shows server name + version + slug + tool count + degraded count; rerunning produces byte-identical output modulo the Regenerated timestamp line"
    why_human: "Phase 17's SC1/SC2 explicitly target operator workflow against a real server. Unit + stub-server tests cover the mechanism, but only a live homelab-mcp run proves the end-to-end operator experience with the canonical SUT (homelab-mcp ~70 tools surfacing the realistic degradation matrix)."
  - test: "Operator runs `uv run pyright` against the freshly generated `src/mcp_test_framework/sdet/generated/homelab_mcp/` directory"
    expected: "Exit 0 — every Params/Response class type-checks clean under strict mode, including degraded `typing.Any` fields"
    why_human: "Test `test_synthetic_generated_passes_pyright_strict` covers a 5-tool synthetic fixture, but real homelab-mcp schemas surface enum/oneOf/anyOf/nested-object combinations the synthetic doesn't model. SC1 says generated files compile under pyright with no errors — needs verification against the real schema set."
deferred:
  - truth: "ROADMAP SC3: `tool(\"name\").call(params)` validates params against inputSchema before the wire call AND returns a typed response object on the way back"
    addressed_in: "Phase 18"
    evidence: "Phase 18 success criteria: 'An SDET importing `from mcp_test_framework.sdet import mcp_session, tool` gets a session-scoped `McpTestClient` wrapper and a `tool(name)` builder'. Phase 17 CONTEXT.md OOS list: 'The `--sdet` flag on `run`, `mcp_session` / `tool(name)` runtime fixtures, `ToolCallError`. Those are Phase 18.' 17-03-PLAN explicitly ships `.call()` as a stub that raises NotImplementedError with a Phase-18 reference message. The Pydantic-validation half of SC3 IS satisfied at `Params(...)` construction time (extra=\"forbid\", required-field enforcement); only the wire body is deferred."
---

# Phase 17: Schema-driven codegen surface — Verification Report

**Phase Goal:** An SDET can run a single command and get importable, typed Python classes for every tool the connected MCP server advertises — covering both `outputSchema`-declared and `outputSchema`-undeclared tools through a uniform base.
**Verified:** 2026-05-13T02:54:47Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Roadmap Success Criteria + Plan must_haves)

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| SC1 | Operator can run `gen-sdet-classes` against a configured MCP server and find generated `<ToolName>Params`/`<ToolName>Response` classes under `src/mcp_test_framework/sdet/generated/<server_slug>/` that compile under mypy/pyright with no errors | ⚠️ PARTIAL → human | CLI registered (`cli.py:881` `@app.command("gen-sdet-classes")`); `--help` works; SAFE-03 fail-loud path tested. End-to-end stub-server test (`test_gen_sdet_classes_cli.py`) confirms tree emission. Pyright gate against 5-tool synthetic passes (`test_synthetic_generated_passes_pyright_strict`). Live homelab-mcp run + pyright pass against real schemas is unverified (covered in human_verification). |
| SC2 | Re-running `gen-sdet-classes` is idempotent — only files inside `generated/<server_slug>/` change; nothing outside that tree is touched; generated files carry a clear "do not hand-edit" header | ✓ VERIFIED | `_codegen.py:449` `shutil.rmtree(target, ignore_errors=True)` where `target = out_root / slug` — scoped to slug dir only. `HEADER_TEMPLATE` (`_codegen.py:43`) emits the AUTOGENERATED + DO NOT HAND-EDIT + "Extend by subclassing" header. Idempotence pinned by `test_emitter_is_idempotent_with_fixed_timestamp`. Behavioral run confirmed byte-identical output with fixed timestamp. |
| SC3 | A test file importing a generated `Params` class and calling `tool("name").call(params)` validates the params against the live `inputSchema` before the wire call (Pydantic) and returns a typed response object on the way back | ⚠️ DEFERRED to Phase 18 | Params half: generated `<Tool>Params` classes ARE Pydantic v2 BaseModel with `model_config = ConfigDict(extra="forbid")` (`_codegen.py:357`) — required/optional/types enforced at construction. Wire half: `ToolWrapper.call()` (`_tool_factory.py:70-82`) raises NotImplementedError with explicit Phase-18 reference. Phase 17 CONTEXT.md OOS explicitly defers the runtime to Phase 18. See `deferred` section. |
| SC4 | The `ToolResponse` base provides `.raw`, `.data`, `.text`, `.is_error` uniformly — test code does not need to branch on whether the response type is declared | ✓ VERIFIED | `response.py:36-101` defines `ToolResponse(BaseModel)` with `raw: CallToolResult`, three `@computed_field @property` accessors. `.data` implements CODEGEN-04 fallback chain (structuredContent → JSON-parse first TextContent → `{"text": concat}` → None). `.text` uses `isinstance(b, TextContent)` filter (Pitfall 7). `test_tool_response.py` (9 tests) pin every branch including ImageContent hygiene. |
| MH-1 | `from mcp_test_framework.sdet import ToolResponse` works at import time | ✓ VERIFIED | `sdet/__init__.py:13` re-exports. Smoke import confirmed in shell. |
| MH-2 | Stable factory seam: `tool(name)` returns `ToolWrapper` with `.params_cls`/`.response_cls`/`.name`; raises RuntimeError when no active registry; raises KeyError listing available names when registry active but unknown name; `.call()` raises NotImplementedError mentioning Phase 18 | ✓ VERIFIED | `_tool_factory.py` implements all four paths. `test_tool_factory.py` (8 tests) pin them. Module-level state isolation verified. |

**Score:** 5/6 truths verified (SC3 is a documented Phase 17 → Phase 18 split — deferred per Step 9b; SC1 is verified for mechanism but needs human live-stack confirmation).

### Deferred Items

| # | Item | Addressed In | Evidence |
| --- | --- | --- | --- |
| 1 | SC3 wire-side: `tool("name").call(params)` returns a typed response object on the way back | Phase 18 | Phase 18 SC2: "An SDET importing `from mcp_test_framework.sdet import mcp_session, tool` gets a session-scoped `McpTestClient` wrapper and a `tool(name)` builder; both require `@pytest.mark.asyncio` markers". Phase 17 CONTEXT.md explicitly lists `tool(name)` runtime fixtures as OOS / Phase 18. 17-03-PLAN ships the seam with `.call()` raising NotImplementedError citing Phase 18. The Pydantic validation half of SC3 IS satisfied today. |

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `src/mcp_test_framework/sdet/__init__.py` | Re-exports `ToolResponse` | ✓ VERIFIED | 16 lines, exports `ToolResponse` only (correct per Phase 17 scope — `tool` factory not yet on public surface until Phase 18 promotes it) |
| `src/mcp_test_framework/sdet/response.py` | `ToolResponse` Pydantic v2 BaseModel + 3 computed_field properties | ✓ VERIFIED | 102 lines; implements all 4 CODEGEN-04 fallback rungs |
| `src/mcp_test_framework/sdet/_slugs.py` | `server_slug` / `pascal_case` / `module_name` helpers | ✓ VERIFIED | 96 lines; keyword guards + identifier-leading-digit guards present |
| `src/mcp_test_framework/sdet/_codegen.py` | JSON-Schema walker + file emitter (D-01..D-03, D-06) | ✓ VERIFIED | 476 lines; `translate_tool` + `generate` (wipe-and-write); HEADER_TEMPLATE; structural-only schema check (documented deviation honoring D-02 "degrade unknown types, not refuse") |
| `src/mcp_test_framework/sdet/_tool_factory.py` | `ToolWrapper` Generic + `tool(name)` factory; `.call()` Phase-18 seam | ✓ VERIFIED | 116 lines; `_REGISTRIES` + `_ACTIVE_SLUG` module-level slots ready for Phase 18's `mcp_session` fixture |
| `src/mcp_test_framework/sdet/generated/.gitkeep` | Placeholder so empty dir tracks in git | ✓ VERIFIED | 0-byte `.gitkeep` present; directory empty as expected (operator runs `gen-sdet-classes` to populate) |
| `src/mcp_test_framework/cli.py` (`@app.command("gen-sdet-classes")`) | Typer subcommand registered alongside `run`/`list-tools`/`config-init`/`version` | ✓ VERIFIED | `cli.py:881` registers `gen-sdet-classes`; `--help` lists `--config` only (no `--output-dir`/`--force` per D-08); error paths map to operator-tone messages; uses `asyncio.Runner` + `AsyncExitStack`-owned `McpTestClient`; class-level `ClientSession.initialize` monkey-patch documented (deviation from plan's `_initialize_result` access; necessary because mcp 1.27.0 doesn't cache InitializeResult on the session) |
| `pyproject.toml` `[dependency-groups].dev` + `[tool.pyright]` | `pyright>=1.1.409` dev dep + scoped strict config | ✓ VERIFIED | `pyright>=1.1.409` declared at L29; `[tool.pyright]` block at L76-86 with include + strict scoped to `sdet/generated` + `sdet/response.py`, `pythonVersion = "3.14"`. Note: `response.py` in strict scope surfaces 1 pyright error under `uv run pyright` (`dict[Unknown, Unknown]` partial-unknown return in `.data`); does NOT affect the test-gate (test invokes pyright via explicit path arg which doesn't pick up the strict scope config). Minor scope-creep config issue, not a goal failure. |
| `tests/framework/unit/test_tool_response.py` | Pins CODEGEN-04 fallback chain + non-text-block hygiene | ✓ VERIFIED | 101 lines, 9 tests, all passing |
| `tests/framework/unit/test_codegen_walker.py` | Pins D-02 coverage + degradation | ✓ VERIFIED | 370 lines, 37 tests, all passing |
| `tests/framework/unit/test_codegen_emitter.py` | Pins file layout + header + idempotence + __init__-last ordering | ✓ VERIFIED | 169 lines, 9 tests, all passing |
| `tests/framework/unit/test_tool_factory.py` | Pins the Phase 17 → Phase 18 seam contract | ✓ VERIFIED | 118 lines, 8 tests, all passing |
| `tests/framework/unit/test_gen_sdet_classes_cli.py` | Pins CLI shape + error paths + stub-server end-to-end | ✓ VERIFIED | 218 lines, 5 tests (collected), all passing |
| `tests/framework/unit/test_codegen_typecheck.py` | Pyright gate against synthetic generated dir + negative-coverage | ✓ VERIFIED | 187 lines, 2 tests, both passing |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| `cli.py gen_sdet_classes` | `_codegen.generate` | direct call | ✓ WIRED | `cli.py:961` `counts = _codegen.generate(...)`. Result counts rendered to stdout digest (`cli.py:978-986`). |
| `cli.py gen_sdet_classes` | `_load_config` (Phase 13 SAFE-01..07 precedence) | `_load_config(config)` strict | ✓ WIRED | `cli.py:906` `cfg = _load_config(config)`. Behavioral test confirmed SAFE-03 fail-loud emits the operator-tone message + exit 2 when no config discoverable. |
| `cli.py gen_sdet_classes` | `serverInfo.name` from MCP initialize handshake | Class-level monkey-patch of `ClientSession.initialize` in `_run_codegen_handshake` (cli.py:989-1051) | ✓ WIRED | Deviation from plan (plan said `_initialize_result.serverInfo`); mcp 1.27.0 doesn't cache that, so executor patched `ClientSession.initialize` to capture the InitializeResult. Documented in 17-04-SUMMARY. Patch is scoped to a single `async with` block and reverted in `finally`. |
| `_codegen.py` | `ToolResponse` | Emitted string `from mcp_test_framework.sdet.response import ToolResponse` | ✓ WIRED | Verified by emitting against a stub tool and confirming the import line appears in every generated file. |
| `_codegen.py` | `jsonschema.validators.Draft202012Validator` | Imported at L28 (deliberately not invoked for `check_schema`; structural-only `_check_schema_structural` honors D-02 degrade-on-unknown) | ✓ WIRED (with documented deviation) | Plan said use Draft202012Validator's `check_schema`; executor replaced with structural-only check after discovering `check_schema` rejects `{"type":"color"}` outright (would violate D-02). Documented in 17-02-SUMMARY. |
| `_codegen.py` | Filesystem | `shutil.rmtree(target, ignore_errors=True)` + `pathlib.Path.write_text` | ✓ WIRED | Scoped to `out_root / slug` only — does NOT touch other dirs. |
| `_tool_factory.py` | `ToolResponse` | `R = TypeVar("R", bound=ToolResponse)` (L43) | ✓ WIRED | TypeVar bound enforces type-safety at the seam. |
| `sdet/__init__.py` | `response.py` | `from mcp_test_framework.sdet.response import ToolResponse` | ✓ WIRED | Smoke import succeeds. |
| Generated `<slug>/__init__.py` | `_REGISTRY` consumer (`_tool_factory.tool`) | `_REGISTRY: dict[str, tuple[type, type[ToolResponse]]]` literal block | ✓ WIRED | Verified by behavioral run — generated `__init__.py` defines `_REGISTRY = {"ping": (PingParams, PingResponse), ...}`. Phase 18's `mcp_session` will populate `_REGISTRIES[slug]` from this. |

### Data-Flow Trace (Level 4)

Phase 17 emits source code rather than rendering runtime data. The data-flow path through this phase is:

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| `_codegen.generate(...)` | `counts` dict | Computed from emitted files | Yes | ✓ FLOWING — behavioral test shows `{"tools": 1, "degraded_fields": 0}` for a stub tool |
| CLI stdout digest | `server_name` / `slug` / `counts['tools']` / `counts['degraded_fields']` | `_codegen.generate` return value | Yes | ✓ FLOWING (verified via stub-server CLI test) |
| Generated `<tool>.py` files | `<Tool>Params` / `<Tool>Response` class definitions | `translate_tool(...)` output | Yes | ✓ FLOWING — confirmed by behavioral run + 70 unit tests + pyright gate |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| `gen-sdet-classes --help` returns 0, lists `--config` only | `uv run mcp-test-framework gen-sdet-classes --help` | Exit 0, output lists `--config` and "CODEGEN-01" / "fail-loud" verbiage; no `--output-dir`/`--force` mention | ✓ PASS |
| `gen-sdet-classes` with no config fails loud (SAFE-03) | `MCPTF_CONFIG_FILE= uv run --project ... mcp-test-framework gen-sdet-classes` (in dir with no config.yaml) | "no config file found: ./config.yaml" + "refuses to run without a config file" + next-step message | ✓ PASS |
| Public import surface works | `python -c "from mcp_test_framework.sdet import ToolResponse; from mcp_test_framework.sdet._tool_factory import tool, ToolWrapper; print('OK')"` | "OK" | ✓ PASS |
| `_codegen.generate` end-to-end against synthetic tool | Drive `generate(...)` with a single `Tool(name='ping', ...)` into tmp; inspect output | Writes `demo_server/ping.py` + `demo_server/__init__.py` with AUTOGENERATED header, `_REGISTRY`, `from .ping import PingParams, PingResponse`, `class PingParams(BaseModel)` w/ `model_config = ConfigDict(extra="forbid")`, `class PingResponse(ToolResponse)` stub | ✓ PASS |
| Full Phase 17 unit-test suite | `MCPTF_CONFIG_FILE=config.yaml uv run pytest tests/framework/unit/test_tool_response.py tests/framework/unit/test_codegen_walker.py tests/framework/unit/test_codegen_emitter.py tests/framework/unit/test_tool_factory.py tests/framework/unit/test_gen_sdet_classes_cli.py tests/framework/unit/test_codegen_typecheck.py` | 70 passed, 1 deselected, 0 failed | ✓ PASS |
| Pyright type-check on synthetic generated dir | `test_synthetic_generated_passes_pyright_strict` (drives generate + invokes `python -m pyright`) | Exit 0; negative-coverage test confirms gate is real (deliberately broken file fails pyright) | ✓ PASS |
| Project-level `uv run pyright` (covers `[tool.pyright]` scope incl. response.py) | `uv run pyright` | 1 error: `response.py:96:28 - Return type, "dict[Unknown, Unknown]", is partially unknown (reportUnknownVariableType)` under strict mode | ⚠️ WARNING (does NOT affect SC1 which is about generated files only; scoping `response.py` in `[tool.pyright].strict` was an executor decision in 17-05; flag for future tightening — see Anti-Patterns Found) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| CODEGEN-01 | 17-04, 17-05 | `mcp-test-framework gen-sdet-classes` introspects + writes generated/<slug>/ idempotently | ✓ SATISFIED | CLI command at `cli.py:881`; `_codegen.generate` wipe-and-write at `_codegen.py:423`; idempotence test passes |
| CODEGEN-02 | 17-02, 17-05 | `<Tool>Params` Pydantic models from `inputSchema` with required/optional/types/defaults | ✓ SATISFIED | `_codegen.translate_tool` + `_emit_field` + `_emit_params_body`; `test_codegen_walker.py` pins all paths; `extra="forbid"` per D-defaults |
| CODEGEN-03 | 17-02, 17-05 | `<Tool>Response` typed when `outputSchema` declared, stub when undeclared (D-06) | ✓ SATISFIED | `_emit_response_body` + `translate_tool` emit stub `class FooResponse(ToolResponse): pass` when `outputSchema=None`; typed body when declared. Test coverage in `test_codegen_walker.py` and `test_codegen_emitter.py` |
| CODEGEN-04 | 17-01, 17-05 | `ToolResponse` uniform `.raw`/`.data`/`.text`/`.is_error`; 4-rung `.data` fallback chain | ✓ SATISFIED | `response.py:36-101`; 9 unit tests pin every rung including ImageContent isinstance filter (Pitfall 7) |
| CODEGEN-05 | 17-03, 17-05 | Typed call wrapper: `tool("name").call(params) -> Response`; Pydantic validates params; parses response | ⚠️ PARTIAL (seam shipped; wire body deferred to Phase 18) | Seam shipped: `ToolWrapper` Generic class, `tool(name)` factory, registry slots populated by codegen; `Params(...)` Pydantic validation works today. Wire body (`.call()` runtime): raises NotImplementedError per documented Phase 17 → Phase 18 split. Deferred in this report (Step 9b). |
| CODEGEN-06 | 17-02, 17-05 | Generated files contain "do not hand-edit" header | ✓ SATISFIED | `HEADER_TEMPLATE` at `_codegen.py:43-48`; emitted on every generated file (per-tool + `__init__.py`); test `test_emitter_header_marker_on_every_file` pins it |

No orphaned requirements: REQUIREMENTS.md maps CODEGEN-01..06 to Phase 17, and Plans 17-01..05 collectively claim all 6 in their `requirements:` frontmatter fields.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `pyproject.toml` | 76-86 | `[tool.pyright].strict` includes `src/mcp_test_framework/sdet/response.py` and `uv run pyright` surfaces 1 error there (`dict[Unknown, Unknown]` partial-unknown on `.data` return) | ℹ️ Info | The project-config strict-mode error does NOT affect SC1 (which targets generated files only) and does NOT affect the `test_codegen_typecheck` gate (which invokes pyright on tmp_path dirs that don't pick up the strict-scope config). Operators running `uv run pyright` against the full project will see this. Trivial fix: annotate the loop-local `parsed: dict[str, Any] | None = None` or cast at the `return parsed` site. Recommend tracking as a v1.4 cleanup or quick-fix follow-up. |
| `cli.py` | 1022-1051 | Class-level monkey-patch of `ClientSession.initialize` to capture InitializeResult | ℹ️ Info (documented deviation) | Executor-applied deviation from plan; the plan said use `client._session._initialize_result.serverInfo`, but mcp 1.27.0 doesn't cache the result on the session. Monkey-patch is scoped to a single `async with` block, reverted in `finally`, and documented in 17-04-SUMMARY. Acceptable; flagged for visibility because monkey-patching SDK classes can mask SDK upgrades. |
| `_codegen.py` | 28-38 | `Draft202012Validator` + `validator_for` imported but only referenced via `_ = (...)` no-op | ℹ️ Info (documented deviation) | Executor-applied deviation from plan; replaced full metaschema preflight with structural-only check to honor D-02 ("degrade unknown types, not refuse"). Imports retained as documentation anchors. Acceptable; could be cleaned up by removing the imports + `_ =` line, but harmless. |
| `tests/framework/test_tool_config.py` + several other v1.2 tests | various | 10 pre-existing test failures at the Phase 17 base commit (`d3a4c64`) — working-directory/path-resolution bugs | ℹ️ Info (pre-existing, out of scope) | Verified pre-existing per `deferred-items.md`. Not Phase 17 regressions. |
| `src/mcp_test_framework/fixtures.py` `_session_needs_preflight` | (pre-existing) | Checks `tests/unit/` but tests live under `tests/framework/unit/` — causes silent preflight bypass; pytest invocations need `MCPTF_CONFIG_FILE=config.yaml` workaround | ⚠️ Warning (pre-existing baseline bug) | Documented in 17-01-SUMMARY and 17-04 deferred-items. NOT caused by Phase 17. Recommend separate triage ticket. |

### Human Verification Required

1. **Live homelab-mcp gen-sdet-classes end-to-end** — Operator runs `mcp-test-framework gen-sdet-classes` with a real config.yaml pointing at uvx-launched homelab-mcp, then inspects `src/mcp_test_framework/sdet/generated/homelab_mcp/` and re-runs to confirm byte-identical output (modulo timestamp).
   - Expected: One file per tool (~70 tools), `__init__.py` with `_REGISTRY` populated, stdout digest shows server name + version + slug + tool count + degraded count, second invocation produces identical output (except for the Regenerated timestamp line).
   - Why human: Phase 17 SC1/SC2 explicitly target the operator workflow against a live server. Unit + stub-server tests cover the mechanism; only a live run proves the canonical 70-tool real-world scale, exercises the schema-coverage matrix homelab-mcp actually surfaces (enum, oneOf, nested objects), and confirms the digest formatting at scale.

2. **Pyright on real generated homelab_mcp dir** — Run `uv run pyright src/mcp_test_framework/sdet/generated/homelab_mcp/` after the live codegen run.
   - Expected: Exit 0 — every Params/Response class type-checks clean in strict mode, including `typing.Any` degradations.
   - Why human: The automated `test_codegen_typecheck` gate uses a 5-tool synthetic fixture (deterministic by design); real homelab-mcp schemas surface combinations the synthetic doesn't model. SC1 promises pyright-clean against the real generated dir.

### Gaps Summary

No blocking gaps. One ROADMAP success criterion (SC3 wire-side: `tool("name").call(params)` returning a typed response object) is deferred to Phase 18 by explicit phase-scope design — documented in Phase 17 CONTEXT.md OOS list, 17-03-PLAN frontmatter, and the runtime stub in `_tool_factory.py:70-82` with a Phase-18-citing NotImplementedError. The Pydantic-validation half of SC3 IS satisfied today via the generated `<Tool>Params` Pydantic classes with `extra="forbid"`. Two human-verification items remain to confirm SC1 against the live homelab-mcp at canonical scale (~70 tools).

Two minor anti-patterns were identified that don't block goal achievement: a project-config `[tool.pyright]` scope that includes `response.py` (1 strict-mode error visible only via `uv run pyright` with no args; doesn't affect the test gate or SC1) and a pre-existing baseline bug in `fixtures.py:_session_needs_preflight` that requires a `MCPTF_CONFIG_FILE=config.yaml` workaround for pytest invocations (NOT caused by Phase 17; documented in `deferred-items.md`).

---

_Verified: 2026-05-13T02:54:47Z_
_Verifier: Claude (gsd-verifier)_
