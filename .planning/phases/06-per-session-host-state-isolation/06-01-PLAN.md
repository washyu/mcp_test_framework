---
id: 06-01
phase: 06
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md
autonomous: true
requirements:
  - ISOL-01
tags:
  - isolation
  - recon
  - keyring
  - phase-06

must_haves:
  truths:
    - "ISOL-01 produces a recorded answer to the keyring-touch question (per ROADMAP success criterion #3)."
    - "Recon evidence is dual-source: PyPI README quotes + cmdkey before/after diff (per D-01)."
    - "Recon output is `.planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md` mirroring the 260506-qxs FINDINGS pattern (per D-02)."
    - "Recon is one-time investigation, not a permanent CI test; regression guard owned by ISOL-03 in Plan 06-03 (per D-03)."
    - "Decision is explicit: ship ISOL-04 in v1.1 OR defer with a written trigger condition (per D-04)."
    - "No homelab-mcp source code is read during this recon (black-box rule + D-01)."
  artifacts:
    - path: ".planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md"
      provides: "FINDINGS-shaped recon doc with PyPI README evidence (§1), cmdkey diff table (§2), ship-or-defer decision with trigger (§3), residual unknowns (§5)."
      contains: "## 1. PyPI README evidence"
      min_lines: 40
  key_links:
    - from: ".planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md"
      to: "ISOL-04 ship-or-defer call (consumed by Plan 06-02)"
      via: "explicit decision section §3 with 'SHIP' or 'DEFER' marker readable by grep"
      pattern: "(SHIP|DEFER):? ISOL-04"
---

<objective>
Run the gating ISOL-01 recon: determine whether `homelab-mcp`'s `list_keyring_credentials` and `list_registered_servers` tools touch the OS keyring at runtime. The output decides whether ISOL-04 (`PYTHON_KEYRING_BACKEND=keyring.backends.null.Null`) ships in Plan 06-02 or is documented as deferred with explicit re-open triggers.

Purpose: Block Plan 06-02 (env-isolation wiring) until the keyring question is answered with recorded evidence — per CD-04 and D-01 / D-04. ISOL-01 is one-time recon (D-03), not a permanent CI test; the regression guard is owned by ISOL-03's hash assertion in Plan 06-03.

Output: `.planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md` — FINDINGS-shaped doc mirroring the 260506-qxs spike template (D-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-per-session-host-state-isolation/06-CONTEXT.md
@.planning/phases/06-per-session-host-state-isolation/06-PATTERNS.md
@.planning/quick/260506-qxs-diagnostic-spike-identify-what-user-visi/FINDINGS.md
@.planning/quick/260506-qxs-diagnostic-spike-identify-what-user-visi/raw/keyring.txt
@CLAUDE.md

<interfaces>
<!-- This plan creates a documentation artifact, not code. The "interface" it produces is a -->
<!-- grep-readable decision marker that Plan 06-02 consumes. -->

The recon doc MUST contain a single explicit decision marker in §3, formatted exactly:
  - `SHIP: ISOL-04` — if PyPI README documents keyring touchpoints OR cmdkey diff shows mutation
  - `DEFER: ISOL-04` — if both evidence sources are clean

Plan 06-02 greps for this token to decide whether to plumb `PYTHON_KEYRING_BACKEND` into `_build_isolated_env`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture PyPI README evidence (no source-read)</name>
  <files>.planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md</files>
  <read_first>
    - .planning/quick/260506-qxs-diagnostic-spike-identify-what-user-visi/FINDINGS.md (section structure to mirror, per D-02)
    - .planning/phases/06-per-session-host-state-isolation/06-CONTEXT.md §"ISOL-01 — Keyring-touch investigation method" (D-01..D-04; especially D-02 for output path + D-03 for "not a permanent test")
    - .planning/quick/260506-qxs-diagnostic-spike-identify-what-user-visi/raw/keyring.txt (the ~20 cmdkey entries this recon is interrogating)
    - CLAUDE.md "Black box" architecture note (do NOT import or read homelab-mcp source)
  </read_first>
  <action>
    Create `.planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md` with the FINDINGS template front-matter and §1.

    Front-matter (mirror FINDINGS.md lines 1-6 exactly):
    ```
    # ISOL-01 Keyring-Touch Recon — homelab-mcp

    **Date:** 2026-05-07
    **Scope:** Determine whether `list_keyring_credentials` and `list_registered_servers` tools mutate (or read) Windows Credential Manager entries scoped to `*@homelab-mcp` / `*@homelab-mcp-proxmox` targets when invoked under the framework's stdio spawn.
    **Method:** PyPI README evidence (no source read per CLAUDE.md black-box rule + CONTEXT D-01) + before/after `cmdkey /list` diff. Mirrors the 260506-qxs FINDINGS.md template (CONTEXT D-02).
    ```

    §1 body — fetch the public PyPI README for `homelab-mcp` via WebFetch (URL: `https://pypi.org/project/homelab-mcp/`). Extract any documented keyring touchpoints. Quote literally; do NOT paraphrase. Required sub-sections:
    - "Documented keyring use:" — direct quotes from PyPI README mentioning keyring, credentials, Credential Manager, `keyring` library, `PYTHON_KEYRING_BACKEND`, or "stored credentials". If none found, state literally: "PyPI README contains no mention of keyring touchpoints (queried 2026-05-07)."
    - "Tool surface descriptions:" — README's description of `list_keyring_credentials` and `list_registered_servers` if present. Same literal-quote rule.

    Constraint: do NOT clone, install, or browse the `homelab-mcp` source repository. PyPI project page README only. If README on PyPI links to GitHub, do NOT follow the link to read source — only the README content rendered on PyPI is in scope.

    Confirm in the section a sentence: "No homelab-mcp source files were read during this recon (CLAUDE.md black-box rule)."
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f = '.planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md'; if (-not (Test-Path $f)) { exit 1 }; $c = Get-Content $f -Raw; if ($c -notmatch '## 1\. PyPI README evidence' -or $c -notmatch 'No homelab-mcp source files were read' -or $c -notmatch '\*\*Date:\*\* 2026-05-07' -or $c -notmatch '\*\*Method:\*\*') { exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the D-02 path: `test -f .planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md` returns 0.
    - Has FINDINGS-style header (D-02 mirroring 260506-qxs): grep finds `**Date:** 2026-05-07` and `**Method:**` lines.
    - Section 1 present: grep finds `## 1. PyPI README evidence`.
    - Black-box compliance: grep finds the literal sentence `No homelab-mcp source files were read during this recon`.
    - No homelab-mcp source path appears as a quoted file: `grep -E 'homelab[_-]mcp/(src|server|client|tools)/' 06-keyring-recon.md` returns no matches.
    - PyPI README quotes are clearly delimited (markdown blockquote `>` or fenced code block).
  </acceptance_criteria>
  <done>
    `.planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md` exists with header + §1 capturing PyPI README evidence (or its documented absence) under the no-source-read constraint.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run cmdkey before/after diff and write §2</name>
  <files>.planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md</files>
  <read_first>
    - .planning/quick/260506-qxs-diagnostic-spike-identify-what-user-visi/raw/keyring.txt (baseline of the ~20 cmdkey entries)
    - .planning/quick/260506-qxs-diagnostic-spike-identify-what-user-visi/FINDINGS.md §1 first table (column shape: `Target | Before | After | Delta` per CONTEXT.md PATTERNS evidence-table pattern)
    - tests/test_homelab_list_registered_servers.py (the test that drives the v1.1 tool surface — invocation we'll wrap in cmdkey snapshots)
    - .planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md (the file we just wrote in Task 1)
  </read_first>
  <action>
    Append §2 to `06-keyring-recon.md` with empirical evidence from a Windows Credential Manager before/after diff.

    Procedure (run in PowerShell, capture into raw/ subdir of the phase dir):

    1. Create raw evidence subdir: `mkdir -Force .planning/phases/06-per-session-host-state-isolation/raw`
    2. Capture BEFORE: `cmdkey /list | Select-String -Pattern 'homelab' > .planning/phases/06-per-session-host-state-isolation/raw/keyring-before.txt`
    3. Run the v1.1 tool surface end-to-end without isolation (the current behavior):
       `uv run pytest tests/test_homelab_list_registered_servers.py -v` (this is intentionally pre-isolation; we are MEASURING the bleed-through that Plan 06-02 will fix). If preflight fails (e.g., no Ollama), record that fact in §2 and use a minimal driver instead: `uv run mcp-test-framework list-tools` followed by a direct `uv run python -c "import asyncio; from mcp_test_framework.mcp_client import McpTestClient; asyncio.run((lambda: McpTestClient('homelab-mcp', [], 30).__aenter__())())"` — document whichever path was taken.
    4. Capture AFTER: `cmdkey /list | Select-String -Pattern 'homelab' > .planning/phases/06-per-session-host-state-isolation/raw/keyring-after.txt`
    5. Diff: `Compare-Object (Get-Content raw/keyring-before.txt) (Get-Content raw/keyring-after.txt) > raw/keyring-diff.txt`

    §2 content — append to the recon doc:
    - Heading: `## 2. cmdkey before/after diff`
    - Sub-section "Procedure:" — list the exact commands run (commands above, copy-pasteable).
    - Sub-section "Evidence files:" — bulleted links to `raw/keyring-before.txt`, `raw/keyring-after.txt`, `raw/keyring-diff.txt` (relative to the recon doc).
    - Sub-section "Diff result:" — a markdown table with columns `Target | Before | After | Delta` for each `homelab-mcp`-scoped entry; "Delta" is one of `unchanged | added | removed | modified`. If the diff is empty, the table can have a single "(all entries unchanged)" row plus a literal sentence "No keyring mutations observed across the test run."
    - Sub-section "Caveats:" — any preflight failures, any tool not exercised due to environment limits (e.g., judge unavailable), and the resulting confidence calibration (HIGH if `list_registered_servers` ran, MEDIUM/LOW otherwise).

    If `cmdkey` is unavailable (non-Windows host), document that constraint literally: "Recon performed on POSIX host; no Windows Credential Manager available. §2 evidence reduced to PyPI README only; ship-or-defer decision in §3 weights §1 accordingly." Do NOT fabricate diff data.

    Note (D-03): the §2 procedure is a one-time recon capture, NOT scaffolding for a permanent test. No pytest test file is added in this plan; the keyring-axis regression guard is downstream in Plan 06-03's ISOL-03 hash test.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f = '.planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md'; $c = Get-Content $f -Raw; if ($c -notmatch '## 2\. cmdkey before/after diff' -or $c -notmatch 'Procedure:' -or $c -notmatch 'Diff result:') { exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - §2 header present: grep finds `## 2. cmdkey before/after diff`.
    - Procedure subsection present with copy-pasteable commands: grep finds `Procedure:` AND `cmdkey /list` in the doc.
    - Diff outcome documented (empty or non-empty): grep finds `Diff result:`.
    - If on Windows: `test -f .planning/phases/06-per-session-host-state-isolation/raw/keyring-diff.txt` returns 0; if on POSIX, the doc literally states "Recon performed on POSIX host".
    - Caveats section present: grep finds `Caveats:`.
    - D-03 enforced: no `tests/test_keyring*.py` or similar file is created by this plan; `git status` after Tasks 1-2 shows only the recon doc + raw/ — no new pytest test files.
  </acceptance_criteria>
  <done>
    §2 of recon doc contains an empirical (or honestly-documented-as-absent) cmdkey diff with raw evidence files referenced; confidence level explicitly stated.
  </done>
</task>

<task type="auto">
  <name>Task 3: Write §3 ship-or-defer decision and §5 open questions</name>
  <files>.planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md</files>
  <read_first>
    - .planning/phases/06-per-session-host-state-isolation/06-CONTEXT.md §"ISOL-01" D-04 (ship-or-defer wording requirement)
    - .planning/quick/260506-qxs-diagnostic-spike-identify-what-user-visi/FINDINGS.md §3 last paragraph (decision-with-trigger pattern, per PATTERNS.md)
    - .planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md (the doc with §1 and §2 already written)
  </read_first>
  <action>
    Append §3 (decision) and §5 (open questions) to the recon doc.

    §3 — Decision: ship ISOL-04 in v1.1, or defer with trigger:

    Required structure:
    - Heading: `## 3. Decision: ship ISOL-04 in v1.1, or defer with trigger`
    - Subsection "Evidence summary:" — 2-3 bullets distilling §1 + §2 into yes/no on "does the framework's tool surface touch the keyring?"
    - Subsection "Decision:" — exactly ONE of these two literal lines as the FIRST line:
        - `**SHIP: ISOL-04**` (if either §1 or §2 produced positive evidence of keyring touchpoints)
        - `**DEFER: ISOL-04**` (if both §1 and §2 are clean)
      followed by a 1-2 sentence rationale citing the evidence.
    - Subsection "Trigger to re-open (only if DEFER):" — if deferred, list explicit conditions that would re-open ISOL-04. Required triggers per D-04:
        - "v1.x adds a tool whose name or description references credentials, secrets, or keyring."
        - "ISOL-03 verification test (Plan 06-03) starts failing on the keyring axis (e.g., new homelab-mcp release mutates Credential Manager)."
        - "Upstream homelab-mcp PyPI README adds a 'requires keyring backend' note."
      If SHIP, this subsection contains literally: "(N/A — shipping in v1.1.)"
    - Subsection "Plan 06-02 consequence:" — exactly one sentence stating what Plan 06-02 must do:
        - If SHIP: "Plan 06-02 MUST add `PYTHON_KEYRING_BACKEND=keyring.backends.null.Null` to the `_build_isolated_env` return dict."
        - If DEFER: "Plan 06-02 MUST add a comment in `_isolation.py` near the env-builder citing this recon's DEFER decision and the trigger conditions above."

    §5 — Open questions (append AFTER §3, no §4 — recon docs omit "Effort estimate" per PATTERNS.md):
    - Heading: `## 5. Open questions`
    - List residual unknowns. At minimum:
        - Q1: "Does `cmdkey` capture the same keyring entries that the Python `keyring` library would write/read?" (relevant if SHIPped — since `PYTHON_KEYRING_BACKEND=null` only short-circuits the Python `keyring` library, not direct Win32 `CredWrite` calls.)
        - Q2: Q3 carried forward from FINDINGS §5 — `MCP_CONNECTION_NONBLOCKING` semantics — note CD-03 disposition (passing through under `MCP_*` allowlist by default).
        - Any others surfaced during Tasks 1-2.

    Final sanity-check: read the whole file end-to-end. If `**SHIP: ISOL-04**` and `**DEFER: ISOL-04**` BOTH appear (or NEITHER), fail fast — fix to exactly one.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$f = '.planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md'; $c = Get-Content $f -Raw; $ship = ([regex]::Matches($c, '\*\*SHIP: ISOL-04\*\*')).Count; $defer = ([regex]::Matches($c, '\*\*DEFER: ISOL-04\*\*')).Count; if (($ship + $defer) -ne 1) { Write-Host \"Expected exactly one decision marker; got SHIP=$ship DEFER=$defer\"; exit 1 }; if ($c -notmatch '## 3\. Decision' -or $c -notmatch '## 5\. Open questions' -or $c -notmatch 'Plan 06-02 consequence:') { exit 1 }; exit 0"</automated>
  </verify>
  <acceptance_criteria>
    - §3 header present: grep finds `## 3. Decision: ship ISOL-04 in v1.1, or defer with trigger`.
    - Exactly one of `**SHIP: ISOL-04**` or `**DEFER: ISOL-04**` appears (verified by powershell count above).
    - "Plan 06-02 consequence:" subsection present and contains directive language: grep finds `Plan 06-02 consequence:`.
    - §5 present: grep finds `## 5. Open questions`.
    - No §4 (effort estimate omitted per PATTERNS.md): `grep -c '^## 4\.' 06-keyring-recon.md` returns 0.
  </acceptance_criteria>
  <done>
    Recon doc is complete with all four sections (§1, §2, §3, §5). Plan 06-02 can grep `SHIP|DEFER: ISOL-04` to determine its env-builder shape with no ambiguity.
  </done>
</task>

</tasks>

## Decisions Honored

This plan honors the following CONTEXT.md decisions (see `.planning/phases/06-per-session-host-state-isolation/06-CONTEXT.md`):

- **D-01:** Investigation method — PyPI README evidence (no source-read) + cmdkey before/after diff. Both pieces of evidence land in a FINDINGS-style document. (Tasks 1 + 2.)
- **D-02:** Recon output path is `.planning/phases/06-per-session-host-state-isolation/06-keyring-recon.md` mirroring the 260506-qxs FINDINGS pattern. (Task 1 — file path + FINDINGS-shaped header; `files_modified` frontmatter pins the path.)
- **D-03:** Recon is a one-time investigation, not a permanent CI test; the regression guard against keyring mutation is owned by ISOL-03's hash assertion in Plan 06-03. (Reflected in the objective and Task 2 acceptance criteria — no pytest test file is created in this plan.)
- **D-04:** Ship-or-defer call for ISOL-04 with explicit trigger conditions on the deferred path. (Task 3 — §3 decision marker + verbatim trigger list.)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| recon doc → Plan 06-02 grep | Plan 06-02 reads the SHIP/DEFER marker programmatically; ambiguity (both or neither marker) → wrong env-builder shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | recon doc decision marker | mitigate | Task 3 verifies exactly one of `**SHIP: ISOL-04**` / `**DEFER: ISOL-04**` appears (regex count == 1). LOW severity — manual edit error during phase work, no external attacker. |
| T-06-02 | Information Disclosure | cmdkey output captured to raw/ | accept | Output contains target hostnames + usernames already visible in user's keyring; no secrets exposed (cmdkey does not print passwords). LOW severity. |
| T-06-03 | Disclosure (open-source prep) | cmdkey raw evidence under .planning/ | accept | If repo is later open-sourced, raw/keyring-*.txt may need scrubbing — already tracked in STATE.md "Deferred Items" (open-source-prep AR-05-15). LOW severity, deferred per existing process. |
| T-06-04 | Spoofing | PyPI README content | accept | PyPI is a trusted distribution channel; README quotes are evidence-of-record. If a README is itself malicious, that's a supply-chain risk far broader than this recon. LOW severity. |
</threat_model>

<verification>
- Recon doc exists with all four required sections (§1, §2, §3, §5).
- Decision marker is unambiguous (regex count == 1).
- Black-box compliance grep clean (no homelab-mcp source paths quoted).
- §2 evidence files exist on Windows host (or are explicitly marked as absent on POSIX).
- `git diff --stat` shows only the recon doc + raw/ subdir as added.
</verification>

<success_criteria>
- ROADMAP success criterion #3 satisfied: "first task of the phase (ISOL-01) produces a recorded answer to 'does list_keyring_credentials / list_registered_servers touch the OS keyring?'"
- Decision is binary (SHIP or DEFER) and grep-readable for Plan 06-02 consumption.
- Black-box rule preserved: no homelab-mcp source read.
- D-01..D-04 all satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/06-per-session-host-state-isolation/06-01-SUMMARY.md` with:
- Decision marker outcome (SHIP or DEFER)
- §3 "Plan 06-02 consequence:" sentence verbatim (so Plan 06-02 doesn't have to re-grep)
- Any caveats from §2 that affect ISOL-03 test design in Plan 06-03
</output>
