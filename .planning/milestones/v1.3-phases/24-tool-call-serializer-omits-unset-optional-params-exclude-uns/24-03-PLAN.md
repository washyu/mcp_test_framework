---
phase: 24-tool-call-serializer-omits-unset-optional-params-exclude-uns
plan: 03
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - .planning/STATE.md
autonomous: true
requirements:
  - SERIALIZER-01
tags:
  - state-rescope
  - audit-trail
  - deferred-items
  - seed-022

must_haves:
  truths:
    - "`.planning/STATE.md` Deferred Items table records the framework-side resolution of the homelab-mcp inputSchema concern as a distinct `resolved` row (Row A), while preserving the upstream-bug residue as a separate `open` row (Row B) with the original Phase 19 deferred-at date."
    - "Row A appears immediately above Row B at the current L155 position; subsequent rows shift down by one (no line-anchor cross-references downstream of L155 exist)."
    - "Row A's resolution note explicitly cites SEED-022 by name and references memory `project_framework_primitives_sdet_safety_principle.md`."
    - "Row B's wording narrows scope to 'SDETs explicitly testing null-handling still trigger the upstream bug' — neither row erases the other's audit trail."
    - "The original L155 row is removed (its content has been split into Row A + Row B)."
    - "D-05: the existing `.planning/STATE.md` Deferred Items L155 row is split into two rows so the audit trail records Phase 24's framework-side resolution without erasing the upstream bug that still exists."
    - "D-06: Row A — **status: `Resolved`**, **deferred-at: `Phase 19 close (2026-05-13)` → resolved Phase 24 (2026-05-15)** — item text: 'Framework default behavior contribution to homelab-mcp inputSchema null trigger — `_tool_factory.py:99` `model_dump(mode='json')` emitted `null` for unset optional Pydantic fields, conflicting with upstream `type: 'string'` declarations.' Resolution note cites SEED-022 by name and references memory `project_framework_primitives_sdet_safety_principle.md`."
    - "D-07: Row B — **status: `Open`**, **deferred-at: `Phase 19 close (2026-05-13)`** (preserves original surfacing date) — item text: 'Upstream homelab-mcp inputSchema bug — Proxmox tools declare optional fields as `type: 'string'` (no `'null'`) but default them to `null`. SDETs explicitly testing null-handling hit `Input validation error: None is not of type 'string'`. Server should declare `type: ['string','null']` or strip null-valued keys before its own jsonschema check.' Category: `upstream-fix`."
    - "D-08: Row A appears immediately above Row B at the original L155 position; subsequent rows shift down by one (no line-anchor cross-references downstream of L155 exist)."
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Two new Deferred Items rows replacing the single L155 entry; Row A `resolved` + Row B `open` (upstream-fix); both inserted at the current L155 position."
      contains: "resolved Phase 24 (2026-05-15)"
  key_links:
    - from: ".planning/STATE.md Row A resolution note"
      to: "memory `project_framework_primitives_sdet_safety_principle.md`"
      via: "in-row text reference"
      pattern: "project_framework_primitives_sdet_safety_principle\\.md"
    - from: ".planning/STATE.md Row A"
      to: ".planning/STATE.md Row B"
      via: "table adjacency at the current L155 position"
      pattern: "Framework default behavior contribution to homelab-mcp inputSchema null trigger"
---

<objective>
Split the existing `.planning/STATE.md` Deferred Items row at L155 (the `homelab-mcp inputSchema` concern) into two rows so the audit trail records that Phase 24 resolved the framework-side half without erasing the upstream bug that still exists. The split is per D-05/D-06/D-07/D-08 of `24-CONTEXT.md` and inherits the existing `resolved-by-deletion` precedent at L179.

This plan is in Wave 2 because the `resolved` status claim on Row A requires Plan 24-01 to have shipped — the row is making a factual claim about the framework's current behavior, not a pre-announcement.

Purpose: clean audit trail. Phase 24 close records the framework-side resolution as a distinct row; the upstream bug stays open with narrower scope so the deferred-items timeline still reflects work that other people (homelab-mcp maintainers) need to do.

Output: STATE.md L155 split into two adjacent rows; one `resolved`, one `open`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-CONTEXT.md
@.planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split STATE.md Deferred Items L155 into Row A (resolved) + Row B (open)</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (read the full Deferred Items table, L148-L180; note row at L155 — the `homelab-mcp inputSchema for Proxmox tools` entry — that is being split; note also the L179 precedent `Resolved-by-deletion` which shows the established style for a resolved row with explanatory note)
    - .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-CONTEXT.md (D-05 split shape; D-06 Row A exact text + resolution note; D-07 Row B exact text + status; D-08 row ordering)
  </read_first>
  <acceptance_criteria>
    - `grep -c "Framework default behavior contribution to homelab-mcp inputSchema null trigger" .planning/STATE.md` returns exactly `1` (Row A).
    - `grep -c "Upstream homelab-mcp inputSchema bug — Proxmox tools" .planning/STATE.md` returns exactly `1` (Row B).
    - `grep -c "Framework deliberately does NOT mask this with .exclude_none=True" .planning/STATE.md` returns `0` (the original L155 row text is gone — it was split, not duplicated).
    - `grep -c "resolved Phase 24 (2026-05-15)" .planning/STATE.md` returns at least `1` (Row A deferred-at column).
    - `grep -c "SEED-022" .planning/STATE.md` returns at least `1` (Row A resolution note cites the principle by name).
    - `grep -c "project_framework_primitives_sdet_safety_principle\.md" .planning/STATE.md` returns at least `1` (Row A resolution note references the memory).
    - Row A appears IMMEDIATELY above Row B in the file. Verify with `grep -n "Framework default behavior contribution to homelab-mcp\|Upstream homelab-mcp inputSchema bug — Proxmox tools" .planning/STATE.md` — the two line numbers returned must differ by exactly `1`.
    - Total Deferred Items table row count is exactly one larger than before the change. If the prior count was 26 rows (counting `|----|` separator excluded), the new count is 27. Use: `awk '/^## Deferred Items/,/^## /' .planning/STATE.md | grep -c "^| .* | .* | .* | .* |$"` — confirm the count increased by exactly 1 vs. a pre-edit measurement.
  </acceptance_criteria>
  <action>
    Open `.planning/STATE.md`. Locate the existing row at L155 (the row whose `Item` cell begins `homelab-mcp inputSchema for Proxmox tools (and likely others) declares optional fields as type: "string"...`).

    **Delete that single row entirely.** Replace it with these two new rows (Row A directly above Row B), preserving the table-cell pipe structure used elsewhere in the table:

    ```markdown
    | upstream-fix | Framework default behavior contribution to homelab-mcp inputSchema null trigger — `_tool_factory.py:99` `model_dump(mode='json')` emitted `null` for unset optional Pydantic fields, conflicting with upstream `type: 'string'` declarations. Resolution: `exclude_unset=True` shipped in Phase 24; SDET-omitted optionals stay off the wire. SDETs explicitly passing `field=None` to test null-handling still trigger the upstream bug — that is SEED-022-by-design (see Row B and memory `project_framework_primitives_sdet_safety_principle.md`). | Resolved | Phase 19 close (2026-05-13) → resolved Phase 24 (2026-05-15) |
    | upstream-fix | Upstream homelab-mcp inputSchema bug — Proxmox tools (and likely others) declare optional fields as `type: 'string'` (no `'null'`) but default them to `null`. SDETs explicitly testing null-handling hit `Input validation error: None is not of type 'string'` (e.g. via `_CpuBumpManageVmParams(extra='allow')` pattern in `docs/SDET-AUTHORING.md`). Server should declare `type: ['string','null']` or strip null-valued keys before its own jsonschema check. | Open | Phase 19 close (2026-05-13) |
    ```

    **Important formatting notes:**
    - The Deferred Items table column order is: `| Category | Item | Status | Deferred At |`. The two new rows MUST match that order. (Confirm by reading the table header at L152-L153 of the current file before pasting.)
    - Row A's `Status` cell is the literal text `Resolved` (capital R; matches the L179 `Resolved-by-deletion` style — single-word status text, not lowercase). Use `Resolved` not `resolved`.
    - Row B's `Status` cell is `Open` (matches the rest of the table; e.g. L154 `Open`).
    - Row A's `Deferred At` cell shows BOTH the original surfacing date AND the resolution event in the arrow notation `Phase 19 close (2026-05-13) → resolved Phase 24 (2026-05-15)`. This matches the L179 precedent `v1.3 Phase 19 close → resolved Phase 20 (2026-05-13)`.
    - Row B's `Deferred At` cell shows ONLY the original surfacing date `Phase 19 close (2026-05-13)` so the timeline still reads correctly.

    Do not touch any other row in the Deferred Items table. Do not touch any other section of STATE.md (the `Next:` line at L35, the Roadmap Evolution entry at L130, performance metrics, etc. are managed by `state.record-session` and execute-phase — they will update naturally during normal close).
  </action>
  <verify>
    <automated>grep -c "Framework default behavior contribution to homelab-mcp inputSchema null trigger\|Upstream homelab-mcp inputSchema bug — Proxmox tools" .planning/STATE.md</automated>
  </verify>
  <done>L155 row split into adjacent Row A (`Resolved`) + Row B (`Open`); original combined row removed; SEED-022 + memory reference preserved in Row A's resolution note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 24 close → STATE.md audit trail | Hand-edited row split; no code execution involved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | T (Tampering) | STATE.md Deferred Items table | mitigate | Acceptance criteria use grep counts on specific phrases from D-06/D-07 verbatim text so a future auditor can spot a regression that dropped or rewrote either row. The L179 `Resolved-by-deletion` precedent shows this hand-edit pattern is the established workflow. |
| T-24-08 | R (Repudiation) | "Resolution claimed before fix shipped" | mitigate | Wave-2 ordering: this plan `depends_on: 24-01`. Row A's `resolved Phase 24 (2026-05-15)` claim is made only after Plan 24-01 ships the serializer line + tests; the Phase 24 SUMMARY records the green pytest tally that backs the claim. |

No new threat surface — STATE.md is a planning artifact; the row split clarifies the audit trail without changing executable behavior.
</threat_model>

<verification>
- `grep -c "Framework default behavior contribution to homelab-mcp inputSchema null trigger" .planning/STATE.md` returns `1`.
- `grep -c "Upstream homelab-mcp inputSchema bug — Proxmox tools" .planning/STATE.md` returns `1`.
- `grep -c "resolved Phase 24 (2026-05-15)" .planning/STATE.md` returns at least `1`.
- `grep -c "project_framework_primitives_sdet_safety_principle\.md" .planning/STATE.md` returns at least `1`.
- The two new rows are adjacent (line-number difference of 1).
</verification>

<success_criteria>
- L155 row split cleanly into two adjacent rows: Row A (`Resolved`, framework-side, Phase 24 close), Row B (`Open`, upstream-fix, original Phase 19 close).
- SEED-022 cited by name + memory file referenced in Row A's resolution note.
- No collateral damage elsewhere in STATE.md.
</success_criteria>

<output>
After completion, create `.planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-03-SUMMARY.md`. Include: (1) before/after line numbers for the affected rows, (2) confirmation that no other STATE.md section was touched (a `git diff --stat .planning/STATE.md` line + a `git diff .planning/STATE.md` excerpt showing only the row-split hunk).
</output>
