---
phase: 24-tool-call-serializer-omits-unset-optional-params-exclude-uns
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - docs/SDET-AUTHORING.md
  - README.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py
  - .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-readme-capture.txt
autonomous: false
requirements:
  - SERIALIZER-DOC-01
tags:
  - docs
  - readme-parity
  - sdet-authoring
  - seed-022

must_haves:
  truths:
    - "`docs/SDET-AUTHORING.md` §`## The inputSchema workaround` softens framing: the `_CpuBumpManageVmParams(extra='allow')` pattern is documented as needed ONLY when an SDET wants to test the server's null-handling path explicitly (not as a default per-Proxmox-call requirement)."
    - "The SEED-022 teaching content (Phase 21 D-06) is preserved — the `_CpuBumpManageVmParams(extra='allow')` code block + the 'framework doesn't paper over upstream bugs' rationale stay in the doc."
    - "`README.md` §`## SDET scenarios` re-captures the sample output as a PASS run (no more `✗ create_returns_pending_vm — failed on setup with ...` rows). The post-snapshot framing paragraph at L420-L427 is rewritten so the SEED-022 teaching shifts from 'the default path triggers the bug' to 'an SDET who explicitly tests null-handling triggers the bug'."
    - "The README §`## SDET scenarios` intro paragraph at L266-L272 no longer claims the snapshot is `shown here mid-failure` and no longer claims the framework's default `surfaces that contract bug as a real test failure instead of masking it`. The rewritten intro reflects post-Phase-24 reality: the framework's `exclude_unset=True` default keeps SDET-omitted optionals off the wire, and the upstream contradiction surfaces only when an SDET explicitly exercises null-handling."
    - "The README cross-link to `docs/SDET-AUTHORING.md` survives the rewrite — the intro-paragraph link (currently L271), the post-snapshot framing-paragraph link (currently L423), and the §`## Further reading` link (L496) all remain present."
    - "On the operator-checkpoint `regen-failed` branch, Task 1 (SERIALIZER-DOC-01 row) and Task 2 (SDET-AUTHORING soften) stay committed; the README re-capture becomes a Deferred Items row in `.planning/STATE.md`; the plan exits with verify status `partial` and a STATE.md cross-reference."
  artifacts:
    - path: "docs/SDET-AUTHORING.md"
      provides: "Softened §`## The inputSchema workaround` section with narrowed framing; `_CpuBumpManageVmParams(extra='allow')` code block + SEED-022 teaching preserved per Phase 21 D-06."
      contains: "_CpuBumpManageVmParams"
    - path: "README.md"
      provides: "Re-captured PASS-output sample in §`## SDET scenarios` + rewritten intro paragraph (L266-L272) + rewritten post-snapshot framing paragraph; cross-link to docs/SDET-AUTHORING.md preserved in at least 3 places."
      pattern: "Result: [0-9]+ PASS"
    - path: ".planning/REQUIREMENTS.md"
      provides: "New SERIALIZER-DOC-01 row added in the SERIALIZER requirement group; traceability + phase-coverage rows updated."
      contains: "SERIALIZER-DOC-01"
  key_links:
    - from: "README.md (intro paragraph at L266-L272)"
      to: "docs/SDET-AUTHORING.md"
      via: "markdown link `[`docs/SDET-AUTHORING.md`](docs/SDET-AUTHORING.md)` — un-anchored"
      pattern: "docs/SDET-AUTHORING\\.md"
    - from: "README.md (post-snapshot framing paragraph at L420-L427)"
      to: "docs/SDET-AUTHORING.md"
      via: "markdown link `[`docs/SDET-AUTHORING.md`](docs/SDET-AUTHORING.md)` — un-anchored"
      pattern: "docs/SDET-AUTHORING\\.md"
    - from: "README.md SDET sample block"
      to: "Phase 24 serializer fix in _tool_factory.py"
      via: "PASS-output snapshot — sample now demonstrates the framework's CORRECT default behavior, not the prior FAIL-due-to-framework-emitting-null"
      pattern: "Result: [0-9]+ PASS"
---

<objective>
Re-capture the README §`## SDET scenarios` sample as a PASS run (currently embeds a FAIL run per Plan 21-02 D-14 re-scope), rewrite BOTH the intro paragraph (L266-L272) that frames the snapshot as "shown here mid-failure" AND the post-snapshot framing paragraph (L420-L427) that frames the `✗` rows as "the framework doing its job," and soften the `docs/SDET-AUTHORING.md` §`## The inputSchema workaround` section so the `_CpuBumpManageVmParams(extra='allow')` pattern is framed as "needed only when you want to test null-handling explicitly" rather than "always needed for Proxmox calls." The SEED-022 teaching is preserved verbatim per Phase 21 D-06 — only the framing around the pattern shifts.

This plan is in Wave 2 because the README re-capture requires the Phase 24 serializer fix from Plan 24-01 to be LIVE so that the sample-scenario run produces a true PASS (not a stale-cached PASS or a contrived hand-edit).

Purpose: keep docs accurate after the serializer fix. The framework no longer contributes `null` for unset optionals by default, so the operator-facing PASS sample is now reachable on the same scenario file that Plan 21-02 ran red, AND the intro/framing prose that taught "the framework surfaces the upstream bug as a failure by default" must shift to teach "the framework's default keeps the bug from surfacing; only explicit-null-testing SDETs hit it."

Output: re-captured PASS sample + rewritten intro paragraph + rewritten post-snapshot framing paragraph + softened SDET-AUTHORING section + SERIALIZER-DOC-01 requirement row.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-CONTEXT.md
@.planning/phases/21-sdet-authoring-docs-readme-parity/21-CONTEXT.md
@README.md
@docs/SDET-AUTHORING.md
@.planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SERIALIZER-DOC-01 requirement row to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (read entire file; note that Plan 24-01 already added SERIALIZER-01 — this task adds a second row to the SAME group)
    - .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-CONTEXT.md (D-06 wording for SEED-022 teaching reference; <specifics> on cross-link invariant)
  </read_first>
  <acceptance_criteria>
    - `grep -c "^| SERIALIZER-DOC-01" .planning/REQUIREMENTS.md` returns `2` (group table + traceability table).
    - `grep -F "Total: 29 requirements mapped across 8 phases (17–24). Coverage: 29/29 (100%)." .planning/REQUIREMENTS.md` matches exactly once.
    - `grep -F "Total: 28 requirements" .planning/REQUIREMENTS.md` returns `0` matches.
    - Phase coverage summary row for Phase 24 now reads `| 24    | SERIALIZER-01, SERIALIZER-DOC-01                                            | 2     |` (or equivalent column spacing); `grep -F "SERIALIZER-01, SERIALIZER-DOC-01" .planning/REQUIREMENTS.md` matches at least once.
  </acceptance_criteria>
  <action>
    Open `.planning/REQUIREMENTS.md`. Inside the `### SERIALIZER — Tool-call wire serializer (Phase 24)` group that Plan 24-01 created, append a second row below `SERIALIZER-01`:

    ```markdown
    | SERIALIZER-DOC-01 | Docs and README sample track the Phase 24 serializer fix. `docs/SDET-AUTHORING.md` §`## The inputSchema workaround` is softened: the `_CpuBumpManageVmParams(extra="allow")` pattern is framed as needed ONLY when the SDET wants to test the server's null-handling path explicitly (not as a default per-Proxmox-call requirement). The SEED-022 teaching content (Phase 21 D-06) is preserved verbatim — the code block and the "framework doesn't paper over upstream bugs" rationale stay in the doc. `README.md` §`## SDET scenarios` re-captures the sample output as a PASS run (no `✗` rows); BOTH the intro paragraph (L266-L272) and the post-snapshot framing paragraph (L420-L427) are rewritten so the SEED-022 teaching shifts from "the default path triggers the bug" to "an SDET who explicitly tests null-handling triggers the bug." The README cross-links to `docs/SDET-AUTHORING.md` still resolve. |
    ```

    Then in the **Traceability Table** append a new row directly below the `SERIALIZER-01` row:

    ```markdown
    | SERIALIZER-DOC-01 | Phase 24 | Pending  |
    ```

    Then update the **Phase coverage summary** Phase 24 row from `| 24    | SERIALIZER-01                                                              | 1     |` to `| 24    | SERIALIZER-01, SERIALIZER-DOC-01                                            | 2     |`.

    Then update the rollup line from `**Total: 28 requirements mapped across 8 phases (17–24). Coverage: 28/28 (100%).**` to `**Total: 29 requirements mapped across 8 phases (17–24). Coverage: 29/29 (100%).**`.
  </action>
  <verify>
    <automated>grep -c "^| SERIALIZER-DOC-01" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>SERIALIZER-DOC-01 lives in the group table + traceability + coverage summary; rollup totals advance 28 → 29.</done>
</task>

<task type="auto">
  <name>Task 2: Soften docs/SDET-AUTHORING.md §`## The inputSchema workaround` framing (SEED-022 teaching preserved)</name>
  <files>docs/SDET-AUTHORING.md</files>
  <read_first>
    - docs/SDET-AUTHORING.md (L245-L310 — the current section)
    - .planning/phases/21-sdet-authoring-docs-readme-parity/21-CONTEXT.md (§D-06 — locks `_CpuBumpManageVmParams(extra="allow")` code block as a SEED-022 teaching moment; Phase 24 narrows framing only)
    - .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-CONTEXT.md (Claude's Discretion §1 — `_CpuBump*` pattern stays; framing narrows from "always needed for Proxmox tools" to "needed only when testing null-handling explicitly")
  </read_first>
  <acceptance_criteria>
    - `grep -F "_CpuBumpManageVmParams(extra=" docs/SDET-AUTHORING.md` matches at least once (code block preserved per Phase 21 D-06).
    - `grep -F "exclude_none=True" docs/SDET-AUTHORING.md` matches at least once AND the surrounding sentence still rejects it (the SEED-022 prohibition stays).
    - `grep -F "model_dump(mode=\"json\", exclude_unset=True)" docs/SDET-AUTHORING.md` matches at least once (the section now teaches that Phase 24 shipped the SEED-022-compatible variant, distinguishing it from the rejected `exclude_none=True`).
    - `awk '/^## The inputSchema workaround/,/^## /' docs/SDET-AUTHORING.md | grep -Fic "always needed"` returns `0` (i.e., the section no longer asserts the pattern is "always needed").
    - `grep -F "project_framework_primitives_sdet_safety_principle.md" docs/SDET-AUTHORING.md` matches at least once (memory reference preserved).
    - `grep -F "SEED-022" docs/SDET-AUTHORING.md` matches at least once.
    - The H2 header text `## The inputSchema workaround (and why the framework does not mask it)` is unchanged (so the long-form heading and the README's un-anchored link still pair sensibly).
  </acceptance_criteria>
  <action>
    Rewrite §`## The inputSchema workaround (and why the framework does not mask it)` in `docs/SDET-AUTHORING.md` (currently L252-L305). Approach: **soften in-place** (Claude's-Discretion option-A from CONTEXT.md). The H2 header text stays. The code block stays. The three numbered observations stay but observation #2 is reframed.

    **New section body** (replaces the current L253-L305 body verbatim, keeping the H2 header at L252):

    ```markdown
    Some upstream MCP servers (homelab-mcp 1.7.0 included) declare an
    `inputSchema` that is self-contradictory: optional fields declared
    `type: "string"` (without `"null"`) AND defaulted to `null` in the same
    schema. The framework's Pydantic-generated `ManageProxmoxVmParams` is
    `extra="forbid"` (matching the declared schema), which prevents transporting
    an action payload like `{"type": "config", "cores": 2}` through the wire.

    **You generally won't hit this bug.** As of Phase 24 the framework's
    `tool().call()` serializer uses `model_dump(mode="json", exclude_unset=True)`,
    which means optional Pydantic fields that you never set on the params
    constructor stay off the wire entirely. The contradiction surfaces only
    when an SDET _explicitly_ chooses to put `null` on the wire — either by
    passing `field=None` to a constructor, or (for tools whose declared
    `extra="forbid"` blocks the action payload shape) by reaching for the
    `extra="allow"` escape hatch below.

    The escape hatch is a per-scenario subclass with `extra="allow"`:

    ```python
    from pydantic import ConfigDict
    from tests.sdet._generated.homelab_mcp import ManageProxmoxVmParams


    class _CpuBumpManageVmParams(ManageProxmoxVmParams):
        """Local override for the homelab-mcp inputSchema contradiction.

        Upstream homelab-mcp declares optional fields as type:string (without
        "null") but defaults them to null in the same schema -- self-contradictory.
        The base ManageProxmoxVmParams is extra="forbid" per the declared schema
        and rejects the action payload below; this subclass relaxes that one
        constraint without leaking outside this scenario.
        """
        model_config = ConfigDict(extra="allow")
    ```

    Three observations on this pattern:

    1. **Upstream is the bug.** The server should either declare
       `type: ["string", "null"]` for the optional fields, or strip null-valued
       keys from its inbound payload before its own jsonschema check. The
       framework correctly reflects the server's declared `extra="forbid"` shape
       — the workaround belongs to the scenario, not to the framework.

    2. **The framework does not paper over upstream bugs.** Phase 24 chose
       `model_dump(mode="json", exclude_unset=True)` deliberately over
       `exclude_none=True`. `exclude_unset=True` discriminates on **user
       intent** — did the SDET set this attribute on the params constructor? —
       not on **value**. An SDET who explicitly writes `field=None` still
       puts `null` on the wire and still triggers the upstream contradiction
       when one exists. `exclude_none=True` would silently drop _every_ null
       field regardless of whether the SDET wanted it on the wire, masking
       the upstream contradiction and also preventing SDETs from testing
       the server's null-handling edge cases when they want to. This is the
       SEED-022 principle: framework primitives; SDET owns safety. The
       framework wraps tool calls; the SDET decides which payloads to send.
       See the in-repo memory file
       `project_framework_primitives_sdet_safety_principle.md` for the full
       statement of the principle.

    3. **The escape hatch lives in the scenario file.** A `_CpuBump*` subclass
       in `tests/sdet/test_proxmox_vm_lifecycle.py` is local to the workaround
       it enables. The framework's role is to surface upstream contract
       violations as test failures; the SDET's role is to decide whether to
       work around an upstream bug, file it, or fail loudly. Both are valid
       choices, and both stay visible at the scenario boundary.
    ```

    Do NOT touch any other section of the file. Do NOT rename the H2 anchor.
  </action>
  <verify>
    <automated>grep -F "exclude_unset=True" docs/SDET-AUTHORING.md</automated>
  </verify>
  <done>Section softened in-place; SEED-022 teaching + `_CpuBump*` code block + memory reference preserved; Phase 24 framing change explained in observation #2.</done>
</task>

<task type="auto">
  <name>Task 3a: Resurrect sample test and capture live PASS-mode stdout to temp file</name>
  <files>tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py, .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-readme-capture.txt</files>
  <read_first>
    - .planning/phases/21-sdet-authoring-docs-readme-parity/21-CONTEXT.md (§D-14 — manual-snapshot precedent: paste-once-with-comment-pointer; no regen script, no regression test)
    - .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-CONTEXT.md (Claude's Discretion §1 — re-capture mechanism options)
    - .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-01-SUMMARY.md (read AFTER Plan 24-01 runs; confirms serializer fix is live before re-capture)
    - README.md L276-L342 (canonical sample code to mirror if git recovery fails)
  </read_first>
  <acceptance_criteria>
    - `ls tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py` shows the file exists (resurrected from git history or hand-authored to mirror README L276-L342).
    - `ls .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-readme-capture.txt` shows the file exists.
    - `grep -E "Result: [0-9]+ PASS" .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-readme-capture.txt` matches at least once (a PASS-mode summary line was captured).
    - `grep -F "MCP Test Framework (SDET)" .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-readme-capture.txt` matches (the captured stdout is the SDET-mode digest header, not an unrelated capture).
    - On `regen-failed` escape (covered in Task 4): if the live stack cannot run the sample, this task exits without committing the temp capture file; Task 4 handles the partial-completion contract.
  </acceptance_criteria>
  <action>
    **Re-capture mechanism (planner's pick of Claude's-Discretion options):** use Phase 21 D-14's manual-snapshot precedent verbatim — temporarily resurrect `tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py` from git history (it was deleted in Plan 21-02 D-14), run it against the live homelab-mcp + Proxmox stack to produce a fresh PASS-mode output capture, and save the captured stdout to a temp file inside the phase directory. Pasting into README.md happens in Task 3b; cleanup (delete resurrected test + temp file) also happens in Task 3b.

    **Step-by-step:**

    1. Recover the deleted sample test file. The commit that deleted it is identifiable via `git log --all --diff-filter=D -- tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py | head -5`. Use `git show <commit>^:tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py > tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py` to recover the deleted file contents. If the file is not recoverable from git history, hand-author a 2-test module that mirrors the README sample exactly (README L276-L342): one `proxmox_vm_lifecycle` module-scope fixture with create+delete + two assertions (`test_create_returns_pending_vm`, `test_delete_returns_ok`).

    2. **Operator coordination point** — the operator must have a live homelab-mcp + Proxmox stack reachable with the right `MCPTF_DOGFOOD_PROXMOX_HOST` / `MCPTF_DOGFOOD_PROXMOX_NODE` env vars set. Claude prompts the operator to confirm the stack is reachable BEFORE invoking the runner; if the operator says "not reachable" or the runner subsequently fails for stack reasons, Claude proceeds to Task 4 with the `regen-failed` branch (do NOT delete the resurrected test file yet — Task 4 owns the cleanup decision per the partial-completion contract).

    3. Run the sample and capture stdout verbatim to a temp file inside the phase directory:

       ```
       uv run mcp-test-framework run --sdet -k proxmox_vm_lifecycle_readme_sample \
         > .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-readme-capture.txt 2>&1
       ```

       On PowerShell:

       ```
       uv run mcp-test-framework run --sdet -k proxmox_vm_lifecycle_readme_sample `
         *> .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-readme-capture.txt
       ```

       Verify the capture contains a `Result: <N> PASS / <M> FAIL / <K> SKIP` line where `<M>` is `0` and `<N>` is `2`. If `<M>` is non-zero, the serializer fix from Plan 24-01 is not live or the env is misconfigured — do NOT proceed to Task 3b; surface the failure to the operator at Task 4 as `regen-failed`.

    4. Do NOT modify README.md or delete the resurrected test in this task. Both happen in Task 3b only after the operator approves the captured stdout at Task 4 — except that Task 3b runs BEFORE Task 4 in the wave (see ordering note below).

    **Ordering note:** Task 3a → Task 3b → Task 4. Task 3b consumes the temp capture file and writes README.md; Task 4 is the operator's verification of the result. The temp capture file is the durable handoff between 3a and 3b — if 3b's verify fails for any reason, re-run 3b without re-running 3a (the live stack run is the expensive step).
  </action>
  <verify>
    <automated>grep -E "Result: [0-9]+ PASS" .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-readme-capture.txt</automated>
  </verify>
  <done>Sample test file resurrected; live `--sdet` run executed against Proxmox; stdout captured verbatim into the phase-directory temp file; PASS-mode summary line confirmed in the capture.</done>
</task>

<task type="auto">
  <name>Task 3b: Paste captured stdout into README; rewrite intro + framing paragraphs; cleanup resurrected test + temp file</name>
  <files>README.md, tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py, .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-readme-capture.txt</files>
  <read_first>
    - .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-readme-capture.txt (the captured stdout produced by Task 3a)
    - README.md L260-L432 (the full §`## SDET scenarios` block: intro paragraph L266-L272, sample code L276-L342, output snapshot L344-L418, post-snapshot framing paragraph L420-L427)
    - .planning/phases/21-sdet-authoring-docs-readme-parity/21-CONTEXT.md (§D-14 — manual-snapshot precedent constraints)
    - .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-CONTEXT.md (Claude's Discretion §1; cross-link invariant)
  </read_first>
  <acceptance_criteria>
    - `grep -E "Result: [0-9]+ PASS" README.md` matches at least once with the same digits/format that appear in the Task 3a capture file (i.e., the line pasted into README matches the captured line — see `<action>` step 4 for the variable-anchored substring check).
    - `grep -F "✗ create_returns_pending_vm — failed on setup" README.md` returns `0` (the previous FAIL rows are gone).
    - `grep -F "shown here mid-failure" README.md` returns `0` (the intro paragraph no longer claims mid-failure framing — BLOCKER #1).
    - `grep -F "surfaces that contract bug as a real test failure" README.md` returns `0` (the intro paragraph no longer claims the framework's default surfaces the upstream bug — BLOCKER #1).
    - `grep -F "two ✗ rows above are the framework doing its job" README.md` returns `0` (the post-snapshot framing paragraph is rewritten).
    - `grep -c "docs/SDET-AUTHORING.md" README.md` returns at least `3` — the intro-paragraph cross-link (~L271), the post-snapshot framing-paragraph cross-link (~L423), and the §`## Further reading` link (~L496) all remain present.
    - `grep -F "exclude_unset" README.md` matches at least once (the rewritten prose teaches the Phase 24 default).
    - `awk '/<!-- mirrors live runner output/,/```$/' README.md` shows the comment-pointer block still precedes the new sample (the Phase 21 D-14 manual-snapshot comment is preserved).
    - `ls tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py` returns "No such file or directory" (the resurrected test file is deleted).
    - `ls .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-readme-capture.txt` returns "No such file or directory" (the temp capture file is deleted post-paste).
  </acceptance_criteria>
  <action>
    Consume the temp capture file from Task 3a and apply the README rewrite. This task has four sub-steps: (a) paste the captured stdout into README.md replacing the FAIL snapshot, (b) rewrite the intro paragraph at L266-L272, (c) rewrite the post-snapshot framing paragraph at L420-L427, (d) cleanup the resurrected test file and temp capture file.

    **Sub-step (a) — paste captured stdout:**

    1. Read `.planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-readme-capture.txt` end-to-end. Identify the portion of the capture that corresponds to README.md L344-L417 (the section starting `========================================` / `MCP Test Framework (SDET)` and ending at the `Result: ...` summary line).

    2. Extract the exact `Result: ` line from the capture (e.g., `Result: 2 PASS / 0 FAIL / 58 SKIP  in 7.4s`). Record this verbatim — it becomes the substring that subsequent verify steps anchor to (per WARNING #2: do NOT hard-code `2 PASS / 0 FAIL / 58 SKIP`; use whatever the capture actually shows).

    3. Replace README.md L344-L417 (the current FAIL-output snapshot inside the triple-backtick `text` block) with the freshly-captured stdout from the temp file. The PASS-row format must match what the runner actually emits — do NOT hand-guess the `✓` / `✗` glyph or the "passed" wording; copy verbatim from the capture file. The skipped-tools list typically does NOT change between runs, but re-paste it verbatim from the captured stdout to be safe.

    **Sub-step (b) — rewrite intro paragraph L266-L272 (BLOCKER #1 fix):**

    4. Replace the current L266-L272 intro paragraph verbatim:

       FROM:
       ```
       Beyond the contract pass, SDETs author stateful scenarios under `tests/sdet/` --
       create resources, verify shape, tear down. Each scenario module renders as a
       per-tool group with nested rows under the same domain UI the contract pass
       uses. The sample below runs against a live Proxmox cluster (gated by
       `MCPTF_DOGFOOD_PROXMOX_HOST`) and is shown here mid-failure: the upstream
       `homelab-mcp` `manage_proxmox_vm`-family `inputSchema` reports `type: "string"`
       on optional fields and defaults them to `null` in the same schema -- the
       framework surfaces that contract bug as a real test failure instead of masking
       it (SEED-022). The `_CpuBumpManageVmParams(extra="allow")` workaround pattern
       is documented in [`docs/SDET-AUTHORING.md`](docs/SDET-AUTHORING.md). Run
       scenarios with `mcp-test-framework run --sdet`.
       ```

       TO (rewrite):
       ```
       Beyond the contract pass, SDETs author stateful scenarios under `tests/sdet/` --
       create resources, verify shape, tear down. Each scenario module renders as a
       per-tool group with nested rows under the same domain UI the contract pass
       uses. The sample below runs against a live Proxmox cluster (gated by
       `MCPTF_DOGFOOD_PROXMOX_HOST`) and passes because the framework's
       `exclude_unset=True` default (shipped in Phase 24) keeps optional fields the
       SDET never set off the wire. The upstream `homelab-mcp`
       `manage_proxmox_vm`-family `inputSchema` is still self-contradictory --
       `type: "string"` on optional fields defaulted to `null` in the same schema --
       but the framework no longer surfaces it on the default path. An SDET who
       _explicitly_ tests the server's null-handling (by passing `field=None` to a
       params constructor, or by reaching for the `_CpuBumpManageVmParams(extra="allow")`
       workaround documented in [`docs/SDET-AUTHORING.md`](docs/SDET-AUTHORING.md))
       still triggers the upstream contradiction -- that is SEED-022-by-design: the
       framework wraps tool calls; the SDET decides which payloads to send. Run
       scenarios with `mcp-test-framework run --sdet`.
       ```

       The cross-link `[`docs/SDET-AUTHORING.md`](docs/SDET-AUTHORING.md)` is preserved (un-anchored).

    **Sub-step (c) — rewrite post-snapshot framing paragraph L420-L427:**

    5. Replace the current L420-L427 paragraph verbatim:

       FROM:
       ```
       The two ✗ rows above are the framework doing its job: a real upstream
       contract bug surfaced as a failing test, with the `ToolCallError` reason
       quoted verbatim on the row. See
       [`docs/SDET-AUTHORING.md`](docs/SDET-AUTHORING.md) for the full authoring
       walkthrough -- module-scope fixtures, cross-file ordering, the conditional
       skip recipe for scenarios that require live infrastructure, and the
       `_CpuBumpManageVmParams(extra='allow')` workaround for the inputSchema bug
       shown above.
       ```

       TO (rewrite):
       ```
       The sample above passes because as of Phase 24 the framework's
       `tool().call()` serializer uses `model_dump(mode="json", exclude_unset=True)`:
       optional Pydantic fields the SDET never set stay off the wire, so the
       upstream homelab-mcp `inputSchema` contradiction (optional fields declared
       `type: "string"` but defaulted to `null`) is no longer triggered by the
       framework's default marshalling. An SDET who explicitly tests the server's
       null-handling path -- by writing `field=None` on a params constructor, or
       by reaching for a per-scenario `_CpuBumpManageVmParams(extra="allow")`
       subclass -- still puts `null` on the wire and still triggers the upstream
       bug when it exists; that is SEED-022-by-design: the framework wraps tool
       calls; the SDET decides which payloads to send. See
       [`docs/SDET-AUTHORING.md`](docs/SDET-AUTHORING.md) for the full authoring
       walkthrough -- module-scope fixtures, cross-file ordering, the conditional
       skip recipe for scenarios that require live infrastructure, and the
       `_CpuBumpManageVmParams(extra='allow')` workaround for the inputSchema bug
       above.
       ```

       The cross-link to `docs/SDET-AUTHORING.md` is preserved as an un-anchored
       markdown link.

    **Sub-step (d) — cleanup:**

    6. Delete the resurrected sample test file: `rm tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py` (or `Remove-Item` on PowerShell). Verify with `ls tests/sdet/` that the file is absent.

    7. Delete the temp capture file: `rm .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-readme-capture.txt` (or `Remove-Item` on PowerShell). The capture is no longer needed once it is pasted into README.md — git history of README.md is the durable artifact.

    8. Do NOT touch the existing comment-pointer `<!-- mirrors live runner output — re-run the framework and refresh this block when the operator-facing output format changes; see docs/SDET-AUTHORING.md for the SDET-mode digest contract -->` (currently at L274). The Phase 21 D-14 manual-snapshot pattern relies on it.

    **Variable-anchored verify (WARNING #2 fix):** the captured `Result: ` line extracted in step 2 is the source of truth — Task 5's operator-checkpoint verifies that the same line appears in README.md (not a hard-coded `2 PASS / 0 FAIL / 58 SKIP` string). The `grep -E "Result: [0-9]+ PASS"` acceptance criterion is a loose invariant; the byte-exact match is operator-verified at Task 5.
  </action>
  <verify>
    <automated>grep -E "Result: [0-9]+ PASS" README.md</automated>
  </verify>
  <done>Captured stdout pasted into README.md replacing FAIL snapshot; intro paragraph (L266-L272) rewritten to drop mid-failure framing; post-snapshot framing paragraph (L420-L427) rewritten to teach Phase 24 default + SEED-022 escape hatch; all three cross-links to docs/SDET-AUTHORING.md preserved; resurrected test file deleted; temp capture file deleted; comment-pointer preserved.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator verifies README sample re-capture against live stack</name>
  <what-built>
    A fresh PASS-output snapshot of `mcp-test-framework run --sdet -k proxmox_vm_lifecycle_readme_sample` against the live homelab-mcp + Proxmox stack, pasted verbatim into `README.md` §`## SDET scenarios` (Task 3a captured; Task 3b pasted). Intro paragraph (L266-L272) rewritten to drop the mid-failure framing. Post-snapshot framing paragraph rewritten to teach the Phase 24 `exclude_unset=True` default behavior and the SEED-022 escape hatch. Resurrected sample test file and temp capture file removed.
  </what-built>
  <how-to-verify>
    Operator action — Claude CANNOT do this autonomously (requires live Proxmox + homelab-mcp credentials + network access that lives only on the operator's machine):

    1. Confirm the resurrected sample test file is now deleted: `ls tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py` returns "No such file" (Claude deleted it in Task 3b sub-step 6).

    2. Confirm the temp capture file is now deleted: `ls .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-readme-capture.txt` returns "No such file" (Claude deleted it in Task 3b sub-step 7).

    3. Read README.md §`## SDET scenarios` end-to-end (L260-L432 in the pre-edit numbering). The output snapshot should now show PASS rows where the prior version showed `✗` FAIL rows. Spot-check the `Result: ` line — it should report `<N> PASS / 0 FAIL / <K> SKIP` for some `<N>` >= 2 and some `<K>`.

    4. Read the rewritten intro paragraph (~L266-L272). It should:
       - No longer claim the snapshot is "shown here mid-failure"
       - No longer claim the framework's default "surfaces that contract bug as a real test failure"
       - Teach the Phase 24 default behavior + SEED-022 escape hatch
       - Preserve the cross-link to `docs/SDET-AUTHORING.md`

    5. Read the rewritten post-snapshot framing paragraph (~L420-L427). It should:
       - No longer claim the `✗` rows are "the framework doing its job"
       - Teach the Phase 24 `exclude_unset=True` default
       - Preserve the cross-link to `docs/SDET-AUTHORING.md`

    6. (Optional but recommended) Re-run the sample manually against the live stack and eyeball-compare against the pasted block:
       - `MCPTF_DOGFOOD_PROXMOX_HOST=<host> uv run mcp-test-framework run --sdet -k proxmox_vm_lifecycle` (the real lifecycle test, not the deleted sample test)
       - Confirm the run completes without `Input validation error: None is not of type 'string'` failures on the default `tool().call(CreateProxmoxVmParams(...))` path.

    Type `approved` if all checks pass. Type `diverged: <description>` if the captured output diverges from the pasted block — Claude will re-run Task 3a + Task 3b. Type `regen-failed: <reason>` if the live stack cannot run the sample at all (e.g., Proxmox unreachable, homelab-mcp version mismatch, env vars unset) — see partial-completion contract below.

    **Partial-completion contract for `regen-failed` (WARNING #3 fix):**

    On `regen-failed`, Claude executes the following recovery sequence:

    1. **Keep committed:** Task 1 (`SERIALIZER-DOC-01` row in REQUIREMENTS.md) and Task 2 (`docs/SDET-AUTHORING.md` soften) stay committed. The docs framing is valid independent of the README re-capture — the section now reflects the Phase 24 default behavior regardless of whether the README sample has been re-captured.

    2. **Revert README:** if Task 3b had partially applied (some sub-step ran before the live stack failure was diagnosed), `git checkout README.md` to discard any in-flight README edits. The README remains in its pre-Plan-24-02 state (current FAIL-output snapshot + current intro + current post-snapshot framing).

    3. **Cleanup:** delete the resurrected `tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py` and the temp capture file `.planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-readme-capture.txt` if either still exists.

    4. **Log Deferred Item:** append a new row to the `.planning/STATE.md` Deferred Items table:

       ```markdown
       | README §`## SDET scenarios` PASS-sample re-capture | Phase 24 close (<YYYY-MM-DD>) | open | docs | Task 3a/3b of Plan 24-02 could not run against the live homelab-mcp + Proxmox stack at Phase 24 close (`regen-failed: <reason captured from operator>`). The README sample still shows the pre-Phase-24 FAIL output even though the framework default no longer triggers it. The intro paragraph (L266-L272) and post-snapshot framing paragraph (L420-L427) remain in their pre-Plan-24-02 state. Re-snapshot when the live stack is reachable — see Phase 21 D-14 precedent. |
       ```

    5. **Mark verify status `partial`:** in the Plan 24-02 SUMMARY (Task 5 output), record `verify_status: partial` with a cross-reference to the new STATE.md Deferred Items row. The plan exits successfully (Task 1 and Task 2 acceptance criteria all pass; Task 3a/3b acceptance criteria are documented as deferred) but with a documented gap.

    6. **Phase 24 closes with the partial:** Phase 24 marks complete when Plan 24-01, Plan 24-02 (partial), and Plan 24-03 all reach their terminal state. The Deferred Items row carries forward to the next phase's close-out review.
  </how-to-verify>
  <resume-signal>Type `approved`, `diverged: ...`, or `regen-failed: ...`</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README sample stdout → README.md committed text | Operator copies live-runner stdout into a tracked file via temp-capture intermediate; no automated regen script. |
| Resurrected sample test → live Proxmox API | One-off `--sdet` run against real infrastructure (operator-controlled, ephemeral test VM at `vmid=9990`). |
| Temp capture file in `.planning/` | Short-lived artifact between Task 3a and Task 3b; deleted in Task 3b sub-step 7. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | T (Tampering) | README sample copy/paste | accept | Manual-snapshot precedent (Phase 21 D-14); operator-eye verification at Task 4 checkpoint catches divergence. Variable-anchored verify (Task 3b sub-step 2 extracts the exact `Result: ` line) prevents accidental string-drift. Comment-pointer in README directs future operators to re-run. |
| T-24-05 | I (Information disclosure) | Captured stdout in README and in temp capture file | mitigate | Sample VMID `9990` is non-sensitive; Proxmox host is parameterized via env var and appears in stdout only as `MCP server: uvx homelab-mcp` (no host string leaks). Operator verifies at Task 4 that no PII / IP / credential text appears in the captured block before approving. Temp capture file is deleted in Task 3b sub-step 7 so it does not persist in the working tree. |
| T-24-06 | D (DoS) | Resurrected sample test against live Proxmox | accept | One-shot run, ephemeral VM at `vmid=9990`, fixture cleanup teardown deletes the VM. Cost is bounded; no recurring CI exposure because the resurrected test file is deleted in Task 3b sub-step 6. |

No new threat surface — the doc change is content-only. The temporary test resurrection mirrors the Phase 21 D-14 precedent which closed cleanly.
</threat_model>

<verification>
- `grep -E "Result: [0-9]+ PASS" README.md` matches (loose invariant per WARNING #2; exact match operator-verified at Task 4).
- `grep -F "exclude_unset" README.md` matches (rewritten prose teaches the Phase 24 default in at least one of the two paragraphs).
- `grep -F "two ✗ rows above are the framework doing its job" README.md` returns no matches.
- `grep -F "shown here mid-failure" README.md` returns no matches (BLOCKER #1).
- `grep -F "surfaces that contract bug as a real test failure" README.md` returns no matches (BLOCKER #1).
- `grep -c "docs/SDET-AUTHORING.md" README.md` returns at least `3` (intro link + framing link + Further-reading link).
- `grep -F "_CpuBumpManageVmParams(extra=" docs/SDET-AUTHORING.md` matches (Phase 21 D-06 lock honored).
- `grep -F "project_framework_primitives_sdet_safety_principle.md" docs/SDET-AUTHORING.md` matches.
- `ls tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py` returns "No such file" (resurrected test deleted).
- `ls .planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-readme-capture.txt` returns "No such file" (temp capture file deleted).
- `grep -c "^| SERIALIZER-DOC-01" .planning/REQUIREMENTS.md` returns `2`.
- Task 4 checkpoint returned `approved` OR returned `regen-failed: ...` with the partial-completion contract executed (Tasks 1+2 committed; STATE.md Deferred Items row appended; verify status `partial`).
</verification>

<success_criteria>
- SDET-AUTHORING.md section softened in-place; SEED-022 teaching + `_CpuBump*` code block preserved.
- README.md sample shows PASS output (re-captured against live stack via temp-capture intermediate); BOTH the intro paragraph and the post-snapshot framing paragraph rewritten to reflect post-Phase-24 reality.
- Resurrected sample test and temp capture file deleted; comment-pointer preserved.
- SERIALIZER-DOC-01 requirement row + traceability + coverage-summary updated.
- Operator approves at Task 4 human-verify checkpoint OR `regen-failed` partial-completion path executed cleanly (Tasks 1+2 committed, README untouched, STATE.md Deferred Items row appended).
</success_criteria>

<output>
After completion, create `.planning/phases/24-tool-call-serializer-omits-unset-optional-params-exclude-uns/24-02-SUMMARY.md`. Include: (1) the captured `Result: ` line from the live re-run (or "regen-failed: <reason>" if the partial-completion path was taken), (2) the operator's Task 4 approval text, (3) confirmation that the resurrected sample test file AND temp capture file were both deleted in Task 3b (or in the `regen-failed` recovery sequence), (4) if `regen-failed`: cross-reference to the new STATE.md Deferred Items row and `verify_status: partial`.
</output>
