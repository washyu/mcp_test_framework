---
phase: 22-scrub-requirement-id-leaks-from-src
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - src/mcp_test_framework/cli.py
autonomous: true
requirements:
  - SCRUB-SRC-01
must_haves:
  truths:
    - "Running `mcp-test-framework --help` shows zero requirement-ID tags"
    - "Running `mcp-test-framework run --help`, `list-tools --help`, `version --help`, `gen-sdet-classes --help`, `config-init --help` shows zero requirement-ID tags"
    - "Each `--help` description still tells the operator what the command does in plain prose"
    - "REQUIREMENTS.md contains a formal SCRUB-SRC-01 row and Phase 22 row in the coverage tables"
    - "D-01: cli.py has zero hits for all three planning-ID shapes (TAG-NN, D-NN, and Phase NN) after the scrub"
    - "D-02: every comment-site in cli.py is rewritten or dropped per the semantic-rewrite policy (per-site judgment), not a blanket regex sed"
    - "D-03: surviving anchors in cli.py comments cite only docs/*.md, README.md, CLAUDE.md, or docs/SDET-AUTHORING.md — never a .planning/ doc"
    - "D-04: no `# noqa: SCRUB-SRC-01` lines and no allowlist entries in cli.py — planning IDs are removed, not suppressed"
    - "D-06: Phase NN shape is scrubbed from cli.py in this plan as one-time discipline; the D-05 regression regex deliberately does not include Phase NN (operator-prose false-positive risk)"
    - "D-07: no .pyc / __pycache__ cleanup is needed — stale caches regenerate on the next pytest / uv run and inherit the scrubbed cli.py source"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Formalized SCRUB-SRC-01 requirement row + Phase 22 coverage entries"
      contains: "SCRUB-SRC-01"
    - path: "src/mcp_test_framework/cli.py"
      provides: "Operator CLI module with all planning-IDs removed from docstrings and comments"
  key_links:
    - from: "src/mcp_test_framework/cli.py"
      to: "docs/ERROR-STYLE.md"
      via: "operator-facing error references rewritten to cite the surviving doc anchor instead of SAFE-NN IDs"
      pattern: "ERROR-STYLE.md"
    - from: "src/mcp_test_framework/cli.py"
      to: "Typer --help"
      via: "function docstrings render directly through --help"
      pattern: '"""[A-Z]'
---

<objective>
Formalize the Phase 22 requirement row (SCRUB-SRC-01) in `.planning/REQUIREMENTS.md`, then scrub all planning-system requirement IDs (TAG-NN, D-NN) and `Phase NN` historical prefixes from `src/mcp_test_framework/cli.py` per the D-02 semantic-rewrite policy. This file is the highest-impact target because its Typer command docstrings render directly through `mcp-test-framework --help` and are operator-perceptible (Success Criterion 1 + 4).

Purpose: Operator-facing `--help` output stops leaking internal planning-system provenance. The 5 user-visible command docstrings (`run`, `list-tools`, `version`, `gen-sdet-classes`, `_emit_yaml_scaffold` for `config-init`) get operator-readable rewrites; internal comments get rewritten or dropped per the per-site judgment in D-02.

Output:
  - `.planning/REQUIREMENTS.md` with a new SCRUB row group containing SCRUB-SRC-01, an updated Traceability Table row, and an updated Phase coverage summary.
  - `src/mcp_test_framework/cli.py` with zero matches for the locked regex and zero `Phase NN` prefixes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-scrub-requirement-id-leaks-from-src/22-CONTEXT.md
@.planning/REQUIREMENTS.md
@docs/ERROR-STYLE.md
@README.md
@src/mcp_test_framework/cli.py

<interfaces>
<!-- Surviving doc anchors comments may cite (per 22-CONTEXT.md canonical_refs) -->
<!-- Drop any anchor pointing to .planning/ (PLAN.md / SUMMARY.md / REQUIREMENTS row). -->
<!-- Keep only anchors pointing to docs/*.md, README.md, CLAUDE.md, SDET-AUTHORING.md. -->

Surviving anchors and their domain:
- docs/ERROR-STYLE.md — operator error message style guide (replaces SAFE-NN / PERSONA-03 anchor refs)
- docs/SDET-AUTHORING.md — SDET surface walkthrough (replaces SDET-NN / CODEGEN-NN / RELOC-NN anchor refs)
- README.md — operator surface description (replaces CLI-NN / PERSONA-NN anchor refs)
- CLAUDE.md — repo guidance (replaces DOC-SDET-NN anchor refs)

Locked regression regex (from 22-CONTEXT.md D-05):
(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-\d+|\bD-\d+\b

Comment-rewrite policy (from 22-CONTEXT.md D-02):
- PURE-PROVENANCE comment (e.g. "# Phase 17 CODEGEN-05: tool(name) factory dispatches against this.")
  → DROP the line. The code names itself.
- RATIONALE-BEARING comment (e.g. "# Phase 13 D-08: LOCKED SAFE-06 message body, copied verbatim from ERROR-STYLE.md spec")
  → REWRITE to preserve the constraint and cite the surviving doc anchor (here: docs/ERROR-STYLE.md).
  Example shape (not exact wording — executor picks): "# Locked error-message body — see docs/ERROR-STYLE.md for the canonical text."

User-visible Typer docstrings in cli.py (must remain operator-readable; do not drop wholesale):
  - run            ~L460  — "Run the test suite ... wrapped around a subprocess pytest."
  - list-tools     ~L698  — "List tools exposed by the configured MCP server."
  - version        ~L938  — "Print the package version."
  - gen-sdet-classes ~L957 — "Generate typed Pydantic Params/Response classes for every tool."
  - _emit_yaml_scaffold ~L1320 — config-init scaffold helper docstring (CLEAN-05 / docs/ERROR-STYLE.md anchor)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Formalize SCRUB-SRC-01 row in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (read the full file — you need the existing group structure, Traceability Table, and Phase coverage summary so the new row blends in)
    - .planning/ROADMAP.md (Phase 22 entry has the locked Success Criteria 1–4; the SCRUB-SRC-01 description should derive directly from these)
    - .planning/phases/22-scrub-requirement-id-leaks-from-src/22-CONTEXT.md (D-04: hard zero, no allowlist; the requirement description must reflect this)
  </read_first>
  <action>
    Add a new `### SCRUB — src/ requirement-ID leak removal (Phase 22)` requirement group to `.planning/REQUIREMENTS.md`, immediately after the existing `### RELOC` group and before the `### UI` group (matches the chronological ordering already used in the file).

    Single row in the new group:

    | ID            | Description |
    | ------------- | ----------- |
    | SCRUB-SRC-01  | (one paragraph derived from ROADMAP Phase 22 Success Criteria 1–4: `mcp-test-framework --help` and every subcommand `--help` show zero matches for the locked regex; `grep -rE '(CLI\|PERSONA\|CODEGEN\|SAFE\|RUNNER\|UX\|ISOL\|JUNIT\|SURFACE\|TEST\|CLEAN\|DOC\|UI\|SDET\|STATE\|PREFLIGHT\|SCRUB\|RELOC)-\d+\|\bD-\d+\b' src/mcp_test_framework/` returns zero hits with no allowlist (per CONTEXT.md D-04); Phase 17–21.1 unit tests continue to pass (no behavior change); operator-facing `--help` prose remains at least as informative as the current ID-tagged version. The scrub also removes `Phase NN(.M)?` historical prefixes per CONTEXT.md D-01, though that shape is NOT in the regression regex (CONTEXT.md D-06).) |

    Update the Traceability Table — add one row:

    | SCRUB-SRC-01  | Phase 22 | Pending  |

    Update the Phase coverage summary table — add one row at the bottom:

    | 22    | SCRUB-SRC-01                                                                | 1     |

    Update the table-total line `Total: 26 requirements mapped across 6 phases (17–21.1). Coverage: 26/26 (100%).` to `Total: 27 requirements mapped across 7 phases (17–22). Coverage: 27/27 (100%).`

    DO NOT touch any other RELOC/UI/DOC row. The SCRUB block is purely additive.
  </action>
  <verify>
    <automated>grep -c "SCRUB-SRC-01" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^| SCRUB-SRC-01" .planning/REQUIREMENTS.md` >= 1 (the requirement-group row; traceability and coverage-summary rows add more)
    - `grep -c "SCRUB-SRC-01" .planning/REQUIREMENTS.md` returns 3 (group row + traceability row + coverage summary row)
    - `grep -c "Phase 22" .planning/REQUIREMENTS.md` >= 2 (traceability row + coverage summary row)
    - `grep "Total: 27 requirements" .planning/REQUIREMENTS.md` returns one matching line
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md has a formal SCRUB-SRC-01 row whose description ties directly to ROADMAP Phase 22 Success Criteria 1–4, the Traceability Table and Phase coverage summary table both include the new row, and the total-count line is updated to 27 reqs across 7 phases.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scrub cli.py — Typer docstrings + 24 ID hits + 37 Phase-NN prefixes</name>
  <files>src/mcp_test_framework/cli.py</files>
  <read_first>
    - src/mcp_test_framework/cli.py (read the whole file — you need to see every comment in context before deciding drop-vs-rewrite per D-02)
    - .planning/phases/22-scrub-requirement-id-leaks-from-src/22-CONTEXT.md (D-01 covers all three shapes; D-02 rewrite policy; D-03 surviving anchors; D-04 hard-zero; D-07 skip .pyc)
    - docs/ERROR-STYLE.md (this is the surviving anchor for the cluster of SAFE-03/SAFE-05/SAFE-06/PERSONA-03/CLEAN-05 references in cli.py — read it so any rewritten comment that needs an anchor points to a real heading or section)
    - README.md (surviving anchor for CLI-01/CLI-02/CLI-03 — operator-facing command surface descriptions)
  </read_first>
  <action>
    Apply the D-02 semantic-rewrite policy site-by-site to `src/mcp_test_framework/cli.py`. There are 24 hits matching the locked regex and 37 `Phase NN(.M)?` prefixes; many sites contain both (e.g. `# Phase 13 D-08: LOCKED SAFE-06 message body, copied verbatim from ERROR-STYLE.md spec`).

    Per-site decision tree (apply for every hit):

    1. **Is this a Typer command docstring rendered through `--help`?**
       (`run` ~L460, `list-tools` ~L698, `version` ~L938, `gen-sdet-classes` ~L957, `_emit_yaml_scaffold` ~L1320, plus the module docstring at L1)
       → REWRITE. The docstring stays — operators read it. Strip the `CLI-NN` / `CODEGEN-NN` / `PERSONA-NN` / `Phase NN` / `D-NN` tags from the prose. The remaining text must still tell the operator what the command does, what its key flags are, and what its exit-code behavior is. Operator-readable prose. NO planning-system vocabulary. Final shape is executor discretion (per CONTEXT.md `<decisions>` "Claude's Discretion" — "Per-site rewrite wording — executor reads each site, picks 'drop' or 'rewrite' per D-02") — preserve at minimum: command name, what it does, key flags it accepts, exit-code semantics where the current docstring documents them.

    2. **Is this a "pure-provenance" inline comment** (e.g. `# Phase 14 D-01: pytest runs as subprocess`, `# Phase 18 D-06: scenario-aware digest under --sdet`, `# D-11: raw mode -- no tempfile, no capture, no render, no discovery.`)?
       → DROP the line entirely. The surrounding code names itself.

    3. **Is this a "rationale-bearing" inline comment** (preserves a constraint the next maintainer would otherwise miss — e.g. the `_emit_yaml_scaffold` "Hand-format a complete self-contained YAML scaffold" docstring at L1320 referring to ERROR-STYLE.md, or the comment about preserving SAFE-05 env-var precedence at L295)?
       → REWRITE. Preserve the constraint in plain prose. Cite the surviving doc anchor (docs/ERROR-STYLE.md, README.md, docs/SDET-AUTHORING.md, CLAUDE.md) ONLY if the original comment pointed at one of those AND the anchor still adds load-bearing value for a maintainer. If the original anchor was a `.planning/` doc (PLAN.md / SUMMARY.md / REQUIREMENTS row), drop the anchor per D-03 — `.planning/` is not part of the shipped surface.

    4. **Is this a top-of-file module docstring history list** (the `Per Phase 5 CONTEXT.md decisions (revised in Phase 13 D-01/D-03):` block at L9, and similar history-style enumerations)?
       → REWRITE to describe the *current* behavior of the module, not the planning history. The module docstring is part of the shipped operator surface (`python -c "import mcp_test_framework.cli; help(...)"`). Keep load-bearing constraints (e.g. "pytest runs as a child subprocess via [sys.executable, '-m', 'pytest', ...]") but drop the `Phase NN D-NN` provenance.

    Three patterns the executor will encounter repeatedly:

    - `Phase 14 D-01/D-02/D-03/D-11/D-15: pytest runs as a child subprocess via [sys.executable, '-m', 'pytest', ...]; default mode allocates an internal tempfile JUnit XML; --raw skips the tempfile` (L462–466 region)
      → REWRITE: keep the technical statement of behavior, drop the `Phase 14 D-01/D-02/D-03/D-11/D-15:` prefix.
    - `# Phase 13 D-08: LOCKED SAFE-06 message body, copied verbatim from ERROR-STYLE.md spec` (~L142)
      → REWRITE: "# Locked error-message body — exact text lives in docs/ERROR-STYLE.md; keep this line in sync if you edit it."
    - `"""Generate typed Pydantic Params/Response classes for every tool (CODEGEN-01).` (~L957)
      → REWRITE: drop ` (CODEGEN-01)` only; the rest of the docstring stays.

    Hard rules:
    - NO `# noqa: SCRUB-SRC-01` lines. NO allowlist. NO planning IDs survive in cli.py (D-04).
    - SEED-022 is referenced by meaning, not by ID. If a comment currently says "per SEED-022, framework wraps tool calls only" (or similar), it stays as "the framework wraps tool calls and nothing else" — the principle survives, the ID does not. (cli.py likely has zero SEED-022 mentions; this is a precaution.)
    - PRESERVE all behavior. This is a pure documentation/comment edit. No code lines, no string literals used as identifiers, no JSON keys change. If a string literal happens to contain a planning ID (very unlikely in cli.py — would only be in error messages, and those are owned by docs/ERROR-STYLE.md, which already scrubbed planning IDs), leave the literal alone and flag it in the SUMMARY.
    - PRESERVE all imports and all function signatures.
    - Run `uv run python -c "from mcp_test_framework import cli; print(cli.app.info.help)"` after the rewrite to confirm the Typer app object still imports cleanly.

    After every site has been touched, run the locked regex against the file:
      `grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/cli.py`
    Must return `0`. If it returns anything else, find the missed hits and apply the per-site decision tree to each.

    Then run the Phase-NN check:
      `grep -cE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/cli.py`
    Must return `0`. The `Phase NN` shape is NOT in the regression regex (D-06) — but this phase scrubs it once and forever.
  </action>
  <verify>
    <automated>grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/cli.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/cli.py` returns `0`
    - `grep -cE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/cli.py` returns `0`
    - `uv run python -c "from mcp_test_framework import cli; print(type(cli.app).__name__)"` prints `Typer` (file is importable)
    - `uv run mcp-test-framework --help` exits 0 and prints the top-level help (operator surface preserved)
    - `uv run mcp-test-framework run --help` exits 0 and the rendered text contains the words "Run", "pytest", and "exit" (or equivalent — operator can tell what `run` does without the planning tags)
    - `uv run mcp-test-framework list-tools --help` exits 0 and the rendered text describes listing tools from the configured MCP server
    - `uv run mcp-test-framework gen-sdet-classes --help` exits 0 and the rendered text describes generating Params/Response classes
    - `uv run pytest tests/ -m "not live_homelab and not live_ollama" -q` exits 0 (no behavior regressions from the scrub)
  </acceptance_criteria>
  <done>
    cli.py contains zero matches for the locked regex, zero `Phase NN(.M)?` prefixes, imports cleanly, every Typer `--help` description renders operator-readable prose without planning tags, and the non-live test suite still passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → `--help` output | Operator reads `mcp-test-framework --help` and each subcommand `--help`. Output is read-only documentation; no input crosses this boundary. |
| maintainer → src/ comments | Future maintainer greps src/ for context. Comments are read-only documentation. |

## STRIDE Threat Register

This plan only edits Python comments, docstrings, and one Markdown file. No network surface changes, no IO surface changes, no auth surface changes, no executable behavior changes. The threat register reflects that:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | cli.py docstrings | accept | Scrub is the intentional change. Behavior preservation is verified by `uv run pytest tests/ -m "not live_homelab and not live_ollama"` exit 0. |
| T-22-02 | Information Disclosure | `--help` output | accept | The scrub *reduces* disclosure (removes internal planning-system IDs from operator-facing prose). Reverse of a typical info-disclosure threat — this is the desired outcome. |
| T-22-03 | Denial of Service | Typer command registration | mitigate | Verify the file imports cleanly post-edit (`uv run python -c "from mcp_test_framework import cli"`) and each `--help` exits 0. A malformed docstring cannot break Typer at import time, but a syntactically invalid Python edit would — the import check catches that. |

No novel attack surface is introduced. No new code paths, no new dependencies, no new IO, no new network calls.
</threat_model>

<verification>
After both tasks complete:

```bash
# Per-file gates
grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/cli.py  # → 0
grep -cE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/cli.py  # → 0

# REQUIREMENTS.md gates
grep -c "SCRUB-SRC-01" .planning/REQUIREMENTS.md  # → 3 (group row + traceability + coverage)
grep "Total: 27 requirements" .planning/REQUIREMENTS.md  # → 1 match

# Operator surface preserved
uv run mcp-test-framework --help  # exits 0, lists subcommands
uv run mcp-test-framework run --help  # exits 0, describes pytest wrapper + exit codes
uv run mcp-test-framework list-tools --help  # exits 0
uv run mcp-test-framework gen-sdet-classes --help  # exits 0
uv run mcp-test-framework version --help  # exits 0

# Behavior preservation
uv run pytest tests/ -m "not live_homelab and not live_ollama" -q  # exits 0
```
</verification>

<success_criteria>
- `cli.py` has zero requirement-ID leaks under the locked regex.
- `cli.py` has zero `Phase NN(.M)?` prefixes.
- All 5 Typer command `--help` outputs remain operator-readable (verb + behavior + key flags).
- `.planning/REQUIREMENTS.md` has the formal SCRUB-SRC-01 row, Traceability Table row, and Phase coverage summary row; total count is 27/27 across 7 phases.
- Non-live test suite (`tests/ -m "not live_homelab and not live_ollama"`) still exits 0 — no behavior regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/22-scrub-requirement-id-leaks-from-src/22-01-SUMMARY.md` summarizing: hit counts before/after, any non-trivial rewrite decisions (especially the 5 Typer command docstrings), any sites where the executor diverged from the default "drop pure-provenance, rewrite rationale-bearing" rule and why.
</output>
</content>
</invoke>