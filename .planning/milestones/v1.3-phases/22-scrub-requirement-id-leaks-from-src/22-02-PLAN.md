---
phase: 22-scrub-requirement-id-leaks-from-src
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_test_framework/sdet/__init__.py
  - src/mcp_test_framework/sdet/session.py
  - src/mcp_test_framework/sdet/_tool_factory.py
  - src/mcp_test_framework/sdet/response.py
  - src/mcp_test_framework/sdet/errors.py
  - src/mcp_test_framework/sdet/_codegen.py
  - src/mcp_test_framework/sdet/_slugs.py
autonomous: true
requirements:
  - SCRUB-SRC-01
must_haves:
  truths:
    - "Future maintainer grepping src/mcp_test_framework/sdet/ for planning IDs finds nothing"
    - "SDET module docstrings still describe what each module does and what its public seam exports"
    - "Behavior of mcp_session fixture, tool(name) factory, ToolResponse base, and ToolCallError is unchanged"
    - "D-01: every file under src/mcp_test_framework/sdet/ has zero hits for all three planning-ID shapes (TAG-NN, D-NN, and Phase NN) after the scrub"
    - "D-02: every comment-site in the SDET package is rewritten or dropped per the semantic-rewrite policy (per-site judgment), not a blanket regex sed — load-bearing invariants like the ToolCallError heuristic chain order survive as prose"
    - "D-03: surviving anchors in SDET package comments cite only docs/SDET-AUTHORING.md, CLAUDE.md, docs/*.md, or README.md — never a .planning/ doc"
    - "D-04: no `# noqa: SCRUB-SRC-01` lines and no allowlist entries anywhere under src/mcp_test_framework/sdet/ — planning IDs are removed, not suppressed"
    - "D-06: Phase NN shape is scrubbed from the SDET package in this plan as one-time discipline; the D-05 regression regex deliberately does not include Phase NN (operator-prose false-positive risk)"
    - "D-07: no .pyc / __pycache__ cleanup is needed under src/mcp_test_framework/sdet/ — stale caches regenerate on the next pytest / uv run and inherit the scrubbed source"
  artifacts:
    - path: "src/mcp_test_framework/sdet/__init__.py"
      provides: "Public SDET surface re-exports — docstring rewritten without planning IDs"
    - path: "src/mcp_test_framework/sdet/_tool_factory.py"
      provides: "tool(name) factory + ToolWrapper Generic — comments rewritten without planning IDs"
    - path: "src/mcp_test_framework/sdet/session.py"
      provides: "mcp_session fixture + registry activation — comments rewritten without planning IDs"
    - path: "src/mcp_test_framework/sdet/response.py"
      provides: "ToolResponse base class — comments rewritten without planning IDs"
    - path: "src/mcp_test_framework/sdet/errors.py"
      provides: "ToolCallError typed exception — comments rewritten without planning IDs"
    - path: "src/mcp_test_framework/sdet/_codegen.py"
      provides: "Codegen pipeline internals — comments rewritten without planning IDs"
    - path: "src/mcp_test_framework/sdet/_slugs.py"
      provides: "Server-slug derivation — comments rewritten without planning IDs"
  key_links:
    - from: "src/mcp_test_framework/sdet/*.py"
      to: "docs/SDET-AUTHORING.md"
      via: "rationale-bearing comments cite the surviving SDET authoring walkthrough instead of planning IDs"
      pattern: "SDET-AUTHORING.md"
---

<objective>
Scrub the SDET package (`src/mcp_test_framework/sdet/`) — 7 files, ~67 hits matching the locked regex, ~50 `Phase NN` prefixes. This is internal dev-facing source (no operator `--help` surface here), but the package is the public-import seam for SDETs (`from mcp_test_framework.sdet import mcp_session, tool, ToolCallError`) and its docstrings appear in IDE hovers and `help(...)` output. Rewrites preserve every constraint, drop every planning-system tag.

Purpose: Removes 67/310 of the in-scope ID hits and ~50/270 of the `Phase NN` prefixes; brings the most semantically dense subtree (codegen + session lifecycle + typed-error contract) into compliance with the locked regression regex.

Output: Seven files with zero matches for the locked regex and zero `Phase NN(.M)?` prefixes. Behavior unchanged. Public import surface (`mcp_test_framework.sdet.__init__`) unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-scrub-requirement-id-leaks-from-src/22-CONTEXT.md
@docs/SDET-AUTHORING.md
@CLAUDE.md
@src/mcp_test_framework/sdet/__init__.py
@src/mcp_test_framework/sdet/session.py
@src/mcp_test_framework/sdet/_tool_factory.py
@src/mcp_test_framework/sdet/response.py
@src/mcp_test_framework/sdet/errors.py
@src/mcp_test_framework/sdet/_codegen.py
@src/mcp_test_framework/sdet/_slugs.py

<interfaces>
Per-file hit counts (locked regex / Phase NN), pre-scrub baseline:
- __init__.py:      7 ID  /  6 Phase
- session.py:       8 ID  /  5 Phase
- _tool_factory.py: 11 ID / 21 Phase   (heaviest comment-density file in the package)
- response.py:      9 ID  /  4 Phase
- errors.py:        5 ID  /  2 Phase
- _codegen.py:     21 ID  /  7 Phase
- _slugs.py:        1 ID  /  2 Phase

Surviving doc anchors comments may cite (per 22-CONTEXT.md canonical_refs):
- docs/SDET-AUTHORING.md — SDET surface walkthrough; replaces SDET-NN / CODEGEN-NN / RELOC-NN anchor references
- CLAUDE.md — repo guidance; replaces dual-persona DOC-SDET-NN references

Surviving invariants that must be preserved in prose (drop the IDs, keep the rule):
- ToolResponse base exposes uniform .raw / .data / .text / .is_error regardless of outputSchema declaration
- .data fallback chain: structuredContent first, then JSON-parse of first TextContent, then text fallback
- mcp_session fixture's 5-step registry activation (read serverInfo, derive slug, locate generated package, load via spec_from_file_location, register)
- ToolCallError code/message strict heuristic chain (preserve the ordered fallback list; drop the D-08 anchor name)
- tool("name") factory dispatches against an active client slot (_ACTIVE_CLIENT) injected by mcp_session
- SEED-022 principle (framework wraps tool calls and nothing else; SDET decides what to call) — reference by meaning if a comment currently invokes it; never by the SEED-022 string itself

Comment-rewrite policy (D-02, same as plan 22-01):
- Pure-provenance comment, DROP the line.
- Rationale-bearing comment, REWRITE preserving the constraint, citing docs/SDET-AUTHORING.md or CLAUDE.md only if the original anchor was a docs/ file AND the anchor still adds value to a maintainer reading the comment.
- If the original anchor was a .planning/ doc, DROP the anchor (D-03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrub the three public-seam files — __init__.py, session.py, _tool_factory.py</name>
  <files>src/mcp_test_framework/sdet/__init__.py, src/mcp_test_framework/sdet/session.py, src/mcp_test_framework/sdet/_tool_factory.py</files>
  <read_first>
    - src/mcp_test_framework/sdet/__init__.py (the public re-exports module; its module docstring appears in `help(mcp_test_framework.sdet)`)
    - src/mcp_test_framework/sdet/session.py (the `mcp_session` fixture — load-bearing registry-activation behavior lives in comments here)
    - src/mcp_test_framework/sdet/_tool_factory.py (the `tool(name)` factory — heaviest comment density, 21 Phase NN prefixes)
    - .planning/phases/22-scrub-requirement-id-leaks-from-src/22-CONTEXT.md (D-02 rewrite policy; D-04 hard zero)
    - docs/SDET-AUTHORING.md (the surviving anchor — read sections "Quickstart", "Fixture surface", "Codegen workflow" so any rewritten comment that cites this doc points at a real section heading)
  </read_first>
  <action>
    Apply the D-02 per-site decision tree to all three files:

    1. Module docstrings (top of each file) — these appear in `help()`, in IDE hovers, in pyright/mypy tooltips when SDETs import the surface.
       - `__init__.py` currently has a chronological "Phase 17 ships X / Phase 18 adds Y / Phase 20 will add Z" enumeration; REWRITE to describe the current public surface (mcp_session, tool, ToolCallError, ToolResponse) without any "Phase N" framing or `(CODEGEN-04, D-07)` parenthetical tags. Final shape: a 3 to 6 line module docstring describing what the package exports and pointing at docs/SDET-AUTHORING.md for the authoring walkthrough.
       - `session.py` currently opens with `mcp_session fixture + registry activation -- Phase 18 SDET-03, Phase 21.1 RELOC-02.`; REWRITE: drop the trailing tag suffix; keep the 5-step activation behavior preserved in prose with no Phase / D-NN / SDET-NN / RELOC-NN tags.
       - `_tool_factory.py` currently opens with `tool(name) factory + ToolWrapper Generic -- the CODEGEN-05 seam.`; REWRITE: drop the trailing ` -- the CODEGEN-05 seam` segment; keep the behavior description (single surface, validate params via Pydantic against inputSchema, raise ToolCallError on isError=True).

    2. Inline pure-provenance comments — examples: `# Phase 14 D-04: --sdet SWAPS the operator-surface scope (NOT additive).`, `# Phase 18 SDET-03: active McpTestClient injected by the mcp_session fixture.`, `# Phase 21.1 RELOC-02: config-driven path, file-location loader, no sys.path mutation.`
       DROP the line entirely if the surrounding code already self-documents the behavior. The `_ACTIVE_CLIENT` module-level slot in `_tool_factory.py` already self-documents via its name; the `spec_from_file_location(...)` call in `session.py` is its own behavior signal.

    3. Inline rationale-bearing comments — example: the `_extract_code_message` heuristic chain comment in `errors.py` (`D-08 strict heuristic chain for code/message extraction, in order: ...`) is a load-bearing maintainer note; the ORDER of the chain is the constraint a regression test pins. REWRITE preserving the constraint. Drop the `D-08` anchor; keep the ordered list of fallback steps. If the comment can naturally cite docs/SDET-AUTHORING.md (e.g. the call-wrapper docstring), do so; otherwise just describe the rule.

    4. The `tool(name)` factory's `D-09` invariants enumeration in `_tool_factory.py` lines 4 through 8: REWRITE the whole module-docstring "Locked invariants" block to drop every `D-NN:` prefix while preserving the invariants themselves (stringly-typed surface, no attribute access; ToolWrapper Generic for IDE completion; etc.). The text after the colon is the constraint; that's what survives.

    5. The session.py "Phase 04.1 invariant" comment (~L27) — `Phase 04.1 invariant: the D-02 mutations are SYNC (module load via spec`; REWRITE to: `Invariant: the registry-activation mutations are SYNC (module load via spec_from_file_location is non-async, so no event-loop wiring needed inside the fixture body)`. Drop the `Phase 04.1` and `D-02` anchors; the technical statement survives.

    6. NO `# noqa: SCRUB-SRC-01` lines. NO allowlist. NO planning IDs survive in these three files (D-04). SEED-022 (framework primitives only; SDET decides what to call) is referenced by meaning if at all, never by ID.

    7. PRESERVE all behavior. No code lines, no string literals used as identifiers, no JSON keys change. No imports change. No function signatures change. After the rewrite, `uv run python -c "from mcp_test_framework.sdet import mcp_session, tool, ToolCallError; print('ok')"` must print `ok`.

    Per-file verification commands (run after the rewrite, all must return 0):
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/sdet/__init__.py
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/sdet/session.py
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/sdet/_tool_factory.py
      grep -cE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/sdet/__init__.py
      grep -cE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/sdet/session.py
      grep -cE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/sdet/_tool_factory.py
  </action>
  <verify>automated: bash -c 'set -e; for f in src/mcp_test_framework/sdet/__init__.py src/mcp_test_framework/sdet/session.py src/mcp_test_framework/sdet/_tool_factory.py; do n=$(grep -cE "(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b" "$f"); p=$(grep -cE "\bPhase [0-9]+(\.[0-9]+)?\b" "$f"); echo "$f ID=$n Phase=$p"; [ "$n" -eq 0 ] && [ "$p" -eq 0 ]; done'</verify>
  <acceptance_criteria>
    - For each of the three files, locked-regex match count returns 0
    - For each of the three files, `Phase NN` match count returns 0
    - `uv run python -c "from mcp_test_framework.sdet import mcp_session, tool, ToolCallError; print('ok')"` prints `ok`
    - `uv run pytest tests/framework/unit/test_sdet_fixtures.py tests/framework/unit/test_sdet_conftest_hook.py -q` exits 0 (or skips; no failure)
  </acceptance_criteria>
  <done>
    The three public-seam files contain zero matches for the locked regex and zero `Phase NN(.M)?` prefixes; the package's public import surface still resolves; SDET fixture and conftest unit tests still pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scrub the four internal-only files — response.py, errors.py, _codegen.py, _slugs.py</name>
  <files>src/mcp_test_framework/sdet/response.py, src/mcp_test_framework/sdet/errors.py, src/mcp_test_framework/sdet/_codegen.py, src/mcp_test_framework/sdet/_slugs.py</files>
  <read_first>
    - src/mcp_test_framework/sdet/response.py (ToolResponse base; CODEGEN-04 .data fallback chain is the load-bearing rationale)
    - src/mcp_test_framework/sdet/errors.py (ToolCallError; the heuristic chain order is the load-bearing rationale)
    - src/mcp_test_framework/sdet/_codegen.py (21 hits; the heaviest single-file count for ID matches; mostly internal pipeline rationale comments)
    - src/mcp_test_framework/sdet/_slugs.py (1 ID hit, 2 Phase hits; smallest file)
    - .planning/phases/22-scrub-requirement-id-leaks-from-src/22-CONTEXT.md (D-02 / D-03 / D-04)
    - docs/SDET-AUTHORING.md (surviving anchor for any CODEGEN-NN rewrites; read the codegen workflow section)
  </read_first>
  <action>
    Apply the same per-site D-02 decision tree as Task 1 to the four remaining files:

    1. response.py — the module docstring at L1 (`ToolResponse base -- uniform .raw / .data / .text / .is_error per CODEGEN-04.`) and the inline comments at L4 (`D-07`), L8 (`D-04: pyright is the static-type-check verifier`), L41 (`D-06: stub when undeclared`), L54, L74, L97 (`only try the FIRST TextContent (CODEGEN-04 spec)`).
       - L1 module docstring: REWRITE to describe the uniform surface in plain prose; drop the `per CODEGEN-04` tag. The four-property surface (.raw / .data / .text / .is_error) survives in the prose because that IS the public API.
       - L4 `D-07: Pydantic v2 BaseModel with @computed_field properties; lazy`: this is rationale-bearing (it explains the design choice). REWRITE: drop the `D-07:` prefix; keep the technical statement.
       - L8 `D-04: pyright is the static-type-check verifier; this module must be`: this is rationale-bearing. REWRITE: drop the `D-04:` prefix and the `per Phase 17 CODEGEN-04 D-04` trailing tag (L27); keep "this module must be pyright-strict-clean" as the constraint.
       - L41 `D-06: stub when undeclared`: rationale-bearing (the stub-vs-alias choice was load-bearing). REWRITE preserving the constraint.
       - L54 `(CODEGEN-04)` parenthetical: drop it; the comment text around it survives.
       - L74 `Best-effort dict view per CODEGEN-04 fallback chain`: REWRITE to `Best-effort dict view via the structuredContent → first-TextContent JSON → text-fallback chain.`
       - L97 `only try the FIRST TextContent (CODEGEN-04 spec)`: REWRITE to drop the `(CODEGEN-04 spec)` parenthetical; the constraint (only the first TextContent gets JSON-parsed) survives in the prose.

    2. errors.py — 5 ID hits, 2 Phase prefixes.
       - L1 module docstring `ToolCallError -- typed exception for MCP tool isError=True responses (UI-02).`: REWRITE to drop the ` (UI-02)` parenthetical.
       - L4 `D-07: plain Exception subclass (NOT a Pydantic model) for traceback-friendly`: REWRITE: drop the `D-07:` prefix; keep the constraint.
       - L9 `D-08: strict heuristic chain for code/message extraction, in order:` and L67 `D-08 heuristic chain. Returns (code, message).`: REWRITE to drop the `D-08` anchor and tag; PRESERVE the ordered list of heuristic steps verbatim, because the ORDER is what a regression test pins.
       - L27 `This module is pyright-strict-clean per Phase 17 CODEGEN-04 D-04.`: REWRITE to `This module is pyright-strict-clean.`

    3. _codegen.py — 21 ID hits, 7 Phase prefixes. This is the heaviest single file in the package for ID density; most comments will be either pure-provenance (drop) or rationale-bearing referencing CODEGEN-02 / CODEGEN-03 / CODEGEN-06 (rewrite citing docs/SDET-AUTHORING.md only if the cited anchor adds load-bearing value; otherwise drop the anchor and keep the constraint).
       - Apply the per-site decision tree mechanically. Read each comment, decide drop or rewrite, apply.
       - Do NOT change any code, any string literal that's a Python identifier, any JSON-Schema keyword. The codegen pipeline is unchanged.

    4. _slugs.py — 1 ID hit, 2 Phase prefixes. Smallest file. Likely all pure-provenance.
       - DROP all three planning-tag lines if they're pure-provenance; REWRITE if any one of them is rationale-bearing (slug derivation rule, deterministic across regen, etc.).

    Hard rules apply to all four files: no `# noqa` allowlist (D-04); no planning IDs survive; no behavior change; SEED-022 by meaning only.

    Per-file verification commands (run after rewrite, all must return 0):
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/sdet/response.py
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/sdet/errors.py
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/sdet/_codegen.py
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/sdet/_slugs.py
      grep -cE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/sdet/response.py
      grep -cE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/sdet/errors.py
      grep -cE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/sdet/_codegen.py
      grep -cE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/sdet/_slugs.py
  </action>
  <verify>automated: bash -c 'set -e; for f in src/mcp_test_framework/sdet/response.py src/mcp_test_framework/sdet/errors.py src/mcp_test_framework/sdet/_codegen.py src/mcp_test_framework/sdet/_slugs.py; do n=$(grep -cE "(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b" "$f"); p=$(grep -cE "\bPhase [0-9]+(\.[0-9]+)?\b" "$f"); echo "$f ID=$n Phase=$p"; [ "$n" -eq 0 ] && [ "$p" -eq 0 ]; done</verify>
  <acceptance_criteria>
    - For each of the four files, locked-regex match count returns 0
    - For each of the four files, `Phase NN` match count returns 0
    - `uv run python -c "from mcp_test_framework.sdet.response import ToolResponse; from mcp_test_framework.sdet.errors import ToolCallError; from mcp_test_framework.sdet import _codegen, _slugs; print('ok')"` prints `ok`
    - `uv run pytest tests/framework/unit/test_codegen_emitter.py tests/framework/unit/test_codegen_walker.py tests/framework/unit/test_codegen_integration_mock.py -q` exits 0 (codegen pipeline behavior preserved)
  </acceptance_criteria>
  <done>
    All four files contain zero matches for the locked regex and zero `Phase NN(.M)?` prefixes; imports resolve; the codegen unit-test suite still passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SDET → SDET package import | An SDET running `from mcp_test_framework.sdet import ...` reads the package's public surface. Comments and docstrings are read-only documentation. |
| maintainer → src/ comments | Future maintainer greps src/ for context. Comments are read-only documentation. |

## STRIDE Threat Register

This plan only edits Python comments and docstrings. No network surface, no IO surface, no auth surface, no executable behavior changes. Threat register reflects that:

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | Tampering | SDET package docstrings | accept | Scrub is the intentional change. Behavior preservation is verified by the codegen unit-test subset (`tests/framework/unit/test_codegen_*.py`, `tests/framework/unit/test_sdet_fixtures.py`, `tests/framework/unit/test_sdet_conftest_hook.py`). |
| T-22-05 | Information Disclosure | help() / IDE hovers | accept | The scrub *reduces* internal-vocabulary disclosure to SDET consumers. Desired outcome. |
| T-22-06 | Denial of Service | package import | mitigate | After each file's rewrite, run the import sanity command in the acceptance criteria — catches any accidental syntax error introduced by a misplaced quote or escape inside a docstring rewrite. |

No new attack surface introduced. No code paths, no new imports, no new IO, no new network.
</threat_model>

<verification>
After both tasks complete, run the per-subtree gate:

  grep -crE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/sdet/ --include="*.py"  # all files must return 0

  grep -crE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/sdet/ --include="*.py"  # all files must return 0

Behavior preservation:
  uv run pytest tests/framework/unit/test_sdet_fixtures.py tests/framework/unit/test_sdet_conftest_hook.py tests/framework/unit/test_codegen_emitter.py tests/framework/unit/test_codegen_walker.py tests/framework/unit/test_codegen_integration_mock.py tests/framework/unit/test_tool_factory.py -q  # exits 0

Public import:
  uv run python -c "from mcp_test_framework.sdet import mcp_session, tool, ToolCallError; from mcp_test_framework.sdet.response import ToolResponse; print('ok')"  # prints ok
</verification>

<success_criteria>
- All 7 files in `src/mcp_test_framework/sdet/` have zero matches for the locked regex.
- All 7 files have zero `Phase NN(.M)?` prefixes.
- Package public import surface (`from mcp_test_framework.sdet import mcp_session, tool, ToolCallError`) still resolves.
- SDET/codegen unit-test subset still passes.
</success_criteria>

<output>
After completion, create `.planning/phases/22-scrub-requirement-id-leaks-from-src/22-02-SUMMARY.md` summarizing: per-file hit counts before/after, any non-trivial rewrite decisions (especially the `_extract_code_message` heuristic-chain comment in errors.py and the module docstring rewrites in __init__.py and _tool_factory.py), any sites where the executor diverged from the default drop-vs-rewrite rule and why.
</output>
</content>
</invoke>