---
phase: 22-scrub-requirement-id-leaks-from-src
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mcp_test_framework/_runner.py
  - src/mcp_test_framework/_isolation.py
  - src/mcp_test_framework/fixtures.py
  - src/mcp_test_framework/config.py
  - src/mcp_test_framework/models.py
  - src/mcp_test_framework/schema_validator.py
  - src/mcp_test_framework/mcp_client.py
  - src/mcp_test_framework/judge_protocol.py
  - src/mcp_test_framework/ollama_judge.py
  - src/mcp_test_framework/rubrics.py
autonomous: true
requirements:
  - SCRUB-SRC-01
must_haves:
  truths:
    - "Future maintainer grepping the rest of src/mcp_test_framework/ for planning IDs finds nothing"
    - "Behavior of the runner (pytest subprocess + JUnit parser + renderer), the host-state isolation layer, the pytest fixtures, the config loader, the data models, the schema validator, the MCP client, and the Ollama judge is unchanged"
    - "Operator-facing error-message rendering through `_emit_operator_error_for_validation` still cites the canonical doc anchor (docs/ERROR-STYLE.md), not planning IDs"
    - "D-01: every file owned by this plan (_runner.py, _isolation.py, fixtures.py, config.py, models.py, schema_validator.py, mcp_client.py, judge_protocol.py, ollama_judge.py, rubrics.py) has zero hits for all three planning-ID shapes (TAG-NN, D-NN, and Phase NN) after the scrub"
    - "D-02: every comment-site in these 10 files is rewritten or dropped per the semantic-rewrite policy (per-site judgment), not a blanket regex sed — load-bearing invariants (locked skip-reason constants, isolation allowlist, Ollama temperature:0 + format:json, AsyncExitStack contract) survive as prose"
    - "D-03: surviving anchors in these 10 files cite only docs/ERROR-STYLE.md, README.md, CLAUDE.md, or docs/SDET-AUTHORING.md — never a .planning/ doc"
    - "D-04: no `# noqa: SCRUB-SRC-01` lines and no allowlist entries anywhere in this plan's 10 files — planning IDs are removed, not suppressed"
    - "D-06: Phase NN shape is scrubbed from these 10 files in this plan as one-time discipline; the D-05 regression regex deliberately does not include Phase NN (operator-prose false-positive risk in error messages, renderer output, and config docstrings)"
    - "D-07: no .pyc / __pycache__ cleanup is needed under these 10 files — stale caches regenerate on the next pytest / uv run and inherit the scrubbed source"
  artifacts:
    - path: "src/mcp_test_framework/_runner.py"
      provides: "Pytest subprocess + JUnit parser + renderer — comments rewritten without planning IDs (highest hit density in src/ — 76 ID + 75 Phase NN)"
    - path: "src/mcp_test_framework/_isolation.py"
      provides: "Per-session host-state isolation — comments rewritten without planning IDs"
    - path: "src/mcp_test_framework/fixtures.py"
      provides: "Session-scoped pytest fixtures — comments rewritten without planning IDs"
    - path: "src/mcp_test_framework/config.py"
      provides: "Pydantic config model + source-ordering — comments rewritten without planning IDs"
    - path: "src/mcp_test_framework/models.py"
      provides: "Pure-data Pydantic models (JudgeResult, ValidationIssue, etc.) — comments rewritten without planning IDs"
    - path: "src/mcp_test_framework/schema_validator.py"
      provides: "Deterministic JSON-Schema checks — comments rewritten without planning IDs"
    - path: "src/mcp_test_framework/mcp_client.py"
      provides: "Async stdio MCP client wrapper — comments rewritten without planning IDs"
    - path: "src/mcp_test_framework/judge_protocol.py"
      provides: "Judge protocol abstraction — comments rewritten without planning IDs"
    - path: "src/mcp_test_framework/ollama_judge.py"
      provides: "Ollama judge implementation — comments rewritten without planning IDs"
    - path: "src/mcp_test_framework/rubrics.py"
      provides: "Rubric definitions — comments rewritten without planning IDs"
  key_links:
    - from: "src/mcp_test_framework/_runner.py"
      to: "docs/ERROR-STYLE.md"
      via: "operator-tone error rendering rationale cited via doc anchor instead of SAFE-NN tag"
      pattern: "ERROR-STYLE.md"
    - from: "src/mcp_test_framework/config.py"
      to: "docs/ERROR-STYLE.md"
      via: "SAFE-03 / SAFE-05 / SAFE-06 missing-field/version-mismatch comments rewritten to cite docs/ERROR-STYLE.md"
      pattern: "ERROR-STYLE.md"
---

<objective>
Scrub the remaining 10 source files in `src/mcp_test_framework/` (everything outside `cli.py` and `sdet/`). This is the largest single plan by hit count — `_runner.py` alone has 76 ID hits + 75 `Phase NN` prefixes, the densest file in the project. The other 9 files range from 1 to 17 hits each. All edits are pure documentation / comment rewrites per D-02; no behavior changes.

Purpose: Removes the bulk of the remaining ID hits in `src/` so the final phase-wide grep gate can pass. After this plan, only `cli.py` and `sdet/*.py` (covered by plans 22-01 and 22-02) remain — and those are running in parallel under Wave 1.

Output: Ten files with zero matches for the locked regex and zero `Phase NN(.M)?` prefixes. Behavior unchanged. All existing tests (non-live subset) continue to pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-scrub-requirement-id-leaks-from-src/22-CONTEXT.md
@docs/ERROR-STYLE.md
@README.md
@CLAUDE.md
@src/mcp_test_framework/_runner.py
@src/mcp_test_framework/_isolation.py
@src/mcp_test_framework/fixtures.py
@src/mcp_test_framework/config.py
@src/mcp_test_framework/models.py
@src/mcp_test_framework/schema_validator.py
@src/mcp_test_framework/mcp_client.py
@src/mcp_test_framework/judge_protocol.py
@src/mcp_test_framework/ollama_judge.py
@src/mcp_test_framework/rubrics.py

<interfaces>
Per-file hit counts (locked regex / Phase NN), pre-scrub baseline:
- _runner.py:          76 ID / 75 Phase   (heaviest file in the project; mostly D-NN + Phase NN history in renderer comments)
- _isolation.py:       17 ID /  2 Phase   (ISOL-NN density)
- fixtures.py:         16 ID / 16 Phase   (TEST-NN + ISOL-NN + SAFE-NN)
- models.py:           16 ID /  6 Phase
- config.py:           15 ID / 10 Phase   (SAFE-NN + Phase 13 D-NN)
- ollama_judge.py:     14 ID /  8 Phase
- mcp_client.py:        9 ID / 12 Phase
- schema_validator.py:  7 ID /  3 Phase
- judge_protocol.py:    3 ID /  3 Phase
- rubrics.py:           2 ID /  1 Phase

Surviving doc anchors comments may cite (per 22-CONTEXT.md canonical_refs):
- docs/ERROR-STYLE.md — operator error-message style guide (replaces SAFE-NN / PERSONA-NN anchor refs; _runner.py, config.py, ollama_judge.py rationales)
- README.md — operator surface description (replaces CLI-NN / PERSONA-NN refs)
- CLAUDE.md — repo guidance

Surviving invariants that must be preserved in prose (drop the IDs, keep the rule):
- _runner.py: pytest runs as a child subprocess via `[sys.executable, '-m', 'pytest', ...]`; default mode allocates an internal tempfile JUnit XML; --raw skips the tempfile; --raw bypasses the renderer; exit-code mapping 0→0 / 1→1 / 2→2 / 5→0; pre-run RenderContext is built BEFORE subprocess so parser sees total_planned_cases=0 and rebuilds post-parse with parsed.total_cases for summary line.
- _isolation.py: isolation is ALWAYS-ON, no toggle; allowlist is exact (PATH, SYSTEMROOT, LANG, USERNAME, MCP_* + tempdir overrides); null keyring backend; sha256 of three registry files; each new pass-through is a hole in the isolation guarantee.
- fixtures.py: session-scoped mcp_client/judge/target_tool/config; tool_config per-test resolution; unknown tool names in `config.tools` → session-start fail-loud; isolation tempdir is single source of truth.
- config.py: source-ordering precedence (CLI > env > YAML > defaults); v2 schema only (`version: 2`); v1 raises so cli.py's operator-error mapper can render the canonical error-message body; SdetConfig required-no-default.
- models.py: JudgeResult (score 1–5, passed, reasoning, raw_response); ValidationIssue; defensive parsing patterns.
- schema_validator.py: Draft 2020-12 validator; iter_errors() returns the full ValidationIssue list per call (no early exit).
- mcp_client.py: stdio_client context manager (no raw subprocess.Popen); ClientSession lifecycle; asyncio.timeout around any operation that could hang; AsyncExitStack teardown contract.
- ollama_judge.py: /api/chat with stream:false and format:"json"; temperature 0; fallback to a failure result with the raw response on parse error.
- judge_protocol.py: Judge protocol abstraction (current implementation: OllamaJudge; future: pluggable backends are deferred to v1.4+).
- rubrics.py: rubric structures (1–5 rubric, pass threshold score>=4).

Comment-rewrite policy (D-02, same as plans 22-01 and 22-02):
- Pure-provenance comment, DROP the line.
- Rationale-bearing comment, REWRITE preserving the constraint, citing docs/ERROR-STYLE.md / README.md / CLAUDE.md only if the original anchor was a docs/ file AND the anchor still adds value to a maintainer reading the comment.
- If the original anchor was a .planning/ doc, DROP the anchor (D-03).
- No `# noqa` allowlist (D-04).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrub _runner.py — the densest file in src/ (76 ID + 75 Phase NN)</name>
  <files>src/mcp_test_framework/_runner.py</files>
  <read_first>
    - src/mcp_test_framework/_runner.py (read the whole file in one pass — the heaviest file; comments tend to cluster in three regions: top-of-file module docstring history, the `run_pytest_subprocess` function, and the renderer/parser internals)
    - .planning/phases/22-scrub-requirement-id-leaks-from-src/22-CONTEXT.md (D-02 / D-03 / D-04)
    - docs/ERROR-STYLE.md (the surviving anchor for operator-tone error rendering rationales)
  </read_first>
  <action>
    Apply the D-02 per-site decision tree to `src/mcp_test_framework/_runner.py`. This file is the largest scrub target by hit count (76 ID matches + 75 Phase NN prefixes); plan to spend most of the task budget here.

    The file has three rough comment clusters:

    1. Top-of-file module docstring (~L1 to L40) currently enumerates the Phase 14 D-01..D-16 + Phase 09 OUTPUT-01 contract; REWRITE to describe the *current* behavior of the runner module without the planning history. The technical contract survives in prose:
       - pytest runs as a child subprocess via `[sys.executable, '-m', 'pytest', ...]`
       - default mode allocates an internal tempfile JUnit XML
       - --raw skips the tempfile entirely
       - exit-code mapping (0→0, 1→1, 2→2, 5→0+warning)
       - pytest-without-tempfile error path
       - operator --junit-xml=PATH passthrough preservation
       - helpers were moved here from cli.py to avoid circular import

    2. Mid-file helpers and constants (~L40 to L350) — includes the `_compose_pre_run_skip_reasons` function, `_PYTEST_EXIT_*` constants, `run_pytest_subprocess`, `_extract_skip_reason_from_message`, the locked skip-reason constant strings, the pre-run RenderContext invariants. Many of these comments are rationale-bearing (e.g. "Pins D-12 locked skip-reason constants"); REWRITE preserving the constraint and dropping the anchor.

    3. Tail-of-file parser + renderer integration (~L350 to end) — `parse_junit_xml`, `_render_per_tool_rows`, `_render_summary`, `render_domain_ui`, debug-appendix rendering. Many pure-provenance comments here (`# Phase 14 D-13: --debug appends AFTER whatever the lower rung rendered.`, `# Phase 18 D-11: pass xml_path so the appendix can scan for ...`); DROP if pure-provenance, REWRITE if the comment preserves a rule a regression test pins.

    Specific high-density sites to handle:
    - The Phase 14 D-01..D-16 enumeration in the top docstring → rewrite as plain prose describing the runner contract.
    - Constant-pinning comments above `_PROXMOX_OPT_IN_SKIP_REASON` and the SAFE-01 skip strings (~L322) → REWRITE: "Locked skip-reason constants. Tests pin both verbatim in tests/framework/unit/test_runner_parser.py — keep in sync if you edit them."
    - The `# Phase 09 D-05: dedup + cap at 3 + ``... (N more)``` line at L365 → REWRITE: drop the `Phase 09 D-05:` prefix; the technical statement (dedup + cap at 3 + `... (N more)` suffix) survives.
    - The `Phase 14 D-15 / Phase 04.1 AsyncExitStack contract` reference at L185 → REWRITE: drop the planning prefix; keep "AsyncExitStack contract" as the constraint name.

    Hard rules:
    - NO `# noqa: SCRUB-SRC-01` lines. NO allowlist. NO planning IDs survive in _runner.py (D-04).
    - PRESERVE all behavior. No code lines, no string literals used as identifiers (especially the locked skip-reason strings), no JSON keys, no function signatures change.
    - SEED-022 is referenced by meaning, not by ID, if at all in this file.

    Verification command after the rewrite (run inside the task to self-check):
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/_runner.py  # must return 0
      grep -cE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/_runner.py                                                                          # must return 0
  </action>
  <verify>automated: bash -c 'n=$(grep -cE "(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b" src/mcp_test_framework/_runner.py); p=$(grep -cE "\bPhase [0-9]+(\.[0-9]+)?\b" src/mcp_test_framework/_runner.py); echo "ID=$n Phase=$p"; [ "$n" -eq 0 ] && [ "$p" -eq 0 ]'</verify>
  <acceptance_criteria>
    - `grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/_runner.py` returns 0
    - `grep -cE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/_runner.py` returns 0
    - `uv run python -c "from mcp_test_framework import _runner; print(type(_runner.run_pytest_subprocess).__name__)"` prints `function`
    - `uv run pytest tests/framework/unit/test_runner_parser.py tests/framework/unit/test_runner_explain.py tests/framework/unit/test_runner_pre_run_digest.py tests/framework/unit/test_runner_sdet_digest.py tests/framework/unit/test_runner_sdet_rows.py tests/framework/unit/test_runner_debug_appendix_d11.py tests/framework/unit/test_runner_encoding.py tests/framework/unit/test_runner_migration.py tests/framework/unit/test_runner_sdet_kwarg.py -q` exits 0 (runner behavior preserved; the locked skip-reason constants string-matched by `test_runner_parser.py` survive verbatim)
  </acceptance_criteria>
  <done>
    `_runner.py` contains zero matches for the locked regex and zero `Phase NN(.M)?` prefixes; imports cleanly; the full runner unit-test subset (9 files) still passes — proving the locked skip-reason constants and exit-code mapping are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Scrub _isolation.py + fixtures.py + config.py — host-state isolation, pytest fixtures, config loader</name>
  <files>src/mcp_test_framework/_isolation.py, src/mcp_test_framework/fixtures.py, src/mcp_test_framework/config.py</files>
  <read_first>
    - src/mcp_test_framework/_isolation.py (17 ID hits; ISOL-NN density; the allowlist constants and pass-through-allowlist rationale are load-bearing)
    - src/mcp_test_framework/fixtures.py (16 ID, 16 Phase; session-scoped fixtures; unknown-tool fail-loud rationale is load-bearing)
    - src/mcp_test_framework/config.py (15 ID, 10 Phase; source-ordering precedence + SAFE-NN error-mapper rationale is load-bearing)
    - .planning/phases/22-scrub-requirement-id-leaks-from-src/22-CONTEXT.md (D-02 / D-03 / D-04)
    - docs/ERROR-STYLE.md (surviving anchor for SAFE-03/SAFE-05/SAFE-06 rationales in config.py)
  </read_first>
  <action>
    Apply the D-02 per-site decision tree to all three files.

    Per-file specifics:

    1. `_isolation.py` (17 ID hits / 2 Phase prefixes) — the file opens with a "Phase 06 implementation of ISOL-02, ISOL-04, ISOL-05, ISOL-07" sentence. Many rationales here are LOAD-BEARING because the allowlist is the security boundary:
       - L1 module docstring header: REWRITE to describe what the module does (per-session host-state isolation: allowlisted env vars only, tempdir overrides, null keyring backend, sha256 of registry files for verification) without the Phase / ISOL tags.
       - L9 `D-05: Isolation is ALWAYS-ON. No toggle, no ``--no-isolation`` CLI escape hatch.`: REWRITE: drop the `D-05:` prefix; keep the constraint verbatim. The "always-on, no toggle" rule is the load-bearing security invariant.
       - L10 `D-06: NO IsolationConfig model, NO extra_env field, NO new public Config`: REWRITE: drop the `D-06:` prefix; the constraint (no config knobs for isolation) survives.
       - L13 `D-07: The allowlist is exact (PATH, SYSTEMROOT, LANG, USERNAME, MCP_*) plus`: REWRITE: drop the `D-07:` prefix; the allowlist enumeration survives. The allowlist itself is referenced in test fixtures; it MUST remain readable.
       - L24 `ISOL-04 wiring: Plan 06-01 recon (06-keyring-recon.md §3) recorded a SHIP`: REWRITE — the `Plan 06-01` and `06-keyring-recon.md` references are .planning/ artifacts; DROP them per D-03; keep the technical statement (null keyring backend is the SHIP decision).
       - L37 `(DOC-07 in Phase 10). Each new pass-through is a hole in the isolation guarantee`: REWRITE: drop the `DOC-07 in Phase 10` parenthetical; the warning ("each new pass-through is a hole in the isolation guarantee") is load-bearing.
       - L46 `# Module-level constants per D-07 (allowlist exact, no widening, no Config field).`: REWRITE: drop `per D-07`; the constraint (allowlist exact, no widening) survives.

    2. `fixtures.py` (16 ID / 16 Phase) — session-scoped fixtures with TEST-NN / ISOL-NN / SAFE-NN density.
       - L97 `CR-01 fix). SAFE-05 is preserved: env vars do NOT inject scalar`: REWRITE: drop `CR-01 fix). SAFE-05 is preserved:` — keep `env vars do NOT inject scalar values into the model; they only direct the YAML source path.` (this rule is pinned by test_config_example.py).
       - L161 `paid by TEST-05 within the locked 120s httpx.Timeout.`: REWRITE: drop the `TEST-05` tag; keep `paid within the locked 120s httpx.Timeout` if the 120s number is load-bearing (it is — pinned by ollama_judge tests).
       - L169-176 `Phase 21.1 RELOC-01 (Rule 3 deviation, plan 21.1-01): the config / live-MCP branch preserves the SAFE-03 behavior where it matters`: REWRITE — drop `Phase 21.1 RELOC-01 (Rule 3 deviation, plan 21.1-01)`; drop `the SAFE-03 behavior` and replace with `the missing-config fail-loud behavior described in docs/ERROR-STYLE.md`.
       - L297 `# Phase 08 D-14 / D-18: unknown tool names in config.tools -> session-start`: REWRITE: drop the planning prefix; keep `unknown tool names in config.tools trigger session-start fail-loud` as the rule.
       - L318 `# (Phase 06 ISOL-05; D-12 fixture shape, D-14 single source of truth, D-15 cleanup)`: DROP entirely if pure-provenance, or REWRITE to a brief "single source of truth for the isolation tempdir; cleanup at session exit" if the comment still adds context.
       - The remaining comments mostly enumerate fixture shape rules; apply the decision tree mechanically.

    3. `config.py` (15 ID / 10 Phase) — Pydantic config + source-ordering. Heavy SAFE-NN density because this is where operator-tone error mapping reads its rationale from.
       - L3 `Precedence (Phase 13 D-05):`: REWRITE to `Precedence:` (drop the `Phase 13 D-05` anchor); the precedence list (CLI > env > YAML > defaults) is the load-bearing rule.
       - L8 `The resolver in cli.py:_load_config (Phase 13 D-03) passes the resolved`: REWRITE: drop `(Phase 13 D-03)`; the cross-reference to `cli.py:_load_config` survives (it's a code symbol, not a planning ID).
       - L15 `values into the model (SAFE-05 preserved): it only directs the YAML`: REWRITE: drop `(SAFE-05 preserved):`; replace with `env vars do not inject scalar values into the model; they only direct the YAML source path` (the technical statement of SAFE-05 — keep the rule, drop the tag).
       - L19 `.env is dead-letter for the framework's config layer (Phase 13 D-07).`: REWRITE: drop `(Phase 13 D-07)`; keep the constraint.
       - L58-61 The `Phase 21.1 RELOC-01: REQUIRED -- no default` block: REWRITE: drop the planning anchors; keep the constraint (`sdet` is required, no default, no env-var override, no CLI flag override; missing key triggers the canonical missing-required-field operator error path — cite docs/ERROR-STYLE.md if needed).
       - L66 `Phase 13 D-08: v2 schema. Plan 13-02 flipped from v1.`: DROP entirely. Pure provenance; the `version: 2` literal and the v2 validator method downstream speak for themselves.
       - L69 `Phase 08 D-01 / TOOLCFG-01: per-tool registry. Plan 13-03 will flip`: DROP entirely. Pure provenance.
       - L76-77 `Phase 13 D-08: only ``2`` is accepted; v1 configs raise so cli.py's operator-error mapper can render the SAFE-06 ERROR-STYLE message.`: REWRITE: drop `Phase 13 D-08:` and `SAFE-06`; keep the rule ("only `2` is accepted; v1 configs raise so cli.py's operator-error mapper can render the canonical version-mismatch error — see docs/ERROR-STYLE.md").

    Hard rules apply to all three files: no `# noqa` allowlist (D-04); no planning IDs survive; no behavior change; the canonical error-message constants survive verbatim (pinned by `tests/framework/unit/test_error_style.py`).

    Verification commands (run after rewrite, all must return 0):
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/_isolation.py
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/fixtures.py
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/config.py
      grep -cE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/_isolation.py
      grep -cE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/fixtures.py
      grep -cE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/config.py
  </action>
  <verify>automated: bash -c 'set -e; for f in src/mcp_test_framework/_isolation.py src/mcp_test_framework/fixtures.py src/mcp_test_framework/config.py; do n=$(grep -cE "(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b" "$f"); p=$(grep -cE "\bPhase [0-9]+(\.[0-9]+)?\b" "$f"); echo "$f ID=$n Phase=$p"; [ "$n" -eq 0 ] && [ "$p" -eq 0 ]; done</verify>
  <acceptance_criteria>
    - For each of the three files, locked-regex match count returns 0
    - For each of the three files, `Phase NN` match count returns 0
    - `uv run python -c "from mcp_test_framework import _isolation, fixtures, config; print('ok')"` prints `ok`
    - `uv run pytest tests/framework/unit/test_config.py tests/framework/unit/test_config_example.py tests/framework/unit/test_config_init.py tests/framework/unit/test_config_sdet_field.py tests/framework/unit/test_error_style.py tests/framework/unit/test_homelab_config.py tests/framework/unit/test_session_loader_invariants.py -q` exits 0 (config + error-style + isolation behavior preserved)
  </acceptance_criteria>
  <done>
    `_isolation.py`, `fixtures.py`, and `config.py` contain zero matches for the locked regex and zero `Phase NN(.M)?` prefixes; imports cleanly; the config + error-style test subset still passes — proving the canonical error-message text survives verbatim.
  </done>
</task>

<task type="auto">
  <name>Task 3: Scrub the seven remaining files — models, schema_validator, mcp_client, judge_protocol, ollama_judge, rubrics</name>
  <files>src/mcp_test_framework/models.py, src/mcp_test_framework/schema_validator.py, src/mcp_test_framework/mcp_client.py, src/mcp_test_framework/judge_protocol.py, src/mcp_test_framework/ollama_judge.py, src/mcp_test_framework/rubrics.py</files>
  <read_first>
    - src/mcp_test_framework/models.py (16 ID hits / 6 Phase; pure-data models)
    - src/mcp_test_framework/schema_validator.py (7 ID / 3 Phase; deterministic schema checks)
    - src/mcp_test_framework/mcp_client.py (9 ID / 12 Phase; async stdio client wrapper)
    - src/mcp_test_framework/judge_protocol.py (3 ID / 3 Phase)
    - src/mcp_test_framework/ollama_judge.py (14 ID / 8 Phase; Ollama HTTP client + temperature 0 + format:json rationale is load-bearing)
    - src/mcp_test_framework/rubrics.py (2 ID / 1 Phase; smallest in this group)
    - .planning/phases/22-scrub-requirement-id-leaks-from-src/22-CONTEXT.md
    - docs/ERROR-STYLE.md (surviving anchor for any operator-tone error rationales)
  </read_first>
  <action>
    Apply the D-02 per-site decision tree to all six files. These are the lower-density files — most comments are 1 to 3 sites per file. Read each file once, apply the decision tree, run the per-file grep gate.

    Per-file specifics:

    1. `models.py` (16 ID / 6 Phase) — pure-data Pydantic models (JudgeResult, ValidationIssue). Comments likely cluster around the score/pass-threshold rationale and the defensive-parsing fallback patterns. PRESERVE the rules ("score 1–5", "passed = score >= 4 by default"); drop the planning anchors.

    2. `schema_validator.py` (7 ID / 3 Phase) — Draft 2020-12 validator + iter_errors() pattern. The "Draft 2020-12" callout and the "iter_errors returns the full list — no early exit" rule are load-bearing; rewrite preserving them.

    3. `mcp_client.py` (9 ID / 12 Phase) — async stdio client wrapper. The "stdio_client context manager (NOT raw subprocess.Popen)" rule and the AsyncExitStack teardown contract are load-bearing; rewrite preserving them. The Phase 02 / Phase 04.1 history can drop entirely.

    4. `judge_protocol.py` (3 ID / 3 Phase) — lightest file in the group; likely all pure-provenance. DROP if pure-provenance; rewrite only if a comment names a load-bearing constraint (e.g. "judges must be sync-or-async-uniform via Protocol").

    5. `ollama_judge.py` (14 ID / 8 Phase) — Ollama HTTP client. The load-bearing rules to preserve:
       - `/api/chat` with `stream: false` and `format: "json"`
       - `temperature: 0` for determinism
       - parse-error fallback to a JudgeResult with `passed=False` + the raw response (NOT a crash)
       - 120s httpx.Timeout
       Rewrite each rationale-bearing comment to preserve these rules without the planning anchors.

    6. `rubrics.py` (2 ID / 1 Phase) — smallest file. Rubric structures. Likely pure-provenance; DROP both ID lines, rewrite the Phase prefix if it's load-bearing (probably isn't — the rubric structure is self-documenting).

    Hard rules apply to all six files: no `# noqa` allowlist (D-04); no planning IDs survive; no behavior change; the 1–5 rubric and pass-threshold semantics survive in prose where currently documented.

    Verification commands (run after rewrite — all must return 0):
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/models.py
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/schema_validator.py
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/mcp_client.py
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/judge_protocol.py
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/ollama_judge.py
      grep -cE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/rubrics.py
      ... and the parallel `Phase NN` greps for all six files; each must return 0.
  </action>
  <verify>automated: bash -c 'set -e; for f in src/mcp_test_framework/models.py src/mcp_test_framework/schema_validator.py src/mcp_test_framework/mcp_client.py src/mcp_test_framework/judge_protocol.py src/mcp_test_framework/ollama_judge.py src/mcp_test_framework/rubrics.py; do n=$(grep -cE "(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b" "$f"); p=$(grep -cE "\bPhase [0-9]+(\.[0-9]+)?\b" "$f"); echo "$f ID=$n Phase=$p"; [ "$n" -eq 0 ] && [ "$p" -eq 0 ]; done</verify>
  <acceptance_criteria>
    - For each of the six files, locked-regex match count returns 0
    - For each of the six files, `Phase NN` match count returns 0
    - `uv run python -c "from mcp_test_framework import models, schema_validator, mcp_client, judge_protocol, ollama_judge, rubrics; print('ok')"` prints `ok`
    - `uv run pytest tests/framework/unit/test_schema_validator.py tests/framework/unit/test_mcp_client.py tests/framework/unit/test_judge_errors.py tests/framework/unit/test_ollama_judge.py tests/framework/unit/test_rubrics.py -q` exits 0
  </acceptance_criteria>
  <done>
    All six files contain zero matches for the locked regex and zero `Phase NN(.M)?` prefixes; imports cleanly; the relevant unit-test subsets pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → runner output | Operator reads renderer output, error messages from `_emit_operator_error_for_validation`. These are read-only strings; scrub only touches comments / docstrings, not the operator-visible error strings themselves. |
| MCP server subprocess → client | stdio_client wraps a subprocess; comments documenting the AsyncExitStack contract are scrubbed but the contract itself is unchanged. |
| Ollama HTTP → judge | The `temperature: 0` and `format: "json"` request invariants are preserved in prose. |
| host environment → isolation allowlist | The exact allowlist (PATH, SYSTEMROOT, LANG, USERNAME, MCP_*) is the security boundary. Comments are scrubbed; the allowlist itself is unchanged. |
| maintainer → src/ comments | Future maintainer greps src/ for context. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | Tampering | _runner.py renderer comments | accept | Scrub is intentional. Behavior preservation verified by the full runner unit-test subset (9 files). |
| T-22-08 | Tampering | _isolation.py allowlist comments | mitigate | The locked allowlist enumeration (PATH, SYSTEMROOT, LANG, USERNAME, MCP_*) survives verbatim in the rewritten comments; the underlying constant tuples are untouched (code preserved). Verified by `tests/framework/unit/test_session_loader_invariants.py`. |
| T-22-09 | Tampering | config.py error-mapper rationale comments | mitigate | The canonical error-message strings live in operator-visible error rendering and are pinned by `tests/framework/unit/test_error_style.py`. Comments around them are scrubbed; the strings themselves are not. |
| T-22-10 | Tampering | ollama_judge.py request invariants comments | mitigate | The `temperature: 0`, `stream: false`, `format: "json"`, 120s timeout invariants survive in the rewritten prose and remain pinned by `tests/framework/unit/test_ollama_judge.py`. |
| T-22-11 | Information Disclosure | help() / IDE hovers / import-time docstrings | accept | The scrub reduces internal-vocabulary disclosure. Desired outcome. |
| T-22-12 | Denial of Service | module import | mitigate | After each file's rewrite, the import sanity command in acceptance criteria catches any accidental syntax error introduced by a misplaced quote inside a multi-line docstring rewrite. |

No new attack surface. No code, imports, signatures, JSON keys, or string literals (except comment/docstring text) change.
</threat_model>

<verification>
After all three tasks complete:

  grep -crE '(CLI|PERSONA|CODEGEN|SAFE|RUNNER|UX|ISOL|JUNIT|SURFACE|TEST|CLEAN|DOC|UI|SDET|STATE|PREFLIGHT|SCRUB|RELOC)-[0-9]+|\bD-[0-9]+\b' src/mcp_test_framework/ --include="*.py" --exclude-dir=sdet | grep -v ":0$"  # nothing prints (every file in scope returns 0; sdet/ is plan 22-02's responsibility, cli.py is plan 22-01's)

  grep -crE '\bPhase [0-9]+(\.[0-9]+)?\b' src/mcp_test_framework/ --include="*.py" --exclude-dir=sdet | grep -v ":0$"  # same — only cli.py and sdet/ remain, owned by other plans

Behavior preservation (the full non-live test subset):

  uv run pytest tests/ -m "not live_homelab and not live_ollama" -q  # exits 0

Imports:

  uv run python -c "from mcp_test_framework import _runner, _isolation, fixtures, config, models, schema_validator, mcp_client, judge_protocol, ollama_judge, rubrics; print('ok')"  # prints ok
</verification>

<success_criteria>
- All 10 files owned by this plan have zero matches for the locked regex.
- All 10 files have zero `Phase NN(.M)?` prefixes.
- Public import surface preserved.
- The full non-live test suite still exits 0.
- The locked skip-reason constants, the canonical error-message strings, the isolation allowlist, and the Ollama request invariants all survive verbatim (verified by their pinning unit tests).
</success_criteria>

<output>
After completion, create `.planning/phases/22-scrub-requirement-id-leaks-from-src/22-03-SUMMARY.md` summarizing: per-file hit counts before/after, any non-trivial rewrite decisions (especially the _runner.py module docstring rewrite, the config.py SAFE-NN rationale rewrites, and the ollama_judge.py request-invariants comments), any sites where the executor diverged from the default drop-vs-rewrite rule and why.
</output>
</content>
</invoke>