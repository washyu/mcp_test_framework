---
phase: 21-sdet-authoring-docs-readme-parity
plan: "02"
type: execute
wave: 2
depends_on: ["21-01"]
files_modified:
  - README.md
autonomous: false
requirements: [DOC-SDET-03]
tags: [docs, readme, sample, char-for-char-parity, snapshot]

must_haves:
  truths:
    - "README has a new H2 '## SDET scenarios' located between '## Sample green run' and '## Isolation guarantee'."
    - "The new H2 contains a minimal 2-test Proxmox VM-lifecycle scenario (create + delete only — modify omitted per D-16) shown char-for-char with the runner's _render_per_tool_rows + scenario-block output."
    - "A comment near the sample notes the renderer source ('# mirrors _runner.py output — re-run the framework when output format changes') so future readers know the parity is manual (D-14)."
    - "The new section cross-links to docs/SDET-AUTHORING.md for the full authoring walkthrough."
  artifacts:
    - path: "README.md"
      provides: "Operator-facing SDET scenario sample section"
      contains: "## SDET scenarios"
      contains_renderer_pointer: "mirrors _runner.py output"
      contains_xlink: "docs/SDET-AUTHORING.md"
  key_links:
    - from: "README.md ## SDET scenarios"
      to: "docs/SDET-AUTHORING.md"
      via: "markdown link in section body"
      pattern: "docs/SDET-AUTHORING\\.md"
    - from: "README.md ## SDET scenarios output block"
      to: "src/mcp_test_framework/_runner.py _render_per_tool_rows"
      via: "char-for-char snapshot of renderer output"
      pattern: "_runner\\.py"
---

<objective>
Add the new `## SDET scenarios` H2 to README between `## Sample green run` and `## Isolation guarantee`. The section shows a minimal 2-test Proxmox VM-lifecycle scenario (create + delete only — D-16 trims the modify test out of the README sample) with char-for-char renderer output. Mirrors the Phase 16 doc-mirroring pattern (`## Sample green run`).

Purpose: An operator scrolling README sees one concrete SDET scenario rendered the way the runner actually emits it. The full authoring walkthrough lives in `docs/SDET-AUTHORING.md` (linked from this section); the README sample is the single-glance entry point.

Output: Updated `README.md` with new H2 in correct position, code block showing the trimmed scenario source, fenced `text` block showing literal runner output, and the renderer-source pointer comment.

This plan is **NOT autonomous** — D-14 prescribes a manual snapshot. The executor must (a) write a temporary 2-test scenario file targeting Proxmox, (b) run `uv run mcp-test-framework run --sdet -k <pattern>`, (c) capture stdout char-for-char into the README block, (d) delete the temporary scenario file. A `checkpoint:human-verify` step confirms the snapshot's accuracy before the plan completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/21-sdet-authoring-docs-readme-parity/21-CONTEXT.md
@.planning/phases/19-stateful-primitives-domain-ui-integration/19-04-SUMMARY.md
@README.md
@docs/SDET-AUTHORING.md
@src/mcp_test_framework/_runner.py

<interfaces>
<!-- Renderer output shape the snapshot must mirror. Extracted from Phase 19-04 live-run output. -->

Per `_render_per_tool_rows` + scenario-block extension (Phase 19-03):
- Pre-run digest: "MCP Test Framework (SDET)" banner + Discovered/Running/Skipping/Judges lines
- Scenario group: bare group header (e.g. `proxmox_vm_lifecycle`) followed by indented per-test rows with PASS/FAIL/SKIP glyphs (`✓` / `✗` / `○`)
- Result line: `Result: N PASS / M FAIL / K SKIP  in T.Ts`

Example shape from Phase 19-04 live run (verbatim):
```
========================================
MCP Test Framework (SDET)
========================================
MCP server:  uvx homelab-mcp
Discovered:  2 scenarios
Running:      2  (basic_call, proxmox_vm_lifecycle)
Skipping:     0  (use --explain to list)
Judges:      (none — SDET scope)
...
proxmox_vm_lifecycle
  ✓ create_returns_pending_vm
  ✓ delete_returns_ok

Result: 2 PASS / 0 FAIL / 58 SKIP  in 6.3s
```

(The Phase 19-04 live run actually FAILED due to the upstream inputSchema bug; for the README sample, the executor needs a PASSING green run with create + delete. The bug remains UNFIXED upstream — see D-16: the modify test that triggered the bug is omitted from the README sample. create + delete alone should work because they don't transport CPU-bump action payloads.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write temporary 2-test scenario file (create + delete only)</name>
  <files>tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py</files>
  <read_first>
    - .planning/phases/19-stateful-primitives-domain-ui-integration/19-04-SUMMARY.md (full scenario source — the README sample is a trimmed subset, D-16)
    - src/mcp_test_framework/sdet/generated/homelab_mcp/__init__.py (verify CreateProxmoxVmParams / DeleteProxmoxVmParams / CreateProxmoxVmResponse / DeleteProxmoxVmResponse are exported)
    - tests/sdet/conftest.py (the JUnit user_properties hook the temp test will inherit)
  </read_first>
  <action>
Create `tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py` — a TEMPORARY file used ONLY to capture the README snapshot. It will be deleted in Task 4.

Structure: a trimmed copy of the Phase 19-04 dogfood (lift the create + delete tests only — OMIT the modify test per D-16). Use:
- `@pytest_asyncio.fixture(scope="module", loop_scope="session")` yield fixture named `proxmox_vm_lifecycle_readme` (different name from the original to avoid collision with anything else)
- A `ProxmoxVmLifecycleReadmeState` dataclass with just `created: CreateProxmoxVmResponse` (no `modified` field — modify test is omitted)
- Two test functions in file order:
  - `test_create_returns_pending_vm(proxmox_vm_lifecycle_readme)` — asserts `state.created.is_error is False` and `state.created.data.get("vmid")` is an int
  - `test_delete_returns_ok(proxmox_vm_lifecycle_readme)` — calls `tool("delete_proxmox_vm").call(...)` and asserts `.is_error is False`
- Reuses the Phase 19-04 patterns: range-aware VMID allocator reading `config.homelab.proxmox.dogfood_vmid_range`, `MCPTF_DOGFOOD_PROXMOX_HOST` env var requirement, name prefix `mcptf-dogfood-readme-` (different prefix so a concurrent dogfood doesn't sweep it)
- Add a brief module docstring: `"""Temporary scenario used to snapshot README's '## SDET scenarios' sample output. Deleted after snapshot capture (Phase 21 Plan 02 Task 4)."""`

Do NOT include any imports from the framework's source files except the public SDET surface (`mcp_test_framework.sdet` + `mcp_test_framework.sdet.generated.homelab_mcp`).
  </action>
  <verify>
    <automated>uv run pytest tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py --collect-only 2>&1 | findstr "collected"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py`
    - `uv run pytest tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py --collect-only` reports exactly 2 collected items
    - File contains `test_create_returns_pending_vm` and `test_delete_returns_ok` function names
    - File does NOT contain `test_modify` (D-16 — modify test omitted)
    - File does NOT contain `_CpuBumpManageVmParams` (the inputSchema workaround stays exclusively in docs/SDET-AUTHORING.md per D-16)
    - File imports from `mcp_test_framework.sdet.generated.homelab_mcp`
    - File uses `@pytest_asyncio.fixture(scope="module", loop_scope="session")`
    - Module docstring identifies the file as the README-sample temporary
  </acceptance_criteria>
  <done>Temporary 2-test scenario file exists; pytest collection succeeds reporting 2 items.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Operator captures the char-for-char snapshot</name>
  <files>(no files modified — operator runs commands and pastes output back)</files>
  <read_first>
    - tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py (the file the operator will run)
    - README.md (the section being populated next task)
  </read_first>
  <action>
**This is a checkpoint:human-verify task — paused work waiting for the operator.**

The executor must STOP here and ask the operator to perform the snapshot capture. The executor cannot run the framework against the operator's live Proxmox infrastructure — only the operator can.

Operator instructions to relay verbatim:

  Run the following commands and paste stdout (literal text, including spacing and any ANSI-free output) into the conversation:

  ```powershell
  # 1. Confirm Proxmox env var is set (required by the temp scenario)
  $env:MCPTF_DOGFOOD_PROXMOX_HOST  # should be non-empty

  # 2. Run the temp scenario via the framework runner, isolating it with -k
  uv run mcp-test-framework run --sdet -k test_proxmox_vm_lifecycle_readme_sample
  ```

  The executor needs the LITERAL stdout block from the start of the pre-run digest (`========================================`) through the `Result:` line. Paste it verbatim into the conversation.

  Expected shape (for sanity — the actual snapshot will differ in counts):

  ```text
  ========================================
  MCP Test Framework (SDET)
  ========================================
  MCP server:  uvx homelab-mcp
  Discovered:  N scenarios
  Running:      1  (proxmox_vm_lifecycle_readme_sample)
  ...
  proxmox_vm_lifecycle_readme_sample
    ✓ create_returns_pending_vm
    ✓ delete_returns_ok

  Result: 2 PASS / 0 FAIL / K SKIP  in T.Ts
  ```

  If the run fails (e.g., upstream inputSchema bug surfaces on create — Phase 19-04 finding 2), paste the FAIL output and we revisit: the create call uses minimal required params only, so it SHOULD pass; if it doesn't, the upstream bug regressed wider than we thought and we re-scope.

After the operator pastes the output, the executor stores it for Task 3.
  </action>
  <verify>
    <automated>echo CHECKPOINT — operator output must be captured in the conversation before Task 3 proceeds</automated>
  </verify>
  <what-built>The temporary 2-test scenario from Task 1 is collected and ready to run against the operator's live Proxmox cluster.</what-built>
  <how-to-verify>
    Run the following commands and paste stdout (literal text, including spacing and any ANSI-free output) into the conversation:

    ```powershell
    # 1. Confirm Proxmox env var is set (required by the temp scenario)
    $env:MCPTF_DOGFOOD_PROXMOX_HOST  # should be non-empty

    # 2. Run the temp scenario via the framework runner, isolating it with -k
    uv run mcp-test-framework run --sdet -k test_proxmox_vm_lifecycle_readme_sample
    ```

    The executor needs the LITERAL stdout block from the start of the pre-run digest (`========================================`) through the `Result:` line. Operator pastes this output into the conversation; executor uses it verbatim as the snapshot in Task 3.

    Expected shape (for sanity — the actual snapshot will differ in counts):
    ```text
    ========================================
    MCP Test Framework (SDET)
    ========================================
    MCP server:  uvx homelab-mcp
    Discovered:  N scenarios
    Running:      1  (proxmox_vm_lifecycle_readme_sample)
    ...
    proxmox_vm_lifecycle_readme_sample
      ✓ create_returns_pending_vm
      ✓ delete_returns_ok

    Result: 2 PASS / 0 FAIL / K SKIP  in T.Ts
    ```

    If the run fails (e.g., upstream inputSchema bug surfaces on create — Phase 19-04 finding 2), operator pastes the FAIL output and we revisit: the create call uses minimal required params only, so it SHOULD pass; if it doesn't, the upstream bug regressed wider than we thought and we re-scope.
  </how-to-verify>
  <resume-signal>Paste the literal stdout from the run (or type "FAILED" with the actual output if the run failed and we need to debug)</resume-signal>
  <acceptance_criteria>
    - Operator has pasted the literal stdout from `uv run mcp-test-framework run --sdet -k test_proxmox_vm_lifecycle_readme_sample` into the conversation
    - The output spans from the pre-run digest banner (`========================================`) through the `Result:` line
    - The `Result:` line shows `2 PASS / 0 FAIL` (otherwise we abort and re-scope per Task 2 fail-handling text)
  </acceptance_criteria>
  <done>Operator's literal runner output is in the conversation history, ready for Task 3 to embed verbatim into README.</done>
</task>

<task type="auto">
  <name>Task 3: Insert new '## SDET scenarios' H2 into README with captured snapshot</name>
  <files>README.md</files>
  <read_first>
    - README.md (specifically the boundary between `## Sample green run` ending ~line 258 and `## Isolation guarantee` beginning at line 260 — D-15 placement)
    - docs/SDET-AUTHORING.md (verify the H2 to link to)
    - tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py (source the test code block from)
  </read_first>
  <action>
Insert a NEW H2 section `## SDET scenarios` in `README.md` between the existing `## Sample green run` section (ends around line 258) and the existing `## Isolation guarantee` section (begins at line 260).

Section structure (in order):

1. **H2 heading**: `## SDET scenarios`

2. **Intro paragraph** (2-3 sentences): "Beyond the contract pass, SDETs author stateful scenarios under `tests/sdet/` — create resources, verify shape, tear down. Each scenario module renders as a per-tool group with nested rows under the same domain UI the contract pass uses (Phase 14). Run them with `mcp-test-framework run --sdet`."

3. **Code block — test source** (use `python` language tag): the TRIMMED 2-test scenario from Task 1. Strip the `_readme_sample` suffix from the fixture/dataclass/file references so the sample reads as a generic example (e.g., rename the fixture to `proxmox_vm_lifecycle`, the dataclass to `ProxmoxVmLifecycleState`, the file header comment to `# tests/sdet/test_proxmox_vm_lifecycle.py`). The reader sees a copy-pasteable sample, not an artifact named "readme_sample".

   Immediately ABOVE the code block, place this comment line (markdown HTML comment, visible only in raw source):
   ```
   <!-- mirrors _runner.py output — re-run the framework when output format changes (Phase 21 D-14) -->
   ```

4. **Output block** (use `text` language tag): the literal stdout captured in Task 2, with one substitution: replace every occurrence of `proxmox_vm_lifecycle_readme_sample` with `proxmox_vm_lifecycle` so the operator's mental model matches the test source above. Include the FULL block from `========================================` through `Result: ...`. Preserve all whitespace and bare group-header shape (D-CONTEXT.md `<specifics>`: bare group header + indented per-test rows).

5. **Closing paragraph** (1-2 sentences): "See [`docs/SDET-AUTHORING.md`](docs/SDET-AUTHORING.md) for the full authoring walkthrough — module-scope fixtures, cross-file ordering, the conditional skip recipe for scenarios that require live infrastructure, and the `_CpuBumpManageVmParams(extra='allow')` workaround for upstream inputSchema bugs."

6. **Invocation pointer** (1 sentence): "Default `mcp-test-framework run` collects only `tests/contract/`; the `--sdet` flag opts the SDET scope into the run. See [`## Commands > Run the test suite`](#run-the-test-suite) above for full flag composition."

Use Edit (not Write) — this is a surgical insertion into a 326-line README. Preserve all lines before line 260 (the `## Isolation guarantee` header) and after; new section lands between them.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "$lines = Get-Content README.md ; $sdetIdx = ($lines | Select-String -Pattern '^## SDET scenarios$' | Select-Object -First 1).LineNumber ; $sampleIdx = ($lines | Select-String -Pattern '^## Sample green run$' | Select-Object -First 1).LineNumber ; $isoIdx = ($lines | Select-String -Pattern '^## Isolation guarantee$' | Select-Object -First 1).LineNumber ; \"sample=$sampleIdx sdet=$sdetIdx iso=$isoIdx\" ; if ($sdetIdx -gt $sampleIdx -and $sdetIdx -lt $isoIdx) { 'ORDER OK' } else { 'ORDER FAIL' }"</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains literal `## SDET scenarios` (new H2)
    - Line number of `## SDET scenarios` is GREATER than line number of `## Sample green run`
    - Line number of `## SDET scenarios` is LESS than line number of `## Isolation guarantee` (D-15 placement)
    - `README.md` contains literal `mirrors _runner.py output` (renderer-source pointer per D-14)
    - `README.md` contains literal `mcp-test-framework run --sdet` (invocation)
    - `README.md` contains literal `proxmox_vm_lifecycle` (test source + output block both use this name — no `_readme_sample` leakage)
    - `README.md` does NOT contain `_readme_sample` (the temp suffix is scrubbed from the published sample)
    - `README.md` contains markdown link `(docs/SDET-AUTHORING.md)` inside the new section
    - The output block is fenced with `text` language tag and contains the bare group header `proxmox_vm_lifecycle` followed by indented `✓ create_returns_pending_vm` and `✓ delete_returns_ok` rows (per Phase 19-03 _render_per_tool_rows shape; CONTEXT.md `<specifics>`)
    - The output block contains a `Result: ... PASS / ... FAIL / ... SKIP  in ...` line
  </acceptance_criteria>
  <done>README has a new `## SDET scenarios` H2 in the correct position (between Sample green run and Isolation guarantee), containing the test source code block, the renderer-source HTML-comment pointer, the literal runner output block matching the Phase 19-03 scenario shape, and a cross-link to docs/SDET-AUTHORING.md.</done>
</task>

<task type="auto">
  <name>Task 4: Delete the temporary scenario file</name>
  <files>tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py</files>
  <read_first>
    - tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py (confirm content is still the temp scenario; if missing, plan is in a bad state)
    - README.md (verify the snapshot has been embedded — Task 3 acceptance criteria all pass)
  </read_first>
  <action>
Use `git rm tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py` to delete the temporary file (NOT `Remove-Item` — we want the deletion staged so the commit is clean). Phase 20 deliberately emptied `tests/sdet/` of SUT-specific scenarios; this temp file violated that contract for the duration of the snapshot run, and now restores the contract.

After deletion, run `uv run pytest --collect-only tests/sdet/ 2>&1` to confirm the directory is back to "no tests collected" — only `__init__.py` and `conftest.py` remain (matches Phase 20-04 final state).
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "(-not (Test-Path tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py)) ; (Test-Path tests/sdet/__init__.py) ; (Test-Path tests/sdet/conftest.py)"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py` does NOT exist (Test-Path returns False)
    - `tests/sdet/__init__.py` still exists (Phase 20 preserved file untouched)
    - `tests/sdet/conftest.py` still exists (Phase 20 preserved file untouched)
    - `uv run pytest --collect-only tests/sdet/` reports `collected 0 items` (matches Phase 20-04 final state)
    - `git status` shows the deletion staged (the file is in the diff as `deleted`)
    - `git diff --name-only -- src/ | wc -l` returns 0 (no src/ changes from this plan)
  </acceptance_criteria>
  <done>Temporary scenario file deleted via git rm; tests/sdet/ is back to Phase 20-04 baseline (no test files, only __init__.py + conftest.py). README's snapshot now stands on its own with no live test fixture backing it (matching Phase 16's manual-snapshot pattern per D-14).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (transient during snapshot capture) | Task 1's temporary scenario file briefly touches a live Proxmox cluster — but this is the operator's own infrastructure under deliberate test, gated by `MCPTF_DOGFOOD_PROXMOX_HOST`. After Task 4, no transient surface remains. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Tampering | `tests/sdet/test_proxmox_vm_lifecycle_readme_sample.py` (transient) | accept | Temporary file with deterministic VMID allocation (range-bound) and name-prefix double-filter (`mcptf-dogfood-readme-`); Task 4 deletes it via `git rm` immediately after snapshot. No long-lived surface. |
| T-21-02-02 | Information disclosure | README.md sample output | mitigate | Snapshot captures only structural shape (pass counts, test names, durations) — no hostnames, no credentials, no VMIDs leak into the published README. Verify by inspection in Task 3 before committing. |

(The doc deliverable itself ships zero threat surface. The only transient threat is the snapshot-capture run, which is gated by an env var on operator-owned infrastructure.)
</threat_model>

<verification>
- `README.md` contains `## SDET scenarios` H2 positioned between `## Sample green run` and `## Isolation guarantee`
- New section contains: test source code block, `<!-- mirrors _runner.py output ... -->` HTML comment, literal text block from the live run, cross-link to docs/SDET-AUTHORING.md
- `tests/sdet/` contains only `__init__.py` and `conftest.py` (Phase 20 baseline restored)
- No `src/` files modified
- No `.planning/` paths leak into README's new section
</verification>

<success_criteria>
- DOC-SDET-03 satisfied: README has one scenario sample with char-for-char renderer parity (Phase 16 doc-mirroring pattern inherited).
- D-13 satisfied: minimal 2-test scenario (not full lifecycle).
- D-14 satisfied: manual snapshot at phase commit time + comment pointer near the sample. No regression test, no regen script.
- D-15 satisfied: new H2 between `## Sample green run` and `## Isolation guarantee`.
- D-16 satisfied: modify test omitted; create + delete only.
- Phase 20-04 final state restored: tests/sdet/ has only `__init__.py` + `conftest.py`.
</success_criteria>

<output>
After completion, create `.planning/phases/21-sdet-authoring-docs-readme-parity/21-02-SUMMARY.md` describing: the captured snapshot's `Result:` line, the line range in README where the new H2 lives, verification that the temp file was deleted, and a note on whether the live run encountered the Phase 19-04 inputSchema bug (it should not, since modify is omitted).
</output>
