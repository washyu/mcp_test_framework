---
phase: 23-test-suite-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/framework/conftest.py
  - tests/framework/test_tool_config.py
  - tests/framework/test_config_init_cli.py
  - tests/framework/smoke/test_smoke_homelab_mcp.py
  - tests/framework/smoke/test_smoke_ollama_judge.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every red test in tests/framework/ caused by bare Config() construction is enumerated before any fix is applied (D-01, D-03 prerequisite)."
    - "Every Config() call site identified in the failure inventory passes sdet=_SDET_STUB or sdet=SdetConfig(generated_root='tests/sdet/_generated') (D-01)."
    - "tests/framework/conftest.py exists and shadows the production session-scoped `config` fixture with one that supplies _SDET_STUB; src/mcp_test_framework/fixtures.py is unmodified (D-02)."
    - "After this plan, every Cluster A red goes green; running `uv run pytest tests/framework/ --tb=no -q` shows zero Cluster A reds (D-03 verification)."
  artifacts:
    - path: "tests/framework/conftest.py"
      provides: "Session-scoped `config` fixture override supplying _SDET_STUB"
      contains: "SdetConfig(generated_root=\"tests/sdet/_generated\")"
    - path: "tests/framework/test_tool_config.py"
      provides: "Tool-config tests with Config(sdet=...) calls"
      contains: "SdetConfig"
    - path: "tests/framework/test_config_init_cli.py"
      provides: "config-init CLI tests with Config(sdet=...) calls"
      contains: "SdetConfig"
  key_links:
    - from: "tests/framework/conftest.py"
      to: "mcp_test_framework.config.Config"
      via: "session-scoped fixture override"
      pattern: "@pytest.fixture\\(scope=\"session\"\\)"
    - from: "tests/framework/conftest.py"
      to: "mcp_test_framework.models.SdetConfig"
      via: "import + stub construction"
      pattern: "SdetConfig\\(generated_root="
---

<objective>
Cluster A — propagate the `_SDET_STUB = SdetConfig(generated_root="tests/sdet/_generated")` pattern from `tests/framework/unit/test_homelab_config.py:51-58` to every other test in `tests/framework/` that bare-constructs `Config()`, and create `tests/framework/conftest.py` to shadow the production session-scoped `config` fixture in `src/mcp_test_framework/fixtures.py:89-101` (which production fixture remains UNMODIFIED — D-02).

Purpose: After Phase 21.1 RELOC-01 made `Config.sdet` REQUIRED with no default, every bare `Config()` in tests/framework/ raises `pydantic.ValidationError: sdet Field required`. D-01 says "tests adapt; framework does not." D-02 says the production fixture stays untouched; the seam is a test-side conftest.

Output:
- `tests/framework/conftest.py` (NEW) — session-scoped `config` fixture override.
- Edits to each Cluster A red site (enumerated in Task 1 by running the failure inventory).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-test-suite-debt-cleanup/23-CONTEXT.md
@.planning/phases/23-test-suite-debt-cleanup/23-PATTERNS.md
@tests/framework/unit/test_homelab_config.py
@src/mcp_test_framework/fixtures.py
@src/mcp_test_framework/config.py
@src/mcp_test_framework/models.py

<interfaces>
<!-- The two contracts every Cluster A fix touches. -->

From `src/mcp_test_framework/models.py` (SdetConfig — required field on Config):
```python
class SdetConfig(BaseModel):
    generated_root: str  # path string, no default in Phase 21.1 reloc form
    # (verify exact field names by reading models.py before editing)
```

From `src/mcp_test_framework/config.py` (Config root model — sdet field):
```python
class Config(BaseModel):
    sdet: SdetConfig  # REQUIRED post Phase 21.1 RELOC-01 (no default_factory)
    # ... other fields ...
```

The `_SDET_STUB` literal (verbatim from `tests/framework/unit/test_homelab_config.py:51-58`):
```python
from mcp_test_framework.config import Config
from mcp_test_framework.models import SdetConfig

# Phase 21.1 RELOC-01: Config.sdet is REQUIRED. Tests in this file that
# construct Config(...) must supply an sdet stub (or set it in YAML).
_SDET_STUB = SdetConfig(generated_root="tests/sdet/_generated")
```

Production fixture being shadowed (`src/mcp_test_framework/fixtures.py:89-101` — DO NOT MODIFY):
```python
@pytest.fixture(scope="session")
def config() -> Config:
    """Load YAML config once per session."""
    return Config()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Failure inventory — enumerate Cluster A red sites</name>
  <files>(no files modified — diagnostic only)</files>
  <read_first>
    - .planning/phases/23-test-suite-debt-cleanup/23-CONTEXT.md (D-01, D-03)
    - .planning/phases/23-test-suite-debt-cleanup/23-PATTERNS.md (Pre-Flight Notes §1)
    - tests/framework/unit/test_homelab_config.py:51-65 (the locked _SDET_STUB pattern)
    - pyproject.toml (confirm `addopts` deselects `live_homelab and live_ollama`)
  </read_first>
  <action>
Run the failure inventory and capture the exact list of Cluster A red tests. Cluster A = any test in `tests/framework/` whose failure traceback shows `pydantic.ValidationError` mentioning `sdet Field required` (or equivalent), or any test in `tests/framework/` that bare-constructs `Config()`.

Steps:
1. Run: `uv run pytest tests/framework/ --tb=short -q 2>&1 | tee /tmp/p23-inventory.txt` (Windows PowerShell: `uv run pytest tests/framework/ --tb=short -q 2>&1 | Tee-Object -FilePath $env:TEMP\p23-inventory.txt`)
2. Grep the output for `sdet Field required` to identify Cluster A reds.
3. Grep `tests/framework/` source for bare `Config()` (no args): `Grep "Config\(\)\s*$" tests/framework/ -n`.
4. Cross-reference (2) and (3) — every bare `Config()` call is a candidate site; only the ones whose tests are RED need fixing (some bare `Config()` sites in `test_runner_migration.py:132, 192` test the `MCPTF_CONFIG_FILE` env-var fallback and may already pass — verify by their pytest pass/fail status).
5. Record the inventory in the task SUMMARY as a table of `file:line` → `test_name` → `red?` → `pattern_to_apply (S1=module stub, S2=inline)`.

DO NOT modify any code in this task. The output is a deterministic list.

Expected Cluster A red sites per CONTEXT/PATTERNS (verify against actual run):
- tests/framework/test_tool_config.py:53 (`test_default_config_version_and_tools`)
- tests/framework/test_tool_config.py:~150 (TestV111SkipFilter region)
- tests/framework/test_tool_config.py::test_call_arguments_forwarded_to_call_tool_via_asyncmock (suspected 12th red)
- tests/framework/test_config_init_cli.py:130 (`cfg = Config()  # MUST NOT raise`)
- tests/framework/smoke/test_smoke_homelab_mcp.py:41, 63
- tests/framework/smoke/test_smoke_ollama_judge.py:78
- tests/framework/test_isolation.py::test_real_state_unchanged (flows through production `config` fixture — fixed by Task 2 conftest, not by editing the test)
  </action>
  <verify>
    <automated>uv run pytest tests/framework/ --tb=no -q 2>&1 | grep -E "failed|error" | head -5</automated>
  </verify>
  <done>Inventory table exists capturing every Cluster A red file:line:test, with the pattern-to-apply (S1 or S2 from PATTERNS.md) noted for each. No source edits in this task.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/framework/conftest.py with `config` fixture override (D-02)</name>
  <files>tests/framework/conftest.py</files>
  <read_first>
    - .planning/phases/23-test-suite-debt-cleanup/23-CONTEXT.md (D-02)
    - .planning/phases/23-test-suite-debt-cleanup/23-PATTERNS.md (§"tests/framework/conftest.py (CREATE — D-02 test-side override seam)")
    - tests/sdet/conftest.py (analog 1 — shape reference, focused single-hook conftest)
    - src/mcp_test_framework/fixtures.py:89-101 (production fixture being shadowed — DO NOT EDIT)
    - tests/framework/test_isolation.py (the single test that flows through the production fixture)
  </read_first>
  <action>
Create `tests/framework/conftest.py` with the following verbatim contents:

```python
"""Phase 23 D-02: test-side override of the session-scoped `config` fixture.

After Phase 21.1 RELOC-01 made Config.sdet REQUIRED, the production fixture
at src/mcp_test_framework/fixtures.py raises pydantic.ValidationError when
constructed bare. tests/framework/ tests don't need a real sdet config -- they
exercise the framework itself, not SDET codegen. This conftest shadows the
fixture with one that supplies the `_SDET_STUB` pattern already established
at tests/framework/unit/test_homelab_config.py.

Production `src/` is intentionally NOT modified -- see Phase 23 CONTEXT.md D-02.
"""
from __future__ import annotations

import pytest

from mcp_test_framework.config import Config
from mcp_test_framework.models import SdetConfig

_SDET_STUB = SdetConfig(generated_root="tests/sdet/_generated")


@pytest.fixture(scope="session")
def config() -> Config:
    return Config(sdet=_SDET_STUB)
```

Notes for executor:
- If `SdetConfig` actually lives at `mcp_test_framework.config` (not `models`), match the import path used by `tests/framework/unit/test_homelab_config.py:51-58` exactly. Use whichever import path THAT file uses — it is the source of truth for the pattern.
- The fixture name `config` and scope `"session"` must match `src/mcp_test_framework/fixtures.py:89` byte-for-byte so pytest's nested-conftest precedence shadows the production fixture for any test collected under `tests/framework/`.
- Do NOT add other fixtures, hooks, or imports to this file. Keep it scoped to D-02.
- Do NOT modify `src/mcp_test_framework/fixtures.py`.
  </action>
  <verify>
    <automated>uv run pytest tests/framework/test_isolation.py::test_real_state_unchanged --tb=short -q</automated>
  </verify>
  <done>
- `tests/framework/conftest.py` exists with the verbatim body above.
- `grep -n "SdetConfig" tests/framework/conftest.py` shows the import and the `_SDET_STUB` line.
- `grep -n "scope=\"session\"" tests/framework/conftest.py` returns one line.
- `tests/framework/test_isolation.py::test_real_state_unchanged` passes (it was a Cluster A red before this task).
- `src/mcp_test_framework/fixtures.py` is UNCHANGED (verify with `git diff src/mcp_test_framework/fixtures.py` → empty).
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply _SDET_STUB pattern to every Cluster A red site from Task 1 inventory</name>
  <files>(driven by Task 1 inventory; expected: tests/framework/test_tool_config.py, tests/framework/test_config_init_cli.py, tests/framework/smoke/test_smoke_homelab_mcp.py, tests/framework/smoke/test_smoke_ollama_judge.py)</files>
  <read_first>
    - Task 1 inventory output
    - .planning/phases/23-test-suite-debt-cleanup/23-PATTERNS.md (§Shared Patterns S1, S2)
    - tests/framework/unit/test_homelab_config.py:51-58 (S1 source of truth)
    - tests/framework/test_tool_config.py:204-212 (S2 inline-kwarg source already in-tree)
  </read_first>
  <action>
For every Cluster A red site enumerated in Task 1, apply pattern S1 (module-level `_SDET_STUB`) or S2 (inline) per PATTERNS.md §Pattern Assignments:

- **S1 (module-level constant) — preferred when a file has ≥2 bare `Config()` sites.** Add at module top (after imports):
  ```python
  from mcp_test_framework.models import SdetConfig

  _SDET_STUB = SdetConfig(generated_root="tests/sdet/_generated")
  ```
  Then replace each `Config()` with `Config(sdet=_SDET_STUB)`.

- **S2 (inline kwarg) — for single-site files or when other kwargs are already being passed.**
  ```python
  Config(sdet=SdetConfig(generated_root="tests/sdet/_generated"))
  ```
  Add `from mcp_test_framework.models import SdetConfig` to the imports if not already present.

Per-file expected changes (verify each against Task 1 inventory before editing; SKIP any site whose test is NOT in the red set):

1. `tests/framework/test_tool_config.py` — apply S1 (module-level stub). Replace `Config()` (line ~53) and any other bare `Config()` in the file with `Config(sdet=_SDET_STUB)`. The file already has an inline `SdetConfig(...)` example near line 204; that is fine to leave OR switch to `_SDET_STUB` for consistency (planner's discretion within this task).
2. `tests/framework/test_config_init_cli.py` — apply S2 inline at line ~130 (`cfg = Config()` becomes `cfg = Config(sdet=SdetConfig(generated_root="tests/sdet/_generated"))`). Add import if missing. NOTE: this test's docstring says "MUST NOT raise" — the new form must continue to not raise.
3. `tests/framework/smoke/test_smoke_homelab_mcp.py` — apply S1 (2 sites at lines ~41 and ~63).
4. `tests/framework/smoke/test_smoke_ollama_judge.py` — apply S2 inline at line ~78 (single site).

DO NOT change tests/framework/unit/test_runner_migration.py lines 132 or 192 — those are intentionally testing the bare-`Config()` env-var fallback and pass when the env var is set; if they're red, the fix is to ensure `MCPTF_CONFIG_FILE` is set in the test setup, NOT to add `sdet=...`. The Task 1 inventory determines whether they're actually red.

DO NOT modify `tests/framework/unit/test_homelab_config.py` — it is the source of the pattern.

DO NOT add an sdet=... call to `tests/framework/test_isolation.py::test_real_state_unchanged` — that test goes green via the conftest in Task 2, not via test-body edit.
  </action>
  <verify>
    <automated>uv run pytest tests/framework/test_tool_config.py tests/framework/test_config_init_cli.py tests/framework/smoke/ --tb=short -q</automated>
  </verify>
  <done>
- Every Cluster A red site from Task 1 inventory has `Config(sdet=_SDET_STUB)` or `Config(sdet=SdetConfig(generated_root="tests/sdet/_generated"))`.
- `Grep "Config\(\)\s*$" tests/framework/ -n` returns ONLY the intentional `test_runner_migration.py:132,192` sites (env-var fallback tests) — if it returns those, they are still bare on purpose.
- `tests/framework/test_tool_config.py`, `test_config_init_cli.py`, both smoke files pass green.
- `src/mcp_test_framework/fixtures.py` and `tests/framework/unit/test_homelab_config.py` are UNCHANGED.
  </done>
</task>

<task type="auto">
  <name>Task 4: Cluster A close-gate — full tests/framework/ run, env-pollution sanity check</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - .planning/phases/23-test-suite-debt-cleanup/23-CONTEXT.md (D-03)
    - Task 1 inventory
  </read_first>
  <action>
Verify D-03 (bulk-run env pollution): run the full `tests/framework/` suite and confirm every Cluster A red from Task 1 inventory is now GREEN, including `test_config_default_homelab` which CONTEXT.md says "passes alone but fails in the full suite."

Steps:
1. Run: `uv run pytest tests/framework/ --tb=no -q`
2. Count failures and errors.
3. If Cluster A reds remain green individually but fail in the full suite (the D-03 symptom), investigate the upstream test mutating `os.environ` or leaving an `MCPTF_*` var set. Likely culprits: tests that set env vars via `os.environ[...]` without monkeypatch. Use `grep -n "os.environ\[.MCPTF" tests/framework/` to enumerate candidates.
4. If env pollution is found, fix the leaking test (preferred: convert to `monkeypatch.setenv(...)`). Do NOT add an env-isolation conftest fixture — captured as deferred per CONTEXT.md §Deferred.
5. If no env pollution, the fixture override from Task 2 has resolved D-03 already — note this in the task SUMMARY.

This task is allowed to modify additional test files if env-pollution leaks are found; record any such edits in the task SUMMARY.

Cluster B and C reds will still be present after this plan — that is expected. They are the scope of Plans 02 and 03.
  </action>
  <verify>
    <automated>uv run pytest tests/framework/ --tb=no -q 2>&1 | grep -E "^[0-9]+ (failed|passed|error)"</automated>
  </verify>
  <done>
- Every Cluster A red from Task 1 is GREEN under the full `uv run pytest tests/framework/` run.
- Cluster B reds (parents[2] sites) and Cluster C reds (README) are still present — that is in-scope for Plans 02 and 03.
- If env-pollution leaks were patched, the fix is documented in the task SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test conftest → production fixture | The new `tests/framework/conftest.py` shadows `src/mcp_test_framework/fixtures.py:config`. Any test that incorrectly assumes it is exercising the production fixture would silently exercise the test override. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01-01 | Tampering / Repudiation | tests/framework/conftest.py override | mitigate | Override is name+scope-identical to production fixture; only `tests/framework/` collects it. Production fixture in `src/` untouched (verified by `git diff` clean). Close-gate (Plan 04) and `tests/contract/` + `tests/sdet/` suites still exercise the production fixture. |
| T-23-01-02 | Information Disclosure | _SDET_STUB literal path "tests/sdet/_generated" | accept | Path string only, no secrets; identical to source pattern in `tests/framework/unit/test_homelab_config.py`. |
| T-23-01-03 | Denial of Service | Env-pollution causing intermittent reds (D-03) | mitigate | Task 4 audits `os.environ[...]` mutation in tests; converts to `monkeypatch.setenv` if found. Full-suite green run is the assertion. |
</threat_model>

<verification>
- All Cluster A reds from Task 1 inventory are green.
- `tests/framework/conftest.py` exists with the verbatim D-02 body.
- `src/mcp_test_framework/fixtures.py` unchanged (`git diff src/mcp_test_framework/fixtures.py` → empty).
- `tests/framework/unit/test_homelab_config.py` unchanged (pattern source, MUST NOT be modified).
- Cluster B and C reds remain (expected; Plans 02/03 scope).
</verification>

<success_criteria>
- Task 1 inventory captures every Cluster A red `file:line:test`.
- `tests/framework/conftest.py` created with verbatim body.
- Every Cluster A red has `sdet=...` supplied via S1 or S2.
- `uv run pytest tests/framework/ --tb=no -q` shows zero Cluster A reds; remaining reds belong to Cluster B/C.
</success_criteria>

<output>
After completion, create `.planning/phases/23-test-suite-debt-cleanup/23-01-SUMMARY.md` recording:
- Task 1 inventory table (file:line:test → red? → pattern applied).
- Task 4 env-pollution audit outcome (none found, or fix applied to <file>).
- Final pre/post `failed/errored` counts.
</output>
