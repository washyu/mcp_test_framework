---
phase: 23-test-suite-debt-cleanup
plan: 04
type: execute
wave: 2
depends_on: ["23-01", "23-02", "23-03"]
files_modified: []
autonomous: true
requirements: []

must_haves:
  truths:
    - "`uv run pytest tests/framework/ --tb=no -q` exits 0 (D-07 close gate)."
    - "Result line shows `failed == 0` and `errored == 0` (D-08 explicit assertion)."
    - "Pre-existing xfailed (currently 2) and skipped (currently 1) counts may remain — they are not red and are out of scope (D-08)."
    - "No `src/mcp_test_framework/` files were modified in any plan of this phase (verified by `git diff src/mcp_test_framework/` returning empty for everything except `tests/framework/conftest.py` from Plan 01)."
  artifacts: []
  key_links:
    - from: "Plan 04 (close gate)"
      to: "Plans 01, 02, 03"
      via: "depends_on chain"
      pattern: "wave: 2"
---

<objective>
Phase 23 close gate — verify that after Plans 01 (Cluster A), 02 (Cluster B), and 03 (Cluster C) have landed, `uv run pytest tests/framework/ --tb=no -q` exits 0 with `failed == 0` and `errored == 0`. This is the D-07/D-08 contract.

Purpose: Single authoritative assertion that v1.3 closes with a green framework suite. The phase is not done until this plan passes; if any unexpected red surfaces, that red is in-scope per D-07 ("any additional reds that appear during phase execution are in scope") and triggers a follow-up plan.

Output: Verification SUMMARY recording exit code, pass/fail/xfail/skip counts, and a confirmation that `src/mcp_test_framework/` was untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-test-suite-debt-cleanup/23-CONTEXT.md
@.planning/phases/23-test-suite-debt-cleanup/23-PATTERNS.md
@pyproject.toml
@.planning/phases/23-test-suite-debt-cleanup/23-01-SUMMARY.md
@.planning/phases/23-test-suite-debt-cleanup/23-02-SUMMARY.md
@.planning/phases/23-test-suite-debt-cleanup/23-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full tests/framework/ suite and assert close-gate</name>
  <files>(no files modified)</files>
  <read_first>
    - .planning/phases/23-test-suite-debt-cleanup/23-CONTEXT.md (D-07, D-08)
    - pyproject.toml (confirm `addopts` still deselects `live_homelab and live_ollama`)
    - 23-01-SUMMARY.md, 23-02-SUMMARY.md, 23-03-SUMMARY.md (prerequisite plans' outcomes)
  </read_first>
  <action>
Run the full framework suite under default addopts:

```
uv run pytest tests/framework/ --tb=no -q
```

Capture the exit code and the summary line. The close-gate is:

- Exit code == 0
- Summary contains `0 failed` (or omits `failed`)
- Summary contains `0 error` (or omits `error`)
- xfailed and skipped counts may be non-zero — D-08 explicitly says pre-existing xfailed=2 and skipped=1 stay as-is.

If any red remains:
- Identify whether it is (a) a Cluster A/B/C site that wasn't covered in Plans 01–03, (b) a new red introduced by a Plan 01–03 edit, or (c) an unrelated red that surfaced under bulk-run env conditions (D-03 territory).
- For (a) or (b): re-open the relevant plan or schedule a follow-up plan. Do NOT silently patch in this close-gate plan.
- For (c): per D-07, additional reds are in-scope; schedule a follow-up plan (do not patch in this task — the close-gate plan stays diagnostic-only).

Verification details:
1. Run `git diff --stat src/mcp_test_framework/` and confirm zero changes (D-02 + scope discipline).
2. Run `git diff --stat tests/framework/` and confirm the only NEW file is `tests/framework/conftest.py` (Plan 01 D-02).
3. Run `git diff --stat README.md` and confirm changes are limited to Plan 03 sentences.
4. Compare current vs. baseline failed/error counts. Baseline (per CONTEXT.md): 12 failed + 1 error. Target: 0 failed + 0 errored.

Record the close-gate result in the task SUMMARY. If green, the phase is done.
  </action>
  <verify>
    <automated>uv run pytest tests/framework/ --tb=no -q 2>&1 | tee /tmp/p23-close-gate.txt; grep -E "passed|failed|error" /tmp/p23-close-gate.txt | tail -1</automated>
  </verify>
  <done>
- `uv run pytest tests/framework/ --tb=no -q` exits 0.
- Summary line shows `0 failed` (or omits `failed` keyword) and `0 error` (or omits `error` keyword).
- `git diff --stat src/mcp_test_framework/` returns empty (no production changes).
- `git diff --stat tests/framework/` shows: 1 new file (`conftest.py`) + edits limited to Plan 01/02/03 targets.
- `git diff --stat README.md` shows Plan 03 sentences only.
- SUMMARY records exit code, full final-line summary text, and the diff-stat snapshots.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| close-gate test run → phase-completion assertion | This is the single load-bearing assertion of the phase. A false-positive green (e.g., addopts deselects something it shouldn't) would let debt carry forward. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-04-01 | Tampering | pyproject.toml addopts could be silently modified to dodge a red | mitigate | Task 1 reads `pyproject.toml` first; if addopts is anything other than the documented `not live_homelab and not live_ollama` filter, fail loud. |
| T-23-04-02 | Repudiation | Plan claims green but src/ was edited en route | mitigate | Task 1 step 1 explicitly runs `git diff --stat src/mcp_test_framework/` and asserts empty. D-02 contract holds. |
| T-23-04-03 | Denial of Service | Test flake — same test passes/fails on repeat runs | accept | If a flake appears, re-run; if it persists, surface as a follow-up plan per D-07. The close-gate is a single-run assertion. |
</threat_model>

<verification>
- Phase 23 close-gate: `uv run pytest tests/framework/ --tb=no -q` exits 0 with `failed == 0 and errored == 0`.
- `src/mcp_test_framework/` git-diff is empty.
- Only `tests/framework/conftest.py` was created; other edits are limited to Plan 01/02/03 target files.
</verification>

<success_criteria>
- Close-gate test run is green.
- D-02 invariant (no `src/` changes) holds.
- SUMMARY records full evidence (exit code, summary line, diff-stat).
</success_criteria>

<output>
After completion, create `.planning/phases/23-test-suite-debt-cleanup/23-04-SUMMARY.md` recording:
- Exit code of `uv run pytest tests/framework/ --tb=no -q`.
- Verbatim summary line (e.g., `123 passed, 2 xfailed, 1 skipped in 4.21s`).
- `git diff --stat` outputs for `src/mcp_test_framework/`, `tests/framework/`, `README.md`.
- Confirmation that Phase 23 close-gate is satisfied.
</output>
