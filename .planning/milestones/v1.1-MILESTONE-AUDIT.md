---
milestone: v1.1
milestone_name: Multi-Tool + Isolation + JUnit
audited: 2026-05-08T00:00:00Z
status: tech_debt
scores:
  requirements: 25/25
  phases: 5/5
  integration: 5/5
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 09-junit-xml-output-per-tool-reporting
    items:
      - "Missing 09-VERIFICATION.md (paper gap — goal-achievement evidenced by tests/test_reporter.py 29 passing unit tests + 3 live subprocess tests deferred by sandbox 120s timeout). Recommend authoring a thin VERIFICATION.md that cites the 09-0N-SUMMARY.md trio and the regression suite as evidence."
      - "3 live subprocess tests in tests/test_reporter.py deferred per 09-03-SUMMARY ('Live tests deferred for sandboxed run'). Re-run with --timeout=600 on a developer host or in CI to capture artifact evidence."
  - phase: 06-per-session-host-state-isolation
    items:
      - "ISOL-06 POSIX arm explicitly deferred per CD-04 — runs as side-effect of normal v1.1 development on a non-Windows host. Windows arm verified empirically. Implementation is platform-agnostic by construction (sets HOME and USERPROFILE unconditionally)."
      - "Phase 06 SC-4 names BOTH code comments AND EXTENDING.md for the env-var passthrough allowlist. Code comments shipped; EXTENDING.md does not yet absorb the _PASSTHROUGH_ALLOWLIST warning from _isolation.py:33-36. Phase 10 added the per-tool walkthrough (which fully satisfies DOC-07's mapping in REQUIREMENTS.md) but did not absorb this Phase 06 forward-reference. Severity: WARNING — operators can still reference the in-source warning."
      - "Open WR-04 from 06-REVIEW.md: POSIX `USER` not in env passthrough allowlist. Cross-platform parity concern for future ISOL-06 POSIX run; informational only — `USERNAME` is diagnostic, no functional dependency for HOME redirect."
  - phase: traceability
    items:
      - "REQUIREMENTS.md traceability table shows 'Pending' for ISOL-03, ISOL-06, DOC-04..07 despite the requirements being marked [x] complete in the body. Fix the table status column to reflect actual completion."
      - "Requirements checkboxes for DOC-04..07 are still '[ ]' in the body of REQUIREMENTS.md — should be '[x]' since Phase 10 PASSED."
  - phase: process-hygiene
    items:
      - "ROADMAP wording 'mtimes' → 'sha256' for ISOL-03: Phase 06 D-09 strengthened the implementation to sha256 (catches same-second identical-content overwrites). ROADMAP.md text still says 'mtimes' — docs-side fix tracked in 06-03-SUMMARY."
---

# v1.1 Milestone Audit

**Status:** TECH DEBT — all requirements satisfied, no critical blockers, accumulated paper-only gaps need review before completion.

**Audited:** 2026-05-08
**Milestone:** v1.1 — Multi-Tool + Isolation + JUnit
**Definition of Done (ROADMAP.md):** A user runs `uv run mcp-test-framework run --junit-xml=results.xml`, the framework spawns homelab-mcp under stdio with isolated `HOME`/`USERPROFILE`, discovers all advertised tools, parameterizes the 10 tests over each tool with config-driven skip/judges/call_arguments, and emits JUnit XML with `[<tool_name>]` test IDs plus a terminal per-tool summary line.

**Verdict:** Definition of Done achieved. Every cross-phase boundary is wired, E2E flow traces end-to-end with no broken links, and all 25 requirements have either passed verification or are deferred under previously-accepted CONTEXT decisions.

---

## Score Card

| Dimension       | Score | Notes |
|-----------------|-------|-------|
| Requirements    | 25/25 | All satisfied (1 partial: ISOL-06 POSIX-arm — explicitly deferred per CD-04, accepted at planning) |
| Phases          | 5/5   | All marked Complete in ROADMAP. Phase 09 has SUMMARY.md trio but no VERIFICATION.md (paper gap, W-1). |
| Integration     | 5/5   | All five phase boundaries wired (06↔07, 07↔08, 08↔09, 09↔10, 10↔08) |
| End-to-end flow | 1/1   | Full CLI → discovery (isolated) → parametrize → tool_config → call_tool → JUnit + per-tool summary trace verified |

---

## Requirements Coverage (3-source cross-reference)

| REQ-ID | Phase | VERIFICATION.md | SUMMARY.md frontmatter | REQUIREMENTS.md | Final Status |
|--------|-------|-----------------|------------------------|------------------|--------------|
| ISOL-01 | 06 | passed | listed (06-01) | [x] (table: Complete) | satisfied |
| ISOL-02 | 06 | passed | listed (06-02) | [x] (table: Complete) | satisfied |
| ISOL-03 | 06 | passed | listed (06-03) | [x] (table says Pending — drift, fix) | satisfied |
| ISOL-04 | 06 | passed | listed (06-02) | [x] (table: Complete) | satisfied |
| ISOL-05 | 06 | passed | listed (06-02) | [x] (table: Complete) | satisfied |
| ISOL-06 | 06 | passed (Windows arm); POSIX deferred per CD-04 | listed (06-03) | [x] (table says Pending — drift, fix) | satisfied (POSIX-arm explicitly deferred) |
| ISOL-07 | 06 | passed | listed (06-02) | [x] (table: Complete) | satisfied |
| MULTI-01..04 | 07 | passed (6/6 static + 3/3 live) | body cites closure (no frontmatter `requirements-completed` field) | [x] (table: Complete) | satisfied |
| TOOLCFG-01..07 | 08 | passed (8/8 must-haves) | listed across 08-01..04 frontmatter (`requirements:` field) | [x] (table: Complete) | satisfied |
| OUTPUT-01..03 | 09 | **missing VERIFICATION.md** (W-1) | listed (09-03 frontmatter `requirements-completed`) | [x] (table: Complete) | satisfied (paper gap on verification doc only) |
| DOC-04..07 | 10 | passed (18/18) | body cites closure | [ ] (drift, fix) | satisfied |

**Total satisfied: 25/25.**
**Orphans:** None — every REQ-ID is verifiable to a phase artifact.

---

## Per-Phase Verification Summary

| Phase | Status | VERIFICATION.md | Score | Critical Gaps | Notes |
|-------|--------|-----------------|-------|---------------|-------|
| 06    | passed | 06-VERIFICATION.md | 5/5 SC, 7/7 reqs | None | 1 human-verify (POSIX arm — pre-accepted deferral) |
| 07    | passed | 07-VERIFICATION.md | 6/6 truths + 3 live | None | Initial status `human_needed`; gaps closed via re-verification |
| 08    | passed | 08-VERIFICATION.md | 8/8 must-haves | None | Clean pass |
| 09    | n/a    | **missing** | n/a (paper gap) | None (functionally) | Goal achieved via tests/test_reporter.py: 29 unit tests pass, 3 live subprocess tests deferred by sandbox 120s timeout. Recommendation W-1 below. |
| 10    | passed | VERIFICATION.md | 18/18 | None | Cross-document anchor + schema-drift guard fully in place |

---

## Cross-Phase Integration

(From integration checker — full report integrated below.)

| Boundary | Verdict | Evidence |
|----------|---------|----------|
| 06↔07 Discovery inherits isolation | PASS | `tests/conftest.py:78-83` calls `async with McpTestClient(...)`; `mcp_client.py:139-164` `__aenter__` builds tempdir + wires `_build_isolated_env`. Live ISOL-03 PASS at new spawn site (07-VERIFICATION.md re-run). |
| 07↔08 Parametrize + per-tool config keyed on tool name | PASS | `fixtures.py:404-418` `tool_config` lookups `config.tools.get(target_tool.name, ToolConfig())`. All 10 tests in `test_mcp_tool_contract.py` request both fixtures. |
| 08↔09 Skip → JUnit `[<tool>]` SUFFIX + per-tool summary | PASS | All 10 tests carry `pytest.skip(reason=tool_config.skip_reason or ...)`. Pytest's JUnit emitter writes `[<tool>]`-suffixed `<skipped>` elements; `_reporter.py:73-91` renders per-tool SKIP rows with em-dash. |
| 09↔10 README CI flag passthrough | PASS | `cli.py:130-140` `--junit-xml: Path \| None` Typer option → `_build_pytest_args` translates to `--junitxml=<path>`. README.md:198 uses literal flag. |
| 10↔08 Schema-drift guard | PASS | `tests/test_readme_snippets.py::test_readme_per_tool_fields_match_model` enforces both directions; deterministic on rename in either README or `models.py:101-159`. |

**End-to-end flow trace:** CLI → config load → pytest args → conftest discovery (isolated) → parametrize → `tool_config` per-test → `mcp_client.call_tool(name, args)` over isolated subprocess → pytest JUnit emit → `_reporter` per-tool summary → `dorny/test-reporter@v2` ingestion. Every link present and exercised by either AsyncMock proof, live verification, or subprocess regression test.

---

## Tech Debt (Non-Blocking)

### W-1: Author 09-VERIFICATION.md (paper gap)
Phase 09 is the only milestone phase without a VERIFICATION.md. Goal-achievement is evidenced by `tests/test_reporter.py` (29 passing unit tests pinning OUTPUT-01..03 contracts + 3 live subprocess tests deferred by environment), but the formal verification artifact is missing. Recommend a thin VERIFICATION.md citing the 09-0N-SUMMARY trio + regression suite + the [<tool>] SUFFIX contract pin.

### W-2: Capture deferred live evidence for Phase 09
The 3 live subprocess tests in `tests/test_reporter.py` are deferred per the 09-03-SUMMARY note. When CI or a developer host can run with `--timeout=600`, capture artifacts (results.xml slice + per-tool summary stdout) into a `raw/` evidence file referenced from the (forthcoming) 09-VERIFICATION.md.

### W-3: Phase 06 SC-4 EXTENDING.md half — env-var passthrough allowlist
Phase 06 SC-4 names both code comments AND EXTENDING.md. Code comments shipped (`_isolation.py:33-36`); the EXTENDING.md half was structurally deferred to Phase 10. Phase 10 added the per-tool walkthrough (DOC-07) but did not absorb the `_PASSTHROUGH_ALLOWLIST` warning from `_isolation.py`. Operators can still reference the in-source warning. Optional: backfill an "Extending the env passthrough allowlist" section in EXTENDING.md or accept as deferred (the in-source forward-reference is explicit).

### W-4: REQUIREMENTS.md traceability-table drift
The `## Traceability` table at REQUIREMENTS.md:88-114 shows "Pending" for ISOL-03, ISOL-06, DOC-04..07 despite each being marked `[x]` in the body. Fix the table status column to reflect actual completion. Also flip body checkboxes `[ ]` → `[x]` for DOC-04..07.

### W-5: ROADMAP wording — "mtimes" → "sha256" for ISOL-03 SC-1
Phase 06 D-09 strengthened ISOL-03 from mtime checks to sha256 byte-equality (catches same-second identical-content overwrites). ROADMAP success-criterion text still says "mtimes". Docs-side fix tracked in 06-03-SUMMARY.

### W-6: WR-04 — POSIX `USER` not in env passthrough allowlist
Cross-platform parity concern for future ISOL-06 POSIX run; informational only. `USERNAME` is diagnostic, no functional dependency for HOME redirect.

---

## Nyquist Compliance

Skipped — `workflow.nyquist_validation` is `false` in config.

---

## Recommendation

**Status:** `tech_debt` — proceed to milestone closeout after addressing the cluster of paper-only gaps (W-1 + W-4 are the cheapest wins; W-2/W-3/W-5/W-6 can be tracked into v1.2 if not addressed now).

The milestone IS complete in substance: every requirement is satisfied, all phase boundaries integrate cleanly, the user-visible E2E promise is delivered. The accumulated debt is documentation hygiene, not functional gaps. Either:

**A.** Address W-1 + W-4 now (estimated 15 min total — author 09-VERIFICATION.md from existing SUMMARY evidence + fix REQUIREMENTS.md table drift), then `/gsd-complete-milestone v1.1`.

**B.** Accept tech debt and proceed directly to `/gsd-complete-milestone v1.1` — track W-1..W-6 in backlog or v1.2 cleanup phase.
