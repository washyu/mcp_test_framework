# Milestone v1.0 — Requirements (Archive)

**Archived:** 2026-05-07 at v1.0 milestone close
**Status:** ✅ All 29 v1 requirements satisfied (3-source cross-reference per `.planning/milestones/v1.0-MILESTONE-AUDIT.md`)
**Original definition date:** 2026-05-04

This archive captures the final state of v1 requirements at milestone close. All checkboxes reflect verified completion (live green run, VERIFICATION.md tables, SUMMARY frontmatter cross-reference). The traceability table at the bottom is updated to "Complete" for all rows; the audit notes that 7 rows had stale "Pending" cells while the underlying evidence was already passed — this archive is the canonical post-close state.

**Core Value:** A `pytest`-runnable test suite that exercises one MCP tool end-to-end (schema → call → judge) and exits non-zero on any failure — proving the framework's integration contract before adding breadth. → **VALIDATED**: `uv run mcp-test-framework run` produces `67 passed, exit 0` against live `homelab-mcp` (via `uvx`) + Ollama (`qwen3.6:latest` at `127.0.0.1:11434`).

---

## v1 Requirements

### Project Setup

- [x] **SETUP-01**: Project uses `uv` for dependency management with a committed `uv.lock`; `uv sync` produces a clean install on a fresh checkout (Python 3.14)
- [x] **SETUP-02**: `pyproject.toml` configures `pytest-asyncio` with `asyncio_mode = "strict"` and `asyncio_default_fixture_loop_scope = "session"` to match the framework's session-scoped fixture pattern
- [x] **SETUP-03**: A lint rule (`ruff TID251` ban) prevents any code under `src/` and `tests/` from importing `homelab_mcp`, mechanically enforcing the black-box principle (belt-and-suspenders: `tests/conftest.py` `sys.modules` guard + dedicated banned-imports unit test)

### Core Modules

- [x] **CORE-01**: `config.py` + `models.py` load configuration with precedence `CLI flags > env vars > YAML > defaults` using `pydantic-settings[yaml]`; produces a frozen `Config` Pydantic model consumed by all fixtures
- [x] **CORE-02**: `schema_validator.py` performs the 7 deterministic structural checks from the spec (non-empty name, non-empty description, valid `inputSchema`, `type=object`, `required` ⊆ `properties`, every property documented, every property typed) using `jsonschema 4.26` with `Draft202012Validator`; returns a list of `ValidationIssue` records (`severity`, `path`, `message`)
- [x] **CORE-03**: `mcp_client.py` exposes an async `McpTestClient` wrapping the official `mcp` SDK's `stdio_client` + `ClientSession` with `list_tools`, `get_tool`, and `call_tool`; every SDK call wrapped in `asyncio.timeout()`; defensive handling of `CallToolResult.isError` and the `content` vs `structuredContent` shape variance
- [x] **CORE-04**: `ollama_judge.py` implements a `Judge` Protocol (defined in `judge_protocol.py`) by POSTing to Ollama `/api/chat` with `stream:false`, `format:json`, `temperature:0`, `think:false`, `num_predict:256`, `keep_alive:"30m"`; system prompt includes `/no_think` directive and a delimited subject block; response parser strips `<think>...</think>` blocks defensively before JSON parsing and validates the `JudgeResult` shape (`passed`, `score 1-5`, `reasoning`, `raw_response`)

### Test Cases — Schema Validation (Category 1, deterministic)

- [x] **TEST-01**: `test_target_tool_exists` — the configured `TARGET_TOOL_NAME` is present in the server's tool list
- [x] **TEST-02**: `test_tool_schema_is_structurally_valid` — `validate_tool_schema` returns no errors for the target tool
- [x] **TEST-03**: `test_tool_has_description` — the target tool's description is non-empty and at least 20 characters
- [x] **TEST-04**: `test_input_schema_properties_are_documented` — every input parameter has both a description and a type

### Test Cases — Description Quality (Category 2, LLM-judged, score >= 4)

- [x] **TEST-05**: `test_description_clarity` — the judge scores whether the description clearly explains what the tool does
- [x] **TEST-06**: `test_description_disambiguation` — the judge scores whether the description gives an LLM agent enough information to know when to call this tool versus a similarly-named alternative *(passes against `list_keyring_credentials` after the Phase 5 config switch; original `list_registered_servers` failed the rubric — framework working as designed; upstream description fix tracked for v2)*
- [x] **TEST-07**: `test_parameters_are_self_explanatory` — the judge scores whether each parameter description is clear enough to call the tool without external documentation

### Test Cases — Output Conformance (Category 3, deterministic)

- [x] **TEST-08**: `test_call_with_no_arguments_succeeds` — the target tool can be called with `{}` and returns a non-error result
- [x] **TEST-09**: `test_response_has_expected_shape` — response is a valid `CallToolResult` with at least one content block (non-empty `content` OR non-null `structuredContent`)
- [x] **TEST-10**: `test_response_content_is_parseable` — text content claiming to be JSON parses; structured content matches any declared output schema (validated via `Draft202012Validator`)

### Pytest Fixtures

- [x] **FIX-01**: Session-scoped `pytest-asyncio` fixtures (`config`, `mcp_client`, `judge`, `target_tool`) using `loop_scope="session"`; `mcp_client` owns the `stdio_client` + `ClientSession` lifecycle through `contextlib.AsyncExitStack` and an owner-task + `anyio.Event` driver (Phase 04.1 fix) so the cancel scope is entered and exited on the same task
- [x] **FIX-02**: A `_preflight` fixture verifies Ollama is reachable, the configured model is in `/api/tags`, and the MCP server command resolves on PATH — failing fast with a clear diagnostic if any pre-condition is unmet
- [x] **FIX-03**: The `target_tool` fixture fails the run early (not per-test) if the configured tool name is absent from the server's tool list

### CLI

- [x] **CLI-01**: `mcp-test-framework run [-k EXPRESSION] [-v] [--config PATH]` resolves config, invokes `pytest.main()` against the test directory, and exits with pytest's exit code (the CI/CD entry point)
- [x] **CLI-02**: `mcp-test-framework list-tools [--config PATH]` connects to the configured MCP server via stdio and prints all available tools with their descriptions — without invoking pytest or Ollama
- [x] **CLI-03**: `mcp-test-framework version` prints the package version

### Operational

- [x] **OPS-01**: Ollama timeouts and malformed JSON responses cause the affected test to fail with the raw response surfaced in the diagnostic; the run continues for other tests (does not crash the suite)
- [x] **OPS-02**: Subprocess (MCP server) and HTTP (Ollama) operations have explicit `asyncio.timeout()` / `httpx.Timeout(120, connect=10)` boundaries — no operation can hang indefinitely
- [x] **OPS-03**: KeyboardInterrupt at the CLI level cleanly tears down the MCP subprocess (no zombie `homelab-mcp.exe` on Windows) and exits with code 130 *(WR-05 fix added an explicit SIGINT handler; 05-UAT.md test 8 verified exit 130 + clean teardown live)*

### Documentation

- [x] **DOCS-01**: README in repo root explains setup (`uv sync`), configuration (env vars + YAML, with precedence rule), how to run tests, and Windows-specific troubleshooting; `docs/EXTENDING.md` covers rubric customization and judge backend swap recipes
- [x] **DOCS-02**: `.env.example` and `config.example.yaml` stub files enumerate every configurable setting with example values
- [x] **DOCS-03**: Judge failures surface the model's `raw_response` in the test failure output so debugging the rubric or response format does not require re-running the suite

## v2 Requirements (Carried Forward)

Deferred to a future milestone — not part of v1.0 scope.

### LLM-Generated Tests (the "Plant Seed")

- **GEN-01**: Judge model reads a tool's `inputSchema`, parameter descriptions, and declared output schema and generates diverse synthetic invocations (the natural next milestone)
- **GEN-02**: Generated invocations are stored as parameterized test cases that can be re-run deterministically

### Generalization

- **MULTI-01**: Multi-tool target selection (parameterize existing fixtures over a tool list)
- **MULTI-02**: Multi-server runs in a single invocation
- **TRANSPORT-01**: HTTP and SSE MCP transports
- **JUDGE-01**: Pluggable judge backends (OpenAI-compatible endpoints) using the v1 `Judge` Protocol — a 1-file addition (seam already shipped in v1.0)

### Quality / Output

- **OUT-01**: JSON / JUnit XML output formats for CI dashboards (one pytest flag away)
- **JUDGE-02**: Best-of-N judge consensus with majority vote / median (only if single-shot proves materially flaky in practice — not observed in v1.0)
- **CONFORM-01**: Generic conformance test pack parametrizable for any MCP server (the "pytest-mcp" value prop)
- **PROP-01**: Property-based / snapshot testing from `inputSchema`

## Out of Scope (validated at v1.0 close)

| Feature | Reason | Still valid? |
|---------|--------|--------------|
| Multiple MCP servers in one run | Generalization deferred until single-server contract is proven | ✓ Single-server contract proven; v2 candidate |
| Multiple tool targets in one run | Same — framework seams should make this easy to add later | ✓ Seams confirmed; v2 candidate |
| HTTP / SSE transports | stdio is sufficient to validate the integration contract | ✓ Validated |
| LLM-generated test inputs | Explicit Phase 2 ("Plant Seed"); judge-only for MVP | ✓ Plant-Seed forward |
| Best-of-N judge consensus | Single-shot with `score >= 4` for MVP | ✓ Adequate in practice; revisit only if flaky |
| Stateful / destructive tool testing | Read-only tools only — out of MVP scope | ✓ |
| Performance / load / security testing | Not the integration contract being validated | ✓ |
| JSON / JUnit / Allure output formats | pytest default terminal output is enough for local CLI | ✓ |
| Web UI / dashboard | CLI-only | ✓ |
| Pluggable judge backends (OpenAI etc.) | Ollama-only for MVP; `Judge` Protocol seam ships in MVP to make swap trivial | ✓ Seam shipped; v2 swap is 1-file |
| Reading / importing `homelab-mcp` source | Black-box subprocess under test — never imported | ✓ Mechanically enforced |
| Calibration dataset for judge | No human-labeled gold set in MVP | ✓ Future-work item |

## Traceability (Final)

All 29 v1 requirements satisfied at v1.0 close. Evidence triangulated across REQUIREMENTS.md (traceability), per-phase VERIFICATION.md tables, and per-plan SUMMARY frontmatter (`requirements-completed` / `requirements_addressed`). See `.planning/milestones/v1.0-MILESTONE-AUDIT.md` for the full coverage table and override register.

| Requirement | Phase | Status |
|-------------|-------|--------|
| SETUP-01 | Phase 1 | Complete |
| SETUP-02 | Phase 1 | Complete |
| SETUP-03 | Phase 1 | Complete |
| CORE-01 | Phase 1 | Complete |
| CORE-02 | Phase 1 | Complete |
| CORE-03 | Phase 2 (closed by 02.1) | Complete |
| CORE-04 | Phase 3 | Complete |
| TEST-01 | Phase 4 | Complete |
| TEST-02 | Phase 4 | Complete |
| TEST-03 | Phase 4 | Complete |
| TEST-04 | Phase 4 | Complete |
| TEST-05 | Phase 4 | Complete |
| TEST-06 | Phase 4 (target switched in Phase 5) | Complete |
| TEST-07 | Phase 4 | Complete |
| TEST-08 | Phase 4 | Complete |
| TEST-09 | Phase 4 | Complete |
| TEST-10 | Phase 4 | Complete |
| FIX-01 | Phase 4 (teardown fixed by 04.1) | Complete |
| FIX-02 | Phase 4 | Complete |
| FIX-03 | Phase 4 | Complete |
| CLI-01 | Phase 5 | Complete |
| CLI-02 | Phase 5 | Complete |
| CLI-03 | Phase 5 | Complete |
| OPS-01 | Phase 3 | Complete |
| OPS-02 | Phase 3 | Complete |
| OPS-03 | Phase 5 (WR-05 fix; UAT test 8) | Complete |
| DOCS-01 | Phase 5 | Complete |
| DOCS-02 | Phase 1 | Complete |
| DOCS-03 | Phase 3 | Complete |

**Coverage:**
- v1 requirements: 29 total
- Mapped + completed: 29 ✓
- Unmapped: 0
- Overrides accepted: 3 (all justified — see milestone audit; 1 superseded by WR-05 + UAT test 8)

---
*Requirements archived 2026-05-07 at v1.0 milestone close. The next milestone will define a fresh REQUIREMENTS.md via `/gsd-new-milestone`.*
