# Fixture for tests/unit/test_banned_imports.py.
#
# The .py.txt extension prevents pytest from collecting this as a test
# module AND prevents ruff from auto-scanning it during normal
# `ruff check src tests` runs (only the smoke test invokes ruff against
# it explicitly, after copying it to a real .py path under tmp_path).
#
# Do NOT rename this file to .py -- doing so would expose it to the
# repo-wide `uv run ruff check src tests` and break the build, since
# this file deliberately violates the ruff TID251 ban on homelab_mcp.
import homelab_mcp
